# Phase 06 deferred items

- **(found in 06-06, pre-existing)** `npx tsc -p packages/company-core --noEmit` reports 3 TS2345 errors in `packages/company-core/src/reducer.test.ts` (lines ~258, 348, 360): `phaseObservedEvent()`-style spreads typed as the `company.started` member. Test-only typing, vitest runs green; company-core has no typecheck script, so no gate catches it. Not touched by 06-06.
- **(found in 06-04, pre-existing)** `apps/api/src/routes/ws-browser.test.ts > GET /ceo/ws > first message is the decisions snapshot` failed once in 4 full-suite runs (passed alone and in the next 3 full runs). Its row-count bracket retries 5 times while other files write `ceo.*` rows in parallel; 06-04's ceo-reconcile.test.ts adds more concurrent `ceo.*` writes. Fix when it recurs: run that file with `--no-file-parallelism` or compare only rows it wrote. Not touched by 06-04.
