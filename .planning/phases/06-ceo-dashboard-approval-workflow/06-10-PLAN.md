---
phase: 06-ceo-dashboard-approval-workflow
plan: 10
type: execute
wave: 6
depends_on: ["06-09", "06-04"]
files_modified:
  - apps/web/src/ceo/ActionBar.tsx
  - apps/web/src/ceo/HistoryTable.tsx
  - apps/web/src/ceo/CeoApp.tsx
  - apps/web/src/ceo/api.ts
  - apps/web/src/ceo/view-model.ts
  - apps/web/src/ceo/view-model.test.ts
  - e2e/ceo-dashboard.spec.ts
autonomous: true
requirements: [CEO-03, CEO-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every pending decision has Approve (or 'Send answers' for questions), Request changes, More research, Discuss and Reject, and each POSTs to /ceo/api/decisions/:decisionId as JSON with the X-PixelFirm-CSRF header (CEO-03)"
    - "Approve on a gated call opens an alert dialog titled 'Approve this action?' that repeats the tool name and the same input pre, with body '{agentName} will run exactly this call. Nothing else is approved.', confirm 'Approve and run' and cancel 'Keep waiting', and focus starting on 'Keep waiting'; 'Send answers' and Reject submit with no dialog"
    - "'Send answers' stays disabled until every question has an answer"
    - "Request changes, More research and Discuss with an empty or whitespace note do not submit: focus moves to the note, which gets aria-invalid and the text 'Add a note. The agent needs to know what you want.'; Approve and Reject never require a note, and a typed note is sent with them"
    - "While submitting, all five buttons and the note are disabled and the clicked button shows a spinner and its in-progress label (Approving…, Sending…, Rejecting…, Requesting changes…, Requesting research…, Sending to discuss…)"
    - "On success the item leaves the queue, the next item is selected and a toast shows the action's success copy; on failure the buttons re-enable, the note is kept and an alert reads 'Decision not sent: {reason}. The agent is still waiting. Try again.'; nothing is marked decided before the server confirms"
    - "A 409, or a decision decided elsewhere or expired while it is open, swaps the detail pane to the matching no-longer-pending alert ('This decision was already made by {decider} {relativeTime}.' or the expired copy) and removes the action buttons for that item"
    - "History lists the latest 50 decided and expired requests newest first with When (absolute, relative in title), Agent, Decision, Action badge, Decided by and Note; Expired rows that are still resumable show 'Resume task', which POSTs /ceo/api/tasks/:taskId/resume and toasts 'Task resumed. {agentName} will ask again.' (CEO-05, D-02)"
    - "While history loads the table shows 5 skeleton rows; a failure shows the loading-error alert with Retry; with nothing yet it shows 'No decisions yet' and 'Every approval, rejection and expired request will be listed here.'"
    - "Losing the live feed changes the status word but leaves the action buttons enabled, because decisions POST over HTTP"
  artifacts:
    - path: apps/web/src/ceo/ActionBar.tsx
      provides: "Note, five actions, approve dialog, submit states"
      exports: ["ActionBar"]
    - path: apps/web/src/ceo/HistoryTable.tsx
      provides: "Audit history table + Resume task"
      exports: ["HistoryTable"]
    - path: apps/web/src/ceo/api.ts
      provides: "CSRF-headed POSTs"
      exports: ["fetchMe", "postDecision", "postResume"]
  key_links:
    - from: "apps/web/src/ceo/ActionBar.tsx"
      to: "POST /ceo/api/decisions/:decisionId (06-13/06-05)"
      via: "api.ts postDecision with Content-Type application/json and X-PixelFirm-CSRF: 1"
      pattern: "X-PixelFirm-CSRF"
    - from: "apps/web/src/ceo/HistoryTable.tsx Resume task"
      to: "POST /ceo/api/tasks/:taskId/resume (06-04)"
      via: "api.ts postResume; button shown only when isResumable"
      pattern: "postResume\\("
  prohibitions:
    - statement: "MUST NOT let a keyboard shortcut or the Enter key trigger any decision: every action needs a pointer click or Tab-and-activate on a labelled button, and Enter in the note inserts a newline"
      status: resolved
      verification: test
---

<objective>
CEO-03 and the visible half of CEO-05 on the dashboard: the five actions with the required-note rule
(D-07), the approve confirmation for gated calls, honest submitting/success/failure/409 states, and the
History tab over the audit events, with Resume for expired tasks (D-02).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-09-SUMMARY.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-04-SUMMARY.md

Tooling: `pnpm` blocked by Device Guard: use `npx --yes pnpm@12.4.2`. Every behaviour is asserted in
Playwright first (browser-test before human UAT).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the action bar — five actions, required notes, approve confirmation, submit states (CEO-03, D-05, D-07)</name>

  <read_first>
    - apps/web/src/ceo/CeoApp.tsx, DetailPane.tsx, QuestionsForm.tsx, view-model.ts, api.ts, e2e/ceo-dashboard.spec.ts (as left by 06-09)
    - apps/api/src/routes/ceo.ts (body schema, 202/400/404/409/503 responses)
    - apps/web/src/components/ui/alert-dialog.tsx, button.tsx, textarea.tsx, field.tsx, spinner.tsx, sonner.tsx, alert.tsx
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md detail item 9 "Action bar", "Approve confirmation", "Submitting", "Live updates", "Copywriting Contract"
  </read_first>

  <files>apps/web/src/ceo/ActionBar.tsx, apps/web/src/ceo/api.ts, apps/web/src/ceo/view-model.ts, apps/web/src/ceo/view-model.test.ts, apps/web/src/ceo/CeoApp.tsx, e2e/ceo-dashboard.spec.ts</files>

  <behavior>
    - view-model: noteRequired("request_changes" | "more_research" | "discuss") is true, for "approve" | "reject" false; validateNote returns the error copy for "   " on a required action and null otherwise; inProgressLabel and successToast return the UI-SPEC copy per action (toast for question answers: "Answers sent to {agentName}.").
    - e2e: clicking Request changes with an empty note sends no request (page.route call count 0), focuses the textarea, sets aria-invalid="true" and shows the required-note copy; with a note it POSTs { action: "request_changes", note } with headers content-type application/json and x-pixelfirm-csrf "1".
    - e2e: Approve on a gated item opens the dialog with the UI-SPEC title/body/buttons and the same tool-input text; focus is on "Keep waiting"; "Keep waiting" closes it and sends nothing; "Approve and run" POSTs { action: "approve" }.
    - e2e: on a question item "Send answers" is disabled until both questions are answered, then POSTs { action: "approve", answers } with no dialog.
    - e2e: Reject POSTs immediately with no dialog.
    - e2e: during a delayed 202 all five buttons and the textarea are disabled and the clicked button reads its in-progress label; after the 202 and the live ceo.decision_made the item leaves, the next is selected and the toast copy appears.
    - e2e: a 503 response keeps the item and the typed note, re-enables the buttons and shows "Decision not sent: worker offline. The agent is still waiting. Try again."
    - e2e: a 409 response shows the no-longer-pending alert and removes the buttons for that item; a live ceo.approval_expired for the selected item shows the expired copy.
    - e2e: pressing Enter in the note inserts a newline and sends nothing; pressing Enter/Space while focus is on the textarea or the page body sends nothing.
    - e2e: after the fake /ceo/ws closes, the status reads Reconnecting and the action buttons stay enabled.
  </behavior>

  <action>
RED first. api.ts: postDecision(decisionId, body) and postResume(taskId) using fetch with method POST, headers { "Content-Type": "application/json", "X-PixelFirm-CSRF": "1" }, same-origin credentials, JSON body; return { ok: true } or { ok: false, status, reason } where reason is the server's error string (for example "worker offline").

ActionBar.tsx (sticky at the bottom of the detail pane, --card background, top border, 16px padding): the "Note to agent" textarea (3 rows, auto-grow to 8, helper text and placeholder from the UI-SPEC), then Approve (default/accent variant, labelled "Send answers" for questions and disabled until allAnswered), Request changes, More research, Discuss (outline), and Reject (destructive rendered as outline, right-aligned). Buttons are 36px tall at >= 768px and 44px below, wrapping to two rows with Approve full-width first. Required notes (D-07): the three note actions stay enabled; with an empty or whitespace note they focus the textarea and show the error instead of submitting. Approve on a ceo_gated_tool item opens the shadcn alert-dialog (UI-SPEC copy, same mono pre of the tool input, focus on Cancel, focus returns to Approve on close); question answers and Reject submit directly. No keyboard shortcut is registered for any action and the textarea never submits on Enter (the prohibition above). Submitting disables everything and shows the spinner plus inProgressLabel; success removes the item (the live decision_made also does, whichever comes first), selects the next with nextSelection and toasts successToast via sonner; failure keeps the note and shows the submit-error alert; 409 or a live decided/expired event for the open item shows the no-longer-pending alert (decider + Intl.RelativeTimeFormat time, or the expired copy) and hides the buttons for that item.
  </action>

  <verify>
    <automated>pnpm --filter web test -- view-model</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>npx playwright test e2e/ceo-dashboard.spec.ts</automated>
    <fails_when>non-zero exit or "0 passed"</fails_when>
    <automated>pnpm --filter web typecheck && pnpm --filter web build && node scripts/check-office-bundle.mjs</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>

  <acceptance_criteria>
    - The e2e spec asserts the intercepted request count is 0 after an empty-note Request changes click and after pressing Enter in the note
    - The e2e spec asserts the POST body equals `{ action: "approve", answers: { ... } }` for Send answers and the request header "x-pixelfirm-csrf" equals "1"
    - The e2e spec asserts `page.locator(":focus")` has the accessible name "Keep waiting" when the approve dialog opens
  </acceptance_criteria>

  <done>The CEO can take any of the five actions on any pending decision, with the same guardrails the server enforces and honest feedback for every outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: History — the audit log on screen, and Resume task for expired requests (CEO-05, D-02)</name>

  <read_first>
    - packages/company-core/src/decisions.ts (decisionHistory, isResumable)
    - apps/web/src/ceo/CeoApp.tsx (Tabs), apps/web/src/components/ui/table.tsx, badge.tsx, skeleton.tsx, empty.tsx, button.tsx
    - apps/api/src/routes/ceo.ts resume route (06-04: 202/404/409/503)
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "History tab" and "UI Considerations" (History loading/error/empty)
  </read_first>

  <files>apps/web/src/ceo/HistoryTable.tsx, apps/web/src/ceo/CeoApp.tsx, apps/web/src/ceo/view-model.ts, apps/web/src/ceo/view-model.test.ts, e2e/ceo-dashboard.spec.ts</files>

  <behavior>
    - view-model: historyBadge maps approve -> "Approved" (success outline), reject -> "Rejected" (destructive), request_changes -> "Changes requested", more_research -> "More research", discuss -> "Discuss" (neutral outline), expired -> "Expired" (muted, dashed border).
    - e2e: a snapshot with 60 closed records shows 50 rows newest first; each row shows the absolute local time with the relative time in its title attribute, the agent, the decision title truncated to 1 line, the action badge, the decider email and the note truncated with the full note in title.
    - e2e: a resumable expired row shows "Resume task"; clicking it POSTs /ceo/api/tasks/:taskId/resume with the CSRF header and shows "Task resumed. {agentName} will ask again."; a live ceo.task_resume_requested removes the button; a non-resumable expired row has no button.
    - e2e: before the first snapshot the History tab shows 5 skeleton rows; with no closed records it shows the history empty copy; a feed failure before any snapshot shows the loading-error alert with Retry.
    - e2e: the Pending tab label reads "Pending (N)" and switching tabs keeps the current selection and draft note.
  </behavior>

  <action>
RED first. Add a History tab next to Pending in CeoApp. HistoryTable.tsx renders decisionHistory(state, 50) in a shadcn table with the six UI-SPEC columns (When: absolute local time via toLocaleString, relative via Intl.RelativeTimeFormat in the title attribute; Decision and Note truncate to one line with the full text in title), historyBadge for the Action column, and an outline "Resume task" button only where isResumable(state, record) is true; it calls postResume and toasts the resume copy (success) or shows the submit-error alert pattern (failure). History is read-only apart from Resume. Loading, empty and error states per the UI-SPEC considerations table.
  </action>

  <verify>
    <automated>pnpm --filter web test -- view-model</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>npx playwright test e2e/ceo-dashboard.spec.ts e2e/office-disconnect.spec.ts</automated>
    <fails_when>non-zero exit or "0 passed"</fails_when>
    <automated>pnpm --filter web typecheck && pnpm --filter web build && node scripts/check-office-bundle.mjs</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>

  <acceptance_criteria>
    - The e2e spec asserts the History row count equals 50 for the 60-record fixture and the first row is the newest
    - The e2e spec asserts the resume POST URL ends with `/ceo/api/tasks/<taskId>/resume` and the toast text equals "Task resumed. <agent> will ask again."
  </acceptance_criteria>

  <done>Every approval, rejection and expiry is visible in History, and a task blocked by a worker restart can be resumed from there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard -> /ceo/api | state-changing requests from the CEO's browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10-01 | Tampering | CSRF | high | mitigate | JSON + X-PixelFirm-CSRF on every POST, matched by the server's requireCsrf; same-origin only |
| T-06-10-02 | Elevation | accidental approval | high | mitigate | confirmation dialog for gated calls with focus on "Keep waiting"; no keyboard shortcuts; e2e tests |
| T-06-10-03 | Repudiation | client-side "decided" state | medium | mitigate | nothing is marked decided before the server's 202 or the live ceo.decision_made |
| T-06-10-04 | Tampering | approving edited input | high | mitigate | the dashboard never sends tool input; the worker approves only the parked call (06-01) |
</threat_model>

<verification>
- `pnpm --filter web test`, `pnpm --filter web typecheck`, `pnpm --filter web build && node scripts/check-office-bundle.mjs`, and `npx playwright test e2e/ceo-dashboard.spec.ts e2e/office-disconnect.spec.ts` green.
</verification>

<success_criteria>
- CEO-03 and CEO-05 are usable end to end from /ceo against the fake feed; 06-11 repeats them against a real session.
</success_criteria>

## Artifacts this phase produces

- `apps/web/src/ceo/ActionBar.tsx`: `ActionBar`
- `apps/web/src/ceo/HistoryTable.tsx`: `HistoryTable`
- `apps/web/src/ceo/api.ts`: `postDecision`, `postResume`
- `apps/web/src/ceo/view-model.ts`: `noteRequired`, `validateNote`, `inProgressLabel`, `successToast`, `historyBadge`

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-10-SUMMARY.md` when done
</output>
