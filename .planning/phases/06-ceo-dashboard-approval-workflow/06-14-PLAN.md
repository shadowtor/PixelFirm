---
phase: 06-ceo-dashboard-approval-workflow
plan: 14
type: execute
wave: 4
depends_on: ["06-08", "06-05", "06-06"]
files_modified:
  - apps/web/src/ceo/api.ts
  - apps/web/src/ceo/ceo-feed.ts
  - apps/web/src/ceo/ceo-feed.test.ts
  - apps/web/src/ceo/CeoApp.tsx
  - apps/web/src/ceo/QueueList.tsx
  - apps/web/src/ceo/view-model.ts
  - apps/web/src/ceo/view-model.test.ts
  - e2e/ceo-dashboard.spec.ts
autonomous: true
requirements: [CEO-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "While decisions load, the queue shows 3 skeleton rows and the detail pane a title-plus-3-block skeleton; no empty-state copy renders before the first snapshot"
    - "Zero pending decisions renders the empty component with 'No decisions waiting' and 'When an agent needs your call, it walks to your office and the request appears here.', and the pending badge and title count are hidden"
    - "A load failure renders 'Couldn't load decisions: {reason}. Check the API is running, then retry.' with a Retry button; a 401/403 from /ceo/api/me renders 'You're not signed in as the CEO. This page is behind Cloudflare Access. Reload to sign in again.' and never the queue"
    - "The header shows 'CEO desk', the accent pending-count badge (hidden at 0), a Kibo Status reading Live, Reconnecting or Offline, and the verified decider email; document.title is '(N) CEO desk · PixelFirm' while N > 0, otherwise 'CEO desk · PixelFirm'"
    - "With the dev bypass active the page shows 'Dev auth bypass is on. Decisions are recorded as {devIdentity}.'"
    - "The pending queue lists one button per thread, oldest first, each showing agent · kind badge ('Question' or 'Gated action · <pattern>') · waited time (accent after 10 minutes), the title clamped to 2 lines, and a 'Round n' chip for later Discuss rounds; the first item is auto-selected, aria-current marks the selection, and the next item is selected when the selected one leaves"
    - "A new decision arriving appends to the queue and is announced through an aria-live polite region as 'New decision from {agentName}: {title}' without changing the selection"
    - "The queue column scrolls on its own while the header, pending badge and tabs stay fixed"
    - "Loading the office route `/` requests nothing under /src/ceo/ (D-08)"
  artifacts:
    - path: apps/web/src/ceo/CeoApp.tsx
      provides: "Dashboard shell, auth gate, header, pending queue"
      exports: ["CeoApp"]
    - path: apps/web/src/ceo/ceo-feed.ts
      provides: "Same-origin /ceo/ws client with reconnect + status"
      exports: ["connectCeoFeed"]
    - path: apps/web/src/ceo/QueueList.tsx
      provides: "Pending queue list"
      exports: ["QueueList"]
    - path: apps/web/src/ceo/view-model.ts
      provides: "Pure display helpers"
      exports: ["waitedLabel", "isLongWait", "kindLabel", "documentTitle", "nextSelection"]
    - path: apps/web/src/ceo/api.ts
      provides: "Same-origin fetch of /ceo/api/me"
      exports: ["fetchMe"]
    - path: e2e/ceo-dashboard.spec.ts
      provides: "Browser-level proof of every visible dashboard behaviour"
  key_links:
    - from: "apps/web/src/ceo/ceo-feed.ts"
      to: "packages/company-core/src/decisions.ts applyDecisionEvent"
      via: "snapshot state replaced, each live event applied with the same step the server folds with"
      pattern: "applyDecisionEvent\\("
    - from: "apps/web/src/ceo/api.ts"
      to: "GET /ceo/api/me (06-05)"
      via: "same-origin fetch; 401/403 drives the auth-failure state"
      pattern: "/ceo/api/me"
---

<objective>
Fill the `/ceo` scaffold from 06-08 with what the CEO actually uses (CEO-02): the auth-aware shell
(header, live status, decider email, dev-bypass banner, loading/empty/error/auth states), the live
feed from `/ceo/ws`, and the pending queue. Split out of 06-08 so the CLI-generated scaffold and the
hand-written dashboard each stay inside the context budget. Detail sections come in 06-09, actions
and history in 06-10.

Output: `api.ts`, `ceo-feed.ts`, `view-model.ts`, `QueueList.tsx`, the completed `CeoApp.tsx`, and
`e2e/ceo-dashboard.spec.ts`, which 06-09 and 06-10 extend.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-08-SUMMARY.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-06-SUMMARY.md

Tooling: `pnpm` blocked by Device Guard: use `npx --yes pnpm@12.4.2`. The e2e spec spawns Vite the
same way e2e/office-disconnect.spec.ts does; if the spawned `pnpm` is blocked, spawn
`npx --yes pnpm@12.4.2` instead and record the deviation.
Browser-test before human UAT (global CLAUDE.md): every visible behaviour here is asserted in
Playwright before any human check.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: opening /ceo shows who is signed in, whether the feed is live, and the right loading, empty, error and auth states</name>

  <read_first>
    - apps/web/src/ceo/CeoApp.tsx and apps/web/src/main.tsx (as left by 06-08)
    - apps/web/src/ws-client.ts (message parsing + CompanyEventSchema re-validation to mirror)
    - packages/company-core/src/decisions.ts (06-06: applyDecisionEvent, pendingQueue, DecisionsState, foldDecisions)
    - apps/api/src/routes/ceo.ts (GET /ceo/api/me shape from 06-05)
    - e2e/office-disconnect.spec.ts (spawn Vite, poll, routeWebSocket)
    - apps/web/src/components/ui/alert.tsx, skeleton.tsx, empty.tsx, tabs.tsx, badge.tsx and apps/web/src/components/kibo-ui/status/index.tsx
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "Dashboard Layout & Interaction Contract" (header, tabs, loading/empty/error states, breakpoints), "Copywriting Contract", "UI Considerations"
  </read_first>

  <files>apps/web/src/ceo/api.ts, apps/web/src/ceo/ceo-feed.ts, apps/web/src/ceo/ceo-feed.test.ts, apps/web/src/ceo/CeoApp.tsx, e2e/ceo-dashboard.spec.ts</files>

  <behavior>
    - ceo-feed: a snapshot frame replaces state; an event frame that fails CompanyEventSchema is dropped; a valid one is applied with applyDecisionEvent; status goes Live on open, Reconnecting on close, Offline after 30 s without reconnecting; reconnect uses 1 s doubling to 30 s; the socket URL is `ws(s)://<location.host>/ceo/ws` with no query string.
    - e2e: me -> 401 shows the auth-failure copy and no queue element; me -> { devBypass: true } shows the dev-bypass banner; before the first snapshot the queue shows 3 skeleton rows and no empty-state copy; an empty snapshot shows "No decisions waiting" with its body copy and no pending badge; a feed that fails before any snapshot shows the loading-error alert with a Retry button; the header shows "CEO desk", the feed status word and the decider email; loading `/` makes no request under /src/ceo/.
  </behavior>

  <action>
RED first for ceo-feed.test.ts; write the e2e spec before the components.

api.ts: fetchMe() -> { ok: true, email, devBypass } | { ok: false, status }; postDecision and postResume are added in 06-10. Requests are same-origin fetches (the Cloudflare Access cookie rides along) and never carry a token in the URL.

ceo-feed.ts: connectCeoFeed({ onSnapshot, onEvent, onStatus }) opens `ws(s)://<location.host>/ceo/ws` (no query string), parses frames like ws-client.ts, re-validates events with CompanyEventSchema, reconnects with backoff, and reports Live / Reconnecting / Offline. It returns { close() }.

CeoApp.tsx (D-08: this page only ever renders on /ceo): fetch me first. 401/403 -> the auth-failure copy only. Other failure -> the loading-error alert with Retry. Success -> header (56px, --card, bottom border): h1 "CEO desk", accent pending badge hidden at 0, Kibo Status with the feed status word, the decider email in muted 12px; the dev-bypass alert when devBypass; Tabs with "Pending (N)" (History arrives in 06-10). Keep DecisionsState in state; snapshot replaces it, live events go through applyDecisionEvent; derive the pending list with pendingQueue. Until the first snapshot show 3 skeleton rows in the queue and the title-plus-3-block skeleton in the detail pane; if the feed fails before any snapshot show the loading-error alert with Retry; an empty pending list shows the empty component with the UI-SPEC copy. Two panes per the UI-SPEC breakpoints (360px queue at >= 1024px, 300px at 768-1023px, single column below 768px with "Back to queue"). Task 2 fills the queue column.

e2e/ceo-dashboard.spec.ts: spawn Vite on port 5198 as office-disconnect.spec.ts does; fake GET /ceo/api/me with page.route and /ceo/ws with page.routeWebSocket, sending snapshots built by importing foldDecisions from "../packages/company-core/src/decisions" (type-only imports there, so it loads in the Playwright runner) over fixture CompanyEvents.
  </action>

  <verify>
    <automated>pnpm --filter web test -- ceo-feed</automated>
    <fails_when>non-zero exit, or "No test files found"</fails_when>
    <automated>npx playwright test e2e/ceo-dashboard.spec.ts</automated>
    <fails_when>non-zero exit, "0 passed", or "vite dev server did not start"</fails_when>
    <automated>pnpm --filter web typecheck && pnpm --filter web build && node scripts/check-office-bundle.mjs</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>

  <acceptance_criteria>
    - The e2e spec has one test per e2e behaviour bullet above and asserts copy by exact text for the auth-failure, empty-state and dev-bypass strings
    - The e2e spec records every request made while loading `/` and asserts none has a path starting with `/src/ceo/`
    - ceo-feed.test.ts asserts the status sequence Live, Reconnecting, Offline with fake timers
  </acceptance_criteria>

  <done>Opening /ceo shows who is signed in and whether the feed is live, with correct loading, empty, error and auth states, and the office route still loads nothing from the dashboard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the live pending queue, FIFO with selection, arrivals and title count (CEO-02)</name>

  <read_first>
    - apps/web/src/ceo/CeoApp.tsx and e2e/ceo-dashboard.spec.ts (as left by Task 1)
    - packages/company-core/src/decisions.ts (PendingItem fields: agentId, kind, reason, title, requestedAt, round, worktreePath)
    - apps/web/src/components/ui/scroll-area.tsx and badge.tsx
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "Dashboard Layout & Interaction Contract" (queue item, kind badge, live updates, accessibility)
  </read_first>

  <files>apps/web/src/ceo/view-model.ts, apps/web/src/ceo/view-model.test.ts, apps/web/src/ceo/QueueList.tsx, apps/web/src/ceo/CeoApp.tsx, e2e/ceo-dashboard.spec.ts</files>

  <behavior>
    - view-model: waitedLabel(requestedAt, now) gives "12 min" for 12 minutes (Intl unit formatting) and isLongWait is true only above 10 minutes; kindLabel gives "Question" for clarifying_question and, for ceo_gated_tool, "Gated action · " followed by the text after the first ": " in the reason: "Gated action · force-push" for "Bash command matched a CEO-gated pattern: force-push", "Gated action · mcp__coolify__deploy" for "MCP tool: mcp__coolify__deploy", "Gated action · production config" for "File change matched a CEO-gated pattern: production config" (the 06-02 path-gate format), and "Gated action" alone for a reason with no ": "; documentTitle(3) is "(3) CEO desk · PixelFirm" and documentTitle(0) is "CEO desk · PixelFirm"; nextSelection picks the next item after the removed one, else the previous, else none.
    - e2e: a snapshot of three pending decisions (one a Round 2 Discuss thread) lists them oldest first with their kind labels and the Round 2 chip, auto-selects the first (aria-current="true"), shows the pending badge "3", and sets document.title to "(3) CEO desk · PixelFirm"; a live ceo.approval_requested appends a fourth item, fills the aria-live region with "New decision from {agent}: {title}", and leaves the selection unchanged; a live ceo.decision_made for the selected item selects the next one; the queue column is its own scroll container; the detail pane shows the selected title as the h2 and the meta line.
  </behavior>

  <action>
RED first for view-model.test.ts, then the e2e cases, then the components.

view-model.ts: implement the five helpers above as pure functions (no React import).

QueueList.tsx: a list of `<button>` items, aria-current="true" on the selected one, line 1 muted 12px (agent · kind badge · waited time, accent when isLongWait), line 2 the title with line-clamp-2, optional "Round n" chip; first item auto-selected; when the selected item leaves, nextSelection decides; a new arrival never steals the selection. Agent-supplied text renders only as React text (never HTML).

CeoApp.tsx: render QueueList in the queue column (its own scroll container; header, pending badge and tabs stay fixed); show the accent pending badge and "Pending (N)" from the pending count; document.title follows documentTitle(pending count); an aria-live="polite" region announces new arrivals as "New decision from {agentName}: {title}". Detail pane in this plan: the title as the h2 heading and the meta line `{agent} · {task title} · {project} · waiting {duration} · #{first 8 of decisionId}` (project = last segment of worktreePath; absent segments are omitted); 06-09 adds the sections.
  </action>

  <verify>
    <automated>pnpm --filter web test -- view-model ceo-feed</automated>
    <fails_when>non-zero exit, or "No test files found"</fails_when>
    <automated>npx playwright test e2e/ceo-dashboard.spec.ts</automated>
    <fails_when>non-zero exit, "0 passed", or "vite dev server did not start"</fails_when>
    <automated>pnpm --filter web typecheck && pnpm --filter web build && node scripts/check-office-bundle.mjs</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>

  <acceptance_criteria>
    - The e2e spec asserts `document.title` equals "(3) CEO desk · PixelFirm" with three pending items
    - view-model.test.ts asserts `kindLabel` outputs by exact string for all three gated reason formats and the no-colon fallback
    - The e2e spec asserts the aria-live region text by exact string and that aria-current stays on the first item after the fourth arrives
  </acceptance_criteria>

  <done>The CEO sees every pending decision in FIFO order, live, with the selection, arrival announcements and title count behaving as the UI-SPEC locks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /ceo/ws frames -> React | agent-authored text is untrusted |
| browser -> /ceo/api | same-origin requests behind Cloudflare Access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14-01 | Tampering | XSS via agent text | high | mitigate | React text nodes only; no dangerouslySetInnerHTML; canon XSS review via /gsd-secure-phase |
| T-06-14-02 | Info disclosure | dashboard code on the office route | high | mitigate | e2e asserts `/` requests nothing under /src/ceo/; 06-08's build-manifest check re-run in every verify |
| T-06-14-03 | Info disclosure | token in URL | medium | mitigate | /ceo/ws and /ceo/api use same-origin cookie/JWT, no query token; unit test on the feed URL |
| T-06-14-04 | Tampering | forged feed frames | low | mitigate | CompanyEventSchema re-validation of every live event |
</threat_model>

<verification>
- `pnpm --filter web test`, `pnpm --filter web typecheck`, `pnpm --filter web build && node scripts/check-office-bundle.mjs`, `npx playwright test e2e/ceo-dashboard.spec.ts e2e/office-disconnect.spec.ts` green (the office spec proves the office route still behaves).
</verification>

<success_criteria>
- The CEO can open /ceo and see the live pending queue with correct loading, empty, error and auth states; the office route stays free of dashboard code and styles.
</success_criteria>

## Artifacts this phase produces

- `apps/web/src/ceo/CeoApp.tsx`: `CeoApp` (shell, auth gate, header, queue column, detail heading)
- `apps/web/src/ceo/QueueList.tsx`: `QueueList`
- `apps/web/src/ceo/ceo-feed.ts`: `connectCeoFeed`
- `apps/web/src/ceo/api.ts`: `fetchMe`
- `apps/web/src/ceo/view-model.ts`: `waitedLabel`, `isLongWait`, `kindLabel`, `documentTitle`, `nextSelection`
- `e2e/ceo-dashboard.spec.ts`

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-14-SUMMARY.md` when done
</output>
