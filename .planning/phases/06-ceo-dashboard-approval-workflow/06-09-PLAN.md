---
phase: 06-ceo-dashboard-approval-workflow
plan: 09
type: execute
wave: 5
depends_on: ["06-14"]
files_modified:
  - apps/web/src/ceo/DetailPane.tsx
  - apps/web/src/ceo/DiffView.tsx
  - apps/web/src/ceo/QuestionsForm.tsx
  - apps/web/src/ceo/view-model.ts
  - apps/web/src/ceo/view-model.test.ts
  - apps/web/src/ceo/CeoApp.tsx
  - e2e/ceo-dashboard.spec.ts
autonomous: true
requirements: [CEO-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The detail pane shows, top to bottom with 24px separators: the title (never truncated) and meta line; 'Runs on approve' with the tool name badge and the exact parked tool input in a mono pre (gated calls only); the questions (AskUserQuestion only); Context as plain text; Recommendation; Links; Changes; and 'Earlier in this thread' for Discuss threads (CEO-02)"
    - "A gated call renders 'Runs on approve' with exactly the toolInput the runtime parked, and a question renders one Choicebox (single select) or checkbox-card group (multiSelect) per question, each with an 'Other (write your own answer)' option that reveals a textarea"
    - "A missing recommendation shows 'The agent did not give a recommendation.'; links are hidden when there are none; a missing diff shows 'No file changes attached to this request.'"
    - "Only http: and https: links become anchors, with target _blank and rel 'noopener noreferrer'; any other scheme renders as plain mono text"
    - "Changes lists one collapsible row per file (path in mono truncated with a title attribute, +added in success colour, -removed in error text), all collapsed except the first when there are more than 3 files; the expanded body is the unified diff with +/- line tints; a truncated diff shows 'Diff cut at {lineCap} lines. {fileCount} files changed, {added} additions, {removed} deletions in total. Open the branch for the rest.'"
    - "The tool-input pre caps at 240px and each diff body at 480px, each scrolling inside with pre-wrap and overflow-wrap anywhere, so a long command never scrolls the page sideways"
    - "Question headers, question text, option labels and descriptions wrap in full and are never truncated or clamped; an option's preview renders in a mono pre under the selected card"
    - "File paths truncate with the full path in the title attribute; the detail title is never truncated"
    - "Agent-supplied text (title, context, recommendation, tool input, question text, previews, diff) renders only as text nodes, never HTML or markdown"
  artifacts:
    - path: apps/web/src/ceo/DetailPane.tsx
      provides: "Detail sections 1-8"
      exports: ["DetailPane"]
    - path: apps/web/src/ceo/DiffView.tsx
      provides: "Per-file collapsible unified diff"
      exports: ["DiffView"]
    - path: apps/web/src/ceo/QuestionsForm.tsx
      provides: "Choicebox / checkbox cards / Other answer inputs"
      exports: ["QuestionsForm"]
  key_links:
    - from: "apps/web/src/ceo/QuestionsForm.tsx"
      to: "apps/web/src/ceo/view-model.ts buildAnswers"
      via: "selections -> answers keyed by exact question text; allAnswered gates Send answers (06-10)"
      pattern: "buildAnswers\\("
    - from: "apps/web/src/ceo/DetailPane.tsx links section"
      to: "apps/web/src/ceo/view-model.ts safeLink"
      via: "only http/https produce an anchor"
      pattern: "safeLink\\("
---

<objective>
Complete CEO-02 on the dashboard: every pending decision shows its title, context, the requesting
agent's recommendation, links and diff, plus the exact call that runs on approve and, for
AskUserQuestion, the questions ready to answer. All of it follows 06-UI-SPEC's detail-pane contract,
and agent text never becomes markup.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-14-SUMMARY.md

Tooling: `pnpm` blocked by Device Guard: use `npx --yes pnpm@12.4.2`. Browser-test before any human
check: all behaviours below are asserted in e2e/ceo-dashboard.spec.ts.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detail sections — runs-on-approve, context, recommendation, links, diff, earlier rounds</name>

  <read_first>
    - apps/web/src/ceo/CeoApp.tsx, apps/web/src/ceo/view-model.ts, e2e/ceo-dashboard.spec.ts (as left by 06-14)
    - packages/company-core/src/decisions.ts (DecisionRecord / PendingItem fields: toolName, toolInput, context, recommendation, links, diff, earlier)
    - apps/web/src/components/ui/collapsible.tsx, alert.tsx, badge.tsx, separator.tsx, scroll-area.tsx
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "Detail pane, top to bottom" items 1-2 and 4-8, "Typography", "Color", "Spacing Scale", "Copywriting Contract"
  </read_first>

  <files>apps/web/src/ceo/DetailPane.tsx, apps/web/src/ceo/DiffView.tsx, apps/web/src/ceo/view-model.ts, apps/web/src/ceo/view-model.test.ts, apps/web/src/ceo/CeoApp.tsx, e2e/ceo-dashboard.spec.ts</files>

  <behavior>
    - safeLink("https://a.test/x") -> { href }; safeLink("http://b.test") -> { href }; safeLink("javascript:alert(1)"), safeLink("file:///etc/passwd"), safeLink("not a url") -> { text } only.
    - splitDiffByFile(unified) returns one chunk per "diff --git" header keyed by the b/ path, preserving line order; diffLineKind classifies "+x" add, "-x" del, "@@" hunk, "+++"/"---" file header, anything else context.
    - truncationCopy({ lineCap: 400, files: 7, added: 900, removed: 12 }) equals "Diff cut at 400 lines. 7 files changed, 900 additions, 12 deletions in total. Open the branch for the rest."
    - e2e: a gated item shows the heading "Runs on approve", the tool badge "Bash" and a pre whose text equals the fixture toolInput; a question item has no "Runs on approve".
    - e2e: context renders the fixture text including a literal "<b>x</b>" as text (no b element); recommendation empty copy shows when absent; a links fixture with one https, one http and one javascript: URL yields exactly two anchors, both target="_blank" rel="noopener noreferrer", and the javascript: string as plain text.
    - e2e: a 5-file diff shows 5 rows with only the first expanded; a 2-file diff shows both collapsed; the truncated fixture shows the truncation copy; a request without a diff shows the no-diff copy.
    - e2e: a Discuss round-2 item shows a collapsed "Earlier in this thread" with round 1's action badge, note and title.
    - e2e: a 600-character single-line tool input does not make the page scroll horizontally (document.scrollingElement.scrollWidth equals clientWidth).
  </behavior>

  <action>
RED first (view-model tests, then e2e). Add pure helpers to view-model.ts: safeLink, splitDiffByFile, diffLineKind, truncationCopy. Create DiffView.tsx: one collapsible per file (shadcn collapsible), header row with the path (mono 12px, truncate, title = full path), "+{added}" in the success colour #2A9D8F and "-{removed}" in #FF8A80, rows collapsed except the first when there are more than 3 files; the body is a mono pre (max-height 480px, overflow auto, white-space pre-wrap, overflow-wrap anywhere) with one element per line tinted rgba(42,157,143,0.12) for adds and rgba(214,40,40,0.14) for deletes, keeping the +/- character (meaning never rests on colour alone). Truncated -> an alert above the list with truncationCopy (lineCap 400). Create DetailPane.tsx rendering sections 1-2 and 4-8 of the UI-SPEC in order, h3 section headings (12px/600 uppercase muted, letter-spacing 0.04em) under the h2 title, 24px gaps with separators: Runs on approve (only when kind is ceo_gated_tool: tool name as a mono badge and the toolInput in a mono pre with max-height 240px), Context (pre-wrap text), Recommendation (a --card block or the empty copy), Links (hidden when none; each through safeLink), Changes (DiffView or the no-diff copy), Earlier in this thread (collapsed collapsible listing each earlier round's action badge, note, timestamp and title). Leave a slot between sections 2 and 4 for 06-09 Task 2's questions. Every agent string is a React text child; no raw-HTML injection prop and no markdown renderer anywhere under ceo/. Mount DetailPane from CeoApp for the selected PendingItem, keeping 06-14's title and meta line as section 1.
  </action>

  <verify>
    <automated>pnpm --filter web test -- view-model</automated>
    <fails_when>non-zero exit, or "No test files found"</fails_when>
    <automated>npx playwright test e2e/ceo-dashboard.spec.ts</automated>
    <fails_when>non-zero exit or "0 passed"</fails_when>
    <automated>pnpm --filter web typecheck && pnpm --filter web build && node scripts/check-office-bundle.mjs</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>

  <acceptance_criteria>
    - The e2e spec asserts the anchor count equals 2 for the three-link fixture and each anchor's rel attribute equals "noopener noreferrer"
    - The e2e spec asserts the context section contains the literal text "<b>x</b>" and `locator("b")` inside it has count 0
    - `grep -rn "dangerouslySetInnerHTML" apps/web/src/ceo` prints nothing and exits 1
  </acceptance_criteria>

  <done>Every pending decision shows its context, recommendation, links and diff, and a gated call shows exactly what would run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: answering an AskUserQuestion — Choicebox, checkbox cards, Other, previews</name>

  <read_first>
    - apps/web/src/ceo/DetailPane.tsx (Task 1), apps/web/src/ceo/view-model.ts
    - apps/web/src/components/kibo-ui/choicebox/index.tsx, apps/web/src/components/ui/checkbox.tsx, textarea.tsx, field.tsx, label.tsx
    - packages/claude-adapter/src/decision-mapping.ts validateAnswers (answers keyed by exact question text; multi-select values joined with ", ")
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md detail item 3 "Questions", "Accent reserved for" items 1 and 6
  </read_first>

  <files>apps/web/src/ceo/QuestionsForm.tsx, apps/web/src/ceo/view-model.ts, apps/web/src/ceo/view-model.test.ts, apps/web/src/ceo/DetailPane.tsx, apps/web/src/ceo/CeoApp.tsx, e2e/ceo-dashboard.spec.ts</files>

  <behavior>
    - buildAnswers(questions, selections): a single-select pick gives { [question]: label }; a multiSelect pick of two labels gives "A, B" in option order; an Other selection gives the trimmed Other text; allAnswered is false while any question lacks a non-empty answer (an Other with only whitespace counts as unanswered).
    - e2e: a two-question fixture (one single, one multiSelect) renders a Choicebox with the option labels and descriptions and a checkbox-card group; choosing "Other (write your own answer)" reveals a textarea; an option with a preview shows it in a pre only while selected; a 3000-character option description is fully visible (not clamped).
    - Selections are kept per decisionId, so switching queue items and back keeps them.
  </behavior>

  <action>
RED first. Add buildAnswers and allAnswered to view-model.ts. Create QuestionsForm.tsx: for each question (1-4) the header chip, the question text (14px body), then a Kibo Choicebox for single select or checkbox cards in the same visual style for multiSelect, each card showing the label (14px/600) and the description (12px muted); the selected card's border uses the accent (UI-SPEC accent item 6); an extra "Other (write your own answer)" choice reveals a textarea; option previews render in a mono pre under the selected card (plain text, never HTML: the runtime never enables HTML previews). All question and option text wraps in full (overflow-wrap anywhere, no clamp). Keep selections in CeoApp state keyed by decisionId and expose { answers, allAnswered } for 06-10's "Send answers" button. Render QuestionsForm as section 3 of DetailPane for clarifying_question items.
  </action>

  <verify>
    <automated>pnpm --filter web test -- view-model</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>npx playwright test e2e/ceo-dashboard.spec.ts</automated>
    <fails_when>non-zero exit or "0 passed"</fails_when>
    <automated>pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit or an "error TS" line</fails_when>
  </verify>

  <acceptance_criteria>
    - view-model.test.ts asserts `buildAnswers` equals `{ "Which DB?": "Postgres", "Which features?": "Auth, Billing" }` for the fixture selections
    - The e2e spec asserts the Other textarea is hidden before and visible after choosing the Other option
    - The e2e spec asserts the long option description's rendered text equals the full 3000-character fixture string
  </acceptance_criteria>

  <done>The CEO can read and answer every question an agent asked, with answers shaped exactly as the worker validates them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decision payload -> DOM | every string is agent-authored and untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09-01 | Tampering | XSS via context/preview/diff | high | mitigate | text nodes only; grep gate on dangerouslySetInnerHTML; e2e literal "<b>x</b>" test (canon XSS: /gsd-secure-phase) |
| T-06-09-02 | Tampering | javascript:/file: links | high | mitigate | safeLink allows http/https only; rel noopener noreferrer; e2e test |
| T-06-09-03 | Spoofing | approve-what-you-didn't-see | high | mitigate | "Runs on approve" shows the stored toolInput verbatim; the worker approves only the parked input (06-01) |
</threat_model>

<verification>
- `pnpm --filter web test`, `pnpm --filter web typecheck`, `npx playwright test e2e/ceo-dashboard.spec.ts`, `pnpm --filter web build && node scripts/check-office-bundle.mjs` green.
</verification>

<success_criteria>
- CEO-02 is visible end to end on /ceo: title, context, recommendation, links and diff for every pending decision.
</success_criteria>

## Artifacts this phase produces

- `apps/web/src/ceo/DetailPane.tsx`: `DetailPane`
- `apps/web/src/ceo/DiffView.tsx`: `DiffView`
- `apps/web/src/ceo/QuestionsForm.tsx`: `QuestionsForm`
- `apps/web/src/ceo/view-model.ts`: `safeLink`, `splitDiffByFile`, `diffLineKind`, `truncationCopy`, `buildAnswers`, `allAnswered`

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-09-SUMMARY.md` when done
</output>
