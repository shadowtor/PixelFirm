---
phase: 06-ceo-dashboard-approval-workflow
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pixel-office/src/constants.ts
  - packages/pixel-office/src/layout/office-layout.json
  - packages/pixel-office/src/layout/officeLayout.ts
  - packages/pixel-office/src/layout/officeLayout.test.ts
  - packages/pixel-office/src/engine/renderer.test.ts
  - packages/pixel-office/src/index.test.ts
  - apps/web/src/App.test.tsx
  - packages/pixel-office/src/ceo/ceo-queue.ts
  - packages/pixel-office/src/ceo/ceo-queue.test.ts
  - packages/pixel-office/src/index.ts
  - packages/pixel-office/src/engine/characters.ts
autonomous: true
requirements: [CEO-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The office grid is 24x13 and grew only right and down: every Phase 5 seat, standing spot, desk, monitor, decor item and the interaction row keep their exact coordinates"
    - "A walled CEO room occupies cols 20-22, rows 1-11 behind a partition wall on col 19 with a single door at (19, 7); it holds the existing desk sprite at (20, 10) w3 h1 with no monitor and at least one existing decor sprite, and tile (21, 11) stays empty"
    - "office-layout.json declares ceoQueue [[21, 8], [20, 6], [22, 6], [21, 4]]; every pair of slots is Chebyshev >= 2 apart, no slot's glyph lands on another slot's sprite, and every slot is reachable from the door while the other three are occupied"
    - "An agent whose status becomes waiting_for_ceo walks via the Phase 5 pathfinding to the lowest-index free slot, faces DOWN on arrival, stands (never seated) and holds the frozen pose with the permission glyph, which shows from the moment the status changes"
    - "0 waiting agents: the room is empty; 1-4: slots fill lowest index first and nobody shuffles when a slot frees; 5 or more: the extra agents stay seated at their own desk with the glyph for their whole wait"
    - "When the status leaves waiting_for_ceo (a decision, expiry or anything else) the glyph updates at once, the slot is released as the walk starts, and the agent walks home through the door; an agent that goes offline while queued releases its slot"
    - "The canvas never shows text about a decision: the glyph is the whole signal (privacy wall D-08)"
    - "Queued agents stand on the four slots and no chair sprite is drawn; 'waiting-chair slots' is only the layout-data name"
    - "Every test or harness value that was derived from the 320x176 grid is re-derived from DEFAULT_COLS/DEFAULT_ROWS x TILE_SIZE: 1920x1080 and 1920x1040 present at 5x, 1280x720 at 3x"
  artifacts:
    - path: packages/pixel-office/src/layout/office-layout.json
      provides: "24x13 tiles, CEO room furniture, ceoQueue slots"
      contains: "ceoQueue"
    - path: packages/pixel-office/src/layout/officeLayout.ts
      provides: "CEO_QUEUE_SLOTS + load-time validation"
      exports: ["CEO_QUEUE_SLOTS", "isCeoQueueTile"]
    - path: packages/pixel-office/src/ceo/ceo-queue.ts
      provides: "Slot assignment, walk in, walk home"
      exports: ["syncCeoQueue", "releaseCeoSlot", "getCeoQueueOccupants", "_resetCeoQueueForTests"]
  key_links:
    - from: "packages/pixel-office/src/index.ts upsertCharacterFromAgent"
      to: "packages/pixel-office/src/ceo/ceo-queue.ts syncCeoQueue"
      via: "called after the status visual is applied, for every status upsert"
      pattern: "syncCeoQueue\\("
    - from: "packages/pixel-office/src/ceo/ceo-queue.ts"
      to: "handoff-choreography.ts blockedTilesFor + characters.ts walkCharacterTo"
      via: "reused Phase 5 walk and occupancy rules, not a new walker"
      pattern: "blockedTilesFor\\("
---

<objective>
CEO-01 on the office canvas: a walled CEO room with a four-slot waiting queue, and agents that walk
there when they need the CEO and walk home when they no longer do (D-10, 06-UI-SPEC "CEO Room
Contract", which locks the 24x13 grid, the room tiles and the slot coordinates).

The canvas stays a pure consumer of AgentStatus. It already maps waiting_for_ceo to the frozen pose
and the amber `?` glyph (Phase 5 D-03); this plan only adds where that agent stands. Nothing here
depends on the approval plans: the office moves on the task.status_changed events the runtime emits
(waiting_for_review in, running out, blocked on expiry).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-CONTEXT.md
@.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md

Typecheck: pixel-office has no typecheck script and its main is src/index.ts; `pnpm --filter web
typecheck` is the guard for its sources (Phase 5 convention).
Live harness: `scripts/verify-pixel-office-live.mjs` refuses any PIXEL_OFFICE_SHOTS path inside the
repo by design. If you capture shots, point it at the session scratchpad; never edit the guard.
TDD RED evidence: count the RED run's `ok` / `not ok` lines and append the three node:test counters.
`pnpm` blocked by Device Guard: use `npx --yes pnpm@12.4.2`.
</context>

## Flagged assumptions

- Edge probe CEO-01 was **unclassified** and stays unresolved. Assumed edge set: queue capacity
  (0 / 1-4 / 5+), entering and leaving waiting_for_ceo, going offline while queued, and a status
  that lands mid-walk (already handled by Phase 5's setRestPose). Not covered and surfaced here: an
  agent that is also a handoff sender when it starts waiting simply walks to the queue (the CEO wait
  wins); its handoff record retires through the Phase 5 paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the 24x13 office with a walled CEO room and its queue slots, guarded like the Phase 5 layout</name>

  <reversibility rating="costly">D-10 (carried from CONTEXT): resizing the grid touches every display-scale value and harness assumption; growing right/down only keeps every existing coordinate, so no seat or desk moves.</reversibility>

  <read_first>
    - packages/pixel-office/src/constants.ts l.13-16
    - packages/pixel-office/src/layout/office-layout.json (whole file) and packages/pixel-office/src/layout/officeLayout.ts (whole file: tiles check l.40-42, toTiles l.76-81, INTERACTION validation l.102-123)
    - packages/pixel-office/src/engine/renderer.test.ts l.1045-1075 (the old map-size literals and FURNITURE count) and its mock-ctx draw-proof helpers (control render with bubbleType null to separate glyph pixels from sprite pixels)
    - packages/pixel-office/src/index.test.ts l.180-245 (seat/standing guards, display-scale cases)
    - apps/web/src/App.test.tsx l.85-130 (displayScaleFor pins and comments)
    - packages/pixel-office/src/handoff/handoff-choreography.test.ts l.90-115 (the Phase 5 layout-data guard style)
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "CEO Room Contract"
  </read_first>

  <files>packages/pixel-office/src/constants.ts, packages/pixel-office/src/layout/office-layout.json, packages/pixel-office/src/layout/officeLayout.ts, packages/pixel-office/src/layout/officeLayout.test.ts, packages/pixel-office/src/engine/renderer.test.ts, packages/pixel-office/src/index.test.ts, apps/web/src/App.test.tsx</files>

  <behavior>
    - OFFICE_TILE_MAP has 13 rows of 24; rows 0 and 12 and cols 0 and 23 are wall; col 19 is wall on rows 1-11 except (19, 7), which is floor; cols 20-22 rows 1-11 are floor.
    - SEATS, STANDING_SPOTS, the interaction block and the 30 Phase 5 furniture entries deep-equal their Phase 5 values (copied as literals from the pre-change office-layout.json).
    - CEO_QUEUE_SLOTS deep-equals [{col:21,row:8},{col:20,row:6},{col:22,row:6},{col:21,row:4}]; all pairwise Chebyshev distances are >= 2.
    - Rendering four waiting_for_ceo characters on the four slots, no glyph pixel of one slot's character lands on a sprite pixel of another slot's character (glyph pixels isolated with the control-render pattern).
    - With the other three slots and FURNITURE_BLOCKED_TILES blocked, findPath from the door (19, 7) to each slot is non-empty; findPath from seat (1, 4) to the door is non-empty.
    - No furniture footprint covers a slot, (19, 7), (20, 7), or (21, r) for r in 4..9; the CEO desk footprint is exactly (20..22, 10); (21, 11) is floor and unblocked.
    - A layout whose ceoQueue slot sits on a wall, on blocking furniture or on a home throws at load naming the slot.
    - displayScaleFor(1920, 1080) === 5, (1920, 1040) === 5, (1280, 720) === 3; DEFAULT_COLS * TILE_SIZE === 384 and DEFAULT_ROWS * TILE_SIZE === 208.
  </behavior>

  <action>
RED first: write officeLayout.test.ts and update the re-derived pins so they fail on the 20x11 grid.

Set DEFAULT_COLS = 24 and DEFAULT_ROWS = 13 in constants.ts. Rewrite office-layout.json "tiles" as 13 strings of 24 chars: row 0 and row 12 all "W"; rows 1-6 and 8-11 "W" + 18 "." + "W" + "..." + "W"; row 7 "W" + 22 "." + "W" (the door on the existing row-7 lane). Keep "seats", "standing", "interaction" and every existing "furniture" entry byte-for-byte; append three furniture entries: { "sprite": "desk", "col": 20, "row": 10, "w": 3, "h": 1, "blocks": true } (the CEO desk, no monitor), { "sprite": "plant", "col": 22, "row": 2, "w": 1, "h": 1, "blocks": true } and { "sprite": "painting", "col": 21, "row": 0, "w": 1, "h": 1, "blocks": false } (existing decor sprites only, clear of the slots, the door, the entrance tile and the col-21 walk path). Add "ceoQueue": [[21, 8], [20, 6], [22, 6], [21, 4]] next to "interaction" (UI-SPEC locked, front-of-line order).

In officeLayout.ts export CEO_QUEUE_SLOTS = toTiles(layout.ceoQueue) with load-time validation in the INTERACTION style: ceoQueue must be a non-empty array; each slot must be a FLOOR_1 tile, not in the blocked set, and not a SEATS/STANDING_SPOTS home, else throw `office-layout.json: ceoQueue slot (c,r) ...` naming the problem. Export isCeoQueueTile(col, row).

Re-derive, never re-hardcode, the Phase 5 values (UI-SPEC: "Every Phase 5 test or harness value derived from 320 x 176 must be re-derived from the constants"): the two renderFrame calls in renderer.test.ts that pass the old literal map width and height take DEFAULT_COLS * TILE_SIZE and DEFAULT_ROWS * TILE_SIZE instead; its FURNITURE count becomes 8 + 16 + 4 + 2 + 3 with a comment naming the three CEO-room entries. index.test.ts's display-scale cases become [1920, 1040, 5], [1920, 1080, 5], [1280, 720, 3], [3840, 2080, 10], [800, 600, 3], [1400, 900, 3], preceded by the two grid-size pins. App.test.tsx's two pins become 3 (1280x720) and 5 (1920x1080) with the comments rewritten for 384x208 (208 x 5 = 1040, leaving a 20px WALL_COLOR strip above and below at 1080). Run the whole pixel-office and web suites and re-derive any other assertion the resize breaks the same way; record each one in the SUMMARY.
  </action>

  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or any "FAIL" line</fails_when>
    <automated>pnpm --filter web test && pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit, or any "FAIL" / "error TS" line</fails_when>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter pixel-office test -- officeLayout` passes with one test per behaviour bullet
    - `grep -c '"ceoQueue"' packages/pixel-office/src/layout/office-layout.json` returns 1
    - `grep -n "DEFAULT_COLS = 24" packages/pixel-office/src/constants.ts` and `grep -n "DEFAULT_ROWS = 13" packages/pixel-office/src/constants.ts` both match
    - renderer.test.ts contains no `renderFrame(ctx, 320, 176` call (`grep -c "renderFrame(ctx, 320, 176" packages/pixel-office/src/engine/renderer.test.ts` returns 0)
  </acceptance_criteria>

  <done>The office has a walled CEO room with four guarded queue slots, and no Phase 5 coordinate moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agents queue for the CEO — walk in, wait facing down, walk home (CEO-01)</name>

  <read_first>
    - packages/pixel-office/src/index.ts (upsertCharacterFromAgent, the offline branch, stepOffice, _resetForTests)
    - packages/pixel-office/src/handoff/handoff-choreography.ts l.1-70 (circular-import note, blockedTilesFor) and l.180-210 (walk-home call shape)
    - packages/pixel-office/src/engine/characters.ts l.100-130 (WALK end: face DOWN at home) and walkCharacterTo l.173-192
    - packages/pixel-office/src/status/status-mapping.ts l.43 (WAITING_FOR_CEO visual)
    - packages/pixel-office/src/handoff/handoff-choreography.test.ts (real stepOffice-loop test style, e.g. "real update loop" describes)
  </read_first>

  <files>packages/pixel-office/src/ceo/ceo-queue.ts, packages/pixel-office/src/ceo/ceo-queue.test.ts, packages/pixel-office/src/index.ts, packages/pixel-office/src/engine/characters.ts</files>

  <behavior>
    - One agent upserted as WAITING_FOR_CEO: bubbleType is "permission" immediately (before moving); after stepping the real loop it stands on (21, 8), dir DOWN, state IDLE, not WALK, and no tile of its path was in FURNITURE_BLOCKED_TILES.
    - Four waiting agents take slots 0, 1, 2, 3 in upsert order; a fifth never walks and stays on its seat with bubbleType "permission".
    - The slot-0 agent upserted as CODING: its slot is released in the same call, its path ends at its seat, its glyph changes at once; the slot-1 agent's tile is unchanged (no shuffle); a new waiting agent then takes slot 0.
    - The fifth (overflow) agent does not walk in when a slot frees; it stays at its desk until its status changes.
    - An agent upserted OFFLINE while queued is removed and its slot is free.
    - The drawn scene for a queued agent contains no text operation (no fillText) beyond what Phase 5 draws for handoffs.
  </behavior>

  <action>
RED first. Create ceo/ceo-queue.ts as a sibling of handoff/ (module state + test reset, the connection-status.ts style): occupants Map<agentId, slotIndex> and an overflow Set<agentId>. syncCeoQueue(ch, status): when status is AgentStatus.WAITING_FOR_CEO and the agent is neither queued nor in overflow, take the lowest free slot index and walkCharacterTo(ch, slot.col, slot.row, getTileMap(), blockedTilesFor(ch, slot)); with no free slot add it to overflow and do nothing else (D-10). When status is anything else, drop it from overflow, and if it holds a slot release it and walk it home to { col: ch.seatCol, row: ch.seatRow } with blockedTilesFor(ch, seat). No shuffling ever. Export releaseCeoSlot(agentId), getCeoQueueOccupants() (read-only view for tests) and _resetCeoQueueForTests(). Reuse blockedTilesFor and walkCharacterTo; do not write a second walker.

In index.ts call syncCeoQueue(ch, status) at the end of upsertCharacterFromAgent (after applyBubble, so the glyph shows from the moment the status changes, including during the walk), call releaseCeoSlot(agentId) in the offline branch before characters.delete, and call _resetCeoQueueForTests() from _resetForTests(). In characters.ts extend the WALK-end facing rule to `home OR isCeoQueueTile(tileCol, tileRow)` so a queued agent faces DOWN toward the viewer and the CEO desk. Standing is automatic: the seated offset only applies on the agent's own seat (05-32). Add a ponytail comment on overflow: a fifth waiting agent waits at its desk for its whole wait; upgrade path is a larger room.
  </action>

  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or any "FAIL" line</fails_when>
    <automated>pnpm --filter web test && pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit, or any "FAIL" / "error TS" line</fails_when>
  </verify>

  <acceptance_criteria>
    - ceo-queue.test.ts drives the real stepOffice loop (no direct position assignment) and asserts the queued agent's `{ tileCol, tileRow, dir }` equals `{ tileCol: 21, tileRow: 8, dir: Direction.DOWN }`
    - ceo-queue.test.ts asserts the fifth agent's tile equals its seat and its bubbleType equals "permission"
    - ceo-queue.test.ts asserts the slot-1 agent's tile is unchanged after slot 0 is released
    - `grep -n "syncCeoQueue(" packages/pixel-office/src/index.ts` returns a match
  </acceptance_criteria>

  <done>An agent that needs the CEO visibly walks to the CEO room and waits in line, and walks back to its desk when it no longer waits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relayed AgentStatus -> canvas | the office only ever receives a status value, never decision content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07-01 | Info disclosure | canvas text | medium | mitigate | queue logic writes no text; test asserts no fillText for a queued agent; decision content never reaches the office feed (06-06) |
| T-06-07-02 | Tampering | office-layout.json | low | mitigate | load-time validation throws on a malformed ceoQueue slot |
| T-06-07-03 | Denial of service | many waiting agents | low | accept | overflow agents wait at their desks; bounded by the four slots |
</threat_model>

<verification>
- `pnpm --filter pixel-office test`, `pnpm --filter web test`, `pnpm --filter web typecheck` green.
- Phase 5 live regression on the resized grid: `pnpm --filter api db:test:up` then `node scripts/verify-pixel-office-live.mjs` exits 0 (servers are spawned by the harness; ports 3000 and the web port must be free; if you set PIXEL_OFFICE_SHOTS, point it outside the repo).
</verification>

<success_criteria>
- CEO-01 on screen: waiting agents walk to and queue in a visible CEO room and return afterwards, with every Phase 5 truth still holding on the larger grid.
</success_criteria>

## Artifacts this phase produces

- `DEFAULT_COLS = 24`, `DEFAULT_ROWS = 13`
- `office-layout.json` key `ceoQueue`; CEO room tiles and furniture
- `packages/pixel-office/src/layout/officeLayout.ts`: `CEO_QUEUE_SLOTS`, `isCeoQueueTile`
- `packages/pixel-office/src/ceo/ceo-queue.ts`: `syncCeoQueue`, `releaseCeoSlot`, `getCeoQueueOccupants`, `_resetCeoQueueForTests`
- New test file `packages/pixel-office/src/layout/officeLayout.test.ts`

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-07-SUMMARY.md` when done
</output>
