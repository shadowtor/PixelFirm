---
phase: 06-ceo-dashboard-approval-workflow
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/components.json
  - apps/web/tsconfig.json
  - apps/web/vite.config.ts
  - apps/web/src/main.tsx
  - apps/web/src/lib/utils.ts
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/components/ui/badge.tsx
  - apps/web/src/components/ui/textarea.tsx
  - apps/web/src/components/ui/field.tsx
  - apps/web/src/components/ui/label.tsx
  - apps/web/src/components/ui/radio-group.tsx
  - apps/web/src/components/ui/checkbox.tsx
  - apps/web/src/components/ui/tabs.tsx
  - apps/web/src/components/ui/scroll-area.tsx
  - apps/web/src/components/ui/collapsible.tsx
  - apps/web/src/components/ui/alert.tsx
  - apps/web/src/components/ui/alert-dialog.tsx
  - apps/web/src/components/ui/table.tsx
  - apps/web/src/components/ui/skeleton.tsx
  - apps/web/src/components/ui/empty.tsx
  - apps/web/src/components/ui/separator.tsx
  - apps/web/src/components/ui/spinner.tsx
  - apps/web/src/components/ui/sonner.tsx
  - apps/web/src/components/kibo-ui/choicebox/index.tsx
  - apps/web/src/components/kibo-ui/status/index.tsx
  - apps/web/src/ceo/ceo.css
  - apps/web/src/ceo/CeoApp.tsx
  - scripts/check-office-bundle.mjs
autonomous: false
requirements: [CEO-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "/ceo is a private view in apps/web chosen by a path check in main.tsx and loaded as a lazy chunk; the office route keeps its static App import and loads nothing from ceo/ (D-08)"
    - "Tailwind, the shadcn theme and the Kibo items are imported only from apps/web/src/ceo/ceo.css through CeoApp, with the UI-SPEC dark palette"
    - "No UI package is installed before the user approves the legitimacy check of shadcn, lucide-react and tailwind-merge"
    - statement: "The office route loads no Tailwind preflight and no ceo chunk: the production build's entry has no Tailwind CSS and CeoApp is a dynamic import only"
      verification: backstop
  artifacts:
    - path: apps/web/src/main.tsx
      provides: "Path check -> lazy CeoApp"
      contains: "import(\"./ceo/CeoApp\")"
    - path: apps/web/src/ceo/CeoApp.tsx
      provides: "Lazy /ceo entry: the only ceo.css import and the page root (06-14 fills the shell and queue)"
      exports: ["CeoApp"]
    - path: apps/web/src/ceo/ceo.css
      provides: "The only Tailwind entry, UI-SPEC theme variables"
      contains: "@import \"tailwindcss\""
    - path: scripts/check-office-bundle.mjs
      provides: "Build-manifest scope-isolation check"
  key_links:
    - from: "apps/web/src/main.tsx"
      to: "apps/web/src/ceo/CeoApp.tsx"
      via: "dynamic import only when location.pathname is /ceo or under /ceo/"
      pattern: "startsWith\\(\"/ceo/\"\\)"
---

<objective>
Scaffold the private `/ceo` dashboard (CEO-02, D-08) inside apps/web without touching the office
route: shadcn + Tailwind + the two Kibo items locked by 06-UI-SPEC, scoped to a lazy chunk, with a
build check that proves the office route loads none of it. This is only the scaffold, and it depends
on no other plan, so it runs in wave 1. The auth-aware shell, the live feed and the pending queue are
06-14; detail sections 06-09; actions and history 06-10.

Package legitimacy gate: shadcn 4.21.0, lucide-react 1.47.0 and tailwind-merge 3.7.0 are flagged
SUS (too-new only) in 06-RESEARCH, so a blocking human verification comes before any install.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md

Project stack skill (global CLAUDE.md): Kibo UI wherever a component exists; follow shadcn's and
Kibo's own docs where they differ from community examples (use Context7 or the official sites).
Tooling: `pnpm` blocked by Device Guard: use `npx --yes pnpm@12.4.2`. apps/web has no @types/node:
resolve the alias with `fileURLToPath(new URL("./src", import.meta.url))` in vite.config.ts.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: package legitimacy check before any install (shadcn, lucide-react, tailwind-merge)</name>
  <read_first>
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md "Package Legitimacy Audit"
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "Registry Safety"
  </read_first>
  <what-built>Nothing installed yet. The next task runs `npx shadcn@4.21.0 init` and `add`, which installs tailwindcss, @tailwindcss/vite, radix-ui, lucide-react, sonner, class-variance-authority, clsx, tailwind-merge and tw-animate-css into apps/web, plus the Kibo choicebox and status items.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/shadcn (version 4.21.0), https://www.npmjs.com/package/lucide-react (1.47.0) and https://www.npmjs.com/package/tailwind-merge (3.7.0).
    2. For each: the repository link is github.com/shadcn-ui/ui, github.com/lucide-icons/lucide, github.com/dcastil/tailwind-merge respectively; weekly downloads are in the millions; the version exists and was published by the project's usual maintainers.
    3. Optionally run `npm view lucide-react@1.47.0 scripts` and `npm view tailwind-merge@3.7.0 scripts` and confirm no postinstall script.
    4. If the executor's dry run shows the CLI would also add any package not in the list above (for example @types/node), check it the same way.
  </how-to-verify>
  <resume-signal>Type "approved" to install, or name the package that failed the check</resume-signal>
  <acceptance_criteria>
    - The user's "approved" reply (or the named package to drop) is recorded in 06-08-SUMMARY.md before Task 2 installs anything
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: shadcn + Tailwind scoped to a lazy /ceo chunk, and a build check that the office route stays clean</name>

  <read_first>
    - apps/web/package.json, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/tsconfig.node.json, apps/web/src/main.tsx, apps/web/index.html
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "Design System", "Scope isolation", "Component Inventory", "Color", "Registry Safety"
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md "Pattern 8" and Assumption A5
    - https://ui.shadcn.com/docs/installation/vite (official; follow it where the CLI prompts differ)
  </read_first>

  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/components.json, apps/web/tsconfig.json, apps/web/vite.config.ts, apps/web/src/main.tsx, apps/web/src/lib/utils.ts, apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/badge.tsx, apps/web/src/components/ui/textarea.tsx, apps/web/src/components/ui/field.tsx, apps/web/src/components/ui/label.tsx, apps/web/src/components/ui/radio-group.tsx, apps/web/src/components/ui/checkbox.tsx, apps/web/src/components/ui/tabs.tsx, apps/web/src/components/ui/scroll-area.tsx, apps/web/src/components/ui/collapsible.tsx, apps/web/src/components/ui/alert.tsx, apps/web/src/components/ui/alert-dialog.tsx, apps/web/src/components/ui/table.tsx, apps/web/src/components/ui/skeleton.tsx, apps/web/src/components/ui/empty.tsx, apps/web/src/components/ui/separator.tsx, apps/web/src/components/ui/spinner.tsx, apps/web/src/components/ui/sonner.tsx, apps/web/src/components/kibo-ui/choicebox/index.tsx, apps/web/src/components/kibo-ui/status/index.tsx, apps/web/src/ceo/ceo.css, apps/web/src/ceo/CeoApp.tsx, scripts/check-office-bundle.mjs</files>

  <action>
Add `"baseUrl": "."` and `"paths": { "@/*": ["./src/*"] }` to apps/web/tsconfig.json. In vite.config.ts add the @tailwindcss/vite plugin, `resolve.alias` "@" to the src directory (fileURLToPath form), `build.manifest: true`, and `server.proxy` for "/ceo/api" to http://localhost:3000 and "/ceo/ws" to { target: "ws://localhost:3000", ws: true } (same origin in dev; no CORS plugin ever, server.ts forbids it). Install with the UI-SPEC's exact commands from the repo root: `pnpm --filter web add -D tailwindcss @tailwindcss/vite`, `npx shadcn@4.21.0 init --base radix --css-variables --no-rtl --pointer --no-monorepo -c apps/web`, `npx shadcn@4.21.0 add button badge textarea field label radio-group checkbox tabs scroll-area collapsible alert alert-dialog table skeleton empty separator spinner sonner -c apps/web`, `npx shadcn@4.21.0 add https://www.kibo-ui.com/r/choicebox.json https://www.kibo-ui.com/r/status.json -c apps/web`. Re-vet the two Kibo files as the UI-SPEC's Registry Safety gate did (grep the installed files for fetch, XMLHttpRequest, sendBeacon, process.env, eval, new Function and remote dynamic import; dependencies limited to lucide-react/radix-ui and the field/badge registry items); stop and report if anything differs. Replace the first line of the UI-SPEC Component Inventory provenance with the output of `npx shadcn info -c apps/web` in the SUMMARY (do not edit the UI-SPEC).

Scope isolation (UI-SPEC hard rule): move whatever global CSS the init wrote into apps/web/src/ceo/ceo.css (`@import "tailwindcss"`, tw-animate-css, and the theme variables), point components.json's tailwind.css at src/ceo/ceo.css, remove any CSS import the init added to main.tsx, and overwrite the generated theme variables with the UI-SPEC Color table (dark only: --background #14141F; --card/--popover/--muted/--secondary #1E1E2E; --border/--input #3A3A5C; --foreground/--card-foreground #E6E6EF; --muted-foreground #A0A0B8; --primary/--ring #FFB703; --primary-foreground #161616; --destructive #D62828), font stacks ui-sans-serif/system-ui and ui-monospace. index.html's body background stays #3A3A5C.

main.tsx (D-08, Pattern 8): keep the #root guard and the static App import; create the root once; when location.pathname is "/ceo" or starts with "/ceo/", `import("./ceo/CeoApp").then(({ CeoApp }) => root.render(<StrictMode><CeoApp /></StrictMode>))`, otherwise render App as today. Create ceo/CeoApp.tsx exporting CeoApp, importing "./ceo.css" (the only place Tailwind is imported) and rendering the page root with its own #14141F background and the h1 "CEO desk" (06-14 completes it).

Create scripts/check-office-bundle.mjs: read apps/web/dist/.vite/manifest.json; fail (exit 1, one message line prefixed "office bundle:" naming the file) if the index.html entry lists any CSS file whose contents include "tailwindcss", or lists src/ceo/CeoApp.tsx in its static imports; also fail unless src/ceo/CeoApp.tsx appears in the entry's dynamicImports and its own css list contains a file that includes "tailwindcss" (the positive control that proves the check can see Tailwind at all, Assumption A5).
  </action>

  <verify>
    <automated>pnpm --filter web build && node scripts/check-office-bundle.mjs</automated>
    <fails_when>non-zero exit from either command, or the script prints a line starting with "office bundle:"</fails_when>
    <automated>pnpm --filter web typecheck && pnpm --filter web test</automated>
    <fails_when>non-zero exit, or an "error TS" / "FAIL" line</fails_when>
  </verify>

  <acceptance_criteria>
    - `node scripts/check-office-bundle.mjs` exits 0 after `pnpm --filter web build`, and exits 1 if `import "./ceo/ceo.css"` is temporarily added to main.tsx (demonstrate once, then revert; record in the SUMMARY)
    - `grep -n 'startsWith("/ceo/")' apps/web/src/main.tsx` returns a match
    - `grep -c '@import "tailwindcss"' apps/web/src/ceo/ceo.css` returns 1
    - apps/web/src/components/kibo-ui/choicebox/index.tsx and apps/web/src/components/kibo-ui/status/index.tsx exist
  </acceptance_criteria>

  <done>shadcn, Tailwind and the Kibo items are installed and scoped to a lazy /ceo chunk, and a build check proves the office route loads none of it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> apps/web | new UI dependencies |
| office route bundle -> OBS (Phase 7) | must never include dashboard code or styles |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08-SC | Tampering | npm/shadcn/Kibo installs | high | mitigate | blocking-human legitimacy checkpoint (Task 1) + Kibo re-vet grep after install |
| T-06-08-02 | Info disclosure | dashboard on the office route | high | mitigate | lazy chunk + build-manifest check with positive control; 06-14 adds the e2e check that `/` requests nothing under /src/ceo/ |

Agent-text XSS, token-in-URL and forged-frame threats moved with the feed and queue code to 06-14.
</threat_model>

<verification>
- `pnpm --filter web test`, `pnpm --filter web typecheck`, `pnpm --filter web build && node scripts/check-office-bundle.mjs`, `npx playwright test e2e/office-disconnect.spec.ts` green (the office spec proves the office route still behaves after the vite.config and main.tsx changes).
</verification>

<success_criteria>
- shadcn, Tailwind and the Kibo items are installed only after the legitimacy approval and live only in the lazy /ceo chunk; the office route is byte-for-byte free of dashboard code and styles.
</success_criteria>

## Artifacts this phase produces

- `apps/web/src/ceo/CeoApp.tsx`: `CeoApp` (page root and the only `ceo.css` import; 06-14 builds the shell on it)
- `apps/web/src/ceo/ceo.css` (the only Tailwind entry), `apps/web/components.json`, shadcn `components/ui/*`, Kibo `choicebox` and `status`, `apps/web/src/lib/utils.ts`
- `apps/web/vite.config.ts`: `@` alias, `build.manifest`, dev proxy for `/ceo/api` and `/ceo/ws`
- `scripts/check-office-bundle.mjs`

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-08-SUMMARY.md` when done
</output>
