---
phase: 06-ceo-dashboard-approval-workflow
plan: 11
type: execute
wave: 7
depends_on: ["06-04", "06-07", "06-10"]
files_modified:
  - scripts/verify-ceo-approval-live.mjs
  - .planning/phases/06-ceo-dashboard-approval-workflow/06-11-live-evidence.md
  - packages/claude-adapter/src/claude-code-runtime.ts
  - packages/claude-adapter/src/claude-code-runtime.test.ts
autonomous: true
requirements: [CEO-01, CEO-02, CEO-03, CEO-04, CEO-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "In a real Claude Code session hosted by the real worker, an AskUserQuestion answered on /ceo reaches the agent as its answer, a gated command approved on /ceo runs exactly as parked, and a gated command rejected on /ceo never runs (CEO-03)"
    - "A gated probe command that the temp repo's .claude/settings.local.json allow-lists still parks for a CEO decision before it runs (CEO-04, research Assumption A1)"
    - "A decision held for more than 120 seconds is still applied: no blocked status is emitted while it waits and the session continues afterwards (D-03, Assumption A2)"
    - "Each of the three decisions leaves ceo.approval_requested -> ceo.decision_made (decidedBy the dev identity) -> ceo.decision_applied in the event log (CEO-05)"
    - "While a decision is pending, the requesting agent is inside the CEO room on the office canvas, and after the last decision it is not (CEO-01)"
    - statement: "Nothing but the pose and glyph reaches the office route: none of the decision titles, question text, context or probe command strings appear in the office page's DOM or in any /ws/browser frame during the whole run"
      verification: backstop
  artifacts:
    - path: scripts/verify-ceo-approval-live.mjs
      provides: "Zero-mock live proof of the CEO approval workflow"
    - path: .planning/phases/06-ceo-dashboard-approval-workflow/06-11-live-evidence.md
      provides: "Transcript and assertion results of the live run"
  key_links:
    - from: "scripts/verify-ceo-approval-live.mjs"
      to: "real API + web + worker + Claude Code"
      via: "spawned processes, dev-bypass /ceo driven by Playwright, events read back from the test database"
      pattern: "WORKER_TASK_PROMPT"
---

<objective>
Prove the phase against a real session, the way Phase 4 and Phase 5 were proven: no mocks, real
Claude Code under the worker, real control plane, real /ceo dashboard in a real browser. This closes
the two assumptions only a live run can settle (A1: a PreToolUse "ask" beats a settings allow rule in
SDK mode; A2: a canUseTool park survives minutes with a string prompt) and the privacy backstop
(nothing reaches the office route).

Output: `scripts/verify-ceo-approval-live.mjs` and `06-11-live-evidence.md`. If A1 or A2 fails, this
plan applies the fallback research names and re-runs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md
@.planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md
@scripts/verify-pixel-office-live.mjs

Project memory: the live harness refuses any screenshot directory inside the repo by design. The new
script keeps the same guard; point CEO_LIVE_SHOTS at the session scratchpad. Never relax the guard.
This run uses the developer's Claude subscription (no ANTHROPIC_API_KEY; the runtime strips it).
`pnpm` blocked by Device Guard: spawn `npx --yes pnpm@12.4.2` instead and record it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: write the live proof script</name>

  <precondition>`claude --version` succeeds and `claude` is logged in with the subscription on this machine; Docker is running (test Postgres on 5434); ports 3000 and 5197 are free.</precondition>

  <read_first>
    - scripts/verify-pixel-office-live.mjs (whole file: shots-dir guard l.196-200, port preflight, db:test:down/up reset, startServer/waitFor, taskkill cleanup l.405-415, scanCanvas and how it builds the office colour set from engine data l.505-625)
    - apps/worker/src/env.ts (WORKER_TASK_* keys from 06-04), apps/api/src/env.ts (CEO_* keys), apps/api/scripts/migrate.mjs
    - apps/web/src/ceo/ActionBar.tsx and QuestionsForm.tsx (accessible names to drive), apps/web/vite.config.ts (proxy)
    - packages/pixel-office/src/layout/office-layout.json (CEO room cols 20-22, rows 1-11)
  </read_first>

  <files>scripts/verify-ceo-approval-live.mjs</files>

  <action>
Write a Node ESM script modelled on verify-pixel-office-live.mjs (copy its helpers rather than importing the file). Steps: refuse a CEO_LIVE_SHOTS path inside the repo at module load; preflight ports 3000 and 5197; reset the test DB with the repo's own `pnpm --filter api db:test:down` / `db:test:up` and apply apps/api/scripts/migrate.mjs; spawn the API (test DATABASE_URL, generated CREDENTIAL_PEPPER/BOOTSTRAP_SECRET/BROWSER_ACCESS_TOKEN, CEO_DEV_AUTH_BYPASS=1, CEO_ALLOWED_ORIGINS=http://127.0.0.1:5197) and the web dev server on 127.0.0.1:5197 (VITE_WS_BASE_URL ws://127.0.0.1:3000 and the same browser token); issue a worker credential through POST /admin/workers.

Create a disposable git repo under os.tmpdir() (never inside this repo) with one commit and `.claude/settings.local.json` containing `{ "permissions": { "allow": ["Bash(node -e *)"] } }` (the A1 allow rule). Spawn the worker against it with WORKER_AGENT_ID=live-ceo-agent, WORKER_TASK_TITLE="CEO approval live proof" and a WORKER_TASK_PROMPT that tells the agent, in order: use the AskUserQuestion tool to ask "Which greeting should the probe write?" with options "hello" and "goodbye"; run the exact command `node -e "require('fs').writeFileSync('deploy-probe-approved.txt', '<answer>')"` with the chosen answer; run `node -e "require('fs').writeFileSync('deploy-probe-rejected.txt', 'x')"`; if a tool call is denied, do not retry it; finish with a one-line summary. Both commands contain "deploy" and are therefore CEO-gated.

Drive Chromium with Playwright: page A opens http://127.0.0.1:5197/ceo; page B opens the office route and records every /ws/browser frame (page.on("websocket") framereceived). Decision 1: wait for the "Question" item, choose "hello", click "Send answers". Decision 2: wait for the item whose Runs-on-approve text contains deploy-probe-approved, hold 125 s, asserting during the hold that no task.status_changed "blocked" row exists and that the office canvas has sprite pixels (pixels in no office colour, the harness's classification) inside the CEO room rectangle (cols 20-22, rows 1-11); then Approve and "Approve and run". Decision 3: wait for the deploy-probe-rejected item and click Reject. Wait up to 10 minutes for the task's completed or failed status.

Assert and print a JSON summary: the approved file exists with content "hello"; the rejected file does not exist; for each of the three decisionIds the ordered event types are requested, decision_made (decidedBy dev-bypass@pixelfirm.invalid), decision_applied (outcome allowed, allowed, denied); every probe command had a ceo.approval_requested before its file appeared (A1); no blocked status during the hold (A2, D-03); the CEO room holds no sprite pixels after the last decision; the office frames and final office DOM contain none of the question text, "deploy-probe", the decision titles or the context strings. Exit 1 naming the first failed assertion. Always tear down: taskkill /T on Windows, remove the temp repo.
  </action>

  <verify>
    <automated>node --check scripts/verify-ceo-approval-live.mjs</automated>
    <fails_when>non-zero exit or a SyntaxError line</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -n "CEO_LIVE_SHOTS" scripts/verify-ceo-approval-live.mjs` shows the in-repo refusal guard at module load
    - The script contains the literal allow rule `Bash(node -e *)` written into the temp repo's .claude/settings.local.json
    - The script's hold uses at least 125000 ms before clicking Approve
  </acceptance_criteria>

  <done>A single command can prove the whole CEO workflow against a real session without mocks.</done>
</task>

<task type="auto">
  <name>Task 2: run the live proof, apply a researched fallback if A1 or A2 fails, record the evidence</name>

  <precondition>Same as Task 1; plans 06-01 through 06-10, 06-13 and 06-14 are complete.</precondition>

  <read_first>
    - scripts/verify-ceo-approval-live.mjs (Task 1)
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md "Assumptions Log" A1 and A2, "Alternatives Considered"
    - packages/claude-adapter/src/claude-code-runtime.ts (runQuery prompt shape, PreToolUse hook)
    - .planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md (evidence file format)
  </read_first>

  <files>.planning/phases/06-ceo-dashboard-approval-workflow/06-11-live-evidence.md, packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts</files>

  <action>
Run `node scripts/verify-ceo-approval-live.mjs` with CEO_LIVE_SHOTS pointing at the session scratchpad. If it passes, write 06-11-live-evidence.md in the 04-04-demo-evidence.md format: date, versions (claude --version, SDK version), the JSON summary, the three decisionIds with their event chains, the hold duration, and the screenshot paths (outside the repo, referenced by path only).

Only if an assumption fails, apply its research fallback, add a unit test for it, record the deviation, and re-run: A1 fails (a gated probe runs with no ceo.approval_requested) -> park inside the PreToolUse hook instead of returning "ask": give that hook matcher a timeout of 86400 seconds and return permissionDecision allow or deny from the same toPermissionResult mapping (updatedInput stays the parked input); A2 fails (the parked call dies before the decision with a string prompt) -> switch runQuery's prompt to an async-iterable of one SDK user message that stays open until the result message arrives. Never weaken an assertion to make the run pass. If a failure is agent behaviour (the model ignored the prompt), re-run once and record both runs.
  </action>

  <verify>
    <automated>node scripts/verify-ceo-approval-live.mjs</automated>
    <fails_when>non-zero exit, or the printed JSON summary has "ok": false</fails_when>
    <automated>pnpm --filter claude-adapter test</automated>
    <fails_when>non-zero exit (must stay green whether or not a fallback was applied)</fails_when>
    <human-check>
      <test>Open the evidence file and the two screenshots it references (the CEO room during the 125 s hold, and /ceo showing the pending queue)</test>
      <expected>An agent stands in the CEO room with the amber question-mark glyph and no decision text on the canvas; /ceo shows the pending item with its context and diff; the evidence lists three complete event chains</expected>
      <why_human>Whether the CEO room "reads well on stream" and the dashboard matches the UI-SPEC's look is a taste judgement the pixel checks cannot make</why_human>
    </human-check>
  </verify>

  <acceptance_criteria>
    - The live script exits 0 and 06-11-live-evidence.md records the JSON summary with every assertion true
    - The evidence records the hold as at least 125 s and the approved probe file content as "hello"
    - If a fallback was applied, 06-11-SUMMARY.md names it, and a new claude-code-runtime.test.ts case covers it
  </acceptance_criteria>

  <done>The phase's truths hold in a real session: gated calls wait for the CEO, the CEO's choice controls the runtime, the audit chain is complete, and the office shows only the queue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live agent -> disposable repo | the agent writes only in a temp repo outside this repository |
| harness -> screenshots | review frames never enter the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11-01 | Tampering | agent acting on this repo | medium | mitigate | worker pointed only at a mkdtemp repo; removed on teardown |
| T-06-11-02 | Info disclosure | screenshots committed | low | mitigate | CEO_LIVE_SHOTS in-repo refusal guard (project memory) |
| T-06-11-03 | Elevation | fallback weakens the gate | high | mitigate | fallbacks keep deny-by-default and the parked-input rule; unit test required; assertions never relaxed |
| T-06-11-04 | Info disclosure | test secrets | low | accept | throwaway values against the local test DB only |
</threat_model>

<verification>
- `node scripts/verify-ceo-approval-live.mjs` exits 0; evidence file written; `pnpm --filter claude-adapter test` green.
</verification>

<success_criteria>
- All four roadmap success criteria for Phase 6 observed against a real session, with evidence on disk.
</success_criteria>

## Artifacts this phase produces

- `scripts/verify-ceo-approval-live.mjs` (env `CEO_LIVE_SHOTS`)
- `.planning/phases/06-ceo-dashboard-approval-workflow/06-11-live-evidence.md`
- Conditional: PreToolUse-park fallback or streaming-prompt fallback in `claude-code-runtime.ts` (only if A1/A2 fail live)

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-11-SUMMARY.md` when done
</output>
