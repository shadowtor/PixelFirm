---
phase: 06-ceo-dashboard-approval-workflow
plan: 06
type: execute
wave: 3
depends_on: ["06-13"]
files_modified:
  - packages/company-core/src/decisions.ts
  - packages/company-core/src/decisions.test.ts
  - packages/company-core/src/index.ts
  - packages/company-core/src/reducer.test.ts
  - apps/api/src/ws/browser-connections.ts
  - apps/api/src/ws/browser-connections.test.ts
  - apps/api/src/routes/ws-browser.ts
  - apps/api/src/routes/ws-browser.test.ts
autonomous: true
requirements: [CEO-02, CEO-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "foldDecisions over the ceo.* events yields every pending decision and the decided/expired history, using the same applyDecisionEvent step the browser applies to each live event (snapshot and live cannot drift)"
    - "The pending queue shows one item per threadId, its newest pending decision, with its round number and earlier rounds; two pending decisions in different threads stay separate items (CEO-02 adjacency edge)"
    - "An empty event list yields no pending and no history; a Phase 4 ceo.approval_requested without a decisionId never appears (it cannot be decided) (CEO-02 empty edge)"
    - "The pending queue is oldest first by requestedAt; equal timestamps keep arrival order, so the order is stable across snapshot and live (CEO-02 ordering edge)"
    - "A request with both a ceo.decision_made and a ceo.approval_expired is shown as expired with the decision attached; both events stay in the log (CEO-05 adjacency edge)"
    - "History lists decided and expired requests newest first, capped at 50; an expired record is resumable until a newer request or a resume request for its task arrives"
    - "An office /ws/browser socket never receives a PRIVATE event, live or in its snapshot, while a /ceo/ws socket receives exactly the ceo.* events"
    - "/ceo/ws refuses a foreign Origin with 403, is guarded by the same requireCeo preValidation as /ceo/api (its 401 paths are tested in 06-05), and its first message is the decisions snapshot"
  artifacts:
    - path: packages/company-core/src/decisions.ts
      provides: "Pure decision projection shared by server snapshot and client live updates"
      exports: ["applyDecisionEvent", "foldDecisions", "pendingQueue", "decisionHistory", "isResumable", "emptyDecisions"]
    - path: apps/api/src/ws/browser-connections.ts
      provides: "Per-socket event filter"
      exports: ["registerBrowserSocket", "acceptsOffice", "acceptsCeo", "broadcastToBrowsers"]
    - path: apps/api/src/routes/ws-browser.ts
      provides: "/ceo/ws snapshot + relay; office snapshot without PRIVATE rows"
      contains: "/ceo/ws"
  key_links:
    - from: "apps/api/src/routes/ws-browser.ts /ceo/ws"
      to: "packages/company-core/src/decisions.ts foldDecisions"
      via: "register (buffering) -> SELECT ceo.* -> snapshot -> flush (CR-03 order)"
      pattern: "foldDecisions\\("
    - from: "apps/api/src/ws/browser-connections.ts broadcastToBrowsers"
      to: "each socket's accepts(event)"
      via: "filter before queue/send"
      pattern: "accepts\\("
  prohibitions:
    - statement: "MUST NOT deliver decision content (title, context, recommendation, tool input, questions, links, notes, diffs) to the office route: no PRIVATE event reaches /ws/browser, live or in the snapshot, and ProjectionState never holds those strings"
      status: resolved
      verification: test
---

<objective>
Build the private read path the dashboard needs and close the privacy leak the tracer left open
(D-08: decision content renders only on /ceo; D-09: diff and context are PRIVATE from day one).
Research Pitfall 2: `broadcastToBrowsers` relays every inserted event verbatim, so today a PRIVATE
decision with its diff would reach the office canvas's WebSocket, the page Phase 7 will stream.

Output: a pure `foldDecisions` projection in company-core (kept out of `reduce()`, which feeds the
office), a per-socket filter so office sockets never see PRIVATE events and CEO sockets see only
`ceo.*`, an office snapshot built from non-PRIVATE rows only, and `/ceo/ws` with the proven
register-before-SELECT snapshot pattern.

Discretion item resolved (CONTEXT "Claude's Discretion", research Open Question 4): the office feed
keeps its Phase 5 `?token=` auth in this phase; Phase 7 rebuilds that route. `/ceo/ws` uses the
Access JWT/cookie from day one and never a query token.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ceo-dashboard-approval-workflow/06-CONTEXT.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-01-SUMMARY.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-13-SUMMARY.md

Tooling: `pnpm` blocked by Device Guard: use `npx --yes pnpm@12.4.2`. Run `pnpm --filter api db:test:up`
before api tests. company-core has no typecheck script; `pnpm --filter web typecheck` and
`pnpm --filter api typecheck` cover it through their imports.
TDD RED evidence: count the RED run's `ok` / `not ok` lines and append the three node:test counters.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: foldDecisions — one projection for the CEO snapshot and every live update</name>

  <read_first>
    - packages/company-core/src/reducer.ts l.285-290 (reduce/fold shape to mirror) and l.129-136 (the ceo.approval_requested handler that must stay untouched)
    - packages/company-core/src/index.ts
    - packages/event-schema/src/payloads/index.ts (ceo.* payloads from 06-01)
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "Queue item", "History tab", "Earlier in this thread"
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md "Pattern 6" and Open Question 5
  </read_first>

  <files>packages/company-core/src/decisions.ts, packages/company-core/src/decisions.test.ts, packages/company-core/src/index.ts</files>

  <behavior>
    - foldDecisions([]) equals emptyDecisions(); pendingQueue of it is []; decisionHistory of it is [].
    - A Phase 4 request { taskId, reason } with no decisionId produces no record.
    - Two pending requests in two threads -> two queue items ordered by requestedAt; equal requestedAt -> arrival order.
    - Thread T with round 1 decided "discuss" and round 2 pending -> one queue item for round 2 with round 2 and earlier = [round 1 record].
    - Request + decision_made -> status "decided", decision { action, note, decidedBy, decidedAt }; + decision_applied -> applied { outcome }.
    - Request + decision_made + approval_expired -> status "expired", decision still attached.
    - 60 closed records -> decisionHistory returns 50, newest first by closed time; foldDecisions(events, { historyLimit: 50 }) keeps every pending record and every record of a pending thread.
    - isResumable is true for an expired record until a ceo.task_resume_requested or a newer request for the same taskId arrives.
    - applyDecisionEvent returns the same state object for any non-ceo event and never mutates its input.
  </behavior>

  <action>
RED first. Create decisions.ts with only `import type` from event-schema (no runtime import, so the root e2e spec can import this file by relative path in 06-14). Export types DecisionRequestView (decisionId, threadId, taskId, agentId from the envelope's sourceAgentId, kind, reason, toolName, toolInput, questions, title, context, recommendation, links, diff, taskTitle, worktreePath, requestedAt), DecisionRecord { request; status: "pending" | "decided" | "expired"; decision?; applied?; expired? }, DecisionsState { records: Record<decisionId, DecisionRecord>; order: string[]; resumeRequestedAt: Record<taskId, string> }, PendingItem { record; round; earlier }. Export emptyDecisions(), applyDecisionEvent(state, event) (pure; ignores non-ceo events and requests without decisionId; expired wins the status over decided but keeps the decision), foldDecisions(events, { historyLimit = 50 } = {}) (fold, then drop closed records beyond the newest historyLimit unless they belong to a thread that still has a pending record: bounds the snapshot, since each request can carry a 64 KiB diff), pendingQueue(state), decisionHistory(state, limit = 50), isResumable(state, record). Tie-breaks always use the index in `order`. Do not touch reducer.ts or ProjectionState (research anti-pattern: /ws/browser sends ProjectionState to the office). Export all from company-core/src/index.ts.
  </action>

  <verify>
    <automated>pnpm --filter company-core test -- decisions</automated>
    <fails_when>non-zero exit, or "No test files found"</fails_when>
  </verify>

  <acceptance_criteria>
    - decisions.test.ts contains one test per behaviour bullet, including the equal-timestamp arrival-order case and the decided-plus-expired case asserting `status` equals "expired" and `decision.action` is present
    - `grep -c "^import type" packages/company-core/src/decisions.ts` is at least 1 and `grep -c "^import {" packages/company-core/src/decisions.ts` returns 0
  </acceptance_criteria>

  <done>One pure projection turns ceo.* events into the queue and history the dashboard shows, identically on the server and in the browser.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the office feed never carries PRIVATE events, live or in its snapshot</name>

  <read_first>
    - apps/api/src/ws/browser-connections.ts and browser-connections.test.ts
    - apps/api/src/routes/ws-browser.ts and ws-browser.test.ts (snapshot + relay tests, collectMessages helper)
    - packages/company-core/src/reducer.test.ts (style)
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md Pitfall 2
  </read_first>

  <files>apps/api/src/ws/browser-connections.ts, apps/api/src/ws/browser-connections.test.ts, apps/api/src/routes/ws-browser.ts, apps/api/src/routes/ws-browser.test.ts, packages/company-core/src/reducer.test.ts</files>

  <behavior>
    - browser-connections: a socket registered with acceptsOffice gets an INTERNAL event and not a PRIVATE one; a socket registered with acceptsCeo gets ceo.* events only; a non-event message (for example a future control frame) goes to both; the CR-03 buffering order is unchanged.
    - ws-browser: after POST /events of a PRIVATE ceo.approval_requested (title "SECRET-TITLE-123") followed by an INTERNAL task.status_changed, the next message an office socket receives is the task.status_changed.
    - ws-browser: an office socket connecting after that PRIVATE row exists gets a snapshot whose JSON contains none of the request's private strings.
    - reducer: fold over events including PRIVATE ceo.approval_requested payloads with unique title/context/toolInput/diff strings -> JSON.stringify(state) contains none of them.
  </behavior>

  <action>
RED first. Change the browserSockets map value to { queue: string[] | null; accepts: (event: { type: string; visibility: string }) => boolean }. registerBrowserSocket(socket, accepts = acceptsOffice). Export acceptsOffice = (e) => e.visibility !== "PRIVATE" (the first step of Phase 7's visibility filtering) and acceptsCeo = (e) => e.type.startsWith("ceo."). broadcastToBrowsers(message) keeps its signature: when message is { type: "event", event } it skips sockets whose accepts(event) is false; the JSON string is still built once. flushBrowserSocket and unregisterBrowserSocket keep their behaviour. In ws-browser.ts build the office snapshot from rows whose visibility is not "PRIVATE" (the snapshot and live filters must match, the 05-09 same-code rule). Add the tests above; assert by equality (the next message equals the INTERNAL event; the list of private strings found in the snapshot JSON equals []).
  </action>

  <verify>
    <automated>pnpm --filter api test -- browser-connections ws-browser</automated>
    <fails_when>non-zero exit, or "No test files found"</fails_when>
    <automated>pnpm --filter company-core test -- reducer</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>

  <acceptance_criteria>
    - ws-browser.test.ts asserts the first message after the PRIVATE post equals the INTERNAL task.status_changed event (by `toEqual` on type and id)
    - reducer.test.ts asserts `privateStrings.filter((s) => json.includes(s))` equals `[]`
    - `grep -n "acceptsOffice" apps/api/src/ws/browser-connections.ts` returns a match
  </acceptance_criteria>

  <done>Nothing PRIVATE can reach the office canvas's socket, closing the tracer's interim leak before anything is deployed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /ceo/ws — the private, authenticated decisions feed</name>

  <read_first>
    - apps/api/src/routes/ws-browser.ts (the /ws/browser handler to mirror, CR-03 comments)
    - apps/api/src/auth/csrf.ts (requireOrigin) and apps/api/src/auth/ceo-auth.ts (requireCeo) from 06-13
    - apps/api/src/db/event-row.ts (rowToCompanyEvent)
    - apps/api/src/routes/ws-browser.test.ts (harness; env header)
  </read_first>

  <files>apps/api/src/routes/ws-browser.ts, apps/api/src/routes/ws-browser.test.ts</files>

  <behavior>
    - Upgrade to /ceo/ws with Origin http://evil.test -> rejected with 403.
    - Upgrade with an allowlisted Origin from loopback with the dev bypass -> first message { type: "snapshot", state } where state equals foldDecisions of the stored ceo.* rows.
    - After the snapshot, POST /events of a new PRIVATE ceo.approval_requested -> the CEO socket receives { type: "event", event } for it; a concurrently connected office socket receives nothing for it.
    - POST /ceo/api/decisions for a pending request -> the CEO socket receives the ceo.decision_made event.
  </behavior>

  <action>
RED first. Register GET /ceo/ws inside registerWsBrowserRoute (it already owns the browser-socket registry and snapshot pattern, and server.ts already registers it): websocket true, preValidation [requireOrigin, requireCeo] (browsers always send Origin on a WebSocket upgrade and cannot set custom headers, so Origin is the D-12 check here), config rateLimit max 20 per minute. Handler: registerBrowserSocket(socket, acceptsCeo); SELECT events where type LIKE 'ceo.%' ordered by occurredAt; send { type: "snapshot", state: foldDecisions(rows.map(rowToCompanyEvent)) }; flushBrowserSocket; same catch/close/error handling as /ws/browser. No query-string token (Pitfall 8). In ws-browser.test.ts add CEO_DEV_AUTH_BYPASS=1 and CEO_ALLOWED_ORIGINS=http://localhost:5173 to the env header and pass `{ origin }` to the ws client for /ceo/ws.
  </action>

  <verify>
    <automated>pnpm --filter api test -- ws-browser</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>pnpm --filter api typecheck</automated>
    <fails_when>non-zero exit or an "error TS" line</fails_when>
  </verify>

  <acceptance_criteria>
    - ws-browser.test.ts asserts a 403 status for the foreign-Origin upgrade
    - ws-browser.test.ts asserts the /ceo/ws snapshot `state` equals `foldDecisions(...)` of the same rows (toEqual)
    - `grep -n '"/ceo/ws"' apps/api/src/routes/ws-browser.ts` returns a match
  </acceptance_criteria>

  <done>The dashboard has an authenticated, Origin-checked live feed of decisions that starts with a complete snapshot and never leaks to the office.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> office browser socket | INTERNAL-tier consumer; must never see PRIVATE |
| API -> CEO browser socket | PRIVATE consumer behind Origin + requireCeo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06-01 | Info disclosure | office relay | high | mitigate | acceptsOffice filter per socket; tests on live + snapshot |
| T-06-06-02 | Info disclosure | ProjectionState | high | mitigate | decisions projected separately; reducer privacy guard test |
| T-06-06-03 | Spoofing | /ceo/ws cross-site upgrade | high | mitigate | requireOrigin (exact allowlist) + requireCeo before upgrade |
| T-06-06-04 | Info disclosure | token in URL | medium | mitigate | no query token on /ceo/ws; Access cookie/JWT only |
| T-06-06-05 | Denial of service | snapshot size | low | mitigate | historyLimit 50 prunes closed records; rateLimit 20/min |
</threat_model>

<verification>
- `pnpm --filter company-core test`, `pnpm --filter api test -- browser-connections ws-browser`, `pnpm --filter api typecheck`, `pnpm --filter web typecheck` green.
</verification>

<success_criteria>
- The dashboard's data source exists and is private; the office source provably carries no PRIVATE content.
</success_criteria>

## Artifacts this phase produces

- `packages/company-core/src/decisions.ts`: `DecisionRequestView`, `DecisionRecord`, `DecisionsState`, `PendingItem`, `emptyDecisions`, `applyDecisionEvent`, `foldDecisions`, `pendingQueue`, `decisionHistory`, `isResumable`
- `apps/api/src/ws/browser-connections.ts`: `acceptsOffice`, `acceptsCeo`, `registerBrowserSocket(socket, accepts)`
- Route `GET /ceo/ws` (WebSocket)

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-06-SUMMARY.md` when done
</output>
