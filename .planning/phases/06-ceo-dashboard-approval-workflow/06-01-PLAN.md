---
phase: 06-ceo-dashboard-approval-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/event-schema/src/payloads/index.ts
  - packages/event-schema/src/payloads/index.test.ts
  - packages/event-schema/src/downlink.ts
  - packages/event-schema/src/index.ts
  - packages/claude-adapter/src/decision-mapping.ts
  - packages/claude-adapter/src/decision-mapping.test.ts
  - packages/claude-adapter/src/claude-code-runtime.ts
  - packages/claude-adapter/src/claude-code-runtime.test.ts
  - packages/claude-adapter/src/index.ts
  - apps/worker/src/decisions.ts
  - apps/worker/src/decisions.test.ts
autonomous: true
requirements: [CEO-03, CEO-04, CEO-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A decision frame that WorkerDownlinkSchema accepts, delivered to the worker broker as the raw WebSocket string, resolves the matching parked canUseTool promise exactly once; a repeat, an unknown decisionId, malformed JSON and any other message type change nothing"
    - "Approve returns { behavior: 'allow', updatedInput } where updatedInput is the exact input object the runtime parked (or, for AskUserQuestion, the parked questions plus validated answers); no field arriving over the wire can change what runs"
    - "Reject, Request Changes, More Research and Discuss return { behavior: 'deny' } with the message `[CEO:REJECT]`, `[CEO:REQUEST_CHANGES] <note>`, `[CEO:MORE_RESEARCH] <note>`, `[CEO:DISCUSS] <note>` (note trimmed, one space after the prefix; Reject with no note is the bare prefix)"
    - "Before parking, the runtime posts a PRIVATE ceo.approval_requested carrying a uuid decisionId; after it returns the result to the SDK it posts a PRIVATE ceo.decision_applied with outcome allowed|denied (the worker half of the D-04 audit chain; 06-13 appends ceo.decision_made on the control plane)"
    - "A tool input over 16000 characters is denied without parking, so nothing is approvable that the CEO could not see in full"
    - "Without an injected awaitDecision the Phase 4 path (requestReview + deny) is unchanged"
    - "A stored Phase 4 ceo.approval_requested row { taskId, reason } still parses after the schema change"
  artifacts:
    - path: packages/event-schema/src/payloads/index.ts
      provides: "Additive ceo.approval_requested fields + 4 appended ceo.* union members"
      contains: "ceo.decision_made"
    - path: packages/event-schema/src/downlink.ts
      provides: "Wire contract for control plane <-> worker messages"
      exports: ["WorkerDownlinkSchema", "WorkerUplinkSchema", "DecisionActionSchema"]
    - path: packages/claude-adapter/src/decision-mapping.ts
      provides: "Pure CEO decision -> SDK PermissionResult mapping"
      exports: ["toPermissionResult", "validateAnswers", "CEO_PREFIX", "CEO_PROTOCOL_APPEND"]
    - path: apps/worker/src/decisions.ts
      provides: "Worker pending-decision broker"
      exports: ["createDecisionBroker"]
  key_links:
    - from: "apps/worker/src/decisions.ts"
      to: "packages/event-schema/src/downlink.ts"
      via: "WorkerDownlinkSchema.safeParse on every inbound frame"
      pattern: "WorkerDownlinkSchema.safeParse"
    - from: "packages/claude-adapter/src/claude-code-runtime.ts"
      to: "packages/claude-adapter/src/decision-mapping.ts"
      via: "canUseTool awaits options.awaitDecision then returns toPermissionResult(decision, parked)"
      pattern: "toPermissionResult\\("
  prohibitions:
    - statement: "MUST NOT approve any tool call other than the exact call the runtime parked and showed the CEO: approve never takes updatedInput, a command, or tool input from the wire, the API, or the dashboard"
      status: resolved
      verification: test
---

<objective>
Tracer for Phase 6, narrowed to the costly commitment: prove the parked-promise half of the approval
round trip before anything is layered on it. A classified tool call parks inside `canUseTool` (D-01)
after posting a PRIVATE `ceo.approval_requested` with a `decisionId`; a decision frame arriving on
the worker's WebSocket, in the exact shape the shared `WorkerDownlinkSchema` defines, is resolved by
the worker's broker; and the runtime returns the SDK permission result: allow with the parked input,
or deny with the typed CEO prefix (D-05). Then the runtime appends `ceo.decision_applied`. The wire
contract and the event contract are the fixed interface the control-plane half (06-13) plugs into.

Purpose: D-01 is the costly one-way-ish commitment of this phase (a live parked promise). Proving it
first, on the same wire schema the control plane will send, means every later plan (control-plane
route, hardening, context, feed, dashboard, office room) builds on a slice that already works.
Output: the full additive event contract, the wire contract, the pure decision mapping, the parked
`canUseTool`, and the worker broker.

Not in this plan (later plans own them): the control-plane half (worker-socket registry, server-side
workerId stamping, the CSRF-guarded dev-bypass decision route, per-action note rules at the API) is
06-13; Access JWT verification and one-decision-per-request (06-05); PreToolUse "ask" hook, watchdog
suspension and abort handling (06-02); request context/diff (06-03); worker hosting the runtime and
restart expiry (06-04); the office-feed PRIVATE filter (06-06). Interim note: until 06-06 lands,
`broadcastToBrowsers` still relays every inserted event, including PRIVATE `ceo.*`, to `/ws/browser`.
Nothing is deployed before 06-12, and 06-06 closes it with tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-CONTEXT.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-PATTERNS.md

Tooling notes carried from earlier phases:
- `pnpm` via the global shim may be blocked by Device Guard in a Bash shell. If a `pnpm ...` verify
  command fails with a Device Guard / execution-policy error, run the same arguments through
  `npx --yes pnpm@12.4.2 ...` and record it as a deviation.
- TDD RED evidence in this repo: `check tdd-red-evidence` cannot parse vitest output. Count the RED
  run's own `ok` / `not ok` lines and append `# tests N`, `# pass P`, `# fail F` from that run.
  Invent no number.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): a decision frame on the worker's WebSocket resolves the parked tool call into the exact SDK result</name>

  <reversibility rating="costly">D-01 (carried from CONTEXT): the runtime, worker broker, downlink message and dashboard all assume a live parked promise; moving to the SDK defer hook later means a resume loop plus decision re-delivery. The appended ceo.* event shapes are also re-validated on every snapshot, so later changes must stay additive.</reversibility>

  <read_first>
    - packages/claude-adapter/src/claude-code-runtime.ts (whole file; canUseTool at l.195-207, emitStatus l.82-89, requestReview l.96-105, runQuery ownership token l.137-168)
    - packages/claude-adapter/src/claude-code-runtime.test.ts l.1-130 (vi.mock of the SDK and event-emitter, pausableQuery/hangingQuery fakes) and l.256-312 (Test 4/5, which must stay green)
    - packages/claude-adapter/src/signal-detection.ts (ClassifiedSignal.kind values, quoted verbatim into the payload enum)
    - packages/claude-adapter/src/event-emitter.ts (buildEnvelope(companyId, type, payload, taskId, visibility, sourceAgentId))
    - packages/event-schema/src/payloads/index.ts (l.19 CeoApprovalRequestedPayload, l.84-107 union; append-only rule)
    - packages/event-schema/src/payloads/index.test.ts (existing test style)
    - packages/event-schema/src/index.ts
    - apps/worker/src/ws-client.ts (how raw frames arrive; the broker takes the raw string) and apps/worker/src/event-emitter.ts
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md sections "Pattern 1", "Pattern 2", "Pattern 4", "Code Examples"
    - packages/claude-adapter/node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts l.203-297 (CanUseTool options: signal, toolUseID) and l.2405-2417 (PermissionResult)
  </read_first>

  <files>packages/event-schema/src/payloads/index.ts, packages/event-schema/src/payloads/index.test.ts, packages/event-schema/src/downlink.ts, packages/event-schema/src/index.ts, packages/claude-adapter/src/decision-mapping.ts, packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts, packages/claude-adapter/src/index.ts, apps/worker/src/decisions.ts, apps/worker/src/decisions.test.ts</files>

  <behavior>
    - Schema: a Phase 4 row `{ taskId, reason }` still parses as ceo.approval_requested; an enriched payload with decisionId/threadId/kind/toolName/toolInput/questions/title/context/recommendation/links/diff/sessionId/worktreePath/workerBootId/workerId/taskTitle parses; a toolInput over 16000 chars is rejected; each of ceo.decision_made, ceo.decision_applied, ceo.approval_expired, ceo.task_resume_requested parses with its required fields and rejects a non-email decidedBy.
    - Wire: WorkerDownlinkSchema accepts `{type:"decision", decisionId, action}` and `{type:"task.resume", taskId, sessionId, worktreePath, agentId}`, rejects any other type and any `updatedInput`-bearing shape is stripped (plain z.object).
    - Runtime: with `awaitDecision` injected, canUseTool("Bash", {command:"npm publish"}, {signal, toolUseID}) posts task.status_changed waiting_for_review then a PRIVATE ceo.approval_requested carrying a uuid decisionId, kind "ceo_gated_tool", toolName "Bash", toolInput = JSON of the input, sourceAgentId = the task's agentId; resolving approve returns `{behavior:"allow", updatedInput}` whose value is the SAME object reference passed in; a decision object carrying an extra `updatedInput` or `input` field changes nothing; then task.status_changed running and PRIVATE ceo.decision_applied {decisionId, taskId, action:"approve", outcome:"allowed"} are posted.
    - Runtime: resolving reject returns `{behavior:"deny", message:"[CEO:REJECT]"}` and decision_applied outcome "denied".
    - Runtime: a tool input whose JSON is over 16000 characters returns the too-large deny without calling awaitDecision.
    - Runtime: with no awaitDecision injected, Phase 4 behaviour is unchanged (existing Test 4 still passes: requestReview + deny).
    - Worker broker (the tracer's far end): a frame string built with JSON.stringify of a WorkerDownlinkSchema-parsed `{type:"decision", decisionId, action:"approve"}` passed to handleDownlink resolves the matching awaitDecision exactly once with `{ action: "approve" }`; a second delivery, an unknown decisionId, malformed JSON, a `task.resume` frame and an unknown type are ignored; a frame carrying an extra `updatedInput` resolves with no such key; aborting the signal rejects that promise and removes it; pendingCount reflects parked calls.
  </behavior>

  <action>
Write the failing tests first (RED), then implement. Record RED evidence per the context note.

Event contract (packages/event-schema; D-04: these append-only events are the audit log, no separate audit table). In payloads/index.ts extend CeoApprovalRequestedPayload with OPTIONAL fields only (stored Phase 4 rows carry just taskId and reason and are re-validated by rowToCompanyEvent on every snapshot): decisionId (uuid), threadId (uuid), kind (enum "clarifying_question" | "ceo_gated_tool", quoted from ClassifiedSignal), toolName (max 200), toolInput (max 16000), questions (max 4 of { question max 2000, header max 64, multiSelect boolean, options max 4 of { label max 500, description max 2000, preview max 16000 optional } }), title (300), context (8000), recommendation (4000), links (max 10 strings of max 2000), diff ({ files max 500 of { path max 1000, added int, removed int }, unified max 65536, truncated boolean, totalAdded int, totalRemoved int }), sessionId (200), worktreePath (1000), workerBootId (uuid), workerId (200, stamped server-side), taskTitle (300). Add CeoDecisionMadePayload { decisionId uuid, taskId, action DecisionActionSchema, note max 4000 optional, answers optional record of max-2000 strings keyed by max-2000 strings, decidedBy email }, CeoDecisionAppliedPayload { decisionId uuid, taskId, action, outcome enum "allowed" | "denied" }, CeoApprovalExpiredPayload { decisionId uuid, taskId, reason enum "worker_restarted" | "aborted" | "superseded" }, CeoTaskResumeRequestedPayload { taskId, decidedBy email }. Append, in this order after agent.handoff_completed, never reordering anything: ceo.decision_made, ceo.decision_applied, ceo.approval_expired, ceo.task_resume_requested (union 17 to 21), each a plain z.object in the l.93 spread form. Update the union-size comment.

Create packages/event-schema/src/downlink.ts: DecisionActionSchema = z.enum(["approve","reject","request_changes","more_research","discuss"]) (payloads/index.ts imports it from here); WorkerDownlinkSchema = discriminatedUnion on type of { type "decision", decisionId uuid, action, note max 4000 optional, answers optional record } and { type "task.resume", taskId, sessionId max 200, worktreePath max 1000, agentId max 200 }; WorkerUplinkSchema = { type "hello", bootId uuid }. Export the three schemas plus inferred types DecisionAction, WorkerDownlink, WorkerUplink from event-schema/src/index.ts. The control plane can only send these two message types: no message carries a prompt, a command or tool input (SEC-03).

Decision mapping (packages/claude-adapter/src/decision-mapping.ts, pure, no I/O, signal-detection.ts style). Export interface CeoDecision { action: DecisionAction; note?: string; answers?: Record<string,string> }, interface ParkedCall { decisionId; toolName; input: Record<string, unknown>; kind: ClassifiedSignal["kind"] }, CEO_PREFIX = { reject: "[CEO:REJECT]", request_changes: "[CEO:REQUEST_CHANGES]", more_research: "[CEO:MORE_RESEARCH]", discuss: "[CEO:DISCUSS]" }, and toPermissionResult(decision, parked): PermissionResult (type-only import from the SDK). In this task implement approve for non-AskUserQuestion (allow with updatedInput set to parked.input, the same reference) and every other action as deny with the prefix (note handling and AskUserQuestion answers come in Task 2). The decision object is read only for action/note/answers, per D-01 and the prohibition above.

Runtime (claude-code-runtime.ts). Extend the options with awaitDecision?: (decisionId: string, signal: AbortSignal) => Promise<CeoDecision>. Rename the local classification variable from signal to cls (the SDK's third canUseTool argument is { signal, toolUseID }). Keep the superseded first line verbatim. When cls is null, allow as today. When awaitDecision is absent, keep the Phase 4 requestReview + deny path unchanged. Otherwise (D-01): decisionId = randomUUID(); set record.status waiting_for_review and emitStatus waiting_for_review; post ceo.approval_requested with visibility "PRIVATE", sourceAgentId = the task's agentId, payload { taskId, reason: cls.reason, decisionId, threadId: decisionId, kind: cls.kind, toolName, toolInput: JSON.stringify(input) }; if that JSON exceeds 16000 characters, do not park: return deny with the message "Tool input is too large to show the CEO in full (over 16000 characters); split the operation into smaller steps." (fail closed, nothing approvable that the CEO could not see); for AskUserQuestion also copy input.questions into payload.questions. Then await options.awaitDecision(decisionId, signal). If the await throws, return deny "No CEO decision was received; the tool call was not run." (06-02 adds the expiry event). If !isCurrent() after the await, return the superseded deny. Otherwise result = toPermissionResult(decision, parked); record.status = "running"; emitStatus running (Pitfall 3: this is what walks the office agent out of the CEO room); post PRIVATE ceo.decision_applied { decisionId, taskId, action, outcome: result.behavior === "allow" ? "allowed" : "denied" }; return result. Make the legacy requestReview's ceo.approval_requested PRIVATE as well (every ceo.* event is PRIVATE). Export the CeoDecision type and toPermissionResult/CEO_PREFIX from packages/claude-adapter/src/index.ts.

Worker broker (apps/worker/src/decisions.ts, factory not class). createDecisionBroker() returns { bootId: randomUUID(), awaitDecision(decisionId, signal), handleDownlink(raw: string), pendingCount() }. Pending is a Map<decisionId, { resolve, reject }>. handleDownlink: JSON.parse in try/catch, WorkerDownlinkSchema.safeParse, act only on type "decision" whose decisionId is pending: delete then resolve with { action, note, answers } built from the parsed fields only, omitting undefined keys (single resolve). Everything else is ignored silently (task.resume is wired in 06-04, which also connects handleDownlink to the ws-client message handler). awaitDecision rejects immediately if the signal is already aborted, and on abort removes the entry and rejects. The broker never accepts an updatedInput: the schema has no such field. The resolved value's type is structurally the CeoDecision shape, typed from the event-schema DecisionAction (apps/worker does not depend on claude-adapter until 06-04).

Tests: extend payloads/index.test.ts and claude-code-runtime.test.ts (new describe "parked canUseTool (D-01)"), and create apps/worker/src/decisions.test.ts. The runtime test's injected awaitDecision returns the decision exactly as the broker would: the fields of a WorkerDownlinkSchema-parsed frame, so both sides of the tracer are tested against the same wire schema. Assert properties by equality, not absence. decision-mapping.test.ts is created in Task 2 with the note rules; this task's mapping is covered through the runtime test.
  </action>

  <verify>
    <automated>pnpm --filter event-schema test -- payloads</automated>
    <fails_when>non-zero exit, or "No test files found" in the output</fails_when>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or any line starting with "FAIL", or "No test files found"</fails_when>
    <automated>pnpm --filter worker test -- decisions</automated>
    <fails_when>non-zero exit, or "No test files found"</fails_when>
    <automated>pnpm --filter event-schema typecheck && pnpm --filter worker typecheck && pnpm --filter claude-adapter typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter worker test -- decisions` passes a test that builds the frame with `JSON.stringify(WorkerDownlinkSchema.parse({ type: "decision", decisionId, action: "approve" }))`, passes it to handleDownlink, and asserts the awaited value equals `{ action: "approve" }`; plus tests for single-resolve, unknown-id ignored, malformed JSON ignored, task.resume ignored, and abort rejects
    - `pnpm --filter claude-adapter test -- claude-code-runtime` passes a test asserting `result.updatedInput` is the identical object (`toBe`) passed to canUseTool when the decision is approve, a test asserting the reject result equals `{ behavior: "deny", message: "[CEO:REJECT]" }`, and the existing "Test 4" still passes unchanged
    - The same file asserts the posted ceo.decision_applied payload equals `{ decisionId, taskId, action: "approve", outcome: "allowed" }` with visibility "PRIVATE"
    - `grep -c '"ceo.task_resume_requested"' packages/event-schema/src/payloads/index.ts` returns 1 and the four new literals appear after `"agent.handoff_completed"` in file order
  </acceptance_criteria>

  <done>A decision frame in the shared wire shape, arriving at the worker, turns into the exact SDK permission result for the parked call and a ceo.decision_applied record; Phase 4 behaviour without a decision channel is unchanged. 06-13 connects the control plane to this frame.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: all five CEO actions at the runtime — notes (D-05/D-07), question answers, and the protocol the agent reads</name>

  <read_first>
    - packages/claude-adapter/src/decision-mapping.ts (as left by Task 1)
    - packages/claude-adapter/node_modules/@anthropic-ai/claude-agent-sdk/sdk-tools.d.ts l.1102-1262 (AskUserQuestionInput, multiSelect) and l.3905-3935 (answers keyed by question text, multi-select comma-separated)
    - packages/claude-adapter/node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts l.2205-2305 (systemPrompt preset + append)
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md "Messages sent to the agent" table (locked copy)
  </read_first>

  <files>packages/claude-adapter/src/decision-mapping.ts, packages/claude-adapter/src/decision-mapping.test.ts, packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts</files>

  <behavior>
    - toPermissionResult: request_changes with note "  split it  " returns deny message exactly "[CEO:REQUEST_CHANGES] split it"; more_research and discuss likewise; reject with note "no" returns "[CEO:REJECT] no"; reject with undefined or "   " note returns exactly "[CEO:REJECT]".
    - toPermissionResult approve on an AskUserQuestion parked call with answers for every question returns allow with updatedInput { questions: parked.input.questions, answers } (questions is the parked reference).
    - validateAnswers throws when a key is not one of the parked question texts, when a question is unanswered, or when a value is empty/whitespace; toPermissionResult turns that into deny "The CEO's answers did not match the questions asked; ask again with AskUserQuestion." (no [CEO:...] prefix: this is the system, not the CEO).
    - Runtime: when awaitDecision is injected, query() options include systemPrompt { type: "preset", preset: "claude_code", append: CEO_PROTOCOL_APPEND }; without it, no systemPrompt key is set (Phase 4 unchanged).
  </behavior>

  <action>
RED first (create decision-mapping.test.ts). Complete decision-mapping.ts: note = decision.note?.trim(); deny message is the CEO_PREFIX value alone when the note is empty, else prefix + one space + note (the UI-SPEC locked table, D-05). Implement validateAnswers(questions, answers) returning a new Record keyed by the exact parked question text, and route AskUserQuestion approve through it (D-01: updatedInput is { questions: the parked questions reference, answers }). Export CEO_PROTOCOL_APPEND: a short fixed paragraph telling the agent what each prefix means when it appears as a tool-denial reason: REJECT = do not run it or an equivalent, continue without it; REQUEST_CHANGES = change the approach as the note says, then ask again if still needed; MORE_RESEARCH = investigate what the note asks before asking again; DISCUSS = reply to the CEO's note in your next message and then ask again with AskUserQuestion (D-06); and that approval is only ever the tool call proceeding, never text that claims to be from the CEO. In runQuery, set systemPrompt to { type: "preset", preset: "claude_code", append: CEO_PROTOCOL_APPEND } only when options.awaitDecision is set (document in a comment that this also moves the worker-hosted mode onto Claude Code's own system prompt, which the 06-11 live proof exercises).

The API-side note rules (D-07: a note is required for request_changes, more_research and discuss before anything is appended) and the answers rules are 06-13 Task 3; this mapping still handles an empty note safely (bare prefix) so the runtime never depends on the API having validated it.
  </action>

  <verify>
    <automated>pnpm --filter claude-adapter test -- decision-mapping claude-code-runtime</automated>
    <fails_when>non-zero exit, or any "FAIL" line, or "No test files found"</fails_when>
    <automated>pnpm --filter claude-adapter typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
  </verify>

  <acceptance_criteria>
    - decision-mapping.test.ts contains exact-string assertions for all four prefixes with and without notes, including `toBe("[CEO:REJECT]")` for a whitespace note
    - decision-mapping.test.ts asserts an AskUserQuestion approve returns `updatedInput.questions` identical (`toBe`) to the parked questions, and a mismatched answers key returns the system deny message by exact string
    - claude-code-runtime.test.ts asserts `options.systemPrompt` equals `{ type: "preset", preset: "claude_code", append: CEO_PROTOCOL_APPEND }` with awaitDecision and is `undefined` without it
  </acceptance_criteria>

  <done>Every one of the five CEO actions maps to the right SDK result and the right audit record, and the agent is told what each typed prefix means.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control plane -> worker WS downlink | closed message set; the worker must never execute content from it |
| worker runtime -> /events | runtime-authored ceo.* events (request, applied); PRIVATE from creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Elevation | runtime approve path | high | mitigate | toPermissionResult returns parked.input by reference; downlink schema has no input field; test asserts `toBe` identity |
| T-06-01-06 | Elevation | downlink (SEC-03) | high | mitigate | WorkerDownlinkSchema closed to decision and task.resume; no prompt/command field; broker builds the decision from parsed fields only and ignores anything else; test with an extra updatedInput key |
| T-06-01-07 | Info disclosure | ceo.* relay to /ws/browser | medium | accept | Interim until 06-06 (same phase, before any deploy); 06-06 adds the per-socket filter with tests |
| T-06-01-09 | Tampering | oversized tool input | medium | mitigate | inputs over 16000 chars denied without parking, so nothing is approvable unseen |
| T-06-01-10 | Repudiation | decision outcome | low | mitigate | runtime posts PRIVATE ceo.decision_applied with allowed/denied after every applied decision; equality test |

Control-plane threats (decision route auth, CSRF, the /events allowlist, workerId stamping, rate limit) moved with that code to 06-13.
</threat_model>

<verification>
- All Task 1 and Task 2 verify commands green.
- `pnpm --filter event-schema test`, `pnpm --filter company-core test` and `pnpm --filter web test` still green (the union grew; replay of stored rows must still parse).
</verification>

<success_criteria>
- The worker half of the round trip (wire frame to broker to runtime to SDK result) works and is tested against the same shared wire schema the control plane will send in 06-13.
- Approve can only ever allow the parked call; every other action denies with the locked prefix.
- ceo.approval_requested and ceo.decision_applied are posted PRIVATE for every parked call.
</success_criteria>

## Artifacts this phase produces

- `packages/event-schema/src/downlink.ts`: `DecisionActionSchema`, `WorkerDownlinkSchema`, `WorkerUplinkSchema`, types `DecisionAction`, `WorkerDownlink`, `WorkerUplink`
- Event members `ceo.decision_made`, `ceo.decision_applied`, `ceo.approval_expired`, `ceo.task_resume_requested`; optional `ceo.approval_requested` fields `decisionId`, `threadId`, `kind`, `toolName`, `toolInput`, `questions`, `title`, `context`, `recommendation`, `links`, `diff`, `sessionId`, `worktreePath`, `workerBootId`, `workerId`, `taskTitle`
- `packages/claude-adapter/src/decision-mapping.ts`: `CeoDecision`, `ParkedCall`, `CEO_PREFIX`, `CEO_PROTOCOL_APPEND`, `toPermissionResult`, `validateAnswers`
- `createClaudeCodeRuntime` option `awaitDecision`
- `apps/worker/src/decisions.ts`: `createDecisionBroker` (`bootId`, `awaitDecision`, `handleDownlink`, `pendingCount`)

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-01-SUMMARY.md` when done
</output>
