---
phase: 06-ceo-dashboard-approval-workflow
plan: 05
type: execute
wave: 3
depends_on: ["06-13"]
files_modified:
  - apps/api/drizzle/0004_ceo_decision_once.sql
  - apps/api/src/routes/events.ts
  - apps/api/src/routes/ceo.ts
  - apps/api/src/routes/ceo-decisions.test.ts
  - apps/api/src/auth/ceo-auth.ts
  - apps/api/src/auth/ceo-auth.test.ts
  - apps/api/src/env.ts
  - apps/api/src/server.ts
  - apps/api/Dockerfile
  - apps/api/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [CEO-04, CEO-05, CEO-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The database allows at most one ceo.decision_made, one ceo.decision_applied and one ceo.approval_expired per decisionId (partial unique index), so two concurrent decisions on the same request produce exactly one 202 and one 409 (CEO-03 adjacency edge)"
    - "A decision on a request that already expired returns 409 and appends nothing"
    - "For every decided request the audit chain reads ceo.approval_requested -> ceo.decision_made -> ceo.decision_applied, each exactly once for that decisionId, in occurred_at order (CEO-05 ordering edge)"
    - "A worker cannot post ceo.decision_applied or ceo.approval_expired for a decision stamped with another worker's id (403); a retried duplicate from the owning worker is accepted without a 500"
    - "In production the decider identity is the email in a Cloudflare Access JWT verified with jose against the team JWKS (RS256, issuer = team domain, audience = the Access AUD), taken from the Cf-Access-Jwt-Assertion header or the CF_Authorization cookie, and it must equal CEO_EMAIL; anything else is 401 (D-11)"
    - "With the Access settings missing and the dev bypass off, every /ceo/api request is 401 (fail closed)"
    - "GET /ceo/api/me returns the verified email and whether the dev bypass is active"
    - "The Access JWT header and the Cookie header are redacted from request logs, and the API image runs with NODE_ENV=production so the dev bypass cannot boot there"
  artifacts:
    - path: apps/api/drizzle/0004_ceo_decision_once.sql
      provides: "One decision per request, enforced by Postgres"
      contains: "events_ceo_decision_once"
    - path: apps/api/src/auth/ceo-auth.ts
      provides: "Access JWT verification inside requireCeo"
      exports: ["requireCeo", "makeAccessVerifier", "_setAccessVerifierForTests"]
    - path: apps/api/src/routes/ceo.ts
      provides: "409 on repeat/expired, GET /ceo/api/me"
      contains: "/ceo/api/me"
  key_links:
    - from: "apps/api/src/routes/ceo.ts decision insert"
      to: "apps/api/drizzle/0004_ceo_decision_once.sql"
      via: "bare onConflictDoNothing(); zero returned rows maps to 409"
      pattern: "onConflictDoNothing\\(\\)"
    - from: "apps/api/src/auth/ceo-auth.ts requireCeo"
      to: "jose jwtVerify"
      via: "issuer, audience, algorithms RS256; email claim compared to CEO_EMAIL"
      pattern: "jwtVerify\\("
  prohibitions:
    - statement: "MUST NOT let BROWSER_ACCESS_TOKEN, a worker credential, or a service token without an email claim authorize a CEO decision or a resume"
      status: resolved
      verification: test
    - statement: "MUST NOT rewrite or delete audit history: decisions are append-only events and a request can be decided only once"
      status: resolved
      verification: test
---

<objective>
Make the decision route trustworthy for real use: exactly one decision per request enforced by the
database (CEO-05, CEO-03; D-04's event log stays the only audit record), and the real CEO identity from Cloudflare Access (D-11) instead of
06-13's dev-only path.

Output: migration 0004, 409 semantics, a worker-ownership check on worker-posted ceo.* events, the
jose-backed Access verifier inside `requireCeo`, `GET /ceo/api/me`, log redaction and the production
NODE_ENV guard.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ceo-dashboard-approval-workflow/06-CONTEXT.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md
@.planning/phases/06-ceo-dashboard-approval-workflow/06-13-SUMMARY.md

Package legitimacy (RESEARCH "Package Legitimacy Audit"): `jose` latest (6.2.12) is flagged SUS only
for being under three weeks old. This plan pins the prior release `jose@6.2.10` (published
2026-08-21, same maintainer panva, repo github.com/panva/jose, named in Cloudflare's own JWT page),
which the research lists as the non-checkpoint resolution. No other package is installed here.

Tooling: `pnpm` blocked by Device Guard: use `npx --yes pnpm@12.4.2`. The api tests need
`pnpm --filter api db:test:up` first. Test files set process.env and then dynamically import.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: one decision per request, enforced by Postgres (CEO-05) [BLOCKING schema push]</name>

  <read_first>
    - apps/api/drizzle/0003_no_truncate_trigger.sql (comment-then-statement style)
    - apps/api/scripts/migrate.mjs (applies every .sql in order; skips 42P07/42710)
    - apps/api/src/routes/events.ts and apps/api/src/routes/ceo.ts (as left by 06-13)
    - apps/api/src/routes/ceo-decisions.test.ts (migration list, harness)
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md "Pattern 3" and Open Question 5
  </read_first>

  <files>apps/api/drizzle/0004_ceo_decision_once.sql, apps/api/src/routes/events.ts, apps/api/src/routes/ceo.ts, apps/api/src/routes/ceo-decisions.test.ts</files>

  <behavior>
    - Two sequential POSTs to the same decision: 202 then 409 {error:"already decided"}; exactly one ceo.decision_made row.
    - Two concurrent POSTs (Promise.all): one 202 and one 409; exactly one row; the worker receives exactly one decision frame.
    - A decision after a ceo.approval_expired for that decisionId: 409 {error:"expired"}; zero ceo.decision_made rows.
    - Worker A posts ceo.decision_applied for a decision stamped with worker B: 403; worker B posts it twice with different event ids: 202 both times, one row.
    - After request, decision and applied, the rows for that decisionId ordered by occurred_at have types exactly ["ceo.approval_requested", "ceo.decision_made", "ceo.decision_applied"].
  </behavior>

  <action>
RED first. Create apps/api/drizzle/0004_ceo_decision_once.sql: a leading `--` comment saying why (one CEO decision, one application and one expiry per request; the database is the only real serializer for two concurrent POSTs), then `CREATE UNIQUE INDEX IF NOT EXISTS events_ceo_decision_once ON events ((payload->>'decisionId'), type) WHERE type IN ('ceo.decision_made', 'ceo.approval_expired', 'ceo.decision_applied');`. No journal entry (0003 has none; migrate.mjs applies every .sql).

In events.ts replace the id-targeted conflict clause (the one keyed on events.id) with a bare `.onConflictDoNothing()` (a conflict on the new index must be a quiet duplicate, not a 23505 500). Before inserting a worker-posted ceo.decision_applied or ceo.approval_expired, look up the ceo.approval_requested with the same payload decisionId; if it is missing or its payload.workerId differs from request.workerId, reply 403 {error:"decision not owned by this worker"}.

In ceo.ts, before the offline check: if a ceo.approval_expired exists for the decisionId reply 409 {error:"expired"}; if a ceo.decision_made exists reply 409 {error:"already decided"}. Keep 503 next. Insert with bare onConflictDoNothing().returning({ id }); zero rows means a concurrent decision won: reply 409 {error:"already decided"} and send nothing down the downlink.

[BLOCKING] Apply the schema before verification: add "../../drizzle/0004_ceo_decision_once.sql" to ceo-decisions.test.ts's migration list (applyIdempotently), then run `pnpm --filter api db:test:up` and apply the runner against the test database: `DATABASE_URL=postgres://postgres:postgres@localhost:5434/pixelfirm_test node apps/api/scripts/migrate.mjs` and confirm its output lists `0004_ceo_decision_once.sql`. The staging database gets it in 06-12.
  </action>

  <verify>
    <automated>pnpm --filter api test -- ceo-decisions events</automated>
    <fails_when>non-zero exit, "No test files found", or a 23505 duplicate-key error in the output</fails_when>
    <automated>DATABASE_URL=postgres://postgres:postgres@localhost:5434/pixelfirm_test node apps/api/scripts/migrate.mjs</automated>
    <fails_when>non-zero exit, a "failed:" line, or no line mentioning 0004_ceo_decision_once.sql</fails_when>
  </verify>

  <acceptance_criteria>
    - ceo-decisions.test.ts has a Promise.all concurrency test asserting the sorted status codes equal `[202, 409]` and the decision_made row count equals 1
    - ceo-decisions.test.ts asserts the ordered type list for one decisionId equals `["ceo.approval_requested", "ceo.decision_made", "ceo.decision_applied"]`
    - `grep -c "onConflictDoNothing({ target" apps/api/src/routes/events.ts` returns 0 and `grep -c "onConflictDoNothing()" apps/api/src/routes/events.ts` returns 1
  </acceptance_criteria>

  <done>Every request can be decided, applied and expired at most once each, and the audit chain for each decision is complete and ordered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the CEO is whoever Cloudflare Access verified — jose JWT verification (D-11)</name>

  <read_first>
    - apps/api/src/auth/ceo-auth.ts (dev path from 06-13), apps/api/src/auth/browser-auth.ts, apps/api/src/auth/browser-auth.test.ts
    - apps/api/src/env.ts (parseEnv from 06-13)
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md "Access JWT verification" code example, Pitfall 6, Assumptions A3 and A6
    - Official docs: https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/validating-json/ (use Context7 or WebFetch; follow Cloudflare's own recommendation where it differs from the research)
  </read_first>

  <files>apps/api/package.json, pnpm-lock.yaml, apps/api/src/auth/ceo-auth.ts, apps/api/src/auth/ceo-auth.test.ts, apps/api/src/env.ts</files>

  <behavior>
    - makeAccessVerifier with a local RS256 JWKS: a token with the right iss/aud/email resolves to the lowercased email; wrong audience, wrong issuer, another key's signature, an expired token, a token with no email claim, and an HS256 token each reject.
    - Server with bypass off, CEO_EMAIL=ceo@example.com and the local verifier injected: POST decision with header Cf-Access-Jwt-Assertion carrying a valid token for ceo@example.com returns 202 and ceo.decision_made.decidedBy equals "ceo@example.com"; the same token in a `CF_Authorization` cookie also works; a valid token for other@example.com returns 401; no token returns 401; `Authorization: Bearer <BROWSER_ACCESS_TOKEN>` and `?token=<BROWSER_ACCESS_TOKEN>` return 401.
    - Server with bypass off and no CF_ACCESS_* settings: every /ceo/api request returns 401 {"error":"unauthorized"}.
  </behavior>

  <action>
RED first. Install the pinned package: `pnpm --filter api add jose@6.2.10` (exact version, no caret: record T-06-SC). Add env keys (optional, fail closed): CF_ACCESS_TEAM_DOMAIN (z.string().url(), e.g. https://<team>.cloudflareaccess.com, no trailing slash), CF_ACCESS_AUD (the Access application AUD tag), CEO_EMAIL (z.string().email(), compared lowercased), each commented.

In ceo-auth.ts export makeAccessVerifier({ teamDomain, audience, jwks? }) returning async (token) => email: keys = jwks ?? createRemoteJWKSet(new URL(`${teamDomain}/cdn-cgi/access/certs`)); jwtVerify(token, keys, { issuer: teamDomain, audience, algorithms: ["RS256"] }); throw unless payload.email is a string; return it lowercased. Keep a module-level verifier built lazily from env (null when either CF_ACCESS setting is missing) and export _setAccessVerifierForTests(v | null). requireCeo order: the 06-13 dev path unchanged; then token = the cf-access-jwt-assertion header, else the CF_Authorization value parsed from the Cookie header (split on ";", trim, first match; Assumption A3 fallback for WebSocket upgrades); no verifier, no token, no CEO_EMAIL, a throw, or an email different from CEO_EMAIL all reply 401 {error:"unauthorized"} (uniform, browser-auth.ts style). On success set request.ceoEmail and ceoDevBypass = false. BROWSER_ACCESS_TOKEN is never read. Never log the token.

Create ceo-auth.test.ts: unit tests with generateKeyPair("RS256"), exportJWK, createLocalJWKSet, SignJWT (no network, no Cloudflare account); integration tests in the ceo-decisions harness style with bypass off, using _setAccessVerifierForTests.
  </action>

  <verify>
    <automated>pnpm --filter api test -- ceo-auth</automated>
    <fails_when>non-zero exit, or "No test files found"</fails_when>
    <automated>pnpm --filter api typecheck</automated>
    <fails_when>non-zero exit or an "error TS" line</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -n '"jose": "6.2.10"' apps/api/package.json` returns a match
    - ceo-auth.test.ts has one rejecting case each for wrong audience, wrong issuer, foreign key, expired, missing email and HS256
    - ceo-auth.test.ts asserts 401 for both BROWSER_ACCESS_TOKEN forms and 202 with decidedBy "ceo@example.com" for a valid token
  </acceptance_criteria>

  <done>Only a request carrying a valid Access JWT for CEO_EMAIL (or the loopback-only dev bypass outside production) is accepted as the CEO, and the recorded decider is that verified email.</done>
</task>

<task type="auto">
  <name>Task 3: GET /ceo/api/me, log redaction, and the production NODE_ENV guard</name>

  <read_first>
    - apps/api/src/server.ts (logger redact l.21)
    - apps/api/Dockerfile
    - apps/api/src/routes/ceo.ts
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-RESEARCH.md Pitfalls 7 and 8
    - .planning/phases/06-ceo-dashboard-approval-workflow/06-UI-SPEC.md (header shows the verified email; dev-bypass banner)
  </read_first>

  <files>apps/api/src/routes/ceo.ts, apps/api/src/server.ts, apps/api/Dockerfile, apps/api/src/auth/ceo-auth.test.ts</files>

  <action>
Add GET /ceo/api/me to registerCeoRoute: preValidation requireCeo only (a same-origin GET may carry no Origin header), config rateLimit max 60 per minute, reply { email: request.ceoEmail, devBypass: request.ceoDevBypass === true }. The dashboard uses it to show the decider and to make 401/403 observable (a failed WS upgrade is opaque in browsers).

In server.ts extend the logger redact list with "req.headers['cf-access-jwt-assertion']" and "req.headers.cookie" (Pitfall 8). In apps/api/Dockerfile add `ENV NODE_ENV=production` before CMD (Pitfall 7, the third bypass guard: 06-13's parseEnv throws when the bypass is on in production). Add ceo-auth.test.ts cases: me returns { email: "ceo@example.com", devBypass: false } with a valid token and 401 without one.
  </action>

  <verify>
    <automated>pnpm --filter api test -- ceo-auth ceo-decisions</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>grep -c "cf-access-jwt-assertion" apps/api/src/server.ts && grep -c "NODE_ENV=production" apps/api/Dockerfile</automated>
    <fails_when>either count prints 0 or the command exits non-zero</fails_when>
  </verify>

  <acceptance_criteria>
    - ceo-auth.test.ts asserts the /ceo/api/me body equals `{ email: "ceo@example.com", devBypass: false }`
    - `grep -n "req.headers.cookie" apps/api/src/server.ts` returns a match
    - `grep -n "ENV NODE_ENV=production" apps/api/Dockerfile` returns a match placed before the CMD line
  </acceptance_criteria>

  <done>The dashboard can learn who it is signed in as, secrets stay out of logs, and the production image cannot run the dev bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare edge -> API | the Access JWT header is forwarded; the API must still verify it |
| direct origin request -> API | a request that skipped the edge carries no valid JWT |
| worker credential -> /events | may only report on its own decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05-01 | Spoofing | requireCeo | high | mitigate | jose jwtVerify with issuer, audience, RS256 only; email must equal CEO_EMAIL; fail closed when unset |
| T-06-05-02 | Spoofing | Access bypass at origin | high | mitigate | JWT verified in the API on every request (header alone is not trusted); tunnel-only ingress verified in 06-12 |
| T-06-05-03 | Tampering | double decision / replay | high | mitigate | partial unique index + bare onConflictDoNothing -> 409 |
| T-06-05-04 | Spoofing | worker forging another worker's applied/expired | medium | mitigate | stamped workerId must equal request.workerId, else 403 |
| T-06-05-05 | Info disclosure | logs | medium | mitigate | redact cf-access-jwt-assertion and cookie |
| T-06-05-06 | Elevation | dev bypass in production | high | mitigate | NODE_ENV=production in the image + parseEnv throw + loopback socket check |
| T-06-05-SC | Tampering | npm install of jose | high | mitigate | pinned jose@6.2.10 (older than the too-new window; canonical repo, 94M weekly downloads); no postinstall script |
</threat_model>

<verification>
- `pnpm --filter api db:test:up`, then `pnpm --filter api test` (whole api suite) and `pnpm --filter api typecheck` green.
- migrate.mjs output against the test database lists 0004.
</verification>

<success_criteria>
- One decision per request, enforced by Postgres; a complete ordered audit chain per decision.
- The decider identity is a verified Access email; the browser token and worker credentials can never decide.
</success_criteria>

## Artifacts this phase produces

- `apps/api/drizzle/0004_ceo_decision_once.sql`: unique index `events_ceo_decision_once`
- `apps/api/src/auth/ceo-auth.ts`: `makeAccessVerifier`, `_setAccessVerifierForTests`
- Route `GET /ceo/api/me`
- Env keys `CF_ACCESS_TEAM_DOMAIN`, `CF_ACCESS_AUD`, `CEO_EMAIL`
- Dependency `jose@6.2.10` (apps/api)

<output>
Create `.planning/phases/06-ceo-dashboard-approval-workflow/06-05-SUMMARY.md` when done
</output>
