---
phase: 05-pixel-office-renderer
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/event-schema/src/payloads/index.ts
  - packages/event-schema/src/payloads/index.test.ts
  - packages/orchestration-adapter/src/types.ts
  - packages/claude-adapter/src/event-emitter.ts
  - packages/claude-adapter/src/claude-code-runtime.ts
  - packages/claude-adapter/src/claude-code-runtime.test.ts
  - packages/claude-adapter/src/claude-code-runtime.integration.test.ts
  - packages/company-core/src/agent-status-derivation.ts
  - packages/company-core/src/agent-status-derivation.test.ts
  - packages/company-core/src/projections.ts
  - packages/company-core/src/reducer.ts
  - packages/company-core/src/reducer.test.ts
autonomous: true
requirements: [OFFICE-01, HANDOFF-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "agent.handoff_requested's payload carries a real fromAgentId (not just toAgentId) so both handoff participants are identifiable"
    - "agent.handoff_completed exists as CompanyEventSchema's 17th discriminated-union member and round-trips through safeParse"
    - "StartTaskInput requires a real agentId, and every task.status_changed event ClaudeCodeRuntime emits for that task now carries sourceAgentId on its envelope — no more agent-anonymous task lifecycle events"
    - "The reducer derives a real AgentStatus (not a hardcoded 'idle'/'working' string) for the owning agent every time task.status_changed fires, using a documented, honest priority-ordered rule table — never a value it cannot justify from real projected data"
    - "gsd.phase_observed's company-wide category re-derives the AgentStatus of every agent whose task is currently starting/running, refining coding/researching/planning/testing/reviewing/discussing/deploying from the single company-wide GSD signal available"
  artifacts:
    - path: "packages/company-core/src/agent-status-derivation.ts"
      provides: "deriveAgentStatus(input) — the pure, testable priority-ordered rule table"
    - path: "packages/event-schema/src/payloads/index.ts"
      provides: "fromAgentId on AgentHandoffRequestedPayload + the new agent.handoff_completed union member"
  key_links:
    - from: "packages/claude-adapter/src/claude-code-runtime.ts"
      to: "packages/company-core/src/reducer.ts"
      via: "task.status_changed envelope now carries sourceAgentId; reducer's handler reads event.sourceAgentId to know which AgentState to update"
      pattern: "sourceAgentId"
    - from: "packages/company-core/src/reducer.ts"
      to: "packages/company-core/src/agent-status-derivation.ts"
      via: "task.status_changed and gsd.phase_observed handlers both call deriveAgentStatus(...)"
      pattern: "deriveAgentStatus\\("
  prohibitions:
    - requirement_id: HANDOFF-01
      category: values
      statement: "MUST NOT show handoff acceptance/completion before the real agent.handoff_completed event has actually been received — no optimistic/predicted completion"
      verification: test

user_setup: []
---

<objective>
Close RESEARCH.md's two backend gaps — `agent.handoff_completed`/`fromAgentId` missing from the event schema, and the total absence of any task→agent identity link anywhere in the runtime/event pipeline — and replace the ad hoc `"idle"`/`"working"`/`"assigned"` status strings 05-01 left behind with a real, honest, priority-ordered `AgentStatus` derivation.

Purpose: OFFICE-01's "sprites match real current state" claim is only true if a real per-agent status signal exists; HANDOFF-01's choreography (Plan 05-04) cannot know WHO is walking without `fromAgentId` and `agent.handoff_completed`.
Output: extended event schema, `agentId`-threaded `AgentRuntime`, and `packages/company-core/src/agent-status-derivation.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-CONTEXT.md
@.planning/phases/05-pixel-office-renderer/05-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `packages/event-schema/src/payloads/index.ts` — `AgentHandoffRequestedPayload.fromAgentId` (new required field), `AgentHandoffCompletedPayload` (new), `agent.handoff_completed` (17th union member).
- `packages/orchestration-adapter/src/types.ts` — `StartTaskInput.agentId: string` (new required field).
- `packages/claude-adapter/src/event-emitter.ts` — `buildEnvelope`'s new optional `sourceAgentId` parameter.
- `packages/claude-adapter/src/claude-code-runtime.ts` — `TaskRecord.agentId`, `emitStatus` now sets `sourceAgentId`, `requestHandoff` now sets `fromAgentId`, new `completeHandoff(taskId, toAgentId)`.
- `packages/company-core/src/agent-status-derivation.ts` — `deriveAgentStatus(input)`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Event schema handoff additions + agentId threading through ClaudeCodeRuntime</name>
  <files>packages/event-schema/src/payloads/index.ts, packages/event-schema/src/payloads/index.test.ts, packages/orchestration-adapter/src/types.ts, packages/claude-adapter/src/event-emitter.ts, packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts, packages/claude-adapter/src/claude-code-runtime.integration.test.ts</files>
  <read_first>
    packages/event-schema/src/payloads/index.ts, packages/event-schema/src/payloads/index.test.ts, packages/event-schema/src/envelope.ts,
    packages/orchestration-adapter/src/types.ts,
    packages/claude-adapter/src/event-emitter.ts, packages/claude-adapter/src/claude-code-runtime.ts (full file — `TaskRecord` interface, `emitStatus`, `requestHandoff`, `startTask`, the role-change poll around the `observed.role !== record.lastRole` branch), packages/claude-adapter/src/claude-code-runtime.test.ts, packages/claude-adapter/src/claude-code-runtime.integration.test.ts (both files' existing `startTask({...})` call sites),
    .planning/phases/05-pixel-office-renderer/05-RESEARCH.md (Common Pitfalls → Pitfall 2, Code Examples → "Event schema additions")
  </read_first>
  <action>
    In packages/event-schema/src/payloads/index.ts: change `AgentHandoffRequestedPayload` from `z.object({ taskId: z.string(), toAgentId: z.string() })` to `z.object({ taskId: z.string(), fromAgentId: z.string(), toAgentId: z.string() })`. Add a new const `AgentHandoffCompletedPayload = z.object({ taskId: z.string(), toAgentId: z.string() })`. Append exactly one new member to `CompanyEventSchema`'s discriminated union, after the existing 16 (never reorder, never insert mid-array — matches the file's own established convention): `z.object({ ...BaseEnvelope.shape, type: z.literal("agent.handoff_completed"), payload: AgentHandoffCompletedPayload })`, with a dated comment "// Phase 5 addition — union grows from 16 to 17." above it, matching the existing "Phase 3 additions"/"Phase 4 addition" comment style already in the file. Update packages/event-schema/src/payloads/index.test.ts: add `fromAgentId` to every existing `agent.handoff_requested` fixture object (grep the file for `handoff_requested` to find them all), and add a new round-trip test asserting a well-formed `agent.handoff_completed` event parses successfully via `CompanyEventSchema.safeParse`.

    In packages/orchestration-adapter/src/types.ts: add `agentId: string;` to `StartTaskInput` (a required field, not optional — the owning agent for this task; every downstream event this runtime emits for the task threads through this value).

    In packages/claude-adapter/src/event-emitter.ts: extend `buildEnvelope`'s signature with a 6th, optional parameter `sourceAgentId?: string`, appended after the existing `visibility` parameter; when provided, include it in the returned envelope object as `sourceAgentId` (omit the key entirely when not provided — do not emit `sourceAgentId: undefined`).

    In packages/claude-adapter/src/claude-code-runtime.ts: add `agentId: string` to the `TaskRecord` interface. In `startTask`, change the `tasks.set(input.taskId, {status:"starting", worktreePath: input.worktreePath})` line to also set `agentId: input.agentId`. Change `emitStatus(taskId, status)` to look up `tasks.get(taskId)?.agentId` and pass it as `buildEnvelope`'s new 6th argument in its existing `buildEnvelope(options.companyId, "task.status_changed", {taskId, status}, taskId)` call. Change `requestHandoff(taskId, toAgentId)` to read `tasks.get(taskId)?.agentId` for the new required `fromAgentId` payload field — if the task record or its `agentId` is missing, throw a clear error (`ClaudeCodeRuntime.requestHandoff: task ${taskId} has no known agentId`) rather than fabricating a placeholder, matching `getStatus`'s existing "never invent state" precedent at its own unknown-taskId guard. Add a new function `completeHandoff(taskId: string, toAgentId: string): Promise<void>` mirroring `requestHandoff`'s shape exactly, posting `agent.handoff_completed` with `{taskId, toAgentId}`. In the existing role-change poll branch (`if (observed.role !== record.lastRole) { record.lastRole = observed.role; await requestHandoff(taskId, observed.role); }`), add a call to `await completeHandoff(taskId, observed.role)` immediately after the `requestHandoff` call — this single-session simulation has no separate receiving process yet (Phase 6+ multi-agent orchestration territory), so the observed role transition IS the completion signal; document this with a one-line comment explaining why request and complete fire back-to-back here rather than being genuinely asynchronous. Update both packages/claude-adapter/src/claude-code-runtime.test.ts and packages/claude-adapter/src/claude-code-runtime.integration.test.ts: add `agentId: "test-agent-1"` (or an equivalent literal already in scope at each call site) to every existing `startTask({...})` call found in `<read_first>`.
  </action>
  <verify>
    <automated>pnpm --filter event-schema test -- payloads</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
    <automated>pnpm --filter orchestration-adapter typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `CompanyEventSchema`'s discriminated union has exactly 17 members; a well-formed `agent.handoff_completed` event parses via `safeParse` with `success: true`.
    - Every `agent.handoff_requested` fixture in `packages/event-schema/src/payloads/index.test.ts` includes `fromAgentId`; omitting it now fails `safeParse`.
    - `StartTaskInput` requires `agentId`; `packages/claude-adapter`'s test suite passes with every `startTask` call site updated.
    - `emitStatus`'s posted envelope includes `sourceAgentId` matching the task's `agentId`.
    - `requestHandoff` throws (does not silently fabricate) when called for a task with no known `agentId`.
  </acceptance_criteria>
  <done>event-schema, orchestration-adapter, and claude-adapter test suites pass with fromAgentId, agent.handoff_completed, StartTaskInput.agentId, and sourceAgentId threading all in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real per-agent AgentStatus derivation in the reducer</name>
  <files>packages/company-core/src/agent-status-derivation.ts, packages/company-core/src/agent-status-derivation.test.ts, packages/company-core/src/projections.ts, packages/company-core/src/reducer.ts, packages/company-core/src/reducer.test.ts</files>
  <read_first>
    packages/company-core/src/reducer.ts (full file), packages/company-core/src/reducer.test.ts, packages/company-core/src/projections.ts, packages/company-core/src/fixtures/stub-events.ts,
    packages/event-schema/src/agent-status.ts (05-01), packages/orchestration-adapter/src/types.ts (AgentTaskStatus's 9 values, updated in Task 1),
    .planning/phases/05-pixel-office-renderer/05-RESEARCH.md (System Architecture Diagram — "reducer maps: agent.online / task.status_changed / gsd.phase_observed / agent.handoff_requested / agent.handoff_completed → AgentStatus")
  </read_first>
  <behavior>
    - `deriveAgentStatus({taskStatus: undefined})` → `AgentStatus.IDLE` (no active task).
    - `deriveAgentStatus({taskStatus: "failed"})` → `AgentStatus.FAILED`.
    - `deriveAgentStatus({taskStatus: "completed"})` → `AgentStatus.COMPLETED`.
    - `deriveAgentStatus({taskStatus: "cancelled"})` → `AgentStatus.COMPLETED` (task is over, not fabricating a "went wrong" signal for an intentional cancellation).
    - `deriveAgentStatus({taskStatus: "blocked"})` → `AgentStatus.BLOCKED`; `deriveAgentStatus({taskStatus: "paused"})` → `AgentStatus.BLOCKED` (both are "not actively working, needs re-activation").
    - `deriveAgentStatus({taskStatus: "waiting_for_review"})` → `AgentStatus.WAITING_FOR_CEO` (this project's `requestReview` always routes to CEO approval, per Phase 4 D-08 — no peer-review path exists).
    - `deriveAgentStatus({taskStatus: "waiting_for_handoff"})` → `AgentStatus.WAITING_FOR_AGENT`.
    - `deriveAgentStatus({taskStatus: "starting", gsdCategory: undefined})` and `deriveAgentStatus({taskStatus: "running", gsdCategory: "unknown"})` → `AgentStatus.CODING` (default active fallback, most common real case).
    - `deriveAgentStatus({taskStatus: "running", gsdCategory: "research"})` → `AgentStatus.RESEARCHING`; `"planning"`/`"requirements"`/`"new_project"` → `AgentStatus.PLANNING`; `"execution"` → `AgentStatus.CODING`; `"verification"` → `AgentStatus.TESTING`; `"review"` → `AgentStatus.REVIEWING`; `"approval"` → `AgentStatus.DISCUSSING`; `"deployment"` → `AgentStatus.DEPLOYING`.
    - `deriveAgentStatus` never returns `AgentStatus.OFFLINE` or `AgentStatus.READING` — both are intentionally unreachable from this function (no live per-agent worker-liveness link and no tool-call-granularity signal exist yet; this is a documented gap, not a bug — a test asserts the function's return type excludes these two by construction, or asserts no input combination in the exhaustive input-space test below yields them).
  </behavior>
  <action>
    Create packages/company-core/src/agent-status-derivation.ts exporting `deriveAgentStatus(input: { taskStatus?: AgentTaskStatus; gsdCategory?: string }): AgentStatus`, implementing the exact priority-ordered rules from `<behavior>` above as sequential `if` checks (no active task → IDLE; then failed/completed/cancelled; then blocked/paused; then waiting_for_review/waiting_for_handoff; then the starting/running branch refined by `gsdCategory`, defaulting to CODING). Add `currentTaskId?: string` and `rawTaskStatus?: AgentTaskStatus` fields to `AgentState` in packages/company-core/src/projections.ts (internal bookkeeping the reducer needs to re-derive status when `gsd.phase_observed`'s company-wide category changes later — harmless additive fields for the renderer, which only ever reads `.status`).

    In packages/company-core/src/reducer.ts: rewrite the `task.status_changed` handler — it currently upserts blindly by `taskId` alone; change it to ALSO require `event.sourceAgentId` (now populated per Task 1). When `sourceAgentId` is present: update `state.tasks[taskId].status` as today, AND upsert `state.agents[sourceAgentId]` with `status: deriveAgentStatus({taskStatus: event.payload.status, gsdCategory: state.gsdObservations[event.companyId]?.category})`, `currentTaskId: taskId`, `rawTaskStatus: event.payload.status` (creating the agent record if it doesn't exist yet, mirroring `agent.handoff_requested`'s existing upsert-if-missing pattern). When `sourceAgentId` is absent (an older-shaped event, or a producer that hasn't been updated), fall back to today's task-only update with no agent-status side effect — never throw, never fabricate an agent update from nothing.

    Rewrite the `gsd.phase_observed` handler: after its existing `gsdObservations` update, iterate `Object.values(state.agents)` and for every agent whose `rawTaskStatus` is `"starting"` or `"running"`, recompute `status: deriveAgentStatus({taskStatus: agent.rawTaskStatus, gsdCategory: event.payload.category})` — the company-wide GSD category shift now refines every currently-active agent's fine-grained sub-state (this is the one company-wide-not-per-agent simplification RESEARCH.md's own architecture diagram implies; document it with a one-line comment).

    Add a new `agent.handoff_completed` handler: reads `event.payload.toAgentId`, upserts `state.agents[toAgentId]` with `status: AgentStatus.CODING` (the receiving agent has now accepted and is starting real work — refined by the next `task.status_changed` for them shortly after), clearing nothing else. Update the existing `agent.handoff_requested` handler to also read `event.payload.fromAgentId` (now present per Task 1) — no behavior change needed to the sending agent's own state yet (HANDOFF-01's walk-away visual is Plan 05-04's frontend concern, driven off the raw event, not off a new AgentStatus value).

    Add packages/company-core/src/agent-status-derivation.test.ts covering every case in `<behavior>` above (RED first — write the failing tests, confirm they fail, then implement `deriveAgentStatus`, confirm GREEN). Extend packages/company-core/src/reducer.test.ts with new cases: a `task.status_changed` event WITH `sourceAgentId` upserts the correct `AgentStatus` on `state.agents[sourceAgentId]`; the SAME event type WITHOUT `sourceAgentId` leaves `state.agents` untouched (only `state.tasks` updates, matching today's behavior); a `gsd.phase_observed` event with a new `category` re-derives status for an agent whose `rawTaskStatus` is `"running"`; a new `agent.handoff_completed` event upserts the receiving agent to `AgentStatus.CODING`.
  </action>
  <verify>
    <automated>pnpm --filter company-core test -- agent-status-derivation</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
    <automated>pnpm --filter company-core test -- reducer</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Every case listed in `<behavior>` has a corresponding passing test in `agent-status-derivation.test.ts`.
    - `reducer.test.ts`'s new cases pass: `sourceAgentId`-bearing `task.status_changed` updates `state.agents`; the same event without `sourceAgentId` does not touch `state.agents`; `gsd.phase_observed` re-derives a running agent's fine-grained status; `agent.handoff_completed` sets the receiving agent to `CODING`.
    - No test asserts `deriveAgentStatus` ever returns `AgentStatus.OFFLINE` or `AgentStatus.READING` — both remain structurally valid `AgentStatus` members (per 05-01/05-02) but unreachable from this derivation function, consistent with the documented gap.
  </acceptance_criteria>
  <done>deriveAgentStatus and the reducer's task.status_changed/gsd.phase_observed/agent.handoff_completed handlers all pass their new tests; company-core's full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new external trust boundary — this plan's changes are internal event-schema/reducer/runtime wiring, consumed only by already-authenticated worker and browser connections established in 05-01.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | packages/company-core/src/reducer.ts (`task.status_changed` now trusts `event.sourceAgentId` to select which agent to update) | medium | mitigate | `sourceAgentId` is set server-side by `ClaudeCodeRuntime` (an already-authenticated, already-trusted producer, same trust tier as every other field on `BaseEnvelope`) — never client-suppliable through any public endpoint; the reducer only ever reads it, never writes it back to a payload |
| T-05-08 | Repudiation | packages/claude-adapter's `requestHandoff`/`completeHandoff` back-to-back emission | low | accept | The single-session simulation genuinely has no asynchronous gap to model yet (documented in Task 1's action) — accepted as a known, flagged simplification pending true multi-agent orchestration in a later phase |

</threat_model>

<verification>
All five `<automated>` commands across both tasks exit 0.
</verification>

<success_criteria>
A real `task.status_changed(running)` event, now carrying `sourceAgentId`, causes the reducer to set that agent's `AgentStatus` to a real, justified value (not a hardcoded string) — refined further when `gsd.phase_observed`'s category shifts. `agent.handoff_completed` exists and is emitted by the same producer that emits `agent.handoff_requested`.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-03-SUMMARY.md` when done
</output>
