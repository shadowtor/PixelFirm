---
phase: 05-pixel-office-renderer
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - packages/pixel-office/src/handoff/handoff-choreography.ts
  - packages/pixel-office/src/handoff/handoff-choreography.test.ts
  - packages/pixel-office/src/handoff/dialogue-templates.ts
  - packages/pixel-office/src/handoff/dialogue-templates.test.ts
  - packages/pixel-office/src/index.ts
  - apps/web/src/ws-client.ts
  - apps/web/src/ws-client.test.ts
  - apps/web/src/App.tsx
autonomous: false
requirements: [HANDOFF-01, HANDOFF-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

assumption_delta_decision:
  detected_signals: ["another", "second"]
  decision: no-change
  rationale: "AgentState has been a plural Record<string, AgentState> since Phase 1 (packages/company-core/src/projections.ts). HANDOFF-01's 'first agent... second agent' phrasing describes an interaction between two members of an already-plural collection, not a new singular-to-plural assumption shift — no identity-model change needed."

must_haves:
  truths:
    - "On a real agent.handoff_requested event, the sending agent's Character walks (via the forked findPath BFS, unmodified) from its current tile to the receiving agent's desk tile, a task icon bubble appears on arrival, and the walk path is recomputed from real tile coordinates — never a scripted/fixed animation"
    - "The receiving agent's Character does not show 'accepted' (return to TYPE pose at its own desk) until the real agent.handoff_completed event for that same taskId has actually been received — no optimistic completion"
    - "Handoff dialogue text is produced exclusively by a static template map keyed by task/agent identifiers — grep-provable absence of any LLM/AI API call or network fetch anywhere in packages/pixel-office/src/handoff/"
    - "Dialogue templates reference the task only by generic title/id placeholders — no raw code/diff/prompt content is interpolated into displayed text"
  artifacts:
    - path: "packages/pixel-office/src/handoff/handoff-choreography.ts"
      provides: "Event-driven walk/icon/accept/return FSM (HANDOFF-01)"
    - path: "packages/pixel-office/src/handoff/dialogue-templates.ts"
      provides: "Deterministic, template-based handoff dialogue (HANDOFF-02)"
  key_links:
    - from: "apps/web/src/ws-client.ts"
      to: "packages/pixel-office/src/handoff/handoff-choreography.ts"
      via: "onEvent relays agent.handoff_requested/agent.handoff_completed to the choreography engine's handleHandoffEvent(event)"
      pattern: "handleHandoffEvent"
    - from: "packages/pixel-office/src/handoff/handoff-choreography.ts"
      to: "packages/pixel-office/src/layout/tileMap.ts"
      via: "findPath(fromAgent.tileCol, fromAgent.tileRow, toAgent.seatCol, toAgent.seatRow, ...) computes the walk route"
      pattern: "findPath\\("
  prohibitions:
    - requirement_id: HANDOFF-02
      category: privacy
      statement: "MUST NOT include real code/diff/prompt or other private task detail inside the canned dialogue templates — templates reference the task by generic title/id placeholder only"
      verification: test
    - requirement_id: HANDOFF-02
      category: transparency
      statement: "MUST NOT phrase templated dialogue to imply spontaneous/live AI generation — keep it plainly templated/functional so a stream viewer isn't misled about what's real vs scripted"
      verification: judgment

user_setup: []
---

<objective>
Implement the full choreographed walk-to-desk handoff sequence (D-04: stand up → walk over → task icon appears → receiving agent accepts → sender returns to idle/desk → receiver moves to their own desk and starts work), driven only by the real `agent.handoff_requested`/`agent.handoff_completed` event pair, with deterministic template dialogue (HANDOFF-02).

Purpose: this is the phase's most user-visible, highest-`reversibility` piece — D-04 is rated `costly` by the user in CONTEXT.md (carried forward, not re-rated), since collapsing back to a simpler icon-transfer later would mean removing a shipped movement system, not simplifying a table.
Output: `packages/pixel-office/src/handoff/{handoff-choreography,dialogue-templates}.ts`, wired end-to-end into `apps/web`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-CONTEXT.md
@.planning/phases/05-pixel-office-renderer/05-RESEARCH.md
@.planning/phases/05-pixel-office-renderer/05-01-SUMMARY.md
@.planning/phases/05-pixel-office-renderer/05-03-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `packages/pixel-office/src/handoff/handoff-choreography.ts` — `handleHandoffEvent(event: CompanyEvent)`, the walk/icon/accept/return FSM.
- `packages/pixel-office/src/handoff/dialogue-templates.ts` — `resolveHandoffDialogue(kind, taskTitle, agentName)`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Handoff choreography FSM (walk-to-desk sequence driven by real event pairs)</name>
  <files>packages/pixel-office/src/handoff/handoff-choreography.ts, packages/pixel-office/src/handoff/handoff-choreography.test.ts, packages/pixel-office/src/index.ts, apps/web/src/ws-client.ts</files>
  <read_first>
    packages/pixel-office/src/layout/tileMap.ts (05-01's forked `findPath`), packages/pixel-office/src/engine/characters.ts (05-01's trimmed `Character` struct/state-transition shape), packages/pixel-office/src/types.ts (`CharacterState`), packages/pixel-office/src/index.ts (`upsertCharacterFromAgent`),
    packages/event-schema/src/payloads/index.ts (05-03's `AgentHandoffRequestedPayload.fromAgentId`, `AgentHandoffCompletedPayload`),
    .planning/phases/05-pixel-office-renderer/05-RESEARCH.md (Architecture Patterns → Pattern 1, the exact `IDLE → findPath → WALK → arrive → TYPE` cycle already worked out there, with the concrete `findPath(fromAgent.tileCol, fromAgent.tileRow, toAgent.seatCol, toAgent.seatRow, ...)` call shape), Common Pitfalls → Pitfall 2
  </read_first>
  <reversibility rating="costly">Per CONTEXT.md D-04 (user-rated, carried forward unchanged): once the renderer's event-to-animation state machine is built around this multi-step walk/path/accept/return sequence, collapsing back to a simple icon-transfer would mean removing a shipped movement system, not simplifying a mapping table.</reversibility>
  <action>
    Create packages/pixel-office/src/handoff/handoff-choreography.ts exporting `handleHandoffEvent(event: CompanyEvent): void`, an in-memory FSM keyed by `taskId`, with exactly these states per active handoff: `WALKING_TO_RECEIVER` → `ICON_VISIBLE` → `RETURNING_TO_DESK` (terminal). On `event.type === "agent.handoff_requested"`: read `event.payload.fromAgentId`/`toAgentId`; set the sending agent's `Character.state = CharacterState.WALK` and `Character.path = findPath(fromCharacter.tileCol, fromCharacter.tileRow, toCharacter.seatCol, toCharacter.seatRow, tileMap, blockedTiles)` (the forked BFS, unmodified — reject any temptation to write a new pathfinder here, per RESEARCH.md's Don't Hand-Roll); record the handoff as `WALKING_TO_RECEIVER` keyed by `taskId`. When the game loop's own path-following (already in 05-01's forked `engine/characters.ts`/`gameLoop.ts`) reports the sending character has arrived (path array empty, character at target tile), transition the handoff to `ICON_VISIBLE`: show a task icon above the sending character (reuse the bubble-overlay rendering path from 05-02's `status-mapping.ts`/renderer, with a distinct `bubble: "handoff-task"` value — do not repurpose an existing `AgentStatus` bubble for this, since a handoff-in-progress icon is a transient render-only overlay, not an `AgentStatus` value). The FSM does NOT transition further on its own — it waits.

    On `event.type === "agent.handoff_completed"`: look up the handoff record by `event.payload.taskId`; if no `ICON_VISIBLE` record exists for that `taskId`, ignore the event (defensive — a completed event with no matching pending handoff is either a duplicate delivery or a race, never fabricate a walk sequence retroactively). If a matching record exists: transition to `RETURNING_TO_DESK` — set the sending character's `Character.state = CharacterState.WALK`, `Character.path = findPath(...)` back to the sender's OWN desk tile; set the receiving agent's `Character.state = CharacterState.TYPE` at the receiver's own seat position (the receiver "accepts and moves to work" — per HANDOFF-01's exact wording, they do not also walk somewhere first, they simply begin working at their existing desk); remove the task icon overlay; delete the handoff's FSM record once the sender's return-path finishes (same arrival-detection mechanism as the outbound leg). This is the load-bearing acceptance criterion RESEARCH.md's own Common Pitfall 2 and HANDOFF-01's kept prohibition both target: the receiving agent's Character MUST NOT reach `CharacterState.TYPE` at any point before the real `agent.handoff_completed` event is processed — there is no timer, no path-length heuristic, no "assume completion after N seconds" fallback anywhere in this file.

    Export `handleHandoffEvent` from packages/pixel-office/src/index.ts. Modify apps/web/src/ws-client.ts's `onEvent` handler: after its existing `agent.online`/`session.started` branch, add `if (event.type === "agent.handoff_requested" || event.type === "agent.handoff_completed") handleHandoffEvent(event);` — every relayed event now reaches both the status-mapping path (already wired) and the choreography engine.

    Add packages/pixel-office/src/handoff/handoff-choreography.test.ts covering: a real `agent.handoff_requested` event sets the sending character's `state` to `WALK` with a non-empty `path` computed via the real forked `findPath` (assert the path's first/last tiles match the sending/receiving agents' actual tile coordinates, not a stub); the receiving character's `state` remains whatever it already was (NOT `TYPE`) immediately after `agent.handoff_requested` alone; only after a subsequent `agent.handoff_completed` for the SAME `taskId` does the receiving character's `state` become `TYPE`; an `agent.handoff_completed` with no matching prior `agent.handoff_requested` for its `taskId` is a no-op (asserted via no character state change and no thrown error).
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test -- handoff-choreography</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `handleHandoffEvent` never sets the receiving character to `CharacterState.TYPE` in response to `agent.handoff_requested` alone.
    - `handleHandoffEvent` computes the walk path via the real, unmodified `findPath` from `layout/tileMap.ts` — no hardcoded/stub path array anywhere in `handoff-choreography.ts`.
    - An `agent.handoff_completed` event with no matching pending handoff record is a safe no-op.
  </acceptance_criteria>
  <done>handoff-choreography.test.ts passes; the receiving character never reaches TYPE before a real agent.handoff_completed for the same taskId.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic dialogue templates (HANDOFF-02) + end-to-end wiring + final visual check</name>
  <files>packages/pixel-office/src/handoff/dialogue-templates.ts, packages/pixel-office/src/handoff/dialogue-templates.test.ts, packages/pixel-office/src/handoff/handoff-choreography.ts, apps/web/src/App.tsx</files>
  <read_first>
    packages/pixel-office/src/handoff/handoff-choreography.ts (Task 1's output, for the icon-visible moment dialogue attaches to), apps/web/src/App.tsx (05-01/05-02's version), apps/web/src/ws-client.ts,
    .planning/phases/05-pixel-office-renderer/05-CONTEXT.md (HANDOFF-02: "dialogue/text shown during the sequence remains deterministic/template-based — never LLM-generated at render time; this constraint is fixed, not open for reconsideration")
  </read_first>
  <action>
    Create packages/pixel-office/src/handoff/dialogue-templates.ts exporting `resolveHandoffDialogue(kind: "requested" | "accepted", taskTitle: string, toAgentName: string): string` — a pure function over a static `Record<"requested"|"accepted", string>` template map using placeholder interpolation only (`"Handing off \"${taskTitle}\" to ${toAgentName}"` for `requested`, `"${toAgentName} accepts \"${taskTitle}\""` for `accepted`), with plainly functional, non-chatty phrasing (no exclamation marks, no simulated personality, no first-person "I" framing) so a stream viewer is never misled into reading it as live/spontaneous AI generation rather than a template — per HANDOFF-02's kept transparency prohibition. `taskTitle`/`toAgentName` are the ONLY interpolated values; never interpolate raw code, diffs, prompts, or file contents — callers must pass the task's `title` field (already a plain string on `TaskState`), never the task's `payload`/`prompt`/git-diff content. Wire `handoff-choreography.ts`'s `ICON_VISIBLE` transition (Task 1) to call `resolveHandoffDialogue("requested", ...)` and attach the result as the task-icon bubble's tooltip/label text; wire the `RETURNING_TO_DESK` transition to call `resolveHandoffDialogue("accepted", ...)` similarly.

    Add packages/pixel-office/src/handoff/dialogue-templates.test.ts asserting: `resolveHandoffDialogue` output for both `kind` values contains the literal `taskTitle`/`toAgentName` strings passed in (proving interpolation, not a canned unrelated sentence); a grep-style test (`fs.readFileSync` over `dialogue-templates.ts` and `handoff-choreography.ts`, asserting neither file's source text contains any of `fetch(`, `anthropic`, `openai`, `claude-agent-sdk`, or `http` as a case-insensitive substring) proving zero network/LLM call surface in either file — this is the mechanical proof for HANDOFF-02's "never LLM-generated at render time" requirement.

    Start both dev servers for the final visual check: `pnpm --filter api dev` and `pnpm --filter web dev` in the background, wait for each to report ready (apps/api logs its listen address; apps/web/Vite logs "ready in"). Confirm `apps/api`'s `/health` returns 200 and Vite's dev URL responds 200 before presenting the checkpoint below.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test -- dialogue-templates</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
    <human-check>
      Open the Vite dev URL in a browser. Confirm: (1) at least one agent renders with a visible pose distinct from the others (status-mapping fidelity from 05-02); (2) a blocked/waiting agent (if one is present in the demo data) shows a frozen pose with an icon overlay, not an actively-animating one; (3) the attribution footer from 05-02 is visible; (4) if a handoff is triggered during the session, the sending agent visibly walks to the receiving agent's desk, a task icon appears, and only after the real completion event does the receiving agent start working at their own desk — never before.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `resolveHandoffDialogue` never returns text containing anything other than its two template shapes plus the two interpolated string arguments.
    - The grep-style no-LLM-surface test passes for both `dialogue-templates.ts` and `handoff-choreography.ts`.
    - Both dev servers report ready before the human-check is presented.
  </acceptance_criteria>
  <done>dialogue-templates.test.ts passes (including the no-LLM-surface grep check); the human-check confirms the full visual chain in a browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan's inputs are the same authenticated, schema-validated event stream established in 05-01.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Information Disclosure | packages/pixel-office/src/handoff/dialogue-templates.ts | medium | mitigate | Templates interpolate only `taskTitle`/`toAgentName` (plain, already-non-secret `TaskState`/agent-name fields) — the grep-style test in Task 2 proves no code/diff/prompt content ever reaches the template, and the file has no network call surface at all |
| T-05-10 | Spoofing | packages/pixel-office/src/handoff/handoff-choreography.ts (trusts `agent.handoff_completed`'s `toAgentId`) | low | accept | The event arrives only via the already-authenticated browser WS relay (05-01), itself sourced only from server-validated `CompanyEventSchema` rows — no client-side forgery path exists into this stream this phase |

</threat_model>

<verification>
Both automated test commands exit 0; the human-check confirms the full visual chain (status fidelity, blocked/waiting signal, attribution, handoff choreography) actually renders correctly in a browser.
</verification>

<success_criteria>
A real handoff — driven by nothing but a real `agent.handoff_requested`/`agent.handoff_completed` event pair — visibly walks one agent to another's desk, shows a task icon with deterministic dialogue, and only transitions the receiving agent to working once the real completion event lands. Phase 5's full requirement set (OFFICE-01/02/03, HANDOFF-01/02) is now satisfied end-to-end.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-04-SUMMARY.md` when done
</output>
