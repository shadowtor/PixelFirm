---
phase: 05-pixel-office-renderer
plan: 41
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pixel-office/src/handoff/dialogue-templates.ts
  - packages/pixel-office/src/handoff/dialogue-templates.test.ts
  - packages/pixel-office/src/handoff/handoff-choreography.ts
  - packages/pixel-office/src/handoff/handoff-choreography.test.ts
  - packages/pixel-office/src/index.ts
  - apps/web/src/App.tsx
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
autonomous: true
gap_closure: true
gap_ids: [CR-01, WR-01, IN-02, IN-01]
requirements: [HANDOFF-01, HANDOFF-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A handoff bubble never presents a task id OR a blank string as a title: when no human-readable title is known (none registered, empty, or whitespace-only) the line says what is happening without inventing one, `hands off to <name>` / `<name> accepts the handoff`"
    - "getActiveHandoffs exposes the untruncated title with participants, phase and the drawn bubble's rect under the same rule as the canvas: `fullTitle` is `string | null`, null when no title is known or the registered one is blank, never the task id"
    - "The blank-is-absent rule is written once, `titleOrNull` in dialogue-templates.ts, and both the bubble resolver and getActiveHandoffs route through it; a padded title is interpolated and exposed trimmed"
    - "apps/web's two title entry points agree: a live task.created with an empty title is not registered, exactly as the snapshot path already skips one"
    - "The docblocks around ActiveHandoff, showsLineOf and the task-title registry describe what ships: the cap is MAX_DIALOGUE_TITLE_CHARS, fullTitle may be null, and nothing falls back to the task id"
    - "A handoff record whose sender went offline or was re-seated stops reserving its aisle slot at once, not one tick later (review IN-02), while a departing sender that is still current keeps its slot until it is home (WR-08 unchanged)"
  artifacts:
    - packages/pixel-office/src/handoff/dialogue-templates.ts
    - packages/pixel-office/src/handoff/handoff-choreography.ts
    - packages/pixel-office/src/index.ts
    - apps/web/src/App.tsx
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
  key_links:
    - "index.ts getTaskTitle -> handoff-choreography.ts call sites (:279, :319, unchanged) -> dialogue-templates.ts resolveHandoffDialogue -> titleOrNull -> Character.bubbleText: the chain that painted `agent-b accepts ` for a blank title"
    - "index.ts getTaskTitle -> dialogue-templates.ts titleOrNull -> handoff-choreography.ts getActiveHandoffs fullTitle: the host read path Phase 6 will consume"
    - "POST /events (TaskCreatedPayload.title is an unconstrained z.string()) -> /ws/browser relay -> App.tsx onEvent task.created guard -> registerTaskTitle: the trust-boundary entry point"
    - "handoff-choreography.ts interactionTileFor reservation loop -> senderIsCurrent: the IN-02 slot hold"
---

<objective>
Close the two gaps 05-VERIFICATION.md (2026-09-23T18:30) found, both created by 05-40's own change
of shape, plus the one review item the verifier asked to fold in.

Gap 1 (failed, review CR-01): the nullish-coalescing fallback only catches `undefined`, so a
registered EMPTY or whitespace title takes the present-title branch. The verifier reproduced it
through the real choreography: `hands  to agent-b`, `agent-b accepts `, `hands     to agent-b`.
That is the incomplete-sentence class 05-40 was chartered to remove. It is reachable: task.created's
title is `z.string()` and App.tsx registers the live value unguarded while the snapshot path three
lines above guards it. Fix: one exported helper, `titleOrNull`, in dialogue-templates.ts. It trims,
and returns null for absent or blank. resolveHandoffDialogue routes every title through it, so the
rule covers every present and future caller with no call-site edits. The belt is a one-condition
guard at App.tsx:85, chosen over tightening the schema (reason in Task 3).

Gap 2 (partial, review WR-01): `ActiveHandoff.fullTitle` still substitutes the raw task id in a
non-nullable field. Fix: `string | null`, resolved through the same `titleOrNull`, plus the three
stale docblocks and the matching UI-SPEC rows.

Fold-ins (cheap, and in files this plan already edits): IN-02, where a dead record holds its aisle
slot for a tick (the verifier's recommendation; one condition plus one test); IN-01, a two-line
tautological assertion deleted; and UI-SPEC line 102's stale four-offset list (verification advisory 7).

Not in this plan, and why: WR-04, WR-05 and IN-06 are test-quality advisories whose must_have
properties are already carried by other non-vacuous guards (the verifier says so for each). WR-05
also needs a live-harness edit. WR-02 is unreachable on the shipped layout. IN-03..IN-07 change no
behaviour. The verifier records all of these as advisory, not as gaps.

Purpose: finish the rule 05-40 wrote down, *never present an identifier or an empty string as a
title*, at every site, before Phase 6 builds on getActiveHandoffs.
Output: `titleOrNull` plus its use at both title reads, nullable `fullTitle`, the App.tsx entry-point
guard, the IN-02 slot release, and corrected docblocks and UI-SPEC rows.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@.planning/phases/05-pixel-office-renderer/05-REVIEW.md
@.planning/phases/05-pixel-office-renderer/05-40-SUMMARY.md
@packages/pixel-office/src/handoff/dialogue-templates.ts

TDD RED evidence in this repo (project memory): `check tdd-red-evidence` parses node:test TAP
counters, and vitest's TAP output has none. A raw capture therefore reads as `zero_tests_discovered`,
a false INVALID_RED. The accepted recipe: count the RED run's own `ok` / `not ok` lines and append
`# tests N`, `# pass P`, `# fail F` from that same run. Invent no number.

Typecheck note carried from 05-40: `pnpm --filter web typecheck` is the whole guard for this
package's sources (`packages/pixel-office` has no `typecheck` script and its `main` is `src/index.ts`).
Do not add an `||` fallback to a pixel-office typecheck.
</context>

<assumption_delta_decision>
Detector: `assumption-delta scan 05` returned `detected: true`. The two pluralization signals ("another",
"second") come from ROADMAP SC3's prose about one agent handing off to a second agent. That is the
handoff concept itself, not a new variant of an identity model.
- Primary noun: `taskId` stays the identity for every lookup (`showsLineOf`, `getDialogueBox`, the
  record map). The title is display data. Making `fullTitle` nullable does not move the anchor. It
  finishes separating display from identity, which 05-37 and 05-40 started.
- Decision: `no-change`. Nothing is added alongside an old primary.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): a blank title means "no title known", one rule at the resolver, proven through the real choreography (CR-01)</name>

  <read_first>
    - `packages/pixel-office/src/handoff/dialogue-templates.ts`, the whole file (58 lines): `DIALOGUE_TEMPLATES`, `capForDialogue`, the three caps, `resolveHandoffDialogue`, and the header's tone and privacy prohibitions
    - `packages/pixel-office/src/handoff/dialogue-templates.test.ts`, the whole file: the `everyLine` helper (lines 37-38), the verb-led describe, the budget test, and the zero-network describe (lines 99-115). Its forbidden substrings (`fetch(`, `anthropic`, `openai`, `claude-agent-sdk`, `http`) are matched against comments too, in both dialogue-templates.ts and handoff-choreography.ts
    - `packages/pixel-office/src/handoff/handoff-choreography.test.ts` lines 1545-1611: the "a task id is never painted as a title (05-40, G-05-1b)" describe. Mirror its `LONG_TASK_ID`, `fullPair()`, `completedEvent(..., COMP_ID)` and exact-sentence pattern
    - `packages/pixel-office/src/handoff/handoff-choreography.ts` lines 270-335: the two `resolveHandoffDialogue` call sites. Read them only; this task must not edit this file
    - `.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md` `gaps[0]` and `05-REVIEW.md` CR-01: the exact malformed strings reproduced
  </read_first>

  <files>packages/pixel-office/src/handoff/dialogue-templates.ts, packages/pixel-office/src/handoff/dialogue-templates.test.ts, packages/pixel-office/src/handoff/handoff-choreography.test.ts</files>

  <reversibility rating="reversible">One new pure helper and one expression inside a pure, import-free module, plus test cases. No persisted state and no interface change: the resolver's signature is untouched.</reversibility>

  <behavior>
    - Test 1: `resolveHandoffDialogue("requested", "", "Ada")` and `resolveHandoffDialogue("requested", "   ", "Ada")` each return exactly `hands off to Ada`.
    - Test 2: `resolveHandoffDialogue("accepted", "", "Ada")` and `resolveHandoffDialogue("accepted", "   ", "Ada")` each return exactly `Ada accepts the handoff`.
    - Test 3: `resolveHandoffDialogue("requested", "  Fix login  ", "Ada")` returns exactly `hands Fix login to Ada`. A padded title is interpolated trimmed and never leaves a doubled space.
    - Test 4: `everyLine` sweeps a present title, `null`, `""` and `"   "` for both kinds. The existing U+2192 test and budget test pass on the widened sweep, and the derived longest line is still exactly `MAX_DIALOGUE_LINE_CHARS` (34).
    - Test 5 (end to end, the case the verifier reproduced): in the id-never-painted describe, for each of `""` and `"   "`, `registerTaskTitle(LONG_TASK_ID, <blank>)`, drive `fullPair()`, and assert the sender's `bubbleText` is exactly `hands off to agent-b`. After `completedEvent`, assert the receiver's `bubbleText` is exactly `agent-b accepts the handoff`.
  </behavior>

  <action>
RED first. Add the test cases below, run them, and confirm the blank cases fail with the malformed
strings the verifier reproduced: a doubled space in the requested line and a trailing space after
"accepts". Record the RED run using the recipe in `<context>`.

In `packages/pixel-office/src/handoff/dialogue-templates.ts`, add and export
`titleOrNull(title: string | null | undefined): string | null` next to `capForDialogue`. It returns the
title trimmed with `String.prototype.trim`, or `null` when the input is null or undefined or trims to
empty. Give it a docblock with three points. Blank means "no title known" (05-41, CR-01). Nullish
coalescing only catches undefined, and task.created's title is an unconstrained `z.string()`, so an
empty or whitespace title arrives as a present value. This is the one place the rule is written:
`resolveHandoffDialogue` and `getActiveHandoffs` both route through it. Write the trim call exactly
once in the package's non-test sources, inside this helper.

In `resolveHandoffDialogue`, pass `taskTitle` through `titleOrNull` first. Cap a non-null result with
`capForDialogue` at `MAX_DIALOGUE_TITLE_CHARS` as today; a null result takes each template's existing
no-title branch. Keep these unchanged: the signature (`taskTitle: string | null`), `DIALOGUE_TEMPLATES`
(two entries yielding the four fixed sentences), all three cap constants, and the ponytail budget
comment. Extend the resolver's docblock by one clause: a blank string is treated exactly like null.
The module stays import-free.

Do not edit `packages/pixel-office/src/handoff/handoff-choreography.ts` in this task. Its two call
sites keep passing the registered title (or null). The resolver now owns the blank rule for every
present and future caller. That is the point: one guard where all callers route through, not one per
call site.

In `dialogue-templates.test.ts`:
- Widen `everyLine` so each kind is evaluated against the present title, `null`, `""` and `"   "`, keeping its default arguments.
- Add Tests 1-3 as cases in the verb-led describe, named for `05-41, CR-01`, each pinning its exact string with `toBe`.
- Leave the tone, surrogate-pair and zero-network tests unmodified.

In `handoff-choreography.test.ts`, add Test 5 inside the existing "a task id is never painted as a
title" describe. Name it for `05-41, CR-01`, with each blank value as its own reported case. Reuse
`LONG_TASK_ID`, `fullPair`, `completedEvent` and `COMP_ID`.

No comment you write in either source file may contain the zero-network describe's forbidden
substrings, notably `http`.
  </action>

  <verify>
    <automated>pnpm --filter pixel-office test -- --reporter=verbose dialogue-templates</automated>
    <fails_when>non-zero exit, or a blank-title case reports a received value other than `hands off to Ada` / `Ada accepts the handoff`, or the padded-title case receives a string containing two consecutive spaces, or the budget test reports a longest line other than 34 code points</fails_when>
    <automated>pnpm --filter pixel-office test -- --reporter=verbose handoff-choreography</automated>
    <fails_when>non-zero exit, or a blank-title choreography case reports `hands  to agent-b`, `hands     to agent-b` or `agent-b accepts ` as the received bubbleText, or any pre-existing choreography case regresses</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function titleOrNull" packages/pixel-office/src/handoff/dialogue-templates.ts` returns 1.
    - The trim rule is written once: `git grep -n "\.trim()" -- packages/pixel-office/src ':!*.test.ts'` returns exactly one line, and that line is in `dialogue-templates.ts`.
    - `dialogue-templates.test.ts` pins with `toBe`: `hands off to Ada` for `("requested", "", "Ada")` and `("requested", "   ", "Ada")`; `Ada accepts the handoff` for the two accepted equivalents; `hands Fix login to Ada` for `("requested", "  Fix login  ", "Ada")`.
    - `everyLine` enumerates `""` and `"   "` alongside a present title and `null`, and the budget test still derives exactly 34.
    - `handoff-choreography.test.ts` drives the real choreography with a registered `""` title and a registered `"   "` title and pins both participants' exact no-title sentences. Removing the `titleOrNull` call from `resolveHandoffDialogue` makes those cases fail.
    - This task's commit does not modify `packages/pixel-office/src/handoff/handoff-choreography.ts`.
    - `DIALOGUE_TEMPLATES` still has exactly two keys, and the zero-network, tone and surrogate-pair tests pass with no edit.
  </acceptance_criteria>

  <done>An empty or whitespace title yields the same complete no-title sentence as a missing one. This is proven through the real FSM, and one helper carries the rule for every caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fullTitle is null when no title is known, under the same rule, and the docs stop describing the old fallback (WR-01)</name>

  <read_first>
    - `packages/pixel-office/src/handoff/handoff-choreography.ts`: line 23 (the existing import from `./dialogue-templates.js`), lines 138-157 (`showsLineOf` and its docblock with the stale numeric cap), lines 376-447 (the `ActiveHandoff` docblock, the interface, `getActiveHandoffs`)
    - `packages/pixel-office/src/index.ts` lines 55-72: the `taskTitles` comment, `registerTaskTitle`, `getTaskTitle`
    - `packages/pixel-office/src/handoff/dialogue-templates.ts` after Task 1: `titleOrNull`
    - `packages/pixel-office/src/handoff/handoff-choreography.test.ts` lines 1173-1350: `ActiveHandoffLike` (1176-1184), the `LONG_TITLE` docblock (1191), `toIconVisible(title)` (1220), and the case at 1346-1350 that currently pins the task id as `fullTitle`
    - `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` line 102 (office-layout paragraph, the interaction offsets sentence), line 150 (handoff dialogue length-rule row), line 173 (`long-text` UI Consideration row)
    - `packages/pixel-office/src/layout/office-layout.json`, the `interaction.colOffsets` array that UI-SPEC line 102 should quote
    - `.planning/phases/05-pixel-office-renderer/05-REVIEW.md` WR-01
  </read_first>

  <files>packages/pixel-office/src/handoff/handoff-choreography.ts, packages/pixel-office/src/handoff/handoff-choreography.test.ts, packages/pixel-office/src/index.ts, .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>

  <reversibility rating="reversible">A public field widens from `string` to `string | null`. The only consumer is this package's own test file; Phase 6, the declared consumer, has not started. Docblocks and spec rows are prose.</reversibility>

  <behavior>
    - Test 1: a live handoff whose task has no registered title reports `fullTitle` as `null`. This replaces the case that pinned the task id.
    - Test 2: a live handoff whose registered title is `"   "` reports `fullTitle` as `null`.
    - Test 3 (existing, must pass unmodified): with `LONG_TITLE` registered, `fullTitle` equals `LONG_TITLE` while the bubble shows the capped one.
  </behavior>

  <action>
In `packages/pixel-office/src/handoff/handoff-choreography.ts`:
- Change `ActiveHandoff.fullTitle` to `string | null`. Its field docblock: the untruncated registered title, trimmed, or null when none is known or the registered one is blank. Never the task id, which is not a title (05-40 G-05-1b, 05-41 WR-01).
- In `getActiveHandoffs`, resolve `fullTitle` by passing `getTaskTitle(record.taskId)` through `titleOrNull`. Import `titleOrNull` on the existing `./dialogue-templates.js` import line, next to `resolveHandoffDialogue`. This removes the task-id fallback on that line. Do not quote the old expression in any comment, because an acceptance grep counts comments.
- In the `ActiveHandoff` interface docblock, rewrite the sentence about `fullTitle`. It becomes: the untruncated title under the same blank-is-absent rule the bubble uses (`titleOrNull`), before `resolveHandoffDialogue` caps it at `MAX_DIALOGUE_TITLE_CHARS`; null when no title is known, so a host shows its own no-title text rather than an id. Drop the old numeric cap. Also drop the claim that this is what the bubble interpolates: with no title, the bubble interpolates nothing. Leave the `box` and `speakerId` sentences as they are.
- In `showsLineOf`'s docblock, replace the old numeric cap with `MAX_DIALOGUE_TITLE_CHARS` by name.
- Change nothing else in this file in this task. The FSM, the call sites, the reservation loop and `box` resolution stay as they are.

In `packages/pixel-office/src/index.ts`, rewrite the last sentence of the `taskTitles` comment, which
documents a taskId fallback at the call site that 05-40 deleted. New meaning: a missing or blank
entry means no title is known, so the handoff line takes its no-title branch and `getActiveHandoffs`
reports `fullTitle: null` (05-40 G-05-1b, 05-41). `registerTaskTitle` and `getTaskTitle` are unchanged.

In `packages/pixel-office/src/handoff/handoff-choreography.test.ts`:
- Widen `ActiveHandoffLike.fullTitle` to `string | null`.
- Rewrite the case at lines 1346-1350 as Test 1, asserting `toBeNull()`, and rename it for `05-41, WR-01`.
- Add Test 2 next to it, using `toIconVisible("   ")`.
- Update the stale code-point count in the `LONG_TITLE` docblock so it names `MAX_DIALOGUE_TITLE_CHARS`.

In `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md`, make scoped edits only, never a whole-file rewrite:
- Line 150, length-rule row. Add that a blank or whitespace title counts as no title (05-41, CR-01), resolved once by `titleOrNull`. In the "Full title kept" sentence, say `getActiveHandoffs()` returns the untruncated title, or `null` when none is known or it is blank (05-41, WR-01), never the task id. Remove the parenthetical that equates it with what the bubble interpolates.
- Line 173, `long-text` row: record the same two facts.
- Line 102: the interaction-slot sentence lists four column offsets, but `office-layout.json` carries six. Change the list to `+1, -1, +3, -3, +5, -5` (verification advisory 7). Touch nothing else in that paragraph.
  </action>

  <verify>
    <automated>pnpm --filter pixel-office test -- --reporter=verbose handoff-choreography</automated>
    <fails_when>non-zero exit, or the no-title host case reports `task-1` (or any non-null value) as fullTitle, or the whitespace host case reports a non-null fullTitle, or any `LONG_TITLE` host-read-path case regresses</fails_when>
    <automated>pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line, in particular a site still treating fullTitle as a non-nullable string</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "fullTitle: string | null" packages/pixel-office/src/handoff/handoff-choreography.ts` returns 1.
    - `grep -c "titleOrNull(getTaskTitle(record.taskId))" packages/pixel-office/src/handoff/handoff-choreography.ts` returns 1.
    - `grep -c "?? record.taskId" packages/pixel-office/src/handoff/handoff-choreography.ts` returns 0.
    - `grep -c "12 code points" packages/pixel-office/src/handoff/handoff-choreography.ts` returns 0, and `grep -c "exact value the bubble" packages/pixel-office/src/handoff/handoff-choreography.ts` returns 0. These gates deliberately count comments, because the defect is in docblocks.
    - `grep -c "fall back to the raw taskId" packages/pixel-office/src/index.ts` returns 0.
    - The no-title and whitespace-title host cases assert `fullTitle` `toBeNull()`, and no case in `handoff-choreography.test.ts` asserts that `fullTitle` equals a task id.
    - `grep -c "exact value the bubble interpolates" .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` returns 0; `grep -c "+1, -1, +3, -3, +5, -5" .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` returns 1; the rows at lines 150 and 173 each cite `05-41`.
    - `pnpm --filter web typecheck` reports no `error TS`.
  </acceptance_criteria>

  <done>The public host read path follows the canvas's rule: an id or a blank string is never presented as a title, and every docblock and spec row around it describes what actually ships.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the live task.created entry point agrees with the snapshot path, and a dead record frees its aisle slot at once (CR-01 belt, IN-02, IN-01)</name>

  <read_first>
    - `apps/web/src/App.tsx` lines 60-100: the snapshot path's title guard (line 70), the unguarded live registration (lines 84-86), and the load-bearing ordering comment below it
    - `apps/api/src/db/event-row.ts`: `rowToCompanyEvent` re-validates every stored row against `CompanyEventSchema` and throws on failure. This is why the belt is not a schema change
    - `packages/event-schema/src/payloads/index.ts` line 7 (`TaskCreatedPayload`). Read only; this plan does not modify event-schema
    - `packages/pixel-office/src/handoff/handoff-choreography.ts` lines 100-145: `interactionTileFor`'s reservation loop and `senderIsCurrent`
    - `packages/pixel-office/src/handoff/handoff-choreography.test.ts` lines 810-1003: the interaction-tile describe (`toIconVisible`, `slotsOfHome`, `NEW_REQUEST_ID`) and the two WR-08 cases to mirror, "a departing sender keeps its aisle slot reserved until it is home" and "the slot is free again once the record retires"
    - `packages/pixel-office/src/handoff/handoff-choreography.test.ts` lines 1455-1485: the "a superseding record with a byte-identical line gets its own rect" case and its tautological assertion
    - `.planning/phases/05-pixel-office-renderer/05-REVIEW.md` IN-01 and IN-02
  </read_first>

  <files>apps/web/src/App.tsx, packages/pixel-office/src/handoff/handoff-choreography.ts, packages/pixel-office/src/handoff/handoff-choreography.test.ts</files>

  <reversibility rating="reversible">One added condition at a client entry point, one added condition in a reservation loop, and a two-line test deletion. No schema, persisted event or wire contract changes.</reversibility>

  <behavior>
    - Test 1 (IN-02): drive agent-a to ICON_VISIBLE on agent-b's first slot. Send agent-a OFFLINE with `upsertCharacterFromAgent("agent-a", AgentStatus.OFFLINE)`. With no frame stepped, agent-c requests a handoff to agent-b. Assert agent-c's path ends on that first slot, because the dead record no longer reserves it. Before the fix, agent-c is sent to the second slot.
    - Test 2 (existing, must pass unmodified): "a departing sender keeps its aisle slot reserved until it is home". A still-current sender keeps its reservation.
  </behavior>

  <action>
Trust-boundary belt for CR-01, in `apps/web/src/App.tsx`'s `onEvent`. Register a live task.created
title only when it is non-empty, mirroring the snapshot path's `if (task.title)` guard: add
`event.payload.title` to the existing `event.type === "task.created" && event.taskId` condition. Add
one comment line: same guard as the snapshot path; the renderer already treats a blank title as
absent via `titleOrNull`, and this only keeps the two entry points agreeing (05-41, CR-01). Do not
move this block relative to the `handleHandoffEvent` call below it; that ordering comment is
load-bearing.

This belt is chosen over `.min(1)` on `TaskCreatedPayload.title`. Do NOT modify
`packages/event-schema`. `apps/api`'s `rowToCompanyEvent` re-validates every stored event against
`CompanyEventSchema` and throws on failure. A stricter schema would therefore crash replay of any
`task.created` with an empty title that the append-only log already accepted, with no migration path.
`.min(1)` would still admit a whitespace title anyway. The resolver rule from Task 1 is the fix; this
guard is the belt.

IN-02, in `interactionTileFor`'s reservation loop in `handoff-choreography.ts`: reserve a record's
target only while its sender is still current. Add `senderIsCurrent(record)` to the existing
`record.fromChar !== fromChar` condition. Update the loop's comment: a record whose sender vanished or
was re-seated is dead and releases its slot at once, instead of at the next `checkHandoffArrivals`
tick (review IN-02). A current departing sender still holds its slot until `retireHandoff` (WR-08).
Add Test 1 to the interaction-tile describe next to the two WR-08 cases, named for review IN-02.

IN-01: in the "a superseding record with a byte-identical line gets its own rect" case, delete the
`expect` whose two sides are the same `resolveHandoffDialogue` call with the same arguments; it can
never fail. Reword the comment above it to point at the later `lineBefore` assertion, which is what
actually proves the byte-identity. Remove that case's `toName` local if nothing else in the case
reads it.

Finish by running the full workspace suite uncached.
  </action>

  <verify>
    <automated>pnpm --filter pixel-office test -- --reporter=verbose handoff-choreography</automated>
    <fails_when>non-zero exit, or the IN-02 case reports agent-c's path ending on the second slot instead of the first, or either WR-08 slot case regresses</fails_when>
    <automated>pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line</fails_when>
    <automated>pnpm --filter web test</automated>
    <fails_when>non-zero exit, or fewer than 24 tests reported passing</fails_when>
    <automated>pnpm turbo run test --force</automated>
    <fails_when>non-zero exit, or fewer than 10 of 10 workspace tasks reported successful, or the pixel-office task reports fewer than 211 passing tests</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c 'event.type === "task.created" && event.taskId && event.payload.title' apps/web/src/App.tsx` returns 1.
    - No file under `packages/event-schema` is modified by this plan.
    - `grep -c "record.fromChar !== fromChar && senderIsCurrent(record)" packages/pixel-office/src/handoff/handoff-choreography.ts` returns 1.
    - The IN-02 case exists, and removing `senderIsCurrent(record)` from the reservation condition makes it fail while both WR-08 cases still pass.
    - `grep -c 'resolveHandoffDialogue("requested", TITLE_X, toName)' packages/pixel-office/src/handoff/handoff-choreography.test.ts` returns 1. The meaningful X-versus-Y comparison in the WR-07 collision case survives and the tautology is gone.
    - `pnpm turbo run test --force` reports 10 of 10 workspace tasks successful; pixel-office reports at least 211 passing (207 at HEAD, plus at least one new case from each of: Task 1 dialogue, Task 1 choreography, Task 2, Task 3); web reports at least 24.
  </acceptance_criteria>

  <done>Both title entry points in apps/web agree, the renderer's rule is backed at the entry, a dead handoff frees its slot the moment its sender is gone, and the whole workspace is green uncached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker to control plane (`POST /events`) | Any worker credential can post `task.created` with `title: ""` or whitespace; `TaskCreatedPayload.title` is an unconstrained `z.string()`. |
| company projection to rendered canvas / host API | `TaskState.title` becomes visible text on the canvas and becomes `ActiveHandoff.fullTitle` for the Phase 6 host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-41-01 | Tampering | `dialogue-templates.ts` `resolveHandoffDialogue` | low | mitigate | Output-side validation (ASVS L1 V5): `titleOrNull` treats an empty or whitespace title as absent before interpolation, so a crafted blank title can only produce the fixed no-title sentence. Guarded end to end by Task 1's choreography cases. |
| T-05-41-02 | Tampering | `packages/event-schema` `TaskCreatedPayload.title` | low | accept | The schema is deliberately not tightened. `apps/api` `rowToCompanyEvent` re-validates stored rows and throws, so `.min(1)` would turn previously accepted events into replay failures in an append-only log. `.min(1)` also misses whitespace. The rendered-output rule (T-05-41-01) covers every source, and the App.tsx guard (Task 3) is the entry belt. |
| T-05-41-03 | Information Disclosure | `handoff-choreography.ts` `getActiveHandoffs` `fullTitle` | low | mitigate | Stop exposing the task id in a title field: `fullTitle` becomes `string | null`. This reduces the surface Phase 6 will render. Task 2's null assertions are the guard. |
| T-05-41-04 | Denial of Service | `interactionTileFor` reservation loop | low | mitigate | A dead record no longer blocks a receiver's aisle slot for a tick (IN-02), so a concurrent request is not denied a slot nobody stands on. Task 3's IN-02 case is the guard. |

No threat reaches `high`, so the ASVS L1 block-on-high threshold is not triggered. `T-05-SC` (package legitimacy) does not apply: this plan installs nothing.
</threat_model>

<verification>
1. `pnpm --filter pixel-office test`: at least 211 passing (207 at HEAD).
2. `pnpm --filter web test` (at least 24) and `pnpm --filter web typecheck` (no `error TS`).
3. `pnpm turbo run test --force`: 10 of 10 workspace tasks, uncached.
4. Revert-proof spot checks, each done independently and then restored:
   - remove the `titleOrNull` call from `resolveHandoffDialogue`, and the blank-title cases in both test files go red;
   - put the task-id fallback back on `fullTitle`, and the Task 2 null cases go red;
   - drop `senderIsCurrent(record)` from the reservation condition, and the IN-02 case goes red.
5. `git grep -n "\.trim()" -- packages/pixel-office/src ':!*.test.ts'` shows exactly one hit, in `titleOrNull`.
6. The live harness (`scripts/verify-pixel-office-live.mjs`) is not re-run by this plan. It posts no `task.created`, so every line it draws takes the null branch, which is byte-identical before and after. The phase verifier's own re-run covers regression.
</verification>

<success_criteria>
- No handoff bubble can present a task id, an empty string or a whitespace string as a title, and a test through the real FSM goes red on each regression.
- `getActiveHandoffs().fullTitle` is `string | null` under the same single rule, never an id.
- The blank-is-absent rule exists once (`titleOrNull`) and every title read routes through it.
- App.tsx's two title entry points agree; event-schema is untouched.
- A dead handoff record frees its slot immediately; WR-08's reservation for a current sender is unchanged.
- The docblocks and UI-SPEC rows describe the shipped rule, and the UI-SPEC offset list matches `office-layout.json`.
</success_criteria>

<artifacts_this_phase_produces>
No new files. Symbols created, changed or removed:

| Symbol | File | Change |
|--------|------|--------|
| `titleOrNull` | `packages/pixel-office/src/handoff/dialogue-templates.ts` | NEW export: trimmed title, or `null` when absent or blank; the only `.trim()` in the package's sources |
| `resolveHandoffDialogue` | `packages/pixel-office/src/handoff/dialogue-templates.ts` | routes `taskTitle` through `titleOrNull` before capping; signature unchanged |
| `ActiveHandoff.fullTitle` | `packages/pixel-office/src/handoff/handoff-choreography.ts` | `string` becomes `string \| null`, resolved via `titleOrNull`, never the task id |
| `interactionTileFor` reservation | `packages/pixel-office/src/handoff/handoff-choreography.ts` | reserves a record's target only while `senderIsCurrent(record)` (IN-02) |
| `ActiveHandoff` / `showsLineOf` docblocks | `packages/pixel-office/src/handoff/handoff-choreography.ts` | cap named as `MAX_DIALOGUE_TITLE_CHARS`; nullable fullTitle documented |
| `taskTitles` comment | `packages/pixel-office/src/index.ts` | documents the no-title branch and a null `fullTitle` instead of the deleted id fallback |
| live `task.created` registration | `apps/web/src/App.tsx` | guarded on a non-empty title, like the snapshot path |
| `everyLine` sweep and 05-41 cases | `packages/pixel-office/src/handoff/dialogue-templates.test.ts` | sweep widened to `""` and `"   "`; exact no-title and trimmed-title cases |
| 05-41 cases | `packages/pixel-office/src/handoff/handoff-choreography.test.ts` | blank-title end-to-end cases, null-`fullTitle` cases, IN-02 slot case; IN-01 tautology removed |
| rows at lines 102, 150, 173 | `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` | six interaction offsets; blank-is-absent; nullable `fullTitle` |

No new fixed dialogue literals: blank titles reuse 05-40's two no-title sentences, `hands off to <name>` and `<name> accepts the handoff`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-41-SUMMARY.md` when done.
</output>
