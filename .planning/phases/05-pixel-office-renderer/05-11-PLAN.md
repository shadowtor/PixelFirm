---
phase: 05-pixel-office-renderer
plan: 11
type: execute
gap_closure: true
wave: 6
depends_on: ["05-08"]
autonomous: false
files_modified:
  - packages/claude-adapter/src/claude-code-runtime.ts
  - packages/claude-adapter/src/claude-code-runtime.test.ts
  - apps/api/src/routes/ws-browser.ts
  - apps/api/src/routes/ws-browser.test.ts
  - apps/api/src/ws/browser-connections.ts
  - apps/api/src/ws/browser-connections.test.ts
  - .planning/phases/05-pixel-office-renderer/deferred-items.md
requirements: [OFFICE-01, HANDOFF-01, HANDOFF-02]
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "No GSD role label is ever written into a field typed as an agent id, so no phantom role-named agent is upserted into the projection"
    - "An observed GSD role change is emitted as the observation it is, and the reducer refines the real agent's status from it"
    - "HANDOFF-01 is delivered as its RENDERING half only in Phase 5: a real agent.handoff_requested/agent.handoff_completed pair riding the live relay drives reducer -> character upsert -> choreography FSM -> canvas, end to end. The production TRIGGER that emits such a pair from real work is deferred to Phase 6's multi-agent orchestration, because the only in-repo trigger fabricated a handoff out of a GSD workflow role change and the phase goal forbids fabricated animation. Verify-phase should score the rendering half and read the absent trigger as this recorded scope boundary, not as a gap."
    - "An event committed while the /ws/browser snapshot SELECT is in flight reaches that connection after its snapshot, rather than being lost for the connection's lifetime"
    - "Handoff dialogue stays a pure deterministic template map with zero LLM/network call surface"
  artifacts:
    - packages/claude-adapter/src/claude-code-runtime.ts
    - packages/claude-adapter/src/claude-code-runtime.test.ts
    - apps/api/src/routes/ws-browser.ts
    - apps/api/src/ws/browser-connections.ts
    - apps/api/src/ws/browser-connections.test.ts
    - .planning/phases/05-pixel-office-renderer/deferred-items.md
  key_links:
    - "claude-code-runtime role poll -> gsd.phase_observed envelope -> company-core reduce -> real agent status refinement"
    - "registerBrowserSocket (buffering) -> snapshot send -> flushBrowserSocket -> ordered delivery"
  prohibitions:
    - "MUST NOT write a GSD role label into any field typed as an agent id"
    - "MUST NOT emit a handoff event to represent something that is not a handoff between two agents"
    - "MUST NOT register a browser socket for live relay in a window where its own snapshot has not yet been sent"
    - "MUST NOT treat TaskState.title as inherently safe to interpolate into rendered handoff dialogue/bubble text without limit or filtering"
---

<objective>
Close CR-04 (a GSD role label is written into `record.agentId` and rides out as `sourceAgentId` on every later event into an append-only table) and CR-03 (the `/ws/browser` route registers its socket after the awaited snapshot SELECT, so an event committed inside that window is in neither the snapshot nor the relay and is lost for the connection's lifetime). Record WR-01 as an explicitly accepted, scoped threat rather than leaving it implied.

**CR-04's root cause is the call site, not `completeHandoff`.** `claude-code-runtime.ts`'s role poll fires when `observeGsdState` reports a different GSD workflow role, and passes that role string — `"Engineering"`, `"QA"`, `"Reviewer"` — as `toAgentId`. A GSD role change is an *observation about the workflow*, not a handoff *to an agent named after the role*. The repo already has the right event for it: `gsd.phase_observed` is in `WORKER_ALLOWED_EVENT_TYPES`, its payload already carries `role`, and `company-core`'s reducer already consumes it to refine every currently-running agent's status. `apps/worker/src/poll-loop.ts` already emits exactly that envelope from exactly that observation. This plan makes the runtime do the same thing the worker already does.

Purpose: stop corrupting agent identity at the source, and stop silently dropping events on connect.
Output: an honest role-observation emission, a buffered-then-promoted browser socket registration, and a written record of what is being deferred and why.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@packages/company-core/src/reducer.ts
@packages/event-schema/src/payloads/index.ts
@apps/worker/src/poll-loop.ts
</context>

<interface_context>
Shapes this plan depends on (already on disk):

- `observeGsdState(planningDir, phaseIdOverride, roadmapPhaseCompleted): Promise<GsdObservation>` where `GsdObservation` is `{ phase: string | undefined; status: string; category: string; role: string }` — `packages/gsd-adapter/src/index.ts`. Note it carries no `active` field, by design: the package is a stateless observer and never claims liveness.
- `GsdPhaseObservedPayload` requires `{ phase?: string; status: enum; category: enum; role: enum; active: boolean }` — `packages/event-schema/src/payloads/index.ts`.
- `apps/worker/src/poll-loop.ts` builds that payload and calls `postEvent(controlPlaneUrl, token, buildEnvelope(companyId, "gsd.phase_observed", payload))` — the exact analog to copy.
- `WORKER_ALLOWED_EVENT_TYPES` in `apps/api/src/routes/events.ts` already contains `gsd.phase_observed`, so no allow-list change is needed.
- `company-core`'s `"gsd.phase_observed"` handler re-derives status for every agent whose `rawTaskStatus` is `starting` or `running`, via `deriveAgentStatus({ taskStatus, gsdCategory: event.payload.category })`.
- `registerBrowserSocket(socket)` / `unregisterBrowserSocket(socket)` / `broadcastToBrowsers(message)` / `_resetBrowserSocketsForTests()` — `apps/api/src/ws/browser-connections.ts`, currently backed by a plain `Set<WebSocket>`.
- `requestHandoff(taskId: string, toAgentId: string): Promise<void>` is a required member of the `AgentRuntime` interface (`packages/orchestration-adapter/src/types.ts:38`) and stays. `completeHandoff` is a module-private helper in `claude-code-runtime.ts` and is NOT part of that interface.
</interface_context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: removing the simulated handoff trigger changes what D-04's choreography can demonstrate</name>
  <decision>Whether to delete the role-change poll's simulated handoff trigger — the only thing in this repo that automatically emits an `agent.handoff_requested` / `agent.handoff_completed` pair — as Task 1 proposes.</decision>
  <context>
The trigger passed `observed.role` — a GSD workflow label such as "Engineering" — as `toAgentId`. The completion helper then wrote that label into `record.agentId`, so a role name became `sourceAgentId` on every later event in an append-only table with no UPDATE path. That is CR-04. It also means the office animated a handoff between two agents whenever a *workflow role* changed, which is a fabricated animation — the exact thing this phase's goal forbids.

What it costs: 05-CONTEXT.md **D-04** is a locked user decision — the full choreographed walk-to-desk sequence, chosen explicitly over the cheaper icon-transfer, accepting the pathfinding scope. That choreography stays built, tested and live-wired, and 05-12 proves it end-to-end on a real canvas with both participants created by live events. What changes is that afterwards nothing in the repo *triggers* it from real work on its own: a handoff must be initiated by a caller supplying a real receiving agent id via the public `requestHandoff` interface method, which is Phase 6's multi-agent orchestration territory.

Why this is a decision rather than a revert: restoring an automatic trigger later means building a real role-to-agent registry, not reverting a line. That is why Task 1 is rated `costly`.
  </context>
  <options>
    <option id="delete-trigger">
      <name>Delete the simulated trigger (recommended — this is what Task 1 implements)</name>
      <pros>Closes CR-04 at its source; stops the office asserting a handoff that never happened; moves toward the phase goal's anti-fabrication rule; keeps the whole choreography intact and provable</pros>
      <cons>No in-repo trigger emits a handoff pair until Phase 6; HANDOFF-01 is deliverable only as its rendering half this phase</cons>
    </option>
    <option id="role-registry-now">
      <name>Keep the trigger but resolve the observed role through a role-to-agent registry first</name>
      <pros>Genuinely fixes the identity bug and keeps an automatic trigger; HANDOFF-01 stays end-to-end including its trigger</pros>
      <cons>The registry is Phase 6 scope and pulling it forward widens this gap-closure round substantially; no real second agent exists yet for it to resolve to, so the registry would have nothing truthful to map onto</cons>
    </option>
    <option id="request-only">
      <name>Keep the trigger but emit only the request half with a real fromAgentId, no receiver</name>
      <pros>Smallest diff; preserves some automatic handoff signal</pros>
      <cons>Still fabricates a handoff out of a role change; the FSM can never reach acceptance without the completion half, so the choreography visibly hangs mid-walk; leaves the phase goal's anti-fabrication rule violated</cons>
    </option>
  </options>
  <resume-signal>Reply with the option id (`delete-trigger`, `role-registry-now`, or `request-only`), or "approved" to take the recommended `delete-trigger`. Record the answer against D-04 in the plan SUMMARY.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: A role observation is emitted as an observation, never as a handoff to a role-named agent</name>
  <files>packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts</files>
  <read_first>
    - packages/claude-adapter/src/claude-code-runtime.ts lines 88-135 (`requestHandoff` and the private completion helper, including its ownership-reassignment line) and lines 226-257 (the role poll inside `runQuery` that calls both)
    - packages/claude-adapter/src/claude-code-runtime.test.ts lines 350-500 (the four role-poll cases — one of them codifies the CR-04 bug as intended behaviour and must be replaced, not merely updated)
    - apps/worker/src/poll-loop.ts lines 120-150 (the existing `gsd.phase_observed` payload construction and `postEvent` call — the analog this task copies)
    - packages/event-schema/src/payloads/index.ts (`GsdPhaseObservedPayload`'s required fields and its four enums)
    - packages/gsd-adapter/src/index.ts lines 12-45 (`GsdObservation`'s exact fields and the comment explaining why `active` is not among them)
    - packages/company-core/src/reducer.ts (the `gsd.phase_observed` handler that consumes this, and the `task.status_changed` handler whose `sourceAgentId` was being corrupted)
    - apps/api/src/routes/events.ts lines 10-26 (confirm `gsd.phase_observed` is already allow-listed)
  </read_first>
  <behavior>
    - Test 1: a role change across two poll ticks posts exactly one event, of type `gsd.phase_observed`, whose payload `role` equals the newly observed role and whose `category`/`status`/`phase` come from the same observation.
    - Test 2: no event posted by the role poll has type `agent.handoff_requested` or `agent.handoff_completed`.
    - Test 3: after a role change, the task record's owning agent id is unchanged and still equals the `agentId` supplied to `startTask` — a subsequent `task.status_changed` emission carries that real agent id as `sourceAgentId`, not a role label.
    - Test 4 (kept): two consecutive poll ticks reporting the same role post zero events.
    - Test 5 (kept): the first poll tick only records a baseline and posts nothing.
    - Test 6 (spans two components): the envelope the poll emits, fed through `company-core`'s `reduce` on a state where the real agent is `running`, refines that agent's status per the observed category, and produces NO agent keyed by the role string.
    - Test 7 (kept, rewritten): `requestHandoff` called directly with a real second agent id still posts `agent.handoff_requested` with the correct `fromAgentId`, and still throws for a task with no known agentId.
  </behavior>
  <!-- planner-discipline-allow: completeHandoff -->
  <action>
In `packages/claude-adapter/src/claude-code-runtime.ts`, change what the role poll does when `observed.role !== record.lastRole`.

Remove both handoff calls from that branch. In their place, post a `gsd.phase_observed` envelope built exactly like `apps/worker/src/poll-loop.ts` builds it: `phase` from `observed.phase`, `status` from `observed.status`, `category` from `observed.category`, `role` from `observed.role`, and `active: true`. Pass `taskId` as the envelope's task correlation argument, as the removed calls already did, so the observation is attributable to the task whose worktree was polled.

`active: true` is honest here and must be justified in a comment: this poll only exists inside `runQuery`, for the lifetime of an in-flight Claude Code stream, so at the moment it fires there genuinely is a live process working in that worktree. That is a different and stronger liveness basis than `poll-loop.ts`'s, which has to combine the observation with a separate process-liveness signal.

Delete the module-private handoff-completion helper entirely. Its only caller was the role poll, its ownership-reassignment line (`record.agentId = toAgentId`) is the mechanism by which a role string became `sourceAgentId` on every later event in an append-only table that cannot be corrected by an UPDATE, and it is not part of the `AgentRuntime` interface, so removing it narrows nothing public. Keep `requestHandoff` exactly as it is: it is a required interface member, it correctly refuses to fabricate a `fromAgentId`, and it is correct whenever its caller supplies a real agent id — the defect was never in that function.

Update the file header comment for `requestHandoff` so it no longer describes the role poll as its trigger, and state plainly that the runtime has no automatic handoff trigger: a handoff is initiated by a caller that knows a real receiving agent id, which is Phase 6's multi-agent orchestration territory.

In `packages/claude-adapter/src/claude-code-runtime.test.ts`, rework the role-poll describe block. The existing case asserting that a later event's `sourceAgentId` equals a role label codifies the defect as intended behaviour — delete it outright rather than inverting its assertion, and replace it with Test 3 above, which asserts the opposite property against the real `startTask` agent id. Rewrite the two cases whose names assert that a role change calls the handoff path. Keep the same fake-timer plus `ROLE_POLL_INTERVAL_MS` advance mechanics the existing cases use, and keep the posted-event capture mechanism they already rely on.

For Test 6, import `reduce` and `emptyState` from `company-core` inside the test file and assert both directions: the real agent's status changed, and `Object.keys(nextState.agents)` contains no entry equal to the observed role string.
  </action>
  <verify>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or the reporter still lists a case whose name mentions reassigning the task's agentId, or any role-poll case asserts a handoff event type</fails_when>
    <automated>test "$(grep -v '^[[:space:]]*//' packages/claude-adapter/src/claude-code-runtime.ts | grep -c 'completeHandoff')" = "0"</automated>
    <fails_when>non-zero exit — the completion helper still exists in non-comment source, so the role-string-as-agent-id write path is still reachable</fails_when>
    <automated>test "$(grep -c 'gsd.phase_observed' packages/claude-adapter/src/claude-code-runtime.ts)" != "0"</automated>
    <fails_when>non-zero exit — the role poll emits nothing, so a real role change is now silently swallowed instead of being reported as an observation</fails_when>
    <automated>pnpm --filter company-core test -- reducer</automated>
    <fails_when>non-zero exit — the reducer contract this task's Test 6 depends on regressed</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/claude-adapter/src/claude-code-runtime.ts` contains no assignment to `record.agentId` outside `startTask`.
    - The role-poll branch posts exactly one envelope, of type `gsd.phase_observed`, carrying `phase`, `status`, `category`, `role` and `active`.
    - `requestHandoff` is still exported in the object returned by `createClaudeCodeRuntime` and still throws for a task with no known agentId.
    - The test file contains no assertion that any `sourceAgentId` equals a GSD role name such as `"Engineering"`.
    - Test 6 asserts both that the real agent's status changed and that no agent key equals the role string.
    - `pnpm --filter claude-adapter test -- claude-code-runtime` exits 0.
  </acceptance_criteria>
  <done>A GSD role change produces a `gsd.phase_observed` event that the reducer turns into a real status refinement on the real agent, and no code path can write a role label where an agent id belongs.</done>
  <reversibility rating="costly">This removes the only in-repo producer of `agent.handoff_completed`. The schema member, the reducer handler, the choreography FSM and the browser relay all stay; what goes away is the simulated trigger that was fabricating a handoff out of a workflow role change. Restoring an automatic trigger later means building a real role-to-agent registry (Phase 6 multi-agent orchestration), not reverting a line. Gated behind the `checkpoint:decision` above, carried in `must_haves.truths` so verify-phase scores the rendering half at the right scope, and recorded as a scoped deferral in Task 3.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Buffer the browser socket across the snapshot window so no event is silently dropped</name>
  <files>apps/api/src/ws/browser-connections.ts, apps/api/src/ws/browser-connections.test.ts, apps/api/src/routes/ws-browser.ts, apps/api/src/routes/ws-browser.test.ts</files>
  <read_first>
    - apps/api/src/routes/ws-browser.ts (the whole file — the awaited SELECT at line 34, the snapshot send at line 37, and the registration at line 39 with the comment explaining why it was moved after the send)
    - apps/api/src/ws/browser-connections.ts (the whole file — the `Set<WebSocket>` registry, `broadcastToBrowsers`'s single-stringify loop, and the readyState guard)
    - apps/api/src/routes/ws-browser.test.ts (the existing route tests — their fake-socket shape and how they assert message ordering)
    - apps/api/src/ws/connection-status.test.ts (the sibling module-state test style to match)
    - apps/api/src/routes/events.ts (where `broadcastToBrowsers` is called, immediately after a non-duplicate insert)
  </read_first>
  <behavior>
    - Test 1: a socket registered but not yet flushed receives nothing from `broadcastToBrowsers` at the moment of the call.
    - Test 2 (the CR-03 regression test, spans registry + route): register, broadcast one event, send the snapshot, then flush — the socket's received messages are the snapshot first and that event second. Against the current code the event is received zero times.
    - Test 3: after flushing, a further `broadcastToBrowsers` reaches the socket immediately with no further flush call.
    - Test 4: an unregistered socket receives nothing whether buffered or flushed.
    - Test 5: a socket that is not `OPEN` at flush time does not throw and does not leave a buffer behind.
    - Test 6 (route-level): the `/ws/browser` handler registers before it awaits the snapshot query, and flushes after the snapshot is sent — asserted through observable message order on a fake socket, not by reading line numbers.
  </behavior>
  <action>
Rework `apps/api/src/ws/browser-connections.ts` from a `Set` to a `Map<WebSocket, string[] | null>`, where an array value means the socket is still buffering and `null` means it has been promoted to direct delivery.

`registerBrowserSocket(socket)` now enters the socket in the buffering state with an empty queue. Add `flushBrowserSocket(socket)` which takes the queued payloads, promotes the entry to the direct state, and then sends each queued payload in order if the socket is `OPEN`. `unregisterBrowserSocket(socket)` deletes the entry and its queue. `broadcastToBrowsers(message)` keeps its single `JSON.stringify` and then, per entry, either appends the payload to a still-buffering queue or sends it directly, preserving the existing `readyState === OPEN` guard on the direct path only — a socket mid-handshake must still buffer, which is the whole point. `_resetBrowserSocketsForTests()` clears the map.

Then fix the ordering in `apps/api/src/routes/ws-browser.ts`. Call `registerBrowserSocket(socket)` BEFORE the awaited `db.select()`, send the snapshot as the first message once the fold completes, and call `flushBrowserSocket(socket)` immediately after that send. Replace the existing comment block with one that states the new invariant: registering first closes the drop window, and buffering rather than direct relay preserves the "first message is always the snapshot" guarantee that the previous ordering was protecting.

Handle the failure path explicitly, because registering first means a thrown SELECT would otherwise leave a registered socket accumulating an unbounded queue with no snapshot ever sent: wrap the select-fold-send sequence in a `try`/`catch` that, on error, unregisters the socket and closes it. Do not flush in that path — flushing without a snapshot would deliver live events to a client with no baseline, which is the exact guarantee the original ordering existed to protect.

Create `apps/api/src/ws/browser-connections.test.ts` covering behaviors 1-5 with a minimal fake socket object exposing `readyState`, an `OPEN` constant and a recording `send`. Match `connection-status.test.ts`'s module-state test style and call `_resetBrowserSocketsForTests()` in a `beforeEach`.

Add behavior 6 to `apps/api/src/routes/ws-browser.test.ts`, asserting the observable message sequence rather than inspecting source.
  </action>
  <verify>
    <automated>pnpm --filter api test</automated>
    <fails_when>non-zero exit, or the reporter does not list `browser-connections.test.ts`, or the snapshot-ordering case in `ws-browser.test.ts` passes without a buffered event being asserted in second position</fails_when>
    <automated>test "$(grep -c 'flushBrowserSocket' apps/api/src/routes/ws-browser.ts)" != "0"</automated>
    <fails_when>non-zero exit — the route never promotes the socket, so every relayed event would stay queued forever</fails_when>
  </verify>
  <acceptance_criteria>
    - In `apps/api/src/routes/ws-browser.ts`, the `registerBrowserSocket` call appears at a lower line number than the `db.select` call.
    - The `flushBrowserSocket` call appears after the `socket.send` that carries the snapshot.
    - The error path unregisters and closes the socket and does not call `flushBrowserSocket`.
    - `apps/api/src/ws/browser-connections.ts` exports `registerBrowserSocket`, `flushBrowserSocket`, `unregisterBrowserSocket`, `broadcastToBrowsers` and `_resetBrowserSocketsForTests`.
    - `apps/api/src/ws/browser-connections.test.ts` exists and contains a case asserting a broadcast before flush is delivered after the flush, in order.
    - `pnpm --filter api test` exits 0.
  </acceptance_criteria>
  <done>An event committed while the snapshot SELECT is in flight arrives at the connecting browser after its snapshot instead of vanishing for the life of the connection.</done>
  <reversibility rating="reversible">A per-socket queue is in-memory only; reverting to the `Set` restores prior behaviour with no persisted state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Record the accepted threat and the scoped deferrals in writing</name>
  <files>.planning/phases/05-pixel-office-renderer/deferred-items.md</files>
  <read_first>
    - .planning/phases/05-pixel-office-renderer/deferred-items.md (the existing two entries and their format — heading, prose, explicit "Fix, when someone picks this up" section)
    - .planning/phases/05-pixel-office-renderer/05-VERIFICATION.md (`advisory:` entries, especially WR-01's wording: "If that is Phase 6/7 work it should be recorded as an accepted threat with the same rigour as T-05-03 rather than left implied")
    - apps/api/src/routes/events.ts lines 30-70 (the request-body fields WR-01 concerns: `sourceAgentId`, `companyId`, `visibility` taken verbatim, versus the heartbeat path keying on `request.workerId`)
    - apps/api/src/routes/ws-browser.ts (the existing T-05-03 comment, for the tone and rigour to match)
    - packages/orchestration-adapter/src/types.ts (confirm `requestHandoff` is interface-required and the completion helper is not)
  </read_first>
  <action>
Append three entries to `.planning/phases/05-pixel-office-renderer/deferred-items.md`, following the existing file's format: a level-2 heading, prose stating what is deferred and why, and a closing paragraph naming what picking it up would involve. These are the written record that nothing in the gap set was silently dropped.

**Entry 1 — WR-01, accepted threat, scoped to Phase 6/7.** State the finding precisely: `apps/api/src/routes/events.ts` takes `sourceAgentId`, `companyId` and `visibility` verbatim from the request body, while only the heartbeat path keys on the authenticated `request.workerId`, so a valid worker credential can author state for any agent, any company and any visibility level. State why it is accepted rather than fixed here: the fix is subject authorization, not event-type authorization, and it requires the workers table to carry agent and company columns — a schema change whose consumers are Phase 6's CEO dashboard and Phase 7's visibility filtering, both of which own the authorization model this would slot into. State the blast radius honestly: the 05-05 allow-list widening increased the set of types a compromised credential can forge, and nothing in Phase 5 narrows the subject. Record it with the same rigour as the existing T-05-03 acceptance: name the threat id `T-05-11-WR01`, the category (Spoofing / Elevation of Privilege), the severity (high), and the condition under which the acceptance expires — the first moment any non-INTERNAL consumer is pointed at this control plane.

**Entry 2 — `agent.handoff_completed` has no in-repo producer.** State that Task 1 removed the role-poll's simulated handoff pair because a GSD workflow role change is not a handoff to an agent named after the role, and that this leaves the event type with no automatic producer. State what remains intact: the schema member, the reducer handler, the choreography FSM, the browser relay, and the public `requestHandoff` interface method, which is correct whenever a caller supplies a real receiving agent id. State what is deferred and to where: a real trigger requires a role-to-agent registry or genuine multi-agent orchestration, which is Phase 6 scope. Explicitly carry forward 05-VERIFICATION.md's unmet ask — "an end-to-end check that drives ClaudeCodeRuntime's own emission path rather than hand-posted synthetic events" — and record why it is not satisfiable in this round: after this fix there is no legitimate ClaudeCodeRuntime handoff emission path to drive, and manufacturing one would be the fabrication the Core Value forbids.

**Entry 3 — office capacity and identity-hue ceilings.** Record the two known ceilings 05-10-PLAN.md introduces deliberately: the default 20x11 grid now seats 54 desks rather than 162, because a desk row pitch of 3 is the smallest that gives a status glyph clearance over the desk row behind it; and per-agent identity uses 12 evenly spaced hue buckets, so colour collisions begin at the thirteenth seated agent. Name the upgrade paths: a larger grid or a scrolling camera for capacity, and on-canvas name labels for identity — 05-UI-SPEC.md's Typography section already reserves the monospace 11px scale for exactly that.

Do not modify `.planning/REQUIREMENTS.md` in this task. OFFICE-01, OFFICE-03 and HANDOFF-01 stay at `Gaps Found`; they flip only when verify-phase says so.
  </action>
  <verify>
    <automated>test "$(grep -c 'T-05-11-WR01' .planning/phases/05-pixel-office-renderer/deferred-items.md)" != "0"</automated>
    <fails_when>non-zero exit — WR-01 was not recorded as a named accepted threat and remains implied, which is exactly what 05-VERIFICATION.md rejected</fails_when>
    <automated>test "$(grep -c 'agent.handoff_completed' .planning/phases/05-pixel-office-renderer/deferred-items.md)" != "0"</automated>
    <fails_when>non-zero exit — the removed-producer consequence of Task 1 is undocumented, so a future reader would read the missing producer as an oversight rather than a decision</fails_when>
    <automated>git diff --name-only -- .planning/REQUIREMENTS.md</automated>
    <fails_when>the command prints `.planning/REQUIREMENTS.md`, meaning requirement statuses were edited by a plan task rather than by verify-phase</fails_when>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` gained exactly three new level-2 headings.
    - The WR-01 entry names a threat id, a STRIDE category, a severity of high, and an expiry condition.
    - The handoff-producer entry explicitly restates 05-VERIFICATION.md's "drives ClaudeCodeRuntime's own emission path" ask and says why it is not satisfiable after Task 1.
    - The ceilings entry names both 54 desks and 12 hue buckets.
    - `.planning/REQUIREMENTS.md` is unmodified.
  </acceptance_criteria>
  <done>Every advisory and every consequence of this round's decisions is written down with a named owner phase, so the next verification reads decisions rather than gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker credential -> `POST /events` | Authenticated but un-scoped: the credential proves a valid worker, not which subject it may author for |
| `observeGsdState` filesystem read -> emitted event payload | Observed workflow text becomes asserted company state |
| `POST /events` commit -> `/ws/browser` connection lifecycle | Ordering between a durable write and a connection's baseline snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11-01 | Spoofing | `claude-code-runtime.ts` role poll | high | mitigate | Task 1: a GSD role label can no longer be written into `record.agentId`, so no later event carries a role name as `sourceAgentId` and the reducer cannot upsert a phantom role-named agent into an append-only log. |
| T-05-11-02 | Repudiation | append-only `events` table | high | mitigate | Task 1: the corruption was permanent by construction (no UPDATE path exists on an append-only log), so preventing the write is the only available mitigation. Historical rows already carrying role names stay as they are and are covered by the deferred-items record. |
| T-05-11-03 | Denial of Service | `/ws/browser` snapshot window | medium | mitigate | Task 2: buffer-then-promote closes the silent drop window; the error path unregisters and closes rather than leaving an unbounded queue attached to a socket that will never get a snapshot. |
| T-05-11-04 | Denial of Service | per-socket buffer growth | low | accept | The buffer lives only for the duration of one `db.select` over the events table. An operator-scale event rate during that window is bounded by the same rate the relay already sustains. |
| T-05-11-WR01 | Spoofing / Elevation of Privilege | `apps/api/src/routes/events.ts` subject fields | high | accept | `sourceAgentId`, `companyId` and `visibility` are taken verbatim from the request body; only the heartbeat path keys on the authenticated `request.workerId`. Accepted for Phase 5 because the fix is subject authorization requiring agent/company columns on the workers table, owned by Phase 6/7 alongside the authorization model those phases introduce. The control plane is INTERNAL-tier only (T-05-03, already accepted). **Acceptance expires the first moment any non-INTERNAL consumer is pointed at this control plane.** Recorded in writing by Task 3, not left implied. |
| T-05-11-05 | Information Disclosure | `TaskState.title` interpolated into handoff dialogue | medium | transfer | Carried forward unchanged to Phase 7 (SAFE-01/SAFE-02). This plan does not render `bubbleText` and does not widen what reaches a client. |
</threat_model>

<planner_assumptions>
The deterministic edge probe returned `unclassified` for all five Phase 5 requirement IDs and proposed zero applicable edges — per the spec-less fallback these stay **unresolved** and are surfaced here rather than dropped:

- **OFFICE-01 / HANDOFF-01 / HANDOFF-02 — edge classification unresolved.** The edges this plan encodes (first poll tick, unchanged role, socket not OPEN at flush time, SELECT throwing mid-window) were derived by reading the call sites, not lifted from a probe.

Prohibition dispositions after this plan:

- "MUST NOT write a GSD role label into any field typed as an agent id" — CLOSED by Task 1, enforced by a test and a non-comment source grep.
- "MUST NOT treat `TaskState.title` as inherently safe to interpolate into rendered handoff dialogue/bubble text without limit or filtering" — still DEFERRED to Phase 7 (SAFE-01/SAFE-02), unchanged from 05-VERIFICATION.md's own disposition. Legitimately outside this phase's boundary; flagged forward, not dropped.

Assumption flagged for the executor: `observeGsdState` returns `status`, `category` and `role` as plain `string`, while `GsdPhaseObservedPayload` narrows each to an enum. `apps/worker/src/poll-loop.ts` already emits the same un-narrowed values through the same schema and has been in production since Phase 3, so the mapping is known to hold in practice; `POST /events` validates with `CompanyEventSchema.safeParse` and returns 400 rather than storing anything malformed. If a value ever falls outside an enum the failure is a visible 400, not a silent corruption.
</planner_assumptions>

<artifacts_this_phase_produces>
New symbols created by this plan (excluded from drift verification):

- `flushBrowserSocket(socket): void` — new export, `apps/api/src/ws/browser-connections.ts`
- `apps/api/src/ws/browser-connections.test.ts` — new file
- Threat id `T-05-11-WR01` — new identifier introduced in this plan's threat register and in `deferred-items.md`

Symbols REMOVED by this plan:

- the module-private handoff-completion helper in `packages/claude-adapter/src/claude-code-runtime.ts` (the CR-04 write path)
</artifacts_this_phase_produces>

<verification>
- `pnpm --filter claude-adapter test -- claude-code-runtime` exits 0 with no case asserting a role label as an agent id.
- `pnpm --filter api test` exits 0 and includes `browser-connections.test.ts`.
- `pnpm --filter company-core test -- reducer` exits 0.
- Non-comment source in `claude-code-runtime.ts` contains no reference to the removed completion helper.
- `.planning/REQUIREMENTS.md` is untouched by this plan.
</verification>

<success_criteria>
- CR-04 closed at its source: a role observation is emitted as an observation and the real agent keeps its identity.
- CR-03 closed: the connect-time drop window is gone and the snapshot-first guarantee survives.
- WR-01 is an explicitly named, severity-rated, expiry-bounded accepted threat rather than an implied one.
- Every consequence of removing the simulated handoff trigger is written down with its owning phase.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-11-SUMMARY.md` when done
</output>
