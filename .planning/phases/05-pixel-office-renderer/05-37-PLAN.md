---
phase: 05-pixel-office-renderer
plan: 37
type: execute
gap_closure: true
gap_ids: [WR-07, WR-08]
wave: 2
depends_on: ["05-36"]
files_modified:
  - packages/pixel-office/src/types.ts
  - packages/pixel-office/src/handoff/handoff-choreography.ts
  - packages/pixel-office/src/handoff/handoff-choreography.test.ts
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
autonomous: true
requirements: [HANDOFF-01, HANDOFF-02]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WR-08 / HANDOFF-01 (D-04): a handoff's interaction slot stays reserved for the WHOLE record, departure included — a second request arriving while the first sender is walking home is never handed the tile that sender is still standing on"
    - "WR-08: the reservation is bounded, not permanent — once retireHandoff removes the record (the sender is home, the only record exit), that slot is available to the next sender again"
    - "WR-07 / HANDOFF-02 (D-04): getActiveHandoffs identifies its speaker by record identity, not by string equality — two live handoffs whose dialogue lines cap to the same text resolve at most one speaker between them; the record whose line was overwritten reports speakerId null and box null rather than another record's rect"
    - "WR-07: the same identity rule governs the CLEAR paths — retiring or completing a handoff clears a character's line only when that line belongs to that record, so a newer line written by a second handoff with identical text survives"
    - "Nothing 05-33 / 05-34 / 05-35 proved regresses: MAX_DIALOGUE_TITLE_CHARS is still 12, getActiveHandoffs still returns fresh read-only plain objects carrying fullTitle / fromAgentId / toAgentId / phase / speakerId / box, the fixed aisle slot preference order and Chebyshev-1 spacing are unchanged, and the four-candidate bubble scorer is untouched"
  artifacts:
    - packages/pixel-office/src/types.ts
    - packages/pixel-office/src/handoff/handoff-choreography.ts
    - packages/pixel-office/src/handoff/handoff-choreography.test.ts
  key_links:
    - "handoff-choreography.ts writes Character.bubbleTextTaskId together with bubbleText/bubbleTextPartnerId (the existing sibling-field pattern in types.ts) -> one identity predicate reads it -> that predicate is the single source for both the clear paths (retireHandoff, the completion branch) and the read path (getActiveHandoffs)"
    - "HandoffRecord.target -> interactionTileFor's `taken` set, for every live record regardless of phase -> interactionSlotsFor's fixed aisle slots -> walkCharacterTo, so the slot is released only by retireHandoff"
    - "getActiveHandoffs speakerId/speakerText -> renderer getDialogueBox(speakerId, text) -> the drawn rect a Phase 6 consumer will hit-test"
  prohibitions:
    - "MUST NOT make getActiveHandoffs a writer — it stays read-only and returns fresh plain objects; a host mutating a result must still change neither the FSM nor the next call"
    - "MUST NOT introduce Math.random, Date.now, a timer, fetch, WebSocket or any dynamic import into packages/pixel-office/src — the pure-consumer property is grep-enforced and is the phase's Core Value"
    - "MUST NOT extend the slot reservation past record retirement — a reservation that outlives its record would strand aisle slots for the tab's lifetime"
    - "MUST NOT touch STATUS_MAP, the pose FSM, the frozen flag or applyBubble's glyph precedence (D-01, D-03), and MUST NOT change MAX_DIALOGUE_TITLE_CHARS, the dialogue templates or the bubble geometry (HANDOFF-02)"
    - "MUST NOT source, generate or swap any animation or glyph asset in this round — D-02's sourcing triage is not re-entered"
---

<objective>
Close review findings WR-07 and WR-08 — the two architectural advisories `05-VERIFICATION.md`
recorded with no reproducible failure and recommended closing "before Phase 6 puts a real consumer
on `getActiveHandoffs()`".

**Decision: planned, not deferred.** Both are confirmed-by-source defects whose fixes are a few
lines each in a package with an existing behavioural suite, and both become live bugs the moment
Phase 6 (CEO Dashboard) consumes the accessor or raises concurrent-handoff volume. Deferring costs
a rediscovery in a phase that will be reading these values for a human approval decision;
WR-07 in particular hands a host a bubble rect attributed to the wrong task. The reason they have
no red test today is that nothing consumes the accessor yet — which is exactly the window in which
they are cheap to fix.

Purpose: make speaker attribution an identity check rather than a string comparison, and make an
aisle slot reserved for as long as a character is standing on it.

Output: one new `Character.bubbleTextTaskId` field, one identity predicate replacing four
string-equality checks, one deleted phase exemption in the slot-reservation set, and a named
regression test for each.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@.planning/phases/05-pixel-office-renderer/05-REVIEW.md
@.planning/phases/05-pixel-office-renderer/05-34-SUMMARY.md
@.planning/phases/05-pixel-office-renderer/05-35-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reserve a handoff's aisle slot for the whole record, departure included (WR-08)</name>
  <files>packages/pixel-office/src/handoff/handoff-choreography.ts, packages/pixel-office/src/handoff/handoff-choreography.test.ts, .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>
  <read_first>
    - packages/pixel-office/src/handoff/handoff-choreography.ts lines 60-125 (the `interactionTileFor` docstring — including the `ponytail:` null-bound note WR-03 corrected — and the `taken` construction, the Chebyshev-1 rejection, the walkability re-check and the `findPath` requirement)
    - packages/pixel-office/src/handoff/handoff-choreography.ts lines 145-175 (`retireHandoff` — the ONLY place a record ends) and lines 225-315 (the `agent.handoff_completed` branch that sets `record.phase = "RETURNING_TO_DESK"` and starts the walk home, and `checkHandoffArrivals`'s `RETURNING_TO_DESK` arm that retires on arrival)
    - packages/pixel-office/src/handoff/handoff-choreography.test.ts lines 320-420 (the `real update loop` describe's helpers: `run(seconds)`, `seatAll()`, `atInteractionSlot(ch, other)`, `toIconVisible()`, `toReturningMidWalk()`) and the `interaction tile (05-27, G-05-1d; fixed slots 05-34, G-05-P2)` describe near line 798 (the existing concurrent-sender cases this one joins)
    - packages/pixel-office/src/layout/officeLayout.ts lines 90-150 (`homeNearRow`, `interactionSlotsFor` — the fixed slot set the reservation applies to)
    - .planning/phases/05-pixel-office-renderer/05-REVIEW.md (WR-08's exact claim and its recommended fix)
  </read_first>
  <behavior>
    - Test 1 (red first): drive handoff A to `ICON_VISIBLE` with the existing helpers, record the
      sender's interaction tile, deliver `agent.handoff_completed` for A so the record flips to
      `RETURNING_TO_DESK` and the sender starts walking home, then — on that same tick, before
      stepping the loop — deliver an `agent.handoff_requested` from a DIFFERENT sender to the SAME
      receiver. The second sender's target must differ from A's tile and must be more than
      Chebyshev distance 1 from it. Fails today: the departing record's target leaves `taken` while
      its sender is still standing there.
    - Test 2 (boundedness): from the same setup, step the loop until sender A reaches its seat and
      its record retires, then request a further handoff to the same receiver. That slot is
      available again — the reservation ends with the record, it does not accumulate.
    - Test 3 (regression): the existing named cases — `the sender stops on the receiver's first
      fixed slot`, `the second concurrent sender gets the next fixed slot`, `three senders take the
      fixed preference order`, `never shoulder-to-shoulder` — all still pass unchanged.
  </behavior>
  <action>
In `packages/pixel-office/src/handoff/handoff-choreography.ts`'s `interactionTileFor`, the loop that
adds live records' targets to `taken` exempts records in the `RETURNING_TO_DESK` phase. Delete that
phase clause so the condition tests only that the record is not the requesting sender's own — the
target is reserved for the record's whole lifetime, which ends at `retireHandoff` and nowhere else.
Keep the `record.fromChar !== fromChar` clause exactly as it is: a sender must still be allowed to
stand on the tile its own record already reserved.

Update that function's docstring to say what is now true: a slot is taken while another present
character's seat, its tile when it is not walking, or another live record's target — **in any
phase, departure included** — lies within Chebyshev distance 1. State the reason in one line: the
record flips to `RETURNING_TO_DESK` and the character to `WALK` on the same tick, so a
phase-exempted target is a tile that is still physically occupied on screen. Cite `review WR-08`.
Leave the `ponytail:` null-bound note intact — the bound it states is about surviving slots and is
unaffected — but append one sentence noting that a departing sender now holds its slot until it is
home, which shortens slot availability by the length of one walk home.

Do not change `interactionSlotsFor`, the `colOffsets` data in `office-layout.json`, the walkability
re-check, or the `findPath` requirement.

Then in `packages/pixel-office/src/handoff/handoff-choreography.test.ts`, add the two cases above to
the existing `interaction tile (05-27, G-05-1d; fixed slots 05-34, G-05-P2)` describe block, reusing
that describe's own helpers (`run`, `seatAll`, `atInteractionSlot`, `toIconVisible`,
`toReturningMidWalk`) rather than writing new drivers — these tests must drive the real update loop
through `stepOffice`, never teleport a character. Name them for the property, for example `a
departing sender keeps its aisle slot reserved until it is home` and `the slot is free again once
the record retires`. Run the full package suite: if an existing case relied on the freed slot, it
encoded the defect — update it and record that in the summary rather than restoring the exemption.

Finally, update `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md`: in the Spacing Scale
section's `**Updated 05-34 (G-05-P2):**` paragraph, the sentence listing when a slot is unusable
says "no other live handoff target lies within one tile of it". Append a `**Corrected 05-37
(WR-08):**` clause stating that a live target now includes a record whose sender is walking home,
because that sender is still standing on the tile.
  </action>
  <reversibility rating="reversible">One boolean clause; restoring it restores the previous behaviour exactly.</reversibility>
  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
    <automated>pnpm --filter pixel-office test -- handoff-choreography</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests run (the name filter matched nothing)</fails_when>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: with handoff A in `RETURNING_TO_DESK` and its sender still on its interaction tile, a new request to the same receiver yields a target whose Chebyshev distance from A's tile is greater than 1.
    - Behavior assertion: after A's sender arrives home and its record retires, a further request to the same receiver may be given A's former tile — proving the reservation is bounded by the record, not permanent.
    - Source assertion: `grep -n 'RETURNING_TO_DESK' packages/pixel-office/src/handoff/handoff-choreography.ts` prints the phase-union declaration, the phase-transition assignment, `checkHandoffArrivals`'s arm and `getActiveHandoffs`'s accepted-line branch — and NO line inside `interactionTileFor`'s `taken` construction (read the printed line numbers against that function's range).
    - Test-command assertion: `pnpm --filter pixel-office test` passes with a test count greater than or equal to the pre-task count, and the four named 05-34 slot cases still appear in the run.
    - Source assertion: `packages/pixel-office/src/layout/office-layout.json` and `officeLayout.ts` are unchanged by this task.
  </acceptance_criteria>
  <done>A departing sender's aisle slot is unavailable to a concurrent request until its record retires; both new cases drive the real update loop; the full `pixel-office` suite is green and the 05-34 slot behaviour is intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Identify the speaker by record identity, not by dialogue text (WR-07)</name>
  <files>packages/pixel-office/src/types.ts, packages/pixel-office/src/handoff/handoff-choreography.ts, packages/pixel-office/src/handoff/handoff-choreography.test.ts</files>
  <read_first>
    - packages/pixel-office/src/types.ts lines 140-160 (the `Character` bubble fields — `bubbleText` and its sibling `bubbleTextPartnerId`, including the "Written only by handoff-choreography.ts, together with bubbleText" contract the new field must follow)
    - packages/pixel-office/src/handoff/handoff-choreography.ts — EVERY site that writes or clears `bubbleText`: the `retireHandoff` receiver clear and sender clear (around lines 155-165), the completion branch's sender clear and receiver write (around lines 238-258), and the `WALKING_TO_RECEIVER` -> `ICON_VISIBLE` sender write (around lines 298-302)
    - packages/pixel-office/src/handoff/handoff-choreography.ts lines 340-412 (the `ActiveHandoff` interface, `getActiveHandoffs`, and the CR-01 freshness comment explaining why `speakerText` is carried alongside `speakerId` for the `getDialogueBox` lookup)
    - packages/pixel-office/src/handoff/dialogue-templates.ts (`resolveHandoffDialogue`, `MAX_DIALOGUE_TITLE_CHARS = 12`, `MAX_DIALOGUE_NAME_CHARS = 10` — the capping that makes two distinct titles collide)
    - packages/pixel-office/src/engine/renderer.ts around lines 405-415 and 555-575 (`getDialogueBox(agentId, text)` and the `framePlacements` push — the drawn-text match stays as it is; this task changes only who is named as the speaker)
    - packages/pixel-office/src/handoff/handoff-choreography.test.ts — the `getActiveHandoffs` describe added by 05-35 (its 7 named cases, including the two CR-01 freshness cases and `is read-only: mutating a returned entry changes neither the next call nor the FSM`)
    - .planning/phases/05-pixel-office-renderer/05-REVIEW.md (WR-07's exact claim and its recommended `bubbleTextTaskId` fix)
  </read_first>
  <behavior>
    - Test 1 (red first): register two task titles that are distinct but identical through the
      first 12 code points (for example `Refactor the billing exporter` and `Refactor the billing
      importer`, which both cap to the same 11 characters plus U+2026), hand both to the SAME
      receiver, and drive both records to `RETURNING_TO_DESK` so both compute the same
      `acceptedText`. `getActiveHandoffs()` must report exactly ONE entry with a non-null
      `speakerId` — the record whose line the receiver's bubble actually carries — and the other
      entry must report `speakerId: null` and `box: null`. Fails today: both entries claim the
      receiver and the same rect.
    - Test 2: the same collision on the sender side — one sender holding two live records whose
      requested lines cap identically resolves at most one speaker.
    - Test 3 (clear path): retiring the record whose line is NOT currently displayed must leave the
      displayed line and its stamp intact; retiring the record whose line IS displayed clears
      `bubbleText`, `bubbleTextPartnerId` and the stamp together.
    - Test 4 (regression): all seven 05-35 `getActiveHandoffs` cases still pass, `fullTitle` is
      still the untruncated title, and a mutated returned entry still changes nothing.
  </behavior>
  <action>
Add `bubbleTextTaskId?: string | null;` to the `Character` interface in
`packages/pixel-office/src/types.ts`, directly after `bubbleTextPartnerId`, following that field's
established shape and doc-comment convention: state that it is the `taskId` of the handoff record
whose line `bubbleText` currently holds, that it is written only by `handoff-choreography.ts`
together with `bubbleText`, and that it exists so a handoff can recognise its OWN line rather than
inferring ownership from string equality — two records to one receiver whose titles cap to the same
12 code points produce byte-identical lines (`review WR-07`).

In `packages/pixel-office/src/handoff/handoff-choreography.ts`, introduce two small module-private
helpers next to the existing record helpers and route every site through them, so the identity rule
has exactly one definition:

- a predicate that answers whether a given `Character` is currently showing a given record's line,
  by comparing `bubbleTextTaskId` against `record.taskId`;
- a clear function that nulls `bubbleText`, `bubbleTextPartnerId` and `bubbleTextTaskId` together,
  so the stamp can never outlive the text it names.

Then replace the string comparisons at all four sites with the predicate, and the three-field
clears with the clear function: `retireHandoff`'s receiver clear and its sender clear, the
`agent.handoff_completed` branch's sender clear, and both branches of `getActiveHandoffs`. This is
the root-cause fix — the clear paths carry the same defect as the read path, so fixing only
`getActiveHandoffs` would leave a record able to wipe another record's identical-looking line.

At the two write sites, stamp the field alongside the text it accompanies: the sender write in the
`WALKING_TO_RECEIVER` -> `ICON_VISIBLE` transition sets it to `record.taskId` next to
`bubbleText = record.requestedText` and `bubbleTextPartnerId = record.toAgentId`; the receiver write
in the completion branch sets it to that record's `taskId` next to
`bubbleText = record.acceptedText` and `bubbleTextPartnerId = null`.

Keep `speakerText` and the `getDialogueBox(speakerId, speakerText)` lookup exactly as they are —
that comparison is CR-01's frame-freshness guard against a rect from a frame that painted a
different line, and it is a different property from speaker identity. Keep the
`record.requestedText !== null` / `record.acceptedText !== null` guards, since `speakerText` feeds
that lookup. Keep `senderIsCurrent(record)` in the `ICON_VISIBLE` branch. Update the comment above
those branches: identity now comes from the stamp the writer left, and the text comparison that
remains is the box-freshness check, not the attribution.

Add the four behaviours above to the `getActiveHandoffs` describe in
`packages/pixel-office/src/handoff/handoff-choreography.test.ts`, reusing that file's existing
event builders (`requestedEvent`, `completedEvent`) and real-update-loop helpers. Assert the
collision is genuine in the test itself — that both records' computed lines are byte-identical —
so the case cannot silently stop testing the collision if the caps ever change.
  </action>
  <reversibility rating="reversible">An additive optional field plus two helpers; the string comparisons could be restored, though doing so restores the defect.</reversibility>
  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
    <automated>pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit (tsc reports an error)</fails_when>
    <automated>grep -rn "Math.random\|Date.now\|fetch(\|WebSocket\|XMLHttpRequest" packages/pixel-office/src --include=*.ts</automated>
    <fails_when>any match on a line that is not a comment (a leading // or * after optional whitespace) — the renderer must stay a pure consumer</fails_when>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: two live records to the same receiver whose `acceptedText` values are byte-identical yield exactly one `getActiveHandoffs()` entry with a non-null `speakerId`; the other reports `speakerId: null` and `box: null`.
    - Behavior assertion: retiring the record whose line is not displayed leaves the displayed character's `bubbleText` and `bubbleTextTaskId` unchanged.
    - Source assertion: `grep -n "bubbleTextTaskId" packages/pixel-office/src/types.ts` prints a field declaration inside the `Character` interface, adjacent to `bubbleTextPartnerId`; and `grep -n "bubbleText" packages/pixel-office/src/handoff/handoff-choreography.ts` shows every `bubbleText =` assignment paired with a `bubbleTextTaskId` assignment in the same statement group, or routed through the clear helper.
    - Source assertion: `grep -n "MAX_DIALOGUE_TITLE_CHARS" packages/pixel-office/src/handoff/dialogue-templates.ts` still shows the declaration bound to `12`.
    - Test-command assertion: `pnpm --filter pixel-office test` passes, and the seven 05-35 `getActiveHandoffs` cases all still appear in the run output.
    - CLI assertion: `pnpm --filter web typecheck` is clean — the new optional field breaks no consumer.
  </acceptance_criteria>
  <done>Speaker attribution is an identity check against the writer's own stamp, the clear paths use the same predicate, colliding capped titles can no longer make two records claim one bubble, and everything 05-35 proved about `getActiveHandoffs` still holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pixel-office` engine -> host (`apps/web`, and Phase 6's dashboard) | `getActiveHandoffs()` is the read surface a host will hit-test and, in Phase 6, route a human approval decision through |
| relayed `CompanyEvent` -> handoff FSM | untrusted-ordering input: events arrive off the WS handler, not off the render loop |
| repo -> npm registry | package installs (none in this round) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-37-01 | Spoofing | `getActiveHandoffs()` speaker attribution | medium | mitigate | This IS WR-07: a record can currently claim another record's bubble and rect purely because their capped lines match. Task 2 replaces the equality check with a `bubbleTextTaskId` stamp written by the only writer, so a host cannot be handed a rect attributed to the wrong task. |
| T-05-37-02 | Tampering | the dialogue-line clear paths | medium | mitigate | The same string-equality identity governs `retireHandoff` and the completion branch, so one record can wipe another's identical-looking line. Routed through the same single predicate, so the read path and the clear paths cannot drift. |
| T-05-37-03 | Information Disclosure | `ActiveHandoff.fullTitle` (untruncated `TaskState.title`) | medium | accept | Unchanged by this round and already dispositioned: stream-safety filtering of `TaskState.title` is deferred to Phase 7 (`05-VERIFICATION.md` deferred item 1, Phase 7 goal). No new consumer ships here, and `dialogue-templates.ts` still bounds length only and says so in-source (SAFE-01/02). |
| T-05-37-04 | Denial of Service | aisle-slot exhaustion via the widened reservation | low | mitigate | Reservations now span the walk home. `retireHandoff` is the only record exit and is driven by real arrival, so reservations are bounded by live records; Task 1's second case asserts the slot returns. `interactionTileFor`'s existing `null` fallback (the sender stays at its own tile) remains the documented worst case. |
| T-05-37-05 | Tampering | the phase's Core Value (pure consumer) | high | mitigate | Neither task may add a timer, random source or network call to `packages/pixel-office/src`; Task 2 carries a grep verify over the whole package source for `Math.random` / `Date.now` / `fetch(` / `WebSocket` / `XMLHttpRequest`, comment lines excluded. |
| T-05-37-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this round — no `package.json` or lockfile is in `files_modified`, and no new import is introduced (the fix is one field on an existing interface). `05-RESEARCH.md`'s Package Legitimacy Audit is not re-entered; no `[ASSUMED]`/`[SUS]` package appears, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `pnpm --filter pixel-office test` — green, including both new WR-08 cases and the four new WR-07 cases, with every 05-32 / 05-33 / 05-34 / 05-35 named case still present.
- `pnpm --filter web typecheck` — clean (the new `Character` field is optional, so no consumer breaks).
- `pnpm --filter web test` — green (05-36's assertions are unaffected by engine-internal changes).
- Pure-consumer grep over `packages/pixel-office/src` — no non-comment match for `Math.random`, `Date.now`, `fetch(`, `WebSocket` or `XMLHttpRequest`.
- The live harness is NOT re-run for this plan: it drives one handoff at a time and cannot observe a two-record collision or a same-tick slot contest. Both properties are proven by the real-update-loop unit tests, which is the surface that can actually reach them.
</verification>

<success_criteria>
- With two identical-looking live lines, exactly one record reports a speaker and a box.
- A departing sender's slot is unavailable to a concurrent request, and available again once its record retires.
- Removing the stamp or restoring the phase exemption turns `pnpm --filter pixel-office test` red.
- `MAX_DIALOGUE_TITLE_CHARS`, the templates, the bubble geometry, the fixed slot data and the four-candidate scorer are all byte-identical to their pre-plan values.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location | Note |
|---|---|---|---|
| `Character.bubbleTextTaskId` | new optional interface field (`string \| null`) | `packages/pixel-office/src/types.ts` | the identity stamp; written only alongside `bubbleText` |
| identity predicate (character-shows-this-record's-line) | module-private function | `packages/pixel-office/src/handoff/handoff-choreography.ts` | the single definition of speaker/line ownership, used by both clear paths and the read path |
| line-clear helper (nulls text, partner id and stamp together) | module-private function | `packages/pixel-office/src/handoff/handoff-choreography.ts` | so the stamp can never outlive its text |
| `a departing sender keeps its aisle slot reserved until it is home` | test case | `handoff-choreography.test.ts` | WR-08 regression |
| `the slot is free again once the record retires` | test case | `handoff-choreography.test.ts` | WR-08 boundedness |
| four colliding-line cases (receiver-side, sender-side, clear-path, read-only regression) | test cases | `handoff-choreography.test.ts` | WR-07 regression |

No new file, no new dependency, no change to any exported function signature. `ActiveHandoff`'s
shape is unchanged — only which record is allowed to claim a `speakerId` changes.

<planner_assumptions>
Flagged, not authored into `must_haves.truths`.

**A1 — Deterministic edge probe (all five rows `unclassified` -> `unresolved`).** Phase 5 has no
plain `-SPEC.md`, so `## Edge Coverage` and `## Prohibitions` are absent and the specless fallback
fires. The probe returned `unclassified` for `OFFICE-01`, `OFFICE-02`, `OFFICE-03`, `HANDOFF-01` and
`HANDOFF-02` (5 applicable, 0 resolved, 5 unresolved, 0 explicit, 0 backstop). Per #1110 none is
auto-resolved with a backstop and none becomes a truth. Recorded as assumptions that each
requirement's edge behaviour is governed by `05-VERIFICATION.md`'s verified truths 1-11:
`OFFICE-01` (truths 1, 5, 8, 11 — untouched here), `OFFICE-02` (05-36's scope), `OFFICE-03`
(truths 2, 8, 10 — untouched here), `HANDOFF-01` (truths 3, 6, 7 — Task 1 tightens its slot rule),
`HANDOFF-02` (truths 3, 9 — Task 2 tightens its attribution).

**A2 — WR-07 and WR-08 are closed now rather than deferred.** The alternative was an explicit
deferral to Phase 6 naming the CEO Dashboard as the consumer that would make them live. Chosen
against: the fixes are a few lines each with cheap deterministic tests, and the absence of a red
test today is a consequence of there being no consumer yet, not evidence that the behaviour is
correct. Recorded here because `05-VERIFICATION.md` classified both as advisory with no reproducible
failure, so this plan is adding scope beyond the one blocking gap — deliberately, and bounded to
these two findings.

**A3 — The eight IN-* review items are NOT planned.** `IN-01`, `IN-02`, `IN-03`, `IN-05`, `IN-06`,
`IN-07`, `IN-08` remain tracked in `05-REVIEW.md` as info-level. None changes a shipped behaviour
and none is reproducible as a defect; `IN-07` (`ActiveHandoff.phase` referencing the non-exported
`HandoffPhase`, TS4033 under `declaration: true`) becomes real the first time a `.d.ts` is emitted,
and `IN-08` (`--passWithNoTests` for `orchestration-adapter`) the first time that package gains
runtime logic. Deferred to whichever phase first emits declarations or gives that package logic.

**A4 — The title-collision fixture.** Two titles identical through 12 code points are chosen by the
planner; no source artifact specifies a pair. The test asserts the collision is real (both computed
lines byte-identical) so it cannot silently stop testing the property if the caps change.
</planner_assumptions>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-37-SUMMARY.md` when done.
</output>
