---
phase: 05-pixel-office-renderer
plan: 38
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pixel-office/src/constants.ts
  - packages/pixel-office/src/engine/renderer.test.ts
  - scripts/verify-pixel-office-live.mjs
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
autonomous: true
gap_closure: true
gap_ids: [G-05-1c, G-05-2a]
requirements: [OFFICE-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A status glyph leaves BUBBLE_ICON_GAP_PX (3) unzoomed px of air between its lowest ink row and its owner's visible head, in every pose, at zoom 1 and 3 — visibly detached from the sprite rather than touching it"
    - "An agent resting on its own seat shows at least 12 fewer visible body rows than the same agent standing in the aisle"
    - "A seated agent's head survives the composited frame: at least 10 of its body rows are still visible with the desk and all furniture drawn"
    - "Every glyph stays on the floor (top y >= 16 unzoomed) for every seat and every standing spot, seated and standing"
    - "The live harness measures the raised head gap on real browser pixels and goes red both when the gap shrinks below BUBBLE_ICON_GAP_PX and when it exceeds BUBBLE_ICON_GAP_PX + 1"
    - "1:1 with real projection state (upsertCharacterFromAgent / despawn-on-offline), no fabricated placeholder agents; 16 desk seats + 4 standing spots, lowest free seat first, reclaimed on despawn"
    - "Character sprite pixels are non-transparent MetroCity frames preserving the 4-direction x {idle, walk, type} sheet shape"
    - "The bubble/badge state glyph is drawn as the top pass, z-sorted after the base sprite, owner-bound and head-anchored"
  artifacts:
    - packages/pixel-office/src/constants.ts
    - packages/pixel-office/src/engine/renderer.test.ts
    - scripts/verify-pixel-office-live.mjs
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
  key_links:
    - "constants.ts BUBBLE_ICON_GAP_PX -> renderer.ts resolveBubbleY and resolveDialogueBox aboveY -> renderer.test.ts constant pin -> verify-pixel-office-live.mjs TRUTH 4 two-sided gap bound and TRUTH 5 TAIL_REACH_PX"
    - "constants.ts CHARACTER_SITTING_OFFSET_PX -> renderScene sittingOffset -> renderer.test.ts G-05-P3 seated/standing delta -> harness TRUTH 4 headTop and firstRowSpriteBottom (both already read the constant, so they follow automatically)"
---

<objective>
Close the two spatial-separation gaps the human UAT round raised against the office at stream
scale: the status glyph sits so close to its owner's head that it reads as part of the sprite
(G-05-1c), and a seated agent is not different enough from a standing handoff sender (G-05-2a).

Both are single-constant changes in `packages/pixel-office/src/constants.ts` whose guards live in
two places — the engine unit suite and the live browser harness. The live harness currently pins
the OLD glyph gap in two spots, so raising the constant without touching the harness turns it red.

Purpose: legibility polish only. No new capability, no new asset, no behaviour change beyond
geometry. Every existing live TRUTH must stay green.
Output: two raised constants, three guards that go red when either is reverted, and a fresh set of
live review frames.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pixel-office-renderer/05-UAT.md
@.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
@packages/pixel-office/src/constants.ts
@packages/pixel-office/src/layout/office-layout.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Raise the glyph head gap end to end — constant, renderer math, unit pin, live pixels (G-05-1c)</name>

  <read_first>
    - `packages/pixel-office/src/constants.ts` — `BUBBLE_ICON_GAP_PX`, `DIALOGUE_TAIL_PX`, `CHARACTER_SITTING_OFFSET_PX`
    - `packages/pixel-office/src/engine/renderer.ts` lines 150-200 (`resolveBubbleY`) and lines 240-310 (`GLYPH_ROWS`, `resolveDialogueBox`'s `aboveY`) — the two consumers of the constant
    - `packages/pixel-office/src/engine/renderer.test.ts` lines 206-292 — the `resolveBubbleY` unit expectations and the "glyph sits on its owner's head" describe, which pins the constant literally
    - `packages/pixel-office/src/engine/renderer.test.ts` lines 664-900 — the `resolveDialogueBox` candidate-scoring describe, whose `above` candidate shifts with the constant
    - `packages/pixel-office/src/layout/office-layout.json` — every seat and standing spot is on row 4 or 8; the interaction slots are on row 6. Row 4 is the topmost occupied row and therefore the only headroom risk
    - `packages/pixel-office/src/engine/renderer.test.ts` lines 334-372 — the CR-02 two-character composite test, which carries CONCRETE expected geometry in both its assertions and its inline arithmetic comment; both constants move those numbers
    - `scripts/verify-pixel-office-live.mjs` lines 185-215 (`readNumberConst` usage), lines 1100-1130 (TRUTH 5's `TAIL_REACH_PX`), lines 1260-1310 (TRUTH 4's gap assertion)
    - `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` line 76 (the placement rule, which hard-quotes the gap as 1) and line 170 (the `overflow (state signal render)` UI Consideration, which says "head-anchored 1 px")
  </read_first>

  <files>packages/pixel-office/src/constants.ts, packages/pixel-office/src/engine/renderer.test.ts, scripts/verify-pixel-office-live.mjs, .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>

  <precondition>Docker is running and `pnpm --filter api db:test:up`'s Postgres container can start — `node scripts/verify-pixel-office-live.mjs` needs a real database, API, Vite server and headless Chromium. Halt and report if Docker is unavailable rather than skipping the live verification.</precondition>

  <reversibility rating="reversible">A spacing constant; reverting it restores the previous geometry with no data migration and no interface change.</reversibility>

  <action>
Raise `BUBBLE_ICON_GAP_PX` in `packages/pixel-office/src/constants.ts` from `1` to `3`. Update its
doc comment to cite `05-38 (G-05-1c)` alongside the existing `05-30` reference and to state that the
value is unzoomed engine px, so at display scale 4 the on-screen air is 12 device px. The UAT ask
was "another 2-4 px or equivalent"; 3 is the low end of that range read in the engine's own unit,
which is the unit every other spacing value in this file uses.

Do not touch `resolveBubbleY` or `resolveDialogueBox` — both already multiply the constant, so the
glyph and the "above" dialogue candidate follow automatically.

Then repair the three places that pin the old value:

1. `renderer.test.ts` "glyph sits on its owner's head (05-30, G-05-1c)" describe: change the literal
   constant pin from `1` to `3`. Leave the derived air assertion
   (`inkBottom + BUBBLE_ICON_GAP_PX * zoom` equals `headTop`) exactly as written — the literal pin is
   what goes red on a revert, the derived assertion is what proves the constant buys real air.
2. `renderer.test.ts` "resolveBubbleY — owner-bound glyph placement" describe: the headroom case
   expectations were computed for a 1 px gap. Re-derive them from the function's own formula
   (`drawY + headTopRow * zoom - BUBBLE_ICON_GAP_PX * zoom - (glyphInkBottomRow + 1) * zoom`):
   `resolveBubbleY(40, 3, 11, 1)` is now `28`, and `resolveBubbleY(120, 3, 11, 3)` is now `84`.
   Update the inline arithmetic comments to match. The two no-headroom cases are unchanged — both
   still floor onto the owner's own sprite box.
3. `scripts/verify-pixel-office-live.mjs`: add
   `const BUBBLE_ICON_GAP_PX = readNumberConst(constantsSrc, "constants.ts", "BUBBLE_ICON_GAP_PX");`
   and `const DIALOGUE_TAIL_PX = readNumberConst(constantsSrc, "constants.ts", "DIALOGUE_TAIL_PX");`
   beside the existing `CHARACTER_SITTING_OFFSET_PX` read, then:
   - TRUTH 4: replace the one-sided `headGap <= 2` bound with a two-sided one —
     `headGap >= BUBBLE_ICON_GAP_PX && headGap <= BUBBLE_ICON_GAP_PX + 1` — and put the computed
     range into the assertion message. The current one-sided bound stays green when the gap shrinks
     back to touching the head, which is precisely the regression this plan exists to prevent.
   - TRUTH 5: replace the literal `TAIL_REACH_PX = 18` with
     `13 + BUBBLE_ICON_GAP_PX + DIALOGUE_TAIL_PX + 2` (13 is the glyph row count, 2 is the interior
     inset slack the existing comment already documents), and update that comment so it no longer
     quotes a fixed 1. The value becomes 20.
4. `renderer.test.ts` "renderScene over the real desk layout — CR-02 two-character composite"
   (around lines 334-372): this test pins concrete glyph rows and carries an inline arithmetic
   comment spelling them out. Raising the gap lifts the glyph by 2, so the two glyph-band
   assertions (`Math.min(...glyphRects...)` and `Math.max(...glyphRects...)`) go from 100..112 to
   **98..110**, and the comment's "ends 1 px above it, y 100..112" follows.

   98..110 is an INTERMEDIATE value, not the end state — it is what makes this task's suite run
   green, and nothing more. The glyph is head-anchored, so Task 2's sink moves the same two
   literals a SECOND time, to 102..114. Do not treat this block as finished here.

Then update the two places in `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` that
hard-quote the old gap, using scoped edits and never a whole-file rewrite:
- Line 76, the placement rule: the parenthetical value after `BUBBLE_ICON_GAP_PX` becomes 3, and the
  citation gains `05-38 (G-05-1c)` beside the existing `05-30, G-05-1c`.
- Line 170, the `overflow (state signal render)` UI Consideration row: "head-anchored 1 px above the
  owner's visible head 05-30" becomes 3 px, cited to 05-38.
Change no other row, and specifically do not touch the Attribution footer row or the Color section.

Off-canvas check, already done and recorded here so the executor does not repeat it: the topmost
occupied tile row is 4. A standing row-4 character's visible head top is about y 50 unzoomed, so the
glyph top lands near y 34, comfortably inside the floor interior that starts at y 16. The existing
"keeps every glyph on the floor for every seat and standing spot" test covers exactly those tiles
and must stay green — if it goes red the arithmetic above is wrong and the constant is too large.

Finally re-run the live harness with review frames written, so the phase has before/after evidence
for the same scenes the UAT round judged.
  </action>

  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or fewer than 196 tests reported passing, or any test in the "glyph sits on its owner's head" or "resolveBubbleY" describes reports a received value that differs from its expectation</fails_when>
    <automated>pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
    <automated>PIXEL_OFFICE_SHOTS=.gsd/uat-shots-05-38 node scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit, or the final line is not `LIVE PROOF: PASS`, or the TRUTH 4 log line reports a gap outside the printed range, or no `states.png` and `cohort.png` appear in `.gsd/uat-shots-05-38`</fails_when>
  </verify>

  <acceptance_criteria>
    - `packages/pixel-office/src/constants.ts` exports `BUBBLE_ICON_GAP_PX` with the value `3`.
    - `renderer.test.ts`'s literal pin asserts the constant is `3`; reverting the constant to `1` makes that single assertion fail.
    - `renderer.test.ts`'s `resolveBubbleY` headroom case expects `28` at zoom 1 and `84` at zoom 3.
    - `scripts/verify-pixel-office-live.mjs` contains no literal `18` as the tail-reach value and no literal `2` as the TRUTH 4 gap ceiling; both are expressions over `BUBBLE_ICON_GAP_PX`, and the tail reach evaluates to 20.
    - TRUTH 4 asserts a LOWER bound on the measured head gap as well as an upper one, so a gap of 1.25 unzoomed px fails it.
    - `05-UI-SPEC.md` line 76 quotes the gap as 3 and line 170 says "head-anchored 3 px", both citing 05-38; no other row of that file is touched by this task.
    - `node scripts/verify-pixel-office-live.mjs` prints `LIVE PROOF: PASS` and its TRUTH 4 log line reports a measured gap of at least 3.
    - The "keeps every glyph on the floor for every seat and standing spot" test passes unchanged — no glyph leaves the canvas at the raised gap.
  </acceptance_criteria>

  <done>The status glyph sits 3 unzoomed px (12 device px at display scale 4) above its owner's visible head in every pose and at every seat, the engine suite and the live browser harness both measure that gap, and both go red if it returns to 1.</done>
</task>

<task type="auto">
  <name>Task 2: Sink a seated agent further behind its desk, with a head-visibility guard (G-05-2a)</name>

  <read_first>
    - `packages/pixel-office/src/constants.ts` — `CHARACTER_SITTING_OFFSET_PX`
    - `packages/pixel-office/src/engine/renderer.ts` lines 500-530 — `renderScene`'s `sittingOffset` branch, the only consumer
    - `packages/pixel-office/src/engine/renderer.test.ts` lines 1168-1300 — the "seated whenever resting on the own seat (05-25, G-05-P3)" describe, its `visibleBodyRows` helper and the existing ">= 8 fewer visible body rows" assertion
    - `packages/pixel-office/src/engine/renderer.test.ts` lines 334-372 — the CR-02 composite test's concrete sprite-box geometry and its line-360 comment, which still reads `CHARACTER_SITTING_OFFSET_PX (6)`
    - `scripts/verify-pixel-office-live.mjs` lines 1260-1300 — TRUTH 4's `headTop` and `firstRowSpriteBottom`, both already derived from the constant
    - `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` line 102 (the office-layout paragraph, which hard-quotes the sitting offset as 6)
  </read_first>

  <files>packages/pixel-office/src/constants.ts, packages/pixel-office/src/engine/renderer.test.ts, .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>

  <reversibility rating="reversible">A drawing offset; reverting it restores the previous seated pose with no data or interface change.</reversibility>

  <action>
The only thing separating a seated agent from a standing one today is how much of its body the desk
eats. Raise `CHARACTER_SITTING_OFFSET_PX` in `packages/pixel-office/src/constants.ts` from `6` to
`10` and extend its doc comment to cite `05-38 (G-05-2a)`. Every extra sunk row is a row the desk
covers, so the seated/standing visible-row delta grows one-for-one with the constant.

Do not touch `renderScene` — the `sittingOffset` branch already reads the constant, and the sitting
RULE (resting on the own seat, whatever the status) is unchanged by this plan.

In `renderer.test.ts`'s "seated whenever resting on the own seat (05-25, G-05-P3)" describe, make
three changes to the "a resting agent at its own desk shows at least 8 fewer visible body rows"
test, renaming it to name the new threshold:

1. Add a literal pin on the constant next to the existing `isOwnSeat` assertions, matching the idiom
   the glyph-gap test already uses: assert `CHARACTER_SITTING_OFFSET_PX` is `10`. This is the
   assertion that goes red on a revert.
2. Raise the delta floor from `8` to `12` — the old slack of 2 above the old offset of 6, carried
   forward unchanged over the new offset of 10.
3. Add a head-visibility guard as a separate assertion in the same test: `seatedRows` must be at
   least `10`, with a failure message naming the measured count. This is the two-sided half of the
   change — it goes red if a future offset sinks the head behind the desk, which is the one way this
   gap could be "closed" into a worse frame.

Also in `renderer.test.ts`, finish the CR-02 composite block (around lines 334-372). Task 1 left it
green but at an intermediate value: it moved the glyph band for the raised gap, and this task moves
BOTH the sprite boxes AND the glyph band **again**. The glyph is head-anchored, so sinking the
sprite carries it along — Task 1's 98..110 becomes 102..114. Re-derive rather than deleting the
comment; the concrete numbers are what catch a silent layout drift.

End state the whole block must carry when this task is done:

| Site | Before this phase | After |
|------|-------------------|-------|
| `firstBox` | `{ top: 46, bottom: 78 }` | `{ top: 50, bottom: 82 }` |
| `laterBox.top` | `110` | `114` |
| glyph band min / max | `100` / `112` | `102` / `114` |
| `.toBeGreaterThan(firstBox.bottom)` | — | derived, no literal, leave as is |

And the prose comment at lines 357-364 embeds five numbers, not just the "1 px above" one:
- "sunk by `CHARACTER_SITTING_OFFSET_PX` (6)" becomes (10)
- "these boxes were y 40..72 and y 104..136" — **leave alone**, it is a historical note about the
  pre-05-32 behaviour, not a live claim
- "sprite boxes y 46..78 and y 110..142" becomes y 50..82 and y 114..146
- "head ink starts at frame row 3 (y 113)" becomes (y 117)
- "ends 1 px above it, y 100..112" becomes "ends 3 px above it, y 102..114"

Then update `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` line 102 with a scoped edit:
in the `**Updated 05-32 (G-05-P3):**` sentence, the parenthetical value after
`CHARACTER_SITTING_OFFSET_PX` becomes the new constant and the sentence gains a
`**Widened 05-38 (G-05-2a):**` note saying the desk now hides more of the seated body so a seated
agent reads more distinctly from a standing handoff sender. Change nothing else in that paragraph —
the interaction-slot, facing and separation-measurement sentences are all still accurate.

Run the suite before committing. The seated agent shows `23 - offset` visible rows, so at `10` it
shows 13 against a guard of 10 — the guard only binds at an offset of 14, and this arithmetic has
been independently re-derived from `office-layout.json` and the sprite sheet. The step-down path
below is therefore a safety net that should not fire: if the head-visibility guard does go red at
`10`, step the constant down by one and retry (do not relax the guard, and do not go below `9` —
below that the change stops being a visible widening); pin whatever value lands and set the delta
floor to `8 + (value - 6)` so the slack stays constant.

Both live-harness call sites for this constant (`headTop`, `firstRowSpriteBottom` in TRUTH 4) read
it through `readNumberConst`, so they follow with no edit — but the harness must still be re-run,
because both the front agent's sprite bottom and the back agent's glyph top move.
  </action>

  <verify>
    <automated>pnpm --filter pixel-office test -- --reporter=verbose seated</automated>
    <fails_when>non-zero exit, or the "seated whenever resting on the own seat" describe reports any failing assertion, or the seated-rows guard prints a measured count below 10</fails_when>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or fewer than 196 tests reported passing</fails_when>
    <automated>PIXEL_OFFICE_SHOTS=.gsd/uat-shots-05-38 node scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit, or the final line is not `LIVE PROOF: PASS`, or TRUTH 4's assertion that the back-row glyph sits strictly below the front agent's sprite bottom fails</fails_when>
    <human-check>Open `.gsd/uat-shots-05-38/handoff.png` at 1:1 and confirm the aisle sender still reads as a full-body standing figure while the two seated agents read as head-and-shoulders behind their desks, and that no seated agent's face is clipped by its desk.</human-check>
  </verify>

  <acceptance_criteria>
    - `packages/pixel-office/src/constants.ts` exports `CHARACTER_SITTING_OFFSET_PX` with a value of at least `9`.
    - The G-05-P3 test pins that constant literally, so reverting it to `6` makes that assertion fail.
    - The same test asserts `standingRows - seatedRows` is at least `12` (or `8 + (value - 6)` if the constant landed below 10).
    - The same test asserts `seatedRows` is at least `10`, with the measured count in the failure message — a future over-sink that buries the head turns this red.
    - The CR-02 composite block carries `firstBox { top: 50, bottom: 82 }`, `laterBox.top` `114` and a glyph band of `102`..`114` — all four literals, not just the sprite-box pair. A block left at Task 1's intermediate `98`..`110` glyph band is a half-updated test and fails this criterion.
    - No comment in `renderer.test.ts` still quotes `CHARACTER_SITTING_OFFSET_PX (6)`, "y 113", "1 px above it" or "y 46..78"; the line-357-364 comment's live numbers match the shipped constants, while its pre-05-32 historical note ("these boxes were y 40..72 and y 104..136") is left untouched.
    - `05-UI-SPEC.md` line 102 quotes the new sitting offset and carries a `Widened 05-38 (G-05-2a)` note; no other row of that file is touched by this task.
    - `pnpm --filter pixel-office test` reports at least 196 passing tests.
    - `node scripts/verify-pixel-office-live.mjs` prints `LIVE PROOF: PASS` with no edit to the two harness expressions that read this constant.
  </acceptance_criteria>

  <done>A resting agent at its own desk shows at least 12 fewer visible body rows than the same agent standing in the aisle while keeping at least 10 rows of head and shoulders above the desk, and the engine suite proves both bounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none in scope | This plan changes two integer drawing constants and three test/harness assertions inside `packages/pixel-office` and `scripts/`. No input crosses a boundary: the package is a pure consumer with zero network, storage, filesystem-write or user-input surface, and no package-manager install occurs. |

## STRIDE Threat Register

No threat surface. Both changed values are compile-time integers consumed only by canvas draw
arithmetic; neither is attacker-influenceable, neither widens an existing surface, and neither
touches the attribution footer, the auth-gated `/ws/browser` route or any event payload. Stating
this explicitly rather than inventing a placeholder threat, per the phase's ASVS level 1 scope.

The phase-level supply-chain threat `T-05-SC` does not apply: this plan installs nothing.
</threat_model>

<verification>
1. `pnpm --filter pixel-office test` — at least 196 passing, including the raised literal pins.
2. `pnpm --filter web test` and `pnpm --filter web typecheck` — unaffected packages stay green.
3. `pnpm turbo run test --force` — 10/10 workspace tasks, uncached.
4. `PIXEL_OFFICE_SHOTS=.gsd/uat-shots-05-38 node scripts/verify-pixel-office-live.mjs` — `LIVE PROOF: PASS`, TRUTH 0-7 all green, review frames written.
5. Revert-proof spot check: temporarily set `BUBBLE_ICON_GAP_PX` back to `1`, confirm the engine suite goes red, then restore. Same for `CHARACTER_SITTING_OFFSET_PX` at `6`.
6. `git diff .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` touches only lines 76, 102 and 170.
</verification>

<success_criteria>
- `BUBBLE_ICON_GAP_PX` is 3 and `CHARACTER_SITTING_OFFSET_PX` is at least 9.
- Three guards (engine constant pins, the seated/standing delta, the live two-sided gap bound) each go red on a revert of the value they protect.
- The live harness prints `LIVE PROOF: PASS` with no TRUTH weakened or removed.
- No change to `renderer.ts`, `handoff-choreography.ts`, any sprite asset, or the attribution footer.
</success_criteria>

<artifacts_this_phase_produces>
No new files. Symbols and values changed:

| Symbol | File | Before | After |
|--------|------|--------|-------|
| `BUBBLE_ICON_GAP_PX` | `packages/pixel-office/src/constants.ts` | `1` | `3` |
| `CHARACTER_SITTING_OFFSET_PX` | `packages/pixel-office/src/constants.ts` | `6` | `10` (min `9`) |
| `BUBBLE_ICON_GAP_PX` (harness local) | `scripts/verify-pixel-office-live.mjs` | not read | read via `readNumberConst` |
| `DIALOGUE_TAIL_PX` (harness local) | `scripts/verify-pixel-office-live.mjs` | not read | read via `readNumberConst` |
| `TAIL_REACH_PX` | `scripts/verify-pixel-office-live.mjs` | literal `18` | `13 + BUBBLE_ICON_GAP_PX + DIALOGUE_TAIL_PX + 2` |
| TRUTH 4 gap bound | `scripts/verify-pixel-office-live.mjs` | `headGap <= 2` | two-sided over `BUBBLE_ICON_GAP_PX` |
| placement rule, line 76 | `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` | gap quoted as `1` | quoted as `3`, cited to 05-38 |
| office-layout paragraph, line 102 | `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` | offset quoted as `6` | new value + `Widened 05-38 (G-05-2a)` note |
| `overflow (state signal render)` row, line 170 | `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` | "head-anchored 1 px" | "head-anchored 3 px", cited to 05-38 |

This plan owns exactly those three `05-UI-SPEC.md` rows. 05-40 owns four different ones (the two
handoff dialogue rows, the length rule and the `long-text` UI Consideration) and runs in a later
wave, so the two plans never contend for the file. Neither touches the Attribution footer row.

Review frames written to `.gsd/uat-shots-05-38/` (gitignored working evidence, not committed source).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-38-SUMMARY.md` when done.
</output>
</content>
