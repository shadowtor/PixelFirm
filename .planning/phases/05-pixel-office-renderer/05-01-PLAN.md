---
phase: 05-pixel-office-renderer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/event-schema/src/agent-status.ts
  - packages/event-schema/src/index.ts
  - packages/company-core/src/projections.ts
  - packages/company-core/src/reducer.ts
  - packages/company-core/src/reducer.test.ts
  - apps/api/package.json
  - apps/api/src/env.ts
  - apps/api/src/auth/browser-auth.ts
  - apps/api/src/auth/browser-auth.test.ts
  - apps/api/src/ws/browser-connections.ts
  - apps/api/src/db/event-row.ts
  - apps/api/src/routes/ws-browser.ts
  - apps/api/src/routes/ws-browser.test.ts
  - apps/api/src/routes/events.ts
  - apps/api/src/server.ts
  - apps/web/package.json
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/tsconfig.node.json
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/ws-client.ts
  - apps/web/src/ws-client.test.ts
  - packages/pixel-office/package.json
  - packages/pixel-office/LICENSE
  - packages/pixel-office/src/index.ts
  - packages/pixel-office/src/index.test.ts
  - packages/pixel-office/src/types.ts
  - packages/pixel-office/src/constants.ts
  - packages/pixel-office/src/colorize.ts
  - packages/pixel-office/src/layout/tileMap.ts
  - packages/pixel-office/src/sprites/spriteData.ts
  - packages/pixel-office/src/engine/characters.ts
  - packages/pixel-office/src/engine/gameLoop.ts
  - packages/pixel-office/src/engine/renderer.ts
autonomous: false
requirements: [OFFICE-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "AgentStatus is a 15-member union (offline/idle/planning/researching/coding/reading/testing/reviewing/discussing/deploying/blocked/waiting_for_agent/waiting_for_ceo/failed/completed) matching PROJECT.md/REQUIREMENTS.md's canonical list verbatim, correcting CONTEXT.md D-01's inherited '14' miscount (RESEARCH.md Pitfall 1)"
    - "A browser client cannot open /ws/browser without the correct BROWSER_ACCESS_TOKEN — connects with no token or the wrong token both receive the same 401 rejection"
    - "A newly-connecting, correctly-authenticated browser client receives a full company-core fold()-derived snapshot of real projection state before any live event message arrives"
    - "Posting a real event to POST /events causes every open /ws/browser socket to receive that same validated event moments later, with zero polling on the client side"
    - "packages/pixel-office renders a real agent's real AgentState.status (currently only AgentStatus.IDLE is reachable end-to-end; full 15-value fidelity lands in 05-02) on an HTML canvas via the forked, unmodified gameLoop/renderer — never a hardcoded or timer-scripted pose independent of the received state"
  artifacts:
    - path: "packages/event-schema/src/agent-status.ts"
      provides: "Canonical 15-value AgentStatus const + type, single source of truth for every other plan in this phase"
    - path: "apps/api/src/routes/ws-browser.ts"
      provides: "Authenticated browser-facing Broadcast Hub route: snapshot-on-connect + live event relay"
    - path: "apps/api/src/auth/browser-auth.ts"
      provides: "Shared-token (BROWSER_ACCESS_TOKEN) query-param auth for the browser WS route"
    - path: "apps/web/src/ws-client.ts"
      provides: "Browser-side WS client applying snapshot + company-core reduce() to relayed events"
    - path: "packages/pixel-office/src/index.ts"
      provides: "Forked Canvas2D engine's public surface + upsertCharacterFromAgent(agentId, status)"
    - path: "packages/pixel-office/LICENSE"
      provides: "Verbatim MIT licence text from pixel-agents-hq/pixel-agents, preserved per OFFICE-02/D-05"
  key_links:
    - from: "apps/web/src/ws-client.ts"
      to: "apps/api/src/routes/ws-browser.ts"
      via: "new WebSocket(`${wsBaseUrl}/ws/browser?token=...`) — browser WebSocket API cannot set custom headers, so auth travels as a query param, not Authorization"
      pattern: "ws/browser\\?token="
    - from: "apps/api/src/routes/events.ts"
      to: "apps/api/src/ws/browser-connections.ts"
      via: "broadcastToBrowsers(event) called after a successful (non-duplicate) insert"
      pattern: "broadcastToBrowsers"
    - from: "apps/api/src/routes/ws-browser.ts"
      to: "packages/company-core"
      via: "fold(events.map(rowToCompanyEvent)) computes the on-connect snapshot"
      pattern: "fold\\("
    - from: "apps/web/src/App.tsx"
      to: "packages/pixel-office/src/index.ts"
      via: "upsertCharacterFromAgent(agentId, status) called from onSnapshot/onEvent handlers, feeding the forked gameLoop's character list"
      pattern: "upsertCharacterFromAgent"
  prohibitions:
    - requirement_id: OFFICE-01
      category: values
      statement: "MUST NOT display any AgentStatus visual that does not correspond to real, currently-projected company state — no scripted/timer-driven animation advancing independent of a real received event"
      verification: test

user_setup: []
---

<objective>
Stand up the phase's foundation: the canonical `AgentStatus` contract, a real (not stubbed) browser-facing Broadcast Hub in `apps/api`, a minimal `apps/web` Vite+React host, and the first files forked from `pixel-agents-hq/pixel-agents` into `packages/pixel-office` — wired end-to-end so a real agent's real status renders on a canvas, driven only by real events flowing through the control plane.

Purpose: every other plan in this phase (status-mapping fidelity, licensing, real per-agent derivation, handoff choreography) depends on this transport + render skeleton existing and being genuinely event-driven, per the Core Value ("never a prerecorded or faked animation") and RESEARCH.md's finding that `pixel-agents-hq/pixel-agents` is a hand-rolled React 19 + Vite + raw Canvas 2D engine — not Phaser.
Output: `packages/event-schema/src/agent-status.ts`, a working `GET /ws/browser` route with snapshot+relay semantics, `apps/web` (new workspace member), `packages/pixel-office` (new workspace member) rendering one real agent.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pixel-office-renderer/05-CONTEXT.md
@.planning/phases/05-pixel-office-renderer/05-RESEARCH.md
@.planning/phases/05-pixel-office-renderer/05-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `packages/event-schema/src/agent-status.ts` — `AgentStatus` const object (15 keys: OFFLINE, IDLE, PLANNING, RESEARCHING, CODING, READING, TESTING, REVIEWING, DISCUSSING, DEPLOYING, BLOCKED, WAITING_FOR_AGENT, WAITING_FOR_CEO, FAILED, COMPLETED) + `AgentStatus` type, re-exported from `packages/event-schema/src/index.ts`.
- `apps/api/src/auth/browser-auth.ts` — `authenticateBrowser(request, reply)` Fastify preValidation hook; augments `FastifyRequest` with `browserAuthed?: boolean`.
- `apps/api/src/ws/browser-connections.ts` — `registerBrowserSocket`, `unregisterBrowserSocket`, `broadcastToBrowsers(message)`.
- `apps/api/src/db/event-row.ts` — `rowToCompanyEvent(row)`.
- `apps/api/src/routes/ws-browser.ts` — `registerWsBrowserRoute(fastify)`.
- `apps/web` — new pnpm workspace member (`web`), `connectOfficeSocket(url, token, handlers)` in `src/ws-client.ts`.
- `packages/pixel-office` — new pnpm workspace member (`pixel-office`), `upsertCharacterFromAgent(agentId, status)` + `startGameLoop(canvas)` exported from `src/index.ts`; forked `types.ts`, `constants.ts`, `colorize.ts`, `layout/tileMap.ts` (incl. `findPath`), `sprites/spriteData.ts` (idle pose only this plan), `engine/characters.ts` (event-driven only, no autonomous wander AI), `engine/gameLoop.ts`, `engine/renderer.ts`.
- `packages/company-core/src/projections.ts` — `AgentState.status` retyped `string` → `AgentStatus`.
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions (spec-less probe fallback — all 5 phase requirements came back `unclassified`/`unresolved` from the deterministic edge probe; none were silently dropped)

- OFFICE-01/02/03 and HANDOFF-01/02 edge coverage is inherently visual/rendering behavior that does not fit the probe's CRUD/validation templates (expected, per the planning context). `must_haves.truths` above and in 05-02/05-03/05-04 were authored directly from CONTEXT.md's D-01 through D-05 and ROADMAP.md's 4 phase success criteria instead, per the fallback protocol.
- The `offline` and `reading` `AgentStatus` values are structurally covered (15/15 rows in 05-02's exhaustive status-mapping table) but are **not honestly derivable from real signals this phase inherits**: no per-agent worker-liveness link exists yet (worker connection status is keyed by `workerId`; nothing links a `workerId` to an `agentId` anywhere in the projection), and no per-tool-call granularity exists to distinguish `reading` from `coding`. Rather than fabricate a fake signal for either, the derivation logic built in 05-03 never emits these two values — this is a documented, deliberate gap (missing-information constraint), not a silent scope cut, flagged for a future phase.
- RESEARCH.md Assumptions Log A1 (icon-overlay-over-AI-generation for the 15-state gap is a recommendation, not yet user-confirmed) is carried forward; A2 (PixelLab/Retro Diffusion pricing) is moot since A1's recommendation makes tier 2 unnecessary for this MVP; A3 (furniture/floor asset provenance undocumented) is resolved directly into 05-02's `references/ASSET-LICENSES.md`; A4 (`apps/web` stood up now, not a throwaway harness) is this plan's approach.
- The `assumption-delta` detector fired on "second agent"/"another" phrasing in HANDOFF-01's text. Decision recorded in 05-04's `<assumption_delta_decision>` block: **no-change** — `AgentState` has been a plural `Record<string, AgentState>` since Phase 1; HANDOFF-01 describes an interaction between two existing members of an already-plural collection, not a new singular→plural transition.
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy check — first-ever frontend install (react, react-dom, vite, @vitejs/plugin-react)</name>
  <read_first>.planning/phases/05-pixel-office-renderer/05-RESEARCH.md (## Package Legitimacy Audit — does not enumerate this specific install), .planning/research/STACK.md (React ^19.3 / Vite ^8 already the project's mandated stack)</read_first>
  <what-built>
    Verified npm registry legitimacy for the packages Task 2 is about to install into the brand-new `apps/web` workspace — this is the first time any frontend framework package lands in this monorepo, and RESEARCH.md's Package Legitimacy Audit table exists but does not name these specific packages (it only covered the fork's own dependencies, correctly finding none to install). Ran `npm view <pkg> version time.created maintainers --json` this session:
    - `react` — current 19.3.0, first published 2011-10-26, maintainers `fb <opensource+npm@fb.com>` / `react-bot <react-core@meta.com>` (Meta).
    - `vite` — current 8.3.0 (matches STACK.md's `^8` pin), first published 2020-04-21, maintainers `yyx990803` (Evan You) / `vitebot@voidzero.dev` (the vitejs org's own publish bot).
    - `@vitejs/plugin-react` — current 6.1.1, first published 2021-09-20, same vitejs org.
    - `react-dom`, `@types/react`, `@types/react-dom` ride the same trust tier (Meta / DefinitelyTyped respectively) and were not separately queried.
  </what-built>
  <how-to-verify>
    Confirm these match the versions already mandated in `.planning/research/STACK.md` (React ^19.3, Vite ^8) and that the maintainer identities above look right to you. No dashboard visit needed — the evidence above is the full registry response from this session.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the installs in Task 2, or name which package needs deeper review.</resume-signal>
  <acceptance_criteria>Human has typed "approved" (or equivalent) before Task 2's `pnpm add` commands run.</acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 2: End-to-end tracer — a real agent's real status renders on screen via the browser Broadcast Hub</name>
  <files>packages/event-schema/src/agent-status.ts, packages/event-schema/src/index.ts, packages/company-core/src/projections.ts, packages/company-core/src/reducer.ts, packages/company-core/src/reducer.test.ts, apps/api/package.json, apps/api/src/env.ts, apps/api/src/auth/browser-auth.ts, apps/api/src/auth/browser-auth.test.ts, apps/api/src/ws/browser-connections.ts, apps/api/src/db/event-row.ts, apps/api/src/routes/ws-browser.ts, apps/api/src/routes/ws-browser.test.ts, apps/api/src/routes/events.ts, apps/api/src/server.ts, apps/web/package.json, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/tsconfig.node.json, apps/web/index.html, apps/web/src/main.tsx, apps/web/src/App.tsx, apps/web/src/ws-client.ts, apps/web/src/ws-client.test.ts, packages/pixel-office/package.json, packages/pixel-office/LICENSE, packages/pixel-office/src/index.ts, packages/pixel-office/src/index.test.ts, packages/pixel-office/src/types.ts, packages/pixel-office/src/constants.ts, packages/pixel-office/src/colorize.ts, packages/pixel-office/src/layout/tileMap.ts, packages/pixel-office/src/sprites/spriteData.ts, packages/pixel-office/src/engine/characters.ts, packages/pixel-office/src/engine/gameLoop.ts, packages/pixel-office/src/engine/renderer.ts</files>
  <read_first>
    apps/api/src/routes/ws.ts, apps/api/src/auth/worker-auth.ts, apps/api/src/routes/admin-workers.ts (requireBootstrapSecret's timing-safe pattern), apps/api/src/routes/events.ts, apps/api/src/server.ts, apps/api/src/db/schema.ts, apps/api/src/env.ts, apps/api/src/ws/connection-status.ts, apps/api/src/routes/ws-auth.test.ts (WS test harness pattern — real Postgres on port 5434, `beforeAll` migrates + issues a credential, `attempt()` helper resolves opened/rejected),
    packages/company-core/src/reducer.ts, packages/company-core/src/projections.ts, packages/company-core/src/index.ts,
    packages/event-schema/src/payloads/index.ts, packages/event-schema/src/envelope.ts, packages/event-schema/src/index.ts,
    packages/company-core/package.json (workspace package.json shape to copy for pixel-office and web),
    .planning/phases/05-pixel-office-renderer/05-RESEARCH.md (Architecture Patterns → Pattern 1 findPath, Pattern 2 the 4-primitive vocabulary IDLE/WALK/TYPE+bubble, Pattern 4 apps/web now not a harness; Code Examples; Sources — the raw.githubusercontent.com URLs for every fork file),
    .planning/phases/05-pixel-office-renderer/05-PATTERNS.md (exact analog file list and core-pattern excerpts already extracted for every new file below)
  </read_first>
  <action>
    Data contract: create packages/event-schema/src/agent-status.ts exporting `AgentStatus` as both a `const` object and derived union type with exactly these 15 string values in this order: offline, idle, planning, researching, coding, reading, testing, reviewing, discussing, deploying, blocked, waiting_for_agent, waiting_for_ceo, failed, completed (corrects CONTEXT.md D-01's "14" — PROJECT.md line ~429-443 and REQUIREMENTS.md line 31 both list 15, per RESEARCH.md Pitfall 1). Re-export both from packages/event-schema/src/index.ts. In packages/company-core/src/projections.ts change `AgentState.status` from `string` to the imported `AgentStatus` type. In packages/company-core/src/reducer.ts fix every handler that assigns a status literal so the file keeps compiling against the new type: `agent.online` keeps setting `AgentStatus.IDLE` (same runtime value "idle", now typed); `session.started` changes its literal from `"working"` to `AgentStatus.CODING` (a started session defaults to the most common active state until Plan 05-03's real per-task derivation refines it); `agent.handoff_requested`'s to-agent upsert changes its literal from `"assigned"` to `AgentStatus.WAITING_FOR_AGENT` (the receiving agent is waiting on the sending agent to complete the handoff — this is also the one honest, reachable derivation path for `waiting_for_agent` in this plan). Update packages/company-core/src/reducer.test.ts's two now-stale assertions (`result.agents["agent-1"].status).toBe("working")` → `.toBe(AgentStatus.CODING)`, `result.agents["agent-2"].status).toBe("assigned")` → `.toBe(AgentStatus.WAITING_FOR_AGENT)`), importing `AgentStatus` from `event-schema`.

    Transport — env + auth: add `BROWSER_ACCESS_TOKEN: z.string().min(1)` to apps/api/src/env.ts's `EnvSchema` (same shape as `BOOTSTRAP_SECRET`). Generate a random 64-hex-char value (`node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`) and persist it into apps/api's local `.env` (gitignored) as `BROWSER_ACCESS_TOKEN=...` — if a value is already set there, reuse it, never overwrite. Create apps/api/src/auth/browser-auth.ts exporting `authenticateBrowser(request, reply)`: a native browser `WebSocket` cannot set an `Authorization` header before the handshake, so read the token from the query string instead — `(request.query as { token?: string }).token` — and timing-safe-compare it against `env.BROWSER_ACCESS_TOKEN` using the exact length-check-then-`timingSafeEqual` pattern from `requireBootstrapSecret` in apps/api/src/routes/admin-workers.ts, replying `401 {error:"unauthorized"}` on any mismatch (missing token, wrong length, wrong value — one uniform response, same posture as `authenticateWorker`). No new DB table, no per-client credential: this MVP has exactly one trusted browser viewer, so a single shared token compared server-side is the proportionate mechanism — a `browser_clients` table mirroring `workers` (with `issueCredential`/`verifyCredential`, already generic) is the documented upgrade path if independently-revocable multi-client credentials are ever needed. Augment the fastify module (`declare module "fastify"`) with `browserAuthed?: boolean`, set `true` on success. Add a `apps/api/src/auth/browser-auth.test.ts` unit test asserting: no token → 401; wrong token → 401 with the byte-identical body/status as no-token (enumeration-resistance parity with the worker pattern); correct token → success (reply.code never called, `request.browserAuthed === true`).

    Transport — registry + relay: create apps/api/src/ws/browser-connections.ts holding an in-memory `Set<WebSocket>` with `registerBrowserSocket(socket)`, `unregisterBrowserSocket(socket)`, and `broadcastToBrowsers(message: unknown)` that `JSON.stringify`s once and calls `.send()` on every socket currently `OPEN` in the set (skip any not open) — same simple-module-state style as `apps/api/src/ws/connection-status.ts`, no heartbeat/staleness tracking needed for browser clients this phase. Create apps/api/src/db/event-row.ts exporting `rowToCompanyEvent(row)`: converts one Drizzle `events` row (`occurredAt: Date`, nullable optional columns) into `CompanyEvent`'s wire shape (`occurredAt.toISOString()`, `null` → `undefined` for `floorId`/`projectId`/`taskId`/`sourceAgentId`/`destinationAgentId`), then runs the result through `CompanyEventSchema.safeParse` — return `.data` on success, throw on failure (a stored row failing its own schema is real corruption, never silently swallowed). Add `company-core` as a new `workspace:*` dependency in apps/api/package.json (needed for `fold`). Create apps/api/src/routes/ws-browser.ts exporting `registerWsBrowserRoute(fastify)`: registers `GET /ws/browser` with `websocket: true`, `preValidation: authenticateBrowser`, `config: { rateLimit: { max: 20, timeWindow: "1 minute" } }` (same shape as `apps/api/src/routes/ws.ts`); on open, call `registerBrowserSocket(socket)`, then `db.select().from(events).orderBy(events.occurredAt)`, map every row through `rowToCompanyEvent`, `fold()` the result via `company-core`'s exported `fold`, and `socket.send(JSON.stringify({ type: "snapshot", state }))`; on `socket.on("close"/"error")` call `unregisterBrowserSocket(socket)`. Modify apps/api/src/routes/events.ts: after the existing successful (non-duplicate) insert branch, add one call `broadcastToBrowsers({ type: "event", event })` using the same already-validated `event` object the insert used — no other change to this file's existing `WORKER_ALLOWED_EVENT_TYPES` gate or dedup logic. Modify apps/api/src/server.ts to register `registerWsBrowserRoute` alongside the existing route registrations (after `registerWsRoute`). Add `apps/api/src/routes/ws-browser.test.ts` mirroring `apps/api/src/routes/ws-auth.test.ts`'s harness exactly (same migration paths, same real Postgres on port 5434, same `attempt()`-style helper) covering: no token → 401; wrong token → 401; correct token → connection opens and the FIRST message received is `{type:"snapshot", state:{...}}` with `state.agents`/`state.tasks` keys present; POSTing a real `company.started` event via `/events` (using an already-issued worker token) then causes a SECOND message `{type:"event", event:{type:"company.started",...}}` to arrive on the still-open browser socket.

    apps/web scaffold: add a new pnpm workspace member `apps/web` (already covered by the root `pnpm-workspace.yaml`'s `apps/*` glob, no config change needed there). package.json: name `"web"`, private, `type: module`, scripts `dev`/`build`/`typecheck` (`tsc --noEmit`)/`test` (`vitest run`), dependencies `react ^19.3`, `react-dom ^19.3`, `company-core: workspace:*`, `event-schema: workspace:*`, `pixel-office: workspace:*`, devDependencies `vite ^8`, `@vitejs/plugin-react`, `typescript ^5.9.3`, `vitest ^5`, `@types/react`, `@types/react-dom`. vite.config.ts: `@vitejs/plugin-react` plugin, default dev port. tsconfig.json: `moduleResolution: "bundler"`, `jsx: "react-jsx"`, `strict: true`, `target: "ES2022"` (does NOT extend the root `tsconfig.json`'s `NodeNext` module resolution — that setting is wrong for a Vite-bundled app). tsconfig.node.json for `vite.config.ts` itself. index.html loading `/src/main.tsx`, mounting `#root`. src/main.tsx: `ReactDOM.createRoot` mounting `<App/>`. src/ws-client.ts exporting `connectOfficeSocket(url: string, token: string, handlers: { onSnapshot(state: ProjectionState): void; onEvent(event: CompanyEvent): void })`: opens `new WebSocket(`${url}/ws/browser?token=${encodeURIComponent(token)}`)`; on message, `JSON.parse`, and for `type === "snapshot"` calls `handlers.onSnapshot(msg.state)`, for `type === "event"` re-validates via `CompanyEventSchema.safeParse(msg.event)` (defense in depth — never trust the wire without re-parsing client-side too) and calls `handlers.onEvent(parsed.data)` only on success. Read `VITE_BROWSER_ACCESS_TOKEN` from `import.meta.env` for the token (Vite only exposes `VITE_`-prefixed vars to client code) — mirror the SAME value already written into apps/api's `.env` into a new apps/web/.env (gitignored) as `VITE_BROWSER_ACCESS_TOKEN=...`. Add apps/web/src/ws-client.test.ts (vitest, mock `WebSocket` global) asserting the snapshot/event message-handling branches dispatch to the right handler and that a re-validation failure on a malformed `event` message does NOT call `onEvent`.

    packages/pixel-office fork: create the new workspace member. Fetch the current `main` SHA (`git ls-remote https://github.com/pixel-agents-hq/pixel-agents main`) and record it once in packages/pixel-office/LICENSE's header alongside the fork's own MIT licence text copied verbatim from `https://raw.githubusercontent.com/pixel-agents-hq/pixel-agents/main/LICENSE`. Fork these files from `https://raw.githubusercontent.com/pixel-agents-hq/pixel-agents/main/webview-ui/src/office/<path>` (per RESEARCH.md's cited, already-verified Sources) into `packages/pixel-office/src/<path>`, each carrying a 3-line header comment (source URL, the SHA fetched, "Forked under MIT — see packages/pixel-office/LICENSE"): `types.ts` (CharacterState/Character/bubbleType/matrixEffect shape, verbatim), `constants.ts` (grid size, frame timings, verbatim), `colorize.ts` (`adjustSprite`/`colorizeSprite` — kept for per-agent identity colour, D-01's explicit "never conflate identity colour with state signal"), `layout/tileMap.ts` (`findPath` BFS, verbatim, unmodified per RESEARCH.md Pattern 1), `sprites/spriteData.ts` (trim to the IDLE pose's sprite frames only this plan — full walk/typing/reading frame sets are not needed until this plan's own render proves the pipe; 05-02 does not need spriteData changes since it only adds bubble/badge JSON assets), `engine/characters.ts` (the `Character` struct + state-transition logic, trimmed to remove the fork's own autonomous idle-wander AI — this office's characters move only in direct response to real events, never on their own timer, per RESEARCH.md Pitfall 2's anti-fabrication rule; no requirement in this project asks for idle wandering), `engine/gameLoop.ts` (requestAnimationFrame update/render loop, verbatim), `engine/renderer.ts` (tile/character draw calls, verbatim). Create packages/pixel-office/package.json (name `"pixel-office"`, private, `type: module`, scripts `test`: `vitest run`, dependencies `event-schema: workspace:*`, `company-core: workspace:*`). Create packages/pixel-office/src/index.ts exporting: `startGameLoop(canvas: HTMLCanvasElement)` (wraps the forked gameLoop's own start function against the given canvas) and `upsertCharacterFromAgent(agentId: string, status: AgentStatus)` — for this plan ONLY maps `AgentStatus.IDLE` to the forked `CharacterState.IDLE` (creating or updating that agent's `Character` at a default desk position); any other `AgentStatus` value passed in this plan is a no-op with a `console.warn` (the exhaustive 15-value mapping table is Plan 05-02's job — this task only needs to prove ONE real value flowing end-to-end). Add packages/pixel-office/src/index.test.ts asserting `upsertCharacterFromAgent` creates a character with `state === CharacterState.IDLE` for `AgentStatus.IDLE`, and that `layout/tileMap.ts`'s forked `findPath` returns an empty array when start === end (sanity check that the fork copied correctly).

    Wire apps/web/src/App.tsx: render a single `<canvas id="office-canvas" width={...} height={...}/>` sized from `packages/pixel-office`'s forked `constants.ts` grid dimensions. On mount: call `startGameLoop(canvasRef.current)`, then `connectOfficeSocket(wsBaseUrl, token, { onSnapshot: (state) => { for (const [agentId, agent] of Object.entries(state.agents)) upsertCharacterFromAgent(agentId, agent.status); }, onEvent: (event) => { if (event.type === "agent.online" || event.type === "session.started") upsertCharacterFromAgent(event.sourceAgentId!, event.type === "agent.online" ? AgentStatus.IDLE : AgentStatus.CODING); } })` — `wsBaseUrl` and `token` both read from `import.meta.env.VITE_*`.
  </action>
  <verify>
    <automated>pnpm --filter api test -- browser-auth</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
    <automated>pnpm --filter api test -- ws-browser</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
    <automated>pnpm --filter company-core test -- reducer</automated>
    <fails_when>non-zero exit, or a failing assertion on the two updated status literals (CODING/WAITING_FOR_AGENT)</fails_when>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
    <automated>pnpm --filter web test -- ws-client</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
    <automated>pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - packages/event-schema exports `AgentStatus` with exactly 15 members, verified by a test asserting `Object.keys(AgentStatus).length === 15`.
    - GET /ws/browser rejects a missing or wrong `?token=` with 401 and the same body/status in both cases; accepts the correct token.
    - The first message a correctly-authenticated browser socket receives is `{type:"snapshot", state:{agents:{...}, ...}}`; a subsequent real POST /events causes a `{type:"event", event:{...}}` message on that same socket.
    - `packages/company-core/src/reducer.test.ts` passes with `agent-1`/`agent-2` now asserting `AgentStatus.CODING`/`AgentStatus.WAITING_FOR_AGENT` instead of the old ad hoc `"working"`/`"assigned"` strings.
    - `packages/pixel-office`'s forked `findPath`, `gameLoop`, `renderer`, `characters` compile and `upsertCharacterFromAgent(id, AgentStatus.IDLE)` produces a `Character` with `state === CharacterState.IDLE`.
    - Every forked file under `packages/pixel-office/src/` carries the 3-line source/SHA/licence header comment.
  </acceptance_criteria>
  <done>All six automated verify commands pass; a real agent.online event, posted through the worker pipeline, renders as an idle character in apps/web via a real WS snapshot+relay round trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → apps/api /ws/browser | First-ever browser-facing (not worker-facing) connection into the control plane; untrusted until token-authenticated |
| apps/api → forked third-party renderer code | Company projection state (derived from real, schema-validated events) flows into code copied from an external MIT repo, not authored in-house |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | apps/api/src/routes/ws-browser.ts | high | mitigate | `authenticateBrowser` timing-safe-compares `?token=` against `BROWSER_ACCESS_TOKEN` before the WS upgrade completes; uniform 401 on any failure, same posture as `authenticateWorker` |
| T-05-02 | Tampering | apps/web/src/ws-client.ts (consumes relayed events) | medium | mitigate | Every relayed `event` message is re-validated client-side via `CompanyEventSchema.safeParse` before being applied — never trusts the wire payload's shape |
| T-05-03 | Information Disclosure | apps/api/src/routes/ws-browser.ts (broadcasts the full unfiltered event/state stream) | medium | accept | Phase 7's visibility-tier filtering has not shipped yet; this route stays INTERNAL-tier and is not exposed publicly this phase, matching RESEARCH.md's explicit "do not expose it publicly yet" — same trust boundary as the existing worker connection |
| T-05-04 | Denial of Service | apps/api/src/routes/ws-browser.ts | low | mitigate | `config.rateLimit: {max:20, timeWindow:"1 minute"}` on the route's HTTP-upgrade request, identical to `GET /ws` |
| T-05-05 | Tampering | packages/pixel-office forked engine code | medium | mitigate | Copied verbatim with an attribution header citing the source commit SHA; no `eval`/dynamic-code paths in the forked modules — `gameLoop`/`renderer`/`characters` only ever draw from typed `Character` struct fields |
| T-05-SC | Tampering | npm installs (react, react-dom, vite, @vitejs/plugin-react) | high | mitigate | Task 1's `checkpoint:human-verify gate="blocking-human"` verifies each package via `npm view` before Task 2 installs anything |

</threat_model>

<verification>
Run all six `<automated>` commands above. All must exit 0 with their respective test files reporting passed counts > 0. `pnpm --filter web typecheck` must report zero `error TS` lines.
</verification>

<success_criteria>
A real `agent.online` event, posted through the existing worker pipeline, is visible moments later as a rendered idle character in `apps/web` — via a real WS connection, a real snapshot, a real relayed event, and the real forked Canvas2D engine. No part of this path is mocked, hardcoded, or timer-scripted.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-01-SUMMARY.md` when done
</output>
