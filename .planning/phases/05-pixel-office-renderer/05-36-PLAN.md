---
phase: 05-pixel-office-renderer
plan: 36
type: execute
gap_closure: true
gap_ids: [G-05-P6, WR-02]
wave: 1
depends_on: []
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/App.test.tsx
  - scripts/verify-pixel-office-live.mjs
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
autonomous: true
requirements: [OFFICE-02, OFFICE-01]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ── Gap-derived (05-VERIFICATION.md gap G-05-P6 / review WR-02, the only open gap) ──
    - "WR-02 / SC4 / OFFICE-02 (D-05): the attribution footer carries its own unconditional backdrop — the footer's inline background declaration is exactly WALL_COLOR (#3A3A5C), the same value the full-viewport surround already uses — so the 11px #cccccc sentence measures 6.74:1 wherever the footer lands, instead of 4.40:1 on floor plank #7d4e13 and 3.21:1 on #926429"
    - "G-05-P6 is untouched by the fix: the backdrop is not black and adds no near-black pixel — the live harness's full-viewport screenshot scan still reports 0 near-black px (every channel <= 16) at every checked viewport"
    - "apps/web/src/App.test.tsx asserts the footer's background declaration by EQUALITY against WALL_COLOR, so BOTH a black strip AND a missing backdrop turn the suite red; the two literal-substring guards that passed for 'no backdrop at all' are gone"
    - "The overflow case is covered at all: a viewport below MIN_DISPLAY_SCALE's 960x528 canvas footprint is exercised both as a unit-level geometry assertion in App.test.tsx (the canvas provably cannot fit, so the pinned footer can land on the floor) and as a third entry in the live harness's OBS_SIZES — and what is asserted there is the footer's BACKDROP, not only its position"
    - "Everything 05-31 proved is unchanged and still passes: the complete attribution sentence, its always-on/ungated properties, full-viewport sizing (scale 4 at 1280x720, 6 at 1920x1080), centring when the canvas fits, CSS box == backing store, image-rendering pixelated, and the WALL_COLOR page body"
    # ── Lifted from 05-UI-SPEC.md `## UI Considerations` (7 covered -> plain truths, 1 backstop -> flat scalar).
    #    Carried forward from the already-verified phase state; this round must not regress any of them. ──
    - "populated: AgentStatus -> pose/bubble mapping is exhaustive and compile-checked for all 15 values with no fallback branch (status-mapping.ts)"
    - "zero-one-many: the agent roster on the floor is 1:1 with real projection state, with no fabricated placeholder agents (upsertCharacterFromAgent / despawn-on-offline)"
    - "loading: blocked / waiting_for_agent / waiting_for_ceo all resolve to frozen: true plus a distinct bubble key (OFFICE-03, D-03)"
    - "empty: character sprite pixels are real MetroCity data, never transparent placeholders (character-metrocity.json)"
    - "overflow (state signal render): the bubble/badge glyph draw pass runs as the top layer, head-anchored 1px above the owner's visible head"
    - "overflow (missing assets): bubble-permission.json and bubble-waiting.json exist in the locked 11x13 {palette, pixels} format and render through the same glyph pass"
    - "long-text: resolveHandoffDialogue caps taskTitle at 12 and toAgentName at 10 code points with U+2026, and getActiveHandoffs exposes the untruncated title"
    - statement: "error (WS disconnect): a socket close/error shows a user-observable disconnect banner (role=status) in apps/web/src/App.tsx"
      verification: backstop
  artifacts:
    - apps/web/src/App.tsx
    - apps/web/src/App.test.tsx
    - scripts/verify-pixel-office-live.mjs
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
  key_links:
    - "WALL_COLOR (packages/pixel-office/src/constants.ts) -> re-exported by packages/pixel-office/src/index.ts -> BOTH the App.tsx wrapper background and the App.tsx footer background (one constant, two consumers, so the surround and the footer can never drift apart)"
    - "App.tsx footer style -> renderToStaticMarkup output -> App.test.tsx's background-declaration equality assertion (the guard that goes red on a deleted backdrop)"
    - "readColorConst('WALL_COLOR') in scripts/verify-pixel-office-live.mjs -> hex-to-rgb -> getComputedStyle(footer).backgroundColor assertion at every OBS_SIZES entry, including the new below-MIN_DISPLAY_SCALE one"
    - "MIN_DISPLAY_SCALE (packages/pixel-office/src/index.ts) x DEFAULT_COLS x TILE_SIZE = the 960x528 canvas floor -> the 800x480 overflow viewport both the unit test and the harness now exercise"
  prohibitions:
    - "MUST NOT reintroduce a black or near-black strip behind the footer — the backdrop is WALL_COLOR, the same value the surround uses, so G-05-P6's no-black-frame property stays intact"
    - "MUST NOT weaken the whole-sentence attribution pin, gate the footer behind a disclosure element, or make the backdrop conditional on viewport, scroll position or state (OFFICE-02, D-05)"
    - "MUST NOT change the footer's copy, its position:fixed placement, or the 11px / monospace typography locked by 05-UI-SPEC's Copywriting Contract and Typography table"
    - "MUST NOT satisfy the overflow-coverage item by relaxing MIN_DISPLAY_SCALE, by removing the wrapper's overflow:auto, or by lowering the harness's NEAR_BLACK_MAX threshold"
    - "MUST NOT weaken or delete any assertion 05-31 added to TRUTH 0 (integer scale, backing-store equality, pixelated, centring-when-fitting, the near-black scan)"
---

<objective>
Close the one open gap in `05-VERIFICATION.md` (G-05-P6 / review WR-02): the attribution
footer lost its backdrop when 05-31 deleted `rgba(0, 0, 0, 0.6)` and did not replace it, so
the 11px `#cccccc` legal notice now depends on whatever sits behind it — and on any viewport
below the 960x528 canvas floor, what sits behind it is a MetroCity floor plank at 4.40:1 or
3.21:1, below WCAG AA.

Purpose: SC4 / OFFICE-02 is about a legal attribution notice. The contrast guarantee must be
unconditional, and the guard protecting it must fail when it is removed — today's substring
check passes for "no backdrop at all", so it pins the regression instead of catching it.

Output: an unconditional `background: WALL_COLOR` on the footer; an equality assertion on that
declaration in `App.test.tsx`; unit-level and live-harness coverage of the below-minimum
viewport where the canvas overflows and the pinned footer can reach the floor.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@.planning/phases/05-pixel-office-renderer/05-REVIEW.md
@.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
@.planning/phases/05-pixel-office-renderer/05-31-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Give the footer an unconditional WALL_COLOR backdrop, proven by an equality assertion</name>
  <files>apps/web/src/App.tsx, apps/web/src/App.test.tsx, .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>
  <read_first>
    - apps/web/src/App.tsx (the whole file — the footer at the end, the wrapper `background: WALL_COLOR` it must match, and the `05-31 (G-05-P6)` comment block above the footer that currently claims the footer is transparent and "never the floor")
    - apps/web/src/App.test.tsx (the whole file — `footerMarkup`/`wrapperMarkup` slicing at the top, the existing `background:${WALL_COLOR.toLowerCase()}` wrapper assertion, and the `overlays the attribution with no black strip behind it` case)
    - packages/pixel-office/src/constants.ts (confirm `WALL_COLOR` is `"#3A3A5C"` and that it is the value the wrapper uses)
    - packages/pixel-office/src/index.ts (confirm `WALL_COLOR` and `MIN_DISPLAY_SCALE` are re-exported from the package root — that is the import App.tsx and App.test.tsx already use)
    - .planning/phases/05-pixel-office-renderer/05-REVIEW.md (WR-02 — the measured contrast table and the exact fix it recommends)
  </read_first>
  <behavior>
    Write the assertion FIRST, watch it go red against today's backdrop-less footer, then add the
    style property.
    - Test 1 (red first): the footer's rendered `style` attribute contains exactly one background
      declaration and it equals `background:#3a3a5c` (case-normalized) — fails today with an empty
      declaration list, and would fail again for `rgba(0,0,0,0.6)`, `black`, `rgb(0,0,0)`, `#010101`
      or a `background-color:` variant.
    - Test 2: the existing whole-sentence attribution pin and the ungated/never-hidden assertions
      still pass unchanged.
    - Test 3: the wrapper surround's background still equals the same `WALL_COLOR` value — one
      constant, two consumers, so the footer and the surround cannot drift apart.
  </behavior>
  <action>
In `apps/web/src/App.test.tsx`, replace the `overlays the attribution with no black strip behind it`
case (currently two `not.toContain` substring guards inside the `App viewport fill` describe block)
with an equality assertion on the footer's background declaration. Derive the declaration from the
rendered markup rather than restating it: extract the footer's `style` attribute value with a regex
over `markup` anchored on `<footer`, split the value on `;`, trim and lowercase each declaration,
keep only those whose property is `background` or `background-color`, and assert the resulting array
`toEqual` exactly `[`background:${WALL_COLOR.toLowerCase()}`]`. `WALL_COLOR` is already imported in
this file from `pixel-office` — do not hardcode `#3A3A5C`. Name the case so it states the property,
not the absence of one (for example: `backs the attribution with the office border colour, so its
contrast never depends on what is underneath`). Keep the three `not.toContain` guards in the
`shows the credit unconditionally` case untouched — they pin gating, not colour.

Run the suite and confirm the new case fails before continuing.

Then in `apps/web/src/App.tsx`, add `background: WALL_COLOR` to the `<footer>` element's inline
`style` object, as a sibling of the existing `color: "#cccccc"`. `WALL_COLOR` is already imported at
the top of the file — reuse that import, do not introduce a literal. Place it last in the object so
the declaration order in the rendered attribute is stable. Change nothing else about the footer: not
the sentence, not `position: "fixed"`, not `bottom/left/right: 0`, not `padding: "4px 8px"`, not
`fontSize: "11px"`, not `fontFamily: "monospace"`, not `color: "#cccccc"`.

Correct the stale claim in the comment block directly above the footer. Two of its clauses are now
false: it says the footer is transparent, and it says the footer overlays the wall row and remainder
"never the floor". Rewrite that final paragraph to state what is actually true and why: the footer
carries its own `WALL_COLOR` backdrop (the same value as the surround, so no black strip returns),
which is what makes the contrast unconditional, because `MIN_DISPLAY_SCALE` floors the canvas at
960x528 while the footer is `position: fixed` — so on a smaller viewport the wrapper's
`overflow: auto` scrolls the canvas under the footer and the footer CAN sit over the floor. Cite
`WR-02` and the measured ratios (6.74:1 on the wall colour versus 4.40:1 and 3.21:1 on the two
MetroCity floor planks).

Finally, update `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md`: in the Copywriting
Contract table's `Attribution footer` row, the sentence beginning `**Presentation updated 05-31
(G-05-P6):**` asserts the footer is transparent and never over the floor. Append a
`**Corrected 05-36 (WR-02):**` clause recording that the footer now carries a `WALL_COLOR` backdrop
— the same value as the surround, so the black strip does not return — and that the "never the
floor" claim held only while the canvas fitted the viewport. Leave the locked properties (always-on,
fixed, ungated, crediting both fork and pack, pointing at the audit) as written.
  </action>
  <reversibility rating="reversible">One inline style property sourced from an existing constant; removing it restores the prior rendering exactly.</reversibility>
  <verify>
    <automated>pnpm --filter web test</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
    <automated>pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit (tsc reports an error)</fails_when>
    <automated>grep -n "background" apps/web/src/App.tsx</automated>
    <fails_when>fewer than two matching lines (the wrapper and the footer must each carry one), or any match whose value is a literal hex/rgb string instead of the WALL_COLOR identifier</fails_when>
  </verify>
  <acceptance_criteria>
    - Source assertion: `apps/web/src/App.tsx`'s `<footer>` style object contains `background: WALL_COLOR` and no color literal; `grep -n "WALL_COLOR" apps/web/src/App.tsx` prints at least the import line, the wrapper style line and the footer style line, and the footer's line number is inside the `<footer` element.
    - Behavior assertion: rendering `<App />` with `renderToStaticMarkup` produces a `<footer` whose `style` attribute contains exactly one background declaration, and it equals `background:#3a3a5c` when lowercased.
    - Test-command assertion: `pnpm --filter web test` passes, and the new background-equality case is present — `grep -v '^\s*//' apps/web/src/App.test.tsx | grep -c "toEqual"` is >= 1.
    - Source assertion: the substring-guard case is gone — `grep -n 'not\.toContain' apps/web/src/App.test.tsx` prints only line numbers that fall inside the `shows the credit unconditionally` case (the three gating guards for `hidden`, `<details` and `<dialog`), and none inside the `App viewport fill` describe block.
    - Source assertion: the attribution sentence in `App.tsx` and the `ATTRIBUTION` constant in `App.test.tsx` are byte-identical to their pre-task values.
    - Source assertion: `05-UI-SPEC.md`'s `Attribution footer` row contains the string `Corrected 05-36 (WR-02)`.
  </acceptance_criteria>
  <done>The footer renders with `background: WALL_COLOR`; the suite's background assertion is an equality on that declaration and was observed red before the style was added; the attribution sentence, placement and typography are unchanged; the stale "transparent / never the floor" claims in both the source comment and the UI-SPEC are corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the overflow case at unit level — prove the canvas cannot fit, so the footer can reach the floor</name>
  <files>apps/web/src/App.test.tsx</files>
  <read_first>
    - apps/web/src/App.test.tsx (as edited by Task 1 — the `App viewport fill` describe block, the `displayScaleFor(1280, 720)` / `displayScaleFor(1920, 1080)` cases, and the existing imports of `DEFAULT_COLS`, `DEFAULT_ROWS`, `MIN_DISPLAY_SCALE`, `TILE_SIZE`, `WALL_COLOR`, `displayScaleFor` from `pixel-office`)
    - packages/pixel-office/src/index.ts (`displayScaleFor` and `MIN_DISPLAY_SCALE` — read the actual clamping expression; the harness restates it as `Math.max(MIN_DISPLAY_SCALE, Math.floor(Math.min(w / 320, h / 176)))`, confirm that is what the function does)
    - apps/web/src/App.tsx (the wrapper's `overflow: "auto"` and `margin: "auto"` — the mechanism that scrolls the canvas under the pinned footer)
    - .planning/phases/05-pixel-office-renderer/05-VERIFICATION.md (the gap's third `missing:` item — this task is the unit-level half of it)
  </read_first>
  <action>
Add one case to `apps/web/src/App.test.tsx`'s `App viewport fill` describe block that makes the
overflow case explicit rather than implied. Declare a single below-minimum viewport as a local
constant (`OVERFLOW_VIEWPORT = { w: 800, h: 480 }`) and assert three things about it, each computed
from the package's own exported constants and never from a restated number:

1. `displayScaleFor(800, 480)` equals `MIN_DISPLAY_SCALE` — the clamp fires, so the office is not
   scaled down to fit.
2. `DEFAULT_COLS * TILE_SIZE * MIN_DISPLAY_SCALE` is strictly greater than `800` and
   `DEFAULT_ROWS * TILE_SIZE * MIN_DISPLAY_SCALE` is strictly greater than `480` — the canvas
   therefore cannot fit this viewport on either axis, so the wrapper's `overflow: auto` scrolls it
   under the `position: fixed` footer and the footer can sit over the floor.
3. The footer's background declaration (reuse the same extraction Task 1 added — factor it into a
   module-level helper in this test file if that avoids duplicating the regex) still equals
   `background:${WALL_COLOR.toLowerCase()}`, asserted in this case too, stating the property the
   overflow case depends on: the backdrop is a static declaration on a static element, so it does
   not vary with viewport.

Name the case so it states the reachability, for example `keeps the credit legible where the canvas
overflows the viewport and scrolls under it`. Write a short comment above it recording WHY 800x480
is the chosen size (it is below the 960x528 floor on both axes) and that the numbers it compares
against come from `MIN_DISPLAY_SCALE`, so a change to the clamp moves this assertion with it rather
than silently invalidating it.

Do not add jsdom, a DOM environment, or any new dependency to `apps/web` — this suite renders with
`renderToStaticMarkup` only, and the assertion is arithmetic over exported constants plus the static
markup.
  </action>
  <reversibility rating="reversible">A test-only addition; deleting the case restores the previous coverage.</reversibility>
  <verify>
    <automated>pnpm --filter web test</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
    <automated>grep -c "OVERFLOW_VIEWPORT" apps/web/src/App.test.tsx</automated>
    <fails_when>a count of 0 (the overflow viewport constant is absent), or a non-zero exit from grep</fails_when>
    <automated>git diff --name-only -- apps/web/package.json</automated>
    <fails_when>the command prints apps/web/package.json (a dependency was added, which this task forbids)</fails_when>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: with `MIN_DISPLAY_SCALE = 3`, `DEFAULT_COLS = 20`, `DEFAULT_ROWS = 11`, `TILE_SIZE = 16`, the new case asserts `960 > 800` and `528 > 480` via the constant expressions, and `displayScaleFor(800, 480) === 3`.
    - Source assertion: the case compares against `DEFAULT_COLS * TILE_SIZE * MIN_DISPLAY_SCALE` and `DEFAULT_ROWS * TILE_SIZE * MIN_DISPLAY_SCALE`, not against the literals `960` or `528`.
    - Test-command assertion: `pnpm --filter web test` passes and its file summary still reports the same number of test FILES as before this plan (no new test file is introduced).
    - Source assertion: `apps/web/package.json` is unchanged by this task.
  </acceptance_criteria>
  <done>`App.test.tsx` proves the below-minimum viewport is reachable (the canvas overflows both axes), and asserts the footer's backdrop in that same case, so the overflow scenario is covered by the fast suite regardless of whether the live harness is run.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the live harness with a below-MIN_DISPLAY_SCALE viewport and assert the footer's backdrop</name>
  <files>scripts/verify-pixel-office-live.mjs</files>
  <precondition>The live harness owns its own stack: Docker must be running for `apps/api/docker-compose.test.yml`, and ports `API_PORT` and `WEB_PORT` must be free — the harness refuses to reuse a server it did not start (WR-06) and exits before writing anything. Assert both before running the full probe; if either is unmet, halt and report rather than weakening the guard.</precondition>
  <read_first>
    - scripts/verify-pixel-office-live.mjs lines 1-60 (the header truth list, including the TRUTH 0 description at :33) and lines 160-215 (`readColorConst`, `readNumberConst`, the `TILE_SIZE` / `DEFAULT_COLS` / `DEFAULT_ROWS` / `MIN_DISPLAY_SCALE` / `MAP_W` / `MAP_H` reads) and lines 660-700 (`NEAR_BLACK_MAX`, `scanViewportForBlack`) and lines 780-925 (the whole TRUTH 0 block: `OBS_SIZES`, the `waitForFunction` on the backing store, the `geom` `page.evaluate`, and every assertion in the loop)
    - apps/web/src/App.tsx (the footer as edited by Task 1 — the harness asserts its computed background)
    - packages/pixel-office/src/constants.ts (`WALL_COLOR`, the value `readColorConst` will read)
    - .planning/phases/05-pixel-office-renderer/05-31-PLAN.md (the task that last edited this harness — reuse its verify commands verbatim)
  </read_first>
  <action>
Extend TRUTH 0 in `scripts/verify-pixel-office-live.mjs` so the overflow case is reachable by the
live probe and so the footer's BACKDROP is asserted, not only its position.

Read the colour from the engine, never restate it: add `const WALL_COLOR = readColorConst("WALL_COLOR");`
alongside the other engine-constant reads near `MIN_DISPLAY_SCALE`, plus a small `hexToRgbCss(hex)`
helper that returns the `rgb(r, g, b)` form `getComputedStyle` reports (parse the three byte pairs,
join with `", "`). `readColorConst` already exists in this file and already reads
`packages/pixel-office/src/constants.ts` — reuse it. This keeps the harness's own rule that
geometry and colour come from the engine's source (the rule review item IN-05 flagged the
hardcoded `TAIL_REACH_PX` for breaking).

Give each `OBS_SIZES` entry an explicit `fits` flag and add a third entry for the overflow case.
The new entry is `{ w: 800, h: 480, obsScale: 3, fits: false }` — write `3` as a literal so the
existing `expected === obsScale` assertion genuinely pins that an 800x480 viewport floors to
`MIN_DISPLAY_SCALE` rather than comparing the constant against itself. Mark `1920x1080` and
`1280x720` as `fits: true`. Order the array `800x480`, then `1920x1080`, then `1280x720`, so the
run still ENDS back at `1280x720` and every later truth sees the viewport it always has — update
the comment above `OBS_SIZES` that explains that ordering.

Add the footer's computed backdrop to the `geom` `page.evaluate` return value as
`footerBg: footer ? getComputedStyle(footer).backgroundColor : null`, alongside the existing
`footer` rect and `footerText`.

Inside the loop:
- Assert `geom.footerBg === hexToRgbCss(WALL_COLOR)` at EVERY entry, outside any `fits` branch —
  this is the WR-02 property and it is exactly the thing that must not be conditional. Its failure
  message must name both the expected and the received colour and say that the attribution's
  contrast now depends on whatever is behind it.
- Keep the integer-scale, expected-scale, backing-height, `CSS box == backing store` and
  `image-rendering: pixelated` assertions running at every entry, unchanged.
- Scope the two centring assertions and the footer bottom-wall-band assertion to `fits` entries
  only, with a comment stating why: centring and "the footer sits on the bottom wall row" are
  properties of the case where the canvas fits, and the overflow entry exists precisely because
  they do not hold there.
- For a `fits: false` entry, assert instead that the canvas genuinely overflows —
  `geom.cssW > geom.viewW || geom.cssH > geom.viewH` — with a message saying the overflow case
  under test did not actually overflow, so the entry is proving nothing. Keep the
  `geom.footerText.includes("pixel-agents-hq/pixel-agents")` assertion at every entry.
- Keep `scanViewportForBlack` running at every entry, `NEAR_BLACK_MAX` unchanged. If — and only if
  — the overflow entry reports near-black pixels that are browser scrollbar chrome rather than
  unpainted page background (identify them from the reported `first` coordinate: a hit in the
  right-edge or bottom-edge gutter of the overflowing viewport), exclude exactly that gutter behind
  a named constant (`SCROLLBAR_GUTTER_PX`) with a comment stating that it is browser chrome painted
  outside the page's own box. Do not lower the threshold and do not skip the scan.

Update the `TRUTH 0` header comment at :33 and the final `TRUTH 0 PASS` log line so both name the
three viewports and the backdrop assertion, and so the log reports the footer colour it observed.
  </action>
  <reversibility rating="reversible">Harness-only additions: one const read, one array entry, one computed-style assertion, and a `fits` guard around two existing assertions.</reversibility>
  <verify>
    <automated>node --check scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit (a SyntaxError is printed)</fails_when>
    <automated>node scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit, any "TRUTH FAILED" line, or no final "LIVE PROOF: PASS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `node scripts/verify-pixel-office-live.mjs` exits 0 and its `TRUTH 0 PASS` line names three viewports including `800x480`, reports scale `3` for it, and names the observed footer background colour.
    - Behavior assertion: reverting the `background: WALL_COLOR` property from `App.tsx` and re-running the probe produces a `TRUTH FAILED` line naming the footer backdrop at the FIRST viewport checked (the executor may verify this by reasoning over the assertion's placement outside any `fits` branch rather than by a second full live run).
    - Source assertion: `grep -n "readColorConst" scripts/verify-pixel-office-live.mjs` prints a `WALL_COLOR` read alongside the existing reads, and `grep -v '^\s*//' scripts/verify-pixel-office-live.mjs | grep -c "footerBg"` is >= 2 (the evaluate return and the assertion).
    - Source assertion: `NEAR_BLACK_MAX` is still `16`, and the near-black scan is still called once per OBS_SIZES entry.
    - Source assertion: `OBS_SIZES` has exactly three entries, the last of which is `1280x720`.
  </acceptance_criteria>
  <done>The live probe exercises a viewport where the canvas overflows and the pinned footer can reach the floor, asserts the footer's computed backdrop equals the engine's own `WALL_COLOR` at every viewport, keeps every 05-31 assertion that still applies, and prints `LIVE PROOF: PASS`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM -> viewer | The attribution footer is a legal notice rendered to a public stream overlay; illegibility is the failure mode, not data exposure |
| harness -> local dev stack | `scripts/verify-pixel-office-live.mjs` resets a database and starts real `apps/api` / `apps/web` servers |
| repo -> npm registry | package installs (none in this round) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-36-01 | Information Disclosure | `apps/web/src/App.tsx` footer | low | accept | The footer interpolates nothing — it renders a compile-time constant sentence. Adding a background property introduces no data path, no state read and no new element. |
| T-05-36-02 | Tampering | `scripts/verify-pixel-office-live.mjs` | medium | mitigate | `assertHarnessOwnedTarget()` and the WR-06 port-refusal guard stay untouched; the new `OBS_SIZES` entry adds a viewport size only — no new network target, no server reuse path, no change to the database reset. Acceptance criteria assert `NEAR_BLACK_MAX` and the scan call count are unchanged, so the probe cannot be silently weakened while claiming a pass. |
| T-05-36-03 | Denial of Service | full-viewport screenshot scan, third viewport | low | accept | One extra `O(viewport px)` scan (800x480 = 384k px) in a developer-run harness; no production path. |
| T-05-36-04 | Spoofing | the verification evidence itself | medium | mitigate | The failing guard is the actual defect here: a substring check that passes for "no backdrop at all" lets a removed accessibility guarantee report green. Replaced with an equality assertion (Task 1) so the evidence cannot claim a property the code does not have. |
| T-05-36-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this round — no `package.json` or lockfile is in `files_modified`, and Task 2 carries an acceptance criterion asserting `apps/web/package.json` is unchanged. `05-RESEARCH.md`'s Package Legitimacy Audit is therefore not re-entered; no `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `pnpm --filter web test` — green, including the new background-equality case and the new overflow-viewport case.
- `pnpm --filter web typecheck` — clean.
- `node --check scripts/verify-pixel-office-live.mjs` then `node scripts/verify-pixel-office-live.mjs` — `LIVE PROOF: PASS`, `TRUTH 0 PASS` naming three viewports including `800x480`.
- `pnpm --filter pixel-office test` — unchanged and green (this plan touches no engine source; run it once as a regression check).
- Contrast: `#cccccc` on `#3A3A5C` is 6.74:1, above WCAG AA's 4.5:1 for 11px normal text — unconditional now, because the backdrop is on the footer itself rather than inherited from what is behind it.
</verification>

<success_criteria>
- The footer's rendered background declaration equals `WALL_COLOR` and nothing else.
- Deleting that property turns `pnpm --filter web test` red (the equality assertion) and, on a live run, produces a `TRUTH FAILED` line at the first viewport.
- The below-minimum viewport is exercised in both the fast suite and the live probe, and the backdrop is what is asserted there.
- No near-black pixel appears at any checked viewport; every 05-31 TRUTH 0 assertion that still applies is intact.
- The attribution sentence, its placement and its typography are byte-identical to their pre-plan values.
</success_criteria>

## Artifacts this phase produces

New or changed symbols introduced by this plan (no new exported runtime symbol — this round adds
one style property, two test cases and three harness identifiers):

| Symbol | Kind | Location | Note |
|---|---|---|---|
| `background: WALL_COLOR` | inline style property | `apps/web/src/App.tsx` `<footer>` | the fix itself; sources the existing `WALL_COLOR` import |
| `backs the attribution with the office border colour…` | test case | `apps/web/src/App.test.tsx` | equality assertion on the footer's background declaration (replaces the substring guard) |
| `keeps the credit legible where the canvas overflows…` | test case | `apps/web/src/App.test.tsx` | the unit-level overflow geometry check |
| `OVERFLOW_VIEWPORT` | test-local constant | `apps/web/src/App.test.tsx` | `{ w: 800, h: 480 }` — below the 960x528 canvas floor on both axes |
| `WALL_COLOR` | harness constant | `scripts/verify-pixel-office-live.mjs` | `readColorConst("WALL_COLOR")` |
| `hexToRgbCss` | harness function | `scripts/verify-pixel-office-live.mjs` | `#RRGGBB` -> `rgb(r, g, b)` for the computed-style comparison |
| `fits` | `OBS_SIZES` entry field | `scripts/verify-pixel-office-live.mjs` | scopes the centring and bottom-wall-band assertions |
| `SCROLLBAR_GUTTER_PX` | harness constant | `scripts/verify-pixel-office-live.mjs` | conditional — only if the overflow viewport's near-black hits are browser scrollbar chrome |

No new file is created. No dependency is added. No exported API of `pixel-office`, `company-core`
or `apps/api` changes.

<planner_assumptions>
Flagged, not authored into `must_haves.truths` — surfaced so they are decided rather than dropped.

**A1 — Deterministic edge probe (all five rows `unclassified` -> `unresolved`).** No plain
`-SPEC.md` exists for Phase 5, so `## Edge Coverage` and `## Prohibitions` are absent and the
specless fallback fires. The orchestrator's probe returned `unclassified` for every one of
`OFFICE-01`, `OFFICE-02`, `OFFICE-03`, `HANDOFF-01`, `HANDOFF-02` (coverage: 5 applicable, 0
resolved, 5 unresolved, 0 explicit, 0 backstop). Per #1110 none is auto-resolved with a backstop and
none becomes a truth. Each is recorded here as an assumption that the requirement's edge behaviour
for this round is governed by `05-VERIFICATION.md`'s already-verified truths 1-11 rather than by a
spec-declared edge list:
  - `OFFICE-01` — unresolved; assumed covered by verified truths 1, 5, 8, 11.
  - `OFFICE-02` — unresolved; this plan's gap IS its open edge (footer legibility where the canvas overflows).
  - `OFFICE-03` — unresolved; assumed covered by verified truths 2, 8, 10; untouched by this plan.
  - `HANDOFF-01` — unresolved; assumed covered by verified truths 3, 6, 7; see 05-37 for WR-08.
  - `HANDOFF-02` — unresolved; assumed covered by verified truths 3, 9; see 05-37 for WR-07.

**A2 — 800x480 is the chosen overflow viewport.** Any viewport below 960x528 on either axis
demonstrates the case; 800x480 is below on BOTH axes, which makes the assertion independent of which
axis the clamp binds on. No source artifact names a specific size — this is a planner choice.

**A3 — The harness's centring and bottom-wall-band assertions are scoped to fitting viewports.**
Neither property is defined when the canvas overflows, and the gap statement asks for the BACKDROP
to be asserted at the overflow size, not the position. Scoping them is a planner call, recorded
rather than silently applied.

**A4 — Browser scrollbar chrome at the overflow viewport.** Whether headless Chromium paints a
scrollbar gutter that trips `NEAR_BLACK_MAX` is not knowable from source. Task 3 handles both
outcomes explicitly and forbids lowering the threshold.
</planner_assumptions>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-36-SUMMARY.md` when done.
</output>
