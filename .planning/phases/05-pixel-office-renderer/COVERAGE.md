No external API integration: Phase 5 is a local rendering (Canvas2D in `packages/pixel-office`) + WebSocket-transport (`apps/api`'s browser-facing Broadcast Hub) phase — the sole "api" match the detector found is the native browser `WebSocket` API referenced in a code comment (`apps/web/src/ws-client.ts` / `ws-client.test.ts`: "a native browser WebSocket cannot set an Authorization header before the handshake"), not a third-party API/SDK/service integration. The one external dependency this gap-closure plan set adds (`pngjs`, a devDependency used only by a one-time asset-decode script, never shipped to the browser bundle) is a plain npm package, not an API — it is gated through the standard package-legitimacy `checkpoint:human-verify` in 05-06-PLAN.md, not this gate.
