---
phase: 05-pixel-office-renderer
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pixel-office/src/sprites/bubble-permission.json
  - packages/pixel-office/src/sprites/bubble-waiting.json
  - packages/pixel-office/src/sprites/bubble-handoff-task.json
  - packages/pixel-office/src/sprites/bubbleSprites.ts
  - packages/pixel-office/src/sprites/bubbleSprites.test.ts
  - packages/pixel-office/src/engine/renderer.ts
  - packages/pixel-office/src/engine/renderer.test.ts
  - packages/pixel-office/src/constants.ts
  - packages/pixel-office/tsconfig.json
autonomous: true
gap_closure: true
requirements: [OFFICE-01, OFFICE-03, HANDOFF-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engine/renderer.ts paints Character.bubbleType's icon above the character sprite for every non-null bubbleType — closing 05-VERIFICATION.md's no-bubble-render gap (Truth 2)"
    - "waiting_for_ceo (bubble: permission) and waiting_for_agent (bubble: waiting) each resolve to a real, distinct-silhouette JSON asset that now actually renders — the two missing assets 05-02 never authored"
    - "handoff-choreography.ts's 'handoff-task' bubbleType (set on arrival, per HANDOFF-01's task-icon-appearing requirement) resolves to a real, distinct icon asset that renders — the handoff choreography previously had no backing asset for its own task icon at all"
    - "Every one of the 12 BubbleType union members resolves to a distinct-silhouette SpriteData grid — no two glyphs are a recolour of each other (OFFICE-03's grayscale-legibility prohibition, extended to the 3 new assets)"
  artifacts:
    - path: "packages/pixel-office/src/sprites/bubbleSprites.ts"
      provides: "BUBBLE_SPRITES: Record<BubbleType, SpriteData> (exhaustive) + resolveBubbleSprite(bubbleType)"
    - path: "packages/pixel-office/src/sprites/bubble-permission.json"
      provides: "waiting_for_ceo's icon asset (previously referenced by STATUS_MAP but never authored)"
    - path: "packages/pixel-office/src/sprites/bubble-waiting.json"
      provides: "waiting_for_agent's icon asset (previously referenced by STATUS_MAP but never authored)"
    - path: "packages/pixel-office/src/sprites/bubble-handoff-task.json"
      provides: "The handoff choreography's task-icon asset (previously had no backing asset at all)"
    - path: "packages/pixel-office/src/engine/renderer.ts"
      provides: "A bubble/badge overlay draw pass inside renderScene, z-attached to each character, drawn after (on top of) the base sprite"
  key_links:
    - from: "packages/pixel-office/src/engine/renderer.ts"
      to: "packages/pixel-office/src/sprites/bubbleSprites.ts"
      via: "resolveBubbleSprite(ch.bubbleType) called once per character with a non-null bubbleType, per render frame"
      pattern: "resolveBubbleSprite"
    - from: "packages/pixel-office/src/handoff/handoff-choreography.ts"
      to: "packages/pixel-office/src/sprites/bubble-handoff-task.json"
      via: "Character.bubbleType = \"handoff-task\" (set in checkHandoffArrivals) now resolves to a real rendered icon, not just a data label"
      pattern: "handoff-task"
  prohibitions: []

user_setup: []
---

<objective>
Add the bubble/badge icon overlay draw pass that `packages/pixel-office/src/engine/renderer.ts` has never had — closing 05-VERIFICATION.md's Truth 2 ("A blocked or waiting-for-input agent is visually distinguishable at a glance from an active agent"), which today fails because `Character.bubbleType`/`bubbleText` are correctly computed data but nothing in `renderer.ts` ever reads them (confirmed: `grep -n "bubble\|Bubble" packages/pixel-office/src/engine/renderer.ts` matches only a header comment). Author the 3 icon assets this needs that have never existed: `bubble-permission.json`/`bubble-waiting.json` (referenced by `STATUS_MAP` since 05-02 but never created — 05-02-SUMMARY.md's own disclosed gap) and `bubble-handoff-task.json` (referenced by `handoff-choreography.ts`'s `bubbleType = "handoff-task"` since 05-04 but likewise never created — the handoff FSM's task-icon-appearing step has never had a real asset to draw).

Purpose: OFFICE-03's "at a glance" requirement and HANDOFF-01's "a task icon appearing" requirement are both currently data-correct-but-invisible per 05-VERIFICATION.md; this plan is the second of the two rendering fixes it recommends (alongside 05-06's sprite pixel data) needed before either can be demonstrated as actually true.
Output: `packages/pixel-office/src/sprites/bubbleSprites.ts` (new resolver), 3 new icon JSON assets, a bubble draw pass in `engine/renderer.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `packages/pixel-office/src/sprites/bubble-permission.json`, `bubble-waiting.json`, `bubble-handoff-task.json` — 3 new 11x13 `{palette, pixels}` icon assets, each a distinct silhouette from all 11 other `BubbleType` glyphs.
- `packages/pixel-office/src/sprites/bubbleSprites.ts` — `BUBBLE_SPRITES: Record<BubbleType, SpriteData>` (exhaustive, all 12 members) + `resolveBubbleSprite(bubbleType: BubbleType): SpriteData`.
- `packages/pixel-office/src/engine/renderer.ts` — new bubble/badge draw pass in `renderScene`, drawn above each character's own sprite.
- `packages/pixel-office/src/constants.ts` — `BUBBLE_ICON_GAP_PX` (vertical spacing between a character's sprite top edge and its bubble icon).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the 3 missing bubble icon assets + exhaustive resolver</name>
  <files>packages/pixel-office/src/sprites/bubble-permission.json, packages/pixel-office/src/sprites/bubble-waiting.json, packages/pixel-office/src/sprites/bubble-handoff-task.json, packages/pixel-office/src/sprites/bubbleSprites.ts, packages/pixel-office/src/sprites/bubbleSprites.test.ts, packages/pixel-office/tsconfig.json</files>
  <read_first>
    packages/pixel-office/src/sprites/bubble-blocked.json, packages/pixel-office/src/sprites/bubble-completed.json (the exact `{palette, pixels}` 11x13 shape and existing silhouette/colour choices — blocked is a red octagon+bar, completed is a teal diagonal checkmark — the 2 new icons this task authors must not repeat either silhouette),
    packages/pixel-office/src/types.ts (the 12-member `BubbleType` union — `permission | waiting | blocked | failed | completed | planning | researching | testing | reviewing | discussing | deploying | handoff-task`),
    packages/pixel-office/src/status/status-mapping.test.ts (the existing `NEW_SPRITE_FILES` shape-validation test pattern this task's own bubbleSprites.test.ts should mirror),
    .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md (Sprite & Icon Asset Inventory's asset format contract: exactly 11 columns x 13 rows, `palette: Record<string, hexColor>`, every glyph a distinct silhouette, colour never the only signal; Color section's reserved `#ffb703` amber for "permission-style")
  </read_first>
  <behavior>
    - Test 1: `BUBBLE_SPRITES` has a defined `SpriteData` entry for every one of the 12 `BubbleType` union members — iterate a literal array of all 12 string values and assert each resolves.
    - Test 2: every one of the 3 newly-authored JSON files parses as valid JSON with a `palette` object and a `pixels` array of exactly 13 rows x 11 columns (same shape assertion `status-mapping.test.ts`'s `NEW_SPRITE_FILES` loop already uses for the 9 existing ones).
    - Test 3: no two of the 12 resolved `SpriteData` grids are pixel-for-pixel identical after normalizing away colour (i.e. comparing only which cells are non-empty vs. empty, not their hex values) — the mechanical proxy for "distinct silhouette, not a recolour."
  </behavior>
  <action>
    Author packages/pixel-office/src/sprites/bubble-permission.json (waiting_for_ceo's icon): a small amber (`#ffb703`, UI-SPEC's own reserved colour for "permission-style") glyph reading as "needs a decision/approval" — e.g. a bold question-mark shape or three amber dots inside a rounded speech-bubble outline — distinct in outline from every existing icon (none of the 9 existing icons use a question-mark or dot-cluster shape).

    Author packages/pixel-office/src/sprites/bubble-waiting.json (waiting_for_agent's icon): a small icon reading as "waiting on someone else" — e.g. an hourglass or clock-face silhouette, in a blue tone (e.g. `#4361ee`) distinct from every other icon's colour AND shape. Do NOT reuse a checkmark shape for this one even though the fork's own original bubble-waiting concept was a checkmark — `bubble-completed.json` (05-02) already claimed the checkmark silhouette for `completed`, and OFFICE-03's kept prohibition requires every glyph to be a distinct silhouette, not a recolour of another glyph's shape.

    Author packages/pixel-office/src/sprites/bubble-handoff-task.json (the handoff FSM's task-icon, `handoff-choreography.ts`'s `bubbleType = "handoff-task"` — this glyph has never existed before, it is not one of the original 15 `AgentStatus` states): a small icon reading as "a task/document being carried" — e.g. a simple folder or document-with-corner-fold silhouette, in a neutral or violet tone (e.g. `#7209b7`) distinct from all 11 other glyphs.

    Create packages/pixel-office/src/sprites/bubbleSprites.ts: import all 12 JSON assets (the 9 from 05-02 plus these 3 new ones) as plain ES module JSON imports (`import bubbleBlocked from "./bubble-blocked.json";` etc. — mirrors 05-06's `resolveJsonModule` addition to this package's tsconfig.json; add that same compiler option here too if 05-06 has not already landed it, checking first so the two plans' independent additions don't conflict). Write a private `resolvePaletteSprite(json: { palette: Record<string, string>; pixels: string[][] }): SpriteData` helper that maps every pixel cell through the palette (`cell === "" ? "" : json.palette[cell] ?? ""`) into the flat hex/"" `SpriteData` shape `drawSpriteData` already consumes. Export `BUBBLE_SPRITES: Record<BubbleType, SpriteData>` with all 12 entries built via `resolvePaletteSprite`, and `resolveBubbleSprite(bubbleType: BubbleType): SpriteData` as a thin `BUBBLE_SPRITES[bubbleType]` accessor (no fallback branch — exhaustive `Record` type makes a missing key a compile-time impossibility, matching `status-mapping.ts`'s own established pattern).
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test -- bubbleSprites</automated>
    <fails_when>non-zero exit, or any of the 12 BubbleType members resolves to undefined, or the distinct-silhouette test finds two identical non-empty-cell patterns</fails_when>
  </verify>
  <acceptance_criteria>
    - All 3 new JSON files are exactly 13 rows x 11 columns, valid JSON, with a non-empty `palette`.
    - `bubble-waiting.json`'s non-empty-cell silhouette pattern differs from `bubble-completed.json`'s.
    - `BUBBLE_SPRITES` has exactly 12 keys, matching `BubbleType`'s 12 members with no extra/missing entries.
  </acceptance_criteria>
  <done>All 12 BubbleType values resolve to a real, distinct-silhouette SpriteData grid via resolveBubbleSprite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the bubble/badge overlay draw pass into engine/renderer.ts</name>
  <files>packages/pixel-office/src/engine/renderer.ts, packages/pixel-office/src/engine/renderer.test.ts, packages/pixel-office/src/constants.ts</files>
  <read_first>
    packages/pixel-office/src/engine/renderer.ts (the full current file — `drawSpriteData`, `renderScene`'s per-character `drawables` map producing `{zY, draw}`, the sitting-offset/z-sort-offset constants already imported from `constants.ts`),
    packages/pixel-office/src/types.ts (`Character.bubbleType: BubbleType | null`),
    packages/pixel-office/src/constants.ts (existing rendering constants block to extend)
  </read_first>
  <behavior>
    - Test 1: given a mock `CanvasRenderingContext2D` (a plain object recording every `fillRect(x,y,w,h)` call and the `fillStyle` in effect at each call, cast to `CanvasRenderingContext2D` for the call site) and a `Character` with `bubbleType: "blocked"`, calling `renderScene` results in at least one recorded `fillRect` call using one of `bubble-blocked.json`'s own palette hex values — proving the bubble is genuinely drawn, not merely computed.
    - Test 2: given a `Character` with `bubbleType: null`, calling `renderScene` results in ZERO `fillRect` calls using any bubble/badge palette colour (only the character's own base-sprite pixels, if any, are drawn) — proving the overlay is conditional, never drawn for a character with no active bubble.
    - Test 3: the bubble's drawn Y-coordinates are strictly less than (above, in canvas-Y-down terms) the character's own base-sprite drawn Y-coordinates for the same character in the same frame — proving the overlay renders above the head, not overlapping/behind the body.
  </behavior>
  <action>
    Add `export const BUBBLE_ICON_GAP_PX = 2;` to packages/pixel-office/src/constants.ts's "Rendering" section (the vertical gap in unzoomed pixels between a character's sprite top edge and the bottom edge of its bubble icon).

    In packages/pixel-office/src/engine/renderer.ts, import `resolveBubbleSprite` from `../sprites/bubbleSprites.js` and `BUBBLE_ICON_GAP_PX` from `../constants.js`. Inside `renderScene`'s existing `characters.map((ch) => {...})` closure (the one currently building each `{zY, draw}` entry from `getCharacterSprites`/`getCharacterSprite`/`drawSpriteData`), extend the `draw` closure so that, AFTER drawing the character's own base sprite via the existing `drawSpriteData(ctx, spriteData, drawX, drawY, zoom)` call, it also checks `if (ch.bubbleType)`: if set, resolve `const bubbleSprite = resolveBubbleSprite(ch.bubbleType);`, compute `const bubbleWidth = bubbleSprite[0]?.length ?? 0;` and `const bubbleHeight = bubbleSprite.length;`, then `const bubbleX = Math.round(drawX + (spriteWidth * zoom - bubbleWidth * zoom) / 2);` (horizontally centered over the base sprite) and `const bubbleY = Math.round(drawY - bubbleHeight * zoom - BUBBLE_ICON_GAP_PX * zoom);` (positioned above the sprite's own top edge, `drawY`), then call `drawSpriteData(ctx, bubbleSprite, bubbleX, bubbleY, zoom);`. This keeps the bubble attached to the SAME `zY`-sorted drawable as its character (no separate z-sort entry needed — it is always drawn immediately after its own character's base sprite, in the same closure, so it can never appear behind a different character that should be in front).
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test -- renderer</automated>
    <fails_when>non-zero exit, or the null-bubbleType test observes any fillRect call using a bubble/badge palette colour, or the bubble-position test finds the bubble's Y coordinate is not strictly above the character's base sprite Y coordinate</fails_when>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or any pre-existing test (status-mapping, index, handoff-choreography, dialogue-templates, spriteData, bubbleSprites) regresses</fails_when>
  </verify>
  <acceptance_criteria>
    - `renderer.ts`'s `renderScene` calls `resolveBubbleSprite` exactly once per character with a non-null `bubbleType`, never for a character with `bubbleType: null`.
    - `grep -c "bubble\|Bubble" packages/pixel-office/src/engine/renderer.ts` now returns a count greater than 1 (previously only the header comment matched).
    - The full `pixel-office` test suite (all files) passes.
  </acceptance_criteria>
  <done>engine/renderer.ts now draws every character's active bubble/badge icon above its base sprite, sourced from the real 12-member BUBBLE_SPRITES table — OFFICE-03's blocked/waiting distinction and HANDOFF-01's task-icon-appearing step are both now genuinely visible, not just data-correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Character.bubbleType (internal, closed enum) -> canvas draw call | `bubbleType` is only ever set by `status-mapping.ts`'s `STATUS_MAP` (a closed `AgentStatus`-keyed table) or `handoff-choreography.ts`'s literal `"handoff-task"` string — never derived from raw event-payload strings, so there is no injection surface into `resolveBubbleSprite`'s lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-24 | Tampering | packages/pixel-office/src/sprites/bubbleSprites.ts (BUBBLE_SPRITES lookup keyed by bubbleType) | low | accept | `BubbleType` is a closed TypeScript union, never an arbitrary string parsed from wire payload data — the type system already forecloses an out-of-set key from ever reaching `resolveBubbleSprite` |
| T-05-25 | Information Disclosure | packages/pixel-office/src/engine/renderer.ts (new draw pass reads Character.bubbleText indirectly via the same struct) | low | accept | This plan only draws `bubbleType`'s icon glyph, never `bubbleText` (rendering dialogue text to canvas remains explicitly out of scope per 05-UI-SPEC.md's Typography section) — no new text-rendering surface is added |

</threat_model>

<verification>
Run both `<automated>` commands in Task 2 (each of which subsumes Task 1's own `bubbleSprites` test run via the full-suite command). All must exit 0, and the full `pixel-office` suite must stay green.
</verification>

<success_criteria>
Every character with an active `bubbleType` (blocked, waiting_for_ceo, waiting_for_agent, failed, completed, planning, researching, testing, reviewing, discussing, deploying, or the in-progress handoff-task icon) now has that icon genuinely painted above its sprite on canvas, sourced from a real, distinct-silhouette asset for all 12 `BubbleType` values — including the 3 that never had a backing asset before this plan.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-07-SUMMARY.md` when done
</output>
