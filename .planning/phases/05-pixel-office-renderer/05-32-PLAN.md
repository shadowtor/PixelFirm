---
phase: 05-pixel-office-renderer
plan: 32
type: execute
gap_closure: true
gap_ids: [G-05-P3, G-05-P5]
wave: 2
depends_on: ["05-31"]
files_modified:
  - packages/pixel-office/src/engine/renderer.ts
  - packages/pixel-office/src/engine/renderer.test.ts
  - packages/pixel-office/src/sprites/bubble-waiting.json
  - packages/pixel-office/src/sprites/bubbleSprites.test.ts
  - scripts/verify-pixel-office-live.mjs
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
autonomous: true
requirements: [OFFICE-01, OFFICE-03]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-05-P3: any character resting (not walking) on its own layout seat is drawn seated: sunk by CHARACTER_SITTING_OFFSET_PX so its desk hides its lower body, whatever its status (idle, blocked, waiting_for_agent, waiting_for_ceo, failed, completed, typing); before this plan only TYPE sank"
    - "G-05-P3: only a character off its seat (walking, waiting at a handoff interaction tile, or on a standing spot) is drawn full-body standing; a resting seated agent shows at least 8 fewer visible body rows than the same agent standing in an open aisle"
    - "The glyph stays head-anchored to the seated frame (05-30 rule, computed from the drawn frame): exactly BUBBLE_ICON_GAP_PX above the first opaque row in every pose, and on the floor for every seat and standing spot"
    - "G-05-P5: the waiting_for_agent glyph is a true hourglass at 1x: full-width caps (3 rows each end), triangular bulbs whose ink widths go 9, 7, 5 down to a single 3-px-ink / 1-px-fill waist and back up 5, 7, 9, sand in the lower bulb; still 11x13, closed near-black outline, all G-05-2 contrast tests and the >= 20-cell shape separation from blocked and '?' pass"
    - "Live: TRUTH 4 measures the blocked glyph 0..2 px above its seated owner's head top, on the floor, below the front agent"
  artifacts:
    - packages/pixel-office/src/engine/renderer.ts
    - packages/pixel-office/src/sprites/bubble-waiting.json
    - scripts/verify-pixel-office-live.mjs
  key_links:
    - "renderScene layout: sittingOffset (not WALK && isOwnSeat) -> drawY -> firstOpaqueRow -> resolveBubbleY (glyph follows the seated head)"
    - "bubble-waiting.json -> BUBBLE_SPRITES.waiting -> STATUS_MAP WAITING_FOR_AGENT (bubble 'waiting', frozen)"
  prohibitions:
    - statement: "MUST NOT change STATUS_MAP or the pose FSM; only how a resting character on its own seat is drawn changes (05-25 prohibition kept)"
      status: resolved
      verification: test
    - statement: "MUST NOT swap a status's animation frames (D-01): an idle or frozen seated agent keeps its idle frame, TYPE keeps its typing frames; only the vertical offset depends on the seat"
      status: resolved
      verification: test
    - statement: "MUST NOT make the hourglass distinguishable by colour only (OFFICE-03 / D-03): the silhouette alone must identify it"
      status: resolved
      verification: test
---

<objective>
Close two UAT native-scale legibility gaps.

G-05-P3 (polish item 3). renderer.ts sinks a character into its desk only when it is TYPE on its own seat. So a blocked, waiting or idle agent stands full-body at its desk, which is the same pose as a handoff sender standing beside it, and "working at the desk" looks the same as "standing nearby". Fix: any character resting on its own seat is drawn seated whatever its status. Only off-seat characters stand. The pose FSM, STATUS_MAP, the animation frames and the frozen pose (D-03) are unchanged. This supersedes 05-25's TYPE-only truth at the user's request (UAT round 2). The glyph follows automatically, because 05-30 anchors it on the drawn frame's head.

G-05-P5 (polish item 5). bubble-waiting.json has a 5-px, 3-row neck and flat-sided bulbs, so at 1x it reads as a spool or a capital I and relies on its blue colour. Fix: redraw it as a true hourglass (grid below) and add a 1x silhouette test.

Output: seated-at-desk rendering and tests, the hourglass asset and silhouette test, live TRUTH 4 on the seated head, and UI-SPEC rows.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-UAT.md
@packages/pixel-office/src/engine/renderer.ts
@packages/pixel-office/src/sprites/bubble-waiting.json
</context>

<interface_context>
Orientation only.
- renderer.ts, renderScene: `sittingOffset = ch.state === CharacterState.TYPE && isOwnSeat(ch) ? CHARACTER_SITTING_OFFSET_PX (6) : 0`, followed by `drawY = round(offsetY + (ch.y + sittingOffset) * zoom - spriteHeight * zoom)`. `isOwnSeat(ch)` (officeLayout.ts) is true when the tile equals the seat and the seat is one of the 16 SEATS; standing spots are never seats. getCharacterSprite: IDLE draws `walk[dir][1]`, TYPE draws `typing[dir][frame % 2]`, WALK draws `walk[dir][frame % 4]`.
- Geometry at zoom 1: seat row 4 has foot y 72, standing sprite y 40..72, seated 46..78. The IDLE frame's ink rows are 3..30. The desk (46x30) under a row-4 seat spans y 66..95, its top rows fully opaque, and it is drawn after the seated agent (desk zY 96 > agent zY 80.5). Row 6 is open aisle with no furniture on rows 6-7.
- renderer.test.ts: the `ownerDrawY(ch)` helper (around line 403) restates the sitting rule. The CR-02 composite ("never paints a later-row agent's blocked glyph...") pins agent-9 IDLE at (1,8) with glyph y 94..106. describe "seated only at the own desk (05-25, G-05-1e)" (around line 866) has the helpers `topY`, `typer`, `isTypingFrame`. describe "glyph sits on its owner's head (05-30)" computes head and glyph from real renders. The "furnished office" tests use a mock ctx that replays 1x1 fillRects at zoom 1 (no OffscreenCanvas in node).
- bubbleSprites.test.ts: `edgeAndInterior` (edge = opaque cell with a transparent or out-of-grid 4-neighbour; every edge cell must be one near-black colour, and every interior cell must be >= 3:1 against it, so a black cell fully surrounded by opaque cells fails). FROZEN = [blocked, permission, waiting]. Pairwise >= 20 differing mask cells. The MetroCity floor mean-contrast test.
- Harness TRUTH 4: `headTop = spriteTopY(targetDesk.row) + headRow`, where headRow is the first opaque row of `down[1]`, with the comment "The blocked owner stands (IDLE = down[1])". The front agent types, so `firstRowSpriteBottom` already adds CHARACTER_SITTING_OFFSET_PX.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Draw every character resting on its own seat as seated (G-05-P3)</name>
  <reversibility rating="reversible">One condition in the renderer.</reversibility>
  <files>packages/pixel-office/src/engine/renderer.ts, packages/pixel-office/src/engine/renderer.test.ts</files>
  <read_first>
    - packages/pixel-office/src/engine/renderer.ts (renderScene, lines 267-320)
    - packages/pixel-office/src/engine/renderer.test.ts (lines 199-370 glyph/CR-02 tests, 400-410 ownerDrawY, 725-800 furnished-office helpers, 866-916 seated describe)
  </read_first>
  <behavior>
    - "seated whatever the status": for IDLE, BLOCKED, WAITING_FOR_AGENT and WAITING_FOR_CEO (applied through `upsertCharacterFromAgent` so the real STATUS_MAP pose, frozen flag and glyph are used) and for TYPE, an agent on SEATS[0] is drawn exactly CHARACTER_SITTING_OFFSET_PX lower than the same character whose seatCol points elsewhere. The drawn frame is unchanged, so an idle or frozen agent keeps `walk[dir][1]` and TYPE keeps a typing frame (D-01).
    - "off-seat stands": a WALK character on its own seat tile, a character on a STANDING_SPOTS tile, and a character on a non-home tile are not lowered.
    - "visible body rows differ": with FURNITURE and glyphs and dialogue off, a resting IDLE agent on SEATS[0] shows at least 8 fewer visible body rows than the same IDLE agent standing on the open aisle tile (2, 6). Visible rows are the rows holding at least one cell where the character's colour survives in the composited frame; isolate them with a control render without that character.
    - "glyph follows the seated head": the 05-30 gap and on-floor tests pass unchanged. They compute the head from real renders, and "IDLE on a seat" is now seated.
    - CR-02 composite: agent-9 (IDLE, now seated at (1,8)) has its blocked glyph at y 100..112 (was 94..106), still strictly below agent-1's box and ending 1 px above agent-9's head. Derive the numbers from the sprite data in the test comment.
  </behavior>
  <action>
RED, then GREEN (G-05-P3 missing items 1-3):
- renderer.ts: change the sitting condition to "not walking and on its own seat": `ch.state !== CharacterState.WALK && isOwnSeat(ch)`. Rewrite the comment above it. A character resting on its own layout seat is drawn seated whatever its status (G-05-P3, 05-32, superseding 05-25's TYPE-only rule), so "at the desk" and "standing nearby" read differently. Only the offset changes: the frames still come from the pose (D-01), and STATUS_MAP and the FSM are untouched.
- renderer.test.ts: update `ownerDrawY` to the same rule. Rename the 05-25 describe to "seated whenever resting on the own seat (05-25, G-05-P3)", keep its TYPE cases, and add the behaviour cases above. Update the CR-02 composite's numeric expectations and comment. If any other existing test fails, update its expectation to the value derived from the sprite data and note why. Never delete an assertion to make the suite pass.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - The new seated-status and visible-rows tests fail on the old condition and pass on the new one. The full pixel-office suite is green, including 05-28's layout guard and 05-30's head-gap and on-floor tests.
    - `grep -n "CharacterState.WALK && isOwnSeat" packages/pixel-office/src/engine/renderer.ts` matches.
  </acceptance_criteria>
  <done>At a glance, agents at their desks sit behind them and only visitors and walkers stand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redraw the waiting_for_agent glyph as a true hourglass with a 1x silhouette test (G-05-P5)</name>
  <reversibility rating="reversible">One sprite asset and one test.</reversibility>
  <files>packages/pixel-office/src/sprites/bubble-waiting.json, packages/pixel-office/src/sprites/bubbleSprites.test.ts</files>
  <read_first>
    - packages/pixel-office/src/sprites/bubble-waiting.json
    - packages/pixel-office/src/sprites/bubbleSprites.test.ts (whole file)
  </read_first>
  <behavior>
    - "1x hourglass silhouette" (new test on `resolveBubbleSprite("waiting")`):
      - the first 3 and the last 3 rows each span all 11 columns (heavy caps);
      - exactly one row between the caps has the minimum ink width; that width is 3, and its non-outline fill is exactly 1 cell (single-pixel waist);
      - ink widths strictly decrease from the first row below the top cap down to the waist, and strictly increase from the waist to the last row above the bottom cap (triangular bulbs);
      - the darker interior fill colour occupies more cells below the waist than above it (sand in the lower bulb).
    - The existing tests stay green: 11x13 JSON shape, 12 distinct silhouettes, not the checkmark, closed near-black outline, interior >= 3:1, outline >= 3:1 against the floor, MetroCity floor legibility, >= 20 differing cells against blocked and permission.
  </behavior>
  <action>
RED (the silhouette test fails on today's spool), then GREEN (G-05-P5 missing items 1-2):
- bubble-waiting.json: keep the palette exactly as is ("1" #000000 outline, "2" #4361ee cap and sand, "3" #a9bcff glass), so the live harness colour sets do not change. Replace `pixels` with this 11x13 grid. In the legend, `X` is "1", `B` and `s` are "2", `g` is "3", and `.` is "" (transparent). Rows 0-12 read left to right:
  row 0 `XXXXXXXXXXX`, row 1 `XBBBBBBBBBX`, row 2 `XBBBBBBBBBX`, row 3 `.XgggggggX.`, row 4 `..XgggggX..`, row 5 `...XgsgX...`, row 6 `....XsX....`, row 7 `...XgsgX...`, row 8 `..XgsssgX..`, row 9 `.XsssssssX.`, row 10 `XBBBBBBBBBX`, row 11 `XBBBBBBBBBX`, row 12 `XXXXXXXXXXX`.
  Ink widths are 11,11,11,9,7,5,3,5,7,9,11,11,11. The cap-to-bulb joins have no internal black line (an enclosed black cell would fail the interior-contrast rule). Every black cell touches transparency or the grid edge. The mask differs from blocked in 63 cells and from permission in 58. The lowest ink row is still row 12, so head anchoring (05-30) does not move.
- bubbleSprites.test.ts: add the "1x hourglass silhouette" describe above, citing G-05-P5.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test -- bubbleSprites</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - The silhouette test fails on the old asset and passes on the new one. Every existing bubbleSprites test passes.
    - `pnpm --filter pixel-office test` stays fully green (the renderer's glyph-placement tests read the asset too).
  </acceptance_criteria>
  <done>The waiting-for-agent glyph reads as an hourglass by shape alone at native scale.</done>
</task>

<task type="auto">
  <name>Task 3: Live TRUTH 4 on the seated head, plus UI-SPEC rows</name>
  <files>scripts/verify-pixel-office-live.mjs, .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>
  <read_first>
    - scripts/verify-pixel-office-live.mjs (TRUTH 4 block, about lines 955-1037; spriteTopY and CHARACTER_SITTING_OFFSET_PX reads, about lines 200-280)
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md (Sprite & Icon Asset Inventory, `bubble-waiting.json` row; Spacing "Office layout" paragraph)
  </read_first>
  <precondition>Docker is running and apps/api/.env holds the four keys the harness checks.</precondition>
  <action>
- Harness TRUTH 4: the blocked owner now rests seated on its own seat, so `headTop = spriteTopY(targetDesk.row) + CHARACTER_SITTING_OFFSET_PX + headRow`. Update the comment to say the owner sits at its own seat (IDLE frame `down[1]`, lowered by the sitting offset, 05-32 / G-05-P3). Keep every assertion: gap 0..2, on the floor (>= TILE_SIZE), strictly below the front agent. Run the harness with PIXEL_OFFICE_SHOTS set to a scratch directory. Read `states.png`, `handoff.png` and `cohort.png`, and record in the SUMMARY that resting agents sit behind their desks, the sender and walkers stand, and the receiver's hourglass reads as an hourglass.
- UI-SPEC (cite 05-32):
  - Asset inventory `bubble-waiting.json` row: "Blue hourglass, 3-row caps, triangular bulbs to a 1 px-fill (3 px ink) waist, sand in the lower bulb, closed 1 px black outline; redrawn 05-32 (G-05-P5)". Add to the asset format contract that the hourglass has a 1x silhouette test.
  - Spacing "Office layout" paragraph: a character resting on its own seat is drawn seated (the desk hides its lower body) whatever its status. Only off-seat characters (walking, at a handoff interaction tile, on a standing spot) stand (G-05-P3).
  </action>
  <verify>
    <automated>node --check scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit (a SyntaxError is printed)</fails_when>
    <automated>node scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit, any "TRUTH FAILED" line, or no final "LIVE PROOF: PASS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `LIVE PROOF: PASS`. TRUTH 4 prints a head gap of 0..2 against the seated head top.
    - `grep -n "05-32" .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` finds both notes.
  </acceptance_criteria>
  <done>The live canvas shows stuck agents seated at their desks with their glyph on their head, and the design contract states both rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | render geometry and one static asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-32-01 | Repudiation (misleading display) | renderer sitting rule | low | mitigate | the frozen pose, the glyph and STATUS_MAP are unchanged, so a seated stuck agent still never animates as working (D-03); tests cover every frozen status seated |
| T-05-32-02 | Repudiation (state signalled by colour only) | bubble-waiting.json | medium | mitigate | the 1x silhouette test plus the kept >= 20-cell shape separation and closed-outline tests (OFFICE-03) |
</threat_model>

<verification>
- `pnpm --filter pixel-office test` green.
- `node scripts/verify-pixel-office-live.mjs` ends `LIVE PROOF: PASS`.
</verification>

<success_criteria>
G-05-P3 and G-05-P5 hold. At native scale a viewer can tell "at the desk" from "standing nearby" by pose, and can tell waiting_for_agent from the other stuck states by its hourglass shape, without relying on colour.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-32-SUMMARY.md` when done.
</output>
