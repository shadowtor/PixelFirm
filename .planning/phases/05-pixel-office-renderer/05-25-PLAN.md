---
phase: 05-pixel-office-renderer
plan: 25
type: execute
gap_closure: true
gap_ids: [G-05-1e]
wave: 15
depends_on: ["05-24"]
files_modified:
  - packages/pixel-office/src/index.ts
  - packages/pixel-office/src/index.test.ts
  - packages/pixel-office/src/engine/renderer.test.ts
  - packages/pixel-office/src/handoff/handoff-choreography.test.ts
  - packages/pixel-office/src/engine/characters.ts
  - packages/pixel-office/src/engine/renderer.ts
autonomous: true
requirements: [OFFICE-01]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "G-05-1e: agents take the layout's desk seats in order, lowest free seat first (reclaimed after despawn), so idle agents sit at workstations in the pods instead of a packed coordinate line"
    - "Agents 17-20 stand on the layout's 4 standing spots; no two concurrently present characters are placed on one tile for up to 20 agents; a 21st+ shares the last standing spot (documented ceiling)"
    - "CHARACTER_SITTING_OFFSET_PX applies only to a TYPE character standing on its own layout seat; elsewhere the typing frames draw at standing height (the status animation is kept, D-01); a walk that ends on the character's own seat or standing spot leaves it facing down"
    - "Every seat and standing spot reaches every other by findPath over the furniture-blocked tiles"
    - "All pre-existing pixel-office tests pass; tests that hard-coded the old 18-per-row desk grid (rows 3/6/9) are rebased onto the layout seats while keeping their intent"
  artifacts:
    - packages/pixel-office/src/index.ts
    - packages/pixel-office/src/engine/characters.ts
    - packages/pixel-office/src/engine/renderer.ts
  key_links:
    - "upsertCharacterFromAgent -> nextDeskPosition() -> SEATS then STANDING_SPOTS (officeLayout.ts) -> createCharacter(seat)"
    - "renderScene sittingOffset -> isOwnSeat(ch) (officeLayout.ts)"
    - "updateCharacter WALK path complete on (seatCol, seatRow) -> dir DOWN"
  prohibitions:
    - statement: "MUST NOT swap a status's animation for another (D-01): TYPE keeps its typing frames off-seat; only the sitting offset depends on the seat"
      status: resolved
      verification: test
    - statement: "MUST NOT change STATUS_MAP"
      status: resolved
      verification: test
---

<objective>
Seat-model slice of UAT gap G-05-1e ("characters feel scattered rather than occupying workstations"). Root cause (.planning/debug/office-bare-floor.md): seats are an unfurnished 18-per-row coordinate grid chosen only for glyph headroom, and TYPE sinks 6 px into a chair that is never drawn. 05-24 added the furnished layout and declared its seats; this plan makes agents actually use them, handles overflow without stacking, and applies the sitting offset only at an agent's own desk.

D-01 is honoured explicitly: a coding agent away from its desk still animates typing — only its vertical offset changes. The live harness still reads the removed desk constants until 05-26 rebases it — do not run `scripts/verify-pixel-office-live.mjs` in this plan.

Output: layout-driven seating with overflow, rebased seat-geometry tests, seated-offset rule, face-down-at-desk.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-UAT.md
@.planning/debug/office-bare-floor.md
@.planning/phases/05-pixel-office-renderer/05-24-SUMMARY.md
@packages/pixel-office/src/index.ts
@packages/pixel-office/src/layout/officeLayout.ts
</context>

<interface_context>
Orientation only — read the files.
- index.ts (after 05-24): `DESK_ROW_START = 3`, `DESK_ROW_PITCH = 3`, `interiorCols`, `deskForSlot`, `DESK_CAPACITY`, `nextDeskPosition()` (lowest free slot by `seatCol,seatRow`), `createCharacter(agentId, col, row, hue)`; tile map is `OFFICE_TILE_MAP`.
- officeLayout.ts (05-24): `SEATS` (16, rows 4/8), `STANDING_SPOTS` [(17,4),(18,4),(17,8),(18,8)], `FURNITURE_BLOCKED_TILES`, `OFFICE_TILE_MAP`, `isOwnSeat(ch)`.
- characters.ts: WALK path-complete branch snaps to the tile centre and sets `state = restPose`; `getCharacterSprite` returns typing frames for TYPE, walk frame 1 for IDLE.
- renderer.ts: `sittingOffset = ch.state === CharacterState.TYPE ? CHARACTER_SITTING_OFFSET_PX : 0` in `renderScene`.
- Tests that hard-code the old grid: index.test.ts describe "desk layout headroom (CR-02)" (rows 3/6, 18 per row, 54 capacity, churn); renderer.test.ts describe "renderScene over the real desk layout — CR-02 two-character composite" (`seatAgents(19)`, `firstBox.top 24`, `laterBox.top 72`, glyph 57..70) and the 05-13 dialogue test "stacks a row-6 owner's box directly above its own glyph slot". handoff-choreography.test.ts `seatAll()` seats a, three fillers, b, c in order — with seats two columns apart b is at (9,4) and a's walk is 8 tiles (2.67 s), inside `run(3)`.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Seats come from the layout (lowest free first, standing overflow), end to end through upsertCharacterFromAgent</name>
  <reversibility rating="reversible">Seat assignment is derived from data; the pre-05-25 formula is recoverable from history and no persisted state holds seats.</reversibility>
  <files>packages/pixel-office/src/index.ts, packages/pixel-office/src/index.test.ts, packages/pixel-office/src/engine/renderer.test.ts, packages/pixel-office/src/handoff/handoff-choreography.test.ts</files>
  <read_first>
    - packages/pixel-office/src/index.ts (desk constants, `deskForSlot`, `DESK_CAPACITY`, `nextDeskPosition`, `upsertCharacterFromAgent`)
    - packages/pixel-office/src/index.test.ts (describe "desk layout headroom (CR-02)" and the findPath sanity describe)
    - packages/pixel-office/src/engine/renderer.test.ts (describe "renderScene over the real desk layout — CR-02 two-character composite", helpers `seatReal`, `ownerDrawY`, and the 05-13 dialogue tests that use them)
    - packages/pixel-office/src/handoff/handoff-choreography.test.ts (`seatAll`, `run`, and the `run(n)` budgets in the 05-17..05-20 describes)
  </read_first>
  <behavior>
    index.test.ts replaces "desk layout headroom (CR-02)" with "office layout seats (G-05-1e)":
    - the 1st, 2nd and 9th agents are seated at SEATS[0] (1,4), SEATS[1] (3,4) and SEATS[8] (1,8);
    - a despawned agent's seat goes to the next new agent (lowest free first), across heavy churn (keep the WR-03 churn test's intent);
    - the 17th agent stands at STANDING_SPOTS[0]; 20 present agents occupy 20 distinct tiles; the 21st shares the last standing spot (the documented ceiling);
    - every seat and standing spot reaches every other via `findPath(..., OFFICE_TILE_MAP, FURNITURE_BLOCKED_TILES)`.
  </behavior>
  <action>
- `index.ts`: delete `DESK_ROW_START`, `DESK_ROW_PITCH`, `interiorCols`, `deskForSlot` and `DESK_CAPACITY` with their comments. `nextDeskPosition()` returns the first entry of `SEATS` then `STANDING_SPOTS` whose `col,row` no present character holds as its seat, else the last standing spot. Replace the old ponytail with the new ceiling: 16 seats + 4 standing spots; a 21st concurrently present agent shares the last standing spot; upgrade path is a larger layout or a second floor (OFFICE-04).
- Rebase the tests that hard-coded the old grid, keeping each test's intent:
  - index.test.ts: the describe above.
  - renderer.test.ts CR-02 composite: seat 9 agents so agent-1 is at (1,4) and agent-9 at (1,8) (same column, consecutive seat rows); block agent-9; keep "glyph rects disjoint from agent-1's sprite box", "glyph above its own head" and horizontal containment; replace the hard-coded numbers with values derived from the two characters (agent-1 box 40..72, agent-9 box top 104 at zoom 1) — assert the glyph lies strictly between agent-1's box bottom and agent-9's box top.
  - renderer.test.ts 05-13 dialogue tests that need a second-row owner use a row-8 seat instead of row 6, with the same assertions.
  - handoff-choreography.test.ts: only if a walk no longer finishes inside its `run(n)` budget because seats are now two columns apart, raise that budget (one-line reason comment). Change nothing else in that file.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - "office layout seats (G-05-1e)" passes; the full pixel-office suite is green.
    - `grep -c "DESK_ROW_START\|DESK_ROW_PITCH" packages/pixel-office/src/index.ts` prints 0.
  </acceptance_criteria>
  <done>New agents land at workstation seats in the pods, overflow agents stand in the right strip on the seat rows, and no hard-coded desk grid remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seated offset only on the own seat, face down at home</name>
  <files>packages/pixel-office/src/engine/characters.ts, packages/pixel-office/src/engine/renderer.ts, packages/pixel-office/src/engine/renderer.test.ts</files>
  <read_first>
    - packages/pixel-office/src/engine/characters.ts (WALK path-complete branch, `getCharacterSprite`)
    - packages/pixel-office/src/engine/renderer.ts (the `sittingOffset` line in `renderScene`)
    - packages/pixel-office/src/engine/renderer.test.ts (mockCtx)
  </read_first>
  <behavior>
    renderer.test.ts:
    - "a TYPE agent on its own seat is drawn seated": drawY includes CHARACTER_SITTING_OFFSET_PX and the sprite is a typing frame.
    - "a TYPE agent off its seat keeps typing but is not lowered": for a TYPE character standing on another tile (and one on a standing spot), drawY has no sitting offset and the sprite is still a typing frame (D-01).
    - "a walk that ends at home faces down": a character walked home from the side via `walkCharacterTo` + `updateCharacter` steps ends with `dir === Direction.DOWN`.
  </behavior>
  <action>
- `renderer.ts`: `sittingOffset` becomes `ch.state === CharacterState.TYPE && isOwnSeat(ch) ? CHARACTER_SITTING_OFFSET_PX : 0`. `getCharacterSprite` keeps returning typing frames for TYPE everywhere (D-01 — never swap a status's animation).
- `characters.ts`: in the WALK path-complete branch, after snapping, set `ch.dir = Direction.DOWN` when `ch.tileCol === ch.seatCol && ch.tileRow === ch.seatRow` (every seat faces the viewer; a standing spot is an overflow agent's home and also faces down). One comment line citing 05-25.
- Nothing else changes: STATUS_MAP, glyph placement and dialogue are untouched.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
    <automated>pnpm --filter web typecheck</automated>
    <fails_when>non-zero exit (tsc prints an "error TS" line)</fails_when>
  </verify>
  <acceptance_criteria>
    - The three new tests pass; the full pixel-office suite is green; web typecheck passes.
    - `grep -n "isOwnSeat" packages/pixel-office/src/engine/renderer.ts` shows the sitting-offset condition.
  </acceptance_criteria>
  <done>Agents sit only at their own desks, face the viewer at home, and keep their status animation everywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | seats are derived from committed layout data; characters still come only from relayed projections |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-25-01 | Repudiation (misleading display) | a status animation hidden by the seated rule | medium | mitigate | typing frames kept for TYPE everywhere (D-01); only the sitting offset depends on the seat; tested |
| T-05-25-02 | Denial of service | more than 20 agents | low | accept | documented ceiling (shared last standing spot), same class as the previous 55th-desk ceiling |
</threat_model>

<verification>
- `pnpm --filter pixel-office test` green (seat model, rebased CR-02 composite, seated rule).
- `pnpm --filter web typecheck` green.
</verification>

<success_criteria>
G-05-1e's seating half holds: idle agents snap to desk seats in pods with clear lanes, overflow agents never stack, and seated agents look seated only at their own desk.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-25-SUMMARY.md` when done.
</output>
