---
phase: 05-pixel-office-renderer
plan: 25
type: execute
gap_closure: true
gap_ids: [G-05-1e, G-05-2]
wave: 15
depends_on: ["05-23", "05-24"]
files_modified:
  - scripts/verify-pixel-office-live.mjs
  - packages/pixel-office/src/sprites/bubbleSprites.test.ts
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
  - .planning/ROADMAP.md
  - apps/web/src/App.tsx
  - apps/web/src/App.test.tsx
  - references/ASSET-LICENSES.md
autonomous: true
requirements: [OFFICE-01, OFFICE-02, OFFICE-03]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The live harness passes against the furnished office: it reads seats, standing spots and furniture from office-layout.json and office colours from office-metrocity.json at run time (nothing restated), counts agent pixels as pixels in no office colour, and keeps TRUTHS 0-5 with their original intent"
    - "TRUTH 4 is proven on a real pod pair: the agent blocked at seat (1,8) has its glyph strictly between the bottom of the agent seated in front at (1,4) and its own sprite top, inside its own column"
    - "New TRUTH 6 (G-05-1e, live): the empty office paints no FALLBACK_FLOOR_COLOR pixel (the bare grey floor is gone), and every desk's sprite rectangle carries at least half of that desk sprite's opaque cells in desk colours"
    - "New TRUTH 7 (performance): with the whole live cohort on the floor, the page sustains at least 30 animation frames per second (the per-(sprite, zoom) cache from 05-24 at work)"
    - "The frozen-state glyphs stay legible on the real floor: for each frozen glyph and each MetroCity floor tile's mean colour, its outline or its main fill reaches >= 3:1 contrast (G-05-2 on the textured floor)"
    - "The in-app attribution credits both MetroCity packs ('character and office sprites: MetroCity packs by JIK-A-4') without asserting a licence, App.test pins the sentence, and ASSET-LICENSES' attribution section matches it (OFFICE-02)"
    - "05-UI-SPEC and ROADMAP describe the furnished office: floor/wall/furniture colour roles, the pod layout, 16 seats + 8 standing spots, the office sprite inventory; ROADMAP Phase 5 gains success criterion 5 for the furnished floor"
  artifacts:
    - scripts/verify-pixel-office-live.mjs
    - apps/web/src/App.tsx
    - .planning/ROADMAP.md
  key_links:
    - "harness -> packages/pixel-office/src/layout/office-layout.json (seats, standing, furniture) + src/sprites/office-metrocity.json (office palette) -> seat claims, agent-pixel counts, TRUTH 4/6"
    - "App.tsx footer sentence <-> App.test.tsx ATTRIBUTION <-> ASSET-LICENSES 'Attribution in the running app'"
  prohibitions:
    - statement: "MUST NOT restate layout coordinates or office colours in the harness — read them from the engine's own data files (05-12 rule: a layout change moves the assertions with it)"
      status: resolved
      verification: test
    - statement: "MUST NOT assert a licence over the shipped character bytes in the footer (the hair layer is still credit-only, ASSET-LICENSES §1)"
      status: resolved
      verification: test
---

<objective>
Finish UAT gap G-05-1e's proof and paperwork, and carry G-05-2 onto the real floor. After 05-24 the live harness no longer runs (it reads the removed `DESK_ROW_START`, and its "any non-grey pixel is a sprite" rule is false on a textured, furnished floor). This plan rebases it on the layout and office-sprite data files, adds live truths for the furnished office and its frame rate, and syncs the design contract, roadmap and attribution with what now renders.

Decisions: D-05 (the audited MetroCity assets are the only new third-party art; the footer and ASSET-LICENSES must say so consistently), D-03 (frozen glyph contrast on the actual background). The public credits/about-the-stream page is a deferred user item and is NOT built here — only the existing footer sentence is made accurate.

Output: rebased harness with TRUTH 6 and 7 and screenshots, floor-texture contrast test, UI-SPEC/ROADMAP/footer/ASSET-LICENSES updates.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-UAT.md
@.planning/phases/05-pixel-office-renderer/05-21-SUMMARY.md
@.planning/phases/05-pixel-office-renderer/05-22-SUMMARY.md
@.planning/phases/05-pixel-office-renderer/05-23-SUMMARY.md
@.planning/phases/05-pixel-office-renderer/05-24-SUMMARY.md
@scripts/verify-pixel-office-live.mjs
</context>

<interface_context>
Orientation only.
- Harness today: `deskPosition(slot)` restates the removed desk formula from `DESK_ROW_START`/`DESK_ROW_PITCH` regex reads; `scanCanvas` counts `sprite` = any pixel that is not exactly FALLBACK_FLOOR_COLOR or WALL_COLOR; `distinctiveBubbleColors(asset, charColors)`; TRUTH 4 seats `INTERIOR_COLS + 1` agents to straddle two desk rows; `spriteTopY(row)` = row * 16 + 8 - SPRITE_HEIGHT (un-offset anchor); 05-21 added TRUTH 0 (display scale) and `PIXEL_OFFICE_SHOTS` screenshots.
- Seat order after 05-24: SENDER slot 0 = (1,4), BLOCKED slot 1 = (3,4), RECEIVER slot 2 = (5,4), then the cohort; slot 8 = (1,8), directly behind-and-below SENDER's pod seat. The sender's walk (1,4) -> (5,4) is 4 tiles (1.33 s), inside the existing 2500 ms wait.
- Office colours at planning time include #926429/#7d4e13 (floor), browns, greens, greys, #ffffff and #f5f5f5; none is #000000, #d62828, #121212 or #f0f0f0 (05-22 tests the dialogue colours).
- 05-24 renders the handoff walk unchanged (the sender still ends on the receiver's seat tile until 05-26), so TRUTH 3/5 keep their current meaning in this plan.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: Rebase the live harness on the layout and office-sprite data; add TRUTH 6 (furnished) and TRUTH 7 (frame rate); floor-texture glyph contrast</name>
  <files>scripts/verify-pixel-office-live.mjs, packages/pixel-office/src/sprites/bubbleSprites.test.ts</files>
  <read_first>
    - scripts/verify-pixel-office-live.mjs (whole file)
    - packages/pixel-office/src/layout/office-layout.json
    - packages/pixel-office/src/layout/officeLayout.ts (how furniture x/y/zY and blocked tiles are derived)
    - packages/pixel-office/src/sprites/bubbleSprites.test.ts (the 05-23 "G-05-2 frozen-state glyph contrast" describe)
  </read_first>
  <precondition>Docker is running and apps/api/.env holds the four keys the harness checks.</precondition>
  <action>
Harness rebase (keep every existing truth's intent and message style; per 05-12's rule read engine data, never restate it):
- Load `packages/pixel-office/src/layout/office-layout.json` and `packages/pixel-office/src/sprites/office-metrocity.json`. `OFFICE_COLORS` = every colour in every office sprite (floorTiles included) plus WALL_COLOR and FALLBACK_FLOOR_COLOR. Replace the floor/wall test in `scanCanvas` with "pixel colour not in OFFICE_COLORS" for the `sprite` (agent pixel) count; pass OFFICE_COLORS into `page.evaluate` as an array.
- `distinctiveBubbleColors(asset, excluded)` excludes character colours AND office colours.
- Replace `deskPosition` with the layout's seats then standing spots (same creation-order `claimDesk`); delete the DESK_ROW_START/DESK_ROW_PITCH/INTERIOR_COLS reads.
- TRUTH 4: seat the cohort until slot 8 is taken (compute the count from the layout: the first pod-row-2 seat whose column matches an occupied pod-row-1 seat); block that agent; assert its glyph pixels (inside its own column band) lie strictly below the front agent's sprite box bottom (un-offset anchor plus CHARACTER_SITTING_OFFSET_PX, read from constants.ts, since the front agent types at its desk) and strictly above its own sprite top. Keep the "no glyph px in the band = vacuous" guard.
- TRUTH 6 (new, G-05-1e), on the empty office right after TRUTH 0: (a) zero pixels of FALLBACK_FLOOR_COLOR anywhere on the canvas; (b) for every furniture entry with sprite "desk", the scaled rectangle from officeLayout's placement rule contains at least half of that sprite's opaque cell count x scale^2 in desk-sprite colours. Log `TRUTH 6 PASS — furnished office: N desks, 0 bare-floor px`.
- TRUTH 7 (new): after the TRUTH 4 cohort is seated (the heaviest scene), count `requestAnimationFrame` callbacks over 1000 ms in the page; assert >= 30. Log the measured fps.
- Screenshots (`PIXEL_OFFICE_SHOTS`): add `empty.png` (after TRUTH 6) and `cohort.png` (after TRUTH 4).
- Update the header comment's truth list with (6) and (7).

Run it with `PIXEL_OFFICE_SHOTS` set to a scratchpad directory. Read `empty.png`, `states.png`, `handoff.png` and `cohort.png` and check: no bare grey floor; seated agents' heads and shoulders show above their desks; stuck agents stand behind their desks with glyphs on the floor, not on the wall; lanes are clear. Record the observations (and anything that still looks wrong, e.g. the handoff banner, which 05-27 replaces) in the SUMMARY.

bubbleSprites.test.ts: in the 05-23 describe add "legible on the MetroCity floor": for each frozen glyph and each of the four `floorTiles` in office-metrocity.json, with B = that tile's mean colour (average of its 256 cells' RGB), `max(contrast(outline, B), contrast(mainFill, B)) >= 3`, where mainFill is the glyph's most frequent non-outline colour. If it fails, stop and report (the fix is a glyph plate or a floor choice, both outside this plan's files).
  </action>
  <verify>
    <automated>node --check scripts/verify-pixel-office-live.mjs</automated>
    <automated>pnpm --filter pixel-office test -- bubbleSprites</automated>
    <automated>node scripts/verify-pixel-office-live.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-pixel-office-live.mjs` prints PASS for TRUTH 0-7 and ends `LIVE PROOF: PASS`.
    - `grep -c "DESK_ROW_START\|INTERIOR_COLS" scripts/verify-pixel-office-live.mjs` prints 0; `grep -n "office-layout.json" scripts/verify-pixel-office-live.mjs` finds the data read.
    - The SUMMARY lists the four screenshots' observations.
  </acceptance_criteria>
  <done>The live harness proves the furnished office, its frame rate and every earlier truth on the real canvas, reading all geometry from the engine's data.</done>
</task>

<task type="auto">
  <name>Task 2: UI-SPEC, ROADMAP success criterion, footer credit and ASSET-LICENSES attribution</name>
  <files>.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md, .planning/ROADMAP.md, apps/web/src/App.tsx, apps/web/src/App.test.tsx, references/ASSET-LICENSES.md</files>
  <read_first>
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md (## Color, ## Spacing Scale exceptions, ## Sprite & Icon Asset Inventory, ## UI Considerations table, ## Registry Safety)
    - .planning/ROADMAP.md (### Phase 5 success criteria only)
    - apps/web/src/App.tsx (footer and its comment) and apps/web/src/App.test.tsx (ATTRIBUTION)
    - references/ASSET-LICENSES.md (§1, §1a and "Attribution in the running app")
  </read_first>
  <action>
- 05-UI-SPEC.md (cite 05-24/05-25, G-05-1e):
  - Color table: Dominant = MetroCity Interior wood-plank floor tiles (`office-metrocity.json` `floorTiles`); FALLBACK_FLOOR_COLOR #808080 is kept only as a constant for tests and the harness's bare-floor check. Secondary = top wall MetroCity wall tile + WALL_COLOR #3A3A5C side/bottom border. Add a "Furniture" row: MetroCity desks, monitors and decor — scenery, never a state signal. The accent rule is unchanged.
  - Spacing exceptions: add the layout (office-layout.json): pods of two seats behind 3-tile desks at cols 1/5/9/13 on rows 5/9, seats on rows 4/8 facing the viewer, lanes at cols 4/8/12/16 and rows 3/6/7, decor on cols 17-18 and the top wall.
  - Sprite & Icon Asset Inventory: add an "Office sprites" sub-table (key, source sheet or "original", size) from office-metrocity.json, pointing at ASSET-LICENSES §1a.
  - UI Considerations "zero-one-many" row: 16 desk seats + 8 standing spots; lowest free seat first, reclaimed on despawn; a 25th+ concurrently present agent shares the last standing spot (accepted ceiling). Remove the rows 3/6/9 x 18 wording.
  - Registry Safety: one sentence that the MetroCity Interior sheets are committed source images decoded by a script (not a registry), audited in ASSET-LICENSES §1a.
- ROADMAP.md, Phase 5 success criteria: append "5. The office floor is furnished — MetroCity floor and wall tiles, desks with monitors grouped in pods with walking lanes — agents sit at desk seats and never walk through furniture or another seated agent." (Use a scoped Edit on that list only.)
- App.tsx footer sentence becomes: `Pixel office renderer forked from pixel-agents-hq/pixel-agents (MIT) · character and office sprites: MetroCity packs by JIK-A-4 · full audit: references/ASSET-LICENSES.md`. Update the footer comment in one or two lines: the office art (§1a) is credited alongside the character pack; still no licence assertion over the shipped bytes because §1's hair layer is credit-only. App.test.tsx `ATTRIBUTION` pins the new sentence (keep it apostrophe-free).
- ASSET-LICENSES "Attribution in the running app": the footer credits the fork (MIT) and both MetroCity packs (character §1, Interior §1a) and asserts no licence; `App.test.tsx` pins the sentence.
  </action>
  <verify>
    <automated>pnpm --filter web test</automated>
    <automated>grep -n "office sprites: MetroCity packs by JIK-A-4" apps/web/src/App.tsx apps/web/src/App.test.tsx</automated>
    <automated>grep -n "furnished" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test` passes with the new pinned sentence.
    - ROADMAP Phase 5 lists success criterion 5; UI-SPEC Color, Spacing, Inventory, UI Considerations and Registry sections cite 05-24/05-25.
    - ASSET-LICENSES' attribution section and the footer describe the same credit.
  </acceptance_criteria>
  <done>What the office renders, what the design contract says, what the roadmap promises and what the footer credits all agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> local test container and dev servers | unchanged (05-12: harness-owned container, port preflight, loopback-only target) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-25-01 | Repudiation (false proof) | harness assertions drifting from the engine's layout | medium | mitigate | all geometry and colours read from office-layout.json / office-metrocity.json / constants.ts at run time |
| T-05-25-02 | Repudiation (licence claim) | footer over-claiming | medium | mitigate | credit-only wording pinned by App.test; ASSET-LICENSES section updated in the same task |
| T-05-25-03 | Denial of service | render cost regressions | low | mitigate | TRUTH 7 frame-rate floor on the heaviest live scene |
</threat_model>

<verification>
- `node scripts/verify-pixel-office-live.mjs` ends `LIVE PROOF: PASS` with TRUTH 0-7.
- `pnpm --filter pixel-office test` and `pnpm --filter web test` green.
</verification>

<success_criteria>
G-05-1e is proven on the live canvas (furnished office, seats, frame rate) and documented consistently; G-05-2's contrast holds on the real floor.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-25-SUMMARY.md` when done.
</output>
