---
phase: 05-pixel-office-renderer
verified: 2026-09-23T11:05:29Z
status: passed
score: 25/26 must-haves verified
covered_files:

  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/phases/05-pixel-office-renderer/05-01-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-01-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-02-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-02-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-03-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-03-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-04-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-04-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-05-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-05-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-06-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-06-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-07-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-07-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-08-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-08-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-09-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-09-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-10-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-10-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-11-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-11-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-12-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-12-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-13-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-13-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-14-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-14-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-15-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-15-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-16-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-16-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-17-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-17-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-18-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-18-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-19-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-19-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-20-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-20-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-21-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-21-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-22-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-22-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-23-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-23-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-24-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-24-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-25-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-25-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-26-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-26-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-27-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-27-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-28-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-28-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-29-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-29-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-30-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-30-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-31-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-31-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-32-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-32-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-33-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-33-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-34-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-34-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-35-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-35-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-36-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-36-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-37-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-37-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-38-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-38-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-39-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-39-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-40-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-40-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-41-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-41-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-REVIEW.md"
  - ".planning/phases/05-pixel-office-renderer/05-UAT.md"
  - ".planning/phases/05-pixel-office-renderer/05-UI-SPEC.md"
  - ".planning/phases/05-pixel-office-renderer/COVERAGE.md"
  - ".planning/phases/05-pixel-office-renderer/deferred-items.md"
  - "apps/api/src/routes/events.ts"
  - "apps/web/index.html"
  - "apps/web/src/App.test.tsx"
  - "apps/web/src/App.tsx"
  - "packages/event-schema/src/payloads/index.ts"
  - "packages/pixel-office/LICENSE"
  - "packages/pixel-office/src/constants.ts"
  - "packages/pixel-office/src/engine/renderer.test.ts"
  - "packages/pixel-office/src/engine/renderer.ts"
  - "packages/pixel-office/src/handoff/dialogue-templates.test.ts"
  - "packages/pixel-office/src/handoff/dialogue-templates.ts"
  - "packages/pixel-office/src/handoff/handoff-choreography.test.ts"
  - "packages/pixel-office/src/handoff/handoff-choreography.ts"
  - "packages/pixel-office/src/index.ts"
  - "packages/pixel-office/src/layout/office-layout.json"
  - "packages/pixel-office/src/layout/officeLayout.ts"
  - "packages/pixel-office/src/sprites/bubble-waiting.json"
  - "packages/pixel-office/src/sprites/bubbleSprites.test.ts"
  - "packages/pixel-office/src/status/status-mapping.ts"
  - "packages/pixel-office/src/types.ts"
  - "references/ASSET-LICENSES.md"
  - "scripts/verify-pixel-office-live.mjs"

covered_digest: "v1:sha256:d78cb4a0d8202fffc91c9ff6278afac79eb00ff2de22337d915e318e72103157"
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 19/22
  gaps_closed:
    - "Gap 1 / review CR-01 (blank title): resolveHandoffDialogue routes every title through titleOrNull (dialogue-templates.ts:50-53, :69), so empty and whitespace titles take the no-title branch. Re-reproduced by this verifier through the real choreography: the committed cases at handoff-choreography.test.ts:1621-1631 pin `hands off to agent-b` / `agent-b accepts the handoff` for both `\"\"` and `\"   \"`. Revert-proved here: removing the titleOrNull call fails exactly 7 cases (4 dialogue, 1 padded-trim, 2 choreography); restored."
    - "Gap 2 / review WR-01 (fullTitle): `fullTitle: string | null` (handoff-choreography.ts:401) resolved as `titleOrNull(getTaskTitle(record.taskId))` (:440); `?? record.taskId` count 0; the three stale docblocks (:150, ActiveHandoff docblock, index.ts:57-61) are corrected. Revert-proved here: putting the id fallback back fails exactly the 2 null cases (:1362, :1368); restored."
    - "Advisory IN-02 (dead record holds its aisle slot a tick): reservation is `record.fromChar !== fromChar && senderIsCurrent(record)` (:114). Revert-proved here: dropping senderIsCurrent fails only the IN-02 case (:1006); both WR-08 cases stay green; restored."
    - "Advisory IN-01 (tautological assertion) removed; advisory 7 (UI-SPEC four offsets) fixed — UI-SPEC now lists `+1, -1, +3, -3, +5, -5` (count 1)."
  gaps_remaining: []
  regressions: []
advisory:

  - finding: "05-REVIEW (new) WR-01: a whitespace-only agent NAME paints an incomplete handoff sentence. `upsertCharacterFromAgent`'s `if (name) ch.name = name` (packages/pixel-office/src/index.ts:171) blocks `\"\"` but admits `\"   \"`, and both call sites use `toChar.name ?? record.toAgentId` (handoff-choreography.ts:280, :320). REPRODUCED by this verifier through the real choreography (temporary test, deleted): name `\"   \"` gives `hands Fix login to    ` / `    accepts Fix login`, and with no title `hands off to    ` / `    accepts the handoff`. Name `\"\"` correctly falls back to `agent-b`."
    category: other
    reason: "Real defect, classified ADVISORY (WARNING) rather than a gap, for two independent reasons. (1) SCOPE: every must_have it could touch is title-scoped. 05-41 truth 1 and 05-40 truth 1 say never present a task id or a blank string AS A TITLE; the receiver-name fallback to its agent id is a documented, deliberate design (handoff-choreography.ts:309-313 comment, test at :1633). No must_have in any of the 41 plans claims a blank NAME yields a complete sentence; 05-40 truth 2 (verb-led even when a value is TRUNCATED) still literally holds — `hands off to    ` is verb-led. Promoting it to a gap would require inventing a must_have, which the verifier may not do. (2) REACHABILITY: it is unreachable through the shipped pipeline. The review's claim that `POST /events accepts name: \"   \" from any worker credential` is factually wrong: `agent.online` is NOT in WORKER_ALLOWED_EVENT_TYPES (apps/api/src/routes/events.ts:18-26), that route is the only `insert(events)` in apps/api, and it 403s the type before schema parsing; no in-repo producer emits agent.online (the live harness's own header says so). It becomes live only when a future phase adds an agent.online producer. Recommendation: fix it before that, at the one write site (index.ts:171), and add `\"   \"` as a NAME to the everyLine sweep. Note the fix as the review writes it (`name?.trim()`) would break 05-41's acceptance gate `exactly one .trim() in pixel-office sources`; route it through a shared helper instead, e.g. reuse titleOrNull under a neutral name."
    evidence_status: "reproduced at module level through the real FSM by this verifier; unreachable through the production ingress (403 at apps/api/src/routes/events.ts:37-39)"
  - finding: "Correction to the PRIOR verification's CR-01 reachability claim: `task.created` is also absent from WORKER_ALLOWED_EVENT_TYPES, so a blank title was never postable by a worker credential either. CR-01 was latent, not live-at-a-trust-boundary."
    category: other
    reason: "Does not reopen or weaken anything: CR-01 was a must_have failure by the letter of 05-40 truth 1 regardless of reachability, and it is now closed in code with revert-proven tests. Recorded so Phase 6/7 threat models start from the right ingress facts."
    evidence_status: "source read (apps/api/src/routes/events.ts:18-26)"
  - finding: "05-REVIEW WR-02 (carried WR-03): opaque fixed footer band can cover the canvas's bottom ~19-21 CSS px below the 960x528 minimum viewport (App.tsx footer + overflow:auto wrapper with no bottom padding)."
    category: other
    reason: "Closed as a product call by human UAT round 3 test 3 (`result: pass`, 'Keep the footer behaviour as-is for now'), measured per-row from live frames; re-confirmed unchanged. Not a must_have failure."
    evidence_status: "resolved by human UAT (05-UAT.md round 3, test 3)"
  - finding: "05-REVIEW IN-01..IN-05: stale `12-code-point` at index.ts:37 and types.ts:162 (cap is 14); capForDialogue does not trimEnd before the ellipsis (`hands Refactor the … to Ada`); zero-width / Hangul-filler titles pass titleOrNull (Phase 7 SAFE ground); MAX_DIALOGUE_LINE_CHARS reads like a cap but is test-only; zero-network grep is single-file. All confirmed present by source read."
    category: other
    reason: "None falsifies a must_have. The 05-41 docblock truth enumerates the ActiveHandoff, showsLineOf and task-title-registry docblocks, all three of which are correct; index.ts:37 is the getActiveHandoffs re-export comment and types.ts:162 was outside 05-41's scope. IN-03 is deferred to Phase 7 (SAFE-01/02)."
    evidence_status: "source read; none reproducible as a must_have failure"
  - finding: "Carried test-quality advisories from the prior round, unchanged and not in 05-41's scope: WR-04 (40-scene sweep pins the 33-cp accepted line as widest; the 34-cp requested line is unasserted there), WR-05 (TRUTH 4 window satisfied by +1 ink-tail row cancelling device-pixel rounding — measured 3.25 again this run), IN-06 (dead fill term in the floor-contrast max()), WR-02 side-tail Y (unreachable on the shipped layout)."
    category: other
    reason: "In each case the property the must_have claims is carried by a different, non-vacuous guard (see the prior report's reasoning, re-confirmed). No change since the prior verification."
    evidence_status: "carried; gap 3.25 re-measured in this verifier's live run"
behavior_unverified_items:

  - truth: "A socket close or error surfaces a user-observable disconnect banner with role=\"status\" in apps/web/src/App.tsx (05-40 must_have, verification: backstop)"
    test: "Open the office against a running API, then kill the API process (or drop the network) and watch the top of the page."
    expected: "A banner with role=\"status\" appears and says the live feed is disconnected; the office does not freeze silently."
    why_human: "Declared `verification: backstop`, so presence and wiring are not evidence. Present and wired (App.tsx markDisconnected registered on both `close` and `error`, driving the role=\"status\" banner), but still no apps/web test exercises the transition (grep over App.test.tsx: no disconnect/close case) and the live harness never closes the socket."
coincidental_reliance_items:

  - truth: "The live harness measures the raised head gap on real browser pixels and goes red when the rendered gap leaves the BUBBLE_ICON_GAP_PX..+1 window (05-38)"
    reason: undeclared-precondition
    harden: "Carried unchanged: the window holds at 3.25 because +1 glyph ink-tail row and -(scale-1)/scale rounding cancel, neither declared. Derive the ink-tail row count from the sprite JSON and compare like for like."
unverified_prohibitions:

  - statement: "MUST NOT ship a state-signal overlay that is data-correct but practically illegible at stream scale"
    requirement_id: OFFICE-03
    verification: judgment
    disposition: "Mechanical floor re-proved in this process (live TRUTH 0/2/4: scale 3/4/6, 1088 blocked-glyph px, 3.25 px above the seated owner's head). This verifier inspected the fresh 1280x720 handoff.png: red bar-in-circle and hourglass (light upper bulb, dark lower bulb) are separable by silhouette, both clear of their owners' heads. NON-AUTHORITATIVE LLM-judge verdict: supported. unverified-prohibition — human confirmation recommended (human item 1)."
  - statement: "MUST NOT treat mechanically painted dialogue pixels as proof the line is legible at stream scale"
    requirement_id: HANDOFF-02
    verification: judgment
    disposition: "Re-proved mechanically (live TRUTH 5: requested 6168 bubble + 1364 text px, accepted 7809 + 1879) and visually: the fresh frame shows `hands off to live-proo…` as one verb-led sentence on its own plate, clear of desks and monitors. Readability at viewing distance remains a judgment. unverified-prohibition — human confirmation recommended (human item 1)."
human_verification:

  - test: "Open the frame captured by this verifier at HEAD (session scratchpad `verif-shots/handoff.png`, 1280x720), or run the office live at your OBS source size with a blocked agent, a waiting agent and a handoff mid-sequence. Glance for one second."
    expected: "The hourglass reads as an hourglass without relying on its blue; the blocked sign is separable from it at a glance; the handoff line reads as a sentence (`hands off to …`), not a debug banner; glyphs sit clear of their owners' heads; seated agents read as sitting while the aisle sender reads as standing."
    why_human: "Your UAT round 3 findings (tests 1 and 2, `severity: cosmetic`) are still recorded as `result: issue` in 05-UAT.md; only you can accept them closed. Automated inspection was done first: the live probe passed end to end in this process and the captured frame shows every requested change."
  - test: "Kill the API process while the office is open."
    expected: "A disconnect banner appears at the top of the page (role=\"status\"); the office does not freeze silently."
    why_human: "Declared `verification: backstop` in 05-40; no automated test or harness step exercises the socket close path."
---

# Phase 5: Pixel Office Renderer Verification Report

**Phase Goal:** The forked Pixel Agents office renders real company state on screen as a pure consumer of projections — never a fabricated animation.
**Verified:** 2026-09-23T11:05:29Z (21:05 +10:00)
**Status:** human_needed
**Re-verification:** Yes — after gap-closure plan 05-41. Both prior gaps re-verified with full depth; every previously passed truth regression-checked.

## What This Verifier Ran Itself

Nothing below is taken from 05-41-SUMMARY.md or 05-REVIEW.md.

1. **The phase probe, end to end.** `PIXEL_OFFICE_SHOTS=<scratchpad>/verif-shots node scripts/verify-pixel-office-live.mjs` — real Docker Postgres, real api + web dev servers, real worker credential, real events over `/ws/browser`, headless Chromium canvas readback. **Exit 0, `LIVE PROOF: PASS`, TRUTH 0-7 all green.** Numbers identical to the prior round (4128 sprite px, 1088 glyph px, gap 3.25, requested 6168+1364, accepted 7809+1879, 8 desks at 17760 px, 59.9 fps) — the null-title path 05-41 does not change is byte-identical, as the plan predicted.
2. **Visual inspection** of the fresh `handoff.png`: blocked and waiting glyphs separable, hourglass legible, `hands off to live-proo…` one verb-led sentence, footer attribution present.
3. **Both suites, uncached:** `vitest run --root packages/pixel-office` → **216 passed (8 files)**; `vitest run --root apps/web` → **24 passed (3 files)**; `pnpm --filter web typecheck` → clean `tsc --noEmit`. (The caller's full `pnpm turbo run test --force` 10/10 was not re-run, per the one-full-run rule.)
4. **Three revert-proofs**, each a temporary source edit restored with `git checkout` (git status clean afterwards): titleOrNull removed → exactly 7 fail; `?? record.taskId` restored → exactly 2 fail; `senderIsCurrent` dropped → exactly the IN-02 case fails, WR-08 cases green.
5. **Reproduction of the review's new WR-01** through the real choreography (temporary test, deleted): whitespace name → `hands Fix login to    ` / `    accepts Fix login`; empty name → correct `agent-b` fallback.
6. **Ingress read** of `apps/api/src/routes/events.ts` to settle reachability: `agent.online` and `task.created` are both 403'd for worker credentials.

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | **SC1** — agents on one floor, sprites/animations matching real current state across all 15 statuses | ✓ VERIFIED | Regression: `STATUS_MAP: Record<AgentStatus, StatusVisual>` (status-mapping.ts:35), 15 entries. Live TRUTH 1: 4128 sprite px from real events. |
| 2 | **SC2** — blocked/waiting visually distinguishable at a glance | ✓ VERIFIED | Live TRUTH 2 (1088 glyph px) and TRUTH 4 (3.25 px above seated owner's head); fresh frame inspected. |
| 3 | **SC3** — handoff walk, task icon, accept, return; deterministic template dialogue | ✓ VERIFIED | Live TRUTH 3 and 5 green. `DIALOGUE_TEMPLATES` still a two-key static map; module imports nothing. |
| 4 | **SC4** — attribution and licence notices visible and preserved | ✓ VERIFIED | LICENSE header + MIT verbatim with pinned commit `3537e140…`; 8 fork-headered sources; footer read off the fresh frame; TRUTH 0 backdrop `rgb(58,58,92)` at three viewports. |
| 5 | **SC5** — furnished floor, desk seats, no walking through furniture/seated agents | ✓ VERIFIED | Live TRUTH 6: 8 desks, 0 bare-floor px; TRUTH 7: 59.9 fps with 9 agents. |
| 6 | 05-38 — glyph leaves BUBBLE_ICON_GAP_PX above the visible head, every pose, zoom 1 and 3 | ✓ VERIFIED | Unchanged file; suite green; live gap 3.25. |
| 7 | 05-38 — seated agent shows >= 12 fewer body rows, >= 10 survive | ✓ VERIFIED | Unchanged; suite green; visible in fresh frame. |
| 8 | 05-38 — live harness goes red when the rendered gap leaves the window | ✓ VERIFIED (coincidental-reliance) | Carried unchanged; see `coincidental_reliance_items`. |
| 9 | 05-39 — hourglass reads by luminance structure alone | ✓ VERIFIED | Unchanged asset and tests; visible in fresh frame. |
| 10 | 05-39 — glass vs sand >= 3:1 contrast | ✓ VERIFIED | Unchanged asset (3.42 measured in the prior round; file untouched since). |
| 11 | 05-39 — light/sand band ratios either side of the waist | ✓ VERIFIED | Unchanged; suite green. |
| 12 | **05-40/05-41** — a handoff bubble never presents a task id OR a blank string as a title; no-title lines are `hands off to <name>` / `<name> accepts the handoff` | ✓ VERIFIED (was ✗ FAILED) | `titleOrNull` (dialogue-templates.ts:50-53) applied at :69 before capping, so every caller is covered with no call-site edit. Choreography cases :1621-1631 drive the real FSM with `""` and `"   "` and pin both exact sentences; the id-prefix guard at :1598-1610 still holds. **Revert-proved:** removing titleOrNull fails exactly those 7 cases. |
| 13 | 05-40 — both lines verb-led | ✓ VERIFIED | Templates unchanged (:16-19); fresh frame reads `hands off to live-proo…`. |
| 14 | 05-40 — static two-entry map, two capped interpolations, zero network/LLM surface | ✓ VERIFIED | Package-wide grep for `Math.random\|Date.now\|setTimeout\|setInterval\|fetch(\|XMLHttpRequest\|WebSocket\|import(` outside tests: 2 hits, both comment lines (index.ts:90-91). |
| 15 | 05-40 — every line fits MAX_DIALOGUE_LINE_CHARS | ✓ VERIFIED | `everyLine` widened to `[title, null, "", "   "]` (dialogue-templates.test.ts:38-39); budget test still derives 34 and passes. |
| 16 | 05-40 — bubble rect attributed by record identity | ✓ VERIFIED | `getDialogueBox(speakerId, record.taskId)` (handoff-choreography.ts:447); renderer untouched since prior round. |
| 17 | **05-40/05-41** — getActiveHandoffs exposes the untruncated title under the same rule: `fullTitle: string \| null`, never the task id | ✓ VERIFIED (was ✗ PARTIAL) | :401 `fullTitle: string \| null`; :440 `titleOrNull(getTaskTitle(record.taskId))`; `?? record.taskId` 0 hits; `12 code points` / `exact value the bubble` 0 hits in the file. Tests :1362 (unregistered) and :1368 (whitespace) assert `toBeNull()`. **Revert-proved:** the id fallback fails exactly those 2. Typecheck clean. |
| 18 | 05-40 (backstop) — socket close/error surfaces a `role="status"` disconnect banner | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present and wired in App.tsx; still no test or harness step exercises the close path. Routed to human item 2. |
| 19 | 05-36 — footer carries unconditional `background: WALL_COLOR` | ✓ VERIFIED | Live TRUTH 0 backdrop at all three viewports, 0 near-black px. |
| 20 | 05-37 — a handoff's aisle slot is reserved for the record's lifetime, released at retirement | ✓ VERIFIED | Now qualified by truth 26 for dead records; for a current sender the reservation is unchanged (:114), `handoffs.delete` still appears once, inside `retireHandoff` (:185). Both WR-08 cases pass. |
| 21 | 05-37 — identity rule governs clear and read paths | ✓ VERIFIED | `showsLineOf` unchanged (:156-158) and still the gate at every clear/read site. |
| 22 | **Core Value** — pure consumer of projections, never a fabricated animation | ✓ VERIFIED | Zero timer/random/network surface in code; every pixel in this run originated in a real POSTed event. |
| 23 | 05-41 — the blank-is-absent rule is written once (`titleOrNull`) and both title reads route through it; a padded title is interpolated and exposed trimmed | ✓ VERIFIED | `git grep "\.trim()"` over non-test sources: exactly one hit, dialogue-templates.ts:51. Both consumers import it (resolver :69; choreography import :23, use :440). Test :65 pins `hands Fix login to Ada` for `"  Fix login  "`. |
| 24 | 05-41 — apps/web's two title entry points agree: a live task.created with an empty title is not registered | ✓ VERIFIED | App.tsx live path `event.type === "task.created" && event.taskId && event.payload.title` mirrors the snapshot path's `if (task.title)`. Ordering comment and position above `handleHandoffEvent` intact. The resolver rule (truth 12) makes whitespace harmless on both paths. `packages/event-schema` unmodified since `b0e30f0`. |
| 25 | 05-41 — docblocks around ActiveHandoff, showsLineOf and the task-title registry describe what ships | ✓ VERIFIED | ActiveHandoff docblock names `MAX_DIALOGUE_TITLE_CHARS` and the null case; showsLineOf docblock names the constant; index.ts:57-61 documents the no-title branch and `fullTitle: null`. UI-SPEC rows 150/173 cite 05-41; line 102 lists six offsets. (Two stale `12` mentions remain outside the enumerated docblocks — index.ts:37, types.ts:162; advisory.) |
| 26 | 05-41 — a record whose sender went offline or was re-seated stops reserving its slot at once, while a current departing sender keeps it (WR-08) | ✓ VERIFIED | `record.fromChar !== fromChar && senderIsCurrent(record)` (:114). Behaviour-dependent, so behavioural evidence required: case :1006 passes and **goes red alone** when the condition is dropped, while both WR-08 cases stay green. |

**Score:** 25/26 truths verified (1 present, behavior-unverified)

### Assessment of the Review's New WR-01 (blank receiver name)

Asked specifically whether this falls inside the same must_have. **It does not, and it is classified advisory (WARNING), not a gap.**

- **It is a real defect.** Reproduced here through the real FSM: a `"   "` name produces `hands Fix login to    ` and `    accepts Fix login`. That is the same incomplete-sentence class CR-01 was, on the other interpolated value.
- **The must_haves are title-scoped.** 05-40 truth 1 and 05-41 truth 1 both say never present an id or a blank string *as a title*. The receiver name falling back to its agent id is intentional and documented (handoff-choreography.ts:309-313, test :1633). No plan's must_have says a blank *name* yields a complete sentence. 05-40's verb-led truth is about truncation, and `hands off to    ` is still verb-led. Calling this a failed must_have would mean inventing a must_have.
- **It cannot be reached today, and the review is wrong on this point.** `agent.online` is not in `WORKER_ALLOWED_EVENT_TYPES` (apps/api/src/routes/events.ts:18-26). That route is the only event insert in apps/api, and it returns 403 before schema parsing. Nothing in the repo emits agent.online. The same applies to `task.created`, which corrects the prior report's claim that CR-01 was reachable at a trust boundary.
- **Recommendation:** fix it before any phase adds an `agent.online` producer. Put the fix at the single write site (index.ts:171) and add `"   "` as a name to the `everyLine` sweep. Don't use the review's suggested `name?.trim()`: it would break 05-41's own gate that pixel-office sources contain exactly one `.trim()`. Share the helper instead.

If you'd rather treat "complete sentence for every interpolated value" as a phase contract, add that must_have and this becomes a one-line gap closure. The verifier can't add it on its own authority.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Stream-safety filtering of `TaskState.title` (incl. review IN-03 zero-width/filler characters) | Phase 7 | Phase 7 goal: classify and filter by visibility before data leaves the server (SAFE-01/02). |
| 2 | Hover / click / dashboard UI over `getActiveHandoffs()` | Phase 6 | CEO Dashboard; 05-35 scopes the UI out. |
| 3 | Hideable-for-stream attribution footer | later phase | 05-UAT.md round 3 Deferred Follow-Ups. |

### Advisory (New Scope, Unevidenced)

| # | Finding | Category | Why Advisory |
|---|---------|----------|--------------|
| 1 | Review WR-01 — blank receiver name gives an incomplete sentence | other | Reproduced, but outside every title-scoped must_have and unreachable through the only ingress (403). See the assessment above. |
| 2 | Prior report overstated CR-01's reachability (`task.created` is also 403'd for workers) | other | Correction only; CR-01 is closed either way. |
| 3 | Review WR-02 — footer band below 960x528 | other | Accepted as a product call in UAT round 3 test 3. |
| 4 | Review IN-01..IN-05 — stale `12` at index.ts:37 / types.ts:162, no `trimEnd` before `…`, invisible-char titles, test-only line budget, single-file network grep | other | None falsifies a must_have; IN-03 deferred to Phase 7. |
| 5 | Carried WR-04 / WR-05 / IN-06 / side-tail Y | other | Unchanged since the prior round; each property is carried by another non-vacuous guard. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/pixel-office/src/handoff/dialogue-templates.ts` | `titleOrNull` export, resolver routes through it, still import-free | ✓ VERIFIED | :50-53, :69; the only `.trim()` in the package sources. |
| `packages/pixel-office/src/handoff/handoff-choreography.ts` | nullable `fullTitle` via titleOrNull; IN-02 reservation; corrected docblocks | ✓ VERIFIED | :23, :401, :440, :114; stale-phrase greps 0. |
| `packages/pixel-office/src/index.ts` | taskTitles comment documents the no-title branch and null fullTitle | ✓ VERIFIED | :57-61. (:37 stale `12` is advisory; :171 name guard is the new WR-01 advisory.) |
| `apps/web/src/App.tsx` | live task.created guard matching the snapshot path | ✓ VERIFIED | Exact acceptance string present once; ordering intact. |
| `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` | rows 102/150/173 updated | ✓ VERIFIED | six offsets (count 1); `exact value the bubble interpolates` count 0; both rows cite 05-41. |
| Test files (dialogue-templates, handoff-choreography) | blank-title, null-fullTitle, IN-02 cases; tautology removed | ✓ VERIFIED | All present, all pass, all three revert-proven non-vacuous. |
| `scripts/verify-pixel-office-live.mjs` | TRUTH 0-7 on real pixels | ✓ VERIFIED | Run in this process, exit 0. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `getTaskTitle` | `Character.bubbleText` | choreography :279/:319 → `resolveHandoffDialogue` → `titleOrNull` | WIRED | Was PARTIAL. Blank titles now take the null branch; revert-proven. |
| `getTaskTitle` | `ActiveHandoff.fullTitle` | `titleOrNull(getTaskTitle(record.taskId))` | WIRED | Was PARTIAL; revert-proven. |
| `POST /events` | `registerTaskTitle` | `/ws/browser` relay → App.tsx guarded `task.created` | WIRED (belt) | Upstream of this, the worker allow-list 403s `task.created` entirely. |
| `interactionTileFor` reservation | `senderIsCurrent` | :114 | WIRED | Revert-proven. |
| `Character.bubbleTextTaskId` | `ActiveHandoff.box` | `getDialogueBox(speakerId, record.taskId)` | WIRED | Unchanged. |
| WS `onEvent` | office | `applyLiveEvent` → upserts → `handleHandoffEvent` | WIRED | Live-proved this run. |
| socket `close`/`error` | `role="status"` banner | `markDisconnected` | WIRED, UNEXERCISED | Truth 18. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| Characters on canvas | `AgentStatus` | WS snapshot + company-core reduce | 4128 sprite px from real events | ✓ FLOWING |
| Status glyph | `statusBubble` | same reducer path | 1088 px, owner-bound | ✓ FLOWING |
| Handoff label | title / name | `registerTaskTitle` → `titleOrNull` → template | `hands off to live-proo…` on canvas; blank title now yields the complete no-title sentence | ✓ FLOWING (a blank *name* is a latent advisory, unreachable) |
| `getActiveHandoffs().fullTitle` | title or null | `titleOrNull(getTaskTitle(...))` | real title or null, never an id | ✓ FLOWING |
| Footer backdrop | `WALL_COLOR` | constants.ts | `rgb(58,58,92)` at 3 viewports | ✓ FLOWING |
| Furniture | layout + sprite JSON | office-layout.json | 8 desks, 0 bare px | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Engine suite | `npx vitest run --root packages/pixel-office` | 216 passed (8 files) | ✓ PASS |
| Web suite | `npx vitest run --root apps/web` | 24 passed (3 files) | ✓ PASS |
| Web typecheck | `pnpm --filter web typecheck` | clean | ✓ PASS |
| CR-01 revert-proof | remove `titleOrNull` from resolver | exactly 7 fail; restored | ✓ PASS (guard non-vacuous) |
| WR-01 revert-proof | restore `?? record.taskId` on fullTitle | exactly 2 fail; restored | ✓ PASS (guard non-vacuous) |
| IN-02 revert-proof | drop `senderIsCurrent(record)` | only IN-02 case fails; restored | ✓ PASS (guard non-vacuous) |
| Single trim rule | `git grep "\.trim()"` over non-test sources | 1 hit, dialogue-templates.ts:51 | ✓ PASS |
| Pure-consumer property | timer/random/network grep | 2 comment hits, 0 code | ✓ PASS |
| Review WR-01 repro (blank name) | temporary FSM test, deleted | `hands Fix login to    ` / `    accepts Fix login` | ℹ️ reproduced (advisory 1) |

`git status` after all temporary edits: no change under `packages/` or `apps/`.

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| Live end-to-end proof (declared phase probe; no `scripts/*/tests/probe-*.sh` exists) | `PIXEL_OFFICE_SHOTS=<scratchpad>/verif-shots node scripts/verify-pixel-office-live.mjs` | TRUTH 0-7 PASS, `LIVE PROOF: PASS`, exit 0; frames written outside the repo and inspected | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| OFFICE-01 | 05-38 (+ earlier) | Forked renderer, one floor, 15 states reflected | ✓ SATISFIED | Truths 1, 5, 7, 22 |
| OFFICE-02 | (10 plans) | Fork attribution and licence preserved | ✓ SATISFIED | Truth 4 |
| OFFICE-03 | 05-38, 05-39 (+ earlier) | Blocked/waiting clear glanceable signal | ✓ SATISFIED (mechanically + visually) | Truths 2, 6, 9-11. Glanceability at viewing distance is a judgment call; see human item 1. |
| HANDOFF-01 | 05-41, 05-40 (+ earlier) | Walk, icon, accept, move to work | ✓ SATISFIED | Truths 3, 16, 17, 20, 21, 26 |
| HANDOFF-02 | 05-41, 05-40 (+ earlier) | Deterministic template dialogue, never LLM | ✓ SATISFIED | Truths 12-15, 23. Was "satisfied with a defect"; the blank-title defect is closed. |

All 5 IDs appear in REQUIREMENTS.md mapped to Phase 5 (lines 31-33, 45-46, 134-138), all `[x]` / `Complete`. No other ID maps to Phase 5. **No orphaned requirements.** Plan 05-41 declares HANDOFF-01 and HANDOFF-02, and both are accounted for.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` in files changed since `b0e30f0` | — | None |
| — | — | `.skip` / `.only` / `.todo` in pixel-office or apps/web | — | None |
| `packages/pixel-office/src/index.ts` | 171 | Truthiness guard admits a whitespace value that reaches the screen | ⚠️ Warning | Advisory 1: real, but outside the must_have scope and unreachable. |
| `packages/pixel-office/src/index.ts` / `types.ts` | 37 / 162 | Stale `12` cap in comments | ℹ️ Info | Advisory 4 |

### Human Verification Required

#### 1. Close your UAT round 3: the polished frame

**Test:** Open `verif-shots/handoff.png` (1280x720, captured at HEAD by this verifier in the session scratchpad), or run the office live at your OBS source size with a blocked agent, a waiting agent and a handoff in progress. Look for one second.
**Expected:** The hourglass reads as an hourglass without relying on its blue. The blocked sign is easy to tell apart from it. The handoff line reads as a sentence (`hands off to …`). Glyphs sit clear of heads. Seated agents look seated and the aisle sender looks like it is standing.
**Why human:** 05-UAT.md still records round 3 tests 1 and 2 as `result: issue`. Only you can accept them. The automated checks came first: the live probe passed and the captured frame shows each change you asked for.

#### 2. The disconnect banner (backstop truth)

**Test:** With the office open, kill the API process.
**Expected:** A `role="status"` banner appears at the top and the office does not freeze silently.
**Why human:** 05-40 marks this `verification: backstop`, and no test or harness step drives the socket close path.

### Gaps Summary

**No gaps remain.** Both prior gaps are closed in code, and the closure goes beyond the SUMMARY's word. Each fix has a test that turns red when this verifier reverts the fix, and turns red alone. Blank titles now yield the complete no-title sentence through the real FSM. `fullTitle` is `string | null` and never an id. The blank-is-absent rule is written once. The IN-02 slot release and the other fold-ins are in place. The live probe is still green, pixel for pixel, so nothing regressed.

The review's new WR-01 (blank receiver name) is a genuine latent defect of the same kind, reproduced here. It is recorded as a WARNING advisory rather than a gap, for two reasons: every must_have it could touch is scoped to titles, and the ingress rejects `agent.online` outright. Fix it before any phase adds an `agent.online` producer.

Status is `human_needed` for two reasons: your own open UAT round 3 acceptance, and the backstop disconnect banner.

---

_Verified: 2026-09-23T11:05:29Z_
_Verifier: Claude (gsd-verifier)_
