---
phase: 05-pixel-office-renderer
verified: 2026-09-23T18:30:00Z
status: gaps_found
score: 19/22 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/phases/05-pixel-office-renderer/05-01-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-01-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-02-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-02-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-03-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-03-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-04-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-04-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-05-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-05-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-06-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-06-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-07-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-07-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-08-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-08-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-09-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-09-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-10-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-10-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-11-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-11-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-12-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-12-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-13-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-13-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-14-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-14-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-15-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-15-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-16-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-16-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-17-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-17-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-18-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-18-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-19-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-19-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-20-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-20-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-21-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-21-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-22-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-22-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-23-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-23-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-24-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-24-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-25-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-25-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-26-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-26-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-27-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-27-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-28-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-28-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-29-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-29-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-30-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-30-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-31-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-31-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-32-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-32-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-33-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-33-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-34-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-34-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-35-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-35-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-36-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-36-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-37-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-37-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-38-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-38-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-39-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-39-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-40-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-40-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-REVIEW.md"
  - ".planning/phases/05-pixel-office-renderer/05-UAT.md"
  - ".planning/phases/05-pixel-office-renderer/05-UI-SPEC.md"
  - ".planning/phases/05-pixel-office-renderer/COVERAGE.md"
  - ".planning/phases/05-pixel-office-renderer/deferred-items.md"
  - "apps/web/index.html"
  - "apps/web/src/App.test.tsx"
  - "apps/web/src/App.tsx"
  - "packages/event-schema/src/payloads/index.ts"
  - "packages/pixel-office/LICENSE"
  - "packages/pixel-office/src/constants.ts"
  - "packages/pixel-office/src/engine/renderer.test.ts"
  - "packages/pixel-office/src/engine/renderer.ts"
  - "packages/pixel-office/src/handoff/dialogue-templates.test.ts"
  - "packages/pixel-office/src/handoff/dialogue-templates.ts"
  - "packages/pixel-office/src/handoff/handoff-choreography.test.ts"
  - "packages/pixel-office/src/handoff/handoff-choreography.ts"
  - "packages/pixel-office/src/index.ts"
  - "packages/pixel-office/src/layout/office-layout.json"
  - "packages/pixel-office/src/layout/officeLayout.ts"
  - "packages/pixel-office/src/sprites/bubble-waiting.json"
  - "packages/pixel-office/src/sprites/bubbleSprites.test.ts"
  - "packages/pixel-office/src/status/status-mapping.ts"
  - "packages/pixel-office/src/types.ts"
  - "references/ASSET-LICENSES.md"
  - "scripts/verify-pixel-office-live.mjs"
covered_digest: "v1:sha256:03f14b3aed18afed7576690d0fc81b65bf786bdf6a1aee52e437a19b70ec8aec"
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 20/20
  gaps_closed:
    - "G-05-1c (05-38): BUBBLE_ICON_GAP_PX is 3 (constants.ts:35), consumed multiplicatively at renderer.ts:176 and :302. renderer.test.ts:260-269 asserts `inkBottom + BUBBLE_ICON_GAP_PX * zoom === headTop` for EVERY glyph, EVERY pose, at zoom 1 and 3 — a behavioural equality, not a constant echo. Live harness re-run in this process: `gap 3.25` measured on real browser pixels at scale 4."
    - "G-05-2a (05-38): CHARACTER_SITTING_OFFSET_PX is 10 (constants.ts:29), applied at renderer.ts:519 only for a non-WALK character on its OWN seat. renderer.test.ts:1276-1306 measures visible body rows off the composited frame: seated is >= 12 rows fewer than the same agent standing, and >= 10 rows still survive so the head is not buried. Visually confirmed in a frame captured by this verifier (handoff.png): the aisle sender shows a full body with legs, the two seated agents show head and shoulders only."
    - "G-05-1a (05-39): bubble-waiting.json decoded by this verifier — palette {1:#000000, 2:#4361ee, 3:#c8d4ff}, 13x11. Measured independently: relLum sand #4361ee = 0.159, glass #c8d4ff = 0.666, contrast 3.42 (>= 3), outline #000000 relLum 0. The four grayscale cases at bubbleSprites.test.ts:255-300 derive sand/glass by LUMINANCE ORDER, never by hex literal, so they keep testing the property if the palette is retuned. Visually confirmed in the captured frame: the glyph reads as an hourglass (light upper bulb, dark lower bulb, pinched waist), not a bowtie."
    - "G-05-1b, canvas half (05-40): both choreography call sites now pass `getTaskTitle(...) ?? null` (handoff-choreography.ts:279, :319); the templates are verb-led with no U+2192 (dialogue-templates.ts:14-20). The guard describe at handoff-choreography.test.ts:1552 asserts a TEN-CHARACTER id prefix appears in neither bubble, so a revert to `?? taskId` cannot pass vacuously through the 14-cp cap. Visually confirmed: the live bubble reads `hands off to live-proo…`, one verb-led sentence, not two ids joined by an arrow."
    - "Prior review WR-01 (renderer rect attribution) is genuinely closed: framePlacements carries `taskId: l.ch.bubbleTextTaskId ?? null` (renderer.ts:577) and `getDialogueBox(agentId, taskId: string)` matches on it (renderer.ts:492-495), so a null-stamped placement is unmatchable by construction; getActiveHandoffs passes `record.taskId` (handoff-choreography.ts:443) — the same identity showsLineOf reads. The prior round's coincidental-reliance flag on `box: null` is therefore retired."
    - "Prior bookkeeping advisory: REQUIREMENTS.md now marks all five Phase 5 IDs `[x]` and `Complete` (lines 31-33, 45-46, 134-138). Ledger reconciled."
  gaps_remaining: []
  regressions:
    - "None in shipped behaviour. The two gaps below are NEW, introduced by 05-40's own change of shape: making the title nullable created a blank-title branch that is not handled, and left the host read path on the old id fallback."
gaps:
  - truth: "A handoff bubble never presents a task id OR a blank string as a title: when no human-readable title is known, the line says what is happening without inventing one (05-40 must_have 1, G-05-1b)"
    status: failed
    reason: "`??` fires only on null/undefined. A registered EMPTY or whitespace title takes the non-null branch and paints a malformed sentence. Reproduced end to end by this verifier through the real choreography (temporary test, since deleted): sender bubble `\"hands  to agent-b\"`, receiver bubble `\"agent-b accepts \"`, whitespace title `\"hands     to agent-b\"`. `\"agent-b accepts \"` is exactly the incomplete-frame class 05-40 was chartered to eliminate, and is worse than the id it replaced because the reader gets no object at all. Reachable at a trust boundary: TaskCreatedPayload.title is `z.string()` with no `.min(1)` (packages/event-schema/src/payloads/index.ts:7), so POST /events accepts `title: \"\"` from any worker credential, and App.tsx:85 calls registerTaskTitle unguarded while the sibling site at App.tsx:70 guards it with `if (task.title)`. No in-repo producer emits task.created today, which is why it is not visible in the live harness — it becomes live the moment one does."
    artifacts:
      - path: "packages/pixel-office/src/handoff/dialogue-templates.ts"
        issue: "resolveHandoffDialogue treats only `null` as 'no title known'; a blank string is interpolated as if it were a title. One guard here covers every present and future caller."
      - path: "packages/pixel-office/src/handoff/handoff-choreography.ts"
        issue: "Lines 279 and 319 resolve with `getTaskTitle(...) ?? null`, which passes a blank title straight through."
      - path: "apps/web/src/App.tsx"
        issue: "Line 85 registers `event.payload.title` unguarded; line 70 (the snapshot path) guards the same value with `if (task.title)`. The two entry points disagree."
      - path: "packages/pixel-office/src/handoff/dialogue-templates.test.ts"
        issue: "The `everyLine` sweep enumerates only the two-valued null/present space, so no test can go red for a blank title."
    missing:
      - "Treat a blank/whitespace title as 'no title known' inside resolveHandoffDialogue (trim, then null), so the rule lives once at the resolver rather than at each call site."
      - "Add `\"\"` and `\"   \"` to the everyLine sweep in dialogue-templates.test.ts and to the id-never-painted describe in handoff-choreography.test.ts, asserting the exact null-branch sentences."
      - "Decide the trust-boundary question: either `.min(1)` on TaskCreatedPayload.title, or make App.tsx:85 guard like App.tsx:70 does. The resolver guard is the fix; this is the belt."
  - truth: "getActiveHandoffs exposes the untruncated title with participants, phase and the drawn bubble's rect (05-40 must_have 6) — under the same rule as the canvas: a task id is not a title"
    status: partial
    reason: "`fullTitle: getTaskTitle(record.taskId) ?? record.taskId` (handoff-choreography.ts:434) still substitutes the raw task id, in a field typed non-nullable `string` and documented as 'the UNTRUNCATED title'. 05-40 applied its own rule at two of three sites; the defect moved from the canvas to the public API rather than being removed. Not a live defect — nothing consumes getActiveHandoffs in this phase — but Phase 6 (CEO dashboard) is the declared consumer, and the docblock is now false in three further ways: it says the cap is 12 (it is 14 since 05-40), it calls fullTitle 'the exact value the bubble interpolates' (the bubble now interpolates nothing when no title is known), and index.ts:58-61 still documents the deleted 'falls back to the raw taskId at the call site' behaviour."
    artifacts:
      - path: "packages/pixel-office/src/handoff/handoff-choreography.ts"
        issue: "Line 434 `?? record.taskId`; stale docblocks at :150 and :377-383."
      - path: "packages/pixel-office/src/index.ts"
        issue: "Lines 58-61 document the removed taskId fallback."
    missing:
      - "`fullTitle: string | null` on the ActiveHandoff interface, resolved with `?? null` (blank included, per the resolver guard above)."
      - "Correct the three stale docblocks: 12 -> MAX_DIALOGUE_TITLE_CHARS, and drop the 'exact value the bubble interpolates' claim."
deferred:
  - truth: "Stream-safety filtering of TaskState.title before it reaches rendered dialogue"
    addressed_in: "Phase 7"
    evidence: "Phase 7 goal: company data is classified and filtered by visibility level before it leaves the server. dialogue-templates.ts bounds length only and says so in-source (SAFE-01/02)."
  - truth: "Hover / click / dashboard UI over the untruncated title and rect getActiveHandoffs exposes"
    addressed_in: "Phase 6"
    evidence: "Phase 6 is CEO Dashboard & Approval Workflow; 05-35's own must_have scopes the UI out ('No hover, click or dashboard UI is built in this phase')."
  - truth: "A hideable-for-stream attribution footer"
    addressed_in: "later phase (logged in 05-UAT.md Deferred Follow-Ups)"
    evidence: "UAT round 3 test 3: 'Keep the footer behaviour as-is for now' — a hide affordance is a new capability needing a licence-compliance decision, not a defect."
advisory:
  - finding: "05-REVIEW WR-04: the 40-scene bubble-placement sweep pins `WIDEST_LINE_CHARS = MAX_DIALOGUE_NAME_CHARS + ' accepts '.length + MAX_DIALOGUE_TITLE_CHARS` = 33 on the ACCEPTED leg, while 05-40 made the REQUESTED line the widest at 34 cps. The requested leg is rendered at 34 cps in all 40 scenes but its length is asserted nowhere, so the comment's 'this really is the widest line the caps allow' names a property the sweep does not check. CONFIRMED by source read (renderer.test.ts:540-548, :613-615)."
    category: other
    reason: "Not a must_have failure: the line-budget truth (05-40 must_have 4) is carried by dialogue-templates.test.ts's budget test, which enumerates all four kind x null/present combinations, DERIVES the max and asserts `toBe(MAX_DIALOGUE_LINE_CHARS)` — it fails on slack as well as on overflow. The sweep's geometry is still scored against the genuinely widest line; only the guard's self-description is wrong. Resolves by asserting the requested leg's length against MAX_DIALOGUE_LINE_CHARS and renaming the constant WIDEST_ACCEPTED_LINE_CHARS."
    evidence_status: "none provided (source read; the widest line IS rendered in every scene, just unasserted)"
  - finding: "05-REVIEW WR-05: TRUTH 4's window `headGap >= BUBBLE_ICON_GAP_PX && headGap <= BUBBLE_ICON_GAP_PX + 1` is satisfied by an asset artefact, not by the geometry it names. CONFIRMED NUMERICALLY by this verifier's own harness run: `gap 3.25` at scale 4 — exactly BUBBLE_ICON_GAP_PX + 1/scale, i.e. +1 because the scan sees only #d62828 (bubble-blocked.json's last distinctive row is 10 while resolveBubbleY anchors on lastOpaqueRow 11), minus (scale-1)/scale for device-pixel rounding."
    category: other
    reason: "Not a must_have failure. 05-38's must_have asks the harness to go red when the rendered gap shrinks or grows; it does for a RENDERER regression (dropping the gap gives ~0.25, below the floor). What it cannot catch is a CONSTANT revert — both sides read BUBBLE_ICON_GAP_PX — and that case is covered by renderer.test.ts:260-269, which asserts the equality per glyph/pose/zoom plus a literal pin at :262. The residual risk is a FALSE RED: repainting bubble-blocked.json's bottom outline row in a distinctive colour makes a correct renderer measure 2.25. Recorded as coincidental-reliance below. Resolves by deriving the glyph's non-distinctive ink tail from the sprite JSON instead of absorbing it in a magic +1."
    evidence_status: "measured (gap 3.25 in this verifier's live run, matching the predicted artefact exactly)"
  - finding: "05-REVIEW IN-06: `Math.max(contrast(outline, bg), contrast(mainFill, bg)) >= 3` in bubbleSprites.test.ts:169-179 has a dead fill term. CONFIRMED NUMERICALLY: the outline is guaranteed near-black by the sibling FROZEN test (relLum <= 0.03) and contrast(#000000, floor mean #8d5f24) = 3.79, so the max() is unconditionally satisfied for every glyph on every tile whatever the fill is."
    category: other
    reason: "Not a must_have failure — no 05-39 truth claims this guard covers the fill. It matters because 05-39's summary reasons about it as though it protected the repaint: it never constrained the fill at all. Measured here: the new fill #c8d4ff scores 3.77 on the floor and the OLD #a9bcff scored 2.98, so the repaint silently fixed a real floor-legibility gap this guard could not see. The 05-39 truths themselves are guarded elsewhere and non-vacuously (the four luminance-derived grayscale cases). Resolves by asserting outline and fill separately and de-duplicating the four-tile loop over distinct means."
    evidence_status: "computed in this process from the shipped assets"
  - finding: "05-REVIEW WR-02 (side-tail Y), carried and re-confirmed unchanged: tailFor's right/left branches derive the tail Y from `sideY` (renderer.ts:303), independent of the unconditionally clamped winner `y` (:374), so a clamped side winner paints its tail outside its own vertical span."
    category: architectural
    reason: "Latent, not live: reaching it needs speaker.headTop < floor.top, and the shipped layout keeps every character on rows 4/6/8 — 05-38's deeper seated sink moves headTop 4px further down, shrinking the window further. The containment test at renderer.test.ts:583-587 still waives the side kinds, so nothing guards it. Resolves with the review's two-line clamp."
    evidence_status: "none provided (unreachable on the shipped layout; no failing test)"
  - finding: "05-REVIEW WR-03 (opaque footer band over the canvas at sub-minimum viewports), carried and re-confirmed unchanged (App.tsx:124, :178-193)."
    category: other
    reason: "Resolved as a product call by human UAT round 3 test 3: `result: pass`, 'Keep the footer behaviour as-is for now.' The CEO measured per-row from live frames — at 1280x720 and 1920x1080 the text sits entirely BELOW the canvas (canvas ends y=703, text y=705..714, zero overlap), and at 800x480 the covered strip is the canvas's own bottom wall row. No seat, standing spot or head-anchored glyph falls in the covered band. No longer a human item; the hideable-for-stream variant is logged as a deferred follow-up."
    evidence_status: "resolved by human UAT (05-UAT.md round 3, test 3)"
  - finding: "05-REVIEW IN-01..IN-07: tautological assertion in the new collision test (:1468-1469, the claim it states is really carried by :1475); a dead record's aisle slot held for up to one tick (the reservation loop omits senderIsCurrent); MAX_DIALOGUE_LINE_CHARS reads like a cap but is enforced nowhere; TAIL_REACH_PX still pins GLYPH_ROWS as a literal 13; the zero-network grep is single-file, not transitive; the dead floor-contrast fill term (above); spriteBoxAt's docblock is wrong in the below-speaker direction and 05-38 widened the error 6 -> 10px. All confirmed present by source read."
    category: other
    reason: "None changes a shipped behaviour; none is reproducible as a defect. IN-02 (dead record holds its slot for a tick) is the one worth queueing with the gaps above before Phase 6 raises concurrent-handoff volume — it is a one-condition fix: `record.fromChar !== fromChar && senderIsCurrent(record)`."
    evidence_status: "none provided (source read; all confirmed present, none reproducible)"
  - finding: "Doc drift, carried from the previous round and still unresolved: 05-UI-SPEC.md line 102 still says the interaction slot set is 'column offsets +1, -1, +3, -3' while office-layout.json carries six ([1,-1,3,-3,5,-5]). The same paragraph WAS updated by 05-38 and 05-37, so the omission is local."
    category: other
    reason: "Documentation only — the JSON is the single source and the tests enumerate it. One-line edit."
    evidence_status: "none provided (no code impact)"
behavior_unverified_items:
  - truth: "A socket close or error surfaces a user-observable disconnect banner with role=\"status\" in apps/web/src/App.tsx (05-40 must_have, verification: backstop)"
    test: "Open the office against a running API, then kill the API process (or drop the network) and watch the top of the page."
    expected: "A banner with role=\"status\" appears and says the live feed is disconnected; the office does not simply freeze silently."
    why_human: "Declared `verification: backstop`, so presence and wiring do not count as evidence. The code IS present and wired — App.tsx:105-107 registers one `markDisconnected` on both `close` and `error`, and :134-136 renders the role=\"status\" banner on that state — but NO test in apps/web exercises the transition (grep over App.test.tsx finds no disconnect case), and the live harness never closes the socket. Present, not behaviourally proven."
coincidental_reliance_items:
  - truth: "The live harness measures the raised head gap on real browser pixels and goes red when the rendered gap leaves the BUBBLE_ICON_GAP_PX..+1 window (05-38)"
    reason: undeclared-precondition
    harden: "The window holds at 3.25 only because two undeclared terms cancel: +1 glyph ink-tail row (the scan's BLOCKED_COLORS misses bubble-blocked.json's non-distinctive bottom outline row, which resolveBubbleY anchors on) and -(scale-1)/scale of device-pixel rounding. Nothing in the harness declares either. Repainting that outline row in a distinctive colour would fail a correct renderer at 2.25. Declare the precondition by deriving the ink-tail row count from the sprite JSON (the harness already derives the palette that way) and comparing like for like."
unverified_prohibitions:
  - statement: "MUST NOT ship a state-signal overlay that is data-correct but practically illegible at stream scale"
    requirement_id: OFFICE-03
    verification: judgment
    disposition: "Mechanical floor re-proved in this process (live TRUTH 0/2/4: integer scale 3/4/6, 1088 blocked-glyph px, glyph 3.25 px above the seated owner's head). Beyond the prior round, this verifier also INSPECTED a captured 1280x720 frame: the blocked glyph (red bar-in-circle) and the waiting glyph (hourglass with a light upper bulb and dark lower bulb) are separable by silhouette at 1:1, both clear of their owners' heads. NON-AUTHORITATIVE LLM-judge verdict: supported. unverified-prohibition — human confirmation recommended (human item 1)."
  - statement: "MUST NOT treat mechanically painted dialogue pixels as proof the line is legible at stream scale"
    requirement_id: HANDOFF-02
    verification: judgment
    disposition: "Re-proved mechanically (live TRUTH 5: requested 6168 bubble px + 1364 text px, accepted 7809 + 1879, both boxes on the floor under the pair) AND visually: the captured frame shows `hands off to live-proo…` as a single readable verb-led sentence on its own plate, clear of desks and monitors. Readability at the CEO's viewing distance remains a judgment. unverified-prohibition — human confirmation recommended (human item 1)."
human_verification:
  - test: "Open .gsd/ or the session scratchpad frame `verif-shots/handoff.png` (1280x720, captured by this verifier at HEAD), or run the office live at your OBS source size with a blocked agent, a waiting agent and a handoff mid-sequence. Glance for one second."
    expected: "The hourglass reads as an hourglass without relying on its blue; the blocked sign is separable from it at a glance; the handoff line reads as a sentence ('hands off to …'), not a debug banner; the glyphs sit clear of their owners' heads and the seated agents read as sitting while the aisle sender reads as standing."
    why_human: "This is the CEO's own UAT round 3 finding (G-05-1a/1b/1c/2a, all four `severity: cosmetic`) and only the CEO can close it. Automated inspection was done first: this verifier captured and visually inspected the frame at HEAD and every element the UAT asked for is visibly changed — the arrow-joined pair of truncated ids is gone, the hourglass has a light upper bulb, the glyph gap tripled, the seated sink went 6 -> 10. What remains is the acceptance judgment at your viewing distance."
  - test: "Kill the API process while the office is open."
    expected: "A disconnect banner appears at the top of the page (role=\"status\"); the office does not freeze silently."
    why_human: "Declared `verification: backstop` in 05-40 — presence and wiring are explicitly not sufficient evidence, and no automated test or harness step exercises the socket close path."
---

# Phase 5: Pixel Office Renderer Verification Report

**Phase Goal:** The forked Pixel Agents office renders real company state on screen as a pure consumer of projections — never a fabricated animation.
**Verified:** 2026-09-23T18:30:00Z
**Status:** gaps_found
**Re-verification:** Yes — after the 05-38 / 05-39 / 05-40 UAT gap-closure round, re-verified against the whole phase goal rather than only the three plans.

## What This Verifier Ran Itself

Nothing below is taken from a SUMMARY.md or 05-REVIEW.md claim.

1. **The phase probe, twice, end to end.** `node scripts/verify-pixel-office-live.mjs` — real Docker Postgres reset, real migrations, real `apps/api` + `apps/web` dev servers, real worker credential, real events over the real `/ws/browser` relay, real headless Chromium reading back canvas pixels. **Exit 0, `LIVE PROOF: PASS`, TRUTH 0-7 green**, both runs. The second run wrote review frames to the session scratchpad at HEAD.
2. **Visual inspection of the captured frames.** `handoff.png` and `states.png` at 1280x720 were opened and read by this verifier before routing anything to a human, per the project's browser-test-first rule. They are the evidence behind the G-05-1a/1b/1c/2a closure claims above.
3. **A reproduction of the review's CR-01**, written as a temporary vitest case driving the REAL choreography (upsert, `agent.handoff_requested`, 600 ticks, `agent.handoff_completed`) with a registered empty title. It printed `"hands  to agent-b"` and `"agent-b accepts "`, and `"hands     to agent-b"` for a whitespace title. The file was deleted immediately; `git status` shows no source change.
4. **Independent measurement of the 05-39 asset**, decoding `bubble-waiting.json` and computing relative luminance and WCAG contrast from the palette rather than trusting the summary's numbers.
5. **Both suites, uncached, in this process:** `vitest run --root packages/pixel-office` → **207 passed (8 files)**; `vitest run --root apps/web` → **24 passed (3 files)**.
6. **Source reads of every 05-REVIEW finding that bears on a must_have** — CR-01, WR-01, WR-02, WR-03, WR-04, WR-05, IN-02, IN-06 — checked against the files. All are factually correct. Two of them falsify a must_have (the gaps); the rest are recorded as advisories with the reasoning.

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | **SC1** — agents visible on one floor, sprites/animations matching their real current state across all 15 statuses | ✓ VERIFIED | `status-mapping.ts:35` is `Record<AgentStatus, StatusVisual>` with 15 explicit entries and no fallback branch (a missing key is a compile error). Live TRUTH 1 re-run here: **4128 sprite px** painted from real POSTed events on an already-open page. |
| 2 | **SC2** — a blocked or waiting agent is visually distinguishable at a glance from an active one | ✓ VERIFIED | Non-colour signals: frozen pose (`frozen: true` on BLOCKED / WAITING_FOR_AGENT / WAITING_FOR_CEO), >= 20 differing silhouette cells between each stuck pair, glyphs drawn as the top pass. Live TRUTH 2: **1088 blocked-glyph px**, owner-bound, 3.25 px above the seated owner's head (TRUTH 4). Visually confirmed in the captured frame: red bar-in-circle vs hourglass, separable at 1:1. |
| 3 | **SC3** — handoff shows walk over, task icon, accept, move to work; dialogue deterministic and template-based, never LLM at render time | ✓ VERIFIED | Live TRUTH 3: icon appeared during the walk and cleared on completion. TRUTH 5: both lines captured (requested 6168 bubble + 1364 text px; accepted 7809 + 1879). `dialogue-templates.ts` is a static two-entry map of pure functions. Repo grep over `packages/pixel-office/src/**/*.ts` (tests excluded) for `Math.random`/`Date.now`/`setTimeout`/`setInterval`/`fetch(`/`XMLHttpRequest`/`WebSocket`/dynamic `import(`: **two comment lines, zero code hits**. |
| 4 | **SC4** — Pixel Agents attribution and licence notices remain visible and preserved | ✓ VERIFIED | Checked properly, not just "a LICENSE exists": `packages/pixel-office/LICENSE` reproduces the upstream **MIT text verbatim** with `Copyright (c) 2026 Pablo De Lucca` and pins commit `3537e140…`; **8 source files** under `packages/pixel-office/src` carry the 3-line fork header (spot-read `engine/renderer.ts`, `layout/tileMap.ts`); `references/ASSET-LICENSES.md` is 193 lines and §2 maps the fork's code to that LICENSE. In the UI the footer is **unconditional** (App.tsx:178-196 — no `&&`, no `<details>`, no `hidden`), `App.test.tsx` pins the exact sentence and asserts it is never hidden, and this verifier **read the sentence off a captured 1280x720 frame**. Live TRUTH 0: credit present and backed by `rgb(58,58,92)` at all three viewports. |
| 5 | **SC5** — furnished floor; agents sit at desk seats; never walk through furniture or another seated agent | ✓ VERIFIED | `office-layout.json` is the single source: 16 seats, 30 furniture entries, `interaction { row: 6, colOffsets: [1,-1,3,-3,5,-5] }`. Live TRUTH 6: **8 desks painted, 0 bare-floor px** (17760 desk px each). TRUTH 7: 59.6 fps with 9 agents. Captured frame shows four desk pods with monitors, walking lanes, wall band and decor. |
| 6 | **05-38 / G-05-1c** — a status glyph leaves BUBBLE_ICON_GAP_PX (3) unzoomed px between its lowest ink row and its owner's visible head, in every pose, at zoom 1 and 3 | ✓ VERIFIED | `BUBBLE_ICON_GAP_PX = 3` (constants.ts:35), consumed at renderer.ts:176 and :302. renderer.test.ts:260-269 asserts `inkBottom + BUBBLE_ICON_GAP_PX * zoom === headTop` across every glyph x pose x zoom — a behavioural equality that goes red if the renderer stops applying the gap — plus a literal pin at :262 that catches a silent constant revert. Live: `gap 3.25` measured on browser pixels. |
| 7 | **05-38 / G-05-2a** — a seated agent shows >= 12 fewer visible body rows than the same agent standing, and >= 10 rows still survive so the head is not buried | ✓ VERIFIED | `CHARACTER_SITTING_OFFSET_PX = 10` (constants.ts:29) applied at renderer.ts:519 only for a non-WALK character on its OWN seat. renderer.test.ts:1276-1306 measures `visibleBodyRows` off the COMPOSITED frame (not from the constant) for both the >= 12 delta and the >= 10 head-survival floor. Visually confirmed: aisle sender full-body with legs, seated agents head-and-shoulders. |
| 8 | **05-38** — the live harness measures the raised head gap on real browser pixels and goes red when the rendered gap leaves the window | ✓ VERIFIED (coincidental-reliance) | The bound fires for a RENDERER regression (dropping the gap measures ~0.25, under the floor). It cannot catch a CONSTANT revert (both sides read `BUBBLE_ICON_GAP_PX`) — that case is renderer.test.ts:262's job. Flagged because the window is satisfied at 3.25 by two undeclared terms cancelling (+1 glyph ink tail, −(scale−1)/scale rounding); see `coincidental_reliance_items`. Advisory only; score and status unchanged. |
| 9 | **05-39 / G-05-1a** — the waiting glyph reads as an hourglass by luminance structure alone: dark plate, light upper bulb, pinch, dark lower bulb, dark plate | ✓ VERIFIED | Four cases at bubbleSprites.test.ts:255-300 derive `sand`/`glass` by LUMINANCE ORDER and `waistRow` from the silhouette — no hex literal, so they keep testing the property if the palette is retuned. Verified visually in the captured frame: light upper bulb, dark lower bulb, pinched waist. |
| 10 | **05-39** — light fill and sand fill differ by >= 3:1 WCAG contrast, so the empty/full split survives desaturation | ✓ VERIFIED | Measured independently by this verifier from `bubble-waiting.json`: `#c8d4ff` relLum 0.666 vs `#4361ee` relLum 0.159 → **contrast 3.42**. Outline `#000000` is strictly darker than both, satisfying the plate assertion. |
| 11 | **05-39** — above the waist light outnumbers sand >= 3:1; below the waist sand outnumbers light | ✓ VERIFIED | Asserted per band at bubbleSprites.test.ts:276-292 with the counts printed in the failure message; both halves of the repaint (palette key and the two reassigned rows) are individually load-bearing. |
| 12 | **05-40 / G-05-1b** — a handoff bubble never presents a task id **or a blank string** as a title; with no human-readable title the line says what is happening | ✗ FAILED | The id half holds (guard describe at handoff-choreography.test.ts:1552 checks a 10-char id PREFIX so a `?? taskId` revert cannot slip through the cap). The blank half does not: **reproduced by this verifier through the real choreography** — `"hands  to agent-b"`, `"agent-b accepts "`, `"hands     to agent-b"`. Reachable at a trust boundary (`title: z.string()`, App.tsx:85 unguarded while App.tsx:70 guards). See gap 1. |
| 13 | **05-40** — both lines are verb-led English, so a bubble reads as an action even when one interpolated value is truncated | ✓ VERIFIED | dialogue-templates.ts:14-20; no U+2192 in any of the four outputs. Live frame reads `hands off to live-proo…` — one sentence, the truncation at the end rather than on both operands. |
| 14 | **05-40** — resolveHandoffDialogue stays a static two-entry map keyed by kind, two length-capped interpolations, zero network or LLM call surface | ✓ VERIFIED | Exactly two keys; the module imports nothing. Repo-wide grep (truth 3) confirms zero network/timer/random surface in the whole package outside two comments. |
| 15 | **05-40** — every possible line fits MAX_DIALOGUE_LINE_CHARS code points and the bubble's world-px budget | ✓ VERIFIED | The budget test enumerates all four kind x null/present combinations, **derives** the max rather than pinning which one wins, and asserts `toBe(MAX_DIALOGUE_LINE_CHARS)` — so it fails on slack as well as on overflow. (The 40-scene sweep's separate "widest line" claim is weaker than it says; advisory WR-04.) |
| 16 | **05-40 / prior WR-01** — a drawn bubble's rect is attributed by the same record identity as its speaker | ✓ VERIFIED | `framePlacements.push({ speakerId, taskId: l.ch.bubbleTextTaskId ?? null, ... })` (renderer.ts:577); `getDialogueBox(agentId: string, taskId: string)` matches on it (renderer.ts:492-495), so a `null`-stamped placement is unmatchable by construction; `getActiveHandoffs` passes `record.taskId` (:443) — the same identity `showsLineOf` reads. New case at handoff-choreography.test.ts:1456 covers two successive records from one speaker with byte-identical lines. The prior round's coincidental-reliance flag on `box: null` is retired. |
| 17 | **05-40** — getActiveHandoffs exposes the untruncated title with participants, phase and the drawn rect, under the same "an id is not a title" rule | ✗ PARTIAL | `fullTitle: getTaskTitle(record.taskId) ?? record.taskId` (handoff-choreography.ts:434) in a non-nullable field documented as "the UNTRUNCATED title". Two of three sites were fixed; the defect moved to the public API Phase 6 will consume. Three docblocks are now stale (cap 12 vs 14, "the exact value the bubble interpolates", index.ts:58-61). See gap 2. |
| 18 | **05-40 (backstop)** — a socket close or error surfaces a user-observable disconnect banner with `role="status"` | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present and wired: one `markDisconnected` registered on both `close` and `error` (App.tsx:105-107) driving the `role="status"` banner (:134-136). Declared `verification: backstop`, so presence is explicitly not evidence — and no apps/web test exercises the transition, nor does the harness close the socket. Routed to human verification; not counted as verified. |
| 19 | **05-36 (regression)** — the attribution footer carries an unconditional `background: WALL_COLOR` | ✓ VERIFIED | App.tsx:190, inline, no conditional. `App.test.tsx:114` / `:137` assert `backgroundDeclarations(markup,"footer")` **by equality**, so both a black strip and a missing backdrop go red. Live TRUTH 0: `credit backed by rgb(58, 58, 92)` at all three viewports, **0 near-black px**. |
| 20 | **05-37 (regression)** — a handoff's aisle slot is reserved for the record's whole lifetime and released only at retirement | ✓ VERIFIED | handoff-choreography.ts:112 `if (record.fromChar !== fromChar) taken.push(record.target)` — no phase exemption; `handoffs.delete` appears exactly once, inside `retireHandoff` (:183). Named regression tests pass in this verifier's 207-test run. |
| 21 | **05-37 (regression)** — the identity rule governs the clear paths as well as the read path | ✓ VERIFIED | `showsLineOf(ch, record) => ch.bubbleTextTaskId === record.taskId` (:155-156) gates the clear paths at :185/:190/:267 and the read path at :420/:426; `clearLine` nulls text, partner and stamp together (:161-164). |
| 22 | **Phase Core Value** — the renderer is a pure consumer of projections, never a fabricated animation | ✓ VERIFIED | Zero `Math.random` / `Date.now` / timer / `fetch` / `WebSocket` / dynamic `import(` in `packages/pixel-office/src` outside two comment lines. Every pixel in the live run originated in a real POSTed event reduced by `company-core` and relayed over `/ws/browser`: 4128 sprite px, 1088 glyph px, a full handoff sequence with both lines, 8 desks, 59.6 fps. |

**Score:** 19/22 truths verified (1 present, behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Stream-safety filtering of `TaskState.title` before it reaches rendered dialogue | Phase 7 | Phase 7 goal: data classified and filtered by visibility before it leaves the server. `dialogue-templates.ts` bounds length only and says so in-source (SAFE-01/02). |
| 2 | Hover / click / dashboard UI over `getActiveHandoffs()`'s title and rect | Phase 6 | Phase 6 is CEO Dashboard & Approval Workflow; 05-35's must_have scopes the UI out. |
| 3 | A hideable-for-stream attribution footer | later phase | 05-UAT.md round 3 Deferred Follow-Ups — a new capability needing a licence-compliance decision, not a defect. |

### Advisory (New Scope, Unevidenced)

| # | Finding | Category | Why Advisory |
|---|---------|----------|--------------|
| 1 | **WR-04** — the 40-scene sweep pins the accepted line (33 cps) as "widest" while the requested line (34 cps) is rendered in every scene and asserted nowhere | other | Confirmed. The line-budget truth is carried non-vacuously by the templates budget test; only the sweep's self-description is wrong. |
| 2 | **WR-05** — TRUTH 4's head-gap window is satisfied by an asset artefact (+1 ink-tail row) cancelling device-pixel rounding | other | Confirmed **numerically by this verifier's own run** (`gap 3.25`). The renderer-regression case is still caught; the residual risk is a false red after a future glyph repaint. Also recorded as coincidental-reliance. |
| 3 | **IN-06** — the floor-legibility `max()`'s fill term is dead; the near-black outline satisfies it unconditionally (3.79 vs the 3.0 floor) | other | Confirmed by computation. The 05-39 repaint fixed a real fill-legibility gap (2.98 → 3.77) that this guard could not see either way. |
| 4 | **WR-02 (side-tail Y)** — the right/left tail Y is independent of the clamped winner `y` | architectural | Confirmed, carried. Unreachable on the shipped layout (rows 4/6/8); 05-38's deeper sink narrows the window further. Nothing guards it. |
| 5 | **WR-03** — opaque fixed footer band over the canvas at sub-minimum viewports | other | Confirmed, carried — but **closed as a product call by human UAT round 3** (`result: pass`, measured per-row from live frames). No longer a human item. |
| 6 | **IN-01..IN-07** — seven info-level review items, all confirmed present | other | None changes a shipped behaviour. IN-02 (a dead record's aisle slot held for up to one tick) is the one worth folding into the gap-closure round. |
| 7 | 05-UI-SPEC.md line 102 still documents four interaction column offsets; the JSON carries six | other | Documentation only; carried unresolved from the previous round. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/pixel-office/src/constants.ts` | `BUBBLE_ICON_GAP_PX = 3`, `CHARACTER_SITTING_OFFSET_PX = 10` | ✓ VERIFIED | Both raised values consumed multiplicatively at renderer.ts:176, :302, :519; docblocks name the plan, direction and guard. |
| `packages/pixel-office/src/engine/renderer.test.ts` | revert guards for both constants, composited-frame geometry | ✓ VERIFIED | :260-269 gap equality per glyph/pose/zoom; :1276-1306 seated/standing body-row delta and head-survival floor, both measured off the render. |
| `packages/pixel-office/src/sprites/bubble-waiting.json` | repainted for luminance separation, silhouette untouched | ✓ VERIFIED | Decoded here: 13x11, palette `{1:#000000, 2:#4361ee, 3:#c8d4ff}`, glass/sand contrast 3.42. |
| `packages/pixel-office/src/sprites/bubbleSprites.test.ts` | grayscale-structure guard derived from the asset | ✓ VERIFIED | Four cases keyed on luminance order and the derived waist row; no hex literal. (The separate floor-contrast `max()` has a dead fill term — advisory 3.) |
| `packages/pixel-office/src/handoff/dialogue-templates.ts` | verb-led nullable-title templates, one declared line budget | ⚠️ SUBSTANTIVE, one hole | Templates, caps (14/10/34) and the pure-function shape are exactly as specified; `null` is handled, **blank is not** (gap 1). |
| `packages/pixel-office/src/handoff/handoff-choreography.ts` | no id painted as a title; identity-keyed rect lookup | ⚠️ WIRED, one residual | Canvas call sites :279/:319 correct; rect lookup :443 correct; **host read path :434 still `?? record.taskId`** (gap 2), and three docblocks are stale. |
| `packages/pixel-office/src/engine/renderer.ts` | `framePlacements` keyed on `taskId`, `getDialogueBox(agentId, taskId)` | ✓ VERIFIED | :577 stamp, :492-495 lookup with a `taskId: string` parameter so a `null` stamp is unmatchable by construction. |
| `apps/web/src/App.tsx` | unconditional attribution footer with its own backdrop; disconnect banner | ⚠️ VERIFIED, one unguarded path | Footer and backdrop verified and live-proved. `registerTaskTitle` at :85 is unguarded where :70 guards (gap 1). Disconnect banner present but behaviour unexercised (truth 18). |
| `packages/pixel-office/LICENSE` + `references/ASSET-LICENSES.md` | upstream MIT preserved and auditable | ✓ VERIFIED | MIT text verbatim with copyright holder and pinned commit; 193-line audit; 8 forked sources carry the 3-line header. |
| `scripts/verify-pixel-office-live.mjs` | TRUTH 0-7 on real browser pixels | ✓ VERIFIED | **Run twice by this verifier, exit 0 both times**, review frames written and inspected. |
| `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` | updated for 05-38 and 05-40 | ⚠️ UPDATED, one stale clause | Both corrections recorded (line 102's sitting-offset and dialogue rows); the interaction-offset count is still stale (advisory 7). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `BUBBLE_ICON_GAP_PX` | glyph Y and dialogue `aboveY` | renderer.ts:176, :302 | WIRED | Multiplicative by `zoom`; unit equality guard per pose and zoom. |
| `CHARACTER_SITTING_OFFSET_PX` | `renderScene` sitting offset | renderer.ts:519, gated on `state !== WALK && isOwnSeat(ch)` | WIRED | Off-seat and walking characters are unaffected; measured on the composite. |
| `bubble-waiting.json` palette | `resolveBubbleSprite` → glyph pass | sprites index → renderer pass 3 | WIRED | 1088 glyph px on canvas live; hourglass visible in the captured frame. |
| `getTaskTitle(taskId)` | `resolveHandoffDialogue(kind, title, name)` | `?? null` at :279 and :319 | ⚠️ PARTIAL | Correct for `undefined`; a blank string flows through as a title (gap 1). |
| `Character.bubbleTextTaskId` | `framePlacements.taskId` → `getDialogueBox` → `ActiveHandoff.box` | one identity, five call sites | WIRED | Speaker attribution and rect attribution cannot disagree (prior WR-01 closed). |
| `getTaskTitle(record.taskId)` | `ActiveHandoff.fullTitle` | `?? record.taskId` | ⚠️ PARTIAL | Presents an id as a title on the API Phase 6 consumes (gap 2). |
| `HandoffRecord.target` | `interactionTileFor`'s `taken` set | every live record, no phase exemption | WIRED | :112; released only by `retireHandoff` (:183). |
| WS `onEvent` | office | `applyLiveEvent` → `upsertCharacterFromAgent`, THEN `handleHandoffEvent` | WIRED | App.tsx:75-99; the load-bearing ordering comment is intact; live-proved end to end. |
| socket `close`/`error` | `role="status"` banner | `markDisconnected` → `disconnected` state | WIRED, UNEXERCISED | App.tsx:105-107 → :134-136; no test or harness step drives it (truth 18). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| Characters on canvas | `AgentStatus` per agent | WS snapshot + company-core `reduce` over relayed events | Yes — 4128 sprite px from real POSTed events in this run | ✓ FLOWING |
| Blocked / waiting glyph | `statusBubble` | same reducer path | Yes — 1088 glyph px, owner-bound, 3.25 px above the head | ✓ FLOWING |
| Handoff label text | `TaskState.title` / agent name | `registerTaskTitle` from snapshot + `task.created` | Yes — `hands off to live-proo…` observed on canvas | ⚠️ STATIC on one input class — a blank registered title yields a malformed sentence (gap 1) |
| `getActiveHandoffs().fullTitle` | untruncated title | `getTaskTitle(taskId) ?? record.taskId` | Yes, but the fallback is an id in a title field | ⚠️ HOLLOW (gap 2) |
| `getActiveHandoffs().box` | drawn bubble rect | renderer's per-frame `framePlacements`, keyed on `taskId` | Yes, or `null` — never another record's rect | ✓ FLOWING |
| Footer backdrop | `WALL_COLOR` | `constants.ts`, one constant, two consumers | Yes — `rgb(58,58,92)` measured in the live DOM at three viewports | ✓ FLOWING |
| Furniture / tiles | sprite pixel data | `office-metrocity.json` + `office-layout.json` | Yes — 8 desks at 17760 px, 0 bare-floor px | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Blank-title handoff line (review CR-01) | temporary vitest case driving the real choreography with `registerTaskTitle(task, "")` | `"hands  to agent-b"` / `"agent-b accepts "`; whitespace → `"hands     to agent-b"` | ✗ FAIL (gap 1) |
| Engine suite | `npx vitest run --root packages/pixel-office` | **207 passed (8 files)** | ✓ PASS |
| Web suite | `npx vitest run --root apps/web` | **24 passed (3 files)** | ✓ PASS |
| Waiting-glyph luminance separation | decode `bubble-waiting.json`, compute relLum + WCAG contrast | sand 0.159, glass 0.666, contrast **3.42** | ✓ PASS |
| Floor-contrast guard's fill term | compute `contrast(#000000, #8d5f24)` | **3.79** ≥ 3 → the `max()` never consults the fill | ℹ️ dead term (advisory 3) |
| Pure-consumer property | repo grep for `Math.random\|Date.now\|setTimeout\|setInterval\|fetch(\|XMLHttpRequest\|WebSocket\|import(` over `packages/pixel-office/src` excluding tests | 2 hits, both comment lines | ✓ PASS |
| Attribution preserved | read `packages/pixel-office/LICENSE`, count fork headers, read the footer off a captured frame | MIT verbatim + 8 headered files + sentence legible at 1280x720 | ✓ PASS |

No source file was modified by this verification; the temporary repro test was deleted and `git status` shows only pre-existing untracked planning artefacts.

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| Live end-to-end proof (this phase's declared probe; no `scripts/*/tests/probe-*.sh` exists) | `node scripts/verify-pixel-office-live.mjs` | Docker Postgres reset + migrations + real api/web dev servers + real worker credential + Playwright canvas readback. TRUTH 0 (three viewports, credit backed at each, 0 near-black px), 1, 2, 3, 4, 5, 6, 7 all PASS → `LIVE PROOF: PASS`, **exit 0** | ✓ PASS |
| Same probe with review frames | `PIXEL_OFFICE_SHOTS=<scratchpad> node scripts/verify-pixel-office-live.mjs` | Exit 0; `handoff.png`, `states.png`, `cohort.png` and three viewport frames written at HEAD and visually inspected by this verifier | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| OFFICE-01 | 05-38 (+ 24 earlier) | Forked renderer, one floor, sprites/animations reflect all 15 states | ✓ SATISFIED | Truths 1, 5, 7, 22 |
| OFFICE-02 | (10 plans) | Fork attribution and licence notices preserved | ✓ SATISFIED | Truth 4 — MIT verbatim, 8 fork headers, 193-line audit, unconditional footer read off a live frame |
| OFFICE-03 | 05-38, 05-39 (+ 14 earlier) | Blocked / waiting agents show a clear glanceable signal | ✓ SATISFIED (mechanically + visually) | Truths 2, 6, 9, 10, 11. Glanceability at the CEO's viewing distance stays a judgment-tier prohibition — human item 1. |
| HANDOFF-01 | 05-40 (+ 19 earlier) | Walk over, task icon, accept, move to work | ✓ SATISFIED | Truths 3, 16, 20, 21; live TRUTH 3/5 |
| HANDOFF-02 | 05-40 (+ 11 earlier) | Deterministic / template-based dialogue, never LLM at render time | ⚠️ SATISFIED WITH A DEFECT | Truths 13, 14, 15 hold and the determinism property is absolute (truth 22). But truth 12 fails for a blank title, so the rendered sentence can be malformed — the dialogue is deterministic and still wrong. Gap 1. |

All 5 IDs declared across the 40 plans appear in REQUIREMENTS.md mapped to Phase 5 (lines 31-33, 45-46, 134-138), and REQUIREMENTS.md maps no additional ID to Phase 5. **No orphaned requirements.** The ledger's status column, stale in the previous round, is now reconciled to `Complete` for all five — note that HANDOFF-02's row is now slightly ahead of this verification.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` | — | **None** in any file this round modified (constants.ts, renderer.ts/.test.ts, dialogue-templates.ts/.test.ts, handoff-choreography.ts/.test.ts, bubble-waiting.json, bubbleSprites.test.ts, verify-pixel-office-live.mjs) |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` / placeholder prose | — | **None** |
| — | — | `it.skip` / `describe.skip` / `.only` / `it.todo` | — | **None** in `packages/pixel-office/src` or `apps/web/src` |
| `packages/pixel-office/src/handoff/dialogue-templates.ts` | 49-58 | Guard that covers one of two absent-value shapes (`null` but not blank) at a value that reaches the screen | 🛑 Blocker | Gap 1 — reproduced. |
| `packages/pixel-office/src/handoff/handoff-choreography.ts` | 434 | Non-nullable field documented as a title, filled with an identifier | ⚠️ Warning | Gap 2 — no consumer yet; Phase 6 is the declared one. |
| `packages/pixel-office/src/handoff/handoff-choreography.test.ts` | 1468-1469 | Tautological assertion (`expect(f(x)).toBe(f(x))`) labelled "asserted, never assumed" | ℹ️ Info | IN-01. The claim it states is really carried three lines later at :1475, so coverage is intact. |
| `packages/pixel-office/src/engine/renderer.test.ts` | 613-615 | Non-vacuity assertion naming a property it does not check | ⚠️ Warning | Advisory 1 (WR-04). |

Every `ponytail:` comment in the changed files names a real ceiling with an upgrade path. None is an unreferenced debt marker.

### Human Verification Required

#### 1. Close your own UAT round: the polished frame

**Test:** Open the captured 1280x720 frame (`handoff.png` in this session's `verif-shots` scratchpad), or run the office live at your OBS source size with a blocked agent, a waiting agent and a handoff mid-sequence.
**Expected:** The hourglass reads as an hourglass without relying on its blue; the blocked sign is separable from it at a glance; the handoff line reads as a sentence (`hands off to …`), not a debug banner; the glyphs sit clear of their owners' heads; seated agents read as sitting while the aisle sender reads as standing.
**Why human:** These are your four `severity: cosmetic` UAT gaps (G-05-1a/1b/1c/2a) and only you can accept them closed. Automated inspection was done first, per the project rule: this verifier captured the frame at HEAD and read it — the arrow-joined pair of truncated ids is gone, the hourglass has a light upper bulb against a dark lower one, the glyph gap tripled to 3 px (measured 3.25 live), and the seated sink went 6 → 10 px. What is left is your acceptance judgment at viewing distance.

#### 2. The disconnect banner (backstop truth)

**Test:** Open the office against a running API, then kill the API process.
**Expected:** A `role="status"` banner appears at the top saying the live feed dropped; the office does not freeze silently.
**Why human:** 05-40 declares this `verification: backstop`, which explicitly rules out presence-and-wiring as evidence. The code is present and wired (App.tsx:105-107 → :134-136), but no test and no harness step exercises the socket close path.

### Gaps Summary

**Two gaps, one concern, one file each, both introduced by 05-40's own change of shape.** Everything the UAT round actually asked for is closed and visible in a frame this verifier captured and looked at: the hourglass reads as an hourglass, the handoff reads as a sentence, the glyph is detached from the head, and a seated agent is plainly seated. The phase goal itself — *real company state on screen as a pure consumer of projections* — is demonstrated rather than asserted, twice, end to end on real browser pixels.

What 05-40 did not finish is the rule it wrote down: *a task id is not a title*. It applied that rule at two of three sites and to one of two absent-value shapes.

1. **Blank is not "absent".** `??` catches `undefined`, so a registered empty or whitespace title takes the *present* branch and paints `"agent-b accepts "` — a sentence with no object, which is the same class of frame the phase was re-opened to eliminate and strictly worse than the id it replaced. This verifier reproduced it through the real choreography, not by reading the review. It is reachable through the production pipeline: `TaskCreatedPayload.title` is `z.string()` with no `.min(1)`, so `POST /events` accepts it from any worker credential, and `App.tsx:85` registers it unguarded while the sibling site three lines above guards exactly the same value. It is invisible today only because no in-repo producer emits `task.created` at all — the moment Phase 6 or a real worker does, it is a live defect. The fix is one expression in the resolver, where every present and future caller routes through it, plus two test inputs.

2. **The rule stopped at the canvas.** `ActiveHandoff.fullTitle` still hands a caller `task-9f3c1b7e-…` in a non-nullable field its own docblock calls "the UNTRUNCATED title". Nothing consumes it in this phase, which is why it is `partial` rather than `failed` — but Phase 6 is the declared consumer, and three docblocks around it now describe behaviour that no longer exists.

Neither gap touches the phase's Core Value, its determinism contract, its attribution, its layout or its performance. Both are small, both are in the same two files, and both are the same sentence written twice: *never present an identifier or an empty string as a title*. Fold review IN-02 (a dead record's aisle slot held for a tick) into the same round if it is cheap.

On the review's own test-quality warnings, three guards in this phase cannot go red for the thing they name — WR-04's "widest line", WR-05's head-gap window (which this verifier's own live run measured at 3.25, exactly the predicted asset-artefact value), and IN-06's dead floor-contrast fill term. None of them falsifies a must_have, because in each case the property the must_have actually claimed is carried by a different, non-vacuous guard. They are recorded as advisories, and WR-05 additionally as a coincidental-reliance item, because a guard that passes for the wrong reason is one glyph repaint away from failing a correct renderer.

---

_Verified: 2026-09-23T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
