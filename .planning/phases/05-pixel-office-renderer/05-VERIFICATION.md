---
phase: 05-pixel-office-renderer
verified: 2026-09-23T14:40:00Z
status: human_needed
score: 20/20 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/phases/05-pixel-office-renderer/05-01-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-01-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-02-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-02-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-03-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-03-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-04-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-04-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-05-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-05-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-06-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-06-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-07-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-07-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-08-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-08-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-09-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-09-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-10-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-10-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-11-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-11-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-12-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-12-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-13-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-13-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-14-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-14-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-15-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-15-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-16-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-16-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-17-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-17-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-18-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-18-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-19-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-19-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-20-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-20-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-21-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-21-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-22-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-22-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-23-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-23-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-24-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-24-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-25-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-25-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-26-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-26-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-27-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-27-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-28-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-28-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-29-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-29-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-30-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-30-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-31-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-31-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-32-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-32-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-33-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-33-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-34-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-34-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-35-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-35-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-36-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-36-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-37-PLAN.md"
  - ".planning/phases/05-pixel-office-renderer/05-37-SUMMARY.md"
  - ".planning/phases/05-pixel-office-renderer/05-REVIEW.md"
  - ".planning/phases/05-pixel-office-renderer/05-UAT.md"
  - ".planning/phases/05-pixel-office-renderer/05-UI-SPEC.md"
  - "apps/web/index.html"
  - "apps/web/src/App.test.tsx"
  - "apps/web/src/App.tsx"
  - "packages/pixel-office/src/engine/renderer.ts"
  - "packages/pixel-office/src/handoff/dialogue-templates.ts"
  - "packages/pixel-office/src/handoff/handoff-choreography.test.ts"
  - "packages/pixel-office/src/handoff/handoff-choreography.ts"
  - "packages/pixel-office/src/index.ts"
  - "packages/pixel-office/src/layout/office-layout.json"
  - "packages/pixel-office/src/layout/officeLayout.ts"
  - "packages/pixel-office/src/types.ts"
  - "scripts/verify-pixel-office-live.mjs"
covered_digest: "v1:sha256:c45c76b1035e2b30d48984f93d0a633195d96d8ae6781be7ebccd0d86f5b8340"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 11/12
  gaps_closed:
    - "G-05-P6 / WR-02 (05-36): the attribution footer now carries an unconditional `background: WALL_COLOR` (App.tsx:190) — not behind a conditional, not viewport- or scroll-dependent. Live harness re-run by this verifier in its own process: `credit backed by rgb(58, 58, 92) (#3A3A5C) at every viewport`, asserted OUTSIDE the `fits` branch at 800x480 (overflows), 1920x1080 and 1280x720, with `0 near-black px` at all three. Mutation-proved non-vacuous: deleting `background: WALL_COLOR` from App.tsx turns `App.test.tsx > backs the attribution with the office border colour` red (`expected [] to deeply equal [ 'background:#3a3a5c' ]`)."
    - "WR-07 (05-37): speaker attribution is now `showsLineOf(ch, record) => ch.bubbleTextTaskId === record.taskId` (handoff-choreography.ts:155-156), used by both clear paths (:185, :190, :268) and the read path (:420, :426). Mutation-proved non-vacuous: replacing the identity predicate with a text-presence check turns `colliding dialogue lines (review WR-07) > only the record whose line the receiver is actually showing reports a speaker and a box` red (`two records claimed one bubble: expected [...] to have a length of 1 but got 2`)."
    - "WR-08 (05-37): `interactionTileFor`'s reservation loop is `if (record.fromChar !== fromChar) taken.push(record.target)` (handoff-choreography.ts:112) — the `phase !== \"RETURNING_TO_DESK\"` exemption is gone, and `handoffs.delete` appears exactly once, inside `retireHandoff` (:183), so the reservation is bounded by the record. Mutation-proved non-vacuous: re-adding the exemption turns `a departing sender keeps its aisle slot reserved until it is home` red (`the second sender was handed the tile the first is still standing on`)."
  gaps_remaining: []
  regressions: []
advisory:
  - finding: "05-REVIEW WR-01: WR-07's record-identity rule stops at the FSM boundary. `renderer.ts:450/570` still stamps `text: l.ch.bubbleText ?? \"\"` into `framePlacements` and `getDialogueBox(agentId, text)` (:485-486) still matches on that string, so `getActiveHandoffs` resolves `speakerId` by stamp and `box` by text. CONFIRMED by source read."
    category: architectural
    reason: "Not a must_have failure: the must_have's stated behaviour ('the record whose line was overwritten reports speakerId null and box null rather than another record's rect') does hold, because `box` short-circuits on `speakerId === null` (handoff-choreography.ts:437). No deterministic evidence of a wrong rect either: `framePlacements` is cleared every frame (renderer.ts:556) and a character draws at most one bubble per frame, so for the same speaker the text lookup can only return the rect of the bubble that is on screen right now. Resolves by carrying `bubbleTextTaskId` into the frame record and keying the lookup on it (the review's own fix). Worth closing before Phase 6 puts a hit-test consumer on `box`."
    evidence_status: "none provided (source read; no failing test, no reproducible wrong rect)"
  - finding: "05-REVIEW WR-04: the new footer guards assert `footer background === WALL_COLOR` against the same constant the app renders from (App.test.tsx imports it; verify-pixel-office-live.mjs regex-reads it from constants.ts), and the text colour `#cccccc` is pinned nowhere. Changing WALL_COLOR moves both sides of the equality together, so the 6.74:1 ratio the comment cites can silently drop below AA while the suite stays green. CONFIRMED by source read."
    category: other
    reason: "Not a must_have failure — 05-36 must_have 3 asks only that the equality turn red for a black strip AND for a missing backdrop, and it does (mutation-proved above). This is a residual class the must_have did not claim. Resolves by asserting the computed WCAG ratio between two exported literals, keeping the equality alongside it."
    evidence_status: "none provided (source read; today's pair is fine — #cccccc on #3A3A5C is 6.74:1)"
  - finding: "05-REVIEW WR-05: `App.test.tsx`'s 'keeps the credit legible where the canvas overflows the viewport and scrolls under it' renders nothing at 800x480 — `markup` is one module-scope `renderToStaticMarkup` with no window — and its fourth assertion repeats the backdrop equality three lines above. CONFIRMED by source read."
    category: other
    reason: "Not a must_have failure: 05-36 must_have 4 asks for 'a unit-level GEOMETRY assertion in App.test.tsx' plus a third harness entry asserting the BACKDROP, and both exist. The geometry assertions are real (they go red if MIN_DISPLAY_SCALE drops or displayScaleFor stops clamping) and the actual overflow coverage is the harness's 800x480 entry, which this verifier ran (it asserts the overflow genuinely occurred before asserting anything about it). The defect is the test NAME overclaiming CI coverage. Resolves by renaming it to what it proves, or by a jsdom render at that viewport."
    evidence_status: "none provided (source read; no behaviour is unguarded — the harness covers it)"
  - finding: "05-REVIEW WR-03: making the footer opaque turned it into a ~19 CSS px band over the canvas. Below the 960x528 canvas floor the wrapper's `overflow: auto` means no scroll position clears the canvas's own bottom ~19 px. CONFIRMED by source read (App.tsx:124, :178-193)."
    category: other
    reason: "Not classified as a Blocker against SC2/OFFICE-03. The permanently-covered strip is the bottom ~19 CSS px of the canvas, i.e. the lower third of grid row 10 at scale 3. `office-layout.json` puts every seat on rows 4 and 8 and every standing spot on rows 4 and 8; row 10 is the bottom wall band the harness already asserts the footer sits on. State glyphs are head-anchored ABOVE a character's drawn head, so no glyph is reachable in that strip. At any other scroll position the band is transient — scrolling clears it. No reproducible glyph occlusion could be constructed. Resolves by `paddingBottom: FOOTER_HEIGHT_PX` on the scroll container, or by making the footer a flow element."
    evidence_status: "none provided (source + layout read; no reproducible occlusion of a state signal)"
  - finding: "05-REVIEW WR-02 (side-tail Y): `tailFor`'s `right`/`left` branches derive the tail's Y from `sideY` (renderer.ts:303), which is independent of the unconditionally clamped winner `y` (:374), so a clamped side winner paints its tail outside its own vertical span. CONFIRMED by source read."
    category: architectural
    reason: "Latent, not live: reaching it needs `speaker.headTop < floor.top`, which needs a character within about a tile of the top interior row, and the shipped layout keeps every character on rows 4/6/8. The new containment test at renderer.test.ts:576-582 explicitly excludes side tails, so nothing guards it. Resolves with the review's two-line clamp."
    evidence_status: "none provided (unreachable on the shipped layout; no failing test)"
  - finding: "05-REVIEW IN-01..IN-07 (unreachable `?? \"\"` empty-text placement; a dead record's slot held for up to one tick because the reservation loop skips `senderIsCurrent`; the null-slot fallback target reserved as though it were an aisle slot; no upper bound on sender-receiver distance at the widened ±5 offsets; `ReadonlySet` cast away at the `isWalkable` boundary; `readColorConst`'s unanchored regex; the disconnect banner keeping a translucent backdrop). All confirmed present by source read."
    category: other
    reason: "Seven info-level items. None changes a shipped behaviour and none is reproducible as a defect. IN-02 and IN-03 are the two worth queueing with WR-01 before Phase 6 raises concurrent-handoff volume."
    evidence_status: "none provided (source read; all confirmed present, none reproducible)"
  - finding: "Doc drift: 05-UI-SPEC.md line 102 still describes the interaction slot set as 'column offsets +1, -1, +3, -3', but `office-layout.json` carries six (`[1, -1, 3, -3, 5, -5]`) after WR-03 widened it. The same paragraph WAS updated for 05-37's WR-08 correction, so the omission is local."
    category: other
    reason: "Documentation only — the JSON is the single source and the tests enumerate it. Resolves by a one-line edit to the UI-SPEC paragraph."
    evidence_status: "none provided (no code impact)"
  - finding: "Bookkeeping drift: `.planning/REQUIREMENTS.md` line 33 leaves OFFICE-03 unchecked and line 136 maps it `Phase 5 | Gaps Found`, while OFFICE-02 — the ID the previous round actually failed — is marked Complete. Both are stale relative to this verification (OFFICE-03 was SATISFIED in the previous round; OFFICE-02's gap is closed in this one)."
    category: other
    reason: "Traceability bookkeeping, not implementation. Should be reconciled when the phase ships so the requirements ledger matches the verification record."
    evidence_status: "none provided (document state, not code)"
behavior_unverified_items: []
coincidental_reliance_items:
  - truth: "WR-07: the record whose line was overwritten reports speakerId null and box null rather than another record's rect"
    reason: incidental-ordering
    harden: "`box` is null here only because the expression short-circuits on `speakerId === null` (handoff-choreography.ts:437) — the box LOOKUP itself is still keyed on bubble text (WR-01). The guard test's message ('the overwritten record was handed another record's rect') therefore reads as attribution coverage it does not have. Declare the precondition by carrying `bubbleTextTaskId` into `framePlacements` and keying `getDialogueBox` on it, so the rect is attributed by the same identity as the speaker."
unverified_prohibitions:
  - statement: "MUST NOT ship a state-signal overlay that is data-correct but practically illegible at stream scale"
    requirement_id: OFFICE-03
    verification: judgment
    disposition: "Mechanical floor re-proved in this process: live TRUTH 0 gives integer scale 3/4/6 at three viewports, TRUTH 4 anchors the blocked glyph 1.25 px above the seated owner's head with 1088 glyph px. Legibility itself is a judgment. NON-AUTHORITATIVE LLM-judge verdict: supported. unverified-prohibition — human review recommended (item 1)."
  - statement: "MUST NOT treat mechanically painted dialogue pixels as proof the line is legible at stream scale"
    requirement_id: HANDOFF-02
    verification: judgment
    disposition: "Honoured in construction and re-proved mechanically (live TRUTH 5: requested 6791 bubble px + 1357 text px, accepted 8175 + 1821, both boxes on the floor under the pair). Readability remains a judgment. unverified-prohibition — human review recommended (item 1)."
human_verification:
  - test: "Open the office at your OBS source size (1280x720 or 1920x1080) with agents in blocked, waiting_for_ceo and waiting_for_agent, plus a handoff mid-sequence. Glance for one second."
    expected: "Each stuck agent and the KIND of stuck is identifiable; the hourglass reads as an hourglass without relying on its blue; the handoff line is readable, sits clear of desks and monitors, and does not read as a debug banner."
    why_human: "Legibility at viewing scale is a judgment. The harness proves geometry only (scale 4/6, 1.25 px head gap, bubble off every desk and glyph). This is UAT round 2 test 1, still `result: issue` in 05-UAT.md — the CEO has not seen a frame since the eight polish/gap fixes landed."
  - test: "Look at a populated office where at least one agent is blocked or waiting at its own desk while a handoff sender waits in the aisle."
    expected: "The agent at its desk clearly reads as sitting (the desk hides its lower body) and the aisle sender clearly reads as standing."
    why_human: "'Reads as sitting' is the exact judgment UAT round 2 polish item 3 raised; the test proves only a >= 8-row difference in visible body rows."
  - test: "Resize the browser below 960x528 (e.g. 800x480) and scroll the office."
    expected: "The attribution footer is legible on its own WALL_COLOR band (this part is now automated and passing), and the band does not hide anything you need — confirm you are content that the bottom ~19 px of the canvas cannot be scrolled clear of it."
    why_human: "Review finding WR-03. Analysis shows only grid row 10 (bottom wall, no seats, no standing spots, no head-anchored glyph) is permanently covered, so it is not classified as a blocker — but whether an opaque band over the canvas is acceptable at sub-minimum viewports is a product call."
---

# Phase 5: Pixel Office Renderer Verification Report

**Phase Goal:** The forked Pixel Agents office renders real company state on screen as a pure consumer of projections — never a fabricated animation.
**Verified:** 2026-09-23T14:40:00Z
**Status:** human_needed
**Re-verification:** Yes — after the 05-36 / 05-37 gap-closure round, re-verified against the whole phase goal rather than only the two fixes.

## What This Verifier Ran Itself

Nothing below is taken from a SUMMARY.md claim. Four things were executed in this process:

1. **The phase probe, end to end.** `node scripts/verify-pixel-office-live.mjs` — real Docker Postgres reset, real migrations, real `apps/api` + `apps/web` dev servers, real worker credential, real events over the real `/ws/browser` relay, real headless Chromium reading back canvas pixels. **Exit 0, `LIVE PROOF: PASS`**, TRUTH 0-7 all PASS including the newly added 800x480 overflow viewport.
2. **Three mutation checks**, to test whether the guards this round added are non-vacuous rather than believing they are. All three went red on the exact reverted behaviour (details in `re_verification.gaps_closed`).
3. **Source reads of every claim in 05-REVIEW.md that bears on a must_have** — WR-01, WR-03, WR-04, WR-05 — checked against the files, not against the review's or the summaries' word. All four claims are factually correct; none of them falsifies a must_have as written. Each is recorded in `advisory:` with the reasoning.
4. **Named-test runs** for the behaviour-dependent truths (slot reservation lifetime, identity-based clearing), plus the orchestrator's uncached full-workspace run (10/10 tasks, pixel-office 196, web 24).

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | **SC1** — agents visible on one floor, sprites/animations matching their real current state across all 15 statuses | ✓ VERIFIED | `status-mapping.ts` is a `Record<AgentStatus, StatusVisual>` with no fallback branch (a missing key is a compile error); unchanged by this round. Live TRUTH 1 re-run here: **5472 sprite px** painted from real POSTed events on an already-open page. |
| 2 | **SC2** — a blocked or waiting agent is visually distinguishable at a glance from an active one | ✓ VERIFIED | Three separable, non-colour-only signals: frozen pose, distinct glyph silhouette (>= 20 differing cells between each stuck pair; four 1x hourglass-silhouette tests), glyphs drawn as the top pass. Live TRUTH 2 re-run: **1088 blocked-glyph px** with no navigation between event and scan; TRUTH 4: glyph owner-bound, 1.25 px above the seated owner's head. |
| 3 | **SC3** — handoff shows walk over, task icon, accept, move to work; dialogue deterministic and template-based, never LLM at render time | ✓ VERIFIED | `dialogue-templates.ts` is a two-entry static map with two length-capped interpolations. Repo grep over `packages/pixel-office/src/**/*.ts` (tests excluded) for `Math.random`/`Date.now`/`setTimeout`/`setInterval`/`fetch(`/`XMLHttpRequest`/`WebSocket`/dynamic `import(`: **two comment lines, zero code hits**. Live TRUTH 3 re-run: icon appeared during the walk and cleared on completion; TRUTH 5 captured both the requested and the accepted line. |
| 4 | **SC4** — Pixel Agents attribution and licence notices remain visible and preserved | ✓ VERIFIED | `App.tsx:178-196` renders the full sentence unconditionally — no disclosure element, no gate. `App.test.tsx` pins the whole sentence, asserts `references/ASSET-LICENSES.md` exists with content, asserts it is never hidden. Live TRUTH 0 re-run: footer present and naming `pixel-agents-hq/pixel-agents` at **all three** viewports. |
| 5 | **SC5** — furnished floor; agents sit at desk seats; never walk through furniture or another seated agent | ✓ VERIFIED | `office-layout.json` is the single source (20x11, 8 desks, 16 seats on rows 4/8, 4 standing spots, aisle rows 3/6/7). Live TRUTH 6 re-run: **8 desks painted, 0 bare-floor px**; TRUTH 7: **59.6 fps with 9 agents**. |
| 6 | **G-05-P1** — every bubble is placed by scoring four ordered candidates against the frame's real obstacles, ranked and clamped | ✓ VERIFIED | `resolveDialogueBox` (renderer.ts:286-376), untouched since `fd6c867` (before this round). Live TRUTH 5 re-run: requested box x 58.75..133.5 y 106..115; accepted box x 42.5..133.75 y 27..36 — both on the floor, tail on the speaker. |
| 7 | **G-05-P2** — the sender waits on a fixed aisle slot from layout data, >= 2 tiles from every home; a second concurrent sender takes the next slot | ✓ VERIFIED | `interaction: { row: 6, colOffsets: [1,-1,3,-3,5,-5] }` → `interactionSlotsFor` → `interactionTileFor` (Chebyshev-1 rejection + real `findPath`). Live TRUTH 5: sender on the aisle at (6,6), receiver seated at (5,4), both fully visible (4544 / 7248 agent px). |
| 8 | **G-05-P3** — any character resting on its own seat is drawn seated whatever its status; only an off-seat character stands | ✓ VERIFIED | `renderer.ts:512` tests `ch.state !== CharacterState.WALK && isOwnSeat(ch)`, not TYPE. Unchanged by this round; named tests pass in the full run. Live TRUTH 4 measures the glyph against the SEATED owner's head. |
| 9 | **G-05-P4** — the 12-code-point cap is kept while the host can read the untruncated title and the drawn bubble rect, read-only | ✓ VERIFIED | `MAX_DIALOGUE_TITLE_CHARS = 12` (dialogue-templates.ts:17). `getActiveHandoffs()` returns fresh plain objects carrying `fullTitle`/`fromAgentId`/`toAgentId`/`phase`/`speakerId`/`box`. Read-only re-proved under the new stamp by `stays read-only with the stamp in play`. |
| 10 | **G-05-P5** — the waiting_for_agent glyph is a true hourglass at 1x, identifiable by silhouette alone | ✓ VERIFIED | `bubble-waiting.json` unchanged this round; four named 1x-silhouette tests plus the >= 20-cell separation test pass in the full run. |
| 11 | **G-05-P6** — the canvas is sized from the full viewport, centred, remainder in WALL_COLOR, no black area | ✓ VERIFIED | Live TRUTH 0 re-run at **three** viewports: 800x480 → scale 3 (960x528, overflows), 1920x1080 → scale 6, 1280x720 → scale 4; pixelated, CSS box == backing store, centred where the canvas fits, **0 near-black px at every one of them**. `NEAR_BLACK_MAX` is still 16 and `MIN_DISPLAY_SCALE` still 3 — the prohibitions against relaxing either hold. |
| 12 | **05-36 / WR-02** — the footer carries an unconditional `background: WALL_COLOR` (#3A3A5C), so 11px `#cccccc` is 6.74:1 wherever it lands instead of 4.40:1 / 3.21:1 on the floor planks **(was the one open gap)** | ✓ VERIFIED | `App.tsx:190` — inline, not behind any conditional. Live: `credit backed by rgb(58, 58, 92) (#3A3A5C) at every viewport`, asserted deliberately OUTSIDE the `fits` branch (verify-pixel-office-live.mjs:913-921). |
| 13 | **05-36** — G-05-P6 is untouched by the fix: the backdrop is not black and adds no near-black pixel | ✓ VERIFIED | WALL_COLOR is `rgb(58,58,92)`, every channel > `NEAR_BLACK_MAX` (16). Live full-viewport scan: **0 near-black px** at 800x480, 1920x1080 and 1280x720. |
| 14 | **05-36** — `App.test.tsx` asserts the backdrop by EQUALITY, so both a black strip and a missing backdrop go red; the two substring guards are gone | ✓ VERIFIED | `backgroundDeclarations(markup, "footer")` `.toEqual(['background:#3a3a5c'])`; no `not.toContain("rgba(0, 0, 0")` / `"#000"` remains. **Mutation-proved:** deleting the declaration gives `expected [] to deeply equal [ 'background:#3a3a5c' ]`. A black strip fails the same equality. |
| 15 | **05-36** — the overflow case is covered at all: unit-level geometry in `App.test.tsx` AND a third harness OBS_SIZES entry asserting the BACKDROP, not only position | ✓ VERIFIED | `App.test.tsx:125-138` asserts `displayScaleFor(800,480) === MIN_DISPLAY_SCALE` and that the canvas exceeds the viewport on **both** axes, all from exported constants. `OBS_SIZES` now leads with `{ w: 800, h: 480, obsScale: 3, fits: false }` and the `else` arm asserts the overflow genuinely occurred before the backdrop check. Ran green here. (Review WR-05's objection to the unit test's NAME is recorded as advisory — it does not touch what the must_have asked for.) |
| 16 | **05-36** — everything 05-31 proved is unchanged and still passes | ✓ VERIFIED | No TRUTH 0 assertion was weakened or deleted: integer scale, backing-store equality, `image-rendering: pixelated`, centring, the near-black scan all still fire. Centring and the bottom-wall-band check were *scoped* to `fits` entries (they are meaningless when the canvas overflows), not relaxed — both fitting viewports still assert them, and they passed. |
| 17 | **05-37 / WR-08** — a handoff's interaction slot stays reserved for the WHOLE record, departure included | ✓ VERIFIED | `handoff-choreography.ts:112` — `if (record.fromChar !== fromChar) taken.push(record.target)`, no phase exemption. Behavioural test `a departing sender keeps its aisle slot reserved until it is home` passes; **mutation-proved** by re-adding the exemption (`the second sender was handed the tile the first is still standing on: expected { col: 10, row: 6 } to not deeply equal { col: 10, row: 6 }`). |
| 18 | **05-37 / WR-08** — the reservation is bounded, not permanent: the slot is free again once `retireHandoff` removes the record | ✓ VERIFIED | `handoffs.delete` appears exactly once in the file, inside `retireHandoff` (:183) — the record map is the only reservation source, so retirement is the only exit. Named test `the slot is free again once the record retires` passes. |
| 19 | **05-37 / WR-07** — `getActiveHandoffs` identifies its speaker by record identity, not string equality; the overwritten record reports `speakerId: null` and `box: null` | ✓ VERIFIED (coincidental-reliance) | `showsLineOf` is `ch.bubbleTextTaskId === record.taskId` (:155-156), read at :420 and :426. The collision test asserts the byte-identical collision **before** relying on it (`expect(resolveHandoffDialogue(...TITLE_X...)).toBe(resolveHandoffDialogue(...TITLE_Y...))`), so it cannot silently stop testing the property. **Mutation-proved.** Flagged coincidental-reliance: the `box: null` half holds via the `speakerId === null` short-circuit, not via identity-keyed rect lookup — see `coincidental_reliance_items` and advisory WR-01. Advisory only; score and status unchanged. |
| 20 | **05-37 / WR-07** — the same identity rule governs the CLEAR paths, so a newer identical-looking line survives a sibling record's retirement | ✓ VERIFIED | `showsLineOf` gates `clearLine` at :185, :190 and :268; `clearLine` nulls text, partner and stamp together (:161-164). Named test `retiring a record clears a line only when that line is its own` asserts the displayed line survives the foreign retirement and then vanishes on its own record's. |

**Score:** 20/20 truths verified (0 present, behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Stream-safety filtering of `TaskState.title` before it reaches rendered dialogue | Phase 7 | Phase 7 goal: "Company data is classified and filtered by visibility level before it leaves the server, so a public overlay can exist without ever leaking private data." `dialogue-templates.ts` bounds length only and says so in-source (SAFE-01/02). |
| 2 | Hover / click / dashboard UI for the untruncated title `getActiveHandoffs()` exposes | Phase 6 | Phase 6 is CEO Dashboard & Approval Workflow; 05-35's own must_have scopes the UI out: "No hover, click or dashboard UI is built in this phase (later work)." |

### Advisory (New Scope, Unevidenced)

Re-verification ran, so this section is present even though nothing here blocks. Each item was confirmed against the source by this verifier, not accepted from the review.

| # | Finding | Category | Why Advisory |
|---|---------|----------|--------------|
| 1 | **WR-01** — `framePlacements` still stamps `text` and `getDialogueBox` still matches on it, so `box` resolves by text while `speakerId` resolves by stamp | architectural | Confirmed. Does not falsify the must_have (`box` short-circuits on a null speaker) and produces no reproducible wrong rect (per-frame record, one bubble per character). Queue with Phase 6. |
| 2 | **WR-04** — both new footer guards assert against the same constant the app renders from; `#cccccc` is pinned nowhere | other | Confirmed. The must_have asked for an equality that catches a black strip and a missing backdrop; it does (mutation-proved). The contrast-ratio class it does not cover was never claimed. |
| 3 | **WR-05** — the 800x480 unit test renders nothing at that viewport and duplicates the assertion above it | other | Confirmed. The must_have asked for a *geometry* assertion plus a harness entry; both exist and both are real. The defect is the test name overclaiming CI coverage. |
| 4 | **WR-03** — the opaque footer is a ~19 CSS px band over the canvas; the canvas's own bottom 19 px cannot be scrolled clear | other | Confirmed. Permanently covers only grid row 10 (bottom wall — no seat, no standing spot, no head-anchored glyph), so no state signal is reachable. Routed to human verification item 3 as a product call. |
| 5 | **WR-02 (side-tail Y)** — `tailFor`'s right/left tail Y is independent of the clamped winner `y` | architectural | Confirmed. Unreachable on the shipped layout (every character on rows 4/6/8); the new containment test explicitly excludes side tails, so nothing guards it. |
| 6 | **IN-01..IN-07** — seven info-level review items, all confirmed present | other | None changes a shipped behaviour; none reproducible. IN-02 (dead record holds its slot for a tick) and IN-03 (fallback target reserved as a real slot) are the two widened by WR-08 and worth queueing with WR-01. |
| 7 | 05-UI-SPEC.md still documents four interaction column offsets; the JSON carries six after WR-03 | other | Documentation only — the JSON is the single source and the tests enumerate it. |
| 8 | REQUIREMENTS.md leaves OFFICE-03 unchecked / "Gaps Found" while marking OFFICE-02 Complete — both stale | other | Bookkeeping, not implementation. Reconcile at ship. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/App.tsx` | full-viewport sizing, WALL_COLOR surround, attribution footer **with its own backdrop** | ✓ VERIFIED | The one property missing last round is present at :190 and live-proved at three viewports. Copy, `position: fixed`, 11px monospace all unchanged (prohibition honoured). |
| `apps/web/src/App.test.tsx` | equality guard on the footer backdrop; overflow-viewport geometry | ✓ VERIFIED | 24 tests pass; the backdrop guard is mutation-proved non-vacuous. |
| `apps/web/index.html` | body painted WALL_COLOR, no black pre-mount frame | ✓ VERIFIED | Pinned to `constants.ts` by App.test. |
| `scripts/verify-pixel-office-live.mjs` | TRUTH 0 extended with a third, overflowing viewport; footer backdrop asserted at every one | ✓ VERIFIED | `OBS_SIZES` = 800x480 (fits:false), 1920x1080, 1280x720. Backdrop assertion sits outside the `fits` branch; the `else` arm refuses to let the overflow entry pass without actually overflowing. **Ran by this verifier: exit 0.** |
| `packages/pixel-office/src/handoff/handoff-choreography.ts` | whole-record slot reservation; identity-based speaker/clear paths | ✓ VERIFIED | :112 reservation, :155-156 `showsLineOf`, :161-164 `clearLine`, :183 the single `handoffs.delete`. Both fixes mutation-proved. |
| `packages/pixel-office/src/handoff/handoff-choreography.test.ts` | named regression tests for WR-07 and WR-08 | ✓ VERIFIED | `a departing sender keeps its aisle slot reserved until it is home`, `the slot is free again once the record retires`, and the four-test `colliding dialogue lines (review WR-07)` describe, which asserts the collision before relying on it. |
| `packages/pixel-office/src/types.ts` | optional `Character.bubbleTextTaskId` sibling field | ✓ VERIFIED | :165 `bubbleTextTaskId?: string \| null;` — `undefined` on a fresh character, `null` after `clearLine`. |
| `packages/pixel-office/src/engine/renderer.ts` | candidate-scored bubble, per-frame placement record | ⚠️ WIRED, one residual | Unchanged this round and all its own must-haves hold; the per-frame record still keys on text (advisory WR-01). |
| `packages/pixel-office/src/layout/office-layout.json` + `officeLayout.ts` | 6 interaction offsets, `homeNearRow` validator | ✓ VERIFIED | `interaction.row = 6`, `colOffsets [1,-1,3,-3,5,-5]`; seats rows 4/8 only. |
| `packages/pixel-office/src/handoff/dialogue-templates.ts` | unchanged caps/templates | ✓ VERIFIED | `MAX_DIALOGUE_TITLE_CHARS = 12`, not in the changed-file set. |
| `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` | updated for both 05-36 and 05-37 | ⚠️ UPDATED, one stale clause | Both corrections recorded; the interaction-offset count is stale (advisory 7). |
| `references/ASSET-LICENSES.md` | present with content | ✓ VERIFIED | Existence + non-emptiness asserted by `App.test.tsx` via a build-time `?raw` read. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `WALL_COLOR` (constants.ts) | App wrapper background **and** footer background | pixel-office re-export → two consumers of one constant | WIRED | Live: wrapper and footer both `rgb(58,58,92)`; they cannot drift apart. |
| `readColorConst("WALL_COLOR")` | `getComputedStyle(footer).backgroundColor` | harness hex→rgb, asserted at every OBS_SIZES entry | WIRED | Ran green at all three viewports, including the overflowing one. |
| `MIN_DISPLAY_SCALE × DEFAULT_COLS × TILE_SIZE` | the 800x480 overflow case | `displayScaleFor` clamp → canvas 960x528 > viewport | WIRED | Asserted in the unit test from exported constants, and re-derived live (`scale 3 (backing 960x528, overflows)`). |
| `HandoffRecord.target` | `interactionTileFor`'s `taken` set | every live record regardless of phase | WIRED | :112; released only by `retireHandoff` (:183). |
| `Character.bubbleTextTaskId` (written at :282 / :328) | clear paths (:185/:190/:268) **and** read path (:420/:426) | one predicate `showsLineOf` | WIRED | Single identity source for all five call sites. |
| `getActiveHandoffs().speakerId` | `getDialogueBox(speakerId, speakerText)` → drawn rect | renderer `framePlacements` | ⚠️ PARTIAL | Speaker by stamp, rect by text (advisory WR-01). Functional today; the seam is the one the review names. |
| WS `onEvent` | office | `applyLiveEvent` → `upsertCharacterFromAgent`, THEN `handleHandoffEvent` | WIRED | App.tsx:78-99; the load-bearing ordering comment is intact. Live-proved end to end. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| Characters on canvas | `AgentStatus` per agent | WS snapshot + company-core `reduce` over relayed events | Yes — 5472 sprite px from real POSTed events in this run | ✓ FLOWING |
| Blocked/waiting glyph | `statusBubble` | same reducer path | Yes — 1088 glyph px, owner-bound | ✓ FLOWING |
| Handoff label text | `TaskState.title` / agent name | `registerTaskTitle` from snapshot + `task.created` | Yes — requested and accepted lines both observed live | ✓ FLOWING |
| `getActiveHandoffs().fullTitle` | untruncated title | `getTaskTitle(taskId) ?? taskId` | Yes — falls back to the taskId, never fabricates | ✓ FLOWING |
| `getActiveHandoffs().box` | drawn bubble rect | the renderer's own per-frame `framePlacements` | Yes, or `null` — never a rect from an earlier frame | ✓ FLOWING |
| Footer backdrop | `WALL_COLOR` | `constants.ts`, one constant, two consumers | Yes — `rgb(58,58,92)` measured in the live DOM | ✓ FLOWING |
| Furniture / tiles | sprite pixel data | `office-metrocity.json`, decoded reproducibly | Yes — 0 bare-floor px, 8 desks at 17760 px each | ✓ FLOWING |

No `Math.random`, `Date.now`, timer, `fetch`, `WebSocket` or dynamic `import(` anywhere in `packages/pixel-office/src` outside two comment lines. The renderer consumes projections only — the phase's Core Value holds.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| WR-08 reservation survives departure | `npx vitest run -t "a departing sender keeps its aisle slot reserved until it is home"` | 1 passed, 195 skipped | ✓ PASS |
| WR-08 guard is non-vacuous | same test with `phase !== "RETURNING_TO_DESK"` re-added to `handoff-choreography.ts:112` | **1 failed** — `the second sender was handed the tile the first is still standing on` | ✓ PASS (goes red on revert) |
| WR-07 guard is non-vacuous | `only the record whose line the receiver is actually showing reports a speaker and a box` with `showsLineOf` mutated to a text-presence check | **1 failed** — `two records claimed one bubble: expected [...] to have a length of 1 but got 2` | ✓ PASS (goes red on revert) |
| Footer backdrop guard is non-vacuous | `backs the attribution with the office border colour` with `background: WALL_COLOR` deleted from `App.tsx` | **1 failed** — `expected [] to deeply equal [ 'background:#3a3a5c' ]` | ✓ PASS (goes red on revert) |
| Pure-consumer property | repo grep for `Math.random\|Date.now\|setTimeout\|setInterval\|fetch(\|XMLHttpRequest\|WebSocket\|import(` over `packages/pixel-office/src` excluding tests | 2 hits, both comment lines | ✓ PASS |
| Full workspace (orchestrator, uncached) | `pnpm turbo run test --force` | 10/10 tasks successful; pixel-office 196, web 24 | ✓ PASS |

All three mutations were reverted with `git checkout --`; the working tree is clean of source changes.

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| Live end-to-end proof (this phase's declared probe; no `scripts/*/tests/probe-*.sh` exists) | `node scripts/verify-pixel-office-live.mjs` | Docker Postgres reset + migrations + real api/web dev servers + real worker credential + Playwright canvas readback. TRUTH 0 (three viewports, credit backed by `rgb(58,58,92)` at every one, 0 near-black px), 1, 2, 3, 4, 5, 6, 7 all PASS → `LIVE PROOF: PASS`, **exit 0** | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| OFFICE-01 | 05-36 (+ 23 earlier) | Forked renderer, one floor, sprites/animations reflect all 15 states | ✓ SATISFIED | Truths 1, 5, 8, 11, 16 |
| OFFICE-02 | 05-36 (+ 8 earlier) | Fork attribution and licence notices preserved | ✓ SATISFIED | Truths 4, 12, 13, 14, 15 — the previous round's only failure, now closed and live-proved at the overflow viewport it failed at |
| OFFICE-03 | (14 plans) | Blocked / waiting agents show a clear glanceable signal | ✓ SATISFIED (mechanically) | Truths 2, 8, 10; UAT round 2 test 2 passed on automated CVD evidence. Glanceability at stream scale remains a judgment-tier prohibition — human item 1. |
| HANDOFF-01 | 05-37 (+ 18 earlier) | Walk over, task icon, accept, move to work | ✓ SATISFIED | Truths 3, 6, 7, 17, 18; live TRUTH 3/5 |
| HANDOFF-02 | 05-37 (+ 10 earlier) | Deterministic / template-based dialogue, never LLM at render time | ✓ SATISFIED | Truths 3, 9, 19, 20; zero network/LLM surface by grep |

All 5 IDs declared across the 37 plans appear in REQUIREMENTS.md mapped to Phase 5, and REQUIREMENTS.md maps no additional ID to Phase 5. **No orphaned requirements.** (The ledger's *status column* for OFFICE-02/OFFICE-03 is stale — advisory 8.)

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` | — | **None** in any file this round modified (App.tsx, App.test.tsx, verify-pixel-office-live.mjs, handoff-choreography.ts/.test.ts, types.ts) |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` / placeholder prose | — | **None** |
| — | — | `it.skip` / `describe.skip` / `.only` / `it.todo` | — | **None** in phase scope (`claude-adapter`'s one skipped test is a pre-existing opt-in integration gate, out of this phase) |
| `apps/web/src/App.tsx` | 178-193 | Opaque fixed overlay over the canvas | ⚠️ Warning | Review WR-03. Covers only grid row 10 permanently — no seat, no standing spot, no head-anchored glyph — so it is not classified as a blocker. Routed to human item 3. |

Every `ponytail:` comment in the changed files names a real ceiling with an upgrade path. None is an unreferenced debt marker.

### Human Verification Required

#### 1. Stream-scale readability of the polished frame

**Test:** Open the office at your OBS source size (1280x720 or 1920x1080) with agents in blocked, waiting_for_ceo and waiting_for_agent, plus a handoff mid-sequence. Glance for one second.
**Expected:** Each stuck agent and the *kind* of stuck is identifiable; the hourglass reads as an hourglass without relying on its blue; the handoff line is readable, sits clear of desks and monitors, and does not read as a debug banner.
**Why human:** Legibility at viewing scale is a judgment. The harness proves geometry only (scale 4/6, 1.25 px head gap, bubble off every desk and glyph). This is UAT round 2 test 1, still `result: issue` — the CEO has not seen a frame since the polish and gap-closure fixes landed.

#### 2. Seated versus standing at a glance (G-05-P3)

**Test:** Look at a populated office where at least one agent is blocked or waiting at its own desk while a handoff sender waits in the aisle.
**Expected:** The agent at its desk clearly reads as sitting (the desk hides its lower body) and the aisle sender clearly reads as standing.
**Why human:** "Reads as sitting" is the exact judgment UAT round 2 polish item 3 raised; the test proves only a >= 8-row difference in visible body rows.

#### 3. The opaque footer band at a sub-minimum viewport (review WR-03)

**Test:** Resize the browser below 960x528 (e.g. 800x480) and scroll the office.
**Expected:** The credit is legible on its own WALL_COLOR band (automated and passing), and you are content that the bottom ~19 px of the canvas cannot be scrolled clear of that band.
**Why human:** Analysis shows only grid row 10 — bottom wall, no seats, no standing spots, no head-anchored glyph — is permanently covered, which is why this is not a blocker. Whether an opaque band over the canvas is acceptable at unsupported viewports is a product call. The fix is one line (`paddingBottom` on the scroll container) if the answer is no.

### Gaps Summary

**No gaps.** The single gap the previous round left open (G-05-P6 / WR-02 — the attribution footer's missing backdrop, and the absence of any coverage for the viewport where it mattered) is closed, and closed in the way the gap asked for rather than around it: an unconditional `background: WALL_COLOR` on the element itself, an equality guard that this verifier proved goes red when the declaration is deleted, and a third live-harness viewport at 800x480 that refuses to pass unless the canvas genuinely overflows — which this verifier ran end to end, exit 0.

The two architectural advisories the previous round recorded with no reproducible failure (WR-07 speaker attribution by string equality, WR-08 the aisle slot released a tick early) were planned rather than deferred, and both are now identity-/lifetime-correct at the FSM with mutation-proved regression tests behind them.

What is *not* finished is the seam the code review names: WR-07's record-identity rule stops at the FSM boundary, so `getActiveHandoffs().box` is still resolved by bubble text while `speakerId` is resolved by the stamp. That does not falsify any must_have — the overwritten record still reports `box: null`, because the expression short-circuits on a null speaker, and the per-frame placement record means the text lookup cannot return a stale rect — but it means the `box: null` assertion passes for a reason other than the one its message claims. It is recorded as a coincidental-reliance item and an advisory, and it is the one item worth closing before Phase 6 puts a real hit-test consumer on that rect.

The phase goal itself — *real company state on screen as a pure consumer of projections, never a fabricated animation* — is demonstrated, not asserted: real events over the real relay drove 5472 sprite pixels, 1088 owner-bound glyph pixels, a full handoff sequence with both dialogue lines, 8 furnished desks and 59.6 fps, with zero `Math.random`, timer, `fetch` or `WebSocket` in the renderer package outside comments.

Status is `human_needed`, not `passed`, because three items need a human's eye: the two stream-scale judgments UAT round 2 left open (the CEO has not seen a frame since these fixes) and one product call on the new opaque footer band.

---

_Verified: 2026-09-23T14:40:00Z_
_Verifier: Claude (gsd-verifier)_
