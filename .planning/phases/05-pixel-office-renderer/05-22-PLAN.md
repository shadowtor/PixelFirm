---
phase: 05-pixel-office-renderer
plan: 22
type: execute
gap_closure: true
gap_ids: [G-05-1e]
wave: 13
depends_on: ["05-20"]
files_modified:
  - packages/pixel-office/assets/metrocity-interior/Home/TilesHouse.png
  - packages/pixel-office/assets/metrocity-interior/Home/LivingRoom-Sheet.png
  - packages/pixel-office/assets/metrocity-interior/Home/Flowers-Sheet.png
  - packages/pixel-office/assets/metrocity-interior/Home/Cupboard-Sheet.png
  - packages/pixel-office/assets/metrocity-interior/Home/Paintings-Sheet.png
  - packages/pixel-office/assets/metrocity-interior/Hospital/Miscellaneous-Sheet.png
  - packages/pixel-office/scripts/decode-metrocity-interior.mjs
  - packages/pixel-office/src/sprites/office-metrocity.json
  - packages/pixel-office/src/sprites/officeSprites.test.ts
  - references/ASSET-LICENSES.md
autonomous: true
requirements: [OFFICE-01, OFFICE-02]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every MetroCity Interior source sheet the office uses is committed under packages/pixel-office/assets/metrocity-interior/ with the pack's own Home/ and Hospital/ relative paths; nothing in the repo references a temp or upload path"
    - "packages/pixel-office/src/sprites/office-metrocity.json is exactly what scripts/decode-metrocity-interior.mjs produces from those in-repo sheets: `node scripts/decode-metrocity-interior.mjs --check` re-decodes and exits non-zero on any difference, and officeSprites.test.ts runs it"
    - "The JSON records, per source sheet, its pack-relative path and SHA-256, and per sprite its sheet and exact source rect, so every decoded pixel is traceable to a committed file"
    - "The decoded set is: floorTiles (four fully opaque 16x16 quadrants of one 32x32 wood-plank block, index (row % 2) * 2 + (col % 2)), wallTop (one fully opaque 16x16 horizontally tileable wall slice), desk (the LivingRoom wooden table, 46x30), plant (large potted plant, 20x41), plantSmall (14x21), bookcase (36x43), cabinet (22x27), painting (14x16), and monitorBack (an original <= 12x10 monitor seen from behind, authored inline in the script and marked source 'original')"
    - "references/ASSET-LICENSES.md cites the Interior pack at its primary source (https://jik-a-4.itch.io/metrocity, 'MetroCity - Free Top Down Interior Asset Pack', Asset license CC0 1.0, 'Credits are not necessary but would be appreciated', re-fetched during execution), records the user-supplied Interior.rar, the copied files with SHA-256, the decoded rects, the user's explicit permission (UAT 2026-09-22) and credits JIK-A-4"
    - "§1 link 2 is upgraded to 'verified for the outfit layer': MetroCity 2.0.rar (a download on the CC0 character-pack listing) Suit.png frame (0,64) is pixel-identical to the outfit layer of the fork's char_0.png frame 0 at offset (8,0), 110/110 opaque pixels (UAT 05 test 3, 2026-09-22); the hair layer was not found in the supplied downloads and stays credit-only"
    - "§3 lists monitorBack as an original authored in this repo; §3's 'no third-party floor/wall assets' paragraph is updated to say the floor/wall/furniture now come from the MetroCity Interior pack (documented provenance, new section), and §4 (fork furniture/floor/wall/carpet/pet packs) stays deferred and unused"
  artifacts:
    - packages/pixel-office/assets/metrocity-interior/
    - packages/pixel-office/scripts/decode-metrocity-interior.mjs
    - packages/pixel-office/src/sprites/office-metrocity.json
    - packages/pixel-office/src/sprites/officeSprites.test.ts
    - references/ASSET-LICENSES.md
  key_links:
    - "assets/metrocity-interior/*.png -> decode-metrocity-interior.mjs (pngjs, tight alpha bbox inside a search rect) -> src/sprites/office-metrocity.json -> (05-24) layout/officeLayout.ts"
    - "officeSprites.test.ts -> execFileSync(node, [decode script, --check]) -> exit 0 only if the committed JSON equals a fresh decode"
  prohibitions:
    - statement: "MUST NOT wire any fork furniture/floor/wall/carpet/pet PNG (ASSET-LICENSES §4 stays deferred, D-05)"
      status: resolved
      verification: test
    - statement: "MUST NOT fetch any sprite source over the network or from a temp/upload path at decode time — the in-repo sheets are the only input"
      status: resolved
      verification: test
    - statement: "MUST NOT claim a licence tier the cited evidence does not support (05-12 WR-09 rule, kept)"
      status: resolved
      verification: review
---

<objective>
First slice of UAT gap G-05-1e (the office is a bare grey floor): bring the MetroCity Interior art into the repo as a reproducible, licence-documented asset pipeline. Plan 05-24 consumes the output; this plan renders nothing.

User context (authoritative, UAT 2026-09-22): the user supplied the MetroCity itch.io packs and explicitly OK'd using them; plans must copy the needed source sheets into the repo, decode at script time into SpriteData JSON reusing the `decode-metrocity-sprites.mjs` approach, and record provenance + JIK-A-4 credit in `references/ASSET-LICENSES.md`, including the link-2 upgrade proven by UAT test 3.

Planner decisions (Claude's discretion, documented):
- Source = MetroCity Interior only. The fork's DESK/PC/CHAIR are NOT used: they sit in ASSET-LICENSES §4 with undocumented provenance, while the Interior pack is CC0 at its own listing (checked at planning time via https://jik-a-4.itch.io/metrocity) and user-approved. This keeps D-05's scoped audit honest: only wired assets are audited, and these get a full record.
- The pack has no small monitor seen from behind (TV-Sheet's CRT back is 24x29 and would cover a seated agent's face), so one tiny original `monitorBack` is authored inline in the decode script and recorded in §3 as an original, exactly like the 12 glyphs.
- The hospital "panel" at x~295 is a bed-head medical panel, not a whiteboard, so it is not used.

D-02 (animation sourcing triage) is unaffected: these are static office sprites, not animation frames.

Output: 6 committed sheets, the decode script with `--check`, `office-metrocity.json`, `officeSprites.test.ts`, and the ASSET-LICENSES update.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-UAT.md
@.planning/debug/office-bare-floor.md
@packages/pixel-office/scripts/decode-metrocity-sprites.mjs
@references/ASSET-LICENSES.md
</context>

<interface_context>
Source files (already extracted by the orchestrator during UAT):
- Extracted pack root: `C:/Users/shado/AppData/Local/Temp/claude/F--Sidegigs-PixelFirm/726d047b-acda-41ee-b81f-6cf429a387b4/scratchpad/uat05/mc/d188b927-Interior/Interior/`
- If that directory is gone, re-extract `C:/Users/shado/.claude/uploads/726d047b-acda-41ee-b81f-6cf429a387b4/d188b927-Interior.rar` (185,267 bytes) into a scratchpad directory with Windows' libarchive tar: `/c/Windows/System32/tar.exe -xf <rar> -C <dir>` (GNU tar cannot read rar).
- These paths are copy sources only. Nothing committed may reference them.

Sprite locations measured at planning time (sheet-relative px; each furniture item is found as the tight alpha>=128 bounding box inside its search rect; the tight boxes measured were):
| key | sheet | search rect x,y,w,h | measured tight box | notes |
|---|---|---|---|---|
| floorTiles | Home/TilesHouse.png | 96,128,32,32 (fixed, no bbox) | 32x32 opaque | horizontal wood planks; fallbacks 32,128 / 96,176 / 32,176 if it does not tile |
| wallTop | Home/TilesHouse.png | inside 16,0,48,64 (fixed 16x16 slice) | opaque | white wall over wood wainscot with brown trim lines at y~35-38; pick the 16-row slice that reads as a wall base when tiled horizontally |
| desk | Home/LivingRoom-Sheet.png | 0,0,96,96 | 25,15,46,30 | wooden table |
| plant | Home/Flowers-Sheet.png | 0,0,64,96 | 22,3,20,41 | |
| plantSmall | Home/Flowers-Sheet.png | 64,0,64,96 | 89,23,14,21 | |
| bookcase | Home/Cupboard-Sheet.png | 256,0,64,96 | 270,20,36,43 | shelves with books |
| cabinet | Hospital/Miscellaneous-Sheet.png | 1320,0,48,64 | 1333,20,22,27 | grey/white drawer cabinet |
| painting | Home/Paintings-Sheet.png | 64,0,32,32 | 73,7,14,16 | teal landscape; fits the 16 px wall row. Chosen over the grey frames at x 0/32/192-288 because those contain #f0f0f0, which is DIALOGUE_TEXT_COLOR — the 05-13 colour guard (extended to office sprites in 05-24) forbids any sprite painting a dialogue colour |

Existing pattern to mirror: `packages/pixel-office/scripts/decode-metrocity-sprites.mjs` (pngjs `PNG.sync.read`, alpha < 128 -> "", lowercase `#rrggbb`, JSON with provenance fields, ponytail note on the alpha threshold). pngjs is already a devDependency of pixel-office — no install.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Committed sheets -> reproducible decode -> office-metrocity.json, guarded by --check</name>
  <reversibility rating="reversible">New files only; nothing imports the JSON until 05-24.</reversibility>
  <files>packages/pixel-office/assets/metrocity-interior/ (6 PNGs), packages/pixel-office/scripts/decode-metrocity-interior.mjs, packages/pixel-office/src/sprites/office-metrocity.json, packages/pixel-office/src/sprites/officeSprites.test.ts</files>
  <read_first>
    - packages/pixel-office/scripts/decode-metrocity-sprites.mjs (whole file — the pattern to mirror)
    - packages/pixel-office/src/sprites/spriteData.test.ts (test style for sprite JSON)
    - .planning/debug/office-bare-floor.md (the MetroCity Interior evidence entry)
  </read_first>
  <behavior>
    officeSprites.test.ts:
    - "office-metrocity.json is the decode of the committed sheets": `execFileSync(process.execPath, [<abs path to scripts/decode-metrocity-interior.mjs>, "--check"])` does not throw.
    - "floor and wall tiles are full opaque tiles": `floorTiles` has 4 entries, each 16x16 with every cell a `#rrggbb` string; `wallTop` is 16x16 fully opaque.
    - "furniture sprites have their measured size and real pixels": desk 46x30, plant 20x41, plantSmall 14x21, bookcase 36x43, cabinet 22x27, painting 14x16, each with at least one opaque cell and every row the same length; monitorBack at most 12 wide and 10 tall, with opaque cells.
    - "every sprite is traceable": each sprite's `sheet` is a key of `sources` (except monitorBack, whose `sheet` is the literal "original"), and each `sources` entry names a file that exists under `packages/pixel-office/assets/metrocity-interior/`.
    - "no office sprite paints a dialogue colour": no cell of any sprite equals `DIALOGUE_BOX_COLOR` or `DIALOGUE_TEXT_COLOR` from constants.ts (case-insensitive) — the live harness counts dialogue pixels by exact colour (05-13 guard).
    RED: write the test first; it fails because the script and JSON do not exist.
  </behavior>
  <action>
1. Copy exactly these six files from the extracted pack root into `packages/pixel-office/assets/metrocity-interior/`, keeping the pack-relative paths: `Home/TilesHouse.png`, `Home/LivingRoom-Sheet.png`, `Home/Flowers-Sheet.png`, `Home/Cupboard-Sheet.png`, `Home/Paintings-Sheet.png`, `Hospital/Miscellaneous-Sheet.png`. Byte-for-byte copies (no re-encode).

2. Write `packages/pixel-office/scripts/decode-metrocity-interior.mjs` in the style of `decode-metrocity-sprites.mjs` (header explaining purpose, source pack, re-run instructions, the same alpha<128 ponytail note):
   - input root = `../assets/metrocity-interior` resolved from the script's own directory; output = `../src/sprites/office-metrocity.json`;
   - a `MANIFEST` array, one entry per key in the interface table: `{ key, sheet, rect: [x, y, w, h], mode }` where mode `"tile"` copies the rect verbatim and `"bbox"` crops to the tight alpha>=128 bounding box inside the rect (throw, naming the key, if the box is empty);
   - `floorTiles` is decoded as the 32x32 block and emitted as four 16x16 quadrants in order [top-left, top-right, bottom-left, bottom-right] so a renderer picks index `(row % 2) * 2 + (col % 2)`;
   - `monitorBack` is authored inline as a small palette + string-row grid (dark grey casing with a 1 px darker edge and a 2 px stand, <= 12x10), emitted with `sheet: "original"` and `rect: null`; its palette avoids `#121212` and `#f0f0f0` (the dialogue colours);
   - output shape: `{ "pack": "MetroCity - Free Top Down Interior Asset Pack by JIK-A-4", "url": "https://jik-a-4.itch.io/metrocity", "licence": "CC0-1.0", "sources": { "<sheet path>": "<sha256 hex>" }, "sprites": { "<key>": { "sheet": "...", "rect": [x, y, w, h] | null, "data": SpriteData } } }` where `rect` is the FINAL (tight) rect and `floorTiles.data` is the array of four SpriteData;
   - SHA-256 via `node:crypto` over the committed file bytes;
   - default run writes the JSON (`JSON.stringify(result, null, 2) + "\n"`) and logs the path; `--check` decodes in memory, compares to the committed file's parsed JSON with a deep equality, prints the first differing key and exits 1 on mismatch, exits 0 on match. No network access anywhere in the script.
   - Guard `main()` so importing the file never writes.

3. Visual self-check before committing the JSON: write a throwaway scratchpad script (not committed) that renders a 4x3 tiling of `floorTiles` with a row of `wallTop` above it, plus each furniture sprite, at 6x into a PNG with pngjs; Read the PNG. Confirm: no visible seam in the plank tiling, the wall strip reads as a wall base, no furniture sprite is cut off or includes a neighbour's pixels. If the floor block seams, switch to the next fallback rect from the interface table and re-check; if the wall slice reads badly, move the 16-row window inside 16,0,48,64. Record the final choices in the SUMMARY.

4. Run the script to generate `office-metrocity.json`, then GREEN the test.
  </action>
  <verify>
    <automated>node packages/pixel-office/scripts/decode-metrocity-interior.mjs --check</automated>
    <automated>pnpm --filter pixel-office test -- officeSprites</automated>
  </verify>
  <acceptance_criteria>
    - `node packages/pixel-office/scripts/decode-metrocity-interior.mjs --check` exits 0.
    - `pnpm --filter pixel-office test` passes (all pre-existing tests plus officeSprites.test.ts).
    - `ls packages/pixel-office/assets/metrocity-interior/Home packages/pixel-office/assets/metrocity-interior/Hospital` lists the six sheets.
    - `grep -cE "AppData/Local/Temp|\.claude/uploads" packages/pixel-office/scripts/decode-metrocity-interior.mjs packages/pixel-office/src/sprites/office-metrocity.json` prints 0 for both files (no temp/upload path is committed).
  </acceptance_criteria>
  <done>The office sprite set is a committed, reproducible derivative of committed MetroCity sheets, verified by a test that re-decodes it.</done>
</task>

<task type="auto">
  <name>Task 2: ASSET-LICENSES — Interior pack provenance, link-2 upgrade, original monitorBack</name>
  <files>references/ASSET-LICENSES.md</files>
  <read_first>
    - references/ASSET-LICENSES.md (whole file)
    - packages/pixel-office/src/sprites/office-metrocity.json (only the top-level `pack`, `url`, `licence`, `sources` fields — do not read the pixel data)
    - .planning/phases/05-pixel-office-renderer/05-UAT.md (test 3 evidence and note)
  </read_first>
  <action>
Before editing, re-fetch https://jik-a-4.itch.io/metrocity and https://jik-a-4.itch.io/metrocity-free-topdown-character-pack (WebFetch) and record, with today's date: the Interior pack's title, its stated Asset license, its credit statement, and its download list (planning-time result: "MetroCity - Free Top Down Interior Asset Pack", Creative Commons Zero v1.0 Universal, "Credits are not necessary but would be appreciated.", Interior.rar 180 kB); and that the character-pack listing (CC0) offers `MetroCity 2.0.rar` as a download. If either fetch fails or disagrees, record exactly what was observed and keep the tier at what the evidence supports (D-05, 05-12 WR-09 rule).

Edits (per D-05 — wired assets get a full audit; nothing unwired is upgraded):
- §1 link 2: retitle to verified for the outfit layer. Evidence: UAT 05 test 3 (2026-09-22) — the user supplied the itch.io downloads; `MetroCity 2.0.rar` (a download on the CC0 character-pack listing above) `Suit.png` frame (0,64) equals the outfit layer of the fork's `char_0.png` (commit 3537e140) frame 0 at offset (8,0), 110/110 opaque pixels identical. The hair layer was not found in the supplied downloads, so the hair layer remains credit-only. Update "What would close link 2" to say only the hair layer is open. Update the section heading so it no longer says the identity "rests on the fork's credit" without qualification.
- New section after §1, "1a. MetroCity Interior pack (office floor, wall and furniture) — CC0 at source, files verified": primary-source citation and fetch date; the user-supplied `Interior.rar` (185,267 bytes, uploaded 2026-09-22) and the user's explicit permission recorded in UAT ("you are OK to use them if it makes things easier"); the six committed sheets with their SHA-256 values copied from `office-metrocity.json`'s `sources`; the decode script and output file; the sprite keys with their final rects; credit: "MetroCity Interior pack by JIK-A-4" (credit given although the listing says it is not required).
- §3: add `monitorBack` (authored inline in `decode-metrocity-interior.mjs`, 05-22) to the originals table. Rewrite the "Third-party assets of undocumented provenance" paragraph: the floor, top wall and furniture now come from the MetroCity Interior pack, whose provenance is documented in §1a (so they are not undocumented); the side/bottom wall border stays a flat `WALL_COLOR` fill; no fork furniture/floor/wall/carpet/pet PNG is wired.
- §4: unchanged in substance; add one sentence that the office furniture comes from §1a, not from the fork's packs.
- Leave "Attribution in the running app" for 05-25 (it changes with the footer), but do not let it become false in the meantime: it still says the footer credits the MetroCity pack, which stays true.
  </action>
  <verify>
    <automated>grep -n "jik-a-4.itch.io/metrocity" references/ASSET-LICENSES.md</automated>
    <automated>grep -n "110/110" references/ASSET-LICENSES.md</automated>
    <automated>pnpm --filter web test</automated>
  </verify>
  <acceptance_criteria>
    - ASSET-LICENSES.md contains the §1a section with the Interior listing URL, CC0 statement, fetch date, the six SHA-256 values (matching `office-metrocity.json` `sources`) and a JIK-A-4 credit.
    - §1 link 2 cites the 110/110 outfit-layer match and states the hair layer is still credit-only.
    - `monitorBack` appears in §3's originals table.
    - `pnpm --filter web test` still passes (App.test reads this file via `?raw`).
  </acceptance_criteria>
  <done>Every office asset the next plans will wire is licence-documented at its primary source, credited to JIK-A-4, and the character pack's provenance is upgraded exactly as far as UAT test 3 proved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied archive -> repo | third-party PNGs enter the repo from an uploaded .rar |
| PNG bytes -> decode script (pngjs) | untrusted image data parsed at script time on a developer machine, never in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-22-01 | Tampering | committed sheets vs decoded JSON drift | medium | mitigate | SHA-256 of every sheet recorded in the JSON; `--check` re-decodes and the test fails on any difference |
| T-05-22-02 | Repudiation | asset provenance / licence claim | medium | mitigate | primary-source citation re-fetched at execution with date; tier limited to cited evidence (05-12 rule); user permission quoted from UAT |
| T-05-22-03 | Tampering | malformed PNG crashing pngjs | low | accept | script-time only on six fixed, hash-pinned files; a crash fails the script loudly, nothing ships |
| T-05-22-SC | Tampering | package installs | low | accept | no new dependency: pngjs ^7.0.0 is already a pixel-office devDependency (05-06) |
</threat_model>

<verification>
- `node packages/pixel-office/scripts/decode-metrocity-interior.mjs --check` exits 0.
- `pnpm --filter pixel-office test` and `pnpm --filter web test` pass.
- ASSET-LICENSES.md §1, §1a, §3, §4 read consistently (no section claims a flat-colour floor while §1a documents the Interior floor).
</verification>

<success_criteria>
The furnished office (05-24) has a committed, reproducible, licence-documented sprite source, and the MetroCity character provenance record reflects the UAT test 3 proof.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-22-SUMMARY.md` when done.
</output>
