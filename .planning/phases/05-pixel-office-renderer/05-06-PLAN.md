---
phase: 05-pixel-office-renderer
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pixel-office/package.json
  - packages/pixel-office/tsconfig.json
  - packages/pixel-office/scripts/decode-metrocity-sprites.mjs
  - packages/pixel-office/src/sprites/character-metrocity.json
  - packages/pixel-office/src/sprites/spriteData.ts
  - packages/pixel-office/src/sprites/spriteData.test.ts
  - references/ASSET-LICENSES.md
autonomous: false
gap_closure: true
requirements: [OFFICE-01, OFFICE-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "getCharacterSprites() returns real, non-transparent pixel data (at least one non-empty-string cell per frame) for the idle/walk/type poses in all 4 directions — closing 05-VERIFICATION.md's invisible-sprite gap (Truth 1)"
    - "The pixel data comes from the fork's own bundled, CC0-credited MetroCity character asset (char_0.png), fetched at the same pinned commit SHA already used throughout this phase — not a placeholder or a different, uncredited source (D-02 tier 1: existing-asset reuse, per RESEARCH.md's own stated intent for this module)"
    - "The CharacterSprites shape (walk: 4-tuple per direction, typing/reading: 2-tuple per direction) is unchanged, so engine/characters.ts and engine/renderer.ts require zero code changes to consume the new real data"
    - "references/ASSET-LICENSES.md's §1 entry no longer says the character pack is 'currently returning transparent placeholder frames' — it accurately describes the wired-in asset"
  artifacts:
    - path: "packages/pixel-office/src/sprites/character-metrocity.json"
      provides: "Decoded MetroCity char_0.png pixel data: {sourceUrl, commitSha, down: SpriteData[7], up: SpriteData[7], right: SpriteData[7]}"
    - path: "packages/pixel-office/scripts/decode-metrocity-sprites.mjs"
      provides: "Reusable, re-runnable Node script (pngjs) that regenerates character-metrocity.json from the pinned-commit PNG"
    - path: "packages/pixel-office/src/sprites/spriteData.ts"
      provides: "getCharacterSprites() built from character-metrocity.json instead of emptySprite() placeholders"
  key_links:
    - from: "packages/pixel-office/src/sprites/spriteData.ts"
      to: "packages/pixel-office/src/sprites/character-metrocity.json"
      via: "JSON import of the decoded down/up/right frame arrays"
      pattern: "character-metrocity.json"
    - from: "packages/pixel-office/src/engine/renderer.ts"
      to: "packages/pixel-office/src/sprites/spriteData.ts"
      via: "getCharacterSprites(ch.palette, ch.hueShift) — unchanged call site, now returns real pixels"
      pattern: "getCharacterSprites"
  prohibitions:
    - requirement_id: OFFICE-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT credit/attribute an asset pack (e.g. 'MetroCity CC0') in-app or in references/ASSET-LICENSES.md that the shipped renderer does not actually load. Resolved here by decoding the real, pinned-commit char_0.png into character-metrocity.json rather than substituting hand-authored or generated art under the same attribution claim; a human/visual spot-check that the rendered character actually resembles the fork's own MetroCity art (not a mis-decoded or corrupted frame) is recommended, per the fallback's descriptor-less authoring rule (no check_* wired here)."

user_setup: []
---

<objective>
Wire real, non-transparent pixel data into `packages/pixel-office/src/sprites/spriteData.ts`'s `getCharacterSprites()` — closing 05-VERIFICATION.md's Truth 1 ("Agents are visible on screen... with sprites/animations matching their real current state"), which today fails because every returned frame is `emptySprite()`'s fully-transparent placeholder. Per D-02's own tier order (05-CONTEXT.md) and RESEARCH.md's finding that the fork ships no unused *animation states* but references/ASSET-LICENSES.md already commits to loading the fork's actual bundled MetroCity CC0 character PNG once real pixel art is wired in — this is tier 1 (existing-asset reuse), not tier 2/3/4: decode the fork's own `webview-ui/public/assets/characters/char_0.png` (pinned to the same commit SHA `3537e140c2094761beae748592aeb92ece8edfdd` used everywhere else in this phase) into the exact `CharacterSprites` shape `engine/characters.ts`/`engine/renderer.ts` already consume, so no downstream rendering code changes.

Purpose: an invisible sprite is the single largest reason 05-VERIFICATION.md scored this phase 1/4 truths — every other piece (event-driven `AgentStatus` derivation, the `STATUS_MAP` pose table, the handoff FSM) is correct and tested, but produces zero visible output. This plan makes the output real.
Output: `packages/pixel-office/src/sprites/character-metrocity.json` (new, generated), `packages/pixel-office/scripts/decode-metrocity-sprites.mjs` (new, reusable), a rewritten `getCharacterSprites()`, and an honest `references/ASSET-LICENSES.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
@references/ASSET-LICENSES.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `packages/pixel-office/scripts/decode-metrocity-sprites.mjs` — one-time-but-reusable Node script (pngjs) decoding the pinned-commit `char_0.png` into `character-metrocity.json`.
- `packages/pixel-office/src/sprites/character-metrocity.json` — generated data asset: `{ sourceUrl, commitSha, down: SpriteData[7], up: SpriteData[7], right: SpriteData[7] }` (16x32 frames).
- `packages/pixel-office/src/sprites/spriteData.ts` — `getCharacterSprites()` now builds `CharacterSprites` from the decoded JSON (LEFT mirrored from RIGHT), instead of `emptySprite()`.
- `references/ASSET-LICENSES.md` §1 — updated to state the real asset is wired in, with the exact source URL/commit SHA.
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy check — pngjs (devDependency, one-time PNG decode script only)</name>
  <what-built>
    RESEARCH.md's Package Legitimacy Audit table exists but does not enumerate `pngjs` in its disposition table (it only concludes "no new external runtime packages are recommended" and separately notes `pngjs` in its Supporting-stack table as an option to verify "at implementation time"). Treating it as [ASSUMED] per the mandatory package-legitimacy gate, I ran real registry checks this session:
    - `npm view pngjs version time.created time.modified maintainers repository.url license --json` -> version `7.0.0` (matches the fork's own pin `^7.0.0` exactly), first published 2012-08-18, single long-standing maintainer `lukeapage`, MIT licence, canonical repo `github.com/pngjs/pngjs`.
    - `https://api.npmjs.org/downloads/point/last-week/pngjs` -> 44,877,407 downloads in the week of 2026-09-13 to 2026-09-19 (this package is a widely-depended-on transitive dependency across the npm ecosystem, not an obscure/typosquat-risk name).
    - Scope: `pngjs` is added as a **devDependency** of `packages/pixel-office` only, imported solely by the one-time decode script (`scripts/decode-metrocity-sprites.mjs`) — it is never imported by any file under `src/`, so it is never bundled into the browser-shipped `apps/web` build.
  </what-built>
  <how-to-verify>
    Confirm the version/age/maintainer/download evidence above looks legitimate to you (13-year-old, extremely widely used PNG codec library — not a plausible supply-chain risk), and that scoping it to a devDependency used only by a dev-time script (never shipped to the browser) is the right containment.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with adding pngjs and running the decode script in Task 2, or name a concern.</resume-signal>
  <acceptance_criteria>Human has typed "approved" (or equivalent) before Task 2 adds the dependency.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Decode the fork's real MetroCity char_0.png into non-transparent CharacterSprites data</name>
  <files>packages/pixel-office/package.json, packages/pixel-office/tsconfig.json, packages/pixel-office/scripts/decode-metrocity-sprites.mjs, packages/pixel-office/src/sprites/character-metrocity.json, packages/pixel-office/src/sprites/spriteData.ts, packages/pixel-office/src/sprites/spriteData.test.ts, references/ASSET-LICENSES.md</files>
  <precondition>Task 1's checkpoint has been approved by a human before this task's `pnpm add -D pngjs` runs.</precondition>
  <read_first>
    packages/pixel-office/src/sprites/spriteData.ts (the full current file — `CharacterSprites` interface, `emptySprite`, `getCharacterSprites`'s cache-key pattern `${paletteIndex}:${hueShift}`, to preserve exactly),
    packages/pixel-office/src/types.ts (`Direction` const: DOWN=0, LEFT=1, RIGHT=2, UP=3 — the exact enum values `getCharacterSprites`'s returned `Record<Direction, ...>` must be keyed by),
    packages/pixel-office/src/colorize.ts (`adjustSprite(sprite, color)` — reuse for non-zero hueShift, never write a new colorization routine),
    packages/pixel-office/src/engine/characters.ts (`getCharacterSprite(ch, sprites)` at the bottom of the file — confirms exactly how `walk`/`typing`/`reading` frame arrays get indexed by `ch.frame`, so the decoded frame COUNT and ORDER must match: `sprites.typing[ch.dir][ch.frame % 2]`, `sprites.walk[ch.dir][ch.frame % 4]`),
    references/ASSET-LICENSES.md (§1's current wording, to correct),
    packages/pixel-office/package.json (existing shape to extend with the new devDependency and script — no build step exists in this package today)
  </read_first>
  <action>
    Add `pngjs` (`^7.0.0`, matching the fork's own pin) as a devDependency of `packages/pixel-office` via `pnpm --filter pixel-office add -D pngjs @types/pngjs` (a `@types/pngjs` package exists on DefinitelyTyped; if it does not resolve, fall back to a local `// @ts-expect-error` or a minimal ambient type only inside the `.mjs` script's own scope, since the script is a plain `.mjs` file, not compiled by this package's `tsc`).

    Create packages/pixel-office/scripts/decode-metrocity-sprites.mjs, a standalone Node script (run manually, not part of any build/test pipeline) that: (1) fetches `https://raw.githubusercontent.com/pixel-agents-hq/pixel-agents/3537e140c2094761beae748592aeb92ece8edfdd/webview-ui/public/assets/characters/char_0.png` (the exact commit SHA already used by every other forked file/attribution header in this phase — never `main`, to avoid supply-chain drift) into a Buffer; (2) decodes it with `pngjs`'s `PNG.sync.read(buffer)` to get `{width, height, data}` (RGBA); (3) BEFORE hardcoding slice geometry, verify by directly reading `https://raw.githubusercontent.com/pixel-agents-hq/pixel-agents/3537e140c2094761beae748592aeb92ece8edfdd/core/src/assets/pngDecoder.ts`'s `decodeCharacterPng` function that the image is genuinely 112 wide x 96 tall, laid out as 3 direction rows (down, up, right, in that row order top-to-bottom) x 7 columns of 16x32-pixel frames each, and that frame indices 0-2 are the walk cycle, 3-4 are typing, 5-6 are reading — these values are this plan's best current understanding (derived from an AI-summarized fetch, not a byte-verbatim read) and MUST be confirmed against the real source file's own logic before the script's slice offsets are finalized; if the real geometry differs, use the actual values instead and note the correction in the SUMMARY; (4) for each of the 3 direction rows x 7 frame columns, slices out the 16x32 pixel block and converts each pixel's RGBA to this repo's `SpriteData` format (`string[][]`: alpha < 128 -> `""`, else `#RRGGBB` uppercase-or-lowercase hex matching the existing bubble/badge JSON convention — semi-transparency is collapsed to a binary transparent/opaque threshold, a deliberate simplification since MetroCity's pixel-art style has no genuine semi-transparent pixels; note this as a ponytail-flagged known ceiling in the script's own header comment); (5) writes the result to packages/pixel-office/src/sprites/character-metrocity.json as `{ sourceUrl, commitSha: "3537e140c2094761beae748592aeb92ece8edfdd", down: SpriteData[7], up: SpriteData[7], right: SpriteData[7] }`. Run the script once now to produce the committed JSON file.

    Add `"resolveJsonModule": true` to packages/pixel-office/tsconfig.json's `compilerOptions` (this package has no `typecheck` script today, so this only helps `apps/web`'s consuming typecheck and future tooling — it does not gate any of this plan's required verify commands).

    Rewrite packages/pixel-office/src/sprites/spriteData.ts's `getCharacterSprites(paletteIndex, hueShift)`: import `character-metrocity.json` as a plain ES module JSON import (`import characterData from "./character-metrocity.json";` — Vite and Vitest both natively support this without import-assertion syntax; no runtime dependency on `pngjs` here). Keep the existing `spriteCache` keyed by `${paletteIndex}:${hueShift}` unchanged. Build the `walk` record as: DOWN -> `[down[0], down[1], down[2], down[1]]`; UP -> `[up[0], up[1], up[2], up[1]]`; RIGHT -> `[right[0], right[1], right[2], right[1]]`; LEFT -> the same 3 RIGHT frames each passed through a small local `flipHorizontal(sprite: SpriteData): SpriteData` helper (`sprite.map(row => [...row].reverse())`) — this repo currently has no such helper; add it as a small private function in this file, matching the fork's own documented `flipSpriteHorizontal` behavior. Build `typing` as: DOWN -> `[down[3], down[4]]`, UP -> `[up[3], up[4]]`, RIGHT -> `[right[3], right[4]]`, LEFT -> `[flipHorizontal(right[3]), flipHorizontal(right[4])]`. Build `reading` identically from indices 5-6. When `hueShift !== 0`, pass every frame through `colorize.ts`'s `adjustSprite(frame, { h: hueShift, s: 0, b: 0, c: 0 })` before caching (matching the pre-existing, never-yet-exercised hueShift parameter's documented intent); when `hueShift === 0` (the only value ever passed today), skip the `adjustSprite` call entirely and use the decoded frames directly — do not fabricate per-agent identity colour differentiation that no caller currently requests (YAGNI; `paletteIndex` remains unused, matching the pre-existing behavior this plan does not change). `emptySprite` may be deleted if nothing else references it, or kept as a small private fallback helper if useful for a malformed/zero-frame edge case — do not leave it as the default path.

    Update references/ASSET-LICENSES.md's §1 entry: replace the sentence "(currently returning transparent placeholder frames — no real pixel art has been wired in yet, per 05-01-SUMMARY.md's documented stub; the CC0 credit applies to the pack this module is designed to load)" with an accurate statement naming the exact wired-in asset (`char_0.png`, fetched from the pinned commit `3537e140c2094761beae748592aeb92ece8edfdd`, decoded via `packages/pixel-office/scripts/decode-metrocity-sprites.mjs` into `character-metrocity.json`) and confirming the CC0 credit now genuinely matches what the renderer loads.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test -- spriteData</automated>
    <fails_when>non-zero exit, or the new test finds every cell of any returned frame is an empty string (still fully transparent)</fails_when>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or any pre-existing test (status-mapping, index, handoff-choreography, dialogue-templates) regresses</fails_when>
  </verify>
  <acceptance_criteria>
    - packages/pixel-office/src/sprites/spriteData.test.ts asserts, for at least one direction and one pose (e.g. `sprites.walk[Direction.DOWN][0]`), that at least one cell in the returned `SpriteData` grid is a non-empty hex string.
    - `character-metrocity.json` exists, contains `down`/`up`/`right` arrays of exactly 7 frames each, and each frame is a 32-row array of 16-column strings (matching the existing `emptySprite(16, 32)` dimensions this replaces).
    - `getCharacterSprites`'s exported signature (`(paletteIndex: number, hueShift?: number) => CharacterSprites`) is unchanged.
    - `grep -c "transparent placeholder frames" references/ASSET-LICENSES.md` returns 0 (the stale sentence is gone).
  </acceptance_criteria>
  <done>getCharacterSprites() returns real, non-transparent MetroCity pixel data in the exact shape engine/characters.ts and engine/renderer.ts already consume, with zero changes required to either of those files, and ASSET-LICENSES.md accurately describes the wired-in asset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev-time decode script -> committed JSON asset | `pngjs` (new devDependency) processes a third-party binary (a PNG fetched over HTTPS from GitHub) at development time only; its output is committed and reviewed, never re-fetched or re-decoded at runtime |
| Committed character-metrocity.json -> browser render path | Static, build-time-baked data consumed by `packages/pixel-office`'s existing typed `SpriteData`/`drawSpriteData` pipeline — no new runtime parsing of untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm install (pngjs, devDependency) | high | mitigate | Task 1's `checkpoint:human-verify gate="blocking-human"` verifies pngjs via `npm view` + npm download-count API before Task 2 installs anything |
| T-05-22 | Tampering | decode-metrocity-sprites.mjs (fetches a binary asset over HTTPS from an external host) | medium | mitigate | Fetch URL is pinned to the exact commit SHA (`3537e140c2094761beae748592aeb92ece8edfdd`) already used by every other forked file/attribution header in this phase, never the mutable `main` branch — supply-chain drift is not possible without a deliberate re-run against a different, explicitly-chosen SHA |
| T-05-23 | Tampering | packages/pixel-office/src/sprites/character-metrocity.json (committed, generated data) | low | accept | Reviewed like any other committed source artifact at PR time; the decode script that produced it is itself committed and re-runnable, so provenance is always reproducible/auditable, not opaque |

</threat_model>

<verification>
Run both `<automated>` commands in Task 2. Both must exit 0. `pnpm --filter pixel-office test` must show the FULL suite green (no regression in status-mapping/index/handoff-choreography/dialogue-templates), and the new `spriteData.test.ts` must positively assert non-transparent output, not merely "no error thrown".
</verification>

<success_criteria>
`packages/pixel-office/src/sprites/spriteData.ts`'s `getCharacterSprites()` returns real MetroCity pixel data for every pose in every direction, sourced from the fork's own pinned-commit bundled asset, in the exact shape the existing (unmodified) render pipeline already expects — and `references/ASSET-LICENSES.md` truthfully describes it.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-06-SUMMARY.md` when done
</output>
