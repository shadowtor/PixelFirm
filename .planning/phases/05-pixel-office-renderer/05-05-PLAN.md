---
phase: 05-pixel-office-renderer
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/events.ts
  - apps/api/src/routes/events.test.ts
  - packages/claude-adapter/src/claude-code-runtime.ts
  - packages/claude-adapter/src/claude-code-runtime.test.ts
  - apps/api/src/routes/ws-browser.ts
  - apps/api/src/routes/ws-browser.test.ts
  - apps/web/src/App.tsx
  - apps/web/src/agent-event-mapper.ts
  - apps/web/src/agent-event-mapper.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [OFFICE-01, OFFICE-02, HANDOFF-01, HANDOFF-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A worker credential's POST /events call for task.status_changed, ceo.approval_requested, agent.handoff_requested, or agent.handoff_completed is accepted (202), not silently 403'd — closing CR-01"
    - "Once a handoff completes, the task's tracked owning agentId is reassigned to the receiving agent, so every task.status_changed event emitted afterward attributes to the correct (new) agent — closing CR-02"
    - "A browser socket's first message is always its own connect-time snapshot, never a same-instant broadcast relay of an event posted immediately after it opens — closing WR-01"
    - "An agent.online or session.started event with no sourceAgentId never creates or mutates a pixel-office Character keyed by the literal string 'undefined' — closing WR-02"
    - "REQUIREMENTS.md accurately reflects that OFFICE-02 and HANDOFF-02 are genuinely satisfied (re-confirmed by this plan's own verify commands), while OFFICE-01, OFFICE-03, and HANDOFF-01 remain marked Gaps Found until 05-06/05-07/05-08 land"
  artifacts:
    - path: "apps/api/src/routes/events.ts"
      provides: "WORKER_ALLOWED_EVENT_TYPES extended to the 4 event types ClaudeCodeRuntime actually emits (CR-01)"
    - path: "packages/claude-adapter/src/claude-code-runtime.ts"
      provides: "completeHandoff reassigns TaskRecord.agentId before returning (CR-02)"
    - path: "apps/api/src/routes/ws-browser.ts"
      provides: "registerBrowserSocket called only after the connect-time snapshot has been sent (WR-01)"
    - path: "apps/web/src/agent-event-mapper.ts"
      provides: "deriveCharacterUpsertFromStatusEvent — a pure, guarded event-to-upsert mapper closing WR-02"
  key_links:
    - from: "packages/claude-adapter/src/claude-code-runtime.ts"
      to: "apps/api/src/routes/events.ts"
      via: "postEvent -> POST /events, now accepted for all 4 previously-403'd types"
      pattern: "WORKER_ALLOWED_EVENT_TYPES"
    - from: "packages/claude-adapter/src/claude-code-runtime.ts (completeHandoff)"
      to: "packages/claude-adapter/src/claude-code-runtime.ts (emitStatus)"
      via: "record.agentId, read fresh by every subsequent emitStatus call for the same taskId"
      pattern: "record.agentId = toAgentId"
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/agent-event-mapper.ts"
      via: "deriveCharacterUpsertFromStatusEvent(event) gates every upsertCharacterFromAgent call from the onEvent handler"
      pattern: "deriveCharacterUpsertFromStatusEvent"
  prohibitions:
    - requirement_id: OFFICE-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT credit/attribute an asset pack (e.g. 'MetroCity CC0') in-app or in references/ASSET-LICENSES.md that the shipped renderer does not actually load — attribution claims must track what pixel data is genuinely wired into getCharacterSprites(), never aspirational/placeholder crediting. Resolved by 05-06-PLAN.md wiring the real MetroCity PNG; a human/visual check that the decoded pixels genuinely originate from that asset (not a relabeled placeholder) is recommended before this closes for good, per the fallback's descriptor-less authoring rule (no check_* wired here)."
    - requirement_id: HANDOFF-02
      category: privacy
      status: unresolved
      verification: null
      resolution: null
      reason: "Genuinely out of this phase's boundary (05-CONTEXT.md scopes Phase 5 to OFFICE-01/02/03 and HANDOFF-01/02 only; visibility-tier filtering is Phase 7, SAFE-01/02) — flagged forward, not silently dropped."
      statement: "MUST NOT treat TaskState.title as inherently safe to interpolate into rendered handoff dialogue/bubble text without limit or filtering — a task title is agent-chosen content and this office has no visibility-tier filtering yet (Phase 7 not built), so an oversized or sensitive title would flow straight into rendered (and eventually stream-facing) text unchecked."

user_setup: []
---

<objective>
Close the two BLOCKER defects (CR-01, CR-02) and two WARNING defects (WR-01, WR-02) that 05-REVIEW.md found and 05-VERIFICATION.md confirmed still unresolved in the current codebase — these are the reasons the real event pipeline cannot currently drive `packages/pixel-office`'s renderer end-to-end, independent of the separate invisible-sprite/no-bubble-render gaps closed by 05-06/05-07. Also correct `.planning/REQUIREMENTS.md`'s premature "Gaps Found" marking for OFFICE-02 and HANDOFF-02, which 05-VERIFICATION.md's own evidence already found genuinely satisfied.

**Assumption-delta decision (recorded per the ai-integration/assumption-delta planning contribution):** The orchestrator's deterministic detector flagged HANDOFF-01's roadmap text ("the second agent accepting it") as a possible singular-to-plural agent-model transition. Reading `packages/pixel-office/src/index.ts` directly: `characters` is `new Map<string, Character>()`, keyed by `agentId` — already a multi-agent collection, not a single hardcoded "the agent" variable. `handleHandoffEvent` looks up `fromAgentId`/`toAgentId` as two independent keys into that same map. **Decision: no-change** — the agent/character identity model was already general (N agents keyed by ID) before this closure plan; the roadmap's "second agent" phrasing describes handoff semantics (always exactly two parties), not an architecture that needs promoting. This is a false-positive signal from prose, not architecture.

Purpose: Once these four fixes land, the real `ClaudeCodeRuntime` -> `POST /events` -> `company-core` reducer -> browser WS -> `packages/pixel-office` pipeline can carry a real handoff and a real status change end-to-end without a wire-path defect silently swallowing the event or misattributing it — a precondition for 05-08's live proof and for HANDOFF-01/OFFICE-01 to ever be demonstrated as actually true rather than logically-correct-but-invisible (05-VERIFICATION.md's own words).
Output: `apps/api/src/routes/events.ts` (CR-01), `packages/claude-adapter/src/claude-code-runtime.ts` (CR-02), `apps/api/src/routes/ws-browser.ts` (WR-01), `apps/web/src/agent-event-mapper.ts` (WR-02, new file), corrected `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@.planning/phases/05-pixel-office-renderer/05-REVIEW.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `apps/api/src/routes/events.ts` — `WORKER_ALLOWED_EVENT_TYPES` extended from 3 to 7 members (adds `task.status_changed`, `ceo.approval_requested`, `agent.handoff_requested`, `agent.handoff_completed`).
- `packages/claude-adapter/src/claude-code-runtime.ts` — `completeHandoff(taskId, toAgentId)` now reassigns `TaskRecord.agentId` before posting the completion event.
- `apps/api/src/routes/ws-browser.ts` — connect handler reordered so `registerBrowserSocket(socket)` runs only after the snapshot message has been sent.
- `apps/web/src/agent-event-mapper.ts` — new file, exports `deriveCharacterUpsertFromStatusEvent(event: CompanyEvent): { agentId: string; status: AgentStatus; name?: string } | null`.
- `.planning/REQUIREMENTS.md` — OFFICE-02 and HANDOFF-02 flip from `[ ]`/"Gaps Found" to `[x]`/"Complete"; OFFICE-01, OFFICE-03, HANDOFF-01 stay "Gaps Found".
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01 (event allow-list) and CR-02 (handoff agentId reassignment)</name>
  <files>apps/api/src/routes/events.ts, apps/api/src/routes/events.test.ts, packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts</files>
  <read_first>
    apps/api/src/routes/events.ts (WORKER_ALLOWED_EVENT_TYPES at line 14, enforced at lines 24-27),
    apps/api/src/routes/events.test.ts (existing allow-list rejection test, the exact assertion shape to mirror for the new acceptance cases),
    packages/claude-adapter/src/claude-code-runtime.ts (TaskRecord interface lines 9-27; emitStatus lines 66-73 reads tasks.get(taskId)?.agentId as sourceAgentId; completeHandoff lines 117-123; startTask lines 313-318 sets agentId once; the role-change poll at lines 219-249 fires requestHandoff then completeHandoff back-to-back on a real observed role change),
    packages/claude-adapter/src/claude-code-runtime.test.ts (the "ClaudeCodeRuntime.requestHandoff / gsd role-change poll" describe block, Test 1 at line 366 — same hangingQuery/observeGsdState-mock/runtimeOptions/startInput harness this task's new regression test reuses verbatim),
    .planning/phases/05-pixel-office-renderer/05-REVIEW.md (CR-01, CR-02 — exact defect description and suggested fix)
  </read_first>
  <behavior>
    - Test A (CR-01): POST /events with a schema-valid `task.status_changed` event ({taskId, status: "completed"}) authenticated with a worker token now returns 202, not 403.
    - Test B (CR-01): POST /events with a schema-valid `agent.handoff_completed` event ({taskId, toAgentId}) authenticated with a worker token now returns 202. (`ceo.approval_requested` and `agent.handoff_requested` share the identical `WORKER_ALLOWED_EVENT_TYPES.has()` code path as these two — not independently retested.)
    - Test C (CR-02): reusing Test 1's exact setup (hangingQuery, observeGsdState mocked PM then Engineering, runtime started, timers advanced through both role-poll ticks so requestHandoff+completeHandoff fire with toAgentId "Engineering") — calling `runtime.pauseTask("task-1")` (advancing GRACEFUL_TIMEOUT_MS for it to settle, same pattern Test 1 already uses to clean up) causes a `task.status_changed` envelope with `payload.status === "paused"` whose `sourceAgentId` is `"Engineering"`, never the original `startInput.agentId` ("test-agent-1").
  </behavior>
  <action>
    In apps/api/src/routes/events.ts, change the `WORKER_ALLOWED_EVENT_TYPES` Set literal (line 14) to include all 7 types a worker credential may submit: keep the existing `worker.heartbeat`, `git.worktree_observed`, `gsd.phase_observed`, and add `task.status_changed`, `ceo.approval_requested`, `agent.handoff_requested`, `agent.handoff_completed` — per 05-REVIEW.md CR-01's exact suggested fix. No other change to this file's dedup/insert/broadcast logic.

    In packages/claude-adapter/src/claude-code-runtime.ts, inside `completeHandoff(taskId, toAgentId)` (currently lines 117-123), before the existing `postEvent(...)` call, look up `const record = tasks.get(taskId);` and if it exists set `record.agentId = toAgentId;` — reassigning ownership so every `emitStatus` call for this `taskId` made AFTER this point (from the SAME in-flight `runQuery`'s subsequent `result`/watchdog/pause/cancel paths, or any later call) reads the receiving agent's ID, never the stale original sender's. This is 05-REVIEW.md CR-02's exact suggested fix.

    Add the acceptance-case tests (Test A, Test B) to apps/api/src/routes/events.test.ts inside the existing `describe("POST /events")` block, using the same `buildServer()`/`fastify.inject`/worker-token harness the existing allow-list rejection test already uses. Add the CR-02 regression test (Test C) to packages/claude-adapter/src/claude-code-runtime.test.ts inside the existing `describe("ClaudeCodeRuntime.requestHandoff / gsd role-change poll")` block, copying Test 1's setup verbatim through the two `advanceTimersByTimeAsync(ROLE_POLL_INTERVAL_MS)` calls, then adding the `pauseTask`/`GRACEFUL_TIMEOUT_MS` assertion described above instead of Test 1's plain `cancelTask` cleanup.
  </action>
  <verify>
    <automated>pnpm --filter api test -- events</automated>
    <fails_when>non-zero exit, or either new acceptance test (task.status_changed / agent.handoff_completed) reports a 403 instead of 202</fails_when>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or the new CR-02 regression test asserts sourceAgentId equal to "test-agent-1" instead of "Engineering" for the post-handoff "paused" event</fails_when>
  </verify>
  <acceptance_criteria>
    - `WORKER_ALLOWED_EVENT_TYPES` in apps/api/src/routes/events.ts contains exactly 7 members: worker.heartbeat, git.worktree_observed, gsd.phase_observed, task.status_changed, ceo.approval_requested, agent.handoff_requested, agent.handoff_completed.
    - `grep -c "task.status_changed" apps/api/src/routes/events.ts` and `grep -c "agent.handoff_completed" apps/api/src/routes/events.ts` each return at least 1 inside the Set literal (not only in a comment).
    - `completeHandoff` in claude-code-runtime.ts contains a line assigning `.agentId = toAgentId` before its `postEvent` call.
    - The new events.test.ts cases and claude-code-runtime.test.ts case all pass.
  </acceptance_criteria>
  <done>CR-01 and CR-02 are fixed and each has a passing regression test that would have failed against the pre-fix code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix WR-01 (snapshot-before-register ordering) and WR-02 (sourceAgentId guard)</name>
  <files>apps/api/src/routes/ws-browser.ts, apps/api/src/routes/ws-browser.test.ts, apps/web/src/App.tsx, apps/web/src/agent-event-mapper.ts, apps/web/src/agent-event-mapper.test.ts</files>
  <read_first>
    apps/api/src/routes/ws-browser.ts (the connect handler at lines 22-40 — registerBrowserSocket(socket) currently runs at line 23, before the snapshot query/fold/send at lines 29-32),
    apps/api/src/routes/ws-browser.test.ts (the existing attempt()/nextMessage() harness and the "POST /events relay" describe block this task's new test extends),
    apps/web/src/App.tsx (the onEvent handler's agent.online/session.started branch at lines 42-48, including the `event.sourceAgentId!` non-null assertion on a schema-optional field),
    apps/web/src/ws-client.ts (OfficeSocketHandlers.onEvent's CompanyEvent type — the input type agent-event-mapper.ts's new function accepts),
    packages/event-schema/src/envelope.ts (BaseEnvelope.sourceAgentId: z.string().optional() — confirms this field is genuinely optional, not a type-only artifact),
    .planning/phases/05-pixel-office-renderer/05-REVIEW.md (WR-01, WR-02 — exact defect description and suggested fix)
  </read_first>
  <behavior>
    - Test A (WR-01): opening an authenticated browser socket and, without any artificial delay, immediately POSTing a real schema-valid worker.heartbeat event to /events, the FIRST message the socket receives is always `{type:"snapshot",...}`, never `{type:"event",...}` — proving the send-then-register order guarantee, not just a usually-fast DB query.
    - Test B (WR-02): `deriveCharacterUpsertFromStatusEvent` returns null for an `agent.online` (or `session.started`) event whose `sourceAgentId` is undefined — the exact malformed-but-schema-valid shape that previously produced a bogus "undefined"-keyed character.
    - Test C (WR-02): `deriveCharacterUpsertFromStatusEvent` returns `{agentId, status: AgentStatus.IDLE, name}` for a well-formed `agent.online` event with both `sourceAgentId` and `payload.name` set, and `{agentId, status: AgentStatus.CODING, name: undefined}` for a well-formed `session.started` event.
    - Test D: `deriveCharacterUpsertFromStatusEvent` returns null for any other event type (e.g. `worker.heartbeat`).
  </behavior>
  <action>
    In apps/api/src/routes/ws-browser.ts's connect handler, move the `registerBrowserSocket(socket);` call so it runs AFTER `socket.send(JSON.stringify({ type: "snapshot", state }));` — i.e. build the snapshot first (the existing `db.select().from(events).orderBy(events.occurredAt)` / `fold` / `socket.send` sequence unchanged), then register the socket for live broadcast, then attach the existing `close`/`error` listeners. This is 05-REVIEW.md WR-01's exact suggested fix: no event broadcast by `broadcastToBrowsers` can reach this socket before its own baseline snapshot, since the socket isn't in the broadcast set yet while the snapshot is being built.

    Create apps/web/src/agent-event-mapper.ts exporting `deriveCharacterUpsertFromStatusEvent(event: CompanyEvent): { agentId: string; status: AgentStatus; name?: string } | null` — a pure function with no side effects: returns null unless `event.type` is `"agent.online"` or `"session.started"` AND `event.sourceAgentId` is truthy; when both hold, returns `{ agentId: event.sourceAgentId, status: event.type === "agent.online" ? AgentStatus.IDLE : AgentStatus.CODING, name: event.type === "agent.online" ? event.payload.name : undefined }`. Import `AgentStatus` from `event-schema` and `CompanyEvent` as a type-only import from `event-schema`.

    In apps/web/src/App.tsx's onEvent handler, replace the existing `if (event.type === "agent.online" || event.type === "session.started") { upsertCharacterFromAgent(event.sourceAgentId!, ...) }` block with: `const upsert = deriveCharacterUpsertFromStatusEvent(event); if (upsert) upsertCharacterFromAgent(upsert.agentId, upsert.status, upsert.name);` — importing `deriveCharacterUpsertFromStatusEvent` from `./agent-event-mapper`. No other change to `onEvent`'s `task.created`/handoff branches.

    Add the WR-01 regression test to apps/api/src/routes/ws-browser.test.ts inside a new `describe("GET /ws/browser ordering (WR-01 regression)")` block, reusing `attempt()`/`nextMessage()` exactly as the existing "POST /events relay" test does, but WITHOUT the existing test's `await nextMessage(result.ws)` snapshot-drain call before firing the POST — fire the POST immediately after `attempt()` resolves (socket open) and assert the very first message received has `type === "snapshot"`.
  </action>
  <verify>
    <automated>pnpm --filter api test -- ws-browser</automated>
    <fails_when>non-zero exit, or the new ordering test observes a first message with type "event" instead of "snapshot"</fails_when>
    <automated>pnpm --filter web test -- agent-event-mapper</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - In ws-browser.ts, the line calling `registerBrowserSocket(socket)` appears textually after the line calling `socket.send(JSON.stringify({ type: "snapshot"` (grep line-number comparison, or direct read).
    - `packages/pixel-office`-facing App.tsx no longer contains the non-null assertion `event.sourceAgentId!`.
    - agent-event-mapper.test.ts covers: missing sourceAgentId -> null, well-formed agent.online -> IDLE+name, well-formed session.started -> CODING, unrelated event type -> null.
  </acceptance_criteria>
  <done>WR-01 and WR-02 are fixed and each has a passing regression test that would have failed (or been flaky in WR-01's case) against the pre-fix code.</done>
</task>

<task type="auto">
  <name>Task 3: Correct REQUIREMENTS.md's premature Gaps-Found marking for OFFICE-02 and HANDOFF-02</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (the `- [ ] **OFFICE-02**` / `- [ ] **HANDOFF-02**` checkbox lines under "### Pixel Office" / "### Handoffs", and the Traceability table rows `| OFFICE-02 | Phase 5 | Gaps Found |` / `| HANDOFF-02 | Phase 5 | Gaps Found |`),
    .planning/phases/05-pixel-office-renderer/05-VERIFICATION.md (Requirements Coverage table: OFFICE-02 "✓ SATISFIED — LICENSE, footer, ASSET-LICENSES.md all confirmed present"; HANDOFF-02 "✓ SATISFIED — Pure template function, grep-proven zero network/LLM surface")
  </read_first>
  <action>
    Re-confirm (do not assume) 05-VERIFICATION.md's OFFICE-02/HANDOFF-02 evidence is still accurate by re-running its own cited checks against the current codebase: `packages/pixel-office/LICENSE` exists and contains the verbatim MIT text; `apps/web/src/App.tsx` contains the `pixel-agents-hq/pixel-agents` attribution string inside a `<footer>`; `references/ASSET-LICENSES.md` exists with its 4 sections; the `pixel-office` dialogue-templates test suite (its zero-network-surface grep-style test) passes. Once re-confirmed, edit `.planning/REQUIREMENTS.md` with the `Edit` tool (scoped replacement, never a full-file `Write`): change `- [ ] **OFFICE-02**:` to `- [x] **OFFICE-02**:` and `- [ ] **HANDOFF-02**:` to `- [x] **HANDOFF-02**:`; in the Traceability table, change `| OFFICE-02 | Phase 5 | Gaps Found |` to `| OFFICE-02 | Phase 5 | Complete |` and `| HANDOFF-02 | Phase 5 | Gaps Found |` to `| HANDOFF-02 | Phase 5 | Complete |`. Leave OFFICE-01, OFFICE-03, and HANDOFF-01's checkbox and Traceability rows exactly as `[ ]`/"Gaps Found" — those remain genuinely open until 05-06/05-07/05-08 land and a future re-verification confirms them.
  </action>
  <verify>
    <automated>grep -c "OFFICE-02\*\*\|HANDOFF-02\*\*" .planning/REQUIREMENTS.md | head -1 && grep -c "\- \[x\] \*\*OFFICE-02\*\*" .planning/REQUIREMENTS.md && grep -c "\- \[x\] \*\*HANDOFF-02\*\*" .planning/REQUIREMENTS.md && grep -c "| OFFICE-02 | Phase 5 | Complete |" .planning/REQUIREMENTS.md && grep -c "| HANDOFF-02 | Phase 5 | Complete |" .planning/REQUIREMENTS.md && grep -c "\- \[ \] \*\*OFFICE-01\*\*" .planning/REQUIREMENTS.md && grep -c "\- \[ \] \*\*OFFICE-03\*\*" .planning/REQUIREMENTS.md && grep -c "\- \[ \] \*\*HANDOFF-01\*\*" .planning/REQUIREMENTS.md</automated>
    <fails_when>any grep in the chain returns 0 (a checkbox or Traceability row did not flip as expected, or OFFICE-01/03/HANDOFF-01 were incorrectly flipped to complete), or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - OFFICE-02 and HANDOFF-02 are `[x]`/"Complete" in both the checkbox list and the Traceability table.
    - OFFICE-01, OFFICE-03, and HANDOFF-01 remain `[ ]`/"Gaps Found" (unchanged by this task).
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately reflects which of this phase's 5 requirements are genuinely satisfied today versus still gap-blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker credential -> POST /events | A single bearer credential per worker already fully controls that worker's own reported git/GSD/task state (established Phase 2-4 trust model); this plan widens which event *types* that already-trusted credential may submit, not who may submit them |
| Browser client -> apps/api /ws/browser | Existing INTERNAL-tier-only trust boundary (T-05-03, accepted in 05-01); this plan only reorders when a socket joins the broadcast set, it does not change who may connect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18 | Tampering | apps/api/src/routes/events.ts (WORKER_ALLOWED_EVENT_TYPES widened) | medium | accept | A worker credential already fully controls its own worker's reported state (SEC-02's one-bearer-credential-per-worker model, established Phase 2); widening which event *types* that same already-trusted credential may submit does not cross a new trust boundary — no cross-check exists (or is newly required) between `request.workerId` and the event's `sourceAgentId`/`fromAgentId`, consistent with every event type this route already accepted |
| T-05-19 | Repudiation / Information Disclosure | packages/claude-adapter/src/claude-code-runtime.ts (completeHandoff reassigns agentId) | low | accept | Pure in-memory bookkeeping computed from data the SAME authenticated runtime instance already generated (fromAgentId/toAgentId both originate from this runtime's own observeGsdState-derived role, never untrusted wire input at this point) |
| T-05-20 | Information Disclosure | apps/api/src/routes/ws-browser.ts (WR-01 reorder) | low | mitigate | Fix only narrows an existing race that could drop a client's view of an event, not widen exposure — no new data reaches any client that couldn't already reach it |
| T-05-21 | Tampering | apps/web/src/agent-event-mapper.ts (WR-02 guard) | low | mitigate | Guards against a schema-valid-but-identity-less event silently fabricating a garbage "undefined"-keyed character — strictly reduces attack/bug surface versus the pre-fix `!` assertion |

</threat_model>

<verification>
Run all 4 `<automated>` commands above (2 in Task 1, 2 in Task 2, 1 chained set in Task 3). All must exit 0. The events.test.ts and claude-code-runtime.test.ts suites must report their new cases passing; the REQUIREMENTS.md grep chain must confirm exactly OFFICE-02/HANDOFF-02 flipped and OFFICE-01/OFFICE-03/HANDOFF-01 unchanged.
</verification>

<success_criteria>
A real ClaudeCodeRuntime instance can post task.status_changed, ceo.approval_requested, agent.handoff_requested, and agent.handoff_completed events through the real POST /events route without a 403; a completed handoff correctly re-points future status events to the receiving agent; a browser socket never receives a live-relayed event before its own snapshot; and no malformed status event can create a bogus "undefined" character. REQUIREMENTS.md's status for OFFICE-02/HANDOFF-02 matches reality.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-05-SUMMARY.md` when done
</output>