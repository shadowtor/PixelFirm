---
phase: 05-pixel-office-renderer
plan: 16
type: execute
gap_closure: true
wave: 9
depends_on: ["05-13", "05-14"]
files_modified:
  - scripts/verify-pixel-office-live.mjs
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
autonomous: true
requirements: [HANDOFF-01, HANDOFF-02, OFFICE-01, OFFICE-03]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a real canvas in a real headless browser, with the page opened once and never navigated, the handoff's templated dialogue line is painted while the sender waits at the receiver's desk: dialogue-box pixels exist, their horizontal extent contains the speaking agent's column centre, they lie entirely above that agent's sprite, and non-box (text) pixels exist inside the box above the glyph slot"
    - "On the same canvas, zero dialogue-box pixels remain once the handoff sequence has ended (sender back at its own desk)"
    - "The dialogue pixels the proof counts carry both 05-13 caps live: the proof's handoff task has no title (the FSM falls back to the 23-character task id) and the receiver has no name (fallback 19-character agent id)"
    - "The four existing live truths (live sprite, live blocked glyph, handoff task icon appears and clears, blocked glyph owner-bound) still pass after 05-13's three-pass renderer and 05-14's desk/hue allocation"
    - "05-UI-SPEC.md describes dialogue rendering as shipped and no longer calls it out of scope; its UI Considerations rows match the code"
    - statement: "A WS socket close/error surfaces a user-observable signal rather than failing silently"
      verification: backstop
  artifacts:
    - scripts/verify-pixel-office-live.mjs
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
  key_links:
    - "real POST /events handoff pair -> /ws/browser relay -> apps/web applyLiveEvent + handleHandoffEvent -> renderScene dialogue pass -> getImageData pixels in DIALOGUE_BOX_COLOR"
    - "packages/pixel-office/src/constants.ts DIALOGUE_BOX_COLOR / BUBBLE_ICON_GAP_PX / BUBBLE_ICON_HEIGHT_PX -> read by the harness at run time, never restated"
  prohibitions:
    - statement: "MUST NOT run the live proof through a pre-existing API or web dev server (a reused server may be connected to a developer's own append-only database)"
      status: resolved
      verification: precondition
    - statement: "MUST NOT treat mechanically painted dialogue pixels as proof the line is legible at stream scale"
      status: open
      verification: judgment
---

<objective>
Give gap 1 the pixel-level evidence the brief requires: 05-13 proves the dialogue reaches `renderScene`'s draw calls; this plan proves it reaches a real canvas's pixels on the live relay path, owner-bound, and leaves nothing behind when the sequence ends — the "seam" test the verifier said was missing (every earlier test asserted the string, none its arrival on screen). It extends the proof the verifier already ran twice (`scripts/verify-pixel-office-live.mjs`) rather than inventing a third harness, and re-runs all its existing truths against 05-13's renderer and 05-14's seating.

Then it records the dialogue contract in 05-UI-SPEC.md, which still says dialogue rendering is deliberately out of scope — a statement 05-CONTEXT.md **D-04** ("Dialogue/text shown during the sequence") overrides — and corrects its stale UI Considerations rows (05-13 planner assumption A6).

Purpose: close the seam between "a string was generated" and "a viewer can see it" with observed pixels.
Output: TRUTH 5 in the live proof, one passing run, and an accurate UI contract.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-pixel-office-renderer/05-CONTEXT.md
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
@.planning/phases/05-pixel-office-renderer/05-12-SUMMARY.md
@.planning/phases/05-pixel-office-renderer/05-13-SUMMARY.md
@.planning/phases/05-pixel-office-renderer/05-14-SUMMARY.md
</context>

<interface_context>
`scripts/verify-pixel-office-live.mjs` (700 lines):

- Reads engine constants by regex: `readColorConst(name)` (`NAME = "#rrggbb"` in constants.ts) and `readNumberConst(src, where, name)` (`NAME = digits`); `DESK_ROW_START`/`DESK_ROW_PITCH` from index.ts. `deskPosition(slot)` / `claimDesk(agentId)` mirror creation-order seating; `tileColumnRange(col)`; `spriteTopY(row) = row*TILE_SIZE + TILE_SIZE/2 - SPRITE_HEIGHT` (un-offset anchor — correct for an IDLE sender).
- `scanCanvas(page, xRange = null)` runs one `page.evaluate` over `getImageData`, returns `{ width, height, scale, sprite, blockedHits, handoffHits, blockedMinY, blockedMaxY }`; coordinates reported back in unzoomed grid units via `scale`.
- `main()`: reset test container (`db:test:down`/`up`), migrations, `ensureServer` for the API (`${API_URL}/health`) and web (`http://localhost:5177`) — **reuses any server already answering** (review WR-06), issue a worker credential, open the office once, assert empty, then TRUTH 1 (baseline sprites; baseline must carry 0 blocked and 0 handoff px), TRUTH 2 (blocked), TRUTH 3 (post `agent.handoff_requested` for SENDER -> RECEIVER on `HANDOFF_TASK`, `sleep(2500)`, `handoffScan`; post `agent.handoff_completed`, `sleep(1500)`, `clearedScan`), TRUTH 4 (19-agent cohort, blocked glyph band), then `LIVE PROOF: PASS`.
- SENDER is created first (slot 0), BLOCKED second (slot 1), RECEIVER by the handoff event (slot 2, `receiverDesk`). At ICON_VISIBLE the sender stands IDLE on the receiver's desk tile. No `task.created` is posted (not in `WORKER_ALLOWED_EVENT_TYPES`), so the dialogue interpolates the raw task id and raw receiver id — both longer than 05-13's caps.

After 05-13: `packages/pixel-office/src/constants.ts` exports `DIALOGUE_BOX_COLOR`, `DIALOGUE_TEXT_COLOR` (6-digit hex), `BUBBLE_ICON_GAP_PX`, `BUBBLE_ICON_HEIGHT_PX` (numeric literals); renderScene draws sprites -> dialogue boxes -> glyphs; the dialogue box's top edge is `max(0, glyphSlotTop - BUBBLE_ICON_GAP_PX - DIALOGUE_BOX_HEIGHT_PX)` where `glyphSlotTop = spriteTop - BUBBLE_ICON_GAP_PX - BUBBLE_ICON_HEIGHT_PX`. For the proof's row-3 speaker: sprite top 24, glyph slot top 9, box y 0..12.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: TRUTH 5 — the handoff dialogue is painted on the live canvas, owner-bound, and gone when the sequence ends</name>
  <precondition>`docker info` exits 0, and `netstat -ano | grep LISTENING | grep -E ':(3000|5177) '` prints nothing — so the harness spawns its OWN API and web servers against the test container instead of reusing one that may be connected to a developer database (review WR-06). If either port is taken, stop and report; do not kill a process you did not start.</precondition>
  <files>scripts/verify-pixel-office-live.mjs</files>
  <read_first>
    - scripts/verify-pixel-office-live.mjs (whole file)
    - packages/pixel-office/src/constants.ts (after 05-13 — the four constants read below)
    - packages/pixel-office/src/engine/renderer.ts (after 05-13 — `resolveDialogueBox` and the three-pass order, to confirm the geometry above)
    - packages/pixel-office/src/handoff/handoff-choreography.ts (after 05-13 — when the requested line is set and when the accepted line is cleared)
    - .planning/phases/05-pixel-office-renderer/05-VERIFICATION.md (the proof output format the verifier recorded)
  </read_first>
  <action>
Read `DIALOGUE_BOX_COLOR` with `readColorConst`, and `BUBBLE_ICON_GAP_PX` and `BUBBLE_ICON_HEIGHT_PX` with `readNumberConst(constantsSrc, "constants.ts", ...)` — never restate them. Add a comment that DIALOGUE_BOX_COLOR's distinctness from every colour a character (at all twelve identity hues) or glyph can paint is enforced by 05-13's renderer colour-guard test, which is what makes a pixel count of it unambiguous (the raw-palette partition used for BLOCKED_COLORS/HANDOFF_COLORS does not cover hue shifts — review WR-04, out of scope here).

Extend `scanCanvas` with two optional inputs — the dialogue box colour (always passed) and a `textAboveY` bound in unzoomed grid units — and four outputs: `dialogueHits` (pixels exactly the box colour), the box-colour bounding box `dialogueMinX`/`dialogueMaxX`/`dialogueMinY`/`dialogueMaxY` in unzoomed units (null when none), and `dialogueTextPx`: the count of pixels inside that bounding box's x-range, from its top row down to but excluding `textAboveY`, whose colour is NOT the box colour (the painted text; rows at and below `textAboveY` are excluded because state glyphs are drawn on top there). Keep the existing return fields and their meaning unchanged.

In `main()`:
- TRUTH 1 baseline: also assert `baseline.dialogueHits === 0`, alongside the existing clean-baseline asserts, so TRUTH 5 cannot pass on leftover pixels.
- Inside TRUTH 3, right after `handoffScan`, run a dialogue scan over the whole canvas with `textAboveY` = the speaker's glyph-slot top, where the speaker is the sender standing on `receiverDesk`: `spriteTopY(receiverDesk.row) - BUBBLE_ICON_GAP_PX - BUBBLE_ICON_HEIGHT_PX`. Assert, each with a message that prints the measured numbers: `dialogueHits > 0`; `dialogueMinX <= receiverDesk.col * TILE_SIZE + TILE_SIZE / 2 <= dialogueMaxX` (the box contains the speaker's column centre); `dialogueMaxY < spriteTopY(receiverDesk.row)` (entirely above the speaker's sprite); `dialogueTextPx > 0` (text actually painted inside the box).
- After the existing `clearedScan`: assert `clearedScan.dialogueHits === 0` — the sender is home, so the receiver's accepted line has been cleared and nothing is stranded.
- Log `TRUTH 5 PASS — <hits> dialogue-box px + <textPx> text px at x a..b, y c..d, above the speaker's sprite (top <y>), cleared after the sequence`.
- Update the header comment's list of truths to include TRUTH 5, and add one line: dialogue is shown for its sequence only (requested line while the sender waits; accepted line until the sender is home).

Run the proof once. Afterwards confirm no dev server was leaked.
  </action>
  <verify>
    <automated>node --check scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit or any SyntaxError on stderr</fails_when>
    <automated>node scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit, or "TRUTH FAILED" in the output, or the final line is not "LIVE PROOF: PASS", or no line starting "[live-proof] TRUTH 5 PASS"</fails_when>
    <automated>netstat -ano | grep LISTENING | grep -E ':(3000|5177) ' ; test $? -eq 1</automated>
    <fails_when>non-zero exit (a listener remains on 3000 or 5177 after the run)</fails_when>
    <human-check>During the proof's handoff window (or by opening http://localhost:5177 and posting a handoff pair the same way the harness does), look at the office at the size you will stream it: can a viewer read the dialogue line, and does it read as the walking agent's? Judgment item — whatever you conclude, record it; it does not block this plan.</human-check>
  </verify>
  <acceptance_criteria>
    - The run's output contains `[live-proof] TRUTH 1 PASS`, `TRUTH 2 PASS`, `TRUTH 3 PASS`, `TRUTH 4 PASS`, `TRUTH 5 PASS` and ends `LIVE PROOF: PASS`; the SUMMARY quotes those lines verbatim with the measured numbers.
    - `grep -c 'page.reload' scripts/verify-pixel-office-live.mjs` is 0 and `grep -c 'page.goto' scripts/verify-pixel-office-live.mjs` is at least 1 (still one navigation; 05-12's property preserved).
    - `grep -c 'DIALOGUE_BOX_COLOR' scripts/verify-pixel-office-live.mjs` is at least 1 and `grep -c 'BUBBLE_ICON_HEIGHT_PX' scripts/verify-pixel-office-live.mjs` is at least 1 (read from source, not restated).
    - `grep -cE 'DROP DATABASE|DROP SCHEMA|DELETE FROM' scripts/verify-pixel-office-live.mjs` is 0 (05-12's no-destructive-SQL property preserved).
    - After the run, `netstat -ano | grep LISTENING | grep -E ':(3000|5177) '` prints nothing.
  </acceptance_criteria>
  <done>The live proof observes the handoff dialogue on a real canvas — present, owner-bound and textual during the sequence, absent after it — and all five truths pass in one run.</done>
</task>

<task type="auto">
  <name>Task 2: 05-UI-SPEC.md records the dialogue contract as shipped and its stale rows are corrected</name>
  <files>.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md (whole file — Sprite & Icon Asset Inventory "Known gap" bullets, Spacing exception, Typography paragraph, Color table, Copywriting dialogue rows, UI Considerations table and its count line)
    - packages/pixel-office/src/constants.ts and packages/pixel-office/src/handoff/dialogue-templates.ts (after 05-13 — the exact constants and caps to quote)
    - packages/pixel-office/src/index.ts (after 05-14 — seating and hue behaviour to quote)
    - .planning/phases/05-pixel-office-renderer/05-CONTEXT.md D-04 and its `<supersessions>` block (the dated-note format 05-12 used)
  </read_first>
  <action>
Edit 05-UI-SPEC.md in place; do not touch the Checker Sign-Off section.

1. Typography: replace the paragraph that says no `fillText` exists and that drawing `bubbleText` is out of scope with the shipped contract — handoff dialogue is drawn by `renderScene`'s dialogue pass in `DIALOGUE_FONT_PX * zoom` px monospace (the reserved 11px scale, no second type system), inside an owner-anchored box `DIALOGUE_BOX_HEIGHT_PX` tall, horizontally centred on its speaker and clamped to the canvas, stacked directly above the speaker's glyph slot (clamped at y = 0 for desk row 3), drawn after all sprites and before all state glyphs. Add the 16px-grid interpretation from 05-13 planner assumption A2: like the 11x13 glyph, the box is anchored to its owner inside the tile-grid canvas rather than tile-aligned.
2. Color table: add a row for the dialogue box (`DIALOGUE_BOX_COLOR`, achromatic, never an accent and never a state signal) and a row for dialogue text (`DIALOGUE_TEXT_COLOR`), each naming the colour-guard test that proves no character (any identity hue) or glyph can paint it.
3. Copywriting: under the two locked dialogue rows, add the length rule — `taskTitle` capped at `MAX_DIALOGUE_TITLE_CHARS` (16) and `toAgentName` at `MAX_DIALOGUE_NAME_CHARS` (12) code points, truncated with U+2026; templates unchanged.
4. UI Considerations: long-text row -> ✅ covered (caps in `resolveHandoffDialogue`, 05-13 tests, live TRUTH 5 draws a capped line); zero-one-many row -> ✅ covered restated as 1:1 up to 54 concurrently seated, desks reclaimed on despawn (05-14), 55+ stack (accepted ceiling in deferred-items.md); the three ⚠ unresolved rows (invisible sprites, missing glyph draw pass, missing permission/waiting assets) -> ✅ covered, each citing the plan that resolved it (05-06, 05-07, 05-07) and the live truth that observed it (TRUTH 1, TRUTH 2/4); error row stays 🧪 backstop with a note that 05-09 added the banner and 05-VERIFICATION.md observed it in a real browser. Recompute the "Applicable state considerations resolved" count line from the final rows (7 covered, 1 backstop, 0 unresolved).
5. Sprite & Icon Asset Inventory: mark each "Known gap the closure plan must resolve" bullet as resolved with the plan that resolved it, and list `bubble-permission.json`, `bubble-waiting.json` and `bubble-handoff-task.json` in the asset table (they exist on disk; `ls packages/pixel-office/src/sprites/*.json` lists 12 glyph files plus the character sheet).
6. Append a dated supersession note (05-16, 2026-09-22) in the same form 05-12 used in 05-CONTEXT.md: the earlier Typography statement that dialogue rendering was out of scope conflicted with D-04 ("Dialogue/text shown during the sequence"); D-04 is a locked user decision and governs; 05-13 implemented the draw pass and 05-16 observed it live. No decision in 05-CONTEXT.md is edited.
  </action>
  <verify>
    <automated>grep -c 'DIALOGUE_BOX_COLOR' .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</automated>
    <fails_when>output is 0</fails_when>
    <automated>grep -c 'MAX_DIALOGUE_TITLE_CHARS' .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</automated>
    <fails_when>output is 0</fails_when>
    <automated>grep -c '⚠ unresolved |' .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</automated>
    <fails_when>output is anything other than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Superseded by 05-16' .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` is at least 1.
    - `grep -c 'bubble-handoff-task.json' .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` is at least 1.
    - `grep -n 'Applicable state considerations resolved' .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` shows the line reading 7 covered, 1 backstop, 0 unresolved.
    - `grep -c 'Approval:' .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` is at least 1 and the Checker Sign-Off lines are unchanged (compare against `git show HEAD:.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` taken before the edit).
    - `git diff --stat "$TASK_START"..HEAD -- .planning/phases/05-pixel-office-renderer/05-CONTEXT.md` prints nothing (TASK_START = the HEAD sha recorded before this task's first commit).
  </acceptance_criteria>
  <done>The UI contract describes the dialogue the office now draws, the caps it applies, and the colours it reserves, and every UI Considerations row matches the code.</done>
</task>

</tasks>

<threat_model>
Security config: `security_asvs_level: 1`, `security_block_on: high`. No threat in this register is rated high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-proof harness -> local API/web dev servers -> Postgres | the harness posts real events and issues a real worker credential through whatever server answers on its ports |
| proof artifact -> verification decision | a passing TRUTH is treated as evidence that a requirement is met |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-16-01 | Tampering | `ensureServer` reusing a dev server connected to a developer's append-only database (review WR-06) — synthetic rows that can never be removed | medium | mitigate | Task 1 `<precondition>`: both ports must be free before the run, so the harness spawns its own servers against the compose test container; stop rather than kill an unknown process. The code-level fix stays routed to `/gsd-code-review 05 --fix`. |
| T-05-16-02 | Repudiation | a TRUTH that passes on the wrong pixels | medium | mitigate | Task 1: clean-baseline assert for dialogue pixels; owner-bound and text-present assertions, not a bare colour count; colour distinctness proven by 05-13's guard test across all identity hues. |
| T-05-16-03 | Denial of Service | leaked dev servers holding 3000/5177 after the run | low | mitigate | Task 1 verify: post-run listener check. |
| T-05-16-04 | Information Disclosure | synthetic task/agent ids in the test container | low | accept | Unchanged from T-05-12-04: local test container only, cleared by the next run's volume reset. |
</threat_model>

<planner_assumptions>
- **D1 — One run is sufficient here.** The per-run clean-store property was already observed twice by the verifier; this plan changes assertions, not the reset mechanism.
- **D2 — Observing the accepted line live is not asserted.** It is shown only while the sender walks home (two tiles, ~0.7 s in the proof's layout), too short a window to sample reliably; its draw is proven at unit level in 05-13 and its clearing is asserted live here.
- **D3 — Human-verification items 1-4 from 05-VERIFICATION.md are not closed by this plan.** The dialogue legibility check is added alongside them as a judgment item.
</planner_assumptions>

<artifacts_this_phase_produces>
- `scripts/verify-pixel-office-live.mjs`: `scanCanvas` gains `dialogueHits`, `dialogueMinX/MaxX/MinY/MaxY`, `dialogueTextPx` and a `textAboveY` input; TRUTH 5 assertions and log line; dialogue clean-baseline assert
- `.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md`: dialogue typography/colour/copy contract, corrected UI Considerations rows and count, updated asset inventory, dated supersession note
</artifacts_this_phase_produces>

<verification>
- `node scripts/verify-pixel-office-live.mjs` exits 0 with TRUTH 1-5 PASS.
- No listeners remain on 3000/5177 afterwards.
- 05-UI-SPEC.md no longer contains an ⚠ unresolved row.
</verification>

<success_criteria>
- Gap 1 has pixel-level evidence on the live path, not only draw-call evidence.
- The design contract matches the shipped renderer.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-16-SUMMARY.md` when done.
</output>
