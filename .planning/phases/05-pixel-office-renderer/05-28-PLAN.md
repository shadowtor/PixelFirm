---
phase: 05-pixel-office-renderer
plan: 28
type: execute
gap_closure: true
gap_ids: [G-05-1c]
wave: 18
depends_on: ["05-27"]
files_modified:
  - packages/pixel-office/src/engine/renderer.ts
  - packages/pixel-office/src/constants.ts
  - packages/pixel-office/src/engine/renderer.test.ts
  - scripts/verify-pixel-office-live.mjs
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
autonomous: true
requirements: [OFFICE-03]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "G-05-1c: every state glyph is anchored to its owner's visible head, not the 16x32 frame box: the glyph's lowest ink row sits exactly BUBBLE_ICON_GAP_PX = 1 px above the first opaque row of the owner's current sprite frame, for every pose (standing, seated with the sitting offset, walking in any direction) and every one of the 12 glyphs (previously 5-6 px of air)"
    - "The glyph stays horizontally centred on its owner (unchanged), and the owner-bound fallback for a head with no headroom still attaches it to the owner, never to a canvas row a neighbour owns (05-10 CR-02, kept)"
    - "No glyph straddles the wall: for every layout seat and standing spot, in both the standing and the seated pose, every glyph's ink lies on floor tiles (y >= 16 and inside cols 1-18 at zoom 1)"
    - "A blocked agent on seat row 8 keeps its glyph strictly below the sprite box of the agent seated in front of it on row 4 (CR-02 intent on the new layout)"
    - "Live: TRUTH 4 measures the blocked glyph's ink within 2 px above its owner's head top and entirely on the floor"
  artifacts:
    - packages/pixel-office/src/engine/renderer.ts
    - scripts/verify-pixel-office-live.mjs
  key_links:
    - "renderScene layout (spriteData, drawY) -> firstOpaqueRow(spriteData) -> resolveBubbleY(drawY, headTopRow, glyphInkBottomRow, zoom) -> drawGlyph"
  prohibitions:
    - statement: "MUST NOT relocate a glyph onto a canvas row another agent owns (05-10 CR-02, kept)"
      status: resolved
      verification: test
    - statement: "MUST NOT change the pass order or draw glyphs behind any sprite (05-13, kept)"
      status: resolved
      verification: test
---

<objective>
Close UAT gap G-05-1c (Medium): the red/orange/blue icons read as a detached legend. Root cause (.planning/debug/glyph-anchoring.md), an AND of three conditions: (1) `resolveBubbleY` anchors to the sprite frame top, so the IDLE frame's 3 transparent rows plus the glyph's own empty bottom row leave 6 px of air instead of 2; (2) the first desk row put glyphs across the wall edge; (3) stuck agents stand while working agents sit, so their glyphs formed one strip. 05-24 already fixed (2) — seat rows 4 and 8 put every glyph on the floor — and seated/standing heights now read as "stopped working" at a desk rather than a scattered line. This plan fixes (1) and locks (2) with a test.

Planner decisions: anchor on the opaque head per frame (computed from the frame's pixels, so a re-decoded sheet cannot silently break it) with 1 px of visible air; the optional 1-2 px tail from the gap's missing list is not added — with the glyph sitting 1 px above the hair it already reads as attached; add it only if UAT still reports detachment. D-03 (glyph overlay + frozen pose) unchanged.

Output: head-anchored glyph placement, gap/wall/pod-pair tests, live TRUTH 4 on the head, UI-SPEC placement note.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-UAT.md
@.planning/debug/glyph-anchoring.md
@.planning/phases/05-pixel-office-renderer/05-27-SUMMARY.md
@packages/pixel-office/src/engine/renderer.ts
</context>

<interface_context>
Orientation only.
- renderer.ts: `resolveBubbleY(drawY, bubbleHeight, zoom)` = `drawY - bubbleHeight*zoom - BUBBLE_ICON_GAP_PX*zoom` if >= 0, else `max(0, drawY)`; after 05-27 only `drawGlyph` calls it. `drawGlyph` centres horizontally on the owner.
- Measured opaque row ranges: down frames walk 0/1/2 rows 2..29 / 3..30 / 2..29, typing 3/4 rows 6..31; up frames 2..29 / 3..30 / 2..29, typing 3..25; right frames 4-5..30. Glyph ink after 05-23: blocked rows 0..11; permission/waiting per their re-authored grids; handoff-task rows 0..9.
- renderer.test.ts: describe "resolveBubbleY — owner-bound glyph placement (CR-02)" calls `resolveBubbleY` directly; the 05-24-rebased CR-02 composite (agent-1 at (1,4), agent-9 blocked at (1,8)); the horizontal-containment test; `spriteBox(ch)`.
- Harness TRUTH 4 (after 05-25) asserts the blocked glyph lies strictly between the front agent's sprite bottom and its owner's sprite frame top (`spriteTopY`, un-offset anchor).
- Layout (05-24): seats rows 4 and 8, standing spots row 7; floor starts at y 16.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Anchor each glyph 1 px above its owner's visible head</name>
  <reversibility rating="reversible">One placement function and one constant.</reversibility>
  <files>packages/pixel-office/src/engine/renderer.ts, packages/pixel-office/src/constants.ts, packages/pixel-office/src/engine/renderer.test.ts</files>
  <read_first>
    - packages/pixel-office/src/engine/renderer.ts (whole file)
    - packages/pixel-office/src/engine/renderer.test.ts (resolveBubbleY describe, CR-02 composite, horizontal containment)
    - .planning/debug/glyph-anchoring.md (Evidence + suggested_fix_direction)
  </read_first>
  <behavior>
    - "1 px of air above the head, every pose and glyph": for each of the 12 glyphs and each of these owners — IDLE (standing on a seat), TYPE on its own seat (sitting offset), TYPE off-seat, WALK frames facing down, up and right — rendered at zoom 1 and zoom 3, (the glyph's lowest ink row bottom edge) + BUBBLE_ICON_GAP_PX * zoom equals the owner's first opaque sprite row y.
    - "no glyph on the wall": for every SEATS and STANDING_SPOTS tile, in IDLE and in seated TYPE, every glyph's ink rect has y >= 16 and lies within x 16..304 at zoom 1.
    - CR-02 composite (rebased by 05-24): agent-9's blocked glyph at (1,8) is strictly below agent-1's sprite box bottom and ends 1 px above agent-9's head; update its numeric expectations to the head-anchored values derived from the sprite data.
    - resolveBubbleY unit tests: rewritten for the new signature, including the headroom-less fallback (a head at y < glyph height still attaches to its own sprite, never y = 0 of a neighbour's row).
    - The horizontal-containment test is unchanged and passes.
  </behavior>
  <action>
RED, then GREEN (G-05-1c missing items):
- constants.ts: `BUBBLE_ICON_GAP_PX = 1`, doc: visible air between the glyph's lowest ink row and the owner's first opaque row (05-28), no longer the frame box.
- renderer.ts:
  - `firstOpaqueRow(sprite)` and `lastOpaqueRow(sprite)` memoised in a `WeakMap<SpriteData, number>` each (SpriteData objects are stable module-level values, see 05-24's cache note); an all-transparent sprite returns 0 / its last index.
  - `resolveBubbleY(drawY, headTopRow, glyphInkBottomRow, zoom)`: `preferred = Math.round(drawY + headTopRow * zoom - BUBBLE_ICON_GAP_PX * zoom - (glyphInkBottomRow + 1) * zoom)`; `preferred >= 0` -> return it; else return `Math.max(0, drawY)` (owner-bound fallback, doc kept from 05-10).
  - `drawGlyph` passes `firstOpaqueRow(l.spriteData)` and `lastOpaqueRow(bubbleSprite)`.
  - Update the resolveBubbleY doc comment: anchored to the visible head (05-28, G-05-1c); the old frame-top anchor left 5-6 px of air.
- Keep the horizontal placement, the pass order and every other test unchanged.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test</automated>
  </verify>
  <acceptance_criteria>
    - The new gap and wall tests pass for all 12 glyphs and all listed poses at zoom 1 and 3; the full suite is green.
    - `grep -n "BUBBLE_ICON_GAP_PX = 1" packages/pixel-office/src/constants.ts` matches.
  </acceptance_criteria>
  <done>Every glyph sits 1 px above its owner's hair in every pose, on the floor, owner-bound.</done>
</task>

<task type="auto">
  <name>Task 2: Live TRUTH 4 on the head + UI-SPEC placement note</name>
  <files>scripts/verify-pixel-office-live.mjs, .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>
  <read_first>
    - scripts/verify-pixel-office-live.mjs (TRUTH 4, `spriteTopY`, the sprite JSON read)
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md (Sprite & Icon Asset Inventory format contract; UI Considerations "overflow (state signal render)" row)
  </read_first>
  <precondition>Docker is running and apps/api/.env holds the four keys the harness checks.</precondition>
  <action>
- Harness TRUTH 4: compute the owner's head top as `spriteTopY(row) + firstOpaqueRow(down[1])` from character-metrocity.json (the blocked owner stands, IDLE); assert `blockedMaxY < headTop` and `headTop - (blockedMaxY + 1) <= 2` (attached: at most 2 px of air in grid px after scale normalisation), `blockedMinY >= 16` (on the floor, not the wall), and keep the "strictly below the front agent" assertion. Messages print the measured band, head top and gap.
- Run the harness with `PIXEL_OFFICE_SHOTS` in the scratchpad; Read `states.png` and `cohort.png`: each glyph sits directly on its agent's head, on the floor, and no longer forms a detached strip. Record in the SUMMARY.
- UI-SPEC: add to the Asset format contract (or the Spacing exceptions) that a glyph is placed centred on its owner with its lowest ink row BUBBLE_ICON_GAP_PX (1) px above the owner's first opaque sprite row, per frame; update the "overflow (state signal render)" UI Considerations row to cite 05-28. Cite G-05-1c.
  </action>
  <verify>
    <automated>node --check scripts/verify-pixel-office-live.mjs</automated>
    <automated>node scripts/verify-pixel-office-live.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `LIVE PROOF: PASS`; TRUTH 4 prints a head-to-glyph gap <= 2 and a glyph top >= 16.
    - `grep -n "05-28" .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` finds the placement note.
  </acceptance_criteria>
  <done>The live canvas proves glyphs are attached to their agents' heads and on the floor; the design contract states the rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | render-only placement change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-28-01 | Repudiation (misleading display) | a glyph read as belonging to a neighbour or to nobody | medium | mitigate | head-anchored placement, owner-bound fallback kept, pod-pair and wall tests, live TRUTH 4 |
</threat_model>

<verification>
- `pnpm --filter pixel-office test` green.
- `node scripts/verify-pixel-office-live.mjs` ends `LIVE PROOF: PASS` with the head-anchored TRUTH 4.
</verification>

<success_criteria>
G-05-1c holds: every status glyph is centred just above its own agent's head, on the floor, and reads as that agent's icon rather than a legend.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-28-SUMMARY.md` when done.
</output>
