---
phase: 05-pixel-office-renderer
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/pixel-office/src/status/status-mapping.ts
  - packages/pixel-office/src/status/status-mapping.test.ts
  - packages/pixel-office/src/sprites/bubble-blocked.json
  - packages/pixel-office/src/sprites/bubble-failed.json
  - packages/pixel-office/src/sprites/bubble-completed.json
  - packages/pixel-office/src/sprites/badge-planning.json
  - packages/pixel-office/src/sprites/badge-researching.json
  - packages/pixel-office/src/sprites/badge-testing.json
  - packages/pixel-office/src/sprites/badge-reviewing.json
  - packages/pixel-office/src/sprites/badge-discussing.json
  - packages/pixel-office/src/sprites/badge-deploying.json
  - packages/pixel-office/src/index.ts
  - references/ASSET-LICENSES.md
  - apps/web/src/App.tsx
autonomous: true
requirements: [OFFICE-01, OFFICE-02, OFFICE-03]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "status-mapping.ts's exhaustive STATUS_MAP has a defined entry for every one of the 15 AgentStatus values — no gaps, no fallthrough default"
    - "waiting_for_agent and waiting_for_ceo resolve to different bubble icons; blocked, waiting_for_agent, and waiting_for_ceo all resolve to frozen: true (idle-loop suppressed), per D-03"
    - "references/ASSET-LICENSES.md lists the MetroCity character pack as confirmed CC0, the fork's own engine/layout/colorize code as MIT (Pablo De Lucca), the furniture/floor/wall/carpet assets actually wired into the one rendered floor as provenance-undocumented (never upgraded to 'confirmed'), and every unused fork asset pack as explicitly deferred per D-05"
    - "apps/web renders a visible, always-on attribution line crediting pixel-agents-hq/pixel-agents (MIT) and the MetroCity CC0 pack — attribution is not only recorded in a planning document"
  artifacts:
    - path: "packages/pixel-office/src/status/status-mapping.ts"
      provides: "STATUS_MAP: Record<AgentStatus, {pose, bubble?, frozen?, frameSpeedMultiplier?}> — the exhaustive OFFICE-01/OFFICE-03 visual mapping"
    - path: "references/ASSET-LICENSES.md"
      provides: "D-05's scoped asset-licence audit"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "packages/pixel-office/src/status/status-mapping.ts"
      via: "onSnapshot/onEvent handlers now call the exhaustive STATUS_MAP lookup instead of 05-01's IDLE-only stub inside upsertCharacterFromAgent"
      pattern: "STATUS_MAP\\["
  prohibitions:
    - requirement_id: OFFICE-02
      category: transparency
      statement: "MUST NOT present provenance-undocumented assets (furniture/floor/wall/carpet/pet) as 'confirmed' licensed in references/ASSET-LICENSES.md — record them as provenance-undocumented, never silently upgraded"
      verification: test
    - requirement_id: OFFICE-02
      category: transparency
      statement: "MUST NOT preserve attribution only in a planning document while the deployed apps/web application itself shows no visible credit"
      verification: test
    - requirement_id: OFFICE-03
      category: safety
      statement: "MUST NOT rely solely on colour/hue to distinguish blocked/waiting from active — the distinction (icon shape + frozen pose) must remain legible in a grayscale/colourblind-simulated view, not just via a differently-coloured icon"
      verification: judgment

user_setup: []
---

<objective>
Complete OFFICE-01's visual fidelity (all 15 `AgentStatus` values, not just `idle`), OFFICE-03's blocked/waiting glanceable signal, and OFFICE-02/D-05's asset-licence audit + in-app attribution.

Purpose: 05-01 proved the pipe works for one status value; this plan makes the mapping actually exhaustive (D-01's literal 1:1 requirement) and closes out the licensing obligation the fork carries.
Output: `packages/pixel-office/src/status/status-mapping.ts`, 9 new bubble/badge icon assets, `references/ASSET-LICENSES.md`, a visible attribution line in `apps/web`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-CONTEXT.md
@.planning/phases/05-pixel-office-renderer/05-RESEARCH.md
@.planning/phases/05-pixel-office-renderer/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `packages/pixel-office/src/status/status-mapping.ts` — `STATUS_MAP` exhaustive lookup table, exported `resolveStatusVisual(status: AgentStatus)`.
- `packages/pixel-office/src/sprites/{bubble-blocked,bubble-failed,bubble-completed,badge-planning,badge-researching,badge-testing,badge-reviewing,badge-discussing,badge-deploying}.json` — 9 new icon assets.
- `references/ASSET-LICENSES.md` — D-05's asset-licence audit document.
- `apps/web/src/App.tsx` (extended) — visible attribution footer.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Exhaustive AgentStatus → visual mapping (OFFICE-01 fidelity, OFFICE-03 blocked/waiting signal)</name>
  <files>packages/pixel-office/src/status/status-mapping.ts, packages/pixel-office/src/status/status-mapping.test.ts, packages/pixel-office/src/sprites/bubble-blocked.json, packages/pixel-office/src/sprites/bubble-failed.json, packages/pixel-office/src/sprites/bubble-completed.json, packages/pixel-office/src/sprites/badge-planning.json, packages/pixel-office/src/sprites/badge-researching.json, packages/pixel-office/src/sprites/badge-testing.json, packages/pixel-office/src/sprites/badge-reviewing.json, packages/pixel-office/src/sprites/badge-discussing.json, packages/pixel-office/src/sprites/badge-deploying.json, packages/pixel-office/src/index.ts</files>
  <read_first>
    packages/event-schema/src/agent-status.ts, packages/pixel-office/src/types.ts (CharacterState/bubbleType shape), packages/pixel-office/src/sprites/spriteData.ts, packages/pixel-office/src/index.ts (05-01's IDLE-only upsertCharacterFromAgent stub),
    .planning/phases/05-pixel-office-renderer/05-RESEARCH.md (Architecture Patterns → Pattern 2's full 15-row per-state disposition table and its "Concrete per-state disposition" table — use this table as the literal source of truth for STATUS_MAP's contents),
    .planning/phases/05-pixel-office-renderer/05-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Per D-02's sourcing triage (tier 1: check the fork for already-existing unused animation states; tier 2: AI-assisted pixel-art generation; tier 3: procedural variation on existing animations; tier 4: commission/hand-draw — tried top-down, no skipping ahead): RESEARCH.md's own Pattern 2 already walked this triage and found tier 1 (reusing the fork's existing `bubble-permission`/`bubble-waiting` sprites) plus tier 3 (new small icon assets in that SAME established JSON format, plus one legitimate procedural variation — animation speed on `deploying`, never tint since hue/palette is already claimed by per-agent identity colour) fully sufficient for all 15 states — tier 2 (AI-generation, real spend) and tier 4 (commissioned art) are correctly never reached. Create packages/pixel-office/src/status/status-mapping.ts exporting a `STATUS_MAP: Record<AgentStatus, { pose: CharacterState | null; bubble?: string; frozen?: boolean; frameSpeedMultiplier?: number }>` with exactly these 15 entries, taken directly from RESEARCH.md Pattern 2's disposition table: `idle` → `{pose: CharacterState.IDLE}`; `coding` → `{pose: CharacterState.TYPE}`; `reading` → `{pose: CharacterState.TYPE}` (same base pose as coding — the fork has no per-tool-use signal to differentiate them further this phase, documented in 05-01's flagged assumptions); `waiting_for_ceo` → `{pose: CharacterState.IDLE, bubble: "permission", frozen: true}` (reuses the fork's existing `bubble-permission.json`); `waiting_for_agent` → `{pose: CharacterState.IDLE, bubble: "waiting", frozen: true}` (reuses `bubble-waiting.json`); `offline` → `{pose: null}` (sentinel meaning "not rendered — despawned from the floor", callers must skip rendering when `pose === null`); `blocked` → `{pose: CharacterState.IDLE, bubble: "blocked", frozen: true}`; `failed` → `{pose: CharacterState.IDLE, bubble: "failed"}`; `completed` → `{pose: CharacterState.IDLE, bubble: "completed"}`; `planning` → `{pose: CharacterState.IDLE, bubble: "planning"}`; `researching` → `{pose: CharacterState.TYPE, bubble: "researching"}`; `testing` → `{pose: CharacterState.TYPE, bubble: "testing"}`; `reviewing` → `{pose: CharacterState.TYPE, bubble: "reviewing"}`; `discussing` → `{pose: CharacterState.TYPE, bubble: "discussing"}`; `deploying` → `{pose: CharacterState.TYPE, bubble: "deploying", frameSpeedMultiplier: 1.5}` (the one legitimate procedural variation this project uses — animation speed, never tint, per RESEARCH.md's explicit rejection of tint-based state signaling since hue/palette is already claimed by per-agent identity colour). Export `resolveStatusVisual(status: AgentStatus)` as a thin `STATUS_MAP[status]` accessor (no fallback branch — a missing key is a compile-time impossibility given the `Record<AgentStatus, ...>` type, not a runtime default).

    Create the 9 new bubble/badge JSON assets under packages/pixel-office/src/sprites/ (`bubble-blocked.json`, `bubble-failed.json`, `bubble-completed.json`, `badge-planning.json`, `badge-researching.json`, `badge-testing.json`, `badge-reviewing.json`, `badge-discussing.json`, `badge-deploying.json`), each following the exact `{palette: {...}, pixels: [[...11 cols...] x13 rows]}` shape already used by the fork's `bubble-waiting.json`/`bubble-permission.json` (read those two files' actual JSON structure before authoring the new ones). Each new icon uses a distinct, simple, single-recognizable pictogram distinct in SHAPE (not only colour) from every other icon in the set — per OFFICE-03's kept prohibition, a grayscale render of the full icon set must still read as 9 visually distinct glyphs. Exact glyph choice is Claude's Discretion per CONTEXT.md (e.g. blocked=stop-octagon, failed=X, completed=checkmark-in-circle, planning=clipboard, researching=magnifying-glass, testing=wrench, reviewing=eye, discussing=speech-dots, deploying=upward-rocket) — pick recognizable shapes, do not reuse the same silhouette twice.

    Update packages/pixel-office/src/index.ts's `upsertCharacterFromAgent(agentId, status)`: replace 05-01's IDLE-only stub with a call to `resolveStatusVisual(status)`; if `pose === null` (the `offline` sentinel), remove/mark despawned the agent's existing `Character` (reuse the fork's own `matrixEffect: 'despawn'` one-shot transition from `types.ts` if present) rather than creating one; otherwise set `Character.state`, `Character.bubbleType`, suppress the idle-loop animation frame advance when `frozen === true`, and apply `frameSpeedMultiplier` to the character's frame-timer accumulation when present.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test -- status-mapping</automated>
    <fails_when>non-zero exit, or "0 passed" appears in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/pixel-office/src/status/status-mapping.test.ts` iterates `Object.values(AgentStatus)` (15 values) and asserts `STATUS_MAP[status]` is defined for every single one — exhaustiveness proven by test, not by type system alone.
    - A test asserts `STATUS_MAP.waiting_for_agent.bubble !== STATUS_MAP.waiting_for_ceo.bubble`.
    - A test asserts `STATUS_MAP.blocked.frozen === true`, `STATUS_MAP.waiting_for_agent.frozen === true`, `STATUS_MAP.waiting_for_ceo.frozen === true`.
    - A test asserts `STATUS_MAP.offline.pose === null`.
    - Every one of the 9 new sprite JSON files parses as valid JSON with a `palette` object and a 13-row `pixels` array.
  </acceptance_criteria>
  <done>status-mapping.test.ts passes with all 15 AgentStatus values resolved, distinct waiting_for_agent/waiting_for_ceo bubbles, and correct frozen flags.</done>
</task>

<task type="auto">
  <name>Task 2: Asset-licence audit + in-app attribution (OFFICE-02, D-05)</name>
  <files>references/ASSET-LICENSES.md, apps/web/src/App.tsx</files>
  <read_first>
    .planning/phases/05-pixel-office-renderer/05-RESEARCH.md (Common Pitfalls → Pitfall 4, the exact honest phrasing already worked out there for the provenance-undocumented assets), .planning/phases/05-pixel-office-renderer/05-CONTEXT.md (D-05's scoping), packages/pixel-office/LICENSE (05-01's copied MIT text + source SHA), apps/web/src/App.tsx (05-01's version, to extend)
  </read_first>
  <action>
    Create references/ASSET-LICENSES.md with four sections, scoped per D-05 to only what this phase's MVP actually wires in (not a full fork audit): (1) "Confirmed CC0" — the MetroCity character pack, credited in the fork's own README, used by `packages/pixel-office/src/sprites/spriteData.ts`; (2) "MIT (fork's own code)" — `packages/pixel-office/src/{engine,layout,colorize.ts,types.ts,constants.ts}`, forked from pixel-agents-hq/pixel-agents by Pablo De Lucca, licence text copied verbatim to `packages/pixel-office/LICENSE`; (3) "Provenance-undocumented, used this phase" — list exactly which furniture/floor/wall/carpet tile assets the one rendered floor's `layout/tileMap.ts` grid actually references (grep the forked tileMap/constants for asset path references to build this list precisely, do not guess), stating explicitly: "no separate credit found in pixel-agents-hq's README beyond the character pack; treated as MIT-covered by default per the repo's blanket licence with no contrary evidence found; flagged for a stronger check before any commercial distribution" — this section's own wording must never claim positive-verification/audit-closed status, only "provenance-undocumented"; (4) "Deferred — not used this phase" — every other fork asset pack not wired into this phase's one floor (any pet sprites, unused furniture/floor variants), to be re-audited when actually used or before commercial distribution, per D-05.

    Extend apps/web/src/App.tsx with a small always-visible fixed-position footer/credit element (not hidden behind a menu or modal) reading: "Pixel office renderer forked from pixel-agents-hq/pixel-agents (MIT) · character sprites: MetroCity pack (CC0) · full audit: references/ASSET-LICENSES.md" — this resolves OFFICE-02's kept prohibition that attribution must be visible in the running app, not only recorded in a planning document.
  </action>
  <verify>
    <automated>grep -ic "confirmed" references/ASSET-LICENSES.md | grep -qv "^0$" &amp;&amp; ! grep -A3 -i "Provenance-undocumented" references/ASSET-LICENSES.md | grep -qi "confirmed"</automated>
    <fails_when>non-zero exit — either no "confirmed" language exists at all (the CC0/MIT sections must use it) or the Provenance-undocumented section itself contains "confirmed"</fails_when>
    <automated>grep -c "pixel-agents-hq/pixel-agents" apps/web/src/App.tsx</automated>
    <fails_when>output is "0" — the attribution line is not present in the shipped component</fails_when>
  </verify>
  <acceptance_criteria>
    - references/ASSET-LICENSES.md has all four sections, with the provenance-undocumented section's own wording never claiming "confirmed" for furniture/floor/wall/carpet assets.
    - apps/web/src/App.tsx renders a visible credit line naming pixel-agents-hq/pixel-agents and the MetroCity CC0 pack, always on screen (not gated behind user interaction).
  </acceptance_criteria>
  <done>references/ASSET-LICENSES.md exists with all four honestly-worded sections; apps/web shows a visible attribution line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan adds pure presentational/documentation artifacts on top of 05-01's already-secured transport.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Repudiation | references/ASSET-LICENSES.md | low | accept | A documentation artifact carries no runtime attack surface; risk is reputational/legal (mis-stated licence provenance), mitigated by the honest-wording verify gate above rather than a security control |

</threat_model>

<verification>
Both `<automated>` commands in Task 1 and Task 2 exit 0.
</verification>

<success_criteria>
Every one of the 15 `AgentStatus` values resolves to a visually distinct pose/overlay combination; blocked and waiting agents freeze with a distinct icon each; `references/ASSET-LICENSES.md` and the running `apps/web` both honestly attribute the fork.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-02-SUMMARY.md` when done
</output>
