---
phase: 05-pixel-office-renderer
plan: 12
type: execute
gap_closure: true
wave: 7
depends_on: ["05-09", "05-10", "05-11"]
files_modified:
  - scripts/verify-pixel-office-live.mjs
  - references/ASSET-LICENSES.md
  - apps/web/src/App.tsx
  - apps/web/src/App.test.tsx
autonomous: true
requirements: [OFFICE-01, OFFICE-02, OFFICE-03, HANDOFF-01, HANDOFF-02]
user_setup: []

estimate:
  tokens: 66000
  raw_tokens: 33000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The artifact named the live end-to-end proof exercises the live path for every state it proves — no page reload, no character created through the connect-time snapshot"
    - "A blocked agent's glyph is proven on a real canvas to sit above its own desk row and nowhere inside the desk row behind it"
    - "No asset is presented as confirmed-licensed on evidence the same document's own rule calls insufficient"
    - "Every asset the shipped renderer actually loads appears in the licence audit"
    - "The in-app attribution footer claims exactly what the audit document supports, no more"
  artifacts:
    - scripts/verify-pixel-office-live.mjs
    - references/ASSET-LICENSES.md
    - apps/web/src/App.tsx
  key_links:
    - "live relay -> applyLiveEvent -> canvas pixels, with no reload in between"
    - "references/ASSET-LICENSES.md provenance tier -> apps/web attribution footer claim"
  prohibitions:
    - "MUST NOT present provenance-undocumented assets as 'confirmed' licensed in references/ASSET-LICENSES.md"
    - "MUST NOT credit/attribute an asset pack in-app or in ASSET-LICENSES.md that the shipped renderer does not actually load"
    - "MUST NOT assert live state rendering in a proof artifact that reloads the page or seeds the state through the connect-time snapshot"
    - "MUST NOT preserve attribution only in a planning document while the deployed apps/web application itself shows no visible credit"
---

<objective>
Make the two artifacts that make claims about this phase tell the truth about it.

`scripts/verify-pixel-office-live.mjs` is named the live end-to-end proof, but it proves Truth 1 and Truth 2 through the connect-time snapshot fold — it calls `page.reload()` before asserting the blocked bubble and documents that workaround in its own comment — and it passes Truth 3 only by pre-seeding both handoff participants through that same snapshot. 05-09 removes the reason for all three workarounds; this plan removes the workarounds.

`references/ASSET-LICENSES.md` §1 titles the MetroCity pack "Confirmed CC0" on the sole evidence of a credit line in the fork's README, while §4 of the same document states the rule that provenance is "never silently upgraded to confirmed". `apps/web`'s footer now asserts that claim to every end user. The 12 bubble/badge JSON assets the renderer genuinely loads appear nowhere in the document while §3 asserts "None". The internal contradiction and the missing rows are fixable here; the provenance itself needs a human, and is routed to one.

Purpose: leave no artifact making a claim stronger than its evidence.
Output: an honest live proof that additionally pins CR-02's fix on a real canvas, a licence audit consistent with its own rule, and a footer that matches it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pixel-office-renderer/05-VERIFICATION.md
@.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
@.planning/phases/05-pixel-office-renderer/05-09-PLAN.md
@.planning/phases/05-pixel-office-renderer/05-10-PLAN.md
</context>

<interface_context>
Depends on the three Wave 6 plans having landed:

- 05-09 makes `apps/web` apply `company-core`'s `reduce` to every relayed event and upsert changed agents, with `handleHandoffEvent` called after the upserts. A relayed `task.status_changed` therefore repaints its own agent with no navigation, and a relayed `agent.handoff_requested` creates the receiving agent's Character.
- 05-10 changes the seating layout to `DESK_ROW_START = 3`, `DESK_ROW_PITCH = 3` — desk rows 3, 6, 9 on the default 20x11 grid, 18 columns per row, 54 desks. At `zoom = 1` with the map exactly filling the 320x176 canvas, a desk-row-3 character's sprite box is `y` 24..56 and a desk-row-6 character's glyph band is `y` 57..70.
- 05-09's `apps/web/src/App.test.tsx` asserts the complete attribution sentence currently in `App.tsx`. Task 3 of this plan changes that sentence and MUST update that assertion in the same commit.

Existing helpers in `scripts/verify-pixel-office-live.mjs`: `openOffice(page, { reload })`, `scanCanvas(page)`, `postEvent(token, event)`, `statusChanged(agentId, taskId, status)`, `assert(condition, message)`, and the constants `SENDER`, `RECEIVER`, `BLOCKED`, `HANDOFF_TASK`, `BLOCKED_COLORS`, `HANDOFF_COLORS`.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: The live proof proves the live path — no reload, no snapshot-seeded characters</name>
  <files>scripts/verify-pixel-office-live.mjs</files>
  <read_first>
    - scripts/verify-pixel-office-live.mjs lines 270-300 (`openOffice`'s reload branch and the `assert` helper) and lines 340-420 (the whole Truth 1 / Truth 2 / Truth 3 sequence, including the three pre-seeding `statusChanged` posts at lines 353-355 and the reload workaround plus its self-documenting comment at lines 373-380)
    - apps/web/src/App.tsx (as rewritten by 05-09 — the `onEvent` ordering the proof now depends on)
    - apps/web/src/agent-event-mapper.ts (as rewritten by 05-09 — `applyLiveEvent`)
    - .planning/phases/05-pixel-office-renderer/05-VERIFICATION.md (`gaps[].missing` entries naming the reload removal and the pre-seeding removal)
  </read_first>
  <action>
Restructure the proof so every character on the canvas is created by an event that arrived over the live relay, after the page was already open.

Move the three baseline `statusChanged` posts so they happen AFTER `openOffice(page, { reload: false })`, not before it. The office must open empty, then fill from the relay. Add an explicit empty-canvas assertion between the open and the first post: the canvas at that moment carries zero non-floor, non-wall pixels. That assertion is what makes the subsequent sprite-pixel count attributable to the live relay rather than to the snapshot — without it, moving the posts proves nothing.

Give the relay a settle window after the posts, reusing the script's existing `sleep` helper with the same duration `openOffice` already allows for render frames, then take the Truth 1 scan. Truth 1's assertion text stays the same; only its provenance changes, so update its log line and the surrounding comment to state that the sprite pixels came from live events on an already-open page.

Remove the reload from Truth 2 entirely. Post the `blocked` status change, sleep for the render settle window, and scan — no `openOffice` call of any kind between the post and the scan. Delete the comment block that documents the reload workaround and replace it with one stating the property now being proven: a connected client re-derives `AgentStatus` from a relayed `task.status_changed` with no navigation, which is 05-VERIFICATION.md's headline gap and the first of its human-verification items.

Remove the receiving agent's pre-seed from Truth 3. After 05-09, the reducer's `agent.handoff_requested` handler upserts the receiver and `App.tsx` applies that upsert before calling the choreography, so the receiving character is created by the handoff event itself. Keep the sending agent's status post — an agent that is mid-task when it hands off is the real production shape, not a workaround — but it is now a live post like every other. Add an assertion before the handoff post that the receiver's desk area carries no sprite pixels yet, so Truth 3 genuinely demonstrates live receiver creation rather than assuming it.

If `openOffice`'s `reload` parameter has no remaining caller after these changes, simplify the helper to drop it rather than leaving an unused branch.

Do not weaken any existing assertion, do not delete the clean-baseline checks on `blockedHits` and `handoffHits`, and do not change the synthetic agent ids — the append-only events table means a later run must still be unable to pass on a previous run's leftover pixels.
  </action>
  <verify>
    <automated>test "$(grep -v '^[[:space:]]*//' scripts/verify-pixel-office-live.mjs | grep -c 'reload')" = "0"</automated>
    <fails_when>non-zero exit — a navigation still stands between a posted event and the assertion it feeds, so the proof still routes around the live path</fails_when>
    <automated>node --check scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit, or any syntax error is printed — the restructured script is not parseable</fails_when>
    <human-check>Run `node scripts/verify-pixel-office-live.mjs` against the local test Postgres and confirm it prints `LIVE PROOF: PASS`. The script starts two dev servers and issues `CREATE DATABASE` against whatever `DATABASE_URL` names with no locality guard, so it must be run deliberately by a human against a local test database, never by an agent as an incidental verification step.</human-check>
    <human-check>Open the office, leave the page open and untouched, and post a `task.status_changed` moving a real agent to `blocked` WITHOUT reloading. Expected: that agent's pose freezes and a blocked glyph appears above it, live, with no navigation. This is 05-VERIFICATION.md human-verification item 1 — the phase's headline claim and the one the automated proof previously routed around.</human-check>
  </verify>
  <acceptance_criteria>
    - Every `statusChanged` post in the main sequence occurs at a higher line number than the single `openOffice` call.
    - An empty-canvas assertion sits between the office opening and the first posted event.
    - Truth 2's blocked assertion is preceded by a post and a sleep, with no navigation call between them.
    - Truth 3 contains no `statusChanged` post for the receiving agent id.
    - `openOffice` has no unused `reload` branch.
    - `node --check scripts/verify-pixel-office-live.mjs` exits 0.
  </acceptance_criteria>
  <done>Every pixel the proof asserts on arrived through the live relay while the page stayed open, and the script no longer contains a comment explaining why it cannot do that.</done>
  <reversibility rating="reversible">The script is a standalone proof harness with no consumers; its structure can be changed freely.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove on a real canvas that the blocked glyph belongs to the blocked agent</name>
  <files>scripts/verify-pixel-office-live.mjs</files>
  <read_first>
    - scripts/verify-pixel-office-live.mjs (as restructured by Task 1, plus `scanCanvas`'s implementation and what it returns, and `distinctiveBubbleColors`'s colour derivation around line 131)
    - packages/pixel-office/src/index.ts (as changed by 05-10 — `DESK_ROW_START`, `DESK_ROW_PITCH` and `nextDeskPosition`'s column formula)
    - packages/pixel-office/src/engine/renderer.ts (as changed by 05-10 — `resolveBubbleY` and the anchoring arithmetic)
    - packages/pixel-office/src/constants.ts (`TILE_SIZE`, `DEFAULT_COLS`, `DEFAULT_ROWS`)
    - .planning/phases/05-pixel-office-renderer/05-VERIFICATION.md (the Truth 2 gap and human-verification item 2)
  </read_first>
  <action>
Add a fourth truth to the live proof: the blocked glyph is attached to the agent it describes, measured on the real composited canvas rather than in a unit-test recording context. 05-VERIFICATION.md reproduced the defect in a throwaway vitest; this makes the office's own proof carry the regression guard.

Seat enough agents to fill the first desk row and reach the second: post one `running` `task.status_changed` per agent for 19 distinct synthetic agent ids, all live, in order, so the first agent lands in column 1 of the first desk row and the nineteenth lands in column 1 of the second desk row. Derive 19 from this repo's own constants rather than hardcoding it — the interior column count is `DEFAULT_COLS - 2`, and one more than that is the first slot on the second desk row.

Then move ONLY the nineteenth agent to `blocked`, live, and scan.

Extend the canvas scan so it reports the y extent of the pixels matching the blocked glyph's distinctive colours, not merely their count. Normalize for backing-store scaling: compute `scale` as the reported canvas height divided by `DEFAULT_ROWS * TILE_SIZE`, and convert measured y values back to grid space by dividing by that scale before comparing.

Assert two things in grid space, both of which failed against the 05-08 code:

1. Every blocked-glyph pixel sits below the first desk row's sprite box, i.e. at a grid y strictly greater than the bottom of a desk-row-3 character's sprite. With `DESK_ROW_START = 3` and a 32-tall sprite anchored on a 16px grid that bottom is y 56.
2. Every blocked-glyph pixel sits above the top of its own character's sprite, i.e. at a grid y strictly less than the top of a desk-row-6 character's sprite, which is y 72.

Compute both bounds from the imported constants and the desk-row formula rather than writing 56 and 72 as literals, so a future layout change moves the assertion with it instead of silently invalidating it. Give each assertion a failure message naming the measured extent and the expected band, in the style the script's existing assertions use.

Use fresh synthetic agent ids distinct from the existing three constants, prefixed consistently with them, so an append-only events table cannot let this truth pass on an earlier run's leftovers.
  </action>
  <verify>
    <automated>node --check scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit, or any syntax error is printed</fails_when>
    <automated>test "$(grep -c 'DEFAULT_COLS' scripts/verify-pixel-office-live.mjs)" != "0"</automated>
    <fails_when>non-zero exit — the seat count was hardcoded rather than derived from the engine's own grid constants, so a layout change would silently invalidate the assertion</fails_when>
    <human-check>Run `node scripts/verify-pixel-office-live.mjs` and confirm the new fourth truth prints a PASS line reporting the measured glyph band. Requires a local test Postgres and two dev servers, so it is a deliberate human run.</human-check>
    <human-check>With 20+ agents seated so both desk rows fill, put ONLY a second-desk-row agent into `blocked` and look at the canvas. Expected: the glyph sits on the blocked agent, not on the agent seated in the row behind it. This is 05-VERIFICATION.md human-verification item 2 — mechanically reproduced during verification, but "which agent does a viewer think is blocked" is the actual user-facing bar and deserves one human look.</human-check>
    <human-check>At typical stream/viewing scale, confirm the blocked, waiting_for_agent and waiting_for_ceo glyphs are legible and distinguishable from each other and from an active agent — and that they remain distinguishable with colour removed (grayscale or a colourblind simulation). This closes 05-VERIFICATION.md's two judgment-tier OFFICE-03 prohibitions, which no automated test can settle.</human-check>
  </verify>
  <acceptance_criteria>
    - The script seats a number of agents derived from `DEFAULT_COLS`, not a literal.
    - The canvas scan returns a y extent for blocked-glyph pixels, not only a count.
    - Both band assertions compute their bounds from the desk-row constants and the sprite height.
    - The new agent ids do not collide with `SENDER`, `RECEIVER` or `BLOCKED`.
    - `node --check scripts/verify-pixel-office-live.mjs` exits 0.
  </acceptance_criteria>
  <done>The office's own live proof would have gone red against the 05-08 code, and the human-verification items that settle "whose agent is blocked" and "is it legible" are recorded against it.</done>
</task>

<task type="auto">
  <name>Task 3: The licence audit and the in-app footer claim only what the evidence supports</name>
  <files>references/ASSET-LICENSES.md, apps/web/src/App.tsx, apps/web/src/App.test.tsx</files>
  <read_first>
    - references/ASSET-LICENSES.md (the whole file — §1's "Confirmed CC0" heading and its README-credit evidence, §3's "None" claim, and §4's closing rule that provenance is "never silently upgraded to confirmed")
    - apps/web/src/App.tsx (the footer sentence, which asserts the CC0 claim to every end user)
    - apps/web/src/App.test.tsx (created by 05-09 — it asserts the complete footer sentence and will go red when that sentence changes)
    - packages/pixel-office/src/sprites/ (the 12 bubble/badge JSON assets the renderer actually loads, plus `character-metrocity.json`)
    - packages/pixel-office/src/sprites/bubbleSprites.ts (which of those files are genuinely imported)
    - .planning/phases/05-pixel-office-renderer/05-VERIFICATION.md (WR-09 in `advisory:`, and human-verification items 3 and 4)
    - .planning/phases/05-pixel-office-renderer/05-CONTEXT.md (D-05 — the audit's deliberate scoping to assets this phase actually wires in)
  </read_first>
  <action>
Resolve §1's self-contradiction in the direction the document's own rule requires. §4 states that provenance is never silently upgraded to confirmed; §1 upgraded the MetroCity pack to "Confirmed CC0" on a README credit line, which §4 classifies as insufficient. Retitle §1 to a "credited, licence not independently verified" tier and rewrite its body to state exactly what is known — the fork's README credits the pack, the file the renderer loads is the fork's own `char_0.png` at the pinned commit, decoded into `character-metrocity.json` — and exactly what is not known: no primary CC0 source has been located, and locating one requires visiting the upstream asset source, which cannot be done from inside this repo. Name the condition that would upgrade it: a citable primary source by URL, an itch.io or OpenGameArt page, or a LICENSE file inside the upstream pack.

Do not wait for that human answer before writing the downgrade. The contradiction is resolvable deterministically today, and the honest tier is the lower one until evidence arrives. Upgrading later is additive.

Fix §3's "None". The 12 bubble and badge JSON assets under `packages/pixel-office/src/sprites/` ARE loaded by the shipped renderer via `bubbleSprites.ts`, so the section claiming no provenance-undocumented assets are in use is wrong as written. Add a row per asset — or one grouped entry if every one of them shares the same provenance — stating that they were hand-authored in this repo during Phase 5 rather than taken from the fork, which makes them originals under this repo's own licence rather than provenance-undocumented third-party assets. Verify that claim before writing it: check whether each file is newly created in this repo's history rather than copied from the fork, and if any of them did come from the fork, record that instead. State whichever is true; do not assume authorship.

Soften `apps/web/src/App.tsx`'s footer to match the audit's new tier. Replace the bare CC0 claim with wording that credits the MetroCity pack and the fork without asserting a licence the audit no longer confirms, keeping the `references/ASSET-LICENSES.md` pointer so a reader can reach the full position. Keep the footer always-on, fixed-position and ungated — OFFICE-02's requirement is that attribution is visible, and softening a licence claim must not weaken the credit itself.

Update `apps/web/src/App.test.tsx`'s expected sentence to the new text in this same commit, so the suite stays green and so the assertion keeps pinning the complete sentence rather than a fragment.
  </action>
  <verify>
    <automated>npx vitest run --root apps/web</automated>
    <fails_when>non-zero exit, or `App.test.tsx` is absent from the reported file list, or the attribution case fails because the expected sentence was not updated alongside the footer</fails_when>
    <automated>test "$(grep -c 'never silently upgraded to confirmed' references/ASSET-LICENSES.md)" != "0"</automated>
    <fails_when>non-zero exit — the document's own provenance rule was removed instead of being applied, which resolves the contradiction in the wrong direction</fails_when>
    <automated>test "$(grep -ci 'bubble-blocked' references/ASSET-LICENSES.md)" != "0"</automated>
    <fails_when>non-zero exit — the glyph assets the renderer actually loads are still absent from the audit while §3 claims none are in use</fails_when>
    <human-check>Locate a primary CC0 source for the MetroCity character pack — an itch.io or OpenGameArt page, or a LICENSE file inside the upstream asset pack — and cite it by URL in `references/ASSET-LICENSES.md` §1. Expected outcome: either a citable primary source is found and §1 is upgraded back to confirmed with the footer restored to match, or the downgraded tier written by this task stands. This is 05-VERIFICATION.md human-verification item 3; it cannot be settled from inside this repo.</human-check>
    <human-check>Visually compare a rendered character in the running office against the fork's own MetroCity art (`webview-ui/public/assets/characters/char_0.png` at pinned commit `3537e140c2094761beae748592aeb92ece8edfdd`). Expected: the rendered figure recognisably matches the upstream sprite — not a mis-decoded, mirrored, or corrupted frame. This is 05-VERIFICATION.md human-verification item 4, requested explicitly by both 05-05 and 05-06's OFFICE-02 prohibitions and never performed.</human-check>
  </verify>
  <acceptance_criteria>
    - `references/ASSET-LICENSES.md` §1's heading no longer contains the word "Confirmed".
    - §1 names the specific evidence that would upgrade the tier.
    - §4's "never silently upgraded to confirmed" rule is still present and is no longer contradicted by §1.
    - Every JSON sprite asset imported by `bubbleSprites.ts` is accounted for somewhere in the document.
    - `apps/web/src/App.tsx`'s footer still renders the fork credit, the MetroCity credit and the `references/ASSET-LICENSES.md` pointer, still with `position: fixed`, and no longer asserts a licence the audit does not confirm.
    - `apps/web/src/App.test.tsx` asserts the new complete sentence and `npx vitest run --root apps/web` exits 0.
  </acceptance_criteria>
  <done>No document or screen in this repo makes a licence claim stronger than the evidence behind it, and the one claim that needs a human to settle is recorded as such rather than asserted.</done>
  <reversibility rating="reversible">A provenance tier is a documentation state; upgrading it once a primary source is cited is a text edit in two files.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo assets -> redistributed derivative | A decoded derivative of third-party art is committed and shipped to end users |
| proof artifact -> release decision | A passing proof is treated as evidence that a requirement is met |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12-01 | Repudiation | `references/ASSET-LICENSES.md` §1 and the in-app footer | high | mitigate | Task 3: the claim is downgraded to match its evidence, so neither the repo nor the running app asserts a licence position that cannot be defended. A CC0 claim asserted to end users on README-credit evidence is a legal representation, not a documentation nicety. |
| T-05-12-02 | Repudiation | `scripts/verify-pixel-office-live.mjs` | high | mitigate | Tasks 1 and 2: an artifact named a live end-to-end proof that demonstrates its truths through a page reload is a claim stronger than its evidence, and it is the artifact a release decision would rest on. |
| T-05-12-03 | Tampering | live proof's `CREATE DATABASE` with no locality guard (WR-05) | medium | accept | Unchanged by this plan and carried forward. The script is gated behind a deliberate human run in both tasks' `human-check` entries rather than being invoked by any automated verify step, which is why 05-VERIFICATION.md declined to execute it. A locality guard belongs with whoever next owns the harness. |
| T-05-12-04 | Information Disclosure | synthetic proof agent ids in an append-only table | low | accept | The proof writes rows that are never deleted. Deliberate: the clean-baseline assertions depend on being able to distinguish this run's pixels from an earlier run's. |
</threat_model>

<planner_assumptions>
The deterministic edge probe returned `unclassified` for all five Phase 5 requirement IDs and proposed zero applicable edges — per the spec-less fallback these stay **unresolved** and are surfaced here rather than dropped:

- **OFFICE-01 / OFFICE-02 / OFFICE-03 / HANDOFF-01 / HANDOFF-02 — edge classification unresolved.** The edges this plan encodes (an empty office at open, a desk row boundary at the eighteenth-to-nineteenth agent, a backing-store scale other than 1) were derived from the engine constants and the proof harness, not lifted from a probe.

Prohibition dispositions after this plan — every one of 05-VERIFICATION.md's six `unverified_prohibitions` is accounted for:

1. "attribution only in a planning document, no visible in-app credit" (OFFICE-02) — CLOSED. 05-09 Task 3 added the first wired test that renders `App`; Task 3 here keeps it pinned through the footer rewrite.
2. "provenance-undocumented assets presented as confirmed" (OFFICE-02) — CLOSED by Task 3, plus a human check that could upgrade the tier back.
3. "colour/hue as the sole blocked/waiting distinction" (OFFICE-03) — STILL FLAGGED, judgment-tier. Routed to the grayscale `human-check` in Task 2. 05-10 keeps hue strictly as an identity channel per 05-UI-SPEC.md's locked separation.
4. "state-signal overlay illegible at stream scale" (OFFICE-03) — STILL FLAGGED, judgment-tier. Routed to the same `human-check`. 05-10 closes the stronger ground it actually failed on (wrong agent), and Task 2 here pins that on a real canvas.
5. "`TaskState.title` interpolated without limit or filtering" (HANDOFF-02) — DEFERRED to Phase 7 (SAFE-01/SAFE-02), unchanged. Legitimately outside this phase's boundary.
6. "crediting an asset pack the renderer does not load" (OFFICE-02) — RESOLVED in substance by 05-06, with the never-performed visual fidelity check routed to a `human-check` in Task 3.

`workflow.human_verify_mode` is `end-of-phase`, so these are `<human-check>` entries attached to the tasks that produce them rather than blocking `checkpoint:human-verify` tasks. Each names its expected outcome so a reviewer can answer it without re-deriving the question.
</planner_assumptions>

<artifacts_this_phase_produces>
New symbols and identifiers created by this plan (excluded from drift verification):

- A fourth truth block and a glyph-band scan extension in `scripts/verify-pixel-office-live.mjs`
- New synthetic proof agent ids for the 19-agent seating scenario, `scripts/verify-pixel-office-live.mjs`
- A revised §1 provenance tier heading and new §3 asset rows, `references/ASSET-LICENSES.md`
- A revised attribution sentence, `apps/web/src/App.tsx` and its assertion in `apps/web/src/App.test.tsx`
</artifacts_this_phase_produces>

<verification>
- `node --check scripts/verify-pixel-office-live.mjs` exits 0.
- Non-comment source in `scripts/verify-pixel-office-live.mjs` contains no navigation between a posted event and the assertion it feeds.
- `npx vitest run --root apps/web` exits 0 with the attribution assertion matching the rewritten footer.
- `references/ASSET-LICENSES.md` carries no "Confirmed" tier unsupported by a cited primary source, and accounts for every sprite asset `bubbleSprites.ts` imports.
- The four human-verification items from 05-VERIFICATION.md each appear as a `<human-check>` with a stated expected outcome.
</verification>

<success_criteria>
- The live proof exercises the live path for every state it proves.
- The proof carries CR-02's regression guard measured on a real composited canvas.
- No artifact in this repo makes a licence claim stronger than its evidence.
- All four of 05-VERIFICATION.md's human-verification items are recorded against the tasks that produce them.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-12-SUMMARY.md` when done
</output>
