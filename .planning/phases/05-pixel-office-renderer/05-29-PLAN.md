---
phase: 05-pixel-office-renderer
plan: 29
type: execute
gap_closure: true
gap_ids: [G-05-1b]
wave: 19
depends_on: ["05-28"]
files_modified:
  - packages/pixel-office/src/handoff/dialogue-templates.ts
  - packages/pixel-office/src/handoff/dialogue-templates.test.ts
  - packages/pixel-office/src/handoff/handoff-choreography.test.ts
  - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
autonomous: true
requirements: [HANDOFF-02]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "G-05-1b / HANDOFF-02: the templates are short, deterministic and plainly functional — requested '<title> → <receiver>', accepted '<receiver> accepts <title>' — with the title capped at 12 and the name at 10 code points (cut with U+2026, never splitting a surrogate pair); no exclamation marks, no first person, only the task title and agent name interpolated, no network/LLM surface"
    - "G-05-1b: the longest possible line (31 code points) is at most 100 world px wide in 05-28's bubble (5 px font, 0.6 em advance, padding and border) — a label under a third of the room, versus 283 px before"
    - "05-UI-SPEC's Typography, Color, Copywriting and UI Considerations sections describe the shipped bubble (05-28) and the new templates (this plan), and no longer describe an 11 px canvas line stacked above the glyph slot"
    - "Live: the harness still passes with the short templates (the bubble shrinks; TRUTH 5's attribution assertions are unchanged)"
  artifacts:
    - packages/pixel-office/src/handoff/dialogue-templates.ts
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md
  key_links:
    - "resolveHandoffDialogue(kind, title, name) -> capForDialogue -> DIALOGUE_TEMPLATES -> Character.bubbleText -> 05-28 bubble"
  prohibitions:
    - statement: "MUST NOT generate or vary dialogue at render time; templates stay a static map with title/name interpolation only (HANDOFF-02, 05-UI-SPEC tone rule)"
      status: resolved
      verification: test
    - statement: "MUST NOT change bubble geometry, colours or the renderer (05-28 owns them)"
      status: resolved
      verification: test
---

<objective>
Finish UAT gap G-05-1b (High: the handoff text reads as a wide debug banner). 05-28 turned the banner into a compact tailed bubble with a 5 world-px font; this plan shortens what goes inside it and amends the design contract the gap says must change ("Amend UI-SPEC typography and template lines").

Root cause for the text half (.planning/debug/handoff-banner-size.md): the templates are full sentences (`Handing off "<title>" to <name>`, up to 46 code points) whose caps were sized to fill one 320 px line.

HANDOFF-02 and the UI-SPEC tone rule are kept exactly: a static template map, only the task title and agent name interpolated, no exclamation marks, no first person, no network/LLM surface. D-04 (dialogue during the sequence) is unchanged.

Output: short templates and caps with tests, rebased choreography text assertions, UI-SPEC amendments.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pixel-office-renderer/05-UAT.md
@.planning/debug/handoff-banner-size.md
@.planning/phases/05-pixel-office-renderer/05-28-SUMMARY.md
@packages/pixel-office/src/handoff/dialogue-templates.ts
</context>

<interface_context>
Orientation only — read the files.
- dialogue-templates.ts: `DIALOGUE_TEMPLATES` { requested: `Handing off "${taskTitle}" to ${toAgentName}`, accepted: `${toAgentName} accepts "${taskTitle}"` }, `MAX_DIALOGUE_TITLE_CHARS = 16`, `MAX_DIALOGUE_NAME_CHARS = 12`, `capForDialogue` (code-point cut, U+2026), `resolveHandoffDialogue(kind, title, name)`; ponytail about ~6.6 px per char on the 320 px canvas.
- dialogue-templates.test.ts: template, cap, surrogate and no-network tests (expects 16/12 caps and a 46-code-point longest line).
- handoff-choreography.test.ts: several assertions check bubbleText content with the old wording (e.g. `toContain("Handing off")`).
- constants.ts after 05-28: `DIALOGUE_FONT_PX = 5`, `DIALOGUE_BOX_PAD_X_PX = 2`, 1 px border, `DIALOGUE_BOX_HEIGHT_PX = 9`, `DIALOGUE_TAIL_PX = 2`, new light fill / dark ink colours.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Short deterministic templates + caps, with the width budget test</name>
  <reversibility rating="reversible">Two template strings and two caps in a static map.</reversibility>
  <files>packages/pixel-office/src/handoff/dialogue-templates.ts, packages/pixel-office/src/handoff/dialogue-templates.test.ts, packages/pixel-office/src/handoff/handoff-choreography.test.ts</files>
  <read_first>
    - packages/pixel-office/src/handoff/dialogue-templates.ts and its test (whole files)
    - packages/pixel-office/src/handoff/handoff-choreography.test.ts (every assertion on bubbleText content)
    - packages/pixel-office/src/constants.ts (dialogue block, as left by 05-28)
  </read_first>
  <behavior>
    dialogue-templates.test.ts:
    - requested with ("Fix login", "Ada") is exactly "Fix login → Ada"; accepted is exactly "Ada accepts Fix login".
    - caps: a 30-code-point title is cut to 12 code points ending in U+2026; a 20-code-point name to 10; surrogate pairs are never split (keep the existing surrogate test).
    - "the longest possible line fits the label budget": the accepted line built from max-cap inputs has 31 code points, and 31 * 0.6 * DIALOGUE_FONT_PX + 2 * (DIALOGUE_BOX_PAD_X_PX + 1) <= 100.
    - the existing no-network/no-LLM surface test and the tone checks (no "!", no standalone "I") stay.
  </behavior>
  <action>
RED, then GREEN (G-05-1b missing item "short label"; HANDOFF-02 kept):
- dialogue-templates.ts: `requested: (taskTitle, toAgentName) => \`${taskTitle} → ${toAgentName}\``, `accepted: (taskTitle, toAgentName) => \`${toAgentName} accepts ${taskTitle}\``; `MAX_DIALOGUE_TITLE_CHARS = 12`, `MAX_DIALOGUE_NAME_CHARS = 10`. Rewrite the ponytail: caps keep the longest line at 31 code points, about 93 world px at the 5 px font — a label under a third of the room; raise them only with a wider layout. Keep the Phase 7 stream-safety note and the header's tone rules.
- dialogue-templates.test.ts: the behavior above, replacing the old 16/12 and 46-code-point expectations.
- handoff-choreography.test.ts: replace content assertions tied to the old wording with the new templates (build the expected string with `resolveHandoffDialogue`, or assert on " → " / " accepts "); change nothing else.
  </action>
  <verify>
    <automated>pnpm --filter pixel-office test</automated>
    <fails_when>non-zero exit, or vitest's "Tests" summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - The template, cap and width-budget tests pass; the full pixel-office suite is green.
    - The no-network/no-LLM test in dialogue-templates.test.ts is still present and passing.
  </acceptance_criteria>
  <done>Handoff dialogue is a short deterministic label whose longest form fits the compact bubble.</done>
</task>

<task type="auto">
  <name>Task 2: UI-SPEC typography, colour and copy for the bubble; live re-check</name>
  <files>.planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md (## Typography, ## Color dialogue rows, ## Copywriting Contract handoff rows and tone rule, UI Considerations long-text row)
    - .planning/phases/05-pixel-office-renderer/05-28-SUMMARY.md (final colours and any geometry notes)
  </read_first>
  <precondition>Docker is running and apps/api/.env holds the four keys the harness checks.</precondition>
  <action>
Amend (do not append contradictions); cite 05-28/05-29 and G-05-4/G-05-1b:
- Typography Label row and the "Handoff dialogue" paragraph: 5 world-px monospace (5N device px, >= 15 px at the minimum scale 3); a speech bubble 9 world px tall with a 1 px ink border and a 2 px tail at the speaker; hung from the speaker's foot line; the requested bubble spans sender to receiver; clamped only to the floor interior; clearance proven by the layout-data guard in renderer.test.ts; pass order unchanged. State that the 11 px canvas scale and the above-the-glyph-slot placement are superseded (the DOM footer keeps 11 px).
- Color: the two dialogue rows take 05-28's values with roles "bubble fill" and "ink (border, tail, text)"; still achromatic, still guarded against every character, glyph and office palette.
- Copywriting: requested `${taskTitle} → ${toAgentName}`, accepted `${toAgentName} accepts ${taskTitle}`, length rule 12/10 code points with U+2026. Tone rule unchanged.
- UI Considerations long-text row: caps 12/10, longest line 31 code points within 100 world px.

Then run the live harness (the on-canvas text changed) with `PIXEL_OFFICE_SHOTS` in the scratchpad and Read `handoff.png`: the short label is readable inside the bubble. Record in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c "05-29" .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md</automated>
    <fails_when>prints 0 (the amendments are missing)</fails_when>
    <automated>node scripts/verify-pixel-office-live.mjs</automated>
    <fails_when>non-zero exit, any "TRUTH FAILED" line, or no final "LIVE PROOF: PASS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - UI-SPEC Typography, Color, Copywriting and UI Considerations cite 05-28/05-29; `grep -n "stacked directly above the speaker" .planning/phases/05-pixel-office-renderer/05-UI-SPEC.md` finds no remaining description of the old placement.
    - `LIVE PROOF: PASS`.
  </acceptance_criteria>
  <done>The design contract matches the shipped bubble and templates, and the live office shows the short label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projection text -> canvas | `TaskState.title` and `AgentState.name` are interpolated into the label |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-29-01 | Tampering / spoofing of dialogue | render-time text generation | high | mitigate | static template map, title/name interpolation only, no network/LLM surface — existing grep-style test kept (HANDOFF-02) |
| T-05-29-02 | Information disclosure | task title shown on stream | medium | accept | unchanged exposure class (05-13 T-05-13-01); shorter caps reduce it; stream-safe filtering is Phase 7 (SAFE-01/02) |
| T-05-29-03 | Denial of service | very long names/titles | low | mitigate | 12/10 code-point caps; width budget test |
</threat_model>

<verification>
- `pnpm --filter pixel-office test` green.
- `node scripts/verify-pixel-office-live.mjs` ends `LIVE PROOF: PASS`.
</verification>

<success_criteria>
G-05-1b holds: the handoff text is a short deterministic label in a compact bubble near the participants, and the UI-SPEC says so.
</success_criteria>

<output>
Create `.planning/phases/05-pixel-office-renderer/05-29-SUMMARY.md` when done.
</output>
