---
phase: 03-worker-git-adapter-gsd-adapter
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/gsd-adapter/package.json
  - packages/gsd-adapter/tsconfig.json
  - packages/gsd-adapter/src/index.ts
  - packages/gsd-adapter/src/state-md.ts
  - packages/gsd-adapter/src/phase-files.ts
  - packages/gsd-adapter/src/role-mapping.ts
  - packages/gsd-adapter/src/state-md.test.ts
  - packages/gsd-adapter/src/role-mapping.test.ts
  - pnpm-lock.yaml
autonomous: true
requirements: [GSD-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Given a real STATE.md with status: planning, readStateMd normalizes it to the exact 7-token vocabulary GSD's own gsd-core source defines (verified verbatim this session), never inventing a new status token."
    - "Given a STATE.md with no current_phase key at all (SyncSmith's real, verified-this-session shape) and no .planning/phases directory, mapToGsdCategory resolves category \"new_project\", never throwing and never guessing a later-pipeline category."
    - "Given a phase directory whose CONTEXT.md exists but no NN-##-PLAN.md exists yet, mapToGsdCategory resolves category \"planning\"; given a NN-##-PLAN.md with no matching NN-##-SUMMARY.md, it resolves \"execution\"."
    - "Given signals that don't cleanly map to any of GSD-01's 9 categories or PROJECT.md's 8 roles, mapToGsdCategory resolves role \"unknown\" — never a specific, plausible-looking role guess (preferring observed over guessed, per GSD-01's own requirement text)."
  artifacts:
    - packages/gsd-adapter/src/state-md.ts (readStateMd, STATUS_EXACT_TOKENS)
    - packages/gsd-adapter/src/phase-files.ts (scanPhaseDir)
    - packages/gsd-adapter/src/role-mapping.ts (mapToGsdCategory)
  key_links:
    - "packages/gsd-adapter/src/state-md.ts's STATUS_EXACT_TOKENS -> gsd-core's own state-document.cjs STATUS_EXACT_TOKENS, quoted verbatim in 03-RESEARCH.md Pattern 4 (must stay byte-identical in meaning, not reinvented)"
    - "packages/gsd-adapter/src/role-mapping.ts -> PROJECT.md's 8-role list (PM/Research Agent/Architect/Engineering/QA/Reviewer/CEO/DevOps) and packages/event-schema's GsdPhaseObservedPayload role enum (Plan 01) — the two must stay in sync"
  prohibitions:
    - statement: "MUST NOT default an ambiguous or unmapped GSD status/file-presence combination to a specific, plausible-looking role or category guess — must resolve to \"unknown\" rather than fabricate a specific pipeline stage the observed data does not actually support."
      category: values
      verification: test
      flagged: true
  flagged_assumptions:
    - "GSD-01 (unclassified, unresolved by the spec-less edge probe): \"approval\" has no STATE.md or file-presence signal at all for a repo with no CEO-gate concept of its own (RESEARCH.md Open Question 3) — mapToGsdCategory never resolves \"approval\" in this phase; it is treated as permanently unresolvable until PixelFirm's own Phase 6 CEO gate exists to observe. \"deployment\" is approximated by a ROADMAP.md phase-checkbox flip from [ ] to [x], which is a proxy, not a direct GSD-emitted signal."
---

<objective>
Build the GSD workflow-state observation layer: parse a pointed-at repository's `.planning/STATE.md` frontmatter and phase-directory file presence, normalize against GSD's own canonical (verified, 7-token) status vocabulary, and map the combination onto GSD-01's 9-category pipeline plus PROJECT.md's 8-role assignment table — always preferring observed signals over guessed ones, and always resolving to "unknown" rather than fabricating a plausible-looking answer when the signals don't clearly support one.

Purpose: GSD-01 is the requirement most exposed to silent fabrication risk — a GSD-adapter that "guesses" a role when data is ambiguous violates the product's Core Value (never show fabricated activity) just as much as a fake animation would.
Output: `packages/gsd-adapter`, a new workspace package, independent of `packages/git-adapter`, ready to be consumed by Plan 04's poll loop.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-CONTEXT.md
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-RESEARCH.md
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-PATTERNS.md
@.planning/PROJECT.md
@packages/company-core/package.json
@packages/company-core/tsconfig.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: state-md.ts (STATE.md frontmatter + status normalization) + phase-files.ts (phase-directory presence scanning)</name>
  <files>packages/gsd-adapter/package.json, packages/gsd-adapter/tsconfig.json, packages/gsd-adapter/src/index.ts, packages/gsd-adapter/src/state-md.ts, packages/gsd-adapter/src/phase-files.ts, packages/gsd-adapter/src/state-md.test.ts</files>
  <read_first>packages/company-core/package.json, packages/company-core/tsconfig.json, 03-RESEARCH.md (Pattern 4 — verbatim STATUS_EXACT_TOKENS quote; Pitfall 2 — vocabulary gap; Pitfall 3 — missing current_phase case)</read_first>
  <behavior>
    - readStateMd(planningDir) against a fixture STATE.md with frontmatter `status: planning` and `current_phase: 3` resolves { status: "planning", currentPhase: 3, raw: {...} }.
    - readStateMd against a fixture STATE.md with NO current_phase key (SyncSmith's real, verified-this-session shape: only gsd_state_version/status/progress) resolves { status: <normalized>, currentPhase: undefined, raw: {...} } — never throws on the missing key.
    - readStateMd against a planningDir with no STATE.md file at all resolves null (first-class "no project yet" signal, not a thrown error).
    - Status normalization: "ready to plan" and "planning complete" both normalize to "planning"; "phase complete — ready for verification" normalizes to "verifying"; an unrecognized raw status string normalizes to "unknown" — matching gsd-core's own STATUS_EXACT_TOKENS table exactly, not a reinvented mapping.
    - scanPhaseDir(planningDir, phaseId) against a fixture phase directory containing only an NN-CONTEXT.md resolves { hasContext: true, hasResearch: false, planCount: 0, summaryCount: 0, hasVerification: false, verificationStatus: undefined, hasReview: false }.
  </behavior>
  <action>
    Create packages/gsd-adapter/package.json mirroring packages/company-core/package.json's shape (name: "gsd-adapter", private: true, type: "module", version: "0.0.0", main: "src/index.ts", scripts.test: "vitest run") with dependencies.gray-matter: "^4.0.3" and dependencies["event-schema"]: "workspace:*" (needed for the GsdPhaseObservedPayload role/category/status enum types this package's output must match). Create packages/gsd-adapter/tsconfig.json identical in shape to packages/company-core/tsconfig.json.

    Create packages/gsd-adapter/src/state-md.ts. Define and export a frozen STATUS_EXACT_TOKENS object copied verbatim (key-for-key, value-for-value) from the quote in 03-RESEARCH.md Pattern 4 / `C:\Users\shado\.claude\gsd-core\bin\lib\state-document.cjs:637-664` — do not paraphrase or re-derive it, copy it exactly so it cannot silently drift from GSD's own canonical vocabulary. Export async readStateMd(planningDir: string): Promise&lt;{ status: string; currentPhase: number | undefined; raw: Record&lt;string, unknown&gt; } | null&gt; that: returns null if `${planningDir}/STATE.md` does not exist (fs.existsSync check first — Pitfall 3's "no project yet" case must be a first-class return, not a catch block); otherwise reads the file, parses it with gray-matter, looks up `data.status` (lowercased, trimmed) in STATUS_EXACT_TOKENS, defaulting to "unknown" if the raw string isn't a recognized key; reads `data.current_phase` if present (undefined otherwise, per Pitfall 3); returns `raw: data` untouched for callers that need the full frontmatter (e.g. `progress`).

    Create packages/gsd-adapter/src/phase-files.ts. Export async scanPhaseDir(planningDir: string, phaseId: string): Promise&lt;{ hasContext: boolean; hasResearch: boolean; planCount: number; summaryCount: number; hasVerification: boolean; verificationStatus: string | undefined; hasReview: boolean }&gt; that globs/reads `${planningDir}/phases/` for a directory matching the phaseId prefix (e.g. "03-*") and checks file presence per the naming convention verified in 03-RESEARCH.md Pattern 4: `${phaseId}-CONTEXT.md`, `${phaseId}-RESEARCH.md`, `${phaseId}-##-PLAN.md` (count via a glob/regex), `${phaseId}-##-SUMMARY.md` (count), `${phaseId}-VERIFICATION.md` (presence + its own frontmatter `status` field via gray-matter if present), `${phaseId}-REVIEW.md`. If the phases directory itself doesn't exist (SyncSmith's real, verified-this-session state), return all-false/zero values rather than throwing.

    Create packages/gsd-adapter/src/index.ts re-exporting state-md.ts and phase-files.ts (relative imports with explicit .js extensions per the workspace's NodeNext convention).

    Add packages/gsd-adapter/src/state-md.test.ts with fixture STATE.md content written to temp files under os.tmpdir() (or inline fixture directories checked into the test, whichever keeps the test self-contained): one fixture matching this repo's real PixelFirm STATE.md shape (status + current_phase + nested progress object — the exact case gray-matter's YAML engine must handle correctly, per RESEARCH's Don't-Hand-Roll rationale for choosing gray-matter over regex), one fixture matching SyncSmith's real, verified-this-session shape (status + progress, no current_phase key), and one case with no STATE.md file at all.
  </action>
  <verify>
    <automated>pnpm --filter gsd-adapter test -- state-md</automated>
    <fails_when>non-zero exit, or "failed" appears in the Vitest summary line, or readStateMd throws on the no-current_phase or no-STATE.md fixtures instead of returning the documented value</fails_when>
  </verify>
  <acceptance_criteria>
    - STATUS_EXACT_TOKENS in state-md.ts is byte-identical (same 18 keys, same 7 distinct values) to the quote in 03-RESEARCH.md Pattern 4.
    - readStateMd never throws for any of: missing STATE.md, missing current_phase key, an unrecognized raw status string.
    - scanPhaseDir never throws when `.planning/phases/` doesn't exist at all.
  </acceptance_criteria>
  <done>state-md.ts and phase-files.ts correctly parse real-shaped GSD project state (including both this repo's and SyncSmith's actually-verified STATE.md shapes) without throwing on any documented edge case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: role-mapping.ts (GSD-01 category + role dispatch table) + package composition</name>
  <files>packages/gsd-adapter/src/role-mapping.ts, packages/gsd-adapter/src/index.ts, packages/gsd-adapter/src/role-mapping.test.ts</files>
  <read_first>packages/gsd-adapter/src/state-md.ts, packages/gsd-adapter/src/phase-files.ts, packages/company-core/src/reducer.ts (dispatch-table-by-key pattern to mirror), 03-RESEARCH.md (Pattern 4's category table, Open Question 3)</read_first>
  <behavior>
    - status "unknown" (no STATE.md at all, i.e. readStateMd returned null) with no phases directory: category "new_project", role "unknown".
    - status "planning" with a phase dir that has NN-CONTEXT.md but zero NN-##-PLAN.md files: category "planning", role "Architect".
    - status "executing" with at least one NN-##-PLAN.md and at least one PLAN without a matching SUMMARY: category "execution", role "Engineering".
    - status "verifying" with an NN-VERIFICATION.md present and its own frontmatter status "passed": category "review", role "Reviewer".
    - a ROADMAP.md phase checkbox for the current phase flipped to [x] (passed in as a boolean signal by the caller, per Open Question 3's recommendation): category "deployment", role "DevOps" — this signal takes precedence over a plain "completed" status when present.
    - any combination not matching one of the defined rows (e.g. status "paused", or status "discussing" with no clear phase-file signal): category "unknown", role "unknown" — never a specific pipeline-stage guess.
  </behavior>
  <action>
    Create packages/gsd-adapter/src/role-mapping.ts. Following packages/company-core/src/reducer.ts's dispatch-table-by-key pattern (pure functions, no Date.now()/Math.random(), spread not mutate — though here the "state" is a plain return value, not a folded object), export a pure function mapToGsdCategory(input: { status: string; phaseFiles: Awaited&lt;ReturnType&lt;typeof scanPhaseDir&gt;&gt; | null; roadmapPhaseCompleted: boolean }): { category: string; role: string } implementing the category/role table from 03-RESEARCH.md Pattern 4 (new_project/research/requirements/planning/execution/verification/review/approval/deployment mapped against PROJECT.md's PM/Research Agent/Architect/Engineering/QA/Reviewer/CEO/DevOps roles), with an explicit final fallback branch returning { category: "unknown", role: "unknown" } for any input combination not matched by an earlier, more specific branch — this fallback is the load-bearing implementation of the kept prohibition against guessing a specific role. "approval" is never returned by this function in this phase (Open Question 3 — no signal source exists yet); its category/role enum members remain reserved in packages/event-schema's GsdPhaseObservedPayload for Phase 6 to eventually populate.

    Update packages/gsd-adapter/src/index.ts to also export role-mapping.ts, and add a composing function observeGsdState(planningDir: string, phaseId: string | undefined, roadmapPhaseCompleted: boolean): Promise&lt;{ phase: string | undefined; status: string; category: string; role: string }&gt; that calls readStateMd, then (if a phaseId is available) scanPhaseDir, then mapToGsdCategory, and returns the combined shape matching packages/event-schema's GsdPhaseObservedPayload minus the `active` field (which Plan 04's poll-loop computes by combining this with git-adapter's process-liveness signal — this package stays a pure, stateless observer and never itself claims liveness).

    Add packages/gsd-adapter/src/role-mapping.test.ts covering every row in the behavior spec above, including the explicit "no match -> unknown/unknown" fallback case with a deliberately nonsensical input combination.
  </action>
  <verify>
    <automated>pnpm --filter gsd-adapter test</automated>
    <fails_when>non-zero exit, or "failed" appears in the Vitest summary line, or any fallback-path assertion resolves a non-"unknown" category or role</fails_when>
  </verify>
  <acceptance_criteria>
    - mapToGsdCategory has a single, explicit final fallback branch returning { category: "unknown", role: "unknown" } — not multiple scattered defaults that could diverge.
    - "approval" never appears as mapToGsdCategory's return value anywhere in this phase's test suite (source-inspectable: no test asserts category === "approval" succeeding).
    - observeGsdState's return shape's `category`/`role`/`status` values are all members of the same enums packages/event-schema's GsdPhaseObservedPayload defines (Plan 01) — a mismatch here would fail CompanyEventSchema.safeParse downstream in Plan 04.
  </acceptance_criteria>
  <done>role-mapping.ts correctly maps every defined GSD-01 signal combination to a category/role pair, defaults every unmatched combination to unknown/unknown, and packages/gsd-adapter's observeGsdState composes the full observation pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointed-at repo's `.planning/` files -> gsd-adapter's parser | Local filesystem content the worker's operator controls (not an untrusted remote input, but still parsed data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | packages/gsd-adapter/src/role-mapping.ts | medium | mitigate | The kept prohibition's fallback-to-unknown design prevents a malformed or adversarially-crafted STATE.md from causing the adapter to assert a specific, false pipeline stage/role into the company event stream |
| T-03-08 | Denial of Service | packages/gsd-adapter/src/phase-files.ts | low | accept | A pathological `.planning/phases/` directory (e.g. thousands of stray files) could slow one poll tick, but this phase's poll interval (Plan 04) tolerates a slow tick without cascading failure — no explicit mitigation needed at MVP scale |
| T-03-SC | Tampering | npm install of `gray-matter` | high | mitigate | Package Legitimacy Audit already completed in 03-RESEARCH.md this session — `gray-matter@^4.0.3` verdict OK (8.6M weekly downloads, github.com/jonschlinkert/gray-matter) — no blocking human-verify checkpoint needed |

</threat_model>

<verification>
`pnpm --filter gsd-adapter test` (full package suite) must pass green. `pnpm test` (root Turborepo suite) at wave merge must still pass for packages/git-adapter (Plan 02, parallel, no shared files) and all pre-existing packages.
</verification>

<success_criteria>
- packages/gsd-adapter exists, is a linked pnpm workspace member, and exports readStateMd/scanPhaseDir/mapToGsdCategory/observeGsdState.
- GSD's own 7-token status vocabulary is reused verbatim, never reinvented.
- Every ambiguous/unmapped input resolves to "unknown" role and category, never a fabricated specific guess.
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

- `packages/gsd-adapter` — new workspace package (`package.json`, `tsconfig.json`)
- `STATUS_EXACT_TOKENS`, `readStateMd(planningDir): Promise<{status, currentPhase, raw} | null>` — `packages/gsd-adapter/src/state-md.ts`
- `scanPhaseDir(planningDir, phaseId): Promise<{hasContext, hasResearch, planCount, summaryCount, hasVerification, verificationStatus, hasReview}>` — `packages/gsd-adapter/src/phase-files.ts`
- `mapToGsdCategory(input): {category, role}` — `packages/gsd-adapter/src/role-mapping.ts`
- `observeGsdState(planningDir, phaseId, roadmapPhaseCompleted): Promise<{phase, status, category, role}>` — `packages/gsd-adapter/src/index.ts`
</artifacts_produced>

<output>
Create `.planning/phases/03-worker-git-adapter-gsd-adapter/03-03-SUMMARY.md` when done
</output>
