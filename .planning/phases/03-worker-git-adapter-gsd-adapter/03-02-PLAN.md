---
phase: 03-worker-git-adapter-gsd-adapter
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/git-adapter/package.json
  - packages/git-adapter/tsconfig.json
  - packages/git-adapter/src/index.ts
  - packages/git-adapter/src/commit.ts
  - packages/git-adapter/src/worktree.ts
  - packages/git-adapter/src/process-liveness.ts
  - packages/git-adapter/src/commit.test.ts
  - packages/git-adapter/src/worktree.test.ts
  - packages/git-adapter/src/no-mutating-git.test.ts
  - pnpm-lock.yaml
autonomous: true
requirements: [WORKTREE-01, WORKTREE-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Given a real git worktree, isGitWorktree resolves true; given a non-git directory, it resolves false without throwing."
    - "listWorktrees against a repo with N real worktrees (verified this session against both PixelFirm and SyncSmith: exactly 1 each) returns N WorktreeRecord entries, each with a real path, headSha, and branch parsed from `git worktree list --porcelain`."
    - "readHead and readBranch return the real current HEAD sha and branch name for a given repoPath, verified against a temp fixture repo with a known commit."
    - "No file under packages/git-adapter/src (excluding test files) invokes a mutating git subcommand — git-adapter is read-only by construction (WORKTREE-02)."
  artifacts:
    - packages/git-adapter/src/commit.ts (readHead, readBranch, isGitWorktree)
    - packages/git-adapter/src/worktree.ts (listWorktrees, WorktreeRecord)
    - packages/git-adapter/src/process-liveness.ts (isAnyClaudeProcessAlive)
  key_links:
    - "packages/git-adapter/src/worktree.ts's parser -> the verified real `git worktree list --porcelain` output format documented in 03-RESEARCH.md Pattern 3"
    - "packages/git-adapter/src/process-liveness.ts -> RESEARCH.md Pitfall 1's mandated coarse, repo-agnostic check (no cwd/cmdline matching)"
  prohibitions:
    - statement: "MUST NOT invoke any mutating git subcommand (merge, rebase, force-push, branch delete, or a checkout that changes HEAD) anywhere in packages/git-adapter's source — the adapter is read-only observation only, never automated branch mutation (WORKTREE-02, extended beyond the requirement's literal \"never merges\" wording to the full class of destructive/mutating operations an adversarial reading of WORKTREE-02 implies)."
      category: safety
      verification: test
      flagged: true
  flagged_assumptions:
    - "WORKTREE-01 (adjacency, unresolved by the spec-less edge probe): behavior when a git plumbing command is invoked while a git operation is mid-flight (e.g. an index.lock file present) is not explicitly handled in this phase — a transient execa failure on one poll tick is expected to surface as a rejected promise the caller (apps/worker, Plan 04) must tolerate without crashing, not retried within git-adapter itself."
    - "WORKTREE-01 (empty, unresolved): behavior when listWorktrees resolves to zero records (the pointed-at path is a valid worktree but the porcelain output is empty) is untested this phase — the function returns an empty array, never throws, but no caller-side 'zero worktrees observed' event is defined."
    - "WORKTREE-01 (ordering, unresolved): the order of records returned by listWorktrees across successive polls is not guaranteed stable by git itself — any future diffing logic must key by worktreePath, never by array index."
    - "WORKTREE-02 (unclassified, unresolved): the static source-scan test proves only that packages/git-adapter's OWN source has no mutating call site at commit time — it cannot prevent a different package or a future dependency bump from introducing one elsewhere."
---

<objective>
Build the read-only git observation layer: real `git` plumbing (via execa, no shell) that reads HEAD/branch/worktree state and a coarse OS-level Claude-process-liveness check, packaged as `packages/git-adapter`, with a structural guarantee (a static negative test) that it can never mutate a repository.

Purpose: WORKTREE-01 requires the system to record real repository/branch/worktree data; WORKTREE-02 requires it never automatically merges — the second is a permanent architectural constraint that must be enforced at the source-scan level from this package's very first commit, not bolted on later.
Output: `packages/git-adapter`, a new workspace package with no dependency on `apps/worker` or `packages/gsd-adapter`, ready to be consumed by Plan 04's poll loop.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-CONTEXT.md
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-RESEARCH.md
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-PATTERNS.md
@packages/company-core/tsconfig.json
@packages/company-core/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: git-adapter package scaffold + commit.ts (readHead/readBranch/isGitWorktree)</name>
  <files>packages/git-adapter/package.json, packages/git-adapter/tsconfig.json, packages/git-adapter/src/index.ts, packages/git-adapter/src/commit.ts, packages/git-adapter/src/commit.test.ts</files>
  <read_first>packages/company-core/package.json, packages/company-core/tsconfig.json, 03-RESEARCH.md (Pattern 1, Security Domain — execa no-shell array-args), 03-PATTERNS.md (git-adapter section)</read_first>
  <behavior>
    - readHead(repoPath) resolves the trimmed stdout of `git rev-parse HEAD` run with `cwd: repoPath` — a real 40-char sha against a temp fixture repo with one commit.
    - readBranch(repoPath) resolves the trimmed stdout of `git rev-parse --abbrev-ref HEAD` — "main" (or the fixture's initial branch) against the same fixture.
    - isGitWorktree(repoPath) resolves true for the fixture repo, and resolves false (never throws) for a plain temp directory with no `.git`.
  </behavior>
  <action>
    Create packages/git-adapter/package.json mirroring packages/company-core/package.json's shape exactly (name: "git-adapter", private: true, type: "module", version: "0.0.0", main: "src/index.ts", scripts.test: "vitest run") with dependencies.execa: "^10.0.1" and devDependencies.vitest inherited from the workspace root (no explicit devDependency needed — company-core's package.json has none either, vitest resolves from the root). Create packages/git-adapter/tsconfig.json identical in shape to packages/company-core/tsconfig.json (extends ../../tsconfig.json, outDir dist, include src).

    Create packages/git-adapter/src/commit.ts exporting three async functions, each using execa with array-form arguments (never a shell string) per RESEARCH.md's Security Domain command-injection mitigation: readHead(repoPath: string): Promise&lt;string&gt; runs execa("git", ["rev-parse", "HEAD"], { cwd: repoPath }) and returns stdout.trim(); readBranch(repoPath: string): Promise&lt;string&gt; runs execa("git", ["rev-parse", "--abbrev-ref", "HEAD"], { cwd: repoPath }) and returns stdout.trim(); isGitWorktree(repoPath: string): Promise&lt;boolean&gt; runs execa("git", ["-C", repoPath, "rev-parse", "--is-inside-work-tree"]) inside a try/catch, returning stdout.trim() === "true" on success and false on any thrown error (never propagates — D-04 requires validating the path before trusting it, and a validation function that throws defeats that purpose).

    Create packages/git-adapter/src/index.ts re-exporting everything from ./commit.js (relative imports need explicit .js extensions under the workspace's NodeNext moduleResolution, per Phase 2's own established fix).

    Add packages/git-adapter/src/commit.test.ts using a temp-git-repo fixture: in a beforeAll, create a directory under os.tmpdir(), run `execa("git", ["init"], { cwd: tmpDir })`, configure a throwaway user.email/user.name (git commit requires it and CI/dev machines may not have global config), write one file, `git add -A`, `git commit -m "test"`. Assert readHead resolves a 40-character hex string, readBranch resolves a non-empty string, isGitWorktree resolves true for tmpDir and false for a second, non-git temp directory.
  </action>
  <verify>
    <automated>pnpm --filter git-adapter test -- commit</automated>
    <fails_when>non-zero exit, or "failed" appears in the Vitest summary line, or readHead's resolved value is not a 40-character hex string</fails_when>
  </verify>
  <acceptance_criteria>
    - packages/git-adapter/package.json's "test" script is "vitest run", matching the workspace-wide convention.
    - Every execa call in commit.ts passes its git subcommand as an array literal, never a template-interpolated shell string (grep-verifiable: no backtick-interpolated string is passed as execa's first two positional args).
    - isGitWorktree never throws for any input, including a nonexistent path.
  </acceptance_criteria>
  <done>packages/git-adapter exists as a linked workspace package with readHead/readBranch/isGitWorktree, all proven against a real temp git fixture, tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: worktree.ts (WORKTREE-01) + process-liveness.ts (D-02 half) + WORKTREE-02 static negative test</name>
  <files>packages/git-adapter/src/worktree.ts, packages/git-adapter/src/process-liveness.ts, packages/git-adapter/src/worktree.test.ts, packages/git-adapter/src/no-mutating-git.test.ts</files>
  <read_first>packages/git-adapter/src/commit.ts, packages/git-adapter/src/commit.test.ts, 03-RESEARCH.md (Pattern 3 — verified porcelain format; Pitfall 1 — Windows process-liveness)</read_first>
  <behavior>
    - listWorktrees(repoPath) against the Task 1 fixture repo (exactly one worktree, no additional `git worktree add` calls made) returns exactly one WorktreeRecord with path === repoPath, a 40-char headSha, and a branch name with no "refs/heads/" prefix.
    - The porcelain parser splits on blank-line-separated blocks then splits each line on the FIRST space only (a value containing spaces, e.g. a branch name, must not be truncated).
    - isAnyClaudeProcessAlive() never throws regardless of platform or execa failure — resolves false on any error (fail-closed, never fabricates "active").
    - No .ts file under packages/git-adapter/src (excluding *.test.ts) contains a call site invoking a mutating git subcommand.
  </behavior>
  <action>
    Create packages/git-adapter/src/worktree.ts exporting a WorktreeRecord interface { path: string, headSha?: string, branch?: string, bare?: boolean, detached?: boolean } and an async listWorktrees(repoPath: string): Promise&lt;WorktreeRecord[]&gt; that runs execa("git", ["worktree", "list", "--porcelain"], { cwd: repoPath }), splits stdout on "\n\n", filters empty blocks, and for each block splits lines on the first space to build key/value pairs: "worktree" -> path, "HEAD" -> headSha, "branch" -> branch (stripping a leading "refs/heads/" prefix if present), "bare"/"detached" -> boolean true (label-only lines, no value). Export it from index.ts alongside commit.ts's exports.

    Create packages/git-adapter/src/process-liveness.ts exporting async isAnyClaudeProcessAlive(): Promise&lt;boolean&gt;. On process.platform === "win32", run execa("powershell", ["-NoProfile", "-Command", a single-quoted PowerShell command using Get-CimInstance Win32_Process filtered to Name -eq 'claude.exe' OR (Name -eq 'node.exe' AND CommandLine -like '*claude*'), piped to Measure-Object or .Count]), parse the numeric stdout, return count > 0. On other platforms, run execa("ps", ["-eo", "comm,args"]) and return true if any line case-insensitively matches "claude". Wrap the entire body in try/catch returning false on any error — this is a Pitfall 1 / D-02 safe-degrade requirement: a probe failure must never be interpreted as "active." Export it from index.ts.

    Add packages/git-adapter/src/worktree.test.ts reusing Task 1's temp-git-repo fixture pattern (extract the fixture-creation helper into a small shared local function within the test file, or duplicate it — no shared test-utils package exists yet, duplication is acceptable at this scale) and asserting listWorktrees returns exactly one record with the expected path/headSha/branch shape.

    Add packages/git-adapter/src/no-mutating-git.test.ts: read every .ts file under packages/git-adapter/src whose name does not end in .test.ts (use fs.readdirSync + a filter), and assert (via a regex constructed from an array of banned git subcommand tokens — merge, rebase, a force-push flag, a branch-delete flag, and checkout — defined as a local const array in the test file, not spelled out in this action) that none of those tokens appear as a quoted execa argument in any of those files. This is the WORKTREE-02 structural guarantee.
  </action>
  <verify>
    <automated>pnpm --filter git-adapter test -- worktree</automated>
    <fails_when>non-zero exit, or "failed" appears in the Vitest summary line, or listWorktrees resolves fewer or more than 1 record against the single-worktree fixture</fails_when>
  </verify>
  <verify>
    <automated>pnpm --filter git-adapter test -- no-mutating-git</automated>
    <fails_when>non-zero exit, or "failed" appears in the Vitest summary line (i.e. a banned git subcommand token was found in a non-test source file)</fails_when>
  </verify>
  <acceptance_criteria>
    - listWorktrees's parser correctly handles a branch value containing no spaces (real-world branch names verified this session never contain spaces, but the split-on-first-space implementation must not assume single-word values for forward compatibility).
    - isAnyClaudeProcessAlive resolves a boolean (never rejects) when invoked in this session's actual environment (Windows 11, PowerShell available, confirmed this session).
    - The no-mutating-git test fails loudly (non-zero exit) if a mutating git call is deliberately introduced — verify by temporarily confirming the test's own regex matches an obviously-mutating string during development, then removing it before commit (do not leave the mutating call in the committed source).
  </acceptance_criteria>
  <done>packages/git-adapter is feature-complete for this phase: listWorktrees and isAnyClaudeProcessAlive work against real fixtures, and a structural test proves the package can never issue a mutating git command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied --repo/WORKER_REPO_PATH -> git-adapter's execa calls | The repo path is local, user-controlled input (validated as a real worktree in Plan 04's env.ts, consumed here) |
| git-adapter -> local git/PowerShell subprocess | Every function shells out to a real OS subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | packages/git-adapter/src/{commit,worktree,process-liveness}.ts | high | mitigate | Every execa call passes subcommand args as an array literal, never a shell-interpolated string — execa's documented default (no shell) is the mitigation, verified by source inspection (no template-literal command strings) |
| T-03-06 | Tampering | packages/git-adapter (whole package) | high | mitigate | WORKTREE-02's static no-mutating-git.test.ts source-scan proves no mutating git subcommand call site exists anywhere in the package at commit time |
| T-03-SC | Tampering | npm install of `execa` | high | mitigate | Package Legitimacy Audit already completed in 03-RESEARCH.md this session — `execa@^10.0.1` verdict OK (152.7M weekly downloads, github.com/sindresorhus/execa, no [ASSUMED]/[SUS] disposition) — no blocking human-verify checkpoint needed |

</threat_model>

<verification>
`pnpm --filter git-adapter test` (full package suite) must pass green; `pnpm test` (root Turborepo suite) must still pass for all pre-existing packages (regression check — this plan touches no shared file, so no regression is expected, but the full-suite gate per 03-VALIDATION.md's Sampling Rate still applies at wave merge).
</verification>

<success_criteria>
- packages/git-adapter exists, is a linked pnpm workspace member, and exports readHead/readBranch/isGitWorktree/listWorktrees/isAnyClaudeProcessAlive.
- Every exported function is proven against a real (temp fixture) git repository, not mocked.
- A structural test guarantees the package can never issue a mutating git command.
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

- `packages/git-adapter` — new workspace package (`package.json`, `tsconfig.json`)
- `readHead(repoPath): Promise<string>`, `readBranch(repoPath): Promise<string>`, `isGitWorktree(repoPath): Promise<boolean>` — `packages/git-adapter/src/commit.ts`
- `WorktreeRecord` interface, `listWorktrees(repoPath): Promise<WorktreeRecord[]>` — `packages/git-adapter/src/worktree.ts`
- `isAnyClaudeProcessAlive(): Promise<boolean>` — `packages/git-adapter/src/process-liveness.ts`
- `packages/git-adapter/src/index.ts` re-exporting all of the above
</artifacts_produced>

<output>
Create `.planning/phases/03-worker-git-adapter-gsd-adapter/03-02-SUMMARY.md` when done
</output>
