---
phase: 03-worker-git-adapter-gsd-adapter
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - apps/worker/package.json
  - apps/worker/tsconfig.json
  - apps/worker/src/env.ts
  - apps/worker/src/ws-client.ts
  - apps/worker/src/index.ts
  - apps/worker/src/poll-loop.ts
  - apps/worker/src/event-emitter.ts
  - apps/worker/src/ws-client.test.ts
  - apps/worker/src/poll-loop.test.ts
  - apps/worker/src/index.integration.test.ts
  - pnpm-lock.yaml
autonomous: true
requirements: [RUNTIME-03, RUNTIME-04, WORKTREE-01, GSD-01]

user_setup:
  - service: pixelfirm-control-plane
    why: "The worker needs a real, revocable {workerId}.{secret} credential to authenticate its outbound WS connection and POST /events calls — Phase 2 already built the issuance endpoint, this plan only consumes it"
    env_vars:
      - name: WORKER_TOKEN
        source: "POST /admin/workers with header X-Bootstrap-Secret against the running control plane (Phase 2, e.g. https://test.pixelfirm.dev) -> response.token"

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "apps/worker refuses to start (fails fast with a clear error, never silently proceeds against an invalid path) when WORKER_REPO_PATH/--repo does not resolve to a real git worktree (D-04)."
    - "apps/worker connects outbound-only to CONTROL_PLANE_URL using the exact {workerId}.{secret} Bearer scheme Phase 2 already built and tested — no new auth mechanism (RUNTIME-03)."
    - "One poll tick against a fixture repo with one worktree and a fixture .planning/ directory emits exactly one git.worktree_observed event and one gsd.phase_observed event, both validating against CompanyEventSchema."
    - "A second, identical poll tick (no filesystem change) emits zero new events — the worker only sends deltas, never re-sends unchanged state."
    - { statement: "The worker's WS client reconnects after a forced socket close, using an increasing (never-decreasing) delay across consecutive failed attempts, capped at 30 seconds.", verification: backstop }
  artifacts:
    - apps/worker/src/env.ts (WORKER_REPO_PATH/--repo validation, WORKER_TOKEN, CONTROL_PLANE_URL, WORKER_COMPANY_ID)
    - apps/worker/src/ws-client.ts (connect, heartbeat sender, reconnect/backoff)
    - apps/worker/src/poll-loop.ts (diff-and-emit loop combining git-adapter + gsd-adapter)
    - apps/worker/src/event-emitter.ts (envelope construction, POST /events dispatch)
  key_links:
    - "apps/worker/src/poll-loop.ts -> packages/git-adapter's listWorktrees/isAnyClaudeProcessAlive and packages/gsd-adapter's observeGsdState (Plans 02-03) — this is the phase's actual integration point"
    - "apps/worker/src/event-emitter.ts's constructed envelopes -> packages/event-schema's CompanyEventSchema (Plan 01) — a shape mismatch here is caught by the real control plane's validation, not silently accepted"
    - "apps/worker/src/ws-client.ts's heartbeat cadence -> apps/api/src/ws/connection-status.ts's HEARTBEAT_INTERVAL_MS/STALE_AFTER_MS/OFFLINE_AFTER_MS constants (Plan 01) — the two must stay numerically consistent (10s heartbeat, matching the server's 10s/30s thresholds)"
  prohibitions:
    - statement: "MUST NOT include raw file contents, diffs, prompts, secrets, or environment-variable values in any emitted git.worktree_observed or gsd.phase_observed payload — only structural metadata (repo path, branch, worktree path, sha, GSD status/category/role) that this phase's payload schemas explicitly define."
      category: privacy
      verification: test
      flagged: true
    - statement: "MUST NOT fabricate a specific 'owning agent' identity for an observed worktree when no real AgentRuntime session exists yet (Phase 4 doesn't exist) — sessionId is always the real, non-fabricated worktree path itself, never an invented agent name or placeholder ID."
      category: values
      verification: test
      flagged: true
  flagged_assumptions:
    - "RUNTIME-03 (unclassified, unresolved by the spec-less edge probe): repo-path edge cases beyond 'exists and is a git worktree' — a bare repo, a submodule, or a worktree with zero commits — are not explicitly handled or tested this phase; isGitWorktree's boolean check is the only gate, and an edge-case repo that passes it but fails a later git-adapter call (e.g. rev-parse HEAD on a zero-commit repo) surfaces as a tolerated per-tick error (Plan 02's flagged adjacency assumption), not a startup-time rejection."
---

<objective>
Assemble the real worker process: a Node entrypoint that validates its pointed-at repository, connects outbound-only to the control plane using Phase 2's existing credential scheme, sends heartbeats, and runs a poll-diff loop combining `packages/git-adapter` and `packages/gsd-adapter` into real, delta-only `CompanyEvent`s. This is where RUNTIME-03, WORKTREE-01, and GSD-01 become genuinely demonstrable together, and where D-02's file+process dual-signal shape for "active" gets locked in.

Purpose: this is the phase's actual payoff — everything built in Plans 01-03 (schema, connection-status derivation, both adapters) only matters once a real worker process wires them together against a real repository.
Output: `apps/worker`, a runnable Node process (`pnpm --filter worker start`) that can be pointed at any git repository, demonstrated against SyncSmith per PROJECT.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-CONTEXT.md
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-RESEARCH.md
@.planning/phases/03-worker-git-adapter-gsd-adapter/03-PATTERNS.md
@apps/api/src/env.ts
@apps/api/src/server.ts
@apps/api/src/auth/credentials.ts
@apps/api/src/routes/ws-auth.test.ts
@apps/api/src/ws/connection-status.ts
</context>

<assumption_delta_decision>
Detector signal: ROADMAP.md's Phase 3 goal line contains a "chosen" term ("a real, user-chosen git repository"), which the assumption-delta scanner flags as a possible singular-to-plural generalization candidate (one repo -> could there someday be many, watched by one process).

Decision: **no-change**. D-04 in 03-CONTEXT.md already explicitly locks this phase's scope to exactly one repository per worker OS process — "user-chosen" describes WHICH single repo an operator points a given worker at, not a request for one process to watch several repos concurrently. D-04's own text already names the reversible generalization path (a config-file-driven multi-repo supervisor spawning one single-repo worker per configured repo) as future, out-of-scope work. Env.ts (Task 1) implements exactly the single-path, single-validation shape D-04 specifies; no multi-repo branching, config array, or watcher-of-watchers is introduced here.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: apps/worker scaffold — env validation (D-04), WS client with heartbeat + reconnect/backoff (D-03)</name>
  <files>apps/worker/package.json, apps/worker/tsconfig.json, apps/worker/src/env.ts, apps/worker/src/ws-client.ts, apps/worker/src/index.ts, apps/worker/src/ws-client.test.ts</files>
  <read_first>apps/api/src/env.ts, apps/api/src/server.ts, apps/api/src/auth/credentials.ts, apps/api/src/routes/ws-auth.test.ts, apps/api/src/ws/connection-status.ts, packages/git-adapter/src/commit.ts</read_first>
  <behavior>
    - env.ts fails fast (throws at import time, matching apps/api/src/env.ts's pattern) when WORKER_TOKEN, CONTROL_PLANE_URL, or WORKER_COMPANY_ID is missing.
    - env.ts's repo resolution: --repo CLI arg takes precedence over WORKER_REPO_PATH when both are supplied (Claude's Discretion, D-04) — resolved value is validated via git-adapter's isGitWorktree before env.ts finishes loading; an invalid path throws synchronously with a message naming the exact path that failed.
    - computeBackoffDelay(attempt, rand) returns a value >= 1000 * 2^attempt (capped at 30000) and < that same base value * 1.5, given a rand() that returns a fixed value in [0,1) — proving the 1s-initial/doubling/30s-cap/20-50%-jitter shape without depending on real timers or true randomness.
    - The WS client's heartbeat sender POSTs a worker.heartbeat event every HEARTBEAT_INTERVAL_MS (imported as a shared constant, matching apps/api/src/ws/connection-status.ts's 10_000).
  </behavior>
  <action>
    Create apps/worker/package.json (name: "worker", private: true, type: "module", version: "0.0.0", scripts: test="vitest run", typecheck="tsc --noEmit", start="tsx src/index.ts", dev="tsx watch src/index.ts") with dependencies: event-schema ("workspace:*"), git-adapter ("workspace:*"), gsd-adapter ("workspace:*"), ws ("^8.21.3"), zod ("^4.6.5"), and devDependencies: "@types/ws" ("^8.18.1"), tsx ("^4"), typescript ("^5.9.3"). Create apps/worker/tsconfig.json mirroring apps/api/tsconfig.json's shape exactly.

    Create apps/worker/src/env.ts following apps/api/src/env.ts's fail-fast Zod-parse-at-import-time pattern. Read CONTROL_PLANE_URL (z.string().url()), WORKER_TOKEN (z.string().min(1)), WORKER_COMPANY_ID (z.string().min(1)) from process.env via a Zod schema. Separately (outside the Zod schema, since it needs an async validation step Zod can't express inline): resolve the repo path from `process.argv` (looking for a `--repo=` prefixed arg) falling back to `process.env.WORKER_REPO_PATH`, throw synchronously if neither is supplied, then call git-adapter's isGitWorktree(resolvedPath) and throw a descriptive error (naming the path) if it resolves false. Export a `loadEnv(): Promise<{ controlPlaneUrl: string; token: string; companyId: string; repoPath: string }>` async function (not a top-level eager parse like apps/api's, since the repo validation is async) so index.ts can await it and fail fast before anything else starts.

    Create apps/worker/src/ws-client.ts. Export a pure `computeBackoffDelay(attempt: number, rand: () => number = Math.random): number` implementing D-03's algorithm: base = Math.min(1000 * 2 ** attempt, 30000); jitterFactor = 0.2 + rand() * 0.3; return Math.round(base * (1 + jitterFactor)). Export `connectWorker(controlPlaneUrl: string, token: string): WebSocket` that derives the WS URL by swapping the http(s) scheme to ws(s) and appending "/ws", then constructs `new WebSocket(wsUrl, { headers: { Authorization: \`Bearer ${token}\` } })` — mirroring the exact client pattern verified this session in apps/api/src/routes/ws-auth.test.ts:37-39. Export `startHeartbeat(controlPlaneUrl: string, token: string): { stop(): void }` that runs `postEvent`-equivalent logic (a fetch POST to `${controlPlaneUrl}/events` with a worker.heartbeat envelope, built via event-emitter.ts once Task 2 exists — for this task, inline a minimal local envelope builder using node:crypto's randomUUID) on a setInterval(HEARTBEAT_INTERVAL_MS) (import HEARTBEAT_INTERVAL_MS as a local constant = 10_000, documented as matching apps/api/src/ws/connection-status.ts's value), returning a stop() that clears the interval. Export `startReconnectingConnection(controlPlaneUrl, token, onOpen, onClose)` that calls connectWorker, wires `on("open")`/`on("close")`/`on("error")`, and on close/error schedules a reconnect via `setTimeout(reconnect, computeBackoffDelay(attempt++))`, resetting `attempt` to 0 on a successful open.

    Create apps/worker/src/index.ts exporting an async `startWorker(): Promise<{ stop(): void }>` that awaits env.ts's loadEnv(), then calls startReconnectingConnection and startHeartbeat, returning a combined stop() that tears both down — mirroring apps/api/src/server.ts's "importable/inject-able, direct-run guard via import.meta.url" pattern (using pathToFileURL for the same Windows-safety reason documented in that file) so index.ts both runs standalone (`pnpm --filter worker start`) and is importable from tests.

    Add apps/worker/src/ws-client.test.ts covering computeBackoffDelay's bounds (attempt 0 through 5, injecting a fixed rand() to make bounds exact) and asserting connectWorker constructs a WebSocket pointed at the derived wss(or ws)://.../ws URL with the correct Authorization header (inspect the constructed WebSocket instance's URL/options without opening a real connection, or use a local stub `ws` server in a beforeAll/afterAll pair).
  </action>
  <verify>
    <automated>pnpm --filter worker test -- ws-client</automated>
    <fails_when>non-zero exit, or "failed" appears in the Vitest summary line, or computeBackoffDelay(5, () =&gt; 0) exceeds 30000 * 1.5</fails_when>
  </verify>
  <acceptance_criteria>
    - env.ts's loadEnv() rejects (throws/rejects, never resolves) when the resolved repo path is not a real git worktree, naming the path in the error message.
    - --repo takes precedence over WORKER_REPO_PATH when both are set, verified by a direct unit test of the precedence logic.
    - WORKER_TOKEN is read only from process.env, never accepted as a CLI arg (source-inspectable: no argv parsing branch reads a token-shaped value) — Information Disclosure mitigation per RESEARCH.md Security Domain (process listing exposes CLI args, not env vars).
    - computeBackoffDelay is monotonically non-decreasing in its base term across increasing attempt values, capped at the 30s base.
  </acceptance_criteria>
  <done>apps/worker exists as a linked workspace package; env.ts validates D-04's repo-targeting config end-to-end (including the async git-worktree check), ws-client.ts implements D-03's connect/heartbeat/reconnect-backoff algorithm exactly, all proven by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: poll-loop.ts + event-emitter.ts — diff-and-emit loop combining git-adapter + gsd-adapter (D-01, D-02)</name>
  <files>apps/worker/src/poll-loop.ts, apps/worker/src/event-emitter.ts, apps/worker/src/index.ts, apps/worker/src/poll-loop.test.ts</files>
  <read_first>apps/worker/src/env.ts, apps/worker/src/ws-client.ts, apps/worker/src/index.ts, packages/git-adapter/src/index.ts, packages/gsd-adapter/src/index.ts, packages/event-schema/src/payloads/index.ts</read_first>
  <reversibility rating="costly">D-02 in 03-CONTEXT.md is already rated costly: once Phase 5's agent-state rendering depends on this file-presence-plus-process-liveness dual-signal shape for the "active" boolean, swapping to a different liveness source (e.g. a real Claude Agent SDK session hook once Phase 4 exists) means changing the GSD adapter's event-emission contract, not just an internal implementation detail. Carried through from CONTEXT.md, not re-derived.</reversibility>
  <behavior>
    - One poll tick against a fixture repo (one worktree, one commit) and a fixture .planning/ directory (matching PixelFirm's own real STATE.md shape) emits exactly one git.worktree_observed event and one gsd.phase_observed event.
    - A second, identical poll tick (nothing on disk changed) emits zero events — diffing is by value (headSha/branch/worktreePath for git; phase/status/category/role/active for gsd), never by "did a tick fire."
    - A third tick after the fixture repo's HEAD advances (a new commit made between ticks) emits exactly one new git.worktree_observed event reflecting the new headSha, keyed by worktreePath (never by array index — Plan 02's flagged ordering assumption).
    - The emitted gsd.phase_observed payload's `active` field is true only when BOTH isAnyClaudeProcessAlive() resolves true AND at least one tracked signal (HEAD sha or STATE.md mtime) changed since the previous tick — never true from process-liveness alone (Pitfall 1's mitigation, D-02).
  </behavior>
  <action>
    Create apps/worker/src/event-emitter.ts. Export `buildEnvelope(companyId: string, type: string, payload: unknown, visibility: "PRIVATE"|"INTERNAL"|"STREAM_SAFE"|"PUBLIC" = "INTERNAL")` returning `{ id: randomUUID(), version: 1, occurredAt: new Date().toISOString(), companyId, visibility, type, payload }` (INTERNAL is the default visibility for this phase's structural git/GSD metadata — never PRIVATE-only-implied secrets, never PUBLIC without Phase 7's filtering existing). Export `async function postEvent(controlPlaneUrl: string, token: string, event: unknown): Promise<void>` that POSTs to `${controlPlaneUrl}/events` with the Bearer header, and logs (never throws) on a non-2xx response — a single failed emit must not crash the poll loop. Refactor ws-client.ts's Task-1 inline heartbeat envelope builder to call this shared buildEnvelope/postEvent instead of duplicating the logic.

    Create apps/worker/src/poll-loop.ts. Maintain closure-scoped last-seen state: `{ worktrees: Map<string, { headSha?: string; branch?: string }>; gsd: { phase?: string; status: string; category: string; role: string; active: boolean } | null; stateMdMtimeMs: number | null }`. Export `startPollLoop({ repoPath, planningDir, phaseId, companyId, controlPlaneUrl, token, intervalMs = 2500 }): { stop(): void }` that runs a tick function on `setInterval`: (1) call git-adapter's listWorktrees(repoPath) inside a try/catch (a transient failure — Plan 02's flagged adjacency assumption — is logged and skipped, never crashes the loop); for each returned record, compare against the last-seen Map keyed by record.path (never by array index), and if new or changed, build a GitWorktreeObservedPayload (repoPath, branch: record.branch ?? "unknown", worktreePath: record.path, headSha: record.headSha ?? "unknown", sessionId: record.path — the real worktree path, never a fabricated agent name) and call event-emitter's postEvent with type "git.worktree_observed"; (2) determine recentFileActivity by stat'ing `${planningDir}/STATE.md`'s mtimeMs and comparing to the last tick's stored value (also true if any worktree changed this same tick); call git-adapter's isAnyClaudeProcessAlive() and gsd-adapter's observeGsdState(planningDir, phaseId, roadmapPhaseCompleted — pass false for this phase, since no ROADMAP-checkbox-reading integration is wired here; Plan 03's flagged GSD-01 assumption already documents "deployment"/"approval" as proxy/unreachable), compute `active = processAlive && recentFileActivity`, and if the combined { phase, status, category, role, active } differs from the last-seen value, call postEvent with type "gsd.phase_observed". Return `{ stop() { clearInterval(handle); } }`.

    Update apps/worker/src/index.ts's startWorker() to also call startPollLoop with the values from loadEnv() (repoPath, controlPlaneUrl, token, companyId) plus a `planningDir` derived as `path.join(repoPath, ".planning")` and a `phaseId` left undefined for now (gsd-adapter's own logic already handles an undefined/missing phase as the "new project" case) — include pollLoop.stop() in the combined stop() teardown.

    Add apps/worker/src/poll-loop.test.ts using a temp-git-repo fixture (reuse the pattern from packages/git-adapter's tests) plus a temp `.planning/STATE.md` fixture, and a stubbed/mocked postEvent (via vitest's module mocking of event-emitter.ts) to assert the tick-by-tick emission behavior from the &lt;behavior&gt; spec above without making real network calls.
  </action>
  <verify>
    <automated>pnpm --filter worker test</automated>
    <fails_when>non-zero exit, or "failed" appears in the Vitest summary line, or the second (no-change) tick's mocked postEvent call count is greater than zero</fails_when>
  </verify>
  <acceptance_criteria>
    - poll-loop.ts's git-adapter call is wrapped in try/catch; a thrown error from listWorktrees is logged, not rethrown, and the next tick still runs.
    - The gsd.phase_observed payload's `active` field is computed in poll-loop.ts, never inside packages/gsd-adapter (which stays a pure, stateless observer per Plan 03's design).
    - No emitted payload anywhere in event-emitter.ts or poll-loop.ts includes file contents, diffs, or environment-variable values — only the structural fields event-schema's payload types define (source-inspectable: the payload object literals list only repoPath/branch/worktreePath/headSha/sessionId or phase/status/category/role/active).
    - sessionId is always assigned from the real worktree path (record.path), never a literal string like "agent-1" or any other fabricated identifier.
  </acceptance_criteria>
  <done>apps/worker's poll loop correctly diffs git and GSD state tick-over-tick, emits only genuine deltas, and computes the D-02 dual-signal "active" boolean by combining process-liveness with recent-file-activity — never from either signal alone.</done>
</task>

<task type="auto">
  <name>Task 3: full-pipeline integration test + live SyncSmith demo</name>
  <files>apps/worker/src/index.integration.test.ts</files>
  <read_first>apps/worker/src/index.ts, apps/worker/src/poll-loop.ts, apps/worker/src/ws-client.ts, apps/worker/src/env.ts, .planning/PROJECT.md</read_first>
  <precondition>A worker credential has been issued via the already-deployed POST /admin/workers endpoint (Phase 2) and is available as WORKER_TOKEN for the human-check step below (user_setup step 1).</precondition>
  <action>
    Create apps/worker/src/index.integration.test.ts: start a lightweight local stub server (node:http for POST /events recording received bodies, plus a `ws` Server instance for the WS upgrade, both listening on ephemeral ports — no real apps/api or Postgres dependency, keeping this package's own test suite DB-free per 03-VALIDATION.md's ~30s feedback-latency target) that accepts any Bearer token and records every event body it receives. Set env vars pointing WORKER_TOKEN/CONTROL_PLANE_URL/WORKER_COMPANY_ID/WORKER_REPO_PATH at this stub server and a temp-git-repo fixture (reusing the Task 1/2 fixture pattern), call startWorker(), wait roughly one poll interval plus a small margin, assert the stub server recorded at least one git.worktree_observed and one worker.heartbeat event, each parseable and matching CompanyEventSchema.safeParse from packages/event-schema, then call the returned stop() and assert no further events arrive after a subsequent wait.
  </action>
  <verify>
    <automated>pnpm --filter worker test -- integration</automated>
    <fails_when>non-zero exit, or "failed" appears in the Vitest summary line, or zero events are recorded by the stub server within the wait window, or any recorded event fails CompanyEventSchema.safeParse</fails_when>
  </verify>
  <verify>
    <human-check>Point a real worker at the real SyncSmith repository (F:/Sidegigs/syncsmith, confirmed to exist this research session with status: planning and zero executed phases) via --repo, running against a real deployed or local control plane using a freshly-issued WORKER_TOKEN. Trigger a real `.planning/` change in SyncSmith (e.g. begin `/gsd-plan-phase` there far enough to create a phase directory) and confirm the corresponding git.worktree_observed / gsd.phase_observed event(s) land in PixelFirm's events table and GET /admin/workers shows the worker as "online". A transition from "no .planning/phases dir" to "phase 1 directory appears" is a valid, sufficient demonstration per 03-RESEARCH.md's Open Question 4 — a full multi-phase SyncSmith run is not required.</human-check>
  </verify>
  <acceptance_criteria>
    - The integration test's stub server never receives a git-adapter/gsd-adapter/heartbeat event with a `payload` field containing anything other than the structural keys event-schema's 3 new payload types define.
    - stop() genuinely halts both the poll loop and the heartbeat sender — no event arrives at the stub server after stop() is called (proving no orphaned timer keeps running).
  </acceptance_criteria>
  <done>A real apps/worker process, run end-to-end against a stub control plane, emits correctly-shaped, schema-valid events and cleanly stops; the live SyncSmith demo confirms the same pipeline works against a real sibling project's real (even if minimal) GSD state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker process's local OS environment (env vars, argv, filesystem) -> the events it emits | The worker is the only component with filesystem access to the pointed-at repo (PROJECT.md's explicit constraint: the control plane never needs direct filesystem access to worker repos) |
| worker -> control plane (POST /events, GET /ws) | Same Phase 2 authenticated boundary, reused unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | apps/worker/src/env.ts | high | mitigate | D-04's path-traversal / arbitrary-path concern: the resolved --repo/WORKER_REPO_PATH is validated via git-adapter's isGitWorktree (a real `git rev-parse --is-inside-work-tree` check) before the worker proceeds to any other operation |
| T-03-10 | Information Disclosure | apps/worker/src/env.ts | high | mitigate | WORKER_TOKEN is read only from process.env, never accepted as a CLI arg — a locally-running process's argv (but not its env) is visible to any other process via OS process-listing tools (verified this session on Windows via Get-CimInstance Win32_Process) |
| T-03-11 | Information Disclosure | apps/worker/src/event-emitter.ts, poll-loop.ts | medium | mitigate | Emitted payloads carry only structural metadata (repo/branch/worktree/sha/status fields) — never raw file contents, diffs, or env var values, per this plan's kept privacy prohibition |
| T-03-12 | Tampering | apps/worker/src/poll-loop.ts | medium | mitigate | No fabricated "owning agent" identity is ever emitted — sessionId is always the real observed worktree path, never a synthetic placeholder, per this plan's kept values prohibition |
| T-03-SC | Tampering | npm install of `ws` (apps/worker's own dependency) | high | mitigate | Package Legitimacy Audit already completed in 03-RESEARCH.md this session — `ws@^8.21.3` verdict OK, already proven server-side in Phase 2's own test suite (245.4M weekly downloads, github.com/websockets/ws) — no blocking human-verify checkpoint needed |

</threat_model>

<verification>
`pnpm --filter worker test` (full package suite, no DB dependency) must pass green. `pnpm test` (root Turborepo suite) at phase close must pass every package from Phases 1-3 with zero regressions. The human-check SyncSmith demo confirms the phase's Core Value claim (the worker observes REAL state, never fabricated) against a real sibling project, not just fixtures.
</verification>

<success_criteria>
- apps/worker runs as a standalone Node process (`pnpm --filter worker start`), connects outbound-only, and can be pointed at any git repository via --repo/WORKER_REPO_PATH.
- One real poll tick against a real repo (fixture or SyncSmith) produces correctly-shaped, schema-valid git.worktree_observed and gsd.phase_observed events, and only on genuine change.
- Worker connection status is observable as online/stale/offline via the control plane's GET /admin/workers, proven against a real running worker process.
- WORKTREE-02's read-only guarantee and GSD-01's "prefer observed over guessed" guarantee both hold in the fully-wired system, not just in isolated package tests.
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces

- `apps/worker` — new workspace package, runnable via `pnpm --filter worker start`
- `loadEnv(): Promise<{controlPlaneUrl, token, companyId, repoPath}>` — `apps/worker/src/env.ts` (D-04's `--repo` / `WORKER_REPO_PATH` resolution + validation)
- `computeBackoffDelay(attempt, rand?)`, `connectWorker(url, token)`, `startHeartbeat(url, token)`, `startReconnectingConnection(url, token, onOpen, onClose)` — `apps/worker/src/ws-client.ts`
- `buildEnvelope(companyId, type, payload, visibility?)`, `postEvent(url, token, event)` — `apps/worker/src/event-emitter.ts`
- `startPollLoop({repoPath, planningDir, phaseId, companyId, controlPlaneUrl, token, intervalMs?}): {stop()}` — `apps/worker/src/poll-loop.ts`
- `startWorker(): Promise<{stop()}>` — `apps/worker/src/index.ts`, the phase's top-level runnable entrypoint
</artifacts_produced>

<output>
Create `.planning/phases/03-worker-git-adapter-gsd-adapter/03-04-SUMMARY.md` when done
</output>
