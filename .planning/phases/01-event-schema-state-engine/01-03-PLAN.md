---
phase: 01-event-schema-state-engine
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/company-core/src/reducer.ts
  - packages/company-core/src/reducer.test.ts
autonomous: true
gap_closure: true
requirements: [EVENT-03]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A schema-valid floor.created/project.created/task.created/agent.online/session.started event with its correlation id (floorId/projectId/taskId/sourceAgentId) omitted no-ops through reduce() — returns the input ProjectionState unchanged — instead of writing a record keyed by the literal string \"undefined\""
    - "The pre-existing full 12-event fixture projection test and full-catalog replay-determinism test (both from 01-02-PLAN.md) continue to pass unmodified after the guard is applied"
  artifacts:
    - path: "packages/company-core/src/reducer.ts"
      provides: "floor.created/project.created/task.created/agent.online/session.started handlers apply the same guard-and-no-op pattern already used by git.commit_created/deployment.started"
    - path: "packages/company-core/src/reducer.test.ts"
      provides: "Regression test coverage proving each of the 5 handlers safely no-ops when its correlation id is omitted"
  key_links:
    - from: "packages/company-core/src/reducer.test.ts"
      to: "packages/company-core/src/reducer.ts"
      via: "reducer.test.ts imports reduce/emptyState from ./reducer and asserts the guarded handlers no-op on a missing correlation id"
      pattern: "reduce(emptyState()"
  prohibitions:
    - requirement_id: "EVENT-03"
      category: "integrity"
      status: "resolved"
      verification: "test"
      statement: "MUST NOT leave any of floor.created/project.created/task.created/agent.online/session.started asserting non-null on an optional envelope correlation id without a preceding guard — every one of the 5 handlers must return state unchanged when its id is missing, exactly like git.commit_created/deployment.started already do."
---

<objective>
Close the EVENT-03 gap identified independently by 01-VERIFICATION.md (Truth 2 failed) and 01-REVIEW.md (CR-01): `packages/company-core/src/reducer.ts`'s `floor.created`, `project.created`, `task.created`, `agent.online`, and `session.started` handlers assert non-null on envelope correlation-id fields (`floorId`/`projectId`/`taskId`/`sourceAgentId`) that `BaseEnvelope` declares `.optional()` on every event type. A schema-valid event of one of these types that omits its id passes `.safeParse()` and then silently corrupts `ProjectionState` (e.g. `state.floors["undefined"] = { id: undefined, ... }`) instead of being rejected or safely no-op'd.

Purpose: Make the Company State Engine actually satisfy "correctly produce projections" (this phase's own goal) for the full space of schema-valid input, not just the happy-path fixture — before Phase 2+ builds a durable event log and eventual renderer/dashboard directly on top of this projection.
Output: `packages/company-core/src/reducer.ts` with all 5 previously-unguarded handlers applying the guard-and-no-op pattern that already exists correctly in the same file (`git.commit_created`, `deployment.started`), plus regression test coverage proving it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-event-schema-state-engine/01-CONTEXT.md
@.planning/phases/01-event-schema-state-engine/01-VERIFICATION.md
@.planning/phases/01-event-schema-state-engine/01-REVIEW.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Schema-valid event -> reducer | An event that passes `CompanyEventSchema.safeParse()` is structurally well-formed but not necessarily semantically complete — `.optional()` envelope fields mean "parses" and "safe to index with" are not the same guarantee. This boundary is internal to Phase 1 today, but is exactly where a real worker/adapter's malformed or partial event (Phase 2+) will land. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-03 | Tampering | packages/company-core/src/reducer.ts (floor.created/project.created/task.created/agent.online/session.started) | high | mitigate | Guard-and-no-op pattern applied to all 5 handlers so a schema-valid event omitting its correlation id cannot corrupt `ProjectionState` with an `"undefined"`-keyed record; `reducer.test.ts` asserts the no-op behavior for each handler individually |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard-and-no-op the 5 unguarded handlers + regression tests (EVENT-03, CR-01)</name>
  <files>packages/company-core/src/reducer.ts, packages/company-core/src/reducer.test.ts</files>
  <read_first>packages/company-core/src/reducer.ts, packages/company-core/src/reducer.test.ts, packages/company-core/src/fixtures/stub-events.ts, packages/event-schema/src/envelope.ts</read_first>
  <behavior>
    - reducer.test.ts: a schema-valid `floor.created` event with `floorId` omitted, passed to `reduce(emptyState(), event)`, returns a result deep-equal to `emptyState()` — no `floors["undefined"]` entry is created
    - reducer.test.ts: a schema-valid `project.created` event with `projectId` omitted behaves the same way for `state.projects`
    - reducer.test.ts: a schema-valid `task.created` event with `taskId` omitted behaves the same way for `state.tasks`
    - reducer.test.ts: a schema-valid `agent.online` event with `sourceAgentId` omitted behaves the same way for `state.agents` — and the `teams` side effect is also skipped (the whole handler no-ops, it does not partially apply)
    - reducer.test.ts: a schema-valid `session.started` event with `sourceAgentId` omitted behaves the same way for `state.agents` (hardens the guard explicitly — the current code happens to no-op here already by incidental key-miss, but must no-op by an explicit, testable contract, matching the other 4 handlers and 01-REVIEW.md's WR-01 consistency note)
    - reducer.test.ts: the pre-existing full 12-event fixture projection test (`fold — full 12-event fixture (EVENT-03)`) and full-catalog replay-determinism test (`replay determinism — full catalog scale (EVENT-04)`) from 01-02-PLAN.md still pass unmodified — the guard must not change behavior when the correlation id IS present
  </behavior>
  <action>
    In `packages/company-core/src/reducer.ts`, apply the guard-and-no-op pattern that already exists correctly in the same file for `git.commit_created` and `deployment.started` (read the optional envelope field into a local const, then return `state` unchanged when it's falsy — never proceed with a trailing non-null-assertion read) to these 5 handlers:

    - `floor.created`: read `event.floorId` into a local `floorId` const; if falsy, return `state` unchanged; otherwise write `state.floors[floorId] = { id: floorId, name: event.payload.name }`.
    - `project.created`: read `event.projectId` into a local `projectId` const; same guard; write `state.projects[projectId] = { id: projectId, name: event.payload.name }`.
    - `task.created`: read `event.taskId` into a local `taskId` const; same guard; write `state.tasks[taskId] = { id: taskId, status: "created", title: event.payload.title }`.
    - `agent.online`: read `event.sourceAgentId` into a local `agentId` const; if falsy, return `state` unchanged (the entire handler no-ops — do not create the `teams` record when the agent id is missing, since agent.online's only reason to touch `teams` is as a side effect of a real agent joining); otherwise write both `state.agents[agentId] = { id: agentId, status: "idle", name: event.payload.name }` and the existing `state.teams[event.payload.teamId]` upsert logic unchanged.
    - `session.started`: read `event.sourceAgentId` into a local `agentId` const, then `const existing = agentId ? state.agents[agentId] : undefined;` and guard on `!agentId || !existing` — this is the exact double-guard shape `git.commit_created`/`deployment.started` already use, just applied to the agent lookup instead of a project/task lookup. On success, write `state.agents[agentId] = { ...existing, status: "working" }`.

    Every handler must still return a NEW object via spread on the success path (never mutate `state` in place) — this discipline is unchanged from the rest of the file.

    In `packages/company-core/src/reducer.test.ts`, add a new `describe` block (e.g. `"EVENT-03 gap closure — omitted correlation id no-ops instead of corrupting state"`) with one `it()` per handler above, each constructing a minimal, schema-shaped `CompanyEvent` object (same literal-object style as the existing `companyStartedEvent()` helper — `id`/`version`/`occurredAt`/`companyId`/`visibility`/`type`/`payload`, cast `as CompanyEvent`) for the relevant `type`, deliberately omitting the one correlation-id field under test, and asserting `reduce(emptyState(), event)` is deep-equal (`toEqual`) to `emptyState()`.
  </action>
  <verify>
    <automated>pnpm --filter company-core test -- reducer.test.ts && pnpm turbo run test</automated>
    <fails_when>Either command exits non-zero — any of the 5 new omitted-correlation-id regression assertions fails to no-op safely, any pre-existing reducer.test.ts assertion regresses, or the full Turborepo workspace suite fails</fails_when>
  </verify>
  <acceptance_criteria>
    - packages/company-core/src/reducer.ts's `floor.created` handler contains `if (!floorId) return state;` before constructing the `floors` record
    - packages/company-core/src/reducer.ts's `project.created` handler contains `if (!projectId) return state;` before constructing the `projects` record
    - packages/company-core/src/reducer.ts's `task.created` handler contains `if (!taskId) return state;` before constructing the `tasks` record
    - packages/company-core/src/reducer.ts's `agent.online` handler contains `if (!agentId) return state;` before constructing the `agents`/`teams` records
    - packages/company-core/src/reducer.ts's `session.started` handler contains `if (!agentId || !existing) return state;`
    - `pnpm --filter company-core test -- reducer.test.ts` exits 0, including the 5 new regression cases and the pre-existing full-fixture/replay-determinism tests from 01-02
  </acceptance_criteria>
  <done>All 5 previously-unguarded handlers (floor.created, project.created, task.created, agent.online, session.started) guard on their optional envelope id and no-op when it's missing; reducer.test.ts proves this per-handler with a passing regression test; the pre-existing full-fixture and replay-determinism tests still pass unmodified; the full Turborepo suite is green.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter company-core test -- reducer.test.ts` (new regression tests + full pre-existing suite), then `pnpm turbo run test` for the whole workspace (event-schema + company-core). Both must exit 0.
</verification>

<success_criteria>
- All 5 previously-unguarded reducer.ts handlers now guard on their optional envelope correlation id and no-op (return state unchanged) instead of asserting non-null and corrupting the projection
- reducer.test.ts has one passing regression test per handler proving the omitted-id case no-ops safely
- No regression: the full 12-event fixture projection test and full-catalog replay-determinism test from 01-02 still pass unmodified
- EVENT-03 ("Company State Engine builds materialized projections... read by downstream consumers") holds for the full space of schema-valid input, not just the happy-path fixture
</success_criteria>

## Artifacts this phase produces

No new exported symbols and no new files. This plan modifies existing handler bodies inside `packages/company-core/src/reducer.ts` (adds local guard consts — `floorId`/`projectId`/`taskId`/`agentId`/`existing` — inside 5 existing dispatch-table entries; no new exports, no signature changes to `reduce()`/`fold()`) and adds one new `describe` block with 5 new `it()` regression cases to the existing `packages/company-core/src/reducer.test.ts` (no new exported symbols).

<output>
Create `.planning/phases/01-event-schema-state-engine/01-03-SUMMARY.md` when done
</output>
