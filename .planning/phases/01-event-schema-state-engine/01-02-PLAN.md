---
phase: 01-event-schema-state-engine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/event-schema/src/payloads/index.ts
  - packages/event-schema/src/envelope.test.ts
  - packages/company-core/src/projections.ts
  - packages/company-core/src/reducer.ts
  - packages/company-core/src/fixtures/stub-events.ts
  - packages/company-core/src/reducer.test.ts
autonomous: true
requirements: [EVENT-01, EVENT-03, EVENT-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every one of the 12 seeded event types (one per required category: company/floor/project/task/agent/session/handoff/review/approval/git/deployment/viewer) passes `.safeParse()` when valid, and fails when malformed (missing required field or wrong discriminator literal)"
    - "Feeding the ordered 12-event fixture sequence into `fold()` produces a `ProjectionState` with a populated entry for every touched agent/floor/team/project/task"
    - "Calling `fold()` on the identical 12-event fixture array twice, both times from a fresh empty state, produces deep-equal `ProjectionState` results — replay determinism proven at full catalog scale, not just the Plan 01 tracer's single event"
  artifacts:
    - path: "packages/event-schema/src/payloads/index.ts"
      provides: "CompanyEventSchema extended to all 12 seeded discriminated-union members"
    - path: "packages/company-core/src/fixtures/stub-events.ts"
      provides: "Shared ordered 12-event fixture array reused by both the projection and replay-determinism tests"
    - path: "packages/company-core/src/reducer.ts"
      provides: "Full dispatch table — one handler per seeded event type, populating all five ProjectionState kinds"
  key_links:
    - from: "packages/company-core/src/reducer.test.ts"
      to: "packages/company-core/src/fixtures/stub-events.ts"
      via: "reducer.test.ts imports stubEventSequence and calls fold(stubEventSequence)"
      pattern: "stubEventSequence"
  edge_cases:
    - requirement_id: "EVENT-01"
      category: "ordering"
      status: "resolved"
      resolution: "The 12-event fixture is authored in realistic causal order (e.g. agent.online before any event referencing that agent, per Pitfall 3), and Array.reduce processes it strictly in that order — the replay-determinism test proves the same order in always yields the same projection out, which is the concrete meaning of 'stable' for this reducer."
  prohibitions:
    - requirement_id: "EVENT-04"
      category: "transparency"
      status: "resolved"
      verification: "test"
      statement: "MUST NOT implement fold()/replay via a cache keyed by array reference (or any other memoization) that would make the replay-determinism test pass without the reducer actually re-executing over the array each call."
---

<objective>
Expand Plan 01's proven tracer to the full 12-category event catalog (D-01): add the remaining 11 representative event types to the discriminated union, register a reducer handler for each, and prove both full projection coverage (EVENT-03) and replay-determinism (EVENT-04) at catalog scale using a single shared, causally-ordered fixture array.

Purpose: Complete Phase 1 scope by scaling Plan 01's proven architecture across every required event category, without introducing any new architectural pattern.
Output: `packages/event-schema` and `packages/company-core` each fully implementing this phase's Success Criteria 1-3 against all 12 seeded event types.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-event-schema-state-engine/01-CONTEXT.md
@.planning/phases/01-event-schema-state-engine/01-RESEARCH.md
@.planning/phases/01-event-schema-state-engine/01-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Fixture/test input -> schema | Test-authored (trusted in Phase 1) but structurally represents the future untrusted boundary where a real worker/adapter (Phase 2+) will submit externally-observed events |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Tampering | packages/event-schema (CompanyEventSchema, all 12 members) | high | mitigate | Same Zod discriminatedUnion + `.safeParse()` + plain `z.object()` payload discipline from Plan 01, applied to all 11 newly-added members |
| T-01-02 | Spoofing | packages/event-schema (sourceAgentId field) | low | transfer | Unchanged from Plan 01 — forward-referenced to Phase 2's SEC-02, not a Phase 1 gap |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Seed the remaining 11 event categories (D-01)</name>
  <files>packages/event-schema/src/payloads/index.ts, packages/event-schema/src/envelope.test.ts</files>
  <action>
    Extend `CompanyEventSchema`'s discriminated union with the remaining 11 representative events from RESEARCH.md's seed table (spread composition, never `.extend()` chains — Pitfall 2): `floor.created`, `project.created`, `task.created`, `agent.online`, `session.started`, `agent.handoff_requested`, `review.started`, `ceo.approval_requested`, `git.commit_created`, `deployment.started`, `viewer.event`. Give each a small, plausible `payload` object (for example `task.created: { title: z.string() }`, `git.commit_created: { sha: z.string(), message: z.string() }`, `agent.handoff_requested: { taskId: z.string(), toAgentId: z.string() }`) — exact field choice is Claude's discretion per CONTEXT.md, but every payload schema must be a plain `z.object()` (never `z.looseObject()`), so unrecognized extra keys are stripped rather than passed through to the reducer (the Tampering mitigation in this phase's threat model).

    Extend `envelope.test.ts` with: (a) one passing `.safeParse()` assertion per newly-added event type (12 total including Plan 01's `company.started`), (b) at least one malformed-event assertion per new type (missing a required payload field, or the wrong `type` literal) proving rejection.
  </action>
  <verify>
    <automated>pnpm --filter event-schema test -- envelope.test.ts</automated>
    <fails_when>vitest run exits non-zero — any of the 12 valid-event assertions fails to parse, or any malformed-event assertion unexpectedly succeeds</fails_when>
  </verify>
  <done>All 12 seeded event types round-trip through `.safeParse()` correctly; envelope.test.ts covers a valid and a malformed case for each</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full projection coverage + shared fixture + replay-determinism at scale (EVENT-03, EVENT-04)</name>
  <files>packages/company-core/src/projections.ts, packages/company-core/src/reducer.ts, packages/company-core/src/fixtures/stub-events.ts, packages/company-core/src/reducer.test.ts</files>
  <behavior>
    - reducer.test.ts: folding the full 12-event fixture produces a `ProjectionState` with a populated entry for every touched agent/floor/team/project/task
    - reducer.test.ts: folding the identical fixture array twice, both times from a fresh empty state, produces deep-equal results (toEqual)
    - reducer.ts contains no cache/memoization keyed by array reference (grep-verified — the EVENT-04 transparency prohibition)
  </behavior>
  <action>
    Register a reducer handler for each of the 11 new event types (extending Plan 01's dispatch table), each returning a NEW state object (spread, never mutate — same discipline as Plan 01) and populating whichever of `agents`/`floors`/`teams`/`projects`/`tasks` that event type touches, so the five `ProjectionState` kinds all end up populated by the full fixture run.

    Build `packages/company-core/src/fixtures/stub-events.ts`: an ordered, hand-crafted array of exactly 12 `CompanyEvent` objects (one per category) in realistic causal order — for example `company.started` then `floor.created` then `project.created` then `agent.online` then `task.created` then `session.started` then `agent.handoff_requested` then `review.started` then `ceo.approval_requested` then `git.commit_created` then `deployment.started` then `viewer.event` — so no handler ever reads a `state.agents[id]`/`state.tasks[id]` entry that doesn't exist yet (Pitfall 3). All `id`/`occurredAt` values are hardcoded literals in the fixture (never `crypto.randomUUID()`/`Date.now()` calls) so the fixture itself is deterministic input.

    Extend `reducer.test.ts` with: (a) a projection assertion folding the full fixture and checking every touched entity kind is populated (EVENT-03), (b) the replay-determinism test folding the same fixture array twice from a fresh default state and asserting `toEqual` (EVENT-04) — this is the direct, full-catalog version of Plan 01's single-event proof.

    Do not introduce any cache/memoization (a `Map`/`WeakMap` keyed by the input array, or similar) inside `fold`/`reduce` or any handler — the replay-determinism proof must come from the reducer genuinely being pure and re-executing every call, not from a cached result.
  </action>
  <verify>
    <automated>pnpm --filter company-core test -- reducer.test.ts && ! grep -v '^\s*//' packages/company-core/src/reducer.ts | grep -Eiq 'memoiz|new Map\(|new WeakMap\('</automated>
    <fails_when>vitest run exits non-zero (a projection or replay-determinism assertion fails), OR the grep after the vitest run exits zero (a memoization pattern was found in reducer.ts, meaning the negation failed)</fails_when>
  </verify>
  <done>The full 12-event fixture folds into a fully-populated ProjectionState; folding it twice from a fresh state is deep-equal; reducer.ts carries no memoization</done>
</task>

</tasks>

<verification>
Run `pnpm --filter event-schema test -- envelope.test.ts`, `pnpm --filter company-core test -- reducer.test.ts`, then `pnpm turbo run test` for the full suite.
</verification>

<success_criteria>
- All 12 seeded event categories validate/reject correctly
- Full ProjectionState (agents/floors/teams/projects/tasks) populated by folding the fixture
- Replay-determinism proven at full catalog scale with no memoization shortcut
</success_criteria>

<output>
Create `.planning/phases/01-event-schema-state-engine/01-02-SUMMARY.md` when done
</output>
