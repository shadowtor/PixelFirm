---
phase: 01-event-schema-state-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.json
  - package.json
  - packages/event-schema/package.json
  - packages/event-schema/tsconfig.json
  - packages/event-schema/src/envelope.ts
  - packages/event-schema/src/payloads/index.ts
  - packages/event-schema/src/index.ts
  - packages/event-schema/src/envelope.test.ts
  - packages/company-core/package.json
  - packages/company-core/tsconfig.json
  - packages/company-core/src/projections.ts
  - packages/company-core/src/reducer.ts
  - packages/company-core/src/index.ts
  - packages/company-core/src/reducer.test.ts
autonomous: false
requirements: [EVENT-01, EVENT-03, EVENT-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `company.started` event with all required envelope fields (id, version, occurredAt, companyId, visibility, payload) passes `.safeParse()`; the same event missing a required field, with an invalid `type` literal, or missing `visibility` is rejected"
    - "Folding a single `company.started` event through `fold()` twice, each time starting from a fresh empty `ProjectionState` (no explicit carryover), produces deep-equal results — proving replay determinism from day one"
    - "`fold([])` returns the untouched empty initial state; `fold([event])` applies exactly one handler; an event with an unrecognized `type` causes the reducer to no-op rather than throw"
    - "The pnpm workspace bootstraps and both `event-schema` and `company-core` packages build/test independently via `pnpm --filter <pkg> test`"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace glob (packages/*) picking up both packages with zero future config edits (D-02)"
    - path: "turbo.json"
      provides: "Turbo 2.x `tasks.test` pipeline entry (not the deprecated `pipeline` key from Turbo 1.x)"
    - path: "packages/event-schema/src/envelope.ts"
      provides: "BaseEnvelope + VisibilitySchema — the shared envelope fields every event carries"
    - path: "packages/event-schema/src/payloads/index.ts"
      provides: "CompanyEventSchema discriminated union, seeded with the `company.started` member"
    - path: "packages/company-core/src/projections.ts"
      provides: "ProjectionState interface (agents/floors/teams/projects/tasks)"
    - path: "packages/company-core/src/reducer.ts"
      provides: "reduce()/fold() dispatch-table reducer with the `company.started` handler"
    - path: "packages/company-core/src/reducer.test.ts"
      provides: "Replay-determinism, empty/single-event, and unknown-type no-op proofs"
  key_links:
    - from: "packages/company-core/src/reducer.ts"
      to: "packages/event-schema/src/index.ts"
      via: "import type { CompanyEvent } from 'event-schema'"
      pattern: "from ['\"]event-schema['\"]"
  edge_cases:
    - requirement_id: "EVENT-01"
      category: "adjacency"
      status: "resolved"
      resolution: "Duplicate/adjacent identical events are not merged or deduplicated in Phase 1 — fold() applies every event in the input array independently, in the order given (D-03's ordered-exactly-once assumption). True dedup-by-event-ID is explicitly deferred to Phase 2's ingestion layer, per D-03's carry-forward requirement."
    - requirement_id: "EVENT-01"
      category: "empty"
      status: "resolved"
      resolution: "reducer.test.ts asserts fold([]) is deep-equal to emptyState() (untouched) and fold([oneEvent]) applies exactly that one handler."
    - requirement_id: "EVENT-03"
      category: "unclassified"
      status: "resolved"
      resolution: "reducer.test.ts asserts that dispatching an event with an unrecognized `type` value returns the input state unchanged rather than throwing, per RESEARCH.md Pattern 2's dispatch-table design (handler-found-or-noop)."
    - requirement_id: "EVENT-04"
      category: "unclassified"
      status: "resolved"
      resolution: "The replay-determinism test calls fold(events) twice with no explicit initial-state argument, so both calls start from a fresh, canonical emptyState() rather than a caller-mutated carryover — this is the concrete proof that 'replay from the start' means what EVENT-04 requires."
  prohibitions:
    - requirement_id: "EVENT-01"
      category: "safety"
      status: "resolved"
      verification: "test"
      statement: "MUST NOT make `visibility` optional or defaultable on any event schema member — every event's visibility level must be an explicit, required literal, since Phase 7's stream-safety filtering (SAFE-01/SAFE-02) depends entirely on every event carrying a real value here, never an implicit gap."
    - requirement_id: "EVENT-03"
      category: "values"
      status: "dismissed"
      reason: "No bespoke values/safety concern beyond RESEARCH.md's existing read-only/immutability anti-pattern (Pattern 2: every handler returns a new object, never mutates state in place), which this plan's reducer.ts implementation already resolves mechanically."
---

<objective>
Bootstrap the pnpm/Turborepo monorepo workspace (D-02 scope only) and build the first end-to-end tracer slice of the event schema + Company State Engine: one representative event type (`company.started`) validated by a Zod discriminated union, folded by a pure reducer into a materialized projection, and proven replay-deterministic by folding the same event twice from a fresh state. This is the thinnest real path through every layer Phase 1 touches — Plan 02 expands the same architecture to the remaining 11 seeded event categories.

Purpose: Prove the event-sourcing architecture (schema validation -> pure reduce -> projection -> replay) end-to-end before investing in the full 12-category catalog, per this phase's tracer-first decomposition.
Output: A working pnpm workspace with `packages/event-schema` and `packages/company-core`, each with a green test suite proving EVENT-01/EVENT-03/EVENT-04 in miniature.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-event-schema-state-engine/01-CONTEXT.md
@.planning/phases/01-event-schema-state-engine/01-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Fixture/test input -> schema | Test-authored (trusted in Phase 1) but structurally represents the future untrusted boundary where a real worker/adapter (Phase 2+) will submit externally-observed events |
| npm registry -> workspace install | `pnpm add` pulls third-party code (zod/vitest/turbo) executed at build/test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Tampering | packages/event-schema (CompanyEventSchema) | high | mitigate | Zod `discriminatedUnion` + `.safeParse()` rejects any event whose `type` doesn't match a known literal; every payload schema is a plain `z.object()` (never `z.looseObject()`) so unrecognized extra keys are stripped rather than silently passed to the reducer |
| T-01-02 | Spoofing | packages/event-schema (sourceAgentId field) | low | transfer | An event's claimed `sourceAgentId` is not authenticated in Phase 1 — there is no transport/auth layer yet to verify who produced an event. Schema validation here only proves an event is well-formed. Forward-referenced to Phase 2's SEC-02 (per-worker credentials), not a Phase 1 gap. |
| T-01-SC | Tampering | npm install of zod/vitest/turbo | high | mitigate | `checkpoint:human-verify` (gate="blocking-human") before install, per RESEARCH.md's Package Legitimacy Audit — all three flagged `SUS` (`too-new`) but assessed as likely false positives (hundreds of millions of weekly downloads, matching long-established GitHub orgs); human confirms via npmjs.com before Task 2 installs anything |

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy checkpoint for zod/vitest/turbo</name>
  <what-built>Nothing yet — this gate runs before any install. RESEARCH.md's Package Legitimacy Audit flagged zod@4.6.5, vitest@5.0.1, and turbo@2.10.13 as `SUS` (reason: `too-new`, based on how recently each *current version* was published — days, not weeks) even though all three have hundreds of millions of weekly downloads and match their long-established GitHub orgs (colinhacks/zod, vitest-dev/vitest, vercel/turborepo).</what-built>
  <how-to-verify>
    Confirm each package is legitimate before it gets installed:
    1. https://www.npmjs.com/package/zod — confirm publisher/org matches colinhacks, weekly downloads in the hundreds of millions
    2. https://www.npmjs.com/package/vitest — confirm publisher/org matches vitest-dev
    3. https://www.npmjs.com/package/turbo — confirm publisher/org matches vercel
    RESEARCH.md's own assessment: these are almost certainly false positives from the legitimacy checker's recency heuristic reacting to routine patch releases, not real slopsquatting signals. This should be a fast confirmation, not an investigation.
  </how-to-verify>
  <resume-signal>Type "approved" once all three packages are confirmed legitimate, or name which one is not</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bootstrap pnpm workspace + package scaffolding (D-02)</name>
  <precondition>Package-legitimacy checkpoint (Task 1) approved</precondition>
  <files>pnpm-workspace.yaml, turbo.json, tsconfig.json, package.json, packages/event-schema/package.json, packages/event-schema/tsconfig.json, packages/company-core/package.json, packages/company-core/tsconfig.json</files>
  <action>
    Enable pnpm first — it is not installed on this machine (RESEARCH.md Environment Availability): run `corepack enable pnpm`, falling back to `npm install -g pnpm` if corepack is unavailable. Record the resolved `pnpm --version` and pin it as the root package.json's `packageManager` field (e.g. `pnpm@[resolved-version]`) so the workspace records the pnpm major actually used, per RESEARCH.md Open Question 2's recommendation (do not blindly force `^9`).

    Create the workspace root (D-02 scope only — no `apps/*`, no other `packages/*` stub directories):
    - `pnpm-workspace.yaml` with `packages: ["packages/*"]`
    - Root `package.json`: `name: "pixelfirm"`, `private: true`, `packageManager` pinned as above, no other scripts needed yet
    - Root `tsconfig.json`: shared base compilerOptions only (`target: ES2022`, `module: NodeNext`, `strict: true`, `skipLibCheck: true`) — no project-references/composite setup, nothing in this phase's verify commands needs `tsc --build`
    - `turbo.json`: use Turborepo 2.x's `tasks` key (NOT the deprecated `pipeline` key from Turbo 1.x) with a `test` task entry

    Create `packages/event-schema/package.json` with the **unscoped** name `"event-schema"` (matching the mandated `pnpm --filter event-schema` verify commands from RESEARCH.md/01-VALIDATION.md — do not scope it `@pixelfirm/event-schema`, that would break those exact commands), `"private": true`, `"type": "module"`, and a `"test": "vitest run"` script (never bare `"vitest"`, which defaults to watch mode and would hang automated verification).
    Create `packages/company-core/package.json` with the unscoped name `"company-core"` (same reasoning), `"test": "vitest run"`, depending on `"event-schema": "workspace:*"`. `company-core` does not need `zod` directly — it only consumes the already-validated `CompanyEvent` type via its workspace dependency on `event-schema`.
    Create a matching `tsconfig.json` for each package, extending the root via `"extends"`.

    Install pinned dependencies — never let `pnpm add` resolve to npm's `latest` dist-tag (per Pitfall 4, `typescript@latest` is now a Go-native 7.0.2 with no programmatic compiler API yet): `pnpm add -w -D typescript@^5.7 vitest@^5.0 turbo@^2.10`, then `pnpm --filter event-schema add zod@^4.6`.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter event-schema list && pnpm --filter company-core list</automated>
    <fails_when>Non-zero exit code — either `pnpm install` fails to resolve the workspace, or either `pnpm --filter` list command fails because the package name filter does not resolve (proves the unscoped `name` fields are wired correctly)</fails_when>
  </verify>
  <done>Workspace installs cleanly; both packages exist with `test` scripts wired to `vitest run`; `pnpm --filter event-schema list` and `pnpm --filter company-core list` both resolve</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: Tracer — one event type validated, reduced, and proven replay-deterministic (D-01, D-04)</name>
  <reversibility rating="costly">D-03: the reducer assumes ordered, exactly-once input with no event-ID dedup — correct for Phase 1's in-memory fixtures, but if Phase 2 planning forgets to add dedup-at-ingestion as a named requirement once a real transport exists, the gap surfaces as production state drift (duplicate event application) rather than a build-time error.</reversibility>
  <files>packages/event-schema/src/envelope.ts, packages/event-schema/src/payloads/index.ts, packages/event-schema/src/index.ts, packages/event-schema/src/envelope.test.ts, packages/company-core/src/projections.ts, packages/company-core/src/reducer.ts, packages/company-core/src/index.ts, packages/company-core/src/reducer.test.ts</files>
  <behavior>
    - envelope.test.ts: a well-formed `company.started` event (id/version/occurredAt/companyId/visibility/payload all present) passes `.safeParse()`
    - envelope.test.ts: the same event with `visibility` removed fails `.safeParse()` (EVENT-01 prohibition: visibility is never optional)
    - envelope.test.ts: the same event with an unknown `type` literal (e.g. "not.a.real.type") fails `.safeParse()`
    - reducer.test.ts: `fold([])` returns `emptyState()` unchanged (deep-equal)
    - reducer.test.ts: `fold([oneCompanyStartedEvent])` applies exactly the `company.started` handler
    - reducer.test.ts: dispatching an event with an unrecognized `type` through `reduce()` returns the input state unchanged, not a thrown error
    - reducer.test.ts: `fold([oneCompanyStartedEvent])` called twice, each with no explicit initial-state argument, produces deep-equal `ProjectionState` results (replay determinism, EVENT-04)
  </behavior>
  <action>
    Per D-01, seed the discriminated union with exactly ONE member for this task: `company.started` (category "company"). Per RESEARCH.md Pattern 1:
    - `envelope.ts`: `VisibilitySchema = z.enum(["PRIVATE","INTERNAL","STREAM_SAFE","PUBLIC"])`; `BaseEnvelope` with `id` (uuid string), `version` (positive int, D-04's schema-version field starting at 1 — no upcast/dispatch machinery), `occurredAt` (ISO datetime string), `companyId` (required string — every event belongs to a company), `floorId`/`projectId`/`taskId`/`sourceAgentId`/`destinationAgentId` (all optional strings, since not every event category touches every scope — this optionality choice is Claude's discretion per CONTEXT.md), `visibility` (required `VisibilitySchema`, never `.optional()` or `.default()` — this is the EVENT-01 safety prohibition the behavior tests above assert).
    - `payloads/index.ts`: compose the union member via spread (`{ ...BaseEnvelope.shape, type: z.literal("company.started"), payload: z.object({ name: z.string() }) }`), never chained `.extend()` (Pitfall 2 — quadratic typecheck cost as the union grows toward ~40 members in later phases). Export `CompanyEventSchema = z.discriminatedUnion("type", [...])` and `export type CompanyEvent = z.infer<typeof CompanyEventSchema>`.
    - `index.ts`: barrel-export the schema and inferred type.
    - `packages/company-core/src/projections.ts`: define the full `ProjectionState` interface now (agents/floors/teams/projects/tasks, all `Record<string, XState>`) even though only the `company.started` handler exists yet — Plan 02 fills in the remaining handlers against this same shape.
    - `packages/company-core/src/reducer.ts`: a `handlers` dispatch table keyed by `CompanyEvent["type"]`, one entry (`"company.started"`) for now; `reduce(state, event)` looks up the handler and no-ops (returns `state` unchanged) when none is found — never throws on an unrecognized type. `fold(events, initial = emptyState())` folds via `Array.reduce(reduce, initial)`. No `Date.now()`, `crypto.randomUUID()`, `Math.random()`, or module-level mutable state anywhere in `reduce`/`fold`/any handler (Pitfall 1) — every value a handler needs must already be on the event object. Every handler returns a NEW object (spread), never mutates `state` in place.
  </action>
  <verify>
    <automated>pnpm --filter event-schema test -- envelope.test.ts && pnpm --filter company-core test -- reducer.test.ts</automated>
    <fails_when>Either vitest run exits non-zero — a safeParse assertion returns the wrong success/failure verdict, or a fold()/reduce() assertion fails a toEqual deep-equality check</fails_when>
  </verify>
  <done>Both test files are green; a `company.started` event flows end-to-end from schema validation through the reducer to a projection, and folding it twice from a fresh state produces identical results</done>
</task>

</tasks>

<verification>
Run `pnpm --filter event-schema test -- envelope.test.ts` and `pnpm --filter company-core test -- reducer.test.ts` — both green. Run `pnpm turbo run test` to confirm the Turborepo task graph picks up both packages.
</verification>

<success_criteria>
- Workspace bootstraps with exactly the D-02 scope (pnpm-workspace.yaml, turbo.json, root tsconfig.json, packages/event-schema, packages/company-core) — no apps/* or other packages/* stubs
- `company.started` validates and is rejected when malformed or missing `visibility`
- `fold()` proven pure and replay-deterministic on a 0-event, 1-event, and unknown-type input
</success_criteria>

<output>
Create `.planning/phases/01-event-schema-state-engine/01-01-SUMMARY.md` when done
</output>
