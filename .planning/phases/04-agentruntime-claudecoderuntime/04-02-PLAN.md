---
phase: 04-agentruntime-claudecoderuntime
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/claude-adapter/src/claude-code-runtime.ts
  - packages/claude-adapter/src/claude-code-runtime.test.ts
  - packages/claude-adapter/src/watchdog.ts
  - packages/claude-adapter/src/watchdog.test.ts
autonomous: true
requirements: [RUNTIME-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "pauseTask(taskId) ends the currently-running query() turn gracefully (the discovered interrupt mechanism, or AbortController.abort() as the documented fallback if no separate graceful method exists in the installed SDK), captures the task's session_id, and sets status to paused."
    - "resumeTask(taskId) starts a new query() call with options.resume set to the paused task's captured session_id and a fixed continuation prompt, reusing the same message-handling loop startTask uses (no duplicated for-await block)."
    - "cancelTask(taskId) attempts the graceful stop first; if the query() call has not exited within a bounded grace period, cancelTask calls AbortController.abort() to guarantee termination, and status becomes cancelled either way."
    - "The watchdog resets its timer on every message the query() stream yields (of any message type, not just result) and fires exactly once — transitioning status to blocked and posting task.status_changed(blocked) — only when the timer expires with zero messages received since the last reset."
    - statement: "[FLAGGED ASSUMPTION — unresolved probe, boundary] The exact behavior at the watchdog's bounded-silence threshold boundary (a message arriving at timeoutMs-1 vs. exactly at timeoutMs vs. timeoutMs+1) is not specified beyond CONTEXT.md's 'bounded window' language — this plan treats any message arriving strictly before the timer fires as a valid reset (no transition), and the timer firing with zero resets since the last one as the sole trigger for the blocked transition. watchdog.test.ts must assert this boundary explicitly using vitest fake timers at timeoutMs-1, timeoutMs, and timeoutMs+1."
      verification: backstop
    - statement: "[FLAGGED ASSUMPTION — unresolved probe, precision] No money/rounding domain applies to RUNTIME-02; the only timing-precision surface is the watchdog's setTimeout/Date.now() millisecond granularity and Node's timer drift under load. This plan does not add sub-millisecond tie-breaking logic and treats setTimeout's platform-standard drift tolerance as acceptable for a 90-120 second bounded window — revisit only if false-positive blocked events are observed against a real long-running GSD task."
      verification: backstop
  artifacts:
    - packages/claude-adapter/src/watchdog.ts
  key_links:
    - "packages/claude-adapter/src/claude-code-runtime.ts's shared runQuery helper -> packages/claude-adapter/src/watchdog.ts (reset on every message, clear on loop completion)"
    - "watchdog timeout -> ClaudeCodeRuntime's graceful-then-hard-kill path (same mechanism cancelTask uses, different terminal status)"
---

<objective>
Implement pauseTask/resumeTask/sendMessage via session capture (no persistent client object exists
in the TypeScript SDK — each query() call is single-shot, so pause/resume/message-injection all go
through the same resume-with-a-new-query() mechanism), cancelTask's graceful-interrupt-then-hard-kill
sequence, and the D-04 bounded-silence watchdog that guarantees getStatus() never leaves a hung task
showing "running" forever.

Purpose: RUNTIME-02 requires ClaudeCodeRuntime to actually pause/resume/cancel a real task, and
PITFALLS.md names the hang/kill-test as this phase's own explicit verification requirement.
Output: a complete, non-stub AgentRuntime implementation for six of the eight methods (startTask/
getStatus from Plan 04-01, pauseTask/resumeTask/cancelTask/sendMessage from this plan) plus the
watchdog module.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agentruntime-claudecoderuntime/04-CONTEXT.md
@.planning/phases/04-agentruntime-claudecoderuntime/04-RESEARCH.md
@packages/claude-adapter/src/claude-code-runtime.ts
@apps/worker/src/poll-loop.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pauseTask/resumeTask/sendMessage via session capture, sharing a single runQuery helper</name>
  <read_first>
    packages/claude-adapter/src/claude-code-runtime.ts (Plan 04-01's startTask implementation — the
    for-await loop this task extracts into a shared helper); 04-RESEARCH.md "Pattern 2: Pause/Resume
    via session capture (not in-process suspend)" section (resume/continue options, "In TypeScript
    the ID is also available earlier as a direct field on the init SystemMessage"); 04-RESEARCH.md
    "Pattern 3: Graceful interrupt vs. hard cancel" and its Assumption A2 / Open Question #2 (the
    exact TypeScript graceful-interrupt call shape is unconfirmed — resolve it here before writing
    cancelTask's graceful step in Task 2 of this plan, since pauseTask needs the same mechanism).
  </read_first>
  <behavior>
    - Test 1: pauseTask(taskId) on a task whose mocked query() generator is mid-stream (has yielded
      an init message with session_id but no result yet) sets status to "paused" and the task's
      recorded session_id matches the init message's session_id.
    - Test 2: resumeTask(taskId) after Test 1 calls the mocked query() a second time with options
      containing resume equal to the captured session_id; when the second mocked generator yields a
      result with subtype "success", status becomes "completed".
    - Test 3: sendMessage(taskId, "do X next") on a "running" task calls query() with resume set to
      the current session_id and prompt equal to exactly "do X next" (the caller-supplied message,
      not a fixed continuation string) — this is a documented interpretation of the AgentRuntime
      interface (sendMessage takes no prompt-shaping parameter beyond the message itself; see Plan
      04-01's RUNTIME-01 unclassified flagged assumption) — after Test 3's mocked generator resolves
      with success, status returns to "completed", never getting stuck on a "paused" transition
      sendMessage never actually performs.
    - Test 4: resumeTask/sendMessage called on a taskId with no captured session_id (never started,
      or started but never reached an init message) reject rather than silently starting a fresh
      unrelated session.
  </behavior>
  <files>packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts</files>
  <action>
    Before writing any code, run `grep -rn "interrupt\|stop_task" node_modules/@anthropic-ai/claude-agent-sdk/**/*.d.ts`
    (or the package's actual installed type-definition path) to resolve Assumption A2: determine
    whether the `Query` object `query()` returns (an AsyncGenerator plus extra methods, per Pattern 1)
    exposes an `.interrupt()` method, or whether the only available stop mechanism is the
    `options.abortController`'s `AbortController.abort()`. Record whichever is true in this plan's
    SUMMARY.md verbatim (method name and where it lives) — do not guess past this point.

    Refactor packages/claude-adapter/src/claude-code-runtime.ts: extract the for-await message loop
    Plan 04-01's startTask wrote into a private shared function, `runQuery(taskId: string, prompt:
    string, resumeSessionId?: string): Promise<void>`, that both `startTask` (prompt = input.prompt,
    no resumeSessionId) and this task's new methods call. `runQuery` creates one `AbortController`
    per invocation, stores it on the task's map entry (add `controller?: AbortController` and
    `runPromise?: Promise<void>` fields to the per-task map value), calls `query({ prompt, options:
    { cwd: <the task's worktreePath, stored on the map entry from startTask>, permissionMode:
    "default", resume: resumeSessionId, abortController } })`, and contains the exact same
    init/result message handling Plan 04-01 wrote (session_id capture, status update,
    task.status_changed posting) — this is a pure extraction, Plan 04-01's four Behavior tests must
    still pass unchanged after this refactor.

    Implement `pauseTask(taskId)`: call the graceful mechanism discovered above against the task's
    stored controller/Query handle (either `.interrupt()` if it exists, else
    `controller.abort()` as the documented fallback), await the task's `runPromise` (or a short
    timeout, whichever comes first — reuse the same grace-period pattern Task 2 of this plan uses for
    cancelTask), then set status to "paused" and post task.status_changed(paused). The session_id
    used for a later resume is whatever was captured during `runQuery` (from the init message,
    available before pause is even possible, per Pattern 2).

    Implement `resumeTask(taskId)`: read the task's stored session_id; if absent, throw (Test 4);
    otherwise call `runQuery(taskId, "Continue the task from where you left off", sessionId)` — the
    exact continuation prompt from 04-RESEARCH.md's own Pattern 2 example, verbatim.

    Implement `sendMessage(taskId, message)`: same as resumeTask but `runQuery(taskId, message,
    sessionId)` — the caller-supplied message becomes the new turn's prompt (documented
    interpretation, see Behavior Test 3's note).
  </action>
  <verify>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or the summary line does not include all of Plan 04-01's original 4 cases plus this task's 4 new cases (8 total) passing</fails_when>
  </verify>
  <acceptance_criteria>
    All four Behavior cases above pass. The discovered graceful-interrupt mechanism (method name and
    source) is recorded in this plan's SUMMARY.md. `runQuery` has exactly one call site each in
    startTask, pauseTask (indirectly via the stored controller), resumeTask, and sendMessage — no
    duplicated for-await loop remains in the file.
  </acceptance_criteria>
  <done>pauseTask/resumeTask/sendMessage are implemented against the shared runQuery helper, not stubs; Plan 04-01's startTask/getStatus tests still pass after the refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cancelTask (graceful then hard kill) + watchdog.ts bounded-silence timer (D-03, D-04)</name>
  <read_first>
    04-RESEARCH.md "Pattern 3: Graceful interrupt vs. hard cancel" code example (the
    cancelTask(taskId) shape: graceful first, `Promise.race([handle.waitForExit(),
    sleep(GRACEFUL_TIMEOUT_MS).then(() => false)])`, hard abort on timeout); 04-RESEARCH.md "Pitfall
    2: No single heartbeat signal exists in the SDK — build one from message cadence" (recommends a
    90-120 second starting timeout, reset on every message of any type); 04-PATTERNS.md's watchdog.ts
    section (recommends `createWatchdog(timeoutMs, onTimeout)` as a factory function, not a class,
    matching this codebase's function-first style).
  </read_first>
  <behavior>
    - Test 1 (watchdog.test.ts, vitest fake timers): createWatchdog(1000, onTimeout) then
      reset() at 999ms, reset() at 1998ms (never allowing 1000ms of silence) — onTimeout is never
      called through 3000ms of simulated time.
    - Test 2 (watchdog.test.ts): createWatchdog(1000, onTimeout) with no reset() calls after
      creation — onTimeout is called exactly once at the 1000ms mark, not before 999ms and not more
      than once by 2000ms (asserts the boundary flagged assumption above: 999ms = no fire, 1000ms =
      fires exactly once).
    - Test 3 (watchdog.test.ts): calling clear() before the timeout elapses prevents onTimeout from
      ever firing, even if simulated time advances far past timeoutMs afterward.
    - Test 4 (claude-code-runtime.test.ts): cancelTask(taskId) on a task whose mocked query()
      generator never yields a result message (simulating a hang) resolves within the test's
      GRACEFUL_TIMEOUT_MS-based wait, the task's status becomes "cancelled", and the mocked
      AbortController's abort() was called (asserting the hard-kill path actually fires when the
      graceful step doesn't produce a clean exit in time).
    - Test 5 (claude-code-runtime.test.ts): a task whose mocked query() generator never yields any
      message at all triggers the watchdog's onTimeout, transitioning status to "blocked" (not
      "cancelled" — a distinct terminal status from an explicit cancelTask call) and posting
      task.status_changed(blocked).
  </behavior>
  <files>packages/claude-adapter/src/watchdog.ts, packages/claude-adapter/src/watchdog.test.ts, packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts</files>
  <action>
    Create packages/claude-adapter/src/watchdog.ts exporting `createWatchdog(timeoutMs: number,
    onTimeout: () => void): { reset(): void; clear(): void }` — a small factory (no class) closing
    over a single `NodeJS.Timeout | undefined` handle. `reset()` clears any existing timer and starts
    a new `setTimeout(onTimeout, timeoutMs)`. `clear()` clears the timer without starting a new one.
    Export `const DEFAULT_WATCHDOG_TIMEOUT_MS = 100_000` (100 seconds — within RESEARCH's
    recommended 90-120s range, a tunable constant, not derived from any external config this phase).

    In packages/claude-adapter/src/claude-code-runtime.ts's `runQuery` helper (from Task 1), create
    one `createWatchdog(DEFAULT_WATCHDOG_TIMEOUT_MS, () => { <graceful-then-hard-kill against this
    task's controller>; set status "blocked"; post task.status_changed(blocked); })` per invocation,
    call `.reset()` immediately before entering the for-await loop and again on every yielded
    message regardless of type (mirroring the "reset on activity, fire on silence" shape from
    apps/worker/src/poll-loop.ts's setInterval/stop() combo, inverted per 04-PATTERNS.md), and call
    `.clear()` when the loop exits for any reason (result message received, graceful/hard cancel,
    or an uncaught stream error) so a completed task's watchdog can never fire after the fact.

    Implement `cancelTask(taskId)` in claude-code-runtime.ts using Task 1's discovered graceful
    mechanism first, then `Promise.race([task.runPromise, sleep(GRACEFUL_TIMEOUT_MS)])` where
    `GRACEFUL_TIMEOUT_MS` is a second tunable constant (e.g. 5000ms grace period — distinct from and
    much shorter than the watchdog's 100s hang-detection window, since this is "did the process exit
    after we asked nicely" not "has anything happened at all"); if the race resolves via the timeout
    rather than `runPromise` completing, call `controller.abort()` to guarantee termination. Set
    status to "cancelled" and post task.status_changed(cancelled) once either path completes.
  </action>
  <verify>
    <automated>pnpm --filter claude-adapter test -- watchdog</automated>
    <fails_when>non-zero exit, or the summary line does not contain "3 passed" for watchdog.test.ts's three cases</fails_when>
  </verify>
  <verify>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or fewer than 10 total passing cases (8 from Task 1 + this task's 2 new cancelTask/watchdog-integration cases)</fails_when>
  </verify>
  <acceptance_criteria>
    watchdog.ts exports createWatchdog and DEFAULT_WATCHDOG_TIMEOUT_MS with no dependency on
    @anthropic-ai/claude-agent-sdk (it is a generic timer utility). cancelTask never leaves a task's
    watchdog running after cancellation (verified by Test 3's clear()-prevents-firing case being
    exercised on the cancelTask path too, not just standalone). A hang (Test 5) and an explicit
    cancel (Test 4) produce different terminal statuses ("blocked" vs "cancelled").
  </acceptance_criteria>
  <done>cancelTask guarantees termination within a bounded grace period; a genuinely silent query() stream is surfaced as "blocked" within DEFAULT_WATCHDOG_TIMEOUT_MS, never left showing "running" indefinitely — this plan's own PITFALLS.md-named verification requirement.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `createWatchdog(timeoutMs, onTimeout)` factory + `DEFAULT_WATCHDOG_TIMEOUT_MS` constant — `packages/claude-adapter/src/watchdog.ts`
- `runQuery` shared internal helper (private, not exported from the package) inside `packages/claude-adapter/src/claude-code-runtime.ts`
- Non-stub `pauseTask`/`resumeTask`/`cancelTask`/`sendMessage` implementations on `createClaudeCodeRuntime`'s returned object

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker-internal call ↔ pause/resume/cancel/sendMessage | These are already-authenticated worker-internal calls (no new external listener added this plan) — the same boundary Phase 2/3's per-worker credential already established |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Denial of Service | watchdog false-negative (a real hang never detected) or false-positive (a legitimate slow tool call killed) | medium | mitigate | Tunable `DEFAULT_WATCHDOG_TIMEOUT_MS` set within RESEARCH's recommended 90-120s range; boundary behavior explicitly tested (watchdog.test.ts Test 2) rather than left to timer-drift luck |
| T-04-05 | Denial of Service | cancelTask's hard-kill path leaving an orphaned Claude Code subprocess | medium | mitigate | `AbortController.abort()` is guaranteed to run after the grace-period race, regardless of whether the graceful step succeeded — Test 4 asserts abort() is actually invoked on a hang, not just attempted |
| T-04-06 | Repudiation | pause/resume/cancel state transitions | low | accept | Every transition posts a task.status_changed event through the existing validated ingestion path (same as Plan 04-01) — no separate audit mechanism needed at this scale |

</threat_model>

<verification>
Run `pnpm --filter claude-adapter test` (full suite, both watchdog.test.ts and
claude-code-runtime.test.ts) — all green, including the boundary-timing assertions.
</verification>

<success_criteria>
pauseTask/resumeTask/sendMessage/cancelTask are real, non-stub implementations sharing one message
loop. A hang is surfaced as "blocked" within the bounded watchdog window; an explicit cancel
guarantees termination within a bounded grace period.
</success_criteria>

<output>
Create `.planning/phases/04-agentruntime-claudecoderuntime/04-02-SUMMARY.md` when done.
</output>
