---
phase: 04-agentruntime-claudecoderuntime
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - packages/claude-adapter/package.json
  - packages/claude-adapter/src/signal-detection.ts
  - packages/claude-adapter/src/signal-detection.test.ts
  - packages/claude-adapter/src/claude-code-runtime.ts
  - packages/claude-adapter/src/claude-code-runtime.test.ts
autonomous: true
requirements: [RUNTIME-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "canUseTool fires requestReview(taskId, reason) for the AskUserQuestion tool and for any Bash command matching a documented CEO-gated pattern (deploy/force-push/destructive-DB/npm publish), sets status waiting_for_review, and returns a deny decision (never an auto-approve) since no Phase 6 dashboard exists yet to grant approval (D-08)."
    - "The Notification hook fires requestReview(taskId, 'permission_prompt: unanswered ~6s') as an independent secondary signal, wired and unit-tested directly, even though this implementation's synchronous canUseTool means it rarely fires in practice."
    - "requestHandoff is invoked automatically when gsd-adapter's reused observeGsdState reports a role change during a running task (polled every 5000ms, matching apps/worker/src/poll-loop.ts's interval convention), posting agent.handoff_requested without altering the task's own AgentTaskStatus (no second agent exists yet to receive control, per CONTEXT.md's domain boundary)."
    - "gsd-adapter's observeGsdState is imported and called directly from packages/claude-adapter — no second .planning/ parser is written (04-RESEARCH.md Anti-Pattern: 'Reimplementing gsd-adapter's file-based observation inside claude-adapter')."
  artifacts:
    - packages/claude-adapter/src/signal-detection.ts
  key_links:
    - "packages/claude-adapter/src/claude-code-runtime.ts's canUseTool/Notification hooks -> packages/claude-adapter/src/signal-detection.ts's classifySignal -> requestReview -> ceo.approval_requested event"
    - "packages/claude-adapter/src/claude-code-runtime.ts's gsd role poll -> gsd-adapter's observeGsdState (reused) -> requestHandoff -> agent.handoff_requested event"
  prohibitions:
    - requirement_id: RUNTIME-02
      category: safety
      statement: "MUST NOT set permissionMode: 'bypassPermissions' for any task ClaudeCodeRuntime drives — doing so silently defeats canUseTool/requestReview's CEO-gate detection (canUseTool never fires for bypassed tools) and would let a CEO-gated operation execute without ever being surfaced, contradicting ARCHITECTURE.md Anti-Pattern 2."
---

<objective>
Wire requestReview and requestHandoff to real, live signals: canUseTool firing on AskUserQuestion
and CEO-gated Bash commands, the Notification hook's permission_prompt as an independent secondary
signal, and gsd-adapter's reused observeGsdState detecting role transitions mid-task.

Purpose: D-08 requires requestReview/requestHandoff to be functionally wired in Phase 4, not
stubbed — this plan is where the two remaining AgentRuntime methods stop throwing "not implemented"
(Plan 04-01's placeholder) and become real, per Plan 04-01's Task 3 note.
Output: a complete, eight-method, non-stub ClaudeCodeRuntime.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agentruntime-claudecoderuntime/04-CONTEXT.md
@.planning/phases/04-agentruntime-claudecoderuntime/04-RESEARCH.md
@packages/claude-adapter/src/claude-code-runtime.ts
@packages/gsd-adapter/src/index.ts
@packages/gsd-adapter/src/role-mapping.ts
@apps/worker/src/poll-loop.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: signal-detection.ts classifier + canUseTool/Notification hook wiring -> requestReview</name>
  <reversibility rating="costly">Once Phase 6's CEO dashboard is built against requestReview actually firing on real detected signals, downgrading this to a stub would silently break the dashboard's data source without a compile-time error (D-08, CONTEXT.md).</reversibility>
  <read_first>
    04-RESEARCH.md "Pattern 4: requestReview/requestHandoff Signal Detection" (canUseTool fires for
    AskUserQuestion, Notification hook fires permission_prompt after ~6s); 04-RESEARCH.md "Pitfall 3:
    allowed_tools/bypassPermissions interactions can silently widen access" (never use
    bypassPermissions); ARCHITECTURE.md Anti-Pattern 2 ("never auto-approve a CEO-gated operation
    just because an agent requested it") — this governs canUseTool's return value, not just an
    external dashboard; packages/gsd-adapter/src/role-mapping.ts (pure classifier function shape to
    mirror for classifySignal — input/result types, no I/O, no side effects).
  </read_first>
  <behavior>
    - Test 1: classifySignal("AskUserQuestion", anyInput) returns { kind: "clarifying_question",
      reason: a non-empty string mentioning the tool name }.
    - Test 2: classifySignal("Bash", { command: "git push --force origin main" }) returns { kind:
      "ceo_gated_tool", reason: a non-empty string }; classifySignal("Bash", { command: "npm test"
      }) returns null (an ordinary test run is not CEO-gated).
    - Test 3: classifySignal("Read", { file_path: "src/index.ts" }) returns null (a plain read is
      never gated).
    - Test 4 (claude-code-runtime.test.ts): a mocked query() call whose options.canUseTool is
      invoked with ("AskUserQuestion", ...) results in requestReview being called with the task's
      taskId and a non-empty reason, status becoming "waiting_for_review", a posted
      ceo.approval_requested event, and canUseTool's own return value having behavior "deny" (never
      "allow" — this must never auto-approve).
    - Test 5 (claude-code-runtime.test.ts): directly invoking the wired Notification hook function
      with a permission_prompt-shaped input calls requestReview with a reason containing
      "permission_prompt".
  </behavior>
  <files>packages/claude-adapter/src/signal-detection.ts, packages/claude-adapter/src/signal-detection.test.ts, packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts</files>
  <action>
    Create packages/claude-adapter/src/signal-detection.ts exporting a pure function
    `classifySignal(toolName: string, input: unknown): { kind: "clarifying_question" |
    "ceo_gated_tool"; reason: string } | null`. Return `{ kind: "clarifying_question", reason:
    "Claude asked a clarifying question via AskUserQuestion" }` when `toolName ===
    "AskUserQuestion"`. For `toolName === "Bash"`, check `(input as { command?: string })?.command`
    against a documented, explicit regex allowlist of CEO-gated patterns covering ARCHITECTURE.md's
    named categories (deploy, destructive op, major dependency change): `/\bforce\b.*push|push.*--force|-f\b/i`
    for force-push, `/\brm\s+-rf\b/i` for destructive filesystem ops, `/\bDROP\s+(TABLE|DATABASE)\b/i`
    for destructive DB ops, `/\bnpm\s+publish\b|\bdeploy\b/i` for publish/deploy — return `{ kind:
    "ceo_gated_tool", reason: "Bash command matched a CEO-gated pattern: <matched pattern name>" }`
    on a match, else `null`. Document in a code comment that this is a first-pass, explicitly
    heuristic allowlist (Claude's Discretion per CONTEXT.md) — not exhaustive coverage of every
    possible destructive command, and that widening it is expected future work, not a gap hidden
    by this phase.

    In packages/claude-adapter/src/claude-code-runtime.ts's `runQuery` helper, add `canUseTool:
    async (toolName, input) => { const signal = classifySignal(toolName, input); if (!signal) return
    { behavior: "allow", updatedInput: input }; await requestReview(taskId, signal.reason); return {
    behavior: "deny", message: \`Escalated to CEO for review (taskId=${taskId}) — no Phase 6
    dashboard exists yet to grant approval; see the ceo.approval_requested event.\` }; }` to the
    query() options. This must return "deny", never "allow", for any classified signal — Phase 4
    guarantees the signal fires and is never silently approved (ARCHITECTURE.md Anti-Pattern 2),
    even though the actual human-approval mechanism is Phase 6's job. Also add `hooks: { Notification:
    [{ hooks: [async (hookInput) => { await requestReview(taskId, \`permission_prompt: ${hookInput.message
    ?? "unanswered ~6s"}\`); return {}; }] }] }` to the same options object, wired as an independent
    secondary signal per 04-RESEARCH.md Pattern 4 (2) even though this implementation's synchronous
    canUseTool means it should rarely fire in practice — it remains correct defensive wiring and is
    directly unit-tested (Test 5) by invoking the hook function, independent of live canUseTool
    timing.

    Never set `permissionMode: "bypassPermissions"` anywhere in this file (already "default" from
    Plan 04-01/04-02) — this is the mechanism the plan's kept prohibition above enforces; do not
    introduce a config flag or environment-variable toggle for it (that would just move the same
    silent-bypass risk one layer up).

    Implement `requestReview(taskId, reason)` on the returned AgentRuntime object (currently a stub
    from Plan 04-01): set status "waiting_for_review", post `task.status_changed(taskId,
    "waiting_for_review")`, then post `ceo.approval_requested` with payload `{ taskId, reason }`
    (reusing the existing discriminated-union member from Phase 1 — no new event type needed here).
  </action>
  <verify>
    <automated>pnpm --filter claude-adapter test -- signal-detection</automated>
    <fails_when>non-zero exit, or the summary line does not contain "3 passed" for signal-detection.test.ts's three classifySignal cases</fails_when>
  </verify>
  <verify>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or fewer than 12 total passing cases (8 from Plan 04-01/04-02 + this task's 2 new canUseTool/Notification cases, plus 2 carried from Plan 04-02's cancelTask/watchdog integration)</fails_when>
  </verify>
  <acceptance_criteria>
    `grep -Ec 'permissionMode:\s*.(bypass' packages/claude-adapter/src/claude-code-runtime.ts`
    reports 0 (the assignment pattern is absent — a code comment merely discussing the disallowed
    mode name does not match this pattern, only an actual assignment would). Test 4 explicitly
    asserts canUseTool's return value has `behavior: "deny"` for a classified signal — a test
    asserting `behavior: "allow"` on a classified signal would be asserting the wrong (prohibited)
    behavior and must not exist in this file.
  </acceptance_criteria>
  <done>requestReview fires on both live-signal sources (canUseTool for AskUserQuestion/CEO-gated Bash, Notification hook for unanswered permission prompts) and never returns an auto-approve for a classified signal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: requestHandoff wired to gsd-adapter's reused observeGsdState role-change polling</name>
  <read_first>
    packages/gsd-adapter/src/index.ts (observeGsdState's signature and the GsdObservation shape,
    lines 12-44 — this task imports and calls this function directly, never reimplements it);
    apps/worker/src/poll-loop.ts (the poll-diff pattern: closure-scoped last-seen state, compare-then-emit-only-on-change,
    the isFirstTick guard so a first observation never spuriously "changes"); 04-RESEARCH.md's
    Architectural Responsibility Map row for "requestReview/requestHandoff signal detection" (D-08
    source 1: reused gsd-adapter poll).
  </read_first>
  <behavior>
    - Test 1: a running task whose mocked observeGsdState returns role "PM" on the first poll tick
      and role "Engineering" on the second poll tick results in requestHandoff being called with
      (taskId, "Engineering") — the new role becomes toAgentId, per CONTEXT.md's discretion note
      that no real second-agent registry exists yet in Phase 4.
    - Test 2: two consecutive poll ticks returning the same role never call requestHandoff (no
      spurious repeated handoffs for an unchanged role).
    - Test 3: requestHandoff posts agent.handoff_requested with payload { taskId, toAgentId } and
      does NOT change the task's own AgentTaskStatus (status before and after the call is identical)
      — Phase 4 has no second agent to actually receive control, so this is an observation-only
      event, not a real control transfer.
    - Test 4: the role-change poll interval is cleared when the task's runQuery loop exits (result
      message, cancel, or watchdog timeout) — no poll tick fires after the task has reached a
      terminal status.
  </behavior>
  <files>packages/claude-adapter/package.json, packages/claude-adapter/src/claude-code-runtime.ts, packages/claude-adapter/src/claude-code-runtime.test.ts</files>
  <action>
    Add `"gsd-adapter": "workspace:*"` to packages/claude-adapter/package.json's dependencies (run
    `pnpm install` after editing so the workspace link resolves — no npm registry package involved,
    this is an internal workspace dependency, no package-legitimacy checkpoint needed).

    In packages/claude-adapter/src/claude-code-runtime.ts's `runQuery` helper, alongside the
    watchdog created in Plan 04-02, start a `setInterval` (5000ms, matching
    apps/worker/src/poll-loop.ts's DEFAULT_INTERVAL_MS convention) that calls `observeGsdState(join(
    input.worktreePath, ".planning"), undefined, false)` from `gsd-adapter` (imported directly — no
    second `.planning/` parser is written in this package, per 04-RESEARCH.md's named
    Anti-Pattern). Track `lastRole` per task (add a `lastRole?: string` field to the task's map
    entry); on the first tick, record the observed role without comparing (mirroring poll-loop.ts's
    `isFirstTick` guard so a first observation is never treated as "changed"); on every subsequent
    tick, if `observed.role !== lastRole`, call `requestHandoff(taskId, observed.role)` and update
    `lastRole`. Clear this interval in the same place `watchdog.clear()` is called (loop exit for any
    reason) so no tick fires after the task reaches a terminal status.

    Implement `requestHandoff(taskId, toAgentId)` on the returned AgentRuntime object (currently a
    stub from Plan 04-01): post `agent.handoff_requested` with payload `{ taskId, toAgentId }`
    (reusing the existing discriminated-union member from Phase 1) via the local buildEnvelope/
    postEvent. Do not modify the task's AgentTaskStatus here — this is deliberately an
    observation-only event in Phase 4 (Test 3), since no second agent exists yet to actually accept
    the handoff (CONTEXT.md domain boundary: "no multi-agent concurrency model" this phase).
  </action>
  <verify>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or fewer than 16 total passing cases across claude-code-runtime.test.ts (12 from prior tasks + this task's 4 new requestHandoff cases)</fails_when>
  </verify>
  <acceptance_criteria>
    `grep -c "observeGsdState" packages/claude-adapter/src/claude-code-runtime.ts` is at least 1
    (imported and called), and no file under packages/claude-adapter/src re-parses `.planning/`
    directly (no `readFile`/`readStateMd`-equivalent logic outside the gsd-adapter import). All four
    Behavior cases pass.
  </acceptance_criteria>
  <done>requestHandoff fires on a real, reused gsd-adapter role-change signal during a live task; ClaudeCodeRuntime now implements all eight AgentRuntime methods with no remaining "not implemented" stubs.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `classifySignal(toolName, input)` pure classifier — `packages/claude-adapter/src/signal-detection.ts`
- `canUseTool` and `Notification` hook wiring inside `runQuery` — `packages/claude-adapter/src/claude-code-runtime.ts`
- Non-stub `requestReview`/`requestHandoff` implementations completing the eight-method `AgentRuntime` — `packages/claude-adapter/src/claude-code-runtime.ts`
- `gsd-adapter` added as a workspace dependency of `claude-adapter` — `packages/claude-adapter/package.json`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code's own tool-call stream ↔ canUseTool's classification logic | The tool name/input arriving at canUseTool originates from the model's own output — treated as data to classify, never as a command to execute directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Elevation of Privilege | `permissionMode` accidentally set to the SDK's full-bypass mode | high | mitigate | Kept prohibition in this plan's `must_haves.prohibitions`; Task 1's assignment-pattern grep acceptance check; `canUseTool` always returns `deny` for a classified signal, never `allow` |
| T-04-08 | Elevation of Privilege | `classifySignal`'s Bash regex allowlist missing a genuinely destructive command pattern | medium | accept | Documented as an explicit, intentionally first-pass heuristic (Task 1 action) rather than a claimed-exhaustive gate; the watchdog (Plan 04-02) remains the last line of defense against anything this misses causing a silent hang instead of a silent auto-approve — a miss here still cannot auto-approve, it can only fail to escalate a Bash command that then runs under the same permission mode any local `claude` CLI session already has |
| T-04-09 | Tampering | `requestHandoff`'s `toAgentId` sourced from `gsd-adapter`'s role mapping | low | accept | Local, trusted `.planning/` file state on the worker's own machine — same trust level every existing `gsd-adapter` consumer already assumes, no new trust boundary crossed |

</threat_model>

<verification>
Run `pnpm --filter claude-adapter test` (full suite) — all green, including the bypassPermissions
grep check and the deny-not-allow assertion.
</verification>

<success_criteria>
requestReview and requestHandoff are real, non-stub implementations firing on live signals.
ClaudeCodeRuntime implements the complete eight-method AgentRuntime with no remaining stubs.
</success_criteria>

<output>
Create `.planning/phases/04-agentruntime-claudecoderuntime/04-03-SUMMARY.md` when done.
</output>
