---
phase: 04-agentruntime-claudecoderuntime
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - packages/claude-adapter/package.json
  - packages/claude-adapter/src/claude-code-runtime.integration.test.ts
  - .planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md
autonomous: false
requirements: [RUNTIME-01, RUNTIME-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ClaudeCodeRuntime.startTask drives one real Claude Code session (via the real, authenticated claude CLI/SDK, no ANTHROPIC_API_KEY) against the real SyncSmith repository, scoped to a disposable git worktree on a throwaway branch created for this demo only (D-05)."
    - "The demo task is a real, currently-unplanned GSD workflow step from SyncSmith's actual roadmap, invoked as a genuine GSD slash-command prompt — not a trivial file-and-commit prompt (D-06)."
    - "Every event ClaudeCodeRuntime posts during the demo (task.status_changed, and ceo.approval_requested/agent.handoff_requested if triggered) parses successfully against CompanyEventSchema.safeParse, captured by a local stub HTTP server mirroring apps/worker/src/index.integration.test.ts's existing pattern."
    - "The demo reaches a real terminal AgentTaskStatus (completed, failed, blocked, or waiting_for_review — never a status invented outside the AgentTaskStatus enum) and the full received-event list plus the terminal status is written to 04-04-demo-evidence.md before the disposable worktree/branch is deleted (D-07)."
    - "After cleanup, `git -C F:/Sidegigs/syncsmith status --porcelain` and `git -C F:/Sidegigs/syncsmith worktree list` show SyncSmith's repository state unchanged from before the demo — no disposable worktree, no throwaway branch, no modification to main (D-07)."
  artifacts:
    - packages/claude-adapter/src/claude-code-runtime.integration.test.ts
    - .planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md
  key_links:
    - "packages/claude-adapter/src/claude-code-runtime.integration.test.ts -> real F:/Sidegigs/syncsmith repo (disposable worktree only) -> createClaudeCodeRuntime -> local stub control-plane server -> CompanyEventSchema validation -> 04-04-demo-evidence.md"
  prohibitions:
    - requirement_id: RUNTIME-02
      category: safety
      statement: "MUST NOT run any mutating git operation against SyncSmith's main branch or main worktree during the demo (merge/rebase/checkout/push onto main, or any operation targeting F:/Sidegigs/syncsmith itself rather than the disposable worktree path) — the demo worktree/branch must be fully isolated and removed afterward without ever touching main."
---

<objective>
Run the actual live proof that ClaudeCodeRuntime's startTask/getStatus/requestReview/requestHandoff
work end-to-end: a disposable git worktree against the real SyncSmith repository, a real
unplanned-phase GSD slash-command as the task prompt, real events captured and schema-validated,
evidence recorded, then full cleanup restoring SyncSmith to its pre-demo state.

Purpose: RUNTIME-02's "ClaudeCodeRuntime implements AgentRuntime... starts... a real Claude Code
task" claim is not satisfied by mocked unit tests alone (04-RESEARCH.md Validation Architecture:
"the unit suite alone cannot prove RUNTIME-02's 'no ANTHROPIC_API_KEY required' claim, only a real
run can"). D-05/D-06/D-07 specify exactly how that real run must be scoped and cleaned up.
Output: a gated integration test (`CLAUDE_CODE_INTEGRATION_TEST=1`) proving the full path, plus a
permanent evidence record of one real run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agentruntime-claudecoderuntime/04-CONTEXT.md
@.planning/phases/04-agentruntime-claudecoderuntime/04-RESEARCH.md
@packages/claude-adapter/src/claude-code-runtime.ts
@apps/worker/src/index.integration.test.ts
@packages/git-adapter/src/worktree.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pick the demo target and write the gated integration test's fixture setup</name>
  <precondition>F:/Sidegigs/syncsmith exists, `git -C F:/Sidegigs/syncsmith rev-parse --is-inside-work-tree` prints true, and `git -C F:/Sidegigs/syncsmith status --porcelain` is empty (clean working tree) before any disposable worktree is created against it.</precondition>
  <read_first>
    F:/Sidegigs/syncsmith/.planning/ROADMAP.md and F:/Sidegigs/syncsmith/.planning/STATE.md (read
    fresh at execution time — this plan was drafted when every SyncSmith phase showed "Plans: TBD";
    re-check this is still true, since state may have changed by execution time); PROJECT.md's
    Context section ("SyncSmith is a sibling repo at F:/Sidegigs/syncsmith already using GSD... no
    phases executed yet"); apps/worker/src/index.integration.test.ts (the lightweight local
    HTTP+WS stub-server pattern this task's fixture setup reuses verbatim — no real apps/api or
    Postgres dependency, per that file's own comment).
  </read_first>
  <files>packages/claude-adapter/package.json, packages/claude-adapter/src/claude-code-runtime.integration.test.ts</files>
  <action>
    Read F:/Sidegigs/syncsmith/.planning/ROADMAP.md's Phase Details fresh. Pick the lowest-numbered
    phase still showing "Plans: TBD" (currently Phase 1: Foundation & Self-Hosted Deployment, but
    re-verify at execution time — do not assume this plan's draft-time snapshot still holds). Check
    whether F:/Sidegigs/syncsmith/.planning/phases/<NN>-*/'s CONTEXT.md already exists for that
    phase: if it does not exist, the demo prompt is `/gsd-discuss-phase <N>` (context-gathering
    only — the smallest, fastest, lowest-risk genuine GSD workflow step, writes exactly one file);
    if CONTEXT.md exists but no PLAN.md, use `/gsd-plan-phase <N>` instead. Do not use
    `/gsd-execute-phase` for this demo unless a PLAN.md already exists — that is a larger, slower,
    higher-risk step than this demo needs to prove RUNTIME-02. Either choice is a real GSD
    slash-command used verbatim as `startTask`'s `prompt`, per D-02 — never a raw freeform string
    like "create a file and commit it". Record the exact chosen command and phase number in this
    plan's SUMMARY.md.

    Create packages/claude-adapter/src/claude-code-runtime.integration.test.ts. Wrap the entire
    `describe` block in `describe.skipIf(process.env.CLAUDE_CODE_INTEGRATION_TEST !== "1")(...)` so
    the default `pnpm --filter claude-adapter test` run reports it skipped, never failed, and never
    requires real Claude Code auth. In `beforeAll`, reuse apps/worker/src/index.integration.test.ts's
    exact local HTTP+WS stub-server pattern (lines 41-69: `createServer` capturing POSTed `/events`
    bodies into a `receivedEvents` array, `WebSocketServer` attached at `/ws`, `httpServer.listen(0,
    "127.0.0.1", ...)` for an OS-assigned port) — this test needs a real endpoint for `postEvent` to
    succeed against, not a mocked one, since this is the one test in the phase meant to prove the
    full real path. In `beforeEach`, create the disposable worktree: `execa("git", ["worktree",
    "add", "-b", <a unique throwaway branch name including a timestamp, e.g.
    \`pixelfirm-demo-${Date.now()}\`>, <a unique disposable path outside
    F:/Sidegigs/syncsmith's own directory tree, e.g. a mkdtemp'd sibling directory>], { cwd:
    "F:/Sidegigs/syncsmith" })` — using execa's array-args form (never a shell string), matching
    packages/git-adapter/src/commit.ts's existing injection-safety convention. Immediately after
    creation, call `listWorktrees("F:/Sidegigs/syncsmith")` and `isGitWorktree(<the new path>)` from
    `git-adapter` (already a reachable dependency — add `"git-adapter": "workspace:*"` to
    packages/claude-adapter/package.json's dependencies if not already present from an earlier
    plan) to assert the new worktree is real and distinct from SyncSmith's main worktree path before
    proceeding.

    Add `"execa": "^10.0.1"` and `"@types/node"` (whichever this monorepo already pins elsewhere,
    e.g. apps/worker's devDependencies) to packages/claude-adapter/package.json's devDependencies
    (the integration test file calls `execa` directly for `git worktree add`/`remove`/`branch -d`,
    since git-adapter only exports read-only inspection functions, not creation — there is no
    existing creation primitive in this codebase to reuse, per D-05's "or a new ad hoc mechanism"
    fallback). Add `"test:integration": "vitest run claude-code-runtime.integration"` to
    packages/claude-adapter/package.json's scripts, alongside the existing `"test": "vitest run"`
    (unchanged — `describe.skipIf` above is what keeps the default `test` script from attempting
    this file for real).
  </action>
  <verify>
    <automated>pnpm --filter claude-adapter typecheck</automated>
    <fails_when>non-zero exit (the new integration test file must compile against the real AgentRuntime/git-adapter types before it is ever run)</fails_when>
  </verify>
  <verify>
    <automated>grep -c "isGitWorktree" packages/claude-adapter/src/claude-code-runtime.integration.test.ts</automated>
    <fails_when>grep exits non-zero (file missing or the worktree-isolation assertion absent)</fails_when>
  </verify>
  <acceptance_criteria>
    The chosen phase number and GSD slash-command are recorded in this plan's SUMMARY.md.
    `listWorktrees("F:/Sidegigs/syncsmith")` after `beforeEach` includes both the main worktree and
    the new disposable one, at two distinct paths. `isGitWorktree(<disposable path>)` returns true.
  </acceptance_criteria>
  <done>The gated integration test file exists, is skipped by default (`pnpm --filter claude-adapter test` reports it as skipped, not failed), and its fixture setup creates a real, verified-isolated disposable worktree against SyncSmith.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the real ClaudeCodeRuntime task to completion, validate every posted event, write evidence</name>
  <read_first>
    packages/claude-adapter/src/claude-code-runtime.ts (the full eight-method AgentRuntime this task
    exercises for real, non-mocked, for the first time); packages/event-schema/src/payloads/index.ts
    (CompanyEventSchema, used here exactly as apps/worker/src/index.integration.test.ts's own test
    validates every received event, lines 110-113).
  </read_first>
  <files>packages/claude-adapter/src/claude-code-runtime.integration.test.ts, .planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md</files>
  <action>
    In the same integration test file, after the disposable worktree from Task 1 is created and
    verified, call `createClaudeCodeRuntime({ companyId: "demo-syncsmith", controlPlaneUrl:
    \`http://127.0.0.1:${port}\`, token: "demo-token" })` (the local stub server accepts any bearer
    token per apps/worker/src/index.integration.test.ts's own comment — a real auth gate is
    apps/api's already-tested concern, not re-tested here) and call `.startTask({ taskId:
    "demo-task-1", repoPath: "F:/Sidegigs/syncsmith", worktreePath: <the disposable worktree path
    from Task 1>, prompt: <the exact GSD slash-command chosen in Task 1> })`. Poll `.getStatus(
    "demo-task-1")` on a short interval (e.g. every 2000ms) until it returns a terminal status
    (completed, failed, blocked, or waiting_for_review) or a generous overall bound is hit (600
    seconds — set this test's own timeout to 600_000ms via the third argument to `it(...)`, matching
    a real GSD command's realistic duration, not the 10_000ms used by the synthetic-fixture
    integration test this pattern is borrowed from).

    Once a terminal status is reached, assert every entry in `receivedEvents` parses successfully via
    `CompanyEventSchema.safeParse(event).success === true` (mirroring
    apps/worker/src/index.integration.test.ts lines 110-113 exactly), and assert `receivedEvents`
    contains at least one `task.status_changed` event whose payload's `taskId` is "demo-task-1" and
    whose final recorded status matches what `.getStatus()` returned. Do not assert that
    `ceo.approval_requested`/`agent.handoff_requested` specifically appear — whether the chosen small
    GSD workflow step triggers a clarifying question or a role transition is genuinely
    content-dependent and must not be faked into passing.

    Before removing anything, write the full evidence record: `receivedEvents` (as formatted JSON),
    the final terminal status, the chosen phase number/slash-command, the disposable worktree
    path/branch name, and a timestamp, to
    `.planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md` via
    `fs.promises.writeFile`, resolving the path from `join(import.meta.dirname, "..", "..", "..",
    ".planning", "phases", "04-agentruntime-claudecoderuntime", "04-04-demo-evidence.md")` (three
    levels up from packages/claude-adapter/src to the repo root). This write must happen
    unconditionally in the test's `afterAll`/`finally` path — even if the terminal-status assertion
    above fails — because evidence-before-cleanup (D-07) matters most exactly when something went
    wrong, not only on a clean pass.

    Only after the evidence file is written, remove the disposable worktree and branch: `execa("git",
    ["worktree", "remove", <disposable path>], { cwd: "F:/Sidegigs/syncsmith" })` then `execa("git",
    ["branch", "-d", <throwaway branch name>], { cwd: "F:/Sidegigs/syncsmith" })` — plain `-d`, never
    `-D`/`--force`, per this plan's kept prohibition; if `git worktree remove` fails because the
    worktree has uncommitted changes, surface the error rather than forcing removal (never silently
    discard real evidence of what the demo actually did).
  </action>
  <verify>
    <automated>CLAUDE_CODE_INTEGRATION_TEST=1 pnpm --filter claude-adapter run test:integration</automated>
    <fails_when>non-zero exit, or any received event fails CompanyEventSchema.safeParse, or no task.status_changed event for "demo-task-1" appears in receivedEvents</fails_when>
  </verify>
  <acceptance_criteria>
    04-04-demo-evidence.md exists, is non-empty, and contains a terminal status string that is one of
    completed/failed/blocked/waiting_for_review/cancelled — never a status outside AgentTaskStatus.
    Every event in the evidence file's JSON dump independently re-parses against
    CompanyEventSchema.safeParse when checked by hand (spot-check at least 3 events if the run
    produced more than 3).
  </acceptance_criteria>
  <done>A real ClaudeCodeRuntime task ran to a real terminal status against the real SyncSmith repository, every posted event validated against the shared schema, and the full evidence record exists before any cleanup ran.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm the demo evidence and SyncSmith's restored clean state</name>
  <read_first>.planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md (Task 2's output — read it before answering this checkpoint).</read_first>
  <what-built>
    A real ClaudeCodeRuntime.startTask run against a disposable SyncSmith worktree, reaching the
    terminal status and event trail recorded in 04-04-demo-evidence.md. The disposable worktree and
    branch have already been removed by Task 2's own cleanup step.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md and confirm the
       terminal status and event trail look like real GSD workflow activity (real phase number, real
       file paths under F:/Sidegigs/syncsmith), not placeholder/fabricated content.
    2. Run `git -C F:/Sidegigs/syncsmith status --porcelain` — expect empty output (clean, same as
       before the demo).
    3. Run `git -C F:/Sidegigs/syncsmith worktree list` — expect exactly the main worktree, no
       leftover disposable worktree.
    4. Run `git -C F:/Sidegigs/syncsmith branch --list "pixelfirm-demo-*"` — expect no output (the
       throwaway branch was deleted).
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe what's wrong (e.g. a leftover worktree/branch, or evidence that looks fabricated) — do not approve a demo that left SyncSmith's repository in a modified state.</resume-signal>
  <acceptance_criteria>All four verification steps above pass and the human's approval is recorded in this plan's SUMMARY.md.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- Gated integration test proving the full real path — `packages/claude-adapter/src/claude-code-runtime.integration.test.ts`
- `"test:integration"` script on `packages/claude-adapter/package.json` (env-gated via `CLAUDE_CODE_INTEGRATION_TEST=1`, never part of the default `pnpm --filter claude-adapter test` run)
- Permanent evidence record of one real RUNTIME-02 demo run — `.planning/phases/04-agentruntime-claudecoderuntime/04-04-demo-evidence.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This demo's disposable worktree ↔ SyncSmith's real main worktree/branch | The one hard isolation boundary this entire plan exists to prove is never crossed |
| Real Claude Code session (inside the disposable worktree) ↔ the rest of the filesystem | The session runs with `permissionMode: "default"` and this machine's existing Claude Code trust level — not a new sandbox introduced by this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Destruction/Tampering | Disposable worktree/demo accidentally mutating or merging into SyncSmith's main branch/worktree | critical | mitigate | `git worktree add` targets a brand-new path, never SyncSmith's own directory; kept prohibition in this plan's `must_haves.prohibitions`; Task 3's checkpoint independently re-verifies `git status --porcelain`/`worktree list`/`branch --list` after cleanup, not just trusting Task 2's own cleanup step succeeded |
| T-04-11 | Repudiation | A failed or ambiguous demo run being silently discarded without evidence | high | mitigate | Task 2's evidence-write happens in `afterAll`/`finally`, unconditionally, before cleanup — a failing run still leaves a permanent record, per D-07 |
| T-04-12 | Elevation of Privilege | The real Claude Code session inside the demo worktree accessing files outside the worktree via absolute paths | medium | accept | Same trust level Claude Code already has on this machine outside this project; out of this phase's scope to sandbox further (matches Assumption A3's personal-use framing) |

</threat_model>

<verification>
Run `CLAUDE_CODE_INTEGRATION_TEST=1 pnpm --filter claude-adapter run test:integration` — passes,
producing 04-04-demo-evidence.md. Confirm Task 3's four manual checks against the real SyncSmith
repository all pass.
</verification>

<success_criteria>
A real Claude Code task, driven entirely through ClaudeCodeRuntime with no ANTHROPIC_API_KEY, ran
to a real terminal status against the real SyncSmith repository inside a disposable, fully-cleaned-up
worktree. Every posted event validated against the shared event schema. RUNTIME-01 and RUNTIME-02
are both now demonstrated, not just unit-tested.
</success_criteria>

<output>
Create `.planning/phases/04-agentruntime-claudecoderuntime/04-04-SUMMARY.md` when done.
</output>
