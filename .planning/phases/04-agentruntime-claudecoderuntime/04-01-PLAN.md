---
phase: 04-agentruntime-claudecoderuntime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/orchestration-adapter/package.json
  - packages/orchestration-adapter/tsconfig.json
  - packages/orchestration-adapter/src/types.ts
  - packages/orchestration-adapter/src/index.ts
  - packages/claude-adapter/package.json
  - packages/claude-adapter/tsconfig.json
  - packages/claude-adapter/src/event-emitter.ts
  - packages/claude-adapter/src/claude-code-runtime.ts
  - packages/claude-adapter/src/claude-code-runtime.test.ts
  - packages/claude-adapter/src/index.ts
  - packages/event-schema/src/payloads/index.ts
  - packages/company-core/src/reducer.ts
  - packages/company-core/src/reducer.test.ts
autonomous: false
requirements: [RUNTIME-01, RUNTIME-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "packages/orchestration-adapter/src/types.ts exports an AgentRuntime interface with all eight methods (startTask/pauseTask/resumeTask/cancelTask/getStatus/sendMessage/requestReview/requestHandoff), verified by an import-statement-scoped grep against packages/orchestration-adapter/src returning no matches (RUNTIME-01)."
    - "ClaudeCodeRuntime.startTask drives one real @anthropic-ai/claude-agent-sdk query() call authenticated via the existing `claude` CLI subscription login, with ANTHROPIC_API_KEY unset in the process env for the whole call (RUNTIME-02)."
    - "A startTask run that reaches a terminal SDKResultMessage posts a task.status_changed event (status: completed on subtype success, failed on subtype error) through packages/claude-adapter's own buildEnvelope/postEvent, and company-core's reducer folds it into tasks[taskId].status."
    - "AgentRuntime is confirmed the primary generic noun; ClaudeCodeRuntime is its first variant, not folded into a shared type — promote decision recorded per ARCHITECTURE.md Anti-Pattern 3 (zero Claude-Code-specific shape in the interface or in company-core)."
    - statement: "[FLAGGED ASSUMPTION — unresolved probe, unclassified] RUNTIME-01's 'independent of any specific coding-agent implementation' is asserted structurally only (zero @anthropic-ai/claude-agent-sdk import in packages/orchestration-adapter, confirmed via grep, never a behavioral proof against a second implementation) — the edge-probe classifier could not further decompose this claim. Review manually if RUNTIME-05 (v2, a second AgentRuntime implementation) later needs a stronger proof than a type/import check."
      verification: backstop
  artifacts:
    - packages/orchestration-adapter/src/types.ts
    - packages/orchestration-adapter/src/index.ts
    - packages/claude-adapter/src/claude-code-runtime.ts
    - packages/claude-adapter/src/event-emitter.ts
    - packages/claude-adapter/src/claude-code-runtime.test.ts
    - .planning/phases/04-agentruntime-claudecoderuntime/COVERAGE.md
  key_links:
    - "packages/claude-adapter/src/claude-code-runtime.ts implements the AgentRuntime type exported by packages/orchestration-adapter/src/types.ts"
    - "packages/claude-adapter/src/claude-code-runtime.ts -> packages/claude-adapter/src/event-emitter.ts (buildEnvelope/postEvent, local copy per this plan's Task 3 rationale) -> control plane POST /events"
    - "packages/event-schema's task.status_changed member -> packages/company-core/src/reducer.ts's new handler -> ProjectionState.tasks[taskId].status"
  prohibitions:
    - requirement_id: RUNTIME-01
      category: values
      statement: "MUST NOT let @anthropic-ai/claude-agent-sdk types/imports, or any Claude-Code-specific concept (session_id, permissionMode, tool names), leak into packages/orchestration-adapter's AgentRuntime interface — it must stay implementable by a future non-Claude-Code AgentRuntime without modification."
    - requirement_id: RUNTIME-02
      category: values
      statement: "MUST NOT silently fall back to ANTHROPIC_API_KEY if present in the environment — ClaudeCodeRuntime's query() calls must rely solely on the existing claude CLI subscription login, never read or forward an API key, so no hidden dual-billing path exists."
---

<objective>
Verify Claude MAX subscription billing posture before writing any ClaudeCodeRuntime code, then
scaffold `packages/orchestration-adapter` (the generic `AgentRuntime` interface) and
`packages/claude-adapter` (`ClaudeCodeRuntime`, its first and only implementation), wiring
`startTask`/`getStatus` through one real `@anthropic-ai/claude-agent-sdk` `query()` call to a real
`task.status_changed` event that folds into `company-core`'s projection.

Purpose: RUNTIME-01 requires a generic runtime interface independent of any coding-agent
implementation; RUNTIME-02 requires that interface's first real implementation to actually drive
Claude Code end-to-end using Claude MAX subscription auth only. 04-RESEARCH.md's Critical Finding
is unambiguous that the "no ANTHROPIC_API_KEY required" assumption needs a live falsification
check before any implementation is built on top of it — that check is this plan's first task.
Output: `packages/orchestration-adapter` (interface, zero SDK deps), `packages/claude-adapter`
(ClaudeCodeRuntime with real startTask/getStatus), a new `task.status_changed` event-schema member,
and a company-core reducer handler for it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-agentruntime-claudecoderuntime/04-CONTEXT.md
@.planning/phases/04-agentruntime-claudecoderuntime/04-RESEARCH.md
@.planning/phases/04-agentruntime-claudecoderuntime/04-PATTERNS.md
@packages/event-schema/src/envelope.ts
@packages/event-schema/src/payloads/index.ts
@apps/worker/src/event-emitter.ts
@apps/worker/src/poll-loop.ts
@apps/worker/src/poll-loop.test.ts
@packages/company-core/src/reducer.ts
@packages/company-core/src/projections.ts
@packages/gsd-adapter/package.json
@packages/git-adapter/package.json
@packages/git-adapter/tsconfig.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Claude MAX subscription billing posture (RUNTIME-02 falsification check)</name>
  <precondition>`claude auth status` reports `loggedIn: true` with `authMethod: "claude.ai"` (subscription session), not an API-key-only session.</precondition>
  <read_first>.planning/phases/04-agentruntime-claudecoderuntime/04-RESEARCH.md Critical Finding section ("Claude MAX subscription auth and billing posture for a third-party product"); .planning/STATE.md Blockers/Concerns ("re-verify Claude Max-subscription billing terms").</read_first>
  <action>
    Confirm ANTHROPIC_API_KEY is unset in the current shell (echo the variable, expect empty).
    Run `claude auth status` and confirm authMethod is claude.ai (subscription), not an API key.
    Run one real headless call with no ANTHROPIC_API_KEY set: `claude -p "Reply with exactly the
    word: pong" --output-format stream-json` and confirm it returns "pong" in a result message.
    This is Claude's automatable half of the check — everything above this line is done by Claude,
    not the human.
  </action>
  <what-built>
    ANTHROPIC_API_KEY confirmed unset; `claude auth status` confirmed subscription-based
    (authMethod: claude.ai); one real `claude -p` call completed successfully with no API key in
    the environment.
  </what-built>
  <how-to-verify>
    Open https://claude.ai (Max/Pro plan usage view) and https://platform.claude.com (API usage
    dashboard) side by side. Compare each dashboard's usage counter from just before the `claude -p`
    call above to just after. Report which dashboard's counter moved: the claude.ai subscription
    pool, or the platform.claude.com API-credit pool.
  </how-to-verify>
  <resume-signal>
    Type "subscription pool" if claude.ai's usage moved (expected — proceed to Task 2). Type
    "API pool" or describe what you saw if platform.claude.com's usage moved instead — this is a
    phase-blocking finding; do not approve, escalate to re-scope RUNTIME-02 before any further
    work in this plan.
  </resume-signal>
  <acceptance_criteria>The dashboard comparison result is recorded verbatim in this plan's SUMMARY.md before Task 3 begins.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify @anthropic-ai/claude-agent-sdk package legitimacy before install</name>
  <read_first>.planning/phases/04-agentruntime-claudecoderuntime/04-RESEARCH.md Package Legitimacy Audit table (flags @anthropic-ai/claude-agent-sdk as SUS/too-new, disposition: flagged, planner must insert a blocking human-verify checkpoint).</read_first>
  <action>
    Do not run `pnpm add` yet. Open https://www.npmjs.com/package/@anthropic-ai/claude-agent-sdk
    and confirm: publisher is the `anthropics` GitHub org (github.com/anthropics/claude-agent-sdk-typescript),
    weekly downloads are in the millions, and the latest version matches what `npm view
    @anthropic-ai/claude-agent-sdk version` reports locally (0.3.278 or newer, published 2026-09-19
    or later).
  </action>
  <what-built>Nothing yet — this checkpoint gates the install in Task 3.</what-built>
  <how-to-verify>
    Visit https://www.npmjs.com/package/@anthropic-ai/claude-agent-sdk and confirm the publisher
    org, repo link, and download count described above match. 04-RESEARCH.md's own assessment is
    that this is very likely a heuristic false positive (8.16M weekly downloads, official Anthropic
    org) — this checkpoint exists to have a human actually see that confirmation, not skip it.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install in Task 3, or describe what looked wrong.</resume-signal>
  <acceptance_criteria>Human confirmation recorded in this plan's SUMMARY.md before `pnpm add` runs in Task 3.</acceptance_criteria>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: Scaffold AgentRuntime interface + ClaudeCodeRuntime, wire startTask/getStatus to a real query() and a real task.status_changed event</name>
  <reversibility rating="costly">Once startTask/getStatus are built around query()'s async-generator message-streaming shape, switching the primary path to subprocess-based stdout parsing means rewriting the runtime's core I/O model, not swapping a client call (D-01, CONTEXT.md).</reversibility>
  <read_first>
    packages/gsd-adapter/package.json and packages/git-adapter/tsconfig.json (naming/scripts/tsconfig
    convention to replicate exactly); packages/event-schema/src/envelope.ts and
    packages/event-schema/src/payloads/index.ts (BaseEnvelope shape, discriminated-union additive
    pattern, "never .extend() chaining" comment at lines 53-58); apps/worker/src/event-emitter.ts
    (buildEnvelope/postEvent shape to replicate locally — see rationale in Action below);
    apps/worker/src/poll-loop.ts (closure-scoped state + never-crash-the-loop try/catch pattern) and
    apps/worker/src/poll-loop.test.ts lines 1-23 (vi.mock("./event-emitter.js", ...) convention to
    mirror for mocking the SDK's query export); packages/company-core/src/reducer.ts (handler
    dispatch-table shape, agent.handoff_requested's upsert-if-missing pattern at lines 90-104) and
    packages/company-core/src/projections.ts (TaskState shape).
  </read_first>
  <behavior>
    - Test 1: startTask({taskId, repoPath, worktreePath, prompt}) with a mocked query() async
      generator yielding a system/init message (session_id captured) then a result message
      (subtype: "success") resolves, and getStatus(taskId) returns "completed" afterward.
    - Test 2: the same run posts exactly two task.status_changed events via the mocked
      buildEnvelope/postEvent — one with status "starting" (emitted synchronously before the
      query() loop begins) and one with status "completed" (emitted on the terminal result).
    - Test 3: a mocked query() generator whose result message has subtype "error" causes
      getStatus(taskId) to resolve "failed", and the final posted event has status "failed", not
      "completed" — startTask must never report success on an error result.
    - Test 4: getStatus(taskId) for a taskId that was never started rejects/throws rather than
      returning a fabricated status (never invent state — this project's Core Value).
  </behavior>
  <files>
    packages/orchestration-adapter/package.json, packages/orchestration-adapter/tsconfig.json,
    packages/orchestration-adapter/src/types.ts, packages/orchestration-adapter/src/index.ts,
    packages/claude-adapter/package.json, packages/claude-adapter/tsconfig.json,
    packages/claude-adapter/src/event-emitter.ts, packages/claude-adapter/src/claude-code-runtime.ts,
    packages/claude-adapter/src/claude-code-runtime.test.ts, packages/claude-adapter/src/index.ts,
    packages/event-schema/src/payloads/index.ts, packages/company-core/src/reducer.ts,
    packages/company-core/src/reducer.test.ts
  </files>
  <action>
    Scaffold packages/orchestration-adapter: package.json name "orchestration-adapter", private
    true, type module, version 0.0.0, main "src/index.ts", scripts { "test": "vitest run",
    "typecheck": "tsc --noEmit" }, zero dependencies (no zod, no SDK — this is a plain-TS-interface
    contract package, mirroring event-schema's "thin dependency-free contract" role but with no
    runtime schema needed since AgentRuntime is a type contract, not wire data). tsconfig.json
    identical shape to packages/git-adapter/tsconfig.json (extends ../../tsconfig.json, outDir dist,
    include src).

    In packages/orchestration-adapter/src/types.ts define: `export type AgentTaskStatus = "starting"
    | "running" | "paused" | "blocked" | "waiting_for_review" | "waiting_for_handoff" | "completed"
    | "failed" | "cancelled"`; `export interface StartTaskInput { taskId: string; repoPath: string;
    worktreePath: string; prompt: string }`; `export interface AgentRuntime { startTask(input:
    StartTaskInput): Promise<void>; pauseTask(taskId: string): Promise<void>; resumeTask(taskId:
    string): Promise<void>; cancelTask(taskId: string): Promise<void>; getStatus(taskId: string):
    Promise<AgentTaskStatus>; sendMessage(taskId: string, message: string): Promise<void>;
    requestReview(taskId: string, reason: string): Promise<void>; requestHandoff(taskId: string,
    toAgentId: string): Promise<void>; }`. In src/index.ts re-export named symbols and types
    separately (`export type { AgentRuntime, AgentTaskStatus, StartTaskInput }`), matching
    gsd-adapter/src/index.ts's split-export convention. Zero import of
    `@anthropic-ai/claude-agent-sdk` anywhere in this package — this is the Anti-Pattern 3
    enforcement mechanism (ARCHITECTURE.md), made structural rather than a review rule.

    Scaffold packages/claude-adapter: package.json name "claude-adapter", same shape as
    packages/gsd-adapter/package.json, dependencies { "@anthropic-ai/claude-agent-sdk": "^0.3",
    "orchestration-adapter": "workspace:*", "event-schema": "workspace:*" }, scripts { "test":
    "vitest run", "typecheck": "tsc --noEmit" }. tsconfig.json identical shape to
    packages/git-adapter/tsconfig.json. Run `pnpm --filter claude-adapter add
    @anthropic-ai/claude-agent-sdk` only after Task 2's checkpoint is approved.

    In packages/claude-adapter/src/event-emitter.ts, write a small LOCAL copy of
    buildEnvelope/postEvent (do not import apps/worker/src/event-emitter.ts — packages must never
    depend on an app; that would be the first reverse-direction import in this monorepo). Export
    `buildEnvelope(companyId: string, type: string, payload: unknown, taskId: string, visibility:
    "PRIVATE" | "INTERNAL" | "STREAM_SAFE" | "PUBLIC" = "INTERNAL")` returning `{ id: randomUUID(),
    version: 1, occurredAt: new Date().toISOString(), companyId, taskId, visibility, type, payload
    }` (taskId is a required 4th param here, unlike apps/worker's optional one, since every event
    this package emits always has a real taskId) and `postEvent(controlPlaneUrl: string, token:
    string, event: unknown): Promise<void>` with the identical try/catch/log-never-throw body as
    apps/worker/src/event-emitter.ts's postEvent. This is a deliberate small duplication (~20 lines)
    rather than restructuring package boundaries mid-phase (ponytail: revisit only if a third
    adapter package needs the same pair — twitch-adapter/youtube-adapter are still future work per
    STACK.md, not built yet).

    In packages/event-schema/src/payloads/index.ts, add `const TaskStatusChangedPayload =
    z.object({ taskId: z.string(), status: z.enum(["starting", "running", "paused", "blocked",
    "waiting_for_review", "waiting_for_handoff", "completed", "failed", "cancelled"]) });` and
    append `z.object({ ...BaseEnvelope.shape, type: z.literal("task.status_changed"), payload:
    TaskStatusChangedPayload })` as discriminated-union member 16, after the existing 15, never
    reordered, never via `.extend()`.

    In packages/company-core/src/reducer.ts, add a `"task.status_changed"` handler to the
    `handlers` dispatch table: destructure `{ taskId, status }` from `event.payload`, upsert
    `state.tasks[taskId]` — `{ ...(state.tasks[taskId] ?? { id: taskId }), status }` — mirroring
    `agent.handoff_requested`'s upsert-if-missing pattern (lines 90-104), because this phase is the
    first real producer of task-lifecycle events and no upstream `task.created` producer exists yet
    for real Claude Code tasks; gating on "task must already exist" the way `review.started`/
    `ceo.approval_requested` do would make this event silently no-op for every real demo run. Add a
    corresponding reducer.test.ts case asserting `fold([taskStatusChangedEvent()])` produces
    `tasks[taskId].status === "completed"` for a status of "completed" on a fresh empty state.

    In packages/claude-adapter/src/claude-code-runtime.ts, write `export function
    createClaudeCodeRuntime(options: { companyId: string; controlPlaneUrl: string; token: string
    }): AgentRuntime` — a factory function (not a class; no classes exist anywhere in
    git-adapter/gsd-adapter/poll-loop.ts, matching this codebase's function-first style) closing
    over a `const tasks = new Map<string, { sessionId?: string; status: AgentTaskStatus }>()`.
    Implement `startTask`: set `tasks.set(input.taskId, { status: "starting" })`, immediately
    `postEvent(options.controlPlaneUrl, options.token, buildEnvelope(options.companyId,
    "task.status_changed", { taskId: input.taskId, status: "starting" }, input.taskId))` (fire the
    "starting" event before the query() loop begins, not after), then call `query({ prompt:
    input.prompt, options: { cwd: input.worktreePath, permissionMode: "default" } })` from
    `@anthropic-ai/claude-agent-sdk` and `for await` over its messages: on `message.type ===
    "system" && message.subtype === "init"` capture `message.session_id` into the task's map entry;
    on `message.type === "result"`, set status to "completed" if `message.subtype === "success"`
    else "failed", update the map, and post the corresponding task.status_changed event. Wrap the
    for-await body's error handling the same "log and continue, never crash" way
    apps/worker/src/poll-loop.ts does (lines 92-96) for any non-terminal stream error.
    `getStatus(taskId)` reads `tasks.get(taskId)?.status`; if the taskId is unknown, throw `new
    Error("ClaudeCodeRuntime.getStatus: unknown taskId")` rather than returning a fabricated status
    (Core Value: never invent state). Implement `pauseTask`/`resumeTask`/`cancelTask`/`sendMessage`/
    `requestReview`/`requestHandoff` as `Promise<void>` stubs that throw `new Error("not implemented
    — see Plan 04-02/04-03")` for now (Plan 04-02 and 04-03 implement these against the same map);
    this keeps `createClaudeCodeRuntime` satisfying the full `AgentRuntime` type from Task 1
    onward, so `orchestration-adapter typecheck` and any caller typed against `AgentRuntime` compile
    against a complete interface immediately, per RUNTIME-01's "exists independent of any specific
    coding-agent implementation" claim.

    In packages/claude-adapter/src/claude-code-runtime.test.ts, mock
    `@anthropic-ai/claude-agent-sdk`'s `query` export via `vi.mock("@anthropic-ai/claude-agent-sdk",
    () => ({ query: vi.fn() }))` and mock `./event-emitter.js` via the exact
    `vi.mock("./event-emitter.js", () => ({ buildEnvelope: vi.fn(...), postEvent: vi.fn(async () =>
    {}) }))` shape from apps/worker/src/poll-loop.test.ts lines 7-10, adapted for the taskId-required
    4-arg buildEnvelope signature. Write an `async function* fakeQuery(messages)` helper yielding a
    fixed array of fake `SDKMessage`-shaped objects for each of the four Behavior cases above.
  </action>
  <verify>
    <automated>pnpm --filter claude-adapter test -- claude-code-runtime</automated>
    <fails_when>non-zero exit, or the summary line does not contain "4 passed" for the four Behavior-block cases</fails_when>
  </verify>
  <verify>
    <automated>pnpm --filter orchestration-adapter typecheck</automated>
    <fails_when>non-zero exit (a TypeScript compile error means the interface itself doesn't type-check standalone)</fails_when>
  </verify>
  <verify>
    <automated>pnpm --filter company-core test -- reducer</automated>
    <fails_when>non-zero exit, or the summary line does not include the new task.status_changed test case in the passed count</fails_when>
  </verify>
  <acceptance_criteria>
    `grep -rEl "from ['\"]@anthropic-ai/" packages/orchestration-adapter/src` returns no matches
    (empty output, exit 1 from grep -l with no match is expected and correct here — this checks for
    an actual import statement, not merely the package name appearing somewhere, so a comment
    explaining why the package is absent does not trip this check). `packages/claude-adapter/package.json`'s
    dependencies include exactly one new production dependency in the `@anthropic-ai/` scope.
    All four Behavior-block test cases pass. `packages/company-core/src/reducer.test.ts` includes a
    passing case for `task.status_changed`.
  </acceptance_criteria>
  <done>
    ClaudeCodeRuntime.startTask drives one real query()-shaped message stream (mocked in tests, real
    in Task 1's manual `claude -p` proof) to a task.status_changed(completed) event that
    company-core folds into ProjectionState.tasks[taskId].status; the full AgentRuntime interface
    type-checks with zero SDK import in orchestration-adapter.
  </done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `AgentRuntime` interface, `AgentTaskStatus` type, `StartTaskInput` type — `packages/orchestration-adapter/src/types.ts`
- `createClaudeCodeRuntime(options)` factory returning an `AgentRuntime` — `packages/claude-adapter/src/claude-code-runtime.ts`
- Local `buildEnvelope`/`postEvent` pair scoped to `packages/claude-adapter` — `packages/claude-adapter/src/event-emitter.ts`
- New discriminated-union member `task.status_changed` (payload `{taskId, status}`) — `packages/event-schema/src/payloads/index.ts`
- New `company-core` reducer handler for `task.status_changed` (upsert-if-missing `tasks[taskId].status`) — `packages/company-core/src/reducer.ts`
- `packages/orchestration-adapter` and `packages/claude-adapter` as new pnpm workspace packages (auto-discovered via existing `pnpm-workspace.yaml`'s `packages/*` glob, no workspace-file edit needed)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker process ↔ real Claude Code session (`query()`) | Untrusted model/tool output stream crosses into ClaudeCodeRuntime's message-handling loop |
| npm registry ↔ this monorepo | New supply-chain dependency (`@anthropic-ai/claude-agent-sdk`) added for the first time this phase |
| ClaudeCodeRuntime ↔ `~/.claude/` OAuth credential store | ClaudeCodeRuntime must never read/log/forward this credential — it is the SDK's own concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | `pnpm add @anthropic-ai/claude-agent-sdk` install | high | mitigate | Package-legitimacy gate (Task 2, `gate="blocking-human"`) before the install runs, per 04-RESEARCH.md's Package Legitimacy Audit SUS flag |
| T-04-01 | Tampering | `packages/claude-adapter/src/event-emitter.ts`'s postEvent path | medium | mitigate | Reuses the exact validated-at-ingestion shape every prior phase's events pass through (`CompanyEventSchema`, plain `z.object()` payloads, never `z.looseObject()`) — no new validation surface introduced |
| T-04-02 | Information Disclosure | ClaudeCodeRuntime's `query()` invocation | high | mitigate | ClaudeCodeRuntime never reads, logs, or transmits the `~/.claude/` OAuth credential — it only ever calls `query()` and lets the SDK's own default credential resolution handle auth (V6 Cryptography, RESEARCH.md Security Domain) |
| T-04-03 | Denial of Service | ClaudeCodeRuntime's `getStatus` on an unknown taskId | low | accept | Throws rather than silently returning a default status — an accepted, intentional fail-loud behavior, not a gap |

</threat_model>

<verification>
Run `pnpm --filter claude-adapter test`, `pnpm --filter orchestration-adapter typecheck`, and
`pnpm --filter company-core test` — all green. Re-confirm Task 1's manual `claude -p` call
succeeded with ANTHROPIC_API_KEY unset before considering this plan done.
</verification>

<success_criteria>
`AgentRuntime` interface exists, type-checks standalone with zero SDK dependency.
`ClaudeCodeRuntime.startTask`/`getStatus` drive one real `query()` call to a real `task.status_changed`
event that folds into `company-core`'s `ProjectionState`. Both checkpoints resolved with recorded
human confirmation.
</success_criteria>

<output>
Create `.planning/phases/04-agentruntime-claudecoderuntime/04-01-SUMMARY.md` when done.
</output>
