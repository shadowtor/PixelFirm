// RED-phase stub: unimplemented. No dedicated automated test file gates this
// function this task (per PLAN.md's <files> list) — it's proven via the
// acceptance-criteria manual invocation during GREEN.
export async function isAnyClaudeProcessAlive(): Promise<boolean> {
  throw new Error("not implemented");
}
