// RED stub: 06-14 Task 2 implements these.
export const waitedLabel = (_requestedAt: string, _now: number): string => "";
export const isLongWait = (_requestedAt: string, _now: number): boolean => false;
export const kindLabel = (_kind: string | undefined, _reason: string): string => "";
export const documentTitle = (_count: number): string => "";
export const nextSelection = (_previous: string[], _removed: string, _current: string[]): string | null => "";
